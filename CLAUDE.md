@AGENTS.md

# 読むもの（必要なときだけ開く）

- `docs/project_knowledge.md` … **現行仕様の正**。設計・データモデル・守るべき約束。
- `handoff_current.md` … いまどこにいるか。直近完了・次の一手・重要パス（200行以内）。
- ★★★`docs/home-spec.md` … **ホームの実装仕様の正**（2026-09-06 確定）。
  配色・寸法・帯の動き・山の形・段取り・検証まで、これだけ読めば実装に入れる。
  ★実装セッションへ渡すプロンプトは `docs/home-implementation-prompt.md`。
- ★`docs/redesign-2026-09.md` … **改修の検討の記録**（確定／保留／却下）。
  ★`docs/redesign-references.md` … **参照のリンクと分析**。サイトのURLはここから開く。
  ★★`docs/redesign-ideas.md` … **アイデアの待ち行列**。上から順に絵にして見せる。
- `docs/archive/*.md` … 経緯と教訓。**全文を読まない。`grep` で必要な節だけ**。
  コード内コメントの `HANDOFF §N` はここの節番号を指す。§5・§7.x=`ui-binder`、
  §8.x=`brief-pipeline`、§9〜§31・§50・§51・§56・§65〜§67=`shell-redesign`、
  §38〜§49=`journal-voice`、§52・§53=`task-app`。
- `design.md` … **UI の規約の正**。文字・余白・配色・動きの表と、機械チェック13本。
- `SYSTEM-DESIGN.md` … 生成パイプラインの設計思想。
- `COWORK-ROUTINES.md` … Cowork に渡すプロンプト（アプリ側は読まない）。

# AIへの絶対ルール（最優先・厳守）

## ★★★0. UI に触るなら `design.md` を必ず開く（セッションをまたいで厳守）

**コンポーネント・CSS・トークンのどれかに1行でも触れる前に `design.md` を読むこと。**
記憶や過去の会話に頼らない。セッションが切れても、この規約は生き続ける。

作業を**終える前に**、`design.md` §5 の**機械チェック13本を必ず走らせる**。
**#1〜#7・#10〜#13 は 0 件**でなければ終わってはいけない。
0 にできない値があるなら、それは**目盛りの外**（`design.md` §6）なので、
**その行に `// ★目盛りの外（理由）` を書く**。**印の無い例外は例外ではない。**

★守るべきことの要点（詳細と数値はすべて `design.md`）:
1. 生の数字を書かない … 余白 `SPACE` / 文字 `TYPE` / 行間 `LEAD` / 字間 `TRACK` /
   太さ `WEIGHT` / 角丸 `RADIUS`（`lib/tokens.ts`）、色は `lib/constants.ts`。
2. **左右のパディングはページ最上位の器だけが持つ**（入れ子で増幅させない）。
3. `fontSize` と `fontWeight` は必ず同時に書く。和文に欧文の字間を当てない。
4. 動きは曲線4本・時間の4分割から引く。**押下だけが非対称**（即座に沈み、ゆっくり戻る）。
5. **見た目が変わる変更は、事前に文面を提示して承認を得てから**行う。
6. 検証は**移行前後の computed style を突き合わせ、「比較できた件数」を必ず出す**
   （件数を出さない検証は、検証していないのと区別がつかない）。

## 1. 漠然とした指示の受け方

ユーザーは**非エンジニア**であり、ファイル名やディレクトリ構造を把握していない。
漠然とした指示（例:「タスクのカードがおかしい」「設定画面を直して」）を受けたら:

1. **いきなりプロジェクト全体を検索しない**（grep・フルスキャン・広範な読み込み禁止）。
2. **まず下の「ファイル地図」で当たりをつける**。
3. 仮説に基づき、**必要最小限のファイルだけを開いてから**作業を始める。
4. 仮説が外れたと分かった時点で初めて、対象を絞った検索に切り替える。

# コミュニケーションスタイル

- 前置き・言い訳・過剰な相槌を挟まない。「〜だと思われます」より「原因は〜」。
- バグは CSS の微調整で誤魔化さず、**根本原因を特定してから**直す。対症療法は即座に見抜かれる。
- 実装後は必ず動作確認する（Playwright でのスクリーンショット／数値確認が定石）。
- 技術的な判断の理由（トレードオフ）は書く。ユーザーは「なぜその設計か」を尋ね返す。
- ユーザーは実機 iPhone で最終確認する。Chromium で問題なくても実機で直っていないことがある。
- **デザイン・文言の変更、AI のプロンプトの変更は、事前に文面を提示して承認を得てから**行う。
- Tailwind 不使用（すべてインライン style）。コミットメッセージは日本語で「何をなぜ」。
- **欧文は Archivo**（可変フォント。幅は `body` の `font-variation-settings: "wdth" 88`、
  太さは使う場所で指定）、**和文は Noto Sans JP**。`SANS` / `LATIN`（旧 `HELV`）は
  `lib/constants.ts`。★`app/layout.tsx` で `weight` を固定で渡さないこと。

# ファイル地図

## 骨格
- `components/AppShell.tsx` — 3アプリの横スライド・タブ・共有state・ダッシュボードの司令塔。
  ★★★**残りのアプリの先読みは「落ち着いてから、1つずつ」**（第115巡）。
  `requestIdleCallback(..., {timeout})` で**まとめて**載せていたので、**ホームの山が
  落ちている最中に必ず割り込み**、全文書のレイアウトが1回 400ms 超になっていた
  （実測 348/516 個）。**`MIN_MS` を過ぎて、軽いフレームが続いてから、1つずつ。**
  ★指が先に触れたら `mountApp` が即座に載せるので、待ちは体感に出ない。
- `components/AppBackdrop.tsx` — アプリごとの地色（`groundOf` が唯一の出どころ）。
- `components/Dashboard.tsx` — 下から引き上げる引き出し。「今日を終える」。
- `components/CreateMenu.tsx` — タブバー右端の丸から広がる輪
  （RECORD / TASKS / SETTING。**文字だけ・半径の線上**）。
  **タスクの追加も設定もどのアプリからでもここから**（第68巡に歯車を廃止）。
- `lib/apps.ts` — アプリとタブの定義（**タブ構成の正**）。

## ブリーフ（EXPLORE）
- `components/tabs/BriefTab.tsx` — カードのデッキ（右=KEEP / 左=SKIP）。
  ★★★**札の面は「面＝EXPLORE のメイン1色／形＝ドメイン」**（第94巡）。
  写真を `lib/cardShape.ts` の形で**マスク**し、右下の**黒い円**（44px ＋
  lucide の `ExternalLink`）が出典へ飛ぶ。★★**ジャンルの文字は置かない**
  （形が言うことを字で二度言わない）。★★**綴じ穴は提案カードから外した**
  （育成カード `checkin`/`milestone` には残っている）。
  ★★★**切るのは写真だけ。札には掛けない** ―― 掛けると `box-shadow` が消え、
  黄とクリームの比 1.27 では縁が1本も出ない。
  ★★★**写真の器は正方形**（`aspectRatio: "1 / 1"`）。**文の塊のほうが余りを取る**。
  ★★**ベゼルは `SPACE.xl`・札の幅は 358**（第98巡。参照どおりの 9.4% では
  画面に対して写真が小さく見えた）。器は **310 の正方形**。判も同じ値。
  ★★**題は2行・本文は2行で頭打ち**（題を野放しにすると本文を押し出す）。
  ★★**下の列は 44px の円3つ**（旗＝輪郭／記事を読む・出典＝塗り）を**左へ寄せる**。
  ★★★**札の比は `BRIEF_CARD_ASPECT`。`ITEM_CARD_ASPECT` とは別物**
  ―― あちらを変えるとストック・ゴールが動くのに、**札は 1px も動かない**。
  ★★**操作（旗・記事を読む・黒い円）は下の1列**に集め、**高さは `LINK_D` で固定**
  （`isTop` で中身が増減しても本文の折り返しが動かないため）。
- `components/tabs/StockTab.tsx` — 候補の一覧・追加シート・ウィッシュ一覧。
- `components/tabs/ExecuteTab.tsx` — 地図（Leaflet）・プラン生成・4ドメインの棚。
- `components/tabs/GoalsTab.tsx` — ゴールのバインダーとチェックイン。
- `components/tabs/ProfileTab.tsx` — 設定（好み・情報源・サインアウト・開発用の実験）。
  入口は**右下の輪の SETTING だけ**（`CreateMenu`）。
- ★`components/tabs/DevStageTab.tsx` — **開発用の `DEV` タブ**（第70巡）。中身は
  「**券**」＝版面の見本帳と「**札**」＝BRIEF の札の4ドメイン（第94巡）と
  「**場**」＝券と鋏の3D。**実機で見るためだけ**に在る。
  ★★「札」は**本番の `CardFace`（`BriefTab` から export）をそのまま**並べる
  ―― 見本用に別実装を作ると、形の割り当てが2か所になる。
  ★★★**タブバーの高さは `navHeightPx()` から引く。自分で測らない**（`.app-nav` の
  矩形は `NAV_H` と一致しない ―― Chromium 81px／実機 132px。第71巡に踏んだ）。
  ★★Explore の刷新が本物になったら、このファイル／`components/explore/samples/`／
  `lib/apps.ts` の `life-dev`／`lib/types.ts` の `LifeTabId`／
  `components/TabIcons.tsx` の `ticket` を**まとめて消す**。
- ★`components/explore/samples/` — **券の見本帳**（**4案**）。
  P1 天地／P2 逆天地／P3 **全面（この案だけ紙が色）**／P4 活字
  ＋ `TicketParts.tsx` ＋ `index.ts`。
  ★★★**写真はデュオトーン**（第81巡）。SVG のフィルタ1本で「影の色 → 紙」の帯へ
  **写す**。★**混色ではできない**（切り捨て／非線形になる）。詳細は `design.md` §3-d。
  ★★★**大きな英語は版面の幅に合わせて組む**（`FitLine`。段から選ばない＝目盛りの外）。
  `wdth` 70 で絞り、**書体を増やさずに2つ目の声**を作っている。
  ★★括弧がラベルを消す … `(12.02 – 03.16)` ／ `WITH 会場 (地名)`。
  ★★色が出るのは**大きな英語**と**写真のデュオトーン**と**鋏痕**だけ。
  ★★振っている軸は1つ ―― **大きな英語と写真がどう出会うか**。
  ★★見本帳は**案ごとに違うドメイン**で並ぶ（`DevStageTab` の `SAMPLES` 4枚）。
  ★★★**切り欠きの安全域は `SAFE`**（＝幅の 14%）。`%` の縦余白は**幅**で解決される
  ので、切り欠きの半径と正確に一致する。**避けるのは文字だけ。写真は噛ませる。**
  ★★★**マスクした要素に `box-shadow` は出ない**（影ごと切り抜かれる）。
  券の影は親の **`filter: drop-shadow`**（`TICKET_SHADOW`）。
  ★★**役と段は1対1**（題 `head` 20 ／ 会期・会場 `body` 13 ／ 要約 `small` 11 ／
  印 `nano` 7 の**4段だけ**）。**写真が余りを全部取る**ので spacer を置かない。
  ★★紙の形は**角丸 0 ＋ 上下の縁のギザギザ ＋ 四隅の大きな円の切り欠き**
  （`scallopMask`。層6枚を `mask-composite: intersect`。使えない環境では
  **ひとりでに素の矩形へ戻る**）。★★★**層を小さな箱に `no-repeat` で置かないこと**
  ―― 箱の外は α が 0 になり、`intersect` が**券を丸ごと消す**（第78巡に消した）。
  ★★**間隔ではなく「数」で持つ**（`calc(100% / N)`。px だと右端が切れる）。
  比は `TICKET_ASPECT`＝**3/4**、数で要るときは `TICKET_H_PER_W`。
  ★★**選ばれた1案を `Ticket.tsx` へ畳んだら、ディレクトリごと消す。**
- ★**刷新中の部品**（`/dev/explore` と `DEV` タブにしか無い。4タブは手つかず）…
  `components/explore/Ticket.tsx`（券）/ `PunchMark.tsx`（鋏痕）/ `lib/ticket.ts`、
  改札鋏（★**three.js／WebGL**。第17巡に SVG の自前投影をやめた）…
  `components/explore/Nipper.tsx`（画角・器・動き）／`NipperViews.tsx`（三面図。開発用）
  ＋ `lib/nipperMesh.ts`（**立体の作り方**。面取り・厚み・針金・段の当て方）
  ＋ `lib/nipperRig.ts`（**組み立てと光**。本番と三面図が同じものを見るための1か所）。
  ★★**鋏の形は `lib/nipperShape.ts` が正**。ただし**2段の生成物で、手で直さない**
  ―― 平面図4枚 → `tools/trace-nipper.mjs` → `lib/nipperShapeRaw.ts`（**生**のトレース）
  → `tools/clean-nipper.mts` → `lib/nipperShape.ts`（**整えた**形。立体はこれを読む）。
  パスの語彙（型と `L`/`A`/`piece`）は `lib/nipperPath.ts`（唯一の手書き）。
  **形を直すときは図を描き直すか、整理の規則の目盛りを直して走らせ直す。**
  元データは `docs/archive/nipper-shape-21.ts` に凍結（誰も import しない。消さない）。
  目で数値を打ち込んでいた 9〜15巡目はプロポーションが合わなかった。
  設計の正は `docs/explore-redesign.md`。
  ★★券と鋏を**同じ3D空間**に置く器 … `components/explore/TicketStage.tsx`
  （**券の面は「深さだけ書く板」**なので、下に敷いた DOM の券がそのまま見える
  ＝版面は劣化しないのに、鋏が券の奥へ回れば隠れる）。
  ★禁じられているのは **CSS の 3D 変形**であって WebGL ではない（design.md 冒頭）。

## タスク（TASK）
**タスク図形は常に GRAVITY 空間にだけ在る**（第52巡に TOP/UNDER の4層を破棄）。
別画面へ遷移せず、スワイプで**GRAVITY の物理法則を一時的に変える**ことで詳細リスト
（ALIGN）と俯瞰（TIMELINE）を見せる。タブは DRIFT（候補）＋ GRAVITY の2つで、
**GRAVITY＝地上／DRIFT＝上空**を**2Dのカメラ**が縦に送る（第62巡）。
- `components/tabs/GravityTab.tsx` — **タスクの本体**。matter.js の山（pile）と、
  **3つの物理モード**（`pile` / `align` / `timeline`）。左端→右スワイプで ALIGN
  （**左に円弧**で図形が並び、右に文字。中央が大きく、上下スワイプで回る）、
  **上**スワイプで TIMELINE（**地面から巨大な曜日が指に連れて伸び**、図形が
  日付レーンへ下から詰まる）、**下**スワイプで**効果線を伴って DRIFT へ**
  （DRIFT からは**上**スワイプで戻る）。★山は**表に出るたびに落とし直す**（毎回ちがう並び）。
  その日の**日付と曜日の英語**も、枠の無い黒い文字の板として一緒に落ちる。
  ★★モード中は **body の位置で描かない** — レイアウトが決めた**スロットへ絵の中心を
  置く**（`ox/oy` 補正をやめたのがズレの根治。詳しくは `docs/project_knowledge.md` §4）。
  ★山では**長押しで図形を掴んで運べる**（すぐ動かすとスワイプ）。口=完了/ゴミ箱=削除。
- `components/tasks/TaskSpace.tsx` — **カメラ**。**GRAVITY＝地上／DRIFT＝上空**を
  縦に積み、器ごと `translateY` で送る（`--cam`）。効果線は**パンの半ば**だけ。
  画面に固定した `Masthead`（TASK）もここ。
- `components/tasks/LayerName.tsx` — 層の名前（GRAVITY / ALIGN / TIMELINE / DRIFT）。
- `components/tasks/DropTargets.tsx` — **口とブラックホール**（掴んでいる間だけ
  「作る」の丸から分離して出る）。DRIFT と GRAVITY の共通部品。当たり判定 `targetAt`、
  **近さ** `aimTargets`、合図 `fireTarget` もここ。
- `components/tabs/DriftTab.tsx` — 候補が**無重力で漂う**層（1枚の canvas＋matter.js）。
  ホールドで図形を運び、右下から出る**口＝完了 / ゴミ箱＝削除**へ落とす（第44巡）。
  上スワイプで**カメラが地上の GRAVITY へ**。★★**場は自分の寸法だけで決める**
  （画面の座標を測らない。列が動くとずれる。第61巡に根治）。
- `components/tasks/` — `TaskComposer`（入力画面。ツールバー＋ポップオーバー）/
  `WhenSheet`（日程。**ここだけキーボードを閉じる**。★★盤は `components/DateGrid.tsx`）/
  `ComposerToolbar` /
  `ComposerFields`（重要度・テキスト。★第93巡にタグの列を廃止）/ `Popover` /
  `TimeRange`（時刻。タイムラインで範囲を選ぶ）/
  `SolidCanvas` /
  `ViewportProbe`（★開発用の数値表示。`lib/debugViewport.ts` と対。直ったら撤去）。
  タスクの追加は`CreateMenu`（タブバー右端の輪）からのみ（第36巡に＋を撤去）。
  `TaskAddButton.tsx`は動作確認用の`DemoSeedButton`だけが残っている。
- `lib/solid.ts` `lib/solidPaint.ts` `lib/textFit.ts` `lib/taskSize.ts` — 図形・描画・寸法。
  ★★★**形は「ピルの積み」**（第99巡。**第115巡の S 字の折りたたみは第116巡に
  ユーザー指定で撤回**「やっぱり、図形の形はピルが積み上がったような形に戻して」）。
  幅の等しいピル（角丸＝段の高さ/2）を隙間なく縦に積む。**左右対称**なので
  `halfWidthAtStack` は `stackEdge` の値そのもの。
  ★★★**`foldsAt`／`sideFolds`／「2段ぶんを1つの弧が覆う」は削除した。復活させない。**
  ★★★**ホームの大きさは「段の高さ」1つ**（第116巡。`lib/taskSize.ts` の **`rowSpecOf`**）。
  `h = 段の数` ／ `w = rowAspect` ―― **`unit` の意味が「段の高さ（px）」に変わった**。
  どの図形でも**段の厚みも字の大きさも同じ**（実測 幅 2.25倍 → 1.00倍・件数 9）。
  ★★**幅だけは題の長さで伸びる**（固定にすると字が縮むか `…` で消える）。
  ★★**提案の円 `OFFER_D`(3.2)・カセット `CASSETTE_ROWS`(2.2) も段の高さの倍数**。
  ★★★**`specOf` は 1 行も変えない** ―― TASK（GRAVITY）は重要度で大きさが変わるまま。
  ★★**点の数も y の並びも変えていない**ので `waist` はそのまま効く。
  ★★★**`stackOutline(rows, ar, waist)` の `waist` が「ピル ⇄ 図形」の変形**（第102巡）。
  0 ＝ 1段（帯のピルそのもの）／1 ＝ n 段。**半幅を lerp するだけなので点の数が変わらず、
  再標本化が要らない**。★`waist = 1` は既存の呼び手と同じ点の列を返す（実測 差 3.5e-9）。
  ★**段の数は題の文字数**（`rowsOf`。最大3段）。
  ★★★**「辺の数（1〜4）＝埋まり具合」はやめた。復活させない**（円・半円・三角・
  四角の語彙ごと削除）。`sides` は `isDated` のためだけに残っている。
  ★★★**当たり判定の光線は「正規化した空間」で飛ばす**（第108巡。`bodyFromOutline`）
  ―― px の空間だと**横長の形は大半の光線が長い辺に当たって凸包に捨てられ**、
  丸い端に1〜2点しか残らない（実測 … 180×60 のピルが**6頂点**・体が絵より
  **10% 小さい**）。正規化すれば**形だけで決まり、大きさに依らない**
  （体÷絵 0.90〜1.02 → **0.96〜1.02**、頂点 6 → 16）。
  ★★★**`GravityTab` の「順番どおりの間引き」を借りないこと** ―― **重心が
  最大 2.87px ずれる**（あちらは `ox/oy` を別に持ち回っているが、ホームは
  `body.position` を絵の中心として描く）。
  ★★★**当たり判定の多角形の費用は「軸の本数 × 頂点数」**（第105巡）。
  `PHYS_VERTS`(12) は**等角に測り直す光線の本数**で、★**偶数**であること
  （左右の対称性を式に保証させる）。★★**第104巡の「左右へ折り返す」は細かい辺を
  大量に生んで最大 16倍にし、着地でフレームが落ちた。復活させない。**
  ★★★**`Bodies.fromVertices` へ渡す前に `atan2` で並べること** ―― 並べ忘れると
  多角形が**自己交差**し、**SAT が「当たっていない」と答えて床をすり抜ける**
  （`world` にも `detector` にも居るのに `pairs: 0`）。
  ★★★**段の数は純粋な関数（`rowsOf`）から引く。行数の実測から引かない** ――
  実測だと ALIGN と山で形が変わり、書体の到着で**絵だけ段数が変わって物理とずれる**。
  ★★**ホームの山も同じ `stackOutline` を読む**（形を2度書かない）。
  ★★★**塗り分けは「日付があるか／ないか」の1軸だけ**（第93巡にタグを全廃）。
  **日付あり＝メインカラーの塗り／日付なし＝輪郭線**（中は地と同じ色・縁と字は
  メインカラー）。**ホームの帯のピルとまったく同じ見え方**を全部に通してある。
  ★★合図は **`isDated(spec)`**（`spec.sides.includes("due")`。`sidesOf()` が
  埋まっている側面だけを返すので、**日付の有無は最初から図形の仕様の中に在る**）。
  ★★★**`paintKey` に日付の有無と地の色を入れること** ―― `sides` は数しか
  見ていないので、入れないと**形が同じで塗りが逆**の絵が入れ替わる。
  ★★**地の色は呼ぶ側が渡す**（`SolidPaint.ground`）。入力画面だけ地が墨。
  ★★**線は内側に引く**（道を `EDGE/2` 内側へ寄せて `EDGE` で引く）。
  ★★**書体は1つ**（`SHAPE_FACE`）。**書体を分類の道具にしない。**

## ジャーナル（JOURNAL）
- `components/VoiceStudio.tsx` — 録音・トリミングのダイヤル・物理キー（録音UIはここ1つ）。
  ★★★**見えていない間は寸法を測らない**（第115巡）―― タブは全部載ったままなので、
  ホームを見ている間も `ResizeObserver` が鳴り、`clientWidth` が**全文書のレイアウトを
  強制**していた（実測 **524ms**・全体の 12.8%）。`appActive` で観測を止める。
- `components/tabs/RecordTab.tsx` / `JournalTab.tsx` — レコード / 今日・アーカイブ。
  ★★★**録音の画面は「カセット」そのもの**（第94〜95巡）。**大きな円2つは墨
  （`INK`）のリール**、後ろに**青い本体**、下に**黒い「下の段」**（左の円＝REC の穴
  ／右のバー＝残り3キーの穴）。本体と下の段は円の**兄弟**として `zIndex: 0`・
  `pointerEvents: "none"` ―― 中に入れると `.vs-reel-spin` で一緒に回る。
  掴めるのは円だけ（`dialAt` は円の式）。
  ★★★**録音画面は「アイコンを拡大して画面に嵌めた絵」**（第96巡）。リールの
  直径も中心間も中心の高さも**全部 `lib/cassette.ts` の比から導く**
  （`CASSETTE_REEL_*_PER_*`）。**画面側に数を1つも置かない。**
  ★★大きさを決める条件は**下の段が画面に収まること**の1つだけ（左右 `SPACE.lg`）。
  本体は段の2倍の幅なので**必ずはみ出す**（実測 … 本体 716×522、片側 163px）。
  ★★★**第95巡の `DIAL_RATIO`/`DIAL_CX`/`DIAL_WAIST` は消えた。復活させない。**
  ★★★**下の段がそのままキーの穴**（キーは `well="transparent"`）。
  ★★★**ラベルは段の外・キーの上**（第96巡。段が薄くキーと両方は入らない）。
  面が青い本体なので色は `bodyInkOn(JOURNAL_FACE)`＝墨（7.12）。**面から導く。**
  ★★リールの**芯**（3本の太い線）は **`MUTED` のベタ**（第101巡にユーザー指定
  「白ではなくグレーにしてあまり目立たないように」。墨の円の上で比 4.3）。
  ★**半透明にはしない**（比 1.94 で消える）。★ホームの山の芯も同じ色。★★★**カセットの寸法は `lib/cassette.ts` の
  1か所**、**芯の形は `lib/reelHub.ts`**。
  ★★★**第100巡**（ユーザー指定）… **縁の白い点5つ → リールの芯**（中心から120°ずつ、
  半径の1/3の**とても太い丸い線**が3本。中心は**曲線で滑らかに**繋がる。回る層の中に
  置くので芯が回る）／**突起は上だけ角丸・下は角**（出っ張ったボタンなので。3技術とも
  `topRoundRectPath`/`topRoundRadii` から）／**波形はリールとリールのあいだの上**
  （`CASSETTE_WAVE_*_PER_D`。生の 136/58 を比に直した。338×58 → **138×43**、
  ベゼル `SPACE.lg`）。
  ★★★**キーの径も縁も `lib/cassette.ts` から導く**（第98巡）。
  `keyLip = deckH/8` ／ `keyD = deckH − keyLip×2`。**生の 42 も `SPACE.sm` も捨てた**
  ―― バーの幅がキーの数から決まるので、縁も同じ式の中に居ないと3つが収まらない。
  ★★★**円の光学的な中心を段の中心線へ置く**（縦に積まない）。3つのキーは
  **バーの端の丸と同心**、REC は**穴と同心**。四方の黒い縁が全部 `KEY_LIP`。
  ★★★**`KEY_LABEL_H` は消えた。復活させない**（コメントは `TYPE.nano × LEAD.flat`
  ＝ 7 なのに値が 12 で、キーの列が 5px 上へずれていた）。
  ★実測 … 墨の円と地の比は **11.25**（第93巡の青い円は 2.02）／青い本体は 2.02。
  ★キーの面は両方の画面で白。★記号は**面から導く**（`bodyInkOn`/`redOn`/`STUDIO_KEY`）。
- `lib/audioTrim.ts` `lib/dayRecords.ts` — 音声の切り出し・1日分の組み立て。

## 共通UI
- `components/Button.tsx` — **押せる面はここだけ**。`Button`（離上で走る／variant・size）と
  `Press`（入力画面専用。押した瞬間に走る）。
- `components/common.tsx` — `PosterCard` / `Masthead` / `SectionLabel`。
- ★★★`components/DateGrid.tsx` — **月カレンダーの盤はここ1つ**（第102巡に `WhenSheet`
  から持ち上げた）。タスクの入力画面（`WhenSheet`）と**日付を割り当てる画面**
  （`components/home/AssignSheet.tsx`）が同じものを読む。**2か所に書くと、次に画素を
  直す人が片方しか直さない。** ★早押しは**今日・明日・来週**（第102巡に「今週末」→「来週」）。
  ★★ここには**4つの実機の不具合を生き延びた作り**が入っている（3か月を横に並べて送る／
  軸の固定 8px ／戻り跳ねの1フレーム対策／隣の月は触るまで作らない／1マスに丸と札は
  2つまで）。**素朴に書き直すと全部戻る。**
- `components/GeoType.tsx` — 幾何アルファベット。`components/TabIcons.tsx` — 面で描いたアイコン。
- `components/BottomSheet.tsx` / `PlanSelectionBar.tsx` / `PlanGenerateSheet.tsx` / `AddWishSheet.tsx` / `SignInGate.tsx` / `LeafletMap.tsx` / `Binder.tsx`（ゴールのみ）。

## データ・ロジック
- `lib/types.ts` — **データモデルの正**。`lib/constants.ts` — 色・書体・部品の寸法。
- ★`lib/palette.ts` — **有彩色の「役 → 色」の対応表**（第73巡）。
  ★★★**色そのものは `lib/constants.ts` の `PALETTE`（6行）だけ。パレットを
  替えるときはこの6行を書き換える。** 役との対応は同ファイルの `SCHEME`。
  ★★★**タスクのタグ（5→2→廃止）はもう色を使わない**（第93巡）。残るのは
  **ドメイン4**（提案・券・鋏痕・マップ）と**状態3**（上から借りる）だけ。
  **9 は参照画像の色数で、要件ではなかった。**
  ★★★**色は「色相の覆い・明度の幅・彩度の幅・見分け」の4つで採点して選ぶ。**
  **見分けだけを最大化しないこと** ―― 第76巡はそれで赤が2つになり、盤の雰囲気が
  図形に出なかった。
  ★★**「メイン × サブ」の組は廃止**（第80巡）。面に載る字は `bodyInkOn()` が
  **面から導く**（表にすると、色を替えた人が片方だけ直す）。
  ★kind（10種）は色を持たない ―― ドメインを通す。
  ★★★**薄い色は、面なら見えるが字にすると消える。** 白い紙の上で大きな字に
  できるのは Magenta 6.07 ／ Azul 4.83 ／ Terracota 3.26 だけ
  （Amarillo 1.43・Rosa 1.50 は不可。Verde 2.81 は**目盛りの外**として券だけ）。
  ★★★**地の上に直接いるものは色をハードコードしない** ―― `inkVarsOn()` が
  `--ink-on` / `--on-ink` / `--muted-on` を置く（`AppShell` の列と `<nav>` の2か所）。
  ★★**地は3アプリともクリームの1色**（第78巡）。暗いのは**機械**（録音の大きな
  ダイヤル）と**オーバーレイ3つ**（タスクの入力・声の録音・設定）。
  ★★**無彩色は5段**（第80巡に `CHARCOAL` と面の `SURFACE` を復活）。
  ★★★**キーは白い面・黒い穴・墨の窓**（第79巡）。`STUDIO.dial*`（画面で反転）と
  `STUDIO.cap`（両方の画面で白）は**別の材料**。**明るい面にアクセントを直接
  置かない** ―― 墨の窓 `STUDIO.socket` の中でだけ盤の色が読める（白い面では
  杏 1.35／若草 1.19／桃 1.77 しか出ない）。
  ★★★**押せる／押せないをグレーアウトで言わない** ―― 沈み（形）＝働いているか、
  ランプ（光）＝押せるか。**キーは死んで見えない。灯りが消えるだけ。**
  ★★★**区切りに線を引かない**（第79巡）。`SectionLabel` ＋ 面のカード ＋ 余白。
  残ってよい `border` は**押せるものの縁**だけ。
  ★★★**盤の赤は2つ。`redOn(面)` が選ぶ** ―― 暗い面には朱、明るい面には深紅。
  録音の赤は乗る面が2つ（キーの面と地）あり、**必要な赤が逆になる**。
  ★地を替えたら `app/manifest.ts` の `background_color`/`theme_color` も同時に直す。
- ★★★`lib/constants.ts` の書体は**3つ** … `SANS`（和欧）／`LATIN`（欧文だけ）／
  ★`DISPLAY`（**大きな欧文と数字だけ**。Anton。第100巡）。**線は `SWISS_XL` 級と
  未読の数**に引く（`TYPE.head`(20) 以下は触らない。タブの文字は「良い」と言われている）。
  ★★★**canvas は `font-variation-settings` を受けない** ―― だから大きな欧文は
  **書体そのもので縦長にする**。`lib/wordPlate.ts` の**偽コンデンスは第100巡に撤回**
  （`SQUEEZE_AIM = 1`／`SQUEEZE_MIN` は 0.88 の安全網）。**Anton は単一ウェイト**なので
  `WORD_WEIGHT` も 400（900 は合成ボールドになる）。
- ★★ホームの日付・曜日の上限は **`PILE_WORD_MAX`(61)**（`pileWorld.ts`）。
  **`SWISS_XL`(72) は TIMELINE と共有なので下げない**。
- ★★★**板の書体は「頼んでから」測る**（第101巡）。**canvas の `ctx.font` は
  読み込みを頼まない**し、`document.fonts.ready` は**頼んでいないものを待たない**。
  `ensureWordFont` で頼み、`wordFontReady` で遅れて届いたら**測り直す**。
  ★★**待つときは必ず締切**（`GATE_MS`）―― 締切が無いと**山が永久に空**になる。
  ★★**焼く箱の余りは字の大きさに比例**（`PLATE_BLEED`）。生の px だと大きい字で切れる。
- `lib/tokens.ts` — **余白・文字・角丸の目盛り（`SPACE`/`TYPE`/`RADIUS`）**。
  数字が不揃いに見えたらここを見る。増やさない。
- `lib/helpers.ts`（`domainOf`/`hasPlace`）/ `lib/dataStore.ts`（永続化・`SERVER_OWNED_KEYS`）/ `lib/supabaseClient.ts`。
- ★★★`components/home/Pile.tsx` ＋ `pileWorld.ts` ＋ `pilePaint.ts` — **ホームの山**
  （第92巡に土台から組み直して3つに割った）。`Pile.tsx`＝器・世界の寿命・指／
  `pileWorld.ts`＝物理の値・壁・図形の作り方／`pilePaint.ts`＝焼き方・描き方。
  ★★★**器を見る ResizeObserver は1つだけ**（2つ付けると先に走ったほうが
  もう一方の番人を黙らせ、**canvas の実解像度が初回のまま固定される**＝ぼやける）。
  ★★★**effect は2本** … 世界（engine・壁・ループ）は1度だけ、中身は署名で図形だけ
  差し替える。**1本にすると、題を1文字直すたびに山ごと落ち直す。**
  ★★★**山の器は `.bleed-x-b`**（左右 ＋ **下**）。下を出さないと、タブの器の
  `paddingBottom: var(--nav-h)` と `floorYOf` で**タブバーの高さが二重に引かれる**。
  ★★★**画面の細かさの上限は 2。`GravityTab` と `SolidCanvas` と同じ**（第107巡）。
  **ホームだけ 3 にしていたのが「GRAVITY ではフレームが落ちない」の答え** ――
  実機（dpr 3）で GRAVITY 1.09M 対 ホーム 2.77M ＝ **2.5倍の画素**を毎フレーム
  塗っていた。★「2 で止めると 1.5倍に伸びる」は**当時の実装のバグの記録**で、
  **1.5 は 3÷2**（実解像度と `setTransform` の食い違い）。**同じ値を2か所から取らない。**
  ★★★**掴んでいる図形には「代理の体」が在る**（第110巡。`syncProxy`／`ghostBodyOf`）。
  **絵は体そのもの**（`stepGhost(mo, g, pin)`）なので、壁で止まる・山を押しのけるが
  そのまま絵に出る。★★**体は絵の中心に置く**（支点ではない ―― `ay` は `h/2` まで
  育つので、支点だと**絵の下半分に当たり判定が無い**）。★★**`piecesRef` に混ぜない**
  （代理は眠らないので**ループが二度と止まらない**／`isLost` が掴んでいる図形を
  画面の上へ飛ばす）。★★★**`clearOverlap` の「前」に消す**（world の全部を見るので、
  着地点に重なった代理を見て**本番を1体ぶん真上へ持ち上げる**）。★★**締切
  `HANDOFF_MS`(300) は必須**（カレンダー経由・早期 return・閾値未満の離上では
  着地が来ない）。
  ★★★**体も作り直さない**（第111巡。`pileWorld.same()` が形の署名で使い回す）――
  作り直すと `Composite` から出し入れすることになり、**接触も眠りも切れる**うえ、
  入れ直しの `clearOverlap` が**落ち着いた山を「AABB が1つも重ならない縦の塔」へ
  積み直す**（＝「リセットが入って落とし直し」の正体。実測 山が 77px 浮いていた）。
  ★★★**`clearOverlap` を掛けてよいのは `Piece.fresh`（新しく湧いた体）だけ。
  着地の1枚にも掛けない**（指を離した所がそのまま正）。
  ★★★**掴んで運ぶ図形は回さない**（第112巡）―― `setVelocity` で引きずると接触が
  毎歩 回りを注ぎ込むので、**角速度を 0 にするだけでは足りない**（実測 0.0234 rad/歩
  ＝ 約 80°/秒が残って漂う）。**角度そのものを `dragRef.angle`（掴んだときの値）に
  留める**。★代理の体は最初からそうしてあった ―― **運ぶ側だけが抜けていた**。
  ★★`Pile` は**もう居ない体だけ**を `Composite.remove` し、**すでに world に居る体は
  投入の列に並ばせない**（`releaseRef.done` に先に入れる）。
  ★★★**山の大きさは中身が変わっても決め直さない**（第110巡。`buildPieces(…, hold)`）
  ―― `unit` は面積の予算 ÷ 全員なので、**1枚増えれば全員が別の大きさで作り直され**、
  位置は前から丸写しなので `clearOverlap` が持ち上げて**山が丸ごと浮く**。
  これが「**離した瞬間に画面が一気に変わる**」の正体。決め直すのは**世界を作るとき**と
  **器が 15% 以上変わったとき**だけ。★安全網は `cap`（いちばん大きな図形が器に入るか）1つ。
  ★★★**ループは止める**（第107巡。`runningRef`/`wake()` は `GravityTab` と同じ形）。
  ★★**停止の条件に `proxyRef` も入れる**（代理が居るあいだ止めると、それを消せる
  唯一のコードも止まって**見えない体が永久に刺さる**）。
  **`dirtyRef` を立てる所は必ず `wake()` も呼ぶ** ―― 起こし忘れると絵が止まる。
  ★★★**`dragRef` を立てる所にも要る**（第108巡）―― 長押しは `HOLD_MS` 後に
  **タイマーの中で**掴むので、そのあいだに山が眠るとループは自分で止まっており、
  **落ち着いた山の図形が1px も動かない**。
  ★★★**眠りは最初から許す**（`enableSleeping: true`。`GravityTab` と同じ）。
  **大域の門（`SETTLE_MS`/`CALM_V`/`CALM_W`/`STILL_FRAMES`/`SLEEP_EPS`/`settleAt`）は
  第108巡に削除。復活させない** ―― 門は**全体の最大値**で測るので**1つでも
  ゆっくり転がっていれば開かず、誰も凍らず、永久に震えた**（5巡ぶんの
  「震えが直らない」の根本原因）。代わりに**体を world へ入れるフレームで全員を起こす**。
  ★★★**`positionIterations` は既定の 6**（`POS_ITER`(10) は削除）―― matter は
  `clamp(20 / iterations, 0, 1)` なので**6 でも 10 でも減衰は 1**。上げても
  柔らかくならず、**全力の押し戻しを4回余計に回して振動が持続する**。
  ★★**刻みの貯金（`MAX_STEPS`）だけは GRAVITY と意図的に違える**（あちらは
  120Hz で半速。揃えると第104巡の貫通が再発する）。
  ★★★**落とす順を「時間」で作るなら、`Composite.add` の直前に重なりを解くこと**
  （`clearOverlap`。第109巡）―― `DROP_EVERY_MS`(60ms) で落ちる距離は **2.5px**、
  散らばりの幅は **200px**（79倍）なので、**時間差は順番を作るだけで位置を分けて
  いない**。実測で **16組が最大 82px めり込んだ状態**で world に入っていた
  （`GravityTab` は `i` の項を持つ幾何のはしごなので **0 組**）。これが
  「**落ちてきてぶつかった時にめり込んで震える**」の真因。
  ★★★**質量は全部の体で同じ密度にする**（箱の面積 ÷ `unit²`）―― **位置ソルバは
  質量を見ないが速度ソルバは見る**ので、密度がばらつくと毎ステップ食い違う。
  ★**「`GravityTab` は板に `setMass` を呼んでいた」は誤り**（呼んでいない）。
  第104巡の「修正」が密度の幅を 1.6 → 9.5倍に広げていた。
  帯からの引き下ろしは `above`（`HomeTab` の `lift`）が合図。
  ★★★**1フレームに焼くのは `BAKE_PER_FRAME`(2) 枚まで**（第115巡）―― 焼くのは
  **塗っている最中**なので、12体なら `layoutInRows` × 12 と `bakeGlyph`（1文字＝
  1枚の canvas）が **150枚ぶん**同じフレームに入る（実測 … ループの1回が 520ms）。
  焼けていない図形には**代役**（1段のピル）があるので、遅れても絵は途切れない。
  ★★★**書体は「落ちる前」に頼む**（`buildPieces` の直前で `ensureGlyphs`）――
  和文は**文字ごとに別の unicode-range の断片**なので、塗りの中で頼むと
  **届くたびに全文書のレイアウトと焼き直し**が落下の最中に走る。
  ★★★**塗るのは「変わった図形の箱」だけ**（第106巡）。**箱は1つにまとめない**
  ―― 落下中は画面じゅうに散らばるので、外接箱を1つにすると結局ほぼ全面になる。
  **前の箱と重なっていれば畳む**（畳まないと同じ画素を2度消す）。
  ★★★**`navHeightPx()` をループの中から呼ばない** ―― `document.body` に probe を
  足して矩形を読む＝**全文書のレイアウトを強制**する。**測った値を憶える**
  （`resize`/`orientationchange`/`visualViewport` でだけ測り直す）。
  ★★★**`stepGhost` は物理と同じ `while (acc >= STEP_MS)` の中で回す**
  ―― 1フレームに1回だと**120Hz の実機でバネも変形も2倍速**になる。
  ★★**幽霊は「動いたとき」だけ塗る**（刻みが進まなかったフレームは絵も変わらない）。
- ★★★**帯のピルを引いて日付を割り当てる**（第102巡にユーザー指定。仕様の正は
  `docs/home-spec.md` §6）… `lib/pullDrag.ts`（**手つきの算数と `pullBus`**）／
  ★`components/home/pillGhost.ts`（**帯のピルの写し取りと曲げ**）／
  `components/home/AssignRail.tsx`（**右端の黒い ASSIGN の帯**）／
  `components/home/AssignSheet.tsx`（**日付を割り当てるカレンダー**。地は墨）。
  ★★★**摘みは** `PULL_ARM`(**44** ＝ ピル1つぶんの高さ) / `PULL_RESIST`(**1.0** ＝
  指と 1:1) / `PULL_GIVE`(1.6)。**ゴムの側は指が引いた距離**なので曲線4本は使わない。
  ★★★**弾けたあとは「ばね1本」**（`K_CATCH`/`D_CATCH`。くびれが一息に開き、
  8% 行き過ぎて 450ms で静止）。★★★**`MORPH_SPAN`(距離)・`MORPH_STEPS`(時計)・
  `shownRows`(段の階段)・`STEP_POP`・`SNAP_POP`・`pop` は全部削除。復活させない**
  （ユーザー「**ぽこぽこは気持ちよくない。一発で・気持ちよく・スムーズに**」。
  ★**バネを2本掛けると面積が 1.2秒 揺れ続けた。一発 ＝ ばね1本**）。
  ★★★**`Ghost.phase` も削除した。`t` が姿を決める**（`t <= 0` ならピル）。
  ★★★**絵に効く値は `ghostKey()` 1か所から取る**（第107巡）。**手で並べない** ――
  第106巡に `bend` を入れ忘れて、**引き出しのゴムが1フレーム目で固まった**。
  ★★★**手つきは「輪ゴム → ばちん → 追いつき」**（第105巡にユーザー確定）…
  ★★**下へ 1px でも引いたら、そこから先は canvas の写し取り**（`.band-row` は
  `overflow: hidden` なので、DOM のままでは帯の外へ垂れられない）。
  ★★★**両端は帯に残り、掴んだ一点だけが垂れる**（`bendAt` の `cos²`。端で厳密に 0 ―― 
  「そこに止まろうとする抵抗」を**数で足していない**）。上の縁は `BEND_TOP`(0.55) 倍。
  越えた瞬間に**図形になって指へ「一気に、でも滑らかに」追いつく**
  （`K_CATCH`/`D_CATCH`。**瞬間移動しない** ―― 4フレームで届き 10.5% 行き過ぎて戻る）
  ＋ `SNAP_POP` ＋ `haptic`、以後**段が 1→2→3 とカクンと生える**
  （`shownRows` ＋ `STEP_POP`）。★★**滑らかにくびれさせない**（第103巡の `waist` の
  連続変形が「**普通すぎる**」と差し戻された）。★`waist` は文字の折り返しが読むので残す。
  ★★★**第104巡の `pullTaut`（ピルを `scaleY` で伸ばす）は削除。復活させない。**
  ★★★**幽霊には寿命を3重に掛ける**（第106巡）… `onLostPointerCapture` ／ unmount の
  後始末 ／ **`window` の `pointerup`（`once`）＝最後の砦**。★`owner`(`pointerId`) を
  持たせ、**自分の指の幽霊だけ**片づける。★★**隠すのは `opacity: 0`**
  （`visibility: hidden` は当たり判定から外れ、**捕捉が外れる原因になる**）。
  ★★**消し忘れると「元のピルが残り続ける」＋「300万画素を 120Hz で塗り続ける」**。
  ★★★**帯へ戻すのは `tTo` を 0 にするだけ**（第106巡。逆再生。`RAIL_NEAR` で届く）。
  ★★★**字も輪郭と同じだけ曲がる**（第111巡）―― **1文字ずつ自分の x で `bendAt` を
  読む**。第110巡までは**文字列の左端 1点**で測った垂れを行ごとに1回足していたので、
  **字は平行に下がるだけで 1px も曲がらなかった**。
  ★★**写し取りは「掴んだ瞬間」ではなく「写し取る瞬間」に測る**（帯が止まるのは段の
  `onPointerDown` ＝ ピルのあと）。**`border` のぶん中身が内へ寄る**（`PILL_EDGE`）。
  **`offsetWidth` は丸まる**ので**矩形 ÷ 実測の押下の倍率**で割り戻す。
  **写真は帯の `<img>` そのもの**を渡す（URL で取り直すとまだ届いていない）。
  ★★**弾みは `pullBus.pop` で注文する**（バネを回すのは山のループ。帯から直接触らせない）。
  ★★★**幽霊の絵は `hx`/`hy` に描かれる**（第108巡）―― `dx`/`dy` だけ更新しても
  **絵は1px も動かない**（`drawGhost`／`drawPillGhost` の `ctx.translate`）。
  ★★★**姿は掛け金 `Ghost.pill` で持つ。`t <= 0` の生の比較で分けない** ――
  ばねは 0 を跨ぐので**必ず点滅する**（落ちるのは 0、戻るのは `PILL_EXIT`(0.2)）。
  ★★★**帯の口と右端の境目には遊び `RAIL_HYST`(22)** ―― 1本の線だと指がその上に
  居るあいだ毎フレーム反転する（実測 10往復で `tTo` の反転 19回 → 0回）。
  ★★★**戻りは2段**（第109巡）… `RAIL_NEAR`(72) で幽霊が生まれて**指に追従**し、
  `PULL_ARM`(44) で**捕まえて**帯へ `K_SETTLE` で吸い付く。**1段だと「急に指から
  離れて戻る」**。★★★**変形と吸い付きは別のばね**（変形 `K_SWING`／吸い付き
  `K_CATCH`）。**姿の掛け金はばねの値で分けない**（`tTo` で分ける）―― 係数を
  柔らかくすると1歩目が閾値に届かず**スナップが黙って消える**。
  ★★★**振れは「絵そのものの速さ」で振る**（指の速さだと、**指を止めた瞬間に
  絵が飛んでいる最中でも 0 になる**＝「硬い」の一因）。
  ★★★**図形そのものの伸び縮みは第109巡に削除**（ユーザー確定「図形自体は
  柔らかくしなくて良い」）。柔らかさは**変形の途中**と**支点まわりの振れ**だけ。
  ★★★**毎フレームの値は `pullBus`（module のただ1つの入れ物）**。ref を props で
  回すと React Compiler の `react-hooks/immutability` が**拒否する**。
  ★★★**絵は山の canvas に描く**（`pilePaint.drawGhost`）。新しい canvas を足さない
  ―― 同じ座標系・同じ dpr・同じ焼き方なので**離した瞬間の継ぎ目が無い**。
  ★★★**`Body.setVelocity` は眠った体を起こさない** ―― 山の図形が長押しで動かない
  のはこれ。掴んだときと動かしている間に `M.Sleeping.set(body, false)`（実測 …
  起こさないと 0.0px／起こすと 156.7px）。
  ★★**帯のピルは2周ぶん DOM に居る**ので、掴んだ id を `Band` に持たせて**両方**隠す。
  ★★**「7本の柱」（`home-spec` の旧 K1）はやめた。復活させない。**
  ★★★**手ざわりは「ぶら下がって揺れる」と「引く速さで伸び縮みする」の2つだけ**
  （第103巡にユーザー確定。**「糸を引いて千切れる」「着地で山が沈む」は採らない**）。
  **支点は指そのもの**で図形はその下へぶら下がり、**振れは指に遅れる**（符号が負）。
  バネは **`K_SWING`/`D_SWING`**（`K_TRAVEL` では減衰が強すぎて揺れて見えない）。
  **伸び縮みは面積を保つ**（`sx·sy = 1`）。摘みは `HANG_T`/`SWING_MAX`/`STRETCH_MAX`。
  ★★★**バネは山のループが回す。指のイベントでは進めない**（`pointermove` の
  間隔は端末任せなので、同じ手つきでも手ざわりが変わる）。
  ★★★**山は落ち直さない**（第103巡）… `buildPieces(…, prev, landing)` が
  **同じ id の位置・角度・速度を写す**。新しい1つだけが、**指を離した所から
  投げた勢いのまま**落ちる（`pullBus.landing`）。**体は作り直すしかない**
  （倍率 `unit` は全体の予算から出るので1つ増えれば全部の大きさが変わる）。
  ★★★**山の物理が実機でだけ壊れる3つ**（第104巡。「何度もすり抜けて落ちる」の根治）…
  **① 実機は 120Hz**。`requestAnimationFrame` ごとに `Engine.update` を1回だと
  **1秒に120歩＝物理が2倍速**になり、めり込みが倍になって床を突き抜ける
  → **実時間を貯めて `STEP_MS` の整数歩だけ**進める（`MAX_STEPS`(2) で頭打ち。
  ★**1歩が重くなると「遅れ → 歩数が増える → さらに遅れる」の悪循環**に入る）。
  ★★**Chromium も Playwright の WebKit も 60fps なので再現しない。**
  「実機でだけ起きる」と言われたら**まず画面の更新頻度を疑う**。
  **② 重さの目盛りは1つ**。**文字の板だけ `setMass` を呼び忘れていた**ので、matter の
  既定の密度（px²）になって**他の全部（solid²）と桁が違った**（実測 板 18.8 対
  タスク 12.67 → 直して 3.17）。`GravityTab` は呼んでいた ―― **片方だけ直さない。**
  **③ 沈んだら「床へ戻す」**（`sink`）。**上から落とし直さない**（`respawn` は本当に
  もう戻れないときだけ）―― 沈む原因が残ると「沈む → 降る → また沈む」で**雨**になる。
  ★★★**指の当たり判定も絵と同じ輪郭**（`pickAt` が `halfWidthAtStack` を読む。第104巡）。
  箱のままだと**ピルの左右の角（絵の外）が触れる**（実測 … 箱の2割は絵の外）。
  ★★★**器が変わったときの3つ**（第103巡。「地面の下に落ちる」「回すと消える」の根治）…
  **床は器の寸法が変わらなくても動く**（`navHeightPx()` 依存。2秒ごとに見に行く）／
  **床が上がったぶんだけ山ごと動かす**（`refitPile`。厚い床板の中に入ると下へ
  押し出される）／**外へ出たら拾い直す**（`isLost`→`respawn`。`GravityTab` の
  `recycle` がホームにだけ無かった）。★器が 15% 以上変われば大きさも決め直す。
- ★★★`components/home/bandMotion.ts` — **帯の「物理っぽい動き」はここ1つ**
  （第109巡に新設・**第113巡に土台から作り直した**）。④a 止まるときの慣性／
  ① 引き抜いた瞬間の**席の畳みと流れの再開**／② 戻すときの**指の指す所に開く隙間**／
  ⑤ 挿し込まれたときの**そのまま収まる受け渡し**。
  ★★**`pullBus` と同じ作法**（module のただ1つの入れ物 ＋ 段ごとの rAF）。
  ★★★**隙間と席は `transform` のずれでは書けない。レイアウト（`.band-pad` の幅）が
  持つ**（第113巡）―― 帯は**同じ id のピルが2周ぶん居る**ので、id で引くずれには
  **1周目と2周目に別の値を書けない**。挿し口より後ろを `+w` 押すと、受け渡しで
  **2周目の全員がレイアウトで `+w` 動く**のに `-w` しか戻せない
  （実測 … **2周目の先頭が 152px 飛んだ**。1周目は 3px）。
  ★★★**両側 ±w/2（第110巡）も片側 +w（第111巡）も同じ理由で直らない。
  `Off = Spring & { hole, gap, wait }`・`bandGap`・`bandHole`・`bandGapCommit`・
  `bandHoleCommit`・`nudge` の表・`STAGGER` は第113巡に削除。復活させない。**
  ★★**器なら2周とも同じ列を描く**ので隙間も席も自動的に2周ぶん・同じ index に生まれ、
  **挿し込みでレイアウトが増える幅（`pad + ピル`）＝開けていた幅**なので、
  **受け渡しの計算がそもそも要らない**（実測 見えているピルの飛び **2.0px**）。
  ★おまけに周と周のあいだの間隔も周の内側と厳密に同じになった。
  ★★★**帯ぜんたいを `lead`（指の器より前で開いたぶん）＋ 隙間の半分だけ左へ戻す**
  ―― 戻さないと**1周目に開いた隙間が2周目を丸ごと押す**ので、**指のそばの境目が
  画面の外へ逃げる**（実測 … 指が x=200 なのに隙間は 405〜563）。
  ★★★**この一様なずれは挿し込みの瞬間に「送り（px）」へ畳み替える**（`holdRef` →
  `phaseRef`）。**`currentTime` を直接ずらさない** ―― 1周ぶんで折り返すので、
  **古い1周の幅で丸めた送りを新しい幅で読み直す**ことになる（実測 156px の飛び）。
  ★★**`transform` から外す瞬間と送りへ畳む瞬間は同じ1回**（`build()` の末尾で `paint()`）。
  ★★★**バネは役で分ける**（第111巡にユーザー確定「**跳ね返りは全部やめる／再開の
  ときだけ軽い慣性**」）… **隙間と席＝`K_OPEN`（第113巡に足した5組目。90% まで
  117ms・行き過ぎ 0.0%）／帯ぜんたいの一発＝`K_SWING`**。
  ★**`K_SETTLE` は遅すぎる**（90% まで 467ms ＝ 指が離れるまでに 150px のうち
  **15px しか開かない**＝「近づけてもピルがどきません」の正体）、
  **`K_CATCH` は 10.5% 行き過ぎる**（跳ね返りを禁じられた帯には使えない）。
  ★★★**隙間は「指が指している所」に開き、並びのほうが合わせる**（第112巡に
  ユーザー確定「**順番がいくら入れ替わっても問題ない。どんな時でも、任意のピルと
  ピルの間に戻せるように**」）。
  ★**x → index は段が出す**（ピルの居場所は流れの `transform` が決めていて React は
  知らない）→ `bandBus.slot.after`（**index ではなく左どなりの id**）→ 離したときに
  `pinBand` が**留め金**を打つ（第114巡。`tasks` の並びは触らない）。
  ★★**帯は輪なので index は 0〜n-1 で足りる**（「n 番目の後ろ」＝「0 番目の前」）。
  ★★★**境目は「まだ何も開いていなかったときの位置」で測る** ―― 器は**左の縁が
  動かないまま右へ育つ**ので、開いた中心で測ると**挿し口が勝手にとなりへ移る**
  （実測 同じ指の位置で index が 1 → 0 へ飛んだ）。
  ★★★**挿し口が移るときは受け皿（`shut`）へ預ける**（1本だと隙間が瞬間移動する）。
  ★**勢いは渡さない**（渡すと閉じるはずの隙間が 130 → 165px まで育つ）。
  ★★**狙われている段は流れを止める** ―― 止めないと挿し口が勝手に変わる（実測 同じ x
  で index が 3 → 1）。第113巡は理由が増えた ―― **隙間はレイアウトなので1周の幅が変わる**。
  ★★★**自分が開けている隙間では `ResizeObserver` で組み直さない** ―― 毎フレーム鳴り、
  組み直すと `track.animate` が新しく走り出して**止めていた段が指の下で流れ始める**。
  ★★★**席を畳むのは「弾けた瞬間」**（`armed`。`taken` では早すぎ、`items` から
  消えたあとでは**もう瞬間移動を終えている**）。実測 … 引き抜いて離した前後の飛び **0px**。
  ★★★**流れの再開と慣性の一発は、席より先に・無条件で**（第111巡）―― 第110巡は
  「後ろにピルが無ければ return」の中に `play()` を置いていたので、**引き抜いたのが
  段の最後のピルだと再開ごと落ちていた**。★★**流れの速さでは再開が見えない**
  （26秒で画面1枚＝0.25px/フレーム）。**見えるのは一発の慣性だけ**（`bandResume`）。
  ★★★**受け渡しの直後は `useLayoutEffect` の中で `paint()`** ―― 数だけ直して DOM を
  次の rAF に任せると、そのあいだの**1フレームだけ帯が飛ぶ**（実測 186.5px）。
  ★★★**`bandBus.aim` に素で代入しない**（`bandAim()` を通す）―― 段の rAF は
  止まっていることがあるので、**起こさないと誰も隙間を開けない**。
  ★★`stepBandMotion` は**実時間を貯めて固定の刻み**（段ごとの rAF が2つとも呼ぶので
  **同じフレームの2度目は 0 歩**＝門を別に作らない。120Hz の2倍速も同時に消える）。
  ★★★**帯は `items` の同一性が変わるたび `cancel()` され、先頭へ飛んでいた**
  （第109巡に根治）。引き金を**中身の署名**にし、**位相を px で引き継ぐ**
  ―― ★★割合で持つと**1周の幅が変わった瞬間に飛ぶ**（実測 -302.6 → -502.8px）、
  ★★**下の段は `direction: "reverse"`** なので**進みを折り返して**持ち越す。
  ★★★**位相を控えるのは専用の `snapPhase()`**（React は**クリーンアップを
  新しい効果の本体より先に**走らせるので `build()` の中では手遅れ）、
  **1周の幅は `lapRef`（組んだときの値）から読む**（クリーンアップ時の DOM は
  **もう新しい幅**）。
  ★★**第109巡の `bandKnockAt`/`bandBump`/`bandKnock` は削除した。復活させない。**
- ★★★**白抜き ⇄ 塗りは「量」で渡る**（第113巡。式は `components/home/pillGhost.ts` の
  `inkMix`、色の混ぜは `lib/constants.ts` の `mixHex` の1つ）。
  ★★★**掛け金（`Ghost.pill`）で切り替えない** ―― `t` が `PILL_EXIT`(0.2) を跨いだ
  1フレームで**輪郭の版面から塗りの図形へ絵ごと入れ替わって**いた（実測 色の差 157）。
  地を敷いて面を `α = mix`・輪郭を `α = 1 − mix` で重ねれば、**跳ぶフレームでも
  塗りの量は飛ばない**（実測 21 ＝ その瞬間の `t` の進みぶんそのもの）。
  ★★**両端は決め打ちにしない**（帯のピルが塗りか／山の図形が塗りか から導く）――
  決め打ちにすると**日付の無い図形（山でも輪郭）が勝手に塗られる**。
  ★★**字の色も同じ量で混ぜる**（面だけ渡って字が飛ぶのを防ぐ）。
- ★★★`lib/cassette.ts` — **カセット（JOURNAL の顔）の寸法はここ1つ**（第94巡）。
  同じ形を**3つの技術**が描く … タブのアイコン（**SVG**）／ホームの山（**canvas**。
  `drawCassette`）／録音画面の青い本体（**DOM**）。塗り分けは**四角＝青／
  リールと下の段＝黒**。★★**ただしタブバーの中だけは1色**（地が選択状態で変わるので
  色を決め打ちできない）。
  ★★**下の段は「左に円・右にバー」**（第95巡。総幅は変えていない）。録音画面では
  この2つが**そのままキーの穴**になる。
  ★★**第96巡にリールが大きく（2.9→3.2）・本体の中心へ（y 11→12）・段が薄く
  （2.8→1.8）なった**（ユーザー指定）。録音画面が読む比も**ここから出す**。
  ★★★**第97巡に半径 3.6・中心 7.6/16.4 へ**。**選んだ数ではなく式の解**で、
  「左の縁 ＝ リール間 ＝ 右の縁 ＝ 段までの隙間 ＝ 上」が**全部 1.6** に揃う。
  **手で振らないこと** ―― 振ると、また中央に固まって見える。
  ★★★**第99巡**（ユーザー指定）… **本体は角丸 2.6 に戻した**／**左上に四角い突起2つ**
  （`w = D`・`h = D/2`・隔 `D/3`、組の中心を左のリールの真上へ）。
  ★★**突起は本体の外へ出る**ので、録音画面の高さの見張りは**外接箱**
  （`CASSETTE_BOX_H_PER_H`）、山の焼き箱は**四方に突起のぶん広げる**。
  ★★★**第98巡に組み直した**（ユーザー指定）… **段は下の縁から
  段の高さぶん浮かす**／**リールは本体の上の縁と段の上の縁の中間**（＝中心より
  1.8 上）／**バーの幅は `2.75 × 段の高さ`**（キー3つが縁も隔も `lip` で詰まる幅。
  `lip = 段の高さ/8`）。★段が痩せた（本体幅の 50% → **38.3%**）ので、
  **録音画面は高さの見張りが勝ち、段が画面の端まで届かなくなった**。
- ★★★`lib/cardShape.ts` ＋ `components/explore/CardShapeDefs.tsx` —
  **BRIEF の札の写真を切り抜く4つの形**（第94〜95巡）。
  ★★★**対応表を手で書かない** ―― 券の鋏痕（`lib/ticket.ts` の `PUNCH_BY_DOMAIN`）
  から `SHAPE_BY_PUNCH` で導く。弧＝バショ **四つ葉** ／斜め＝タイケン **六角形** ／
  直角＝ジョウホウ **波打つ四角** ／切れ込み＝モノ **トゲトゲ**。
  ★★★**形は参照画像（Google Labs）に在るものだけ**（第95巡。ドームと台形は捨てた）。
  ★★★**CSS のマスクを使わない。`clip-path` で切る** ―― 実機で `mask-size` が
  効かず、SVG が固有の 1:1 の比のまま置かれた（**Chromium でも Playwright の
  WebKit でも再現しない。実機の WebKit だけ**）。`clipPathUnits="objectBoundingBox"`
  のパスを 0〜1 の器で書けば、画像としての寸法計算が存在しない。
  ★★★**id は札ごとに変える**（`useId()`）―― `AppShell` はタブを全部載せたまま
  横へ送るので、BRIEF の札と `DEV` の見本帳が**同時に存在する**。
  ★★★**形の正は点の列**（`cardShapePoints`）。SVG の `clip-path`（札）も
  canvas の輪郭（**ホームの山の提案**。`traceCardShape`）も**そこから導く**。
  ★★★**形は「円の和」で作る**（`unionOfCircles`。第98巡）。参照の半径 profile を
  測ると3つとも円の集まりの外縁だった ―― スカラップ四角（四角の周に16個）／
  ロゼット（円の周に12個）／クアトレフォイル（四隅に4個）。**六角形だけ多角形。**
  ★★★**正弦波（超楕円 × cos Nθ）で円に似せないこと** ―― 角で振幅が √2 倍に
  伸び、山が辺に揃わない。`polar` と `superR` は削除済み。**復活させない。**
  ★半径と個数は**「谷÷峰」の実測から解く**（目で振らない）。
  ★★★**点の列は最後に外接箱を 0〜1 へ正規化する**（`fitBox`。第97巡）。
  `objectBoundingBox` は 0〜1 のパスを器の寸法へそのまま掛けるので、
  **外接箱が 0〜1 でないと形ごとに見えるベゼルが変わる**
  （実測 58.9 ／ 35.9 ／ はみ出して切れる ／ 32 → 全部 32 になった）。
  ★★★**形を入れる器は必ず正方形** ―― `objectBoundingBox` は器の比へ**引き伸ばす**
  ので、横長の器に入れると潰れる（第96巡に実測 … 308×275 で横に 12% 伸びていた）。
- ★★★`lib/reelHub.ts` — **リールの芯（3本の太い線）の形はここ1つ**（第100巡）。
  録音画面（**SVG**。`hubPath`）とホームの山のカセット（**canvas**。`traceHub`）が
  **同じ点の列**を読む。★★**タブのアイコンには出さない**（地が変わるので色を
  決め打ちできない）。★★★**形は「3本のカプセルの smooth-min」** ―― 中心の
  繋ぎは**式が作る**（隅を手で丸めない）。★`lib/cardShape.ts` と同じ極座標の作法。
  ★★**`fitBox` を通さない**（芯はリールの中に小さく在る。器いっぱいに伸ばさない）。
  ★★★**前身の `lib/dial.ts`（白い点5つ）は第100巡に削除。復活させない。**
  ★★★**`lib/taskTags.ts` と `lib/tagPattern.ts` は第93巡に削除**（タグを全廃）。
  タスクの分類は「日付があるか／ないか」だけで、**塗りと輪郭**がそれを言う。
- ★★★**列（`[data-tab-scroll-root]`）は `overflowX: "clip"` を持つ**（第103巡）。
  `overflowY` だけ書くと、CSS の規則で**横にもスクロールできる** ―― ASSIGN の帯が
  48px はみ出しただけで指で横へ送れて見えた。**はみ出す面の側も包むこと。**
- ★★★`lib/pileBox.ts` — **山の器（左右の内寸と床）はここ1つ**（第91巡）。
  GRAVITY とホームが同じものを読む。床は `floorYOf`。
  ★ホームの山は `.bleed-x-b` で列のパディングの外へ出す。
  ★★★**左右は対称。セーフエリアは全画面で同じ `SPACE.lg`**（第102巡にユーザー確定）。
  **タブバーは右下の「作る」の丸まで含めて** `[SPACE.lg, w − SPACE.lg]` ―― 帯（白い
  ピル）の右端 310 に合わせてはいけない。**第101巡に右を寄せたのは誤り。もう振らない**
  （`pileLeftOf`/`pileRightOf`/`pileSpanOf` は削除した。**復活させない**）。
  ★`NAV_CREATE_SLOT`(64) は**行の寸法**として残る（`AppShell` と `Dashboard` が読む）。
- ★★★`lib/wordPlate.ts` — **「文字そのものが図形」の板の作り方はここ1つ**（第89巡）。
  ★★★**ホームの日付と曜日は「黒いピルに白い文字」**（第114巡にユーザー指定）。
  日付 `9.15`／曜日 `WD_SHORT`（`WED`）。面 ＝ `INK`・字 ＝ `PAPER`、角丸は
  **高さの半分**（帯のピルと同じ形）。遊びは**字の高さに対する比**（`PILL_PAD`）
  ―― 生の px を置かないので、字の大きさが変わってもピルと字の関係が動かない。
  ★★★**大きさの物差しは長いほうの綴り（`WD_FULL`）のまま** ―― 3文字で測ると
  器いっぱいまで太って**今までの倍近い字**になる。
  ★★**TASK（GRAVITY）の板は変えていない**（面を持たない `pill` 無しのまま）。
  GRAVITY の日付・曜日／TIMELINE の「自由」／**ホームの山の日付・曜日**が同じ関数を読む。
  canvas に焼いて貼り（`wordBitmap`）、**当たり判定は塗りの実測**（`inkBoxOf`）、
  横の細さは `ctx.scale`、字間は `PLATE_TRACK` を**1文字ずつ**送って詰める。
  ★**DOM で組み直さないこと** ―― 板だけが物理と別の座標系に居ると、板まわりだけ
  挙動が違う（第84〜88巡はこれで何度も壊れた）。
- `lib/spring.ts` — **canvas の図形だけの動きの土台（バネ＝減衰振動）**。係数は**5組**
  （運ぶ `K_TRAVEL`／吸い付く `K_SETTLE`／揺れる `K_SWING`／追いつく `K_CATCH`／
  **ひらく `K_OPEN`**。第113巡に追加。**90% まで 117ms・行き過ぎ 0.0%** ―― 帯の
  隙間は「速い」と「跳ね返らない」を同時に要求され、既存の4組はどちらか片方しか
  満たせない。表と実測は `lib/spring.ts`）。
  CSS の transition には使わない。★★**ゴムの手ざわり `rubber(raw, max)` もここ1つ**
  （第102巡に `GravityTab` の `rubberRise` から持ち上げた）。閾値で傾き 1 ＝**継ぎ目が
  無い**。引き下ろしと TIMELINE の伸びが**同じ1本**を読む。
- `lib/scroll.ts` — **スクロールの語彙はここだけ**（指の 1:1 ＋投げ＋減衰＋最寄りへ吸着）。
  強さを触るのは `SCROLL_GAIN` と `FLICK_K` の2つだけ。いまは ALIGN の縦送りが使う。
- `lib/motion.ts` — **動きの語彙（曲線4本・時間5つ・＋の丸の場所）**。
  CSS 側は `app/globals.css` の `:root`。数字はこの2つだけ。増やさない。
  JS のタイマーは `ms(T_OUT)` のようにここから引く（数字を書き写さない）。
- `lib/viewportKick.ts` — iOS の起動直後だけ画面が縮む不具合への対処（未確証）。
  ★**画面の上下の帯**は別件で、`statusBarStyle: "default"` で解決済み（第35巡）。
- `lib/ground.ts` — **画面の地色（html の背景 ＋ theme-color）を知っている唯一の場所**。
  背景が途切れたらここを見る。全画面の面を作ったら `pushGround` を呼ぶ。
- ★★★`lib/printGrain.ts`（券・CSS の面）— **質感の入口はいまここだけ**。
  ★★★`lib/paperTexture.ts` の `paperize()` は**第92巡に図形から外した**
  （ユーザー指定）。**どこからも呼ばれていない**が、戻せるように消していない。
  ★★**`PAPER_ALPHA` を 0 にして誤魔化さないこと。呼ばないのが正。**
  以下は戻すときのための記録 —— **素材は同じ 1 枚**（`public/crumple.webp`。第77巡に
  ユーザー指定でしわ紙へ。作り方は `tools/make-crumple.mjs`。元画像はリポジトリに
  無く、引数で渡す）。「券は板紙・図形は切った紙。混ぜない」は第76巡に**撤回**。
  ★★★**テクスチャは明暗だけを足すもので、色を変えるものではない。**
  タイルは**平均 128 の無彩色**で、**128 は `soft-light` の恒等点**だから地の色が
  原理的に動かない。★★**`multiply` を使わないこと** ―― 原理的に暗くしかできず、
  質感だけを乗せられない（実測 ΔE 0.135・彩度 −20%。`soft-light` は ΔE 0.001）。
  ★★★**しわは低い刻みなので拡大縮小してよい。網点は高い刻みなのでいけない。**
  **どちらの規則を当てるかはテクスチャの刻みで決まる** ―― しわは**繰り返さず**
  面いっぱいへ伸ばす（継ぎ目も周期も存在しない）、網点は 1画像画素=1デバイス画素で
  敷き整数周期で切る。★写真からは**照明のかたよりだけ**を抜く（半径 96）。
  ★canvas 側は `soft-light` のあと **`destination-in` で元の α に切り抜く**
  （ブレンドは `source-over` で合成されるので、透明な地にも乗ってしまう）。
  ★強さの目盛りは `PAPER_ALPHA`（図形 0.5）と `GRAIN_ALPHA`（券 0.55）の2つだけ。
  ★`paper-kraft` / `make-paper.py` / `print-grain` / `make-grain.mjs` / `halftone` /
  `make-halftone.mjs` は**まだ消していない**（試している最中）。決まったら消す。
- `lib/briefPipeline.ts` `lib/deckStyle.ts` `lib/planPipeline.ts` `lib/taskSuggest.ts` — 生成。
  ★ブリーフの取得は **Jina → 直接**の2段（第64巡）。**失敗は必ず HTTP コードごと
  `SiteTrace` に残す** — 残さなかったせいで6日間の停止に気づけなかった。
  ★★**Jina に鍵を送らない**（鍵ありはトークンを消費して必ず尽きる。鍵なしは
  20 RPM だがトークン無制限＝永久に無料）。18 RPM に絞る仕掛けと締切は
  `jinaSlot` / `JINA_PACE_BUDGET_MS`。単体チェックは `npx tsx tools/jina-check.mjs`。
- ★★★`lib/homeBand.ts` — **帯の中身と並びの規則の正**。
  ★★★**並びは「留め金」（`AppState.bandPins`）**（第114巡）。1つの留め金は
  「**この id は、この id の次に置く**」（`after` が空なら段の先頭）。
  ★★★**`tasks` や `items` の並びは触らない。第112巡の `reorderSomeday` は削除。
  復活させない** ―― ① `tasks` の並びは **GRAVITY の山・ALIGN の一覧と共有**
  していて関係ない画面まで動く、② 声の候補（`inbox`）とフォローアップ
  （`Task.suggestions`）は**持ち主が別なので動かせず**、
  **「任意のピルとピルの間」が原理的に作れなかった**。
  ★★**留め金は表示の並びだけ**を持つので **5種類すべて・2段とも**置ける。
  ★★**隣が消えたら自然な位置へ戻るだけ**（全件の並びを持つと翌日には消えた id の
  列になる）／**帯に居なくなった留め金は捨てる**。
  ★★★**留め金の付いた提案は `pickTodayItems` の時間帯の門を通さない**（`force`）
  ―― 通していたので、**行き先でない種類や閉まっている時間だと「帯へ戻しました」と
  出るのに現れなかった**。**表はおすすめの並べ方であって、ユーザーの意思を
  却下する門ではない。**
  ★★★**`bandRows(state, keepId?)`** … 下の段は `BAND_LIMIT_TASKS`(9) で打ち切るが、
  **`keepId` は切り落とさない**（第111巡）。
  ★**並びの規則を知っているのはこのファイルだけ。式を書き写さない。**
- ★★★`lib/todayPick.ts` — **帯の「今日行くのがおすすめ」の選び方はここ1つ**（第99巡）。
  ストック（`items`）から**今日これから行ける**ものを選ぶ。
  ★★★**AI を使っていない** ―― `Item` に営業時間も `start` も `venue` も無いので、
  モデルに聞いても材料が無い。**`kind` ごとの時間帯の表**（＝近似）で決めている。
  ★★**今日もう閉まったものは出さない**（23時なら 0 件。嘘を出さない）。
  ★**`offer` の後ろに積む**ので、未読の提案が残っているうちは出ない。
- `lib/myBrainPaths.ts` — **my-brain のパスを知っている唯一の場所**。`myBrain.ts` / `myBrainWrite.ts` / `myBrainSyncClient.ts`。

## サーバー関数（app/api）
- `cron/build-brief` 夜間生成 / `generate-brief` 実験 / `generate-plan` / `transcribe` / `suggest-subtasks` / `resolve-place` / `mybrain/*`。

# デザインシステム（2026-08-23・第33巡に確定）

## 目標

**Awwwards の「Site of the Day」級の UI/UX。** そこへ届く道は、汎用の UI キットで
見た目を揃えることではなく、**自分の語彙をひとつも破らないこと**。手で作った顔
（`GeoType` の幾何アルファベット / `SolidCanvas` の図形 / matter.js / Leaflet）が
このアプリの価値なので、それを平均的な見た目へ寄せる変更はしない。

## 守ること

★**規約の正は `design.md`** … 文字（`TYPE`×太さ・行間・字間）／余白／配色／
動き（曲線4本・時間の4分割）の表と、**機械チェック13本**。作業の前後に走らせる。
寸法の段（`SPACE`/`TYPE`/`RADIUS`）と目盛りの外の例外は
`docs/project_knowledge.md` §3。**この2つ以外に UI の規約を書かない。**

★★**iOS の「上下の帯」は解決済み。触らないこと**（第35〜37巡）。
`apple-mobile-web-app-status-bar-style` は **`default`**
（`black-translucent` は画面の下 47px がどの要素からも塗れなくなる）。
タブバーが下がるぶんは `NAV_BOTTOM_GAP` が `env(safe-area-inset-bottom)`
を比率で使って吸収する（`env(safe-area-inset-top)` は `default` では 0
になり使えない）。★★上 47px は**常に固定の白地に黒文字**（iOS の制約で
`theme-color` を一切読まない。`default`/`black` は静的固定、動的に色を
追従できるのは `black-translucent` だけであり、それは下の帯の復活と
両立しない）。**ユーザー確定で「白い帯は許容する」**（2026-08-23）。
これ以上この件を追わないこと。
★**`position: fixed` の面を伸ばして下の帯を埋めようとしないこと** —
第34巡に試して実機で1pxも動かなかった。詳しくは
`docs/project_knowledge.md` §3「iOS のホーム画面アプリ『上下の帯』」。

# 恒久ルール（全セッション厳守）

## ★配布（2026-08-18にユーザー確定・確認不要）

**毎回 `main` へ push する。**ユーザーへの確認は要らない。

`main` は **Vercel の本番ブランチ**で、ユーザーはここから実機（iPhone の
ホーム画面に追加した PWA）で確認する。**作業ブランチへ push しただけでは
実機に何も届かない。** 第9〜11巡はこれで3巡ぶん届いていなかった。

作業ブランチへ push したら、続けて必ず:

```
git checkout main && git merge --ff-only <作業ブランチ> && git push -u origin main
```

ユーザーが「変更が確認できません」と言ったら、まず `git log --oneline origin/main -1`
で `main` の先頭を疑うこと。

## 作業を終えるたびに必ず行う後始末
コードやドキュメントに変更を加えたら、**コミットの前に**必ず次の3つを行う。
省略しない。「次のセッションでやる」は禁止（引き継がれずに必ず腐る）。

1. **選別** … `handoff_current.md` を読み、価値の無くなった記述を消す。
   - 完全削除: 一時的な実行ログ、測定値の羅列、撤回されて跡形も無い案、会話のつなぎ。**アーカイブせず捨てる。**
   - 退避: 「なぜそう設計したか」「同じ轍を踏まないための教訓」がある記述は `docs/archive/` の該当ファイルへ移す。
2. **追記** … 今回確定した仕様・設計判断は `docs/project_knowledge.md` の該当章へ**追記ではなく上書き**で反映する（同じ話題の古い記述を残さない）。
3. **更新** … `handoff_current.md` を「いまどこにいるか / 直近完了（1行ずつ） / 次の一手 / 未解決 / 重要パス」の形に書き直す。**常に200行以内**。

この `CLAUDE.md` のファイル地図も、ファイルを増減させたら同時に直す。
実在しないファイルを指したまま放置しない。

## トークン節約
- **部分読み取りの徹底** … ファイル全体を出力（`cat` 等）しない。必ず `grep` / `head` / `tail` / `sed -n 'X,Yp'` で必要な箇所だけ読む。
- **ソースコードの分割** … 1ファイルが200〜300行を超えたら、UIコンポーネントやユーティリティを別ファイルへ分割する。
- **ログ出力の抑制** … 長大な出力やエラーが予想されるコマンドは `> /tmp/.../x.log 2>&1` へ逃がし、そのログを `grep` で絞って読む。
