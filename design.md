# design.md — UI の目盛りと、それを強制する grep

数値の持ち主は `lib/tokens.ts`（余白・文字）/ `lib/constants.ts`（色）/
`app/globals.css` の `:root`（動き）の**3か所だけ**。実装が食い違ったら本ファイルが正。
★段を増やさないこと。**目盛りの外に居てよいもの**は §7。段を決めた経緯は
`docs/project_knowledge.md` §3。
★入れないもの … shadcn/ui・Tailwind・Framer Motion・`layoutId`・CSS の 3D 変形
（★**WebGL は別件**で、使ってよい。改札鋏は three.js で描いている ―― 第17巡）
（`perspective` / `rotateX/Y`）。理由は `docs/archive/shell-redesign-2026-08.md` §65。

---

## 1. 文字 — この表から引く

| `TYPE` の段 | 太さ | 行間 | 字間 |
|---|---|---|---|
| **`poster` 38** | **`black`** | `snug` | 欧文 `tight` |
| `display` 26 / `head` 20 | `bold` | `snug` | 数字 `tight` / 和文 `normal` |
| `lead` 16 / `body` 13 | `text` | `body` | `normal` |
| `small` 11 / `micro` 9 / `nano` 7 | `bold` | `flat` | 欧文 `caps` / 和文 `normal` |

```ts
LEAD   = { flat: 1.0, snug: 1.3, body: 1.7 }
TRACK  = { tight: "-0.02em", normal: "0", caps: "0.16em", wide: "0.24em" }
WEIGHT = { text: 400, bold: 700, heavy: 800, black: 900 }
```

- `heavy`(800) は数字の主役だけ。`wide`(0.24em) は層名・ロゴ的な大文字だけ。
- ★★★**`poster`(38) と `black`(900) は「券の大きな英語」だけ**（第80巡にユーザー指定
  「Exhibition などの英語を太いボールドで大きく」）。§5-2 の「中間を作らない」に
  反しないのは、どちらも**中間ではなく上の端**だから。**ここ以外で使わない。増やさない。**
  ★第74巡は `black` が**券の題**だった。第80巡に**持ち主が題から英語へ移り**、題は
  `bold` へ下がった（主役が2つあると版面が割れる）。
- ★★**大きな欧文の caps に `caps`(0.16em) を当てない。** 字間は**小さい字を読ませる
  ため**のもので、大きくすると語が散る。`poster` は `tight`。
- ★★★**券の大きな英語だけは「段」から選ばない ―― 版面の幅に合わせて組む**（第81巡）。
  大きさを固定すると**短い語だけ弱く見える**（実測 … `EXHIBITION` は幅の 88% でも
  `THING` は 48%）。幅は文字サイズに**比例**するので、いまの幅を1回測れば次の
  大きさが直接出る（`次 = いま × 器の幅 ÷ いまの幅`。反復は要らない）。
  ★★**`textLength` で伸ばさない** ―― `spacingAndGlyphs` は字形が歪み、`spacing` は
  字間だけ間延びする。**正しいのは大きさを変えること。**
  ★この1行は `design.md` §7「目盛りの外」に属する（大きさが幾何から決まるため）。
- ★★★**大きな欧文と数字は `DISPLAY`（Anton）**（2026-09-13・第100巡にユーザー確定
  「タブの文字は良いですが、**大きい文字と数字が変**です」）。線は
  **`SWISS_XL` 級の巨大な文字と、未読の数（円の径の 0.9 倍）**に引く ――
  `TYPE.head`(20) 以下は `SANS` / `LATIN`（Archivo）のまま。**役を増やして、
  大きい所だけ差し替える。**
  ★★★**「変」の正体は書体ではなく潰し方だった**（実測）―― 山と TIMELINE の
  巨大な文字は **canvas** に焼くので `font-variation-settings: "wdth" 88` が
  **届かない**（CSS にしか効かない）。そこを補うために `lib/wordPlate.ts` が
  **横を 50% に潰し**、太さ 900 と合わさって「太くて不自然に細長い」字になっていた。
  → **縦長の書体（Anton）へ替え、同時に偽コンデンスをやめた**（`SQUEEZE_AIM = 1`。
  `SQUEEZE_MIN` は 0.88 の安全網へ格下げ）。**片方だけでは直らない。**
  ★★**Anton は単一ウェイト（400）・軸なし**。`fontWeight` に 800/900 を渡すと
  **合成ボールド**が掛かって汚れる（`WORD_WEIGHT` も 400 にした）。和文のグリフは
  無いので**和文に当てない**（TIMELINE の「自由」は `SANS`）。
  ★詰めは**字間だけ**で作る（潰しでは作らない）。★★**板の字間は `TRACK.tight`
  （-0.02em）と同値**（第101巡に -0.06 から緩めた。Anton はもともと詰まった書体
  なので、-0.06 は**二重に詰めていた**）。**もう目盛りの外ではない。**
- ★★★**書体を増やさずに「2つ目の声」を作る**（第81巡。★**券だけ**の話）。Archivo は可変フォントで
  `wdth`(62〜125) を持つ。既定は 88 で、**券の大きな英語だけ 70 まで絞る**と
  凝縮した表示用の顔になり、データ行（88 のまま）と質感の差が生まれる。
- ★★**括弧が「ラベル」を消す**（第81巡・参照の作法）。`(12.02 – 03.16)` /
  `WITH 東京都現代美術館 (清澄白河)`。「会期:」のような見出し語を券に置かない。
  ★★**和文が混ざる括弧に `caps` を当てない**（会期は「通年」「受注」のこともある。
  出どころがユーザーの入力なので、書体を決め打ちできない）。
- ★★**券の要約だけ `small`(11) を `text`/`body` で使う**（表では `bold`/`flat`）。
  第80巡にユーザー指定「日本語の詳細な説明の文字はもっと小さくて良い」。
  **券だけの例外**で、他所へ持ち出さない。
  ★和文で 900 が本当に出るのは、`app/layout.tsx` が `Noto Sans JP` を**可変**で
  読んでいるから（`weight` を渡すと 700 で頭打ちになる。第73巡に踏んだ）。
- ★**`fontSize` と `fontWeight` は必ず同時に書く**（片方だけだと既定 400 へ落ちて弱く見える）。
- ★**和文に `caps` / `wide` を当てない**。和文は字面が正方形なので、欧文用の字間を足すと
  **字間だけが抜けて見える**。和文は必ず `normal`。
- ★**`caps` / `wide` を `textAlign: "center"` と使うときは `marginRight: "-0.16em"` を添える**。
  字間は最後の字の右にも付くので、そのぶん右へずれて見える。
- ★★**横並びは `alignItems: "baseline"`。** `center` は行ボックスの中心で揃えるため、
  文字サイズが違うと**ベースラインは原理的に揃わない**。`center` が正しいのは
  ①両側とも文字でない ②高さの決まった箱どうし（★**両方に同じ `height` を与える**。
  片方だけ `padding` で膨らませると下端が揃わない）③意図した意匠（理由をコメントに書く）の3つ。
- 入力欄は `lead`(16) 以上（15 以下は iOS が勝手に拡大する）。

## 2. 余白 — `SPACE` から引く。左右は「ページ最上位の器」だけが持つ

★`padding` / `margin` / `gap` / `rowGap` / `columnGap` に**数字を直接書かない**。
必ず `SPACE.*`（`hair` 2 / `xs` 4 / `sm` 8 / `md` 12 / `lg` 16 / `xl` 24 / `xxl` 32）。
複合指定も同じ … `` padding: `${SPACE.md}px ${SPACE.lg}px` ``。
角丸も同じ … `RADIUS`（`sm` 4 / `md` 8 / `lg` 12 / `xl` 18 / `sheet` 28 / `pill` 999 / `circle` "50%"）。

```
[タブ直下の最上位コンテナ]   ← ★ここだけが paddingInline（= SPACE.lg）
  └ セクション / カード / 行  ← 左右パディングは 0。内側の余白は上下か gap で作る
```

- ★`paddingLeft` / `paddingRight` / `paddingInline` を書いてよいのは最上位だけ。
  入れ子が 1 段増えると実効 32px になる。
- ★**負の余白は禁止**（重ねたいなら `position` で重ねる）。要るなら `-SPACE.sm` の形だけ。
- 端まで伸ばす面は `full-bleed` の器を**1つだけ**（★二重に掛けない）。
- ★★★**左右のセーフエリアは全画面で同じ `SPACE.lg`、左右対称**（2026-09-14・第102巡に
  ユーザー確定）。**タブバーは右下の「作る」の丸まで含めて**この幅なので、帯（白いピル）の
  右端に合わせてはいけない ―― 第101巡に山の右の壁をそこへ寄せて**右端が大きく開いた**。
  山の器は `lib/pileBox.ts` の `PILE_INSET` の1か所。**もう振らない。**
- 合計値は手で足さず**式で書く**（`MAST_H + TAB_PAD_TOP + …`）。

## 3. 色 — 無彩色5段。有彩色は6色（要るのは5）

### 3-1. 無彩色

| 名前 | 値 | 比 | 役 |
|---|---|---|---|
| `INK` | `#2C2627` | 14.4 | 主役の文字 |
| `CHARCOAL` | `#383432` | 11.9 | **全画面のオーバーレイの地**（第80巡に復活） |
| `SECOND` | `#5A5A54` | 6.7 | 副文（二番目に強い文字） |
| `MUTED` | `#8E8E88` | 3.2 | 補助・非活性。★**本文に使わない**（4.5 を割る） |
| `SURFACE` | `#EDEAE2` | 1.17 | **面**（カード・入力欄・小区分）。第80巡に復活 |

★暗い画面用の面は `SURFACE_DIM` `#464240`（地との比 1.24）／
`SURFACE_DIM_2` `#545049`（カードの上で 1.24）。
★★★**6段目 `TASK_GREY` `#BFBBB2`**（2026-09-17・第118巡にユーザー指定
「**タスクのグレーももっと薄いナチュラルな柔らかいグレーに**」）。
**「無彩色は5段で打ち止め」を承知で足している** ―― 上の5段は**文字と面の階層**で、
こちらは**タスクの図形という1つの役の面**。階層には属さない。
`MUTED`(#8E8E88) を薄めると**副文が AA を割る**ので、あちらは触れない。
実測 … 地との比 **1.86**／**黒い字 7.76**（`bodyInkOn` が自動で黒を返す）。
★**暖色寄り**（地のクリームと同じ方向）。冷たいグレーはクリームの上で「汚れ」に見える。
★**輪郭だけのピル（日付なし）の線は薄くなる**（1.86）。字は第117巡に黒へ移して
あるので**読めなくなるのは線だけ** ―― ユーザー確定の上での選択。

★★★**半透明の重ねがけをやめるために面のグレーが在る** ―― 白の 8%/16% を重ねると
下に何が居るかで色が変わり、**重ねる順で結果が動く**。不透明の1枚なら動かない。
★黒は **Velvet Black `#2C2627`**（ユーザー指定）。`CHARCOAL` は**ほんの少しだけ
明るい**（地を黒そのものにすると、その上の墨のものが沈んで層が読めない）。

### 3-2. 有彩色 — **7色。要るのは5**（2026-09-17・第117巡に総入れ替え）

★★★**色そのものは `lib/constants.ts` の `PALETTE`（7行）だけ**。役との対応は
すぐ下の `SCHEME` と `lib/palette.ts`。**パレットの差し替えは7行の書き換えで済む。**

参照画像 … **Jardins de Métis（Reford Gardens）の入場券**（ユーザー指定）。
彩度のある画素を色相 × 明度でクラスタリングし、各塊の平均を採った。

★★★**タスクは有彩色を持たない**（第117巡にユーザー指定「**提案カード以外の色を
無彩色にします。タスクに関する図形はグレーを主に使って文字は黒で、つまりグレーの
塗りに黒の字**」）。だから有彩色が要るのは **ドメイン4 ＋ JOURNAL 1 ＝ 5**。
状態3（危険・肯定・選ばれている）は上から**借りる**。

| 役 | 色 | 紙の上 | 墨の上 | 面に載る字 |
|---|---|---|---|---|
| **バショ**（四つ葉） ／ 肯定 | 緑 `#2B903E` | 3.90 | 3.65 | 紙 3.90 |
| **タイケン**（六角形） ／ 危険 | 赤 `#DB3321` | 4.46 | 3.19 | 紙 4.46 |
| **ジョウホウ**（波打つ四角） ／ 選択 | 青 `#6D8CC1` | 3.26 | 4.37 | 墨 4.37 |
| **モノ**（トゲトゲ） | 黄 `#E3CB18` | 1.57 | 9.07 | 墨 9.07 |
| **JOURNAL**（★別枠） | **ラベンダー `#B2AFCE`** | 2.05 | 7.01 | 墨 7.01 |
| 予備（暗い面の赤） | ピンク `#DD5591` | 3.48 | 4.09 | 墨 4.09 |
| 予備（暗い面） | 深緑 `#083B2C` | 12.02 | 1.18 | 紙 12.02 |
| **タスク**（無彩色） | グレー `#BFBBB2`（`TASK_GREY`） | 1.86 | 7.76 | **墨 7.76** |

★★★**色と形は組**（ユーザー指定「**各ジャンルに対して図形の形が割り当たっているのは
維持して、色とその形がセットで印象が合うように**」）。形は `lib/cardShape.ts` の
まま1行も変えていない ―― 四つ葉のやわらかい弧に緑、いちばん強い六角形に赤、
直角のジョウホウに青、バースト状のトゲトゲに黄。
★★★**JOURNAL は「列に入らないこと」で別枠を言う** ―― 4色は赤・緑・青・黄の
原色寄りの列を作るので、**唯一のパステルであるラベンダー**が明らかにその外に居る
（第117巡はピンクだったが、第118巡にユーザー指定で替えた）。
★★**ピンクは消さない** ―― `redOn()` が**暗い面の赤**として読んでいる
（墨の上で 赤 3.19 ／ **ピンク 4.09**）。**ラベンダーは赤の役ではない。**
★★**赤は1つなので `growth`（タイケン）と `danger` が同じ色**。この2つは同じ画面に
出ないので衝突しない。
★★★**輪郭だけのタスクも字は黒**（第117巡）―― 「線と同じ色の字」は有彩色のときは
目盛りの外として許していたが、無彩色だと地の上で **3.2** しか出ず本文が読めない。
★★★**`lib/appAccent.ts`（アプリごとのアクセント配色のテスト）は削除した。
復活させない。**

### 3-3. **色は面ではなく、ワンポイント**（第80巡・ユーザー指定）

★★★「色は**背景として使うのではなく**、大きな文字のアクセントや、何かしらの
アイコンなど**ワンポイントで**使う」。**券は白い紙1色**になり、色が出るのは
**大きな英語**と**鋏痕**の2か所だけ。

- ★★★**札（BRIEF）の形は4ドメインに対応する**（第94巡）。券の鋏痕と**同じ性格**を
  引き継ぐので、約束は**1つ**のまま ―― 弧＝バショ **四つ葉** `clover` ／
  斜め＝タイケン **六角形** `hexagon` ／ 直角＝ジョウホウ **波打つ四角** `wave` ／
  切れ込み＝モノ **トゲトゲ** `starburst`。
  ★★★**形を入れる器は必ず正方形**（第96巡）。`clipPathUnits="objectBoundingBox"` は
  0〜1 のパスを**器の比へ引き伸ばす**ので、横長の器に入れると**形が潰れる**
  （実測 … 器 308×275 で四つ葉とトゲトゲが横に 12% 伸びていた）。
  ★★**参照画像の器を実測したら全部正方形だった**（218×218 ／ 218×217 ／ 188×188）。
  ★★**ベゼルは第98巡に `SPACE.xl` へ**（幅 358 で 6.7%）。参照の実測は 9.4%
  （25/267）で `SPACE.xxl` と同じだったが、**画面に対して写真が小さく見える**という
  ユーザー指摘で下げた。★札の幅も 340 → **358**（タブバー・山と同じ内寸）。
  → 写真の器 276 → **310**（面積 +26%）。
  ★★**題は2行・本文は2行で頭打ち**（`WebkitLineClamp`）。題を野放しにすると
  英語の見出しが4行になって**本文を押し出す**（第97巡に実機で起きた）。
  ★★**下の列は 44px の円3つ**（旗・記事を読む・出典）を**左へ寄せて `SPACE.sm`**。
  **言い分けは「塗り／輪郭」1本** ―― 輪郭＝この札について言う（旗）／
  塗り＝外か別の画面へ行く。**3つとも違う形にしない。**
  ★★★**形の正は点の列**（`cardShapePoints`）。SVG の `clip-path`（札）も
  canvas の輪郭（ホームの山の提案）も**そこから導く**。技術をまたいでも2度書かない。
  ★★★**形は「円の和」で作る**（第98巡。`unionOfCircles`）。ユーザー指摘
  「**添付した画像の形はない。円を組み合わせた形でもないし気持ちが悪い**」。
  参照の半径 profile を測ると**3つとも円の集まりの外縁**だった ――
  **スカラップ四角**（Vids／/Code。四角の周に1辺4個・計16個。谷÷峰 **0.743**）／
  **ロゼット**（MusicFX。円の周に **12個**。谷÷峰 **0.919**）／
  **クアトレフォイル**（GenChess。四隅に4個。谷÷峰 **0.717**）。
  ★★★**正弦波で円に似せない** ―― 第94〜97巡の `wave` は「超楕円 × cos(8θ)」で、
  **角では振幅が √2 倍に伸び、山が辺に揃わない**（1辺2山）。参照のどれにも似ない。
  ★半径と個数は**実測の谷÷峰から解いて**決める（目で振らない）。実装後の実測は
  四つ葉 0.716／波 0.745／トゲトゲ 0.924 で、**参照と 0.005 以内で一致**。
  ★★★**点の列の外接箱は 0〜1 へ正規化する**（第97巡。`fitBox`）。
  `objectBoundingBox` は 0〜1 のパスを**器の寸法へそのまま掛ける**ので、
  **外接箱が 0〜1 でないと形ごとに見えるベゼルが変わる。** 第96巡の実測
  （器 276・指定ベゼル 32）… 四つ葉は外接箱 80.5% で**ベゼル 58.9**／
  六角形は幅 97.2% で**左右 35.9・上下 32**／波打つ四角は **107.5%** で
  **器をはみ出して切れていた**／トゲトゲだけ 100%（32）。
  ユーザー指摘「図形が縦長」「四辺でばらばら」「ベゼルの太さと合っていない」は
  **全部この1点から出ていた**。正規化後の実測は **4形 × 4辺 ＝ 16件すべて 32**。
  ★★**形の性格の目盛り（`CLOVER_AMP` など）は触らない** ―― 正規化は最後の一手で、
  葉の深さや揺れの数を変えずに**四辺へ届かせる**だけ。両軸を別々に伸ばす
  （器が正方形なので歪まない）。
  ★★★**ホームの山で形を持てるのは提案だけ** ―― タスク・未読の数・カセット・
  文字の板は `ItemDomain` を持たない。**持たせると同じ形が2つの意味を持つ。**
  ★★★**形は参照画像（Google Labs）に在るものだけを使う**（第95巡にユーザー指摘
  「形が写真＝参照と違うものばかり」）。第94巡の**ドームと台形は参照に1枚も無かった**
  ので捨てた。★トゲトゲはホームの山の EXPLORE のバッジ（`zigVerts`）と同じ性格。
  ★★★**対応表を手で書かない。`PUNCH_BY_DOMAIN` から導く**（`lib/cardShape.ts` の
  `SHAPE_BY_PUNCH`）。**2つ持つと片方だけ直されて必ず食い違う。**
  ★★だから**ジャンルの文字を札に置かない** ―― 形が言うことを、字で二度言わない。
- ★★★**「大きな文字の色」に使えるかは、白い紙の上で決まる。** 実測 …
  Magenta 6.07 ／ Azul 4.83 ／ Terracota 3.26 ／ **Verde 2.81 ★** ／
  Amarillo 1.43 ✗ ／ Rosa 1.50 ✗。**薄い色は、面なら見えるが字にすると消える。**
- ★**目盛りの外** … 券の「モノ」の英語は Verde 2.81 で、大きな文字の 3.0 に
  0.19 届かない。`poster`(38) の 900 なので実用上は読めるが、**規約の外**。
  代わりに置ける色は 1.43／1.50 しかなく、もっと悪い。
- ★★★**暗い地の上で、赤は「文章」にならない**（設定画面）。Terracota は
  暗い地で 3.62 ＝ **図形の 3.0** は満たすが**本文の 4.5 は満たさない**。
  赤が持てるのは**面・縁・印**だけで、エラーの文章は紙色で書く。
  ★危険は**2つの赤を役で分ける** … **縁とアイコン＝Terracota**（3.62）／
  **塗る面＝Magenta**（その上の紙色が 6.07）。逆にはできない。
- ★★★**盤の赤は2つ。`redOn(面)` が選ぶ**（`lib/palette.ts`）―― 暗い面には
  Terracota、明るい面には Magenta。★**REC の輪だけは `redOn()` を使わない**
  （白い面では Magenta 6.07 が返るが、待機の墨 14.41 と見分けが付かず
  「光った」が読めない。**光ったと分かるか**で選ぶので Terracota 3.30）。
- ★★★**タスクの形は「ピルの積み」1つ**（2026-09-13・第99巡にユーザー確定）。
  幅の等しいピル（角丸＝段の高さ/2）を縦に積む。**段の数は題の文字数**
  （`rowsOf`。≤5 →1／≤12 →2／それ以上 →3。**最大3段**）。箱の比は
  **1段の比 ÷ 段の数**、1段の比は `max(3, 1行の字数 × 0.62)`。
  ★参照画像の実測 … 399 × 392 に3段・1段 131・くびれ/最大 0.717（実装は 0.667）。
  ★★★**「辺の数（1〜4）＝どれだけ埋まっているか」はやめた** ―― 円・半円・三角・
  四角の語彙は消えた。**復活させない。** 図形が言うのは**塗り／輪郭**と**大きさ**の2つだけ。
  ★★★**段の数は純粋な関数から引く**（行数の実測から引かない）―― 実測だと
  ALIGN と山で同じタスクの形が変わり、書体が遅れて届いた瞬間に**絵だけ段数が
  変わって物理の当たり判定とずれる**（物体は1度しか作らない）。
  ★★**ホームの山も同じ形**（`lib/solid.ts` の `stackOutline` を山も読む）。
- ★★★**段の中に1行。行は段の中心に置く**（2026-09-13・第100巡にユーザー指定
  「文字の行とピルの段がうまく噛み合っていません。**一つのピルの段に対して一行**、
  **一行が段の中心**に来るように。**文字とピルの輪郭の間のスペーシング**も適切に」）。
  ★★★**壊れていた理由は「刻みが2つあった」こと** ―― 文字は **`size × LINE_H`**
  の刻みで塊を組んで箱の中心に置き、段は **`boxH / rows`** の刻みで並ぶ。
  一致するのは偶然だけで、どちらも強制されていなかった。
  → **段が先。`layoutInRows` が段へ1行ずつ入れる**（`lib/textFit.ts`）。
  ★★**ベゼルは1つの数から四方へ回る** … 字は `pitch × ROW_FILL`(0.62)、
  余り `(pitch − size × LINE_H) / 2` を**上下にも左右にも**使う。だから
  **上下左右の余白が同じ px** になる（実測 … 3段の題で 上15 下12 左14 右14、
  dpr 3 の device px）。★箱の比（`rowAspect`）も**同じ `ROW_FILL` から**出す
  ―― 箱を決める側と字を詰める側が別の数を持つと、箱が字に足りない。
  ★★★**割り付けは1つだけ**（`layoutInRows`）。`fitText` と `layoutInShape` は
  第100巡に削除した。**復活させない** ―― 2つあると、片方だけが段を知らないまま残る。
  ★★**ホームの山も同じ割り付けを読む**（第99巡は `fitText(..., pw×0.82, ph×0.7)`
  で、`0.7` のぶん**同じタスクの絵が TASK と違っていた**）。
  ★★★**左右のベゼルは上下の `ROW_SIDE`(2) 倍**（2026-09-13・第101巡にユーザー指定
  「ピル内の文字が、**ピルの左右の外形線に対して詰まりすぎ**」）。ピルの左右は
  **角丸なので字の角といちばん近づく** ―― 四方を同じ px にすると左右だけ詰まって見える。
  ★★**`rowAspect` も同じ `ROW_SIDE` を読む**ので、**箱が横に広がるだけで字は
  小さくならない**（読まないと、空けたぶん字が縮む）。
  実測（dpr 3・18字の題）… 左右 14 → **25**（上下は 15/12 のまま）。
- ★★★**当たり判定は「描いた形」。外へ `PHYS_GAP`(1px)**（2026-09-13・第101巡に
  ユーザー指定「**図形や文字から1ピクセルだけ外側にオフセット**し、**図形同士が
  隣り合った時にもそれぞれの図形の形がはっきり分かる**ように」）。
  ★★★**第100巡までホームの山だけ体が矩形だった** ―― GRAVITY と DRIFT は
  `stackOutline` を使っているのに、山は `Bodies.rectangle`。ピルの丸い端のぶん
  **絵より広い箱**で当たるので、隣り合うと離れて見え、角では重なって見えた。
  ★**文字の板も同じ規則**（`PLATE_PAD`/`PLATE_PAD_Y` を 4/2 → **1/1**）。
  ★★くびれ・切れ込みは**凸包に潰れる**（`poly-decomp` が無い）。**それでよい。**
- ★★★**塗り＝日付あり／輪郭＝日付なし**（2026-09-12・第93巡にユーザー確定）。
  タスクの性質はこの1軸だけで、**全アプリで同じ言い方**をする ―― もとは
  ホームの帯のピルの見え方で、それを図形（GRAVITY／DRIFT／ALIGN／入力画面の
  下書き／ホームの山）へ広げた。
  ・**塗り** … メインカラーの面。字は `bodyInkOn(面)`（墨。オレンジの上で 5.64）。
  ・**輪郭** … 中は**地と同じ色**（透けさせない）、縁は **1px**、字は**縁と同じ色**。
  ★★**これは「区切りに線を引かない」の例外ではない** ―― 線が付くのは
  **押せる（掴める）もの**の縁だけ、という規則の中に収まっている。
  ★★**目盛りの外** … 輪郭の字はクリームの地の上で **2.55**（本文の下限を割る）。
  ユーザー確定（2026-09-11）。**輪郭はタスクだけ**にしてあるのはこのため ――
  EXPLORE の黄は **1.27** で、線にすると実機で消える。
  ★線は**内側に**引く（`lib/solidPaint.ts`。実効 1.08 CSS 画素を実測）。
- ★★★**明るい面にアクセントを直接置かない ―― 暗い窓の中で灯らせる**（第79巡）。
  白いキーの面の上では盤の色が出ないが、墨の窓 `STUDIO.socket` の中なら
  Amarillo 9.97 ／ Verde 5.07 ／ Rosa 9.49 と全部出る。
- ★★★**押せる／押せないを面の色（グレーアウト）で言わない**（第79巡）。
  **沈み**（形）＝いま働いているか、**ランプ**（光）＝いま押せるか。
- ★★★**区切りに線を引かない**（第79巡）。`SectionLabel` ＋ 面のカード ＋ 余白。
  残ってよい `border` は**押せるものの縁**だけ。
- ★**地は3アプリともクリーム `#FFFBF5` の1色**。
  ★★★**録音の画面は「カセット」そのもの**（第94〜95巡）。大きな円2つは
  **墨（`INK`）のリール**、その後ろに**青い本体**、下に**黒い「下の段」**
  （左の円＝REC の穴／右のバー＝残り3キーの穴）。タブのアイコンと同じ
  「**四角＝青／リールと下の段＝黒**」の塗り分け（`lib/cassette.ts`）。
  実測 … 墨の円とクリームの地の比は **11.25**（第93巡の青い円は 2.02 しかなく、
  明るい画面で輪郭が消えていた）。青い本体は地の上で 2.02、その上の墨の円が 7.12。
  ★★★**リールの芯は「3本の太い線」**（2026-09-13・第100巡にユーザー指定で、
  縁の白い点5つから差し替えた）。中心から **120° ずつ**、**半径の 1/3 ほど**の
  **とても太く角の丸い線**が3本、**中心では曲線で滑らかに繋がる**。
  ★★★**「滑らかに繋ぐ」を目で作らない ―― 式で作る** … 3本を丸い端の太線
  （カプセル）の**距離場**として書き、`min` ではなく **smooth-min**（二次の
  ブレンド）で結ぶ。中心のまわりが**ひとりでに曲線で繋がる**ので、腕の太さや
  長さを変えても丸めをやり直さなくてよい（`lib/reelHub.ts`）。
  実測 … 峰は 270°/30°/150°（**120° ちょうど**）・外へ出るのは半径の **0.44**・
  隣り合う接線の折れの最大 **14.3°**（＝角が無い）。
  ★★★**色は `MUTED` のベタ**（2026-09-13・第101巡にユーザー指定「円の中の線は
  **白ではなくグレー**にしてあまり目立たないように」。墨の円の上で比 **4.3**。
  白は 11.25 で強すぎた）。★★**ここだけ `bodyInkOn` から外れる** ―― 芯は
  「面の上で読ませる文字」ではなく**控えめに在る部品**だから。
  ★**半透明にはしない**（比 1.94 で消える。ベタのグレーはこの但し書きを破らない）。
  ★★**出すのはホームの山と録音画面の2つだけ** ―― **タブのアイコンには出さない**
  （地が選択状態で変わるので色を決め打ちできない）。
  ★★**録音画面では回る層（`.vs-reel-spin` の中）に置く**ので、芯が回って
  「録音中」が読める（点と同じ役目を果たす）。
  ★★★**録音画面は「アイコンを拡大して画面に嵌めた絵」**（第96巡）。
  **リールの直径も中心間も中心の高さも、全部 `lib/cassette.ts` の比から導く**
  （`CASSETTE_REEL_*_PER_*`）。画面側に数を1つも置かない。
  ★★**大きさを決める条件は1つだけ** ―― 下の段が**タブバー・山と同じ内寸**
  （左右 `SPACE.lg`）いっぱいに収まること。本体は段の2倍の幅なので**必ず
  画面からはみ出す** ＝ ユーザー指定「はみ出している感じは維持」が式から出る。
  ★★★**本体は角丸に戻した（2.6）・左上に四角い突起2つ**（第99巡にユーザー指定）。
  突起は `w = D` ／ `h = D/2` ／ 隔 `D/3`、**組の中心を左のリールの真上**へ。
  ★★★**丸めるのは上の2隅だけ。下は角**（2026-09-13・第100巡にユーザー指定
  「四角から**ボタンが出っ張っている**ことを示すデザインなので、下が角丸だとおかしい」）。
  ★★**3つの技術が同じ形を持つ必要がある** ―― SVG の `rx` も canvas の単一半径も
  CSS の一括指定も**四隅**を丸めてしまうので、`lib/cassette.ts` に
  `topRoundRectPath`（SVG の `d`）と `topRoundRadii`（canvas と CSS の4値）を置き、
  タブアイコン・ホームの山・録音画面がそれぞれの語で読む。
  実測（390×844）… 突起 77.2 × 38.6 ／ 角丸 `12.87px 12.87px 0px 0px` ／
  上端は題の `SPACE.lg` 下。
  ★★★**波形の帯は「リールとリールのあいだの上」**（2026-09-13・第100巡にユーザー指定
  「以前のように**円と円の間の上あたり**に表示し、**もっと小さく**して、
  **ちゃんとベゼルもとって**」）。第99巡は**リールより上**に出ていた。
  ★★**位置も高さもリールの直径から**導く（`CASSETTE_WAVE_CY_PER_D` 0.44 ／
  `CASSETTE_WAVE_H_PER_D` 0.14）―― 第98巡の生の 136px を比に直したもの。
  ★帯の幅は「その高さでの円と円の隙間」から出るので、**上寄りに置くほど広く取れる**
  （それが 136 の正体）。ベゼルは `SPACE.lg`（第99巡は生の 6）。
  実測 … **338 × 58 → 138 × 43**、左右のクリアランスはちょうど 16。
  ★★**突起は本体の外へ出る**ので、録音画面の高さの見張りは**本体ではなく外接箱**
  （`CASSETTE_BOX_H_PER_H`）。山の焼き箱も四方に突起のぶん広げる（1px だと頭が切れる）。
  ★★★**段（下のバー）の幅はキーの数から出る**（第98巡）。キー3つが縁も隔も
  `lip` で詰まる幅は `3(D − 2·lip) + 4·lip`。`lip = D/8` なら **`barW = 2.75 D`**。
  第97巡は `4.0 D` で、実測の隔が 33.6px（キーの径 51.1 の 66%）＝ユーザー指摘
  「ボタンとボタンの間隔を取りすぎ」。→ **10.2px（＝`lip`）** に詰まった。
  ★★★**本体は角丸 0・段は下の縁から段の高さぶん浮かす・リールは本体の上の縁と
  段の上の縁の中間**（すべて第98巡のユーザー指定）。中間に置くと**本体の中心より
  1.8 上**になり「少し上」が**式から出る**。★5つの隙間（左・リール間・右・上・
  段まで）が**全部 1.6**。
  ★★★**段が痩せた（本体幅の 50% → 38.3%）ので、録音画面は高さの見張りが勝つ**。
  結果、本体は**題の `SPACE.lg` 下から始まり**（「下によりすぎ」が消える）、
  段は**画面の端まで届かない**（「四角に対する穴の大きさが違う」が消える）。
  実測 … 本体 876×639・段 335（左右の余白 27.3）・リールは片側 243 はみ出す。
  ★★★**リールの半径 3.6・中心 7.6/16.4 は選んだ数ではなく、式の解**（第97巡）。
  ユーザー指摘「**円が小さすぎる**」「**中心の距離が近すぎて真ん中に寄りすぎ**」。
  第96巡の実測（24 の器）は 左の縁 2.8 ／ **リール間 0.8** ／ 右の縁 2.8 で、
  **間が縁の 1/3.5** ―― 2つの丸が中央に固まって見えていた。
  → 「**本体の左の縁 ＝ リール間 ＝ 右の縁 ＝ 下の段までの隙間**」を条件にすると
  `5.2 − R = (9.6 + R)/1.5 − 2R` の解が **R = 3.6 ちょうど**。
  検算（4件）… 4つの隙間が**全部 1.6**／上だけ 3.4（ラベル面）。
  実測 … `D/H 0.5143`（アイコン 7.2/14 ＝ 0.5143）／`中心間/W 0.4583`（8.8/19.2）／
  リールの中心 y ＝ 本体の中心 y（差 **0px**）／本体は片側 **163px**・リールは
  片側 **103.3px** はみ出す（前は 58.6）。
  ★**目で選んだ数ではないので、次に誰が触っても同じ値へ戻る。**
  ★第95巡の `DIAL_RATIO`／`DIAL_CX`／`DIAL_WAIST`（腰を式で縛る仕掛け）は
  **役目を終えて消えた** ―― 腰＝リールの隙間も、いまはアイコンの比が決める。
  ★★★**下の段がそのままキーの穴になる** ―― キーは「白い面・黒い穴・墨の窓」
  （§3-3・第79巡）なので、**新しい材料を1つも足していない**。
  ★★★**ラベルは段の外・キーの上**（第96巡。段が薄く、キーとラベルの両方は
  中に入らない）。面が**青い本体**になるので色は
  `bodyInkOn(JOURNAL_FACE)`＝墨（実測 **7.12**）。**面から導く。**
  ★★★**キーの寸法はバーの高さから導く。光学的な中心を中心線へ**（第97巡）。
  `KEY_D = deckH − KEY_LIP × 2`（`KEY_LIP` ＝ `SPACE.sm`）。**生の 42 は捨てた**
  ―― 段の高さ（アイコンの比から出る 67.1）と何の関係も無く、ユーザー指摘
  「バーとボタンの幅とか高さが全く合っていない」の正体だった。実測 **51.1**。
  ★★★**縦は「積む」のをやめて、円の中心を `deckH/2` へ置く**。第96巡は
  ラベル → キーを flex で積んでいたので、**2つの誤差が足し算**されていた ――
  ① `KEY_LABEL_H = 12` と書いてあったが実際の行は `TYPE.nano`(7) × `LEAD.flat`
  ＝ **7**。余り 5px が `flex-start` で捨てられ**列ごと 5px 上**。
  ② 箱は `keyD + KEY_DEPTH` 高なのに見える円は `keyD`。`alignItems: center` は
  **溝を含めた箱**を中央に置くので**さらに 2px 上**。
  合わせて **7px 上**（実測の黒い余白 上 5.6 / 下 19.6）で、押しても −7 → −3 と
  動くだけで**一度も中心線を通らなかった**。
  → 正した実測 … キーの中心 y ＝ 段の中心 y（**差 0px。4件**）／黒い余白 **8 / 8**／
  押すと中心から **4.04px**（＝`KEY_DEPTH`）沈む。
  ★★★**3つのキーはバーの端の丸と同心**（`space-evenly` をやめた。端の余白が
  28.8 になり、上下の 8 と揃わなかった）。バーの角丸は `deckH/2` なので、端の丸の
  中心は端から `deckH/2`。実測 … PAUSE 139.1 ／ SEND 239.8 ／ CANCEL 340.4 が
  バーの左の丸 139.05 ／ バーの中心 239.75 ／ 右の丸 340.45 と一致。
  REC は穴（`knob`）と同心（49.6 対 49.6）。**四方の黒い縁が全部 `KEY_LIP`。**
  ★★**キーの中の部品（窓・ランプ・2本線・✕）も径に対する割合**で持つ ――
  生の px のままだと、径を導くようにした途端に比が崩れる（窓が 52% → 43%）。
  ★★★**`KEY_LABEL_H` のような「コメントと値が違う定数」を作らない。**
  いまラベルは**キーと同じ中心の x へ絶対配置**（`bottom: calc(100% + …)`）なので、
  **ラベルの高さを誰も知らなくていい。知らなければ食い違えない。**
  ★残る暗いものは**オーバーレイ4つ**（タスクの入力・声の録音・設定・**日付を割り当てる
  カレンダー**。第102巡に追加）とキーの穴。
  ★地の上に直接いるものは色をハードコードせず、`AppShell` が置く
  `--ink-on` / `--on-ink` / `--muted-on` を読む（`inkVarsOn`）。
- ★同じ役のものを何十枚も並べる場所（バインダー）は、**色相を変えず明暗だけ**
  散らす（`DOMAIN_STEPS`）。
- ★16 進をコンポーネントに書かない。無彩色の梯子に有彩色を混ぜない。

## 3-b. 質感 — 平均 128 を `soft-light`。**色は動かさない**

★★★**図形からは外した**（2026-09-11・第92巡。ユーザー指定「図形の紙のテクスチャは
なくしてください」）。`paperize()` は**どこからも呼ばれていない** ―― 生きている
使い道は**券の面（`lib/printGrain.ts`・CSS。`DEV` タブにしか無い）だけ**。
★★**`PAPER_ALPHA` を 0 にして誤魔化さないこと。呼ばないのが正。**
以下の規則は**戻すときのために生かしてある**。

★★★**テクスチャは明暗だけを足すもので、色を変えるものではない。**
乗せたあとの**平均の色が動いていたら、それは質感ではなく色の変更**である。

- タイルは **平均 128 の無彩色**（`tools/make-crumple.mjs`）。128 は `soft-light` の
  **恒等点**なので、地の色は原理的に動かない。
- 券は CSS の `mix-blend-mode: soft-light`（`lib/printGrain.ts`）、図形は canvas の
  `globalCompositeOperation = "soft-light"` ＋ `destination-in` で元の α へ切り抜く
  （`lib/paperTexture.ts`）。★**質感は色のすぐ上・文字の下**。最前面へ出さない。
- ★★**`multiply` を使わない** ―― 原理的に**暗くしかできない**ので、質感だけを
  乗せられない。実測 … `multiply` は ΔE 0.135・**彩度 −20%**、`soft-light` は
  **ΔE 0.001**・彩度そのまま（142,884 画素）。
- ★★★**しわは低い刻みなので拡大縮小してよい。網点は高い刻みなのでいけない。**
  だから**しわは繰り返さない**（1枚を面いっぱいへ伸ばす＝継ぎ目も周期も存在しない）。
  網点のときは「1画像画素 = 1デバイス画素」で敷き、整数周期で切って継ぎ目を消していた。
  **どちらの規則を当てるかは、テクスチャの刻みで決まる。**
- ★写真から作るときは**照明のかたよりだけを抜く**（半径 96 のぼかしを引く）。
  質感そのものは削らない ―― 同じ「ハイパス」でも半径が2桁ちがえば別の操作になる。
- ★★★**タグの「柄」（べた塗り／網点）は第93巡に廃止**（2026-09-12）。タグごと
  無くなったので、柄で分類する必要も消えた。分類は**塗りと輪郭**が言う（§3-3）。
- ★★★**デュオトーン（写真を1色に染める）はこの規則の外**（第81巡）。
  質感の規則は「**地の色を動かすな**」だが、デュオトーンは**色を動かすことが目的**。
  ―― **どちらの規則を当てるかは「色を動かすつもりがあるか」で決まる。**

## 3-c. 券の外形 — **角丸は 0。丸みは切り欠きが持つ**

★★**上下の縁のギザギザ（11個）＋ 四隅の大きな円の切り欠き**（第72巡・第78巡に
ユーザー指定）。`components/explore/samples/TicketParts.tsx` の `scallopMask()`。

- ★★**マスクの層は6枚**を `mask-composite: intersect` で掛け合わせる
  （上のギザギザ / 下のギザギザ / 四隅の円 4枚）。
- ★★★**小さな箱に `no-repeat` で置いてはいけない。** 箱の外はその層の α が 0 に
  なり、`intersect` は「全部の層が不透明な所」しか残さないので、**券が丸ごと消える**
  （第78巡に実際に消した）。層は必ず**面いっぱい**（`100% 100%`）に敷く。
- ★`circle` の半径は**割合で書けない**ので、四隅は `ellipse` に縦横で別の割合を
  与えて**結果として真円**にする（券の比は `TICKET_ASPECT`＝3/4 で固定）。
- ★★★**`mask-composite` が無い環境では、ひとりでに素の矩形へ戻る**（既定の `add`
  が穴を埋める）。JS の判定は要らない。**実測で確認すること** ―― 四隅の画素が
  `intersect` 有りで地・無しで券の面になる。
- ★★切り欠きの半径は**券の幅の 14%**（第80巡にユーザー指定「もう少し大きい円で」。
  第78巡は 7%）。ギザギザの半径（幅の約 2.5%）の5倍以上。
- ★★★**欠けの安全域は「幅の％」の縦余白1つで解ける**（第80巡。ユーザー指摘
  「切り欠いた部分に対してスペーシングが考えられていない」）。CSS の `padding` に
  `%` を書くと**必ず親の「幅」に対して解決される**ので、`paddingTop: 14%` は
  **切り欠きの半径とぴったり同じ px** になる。円は**角から半径の距離までしか
  食わない**ので、そこより内側なら**横位置がどこでも掛からない**。
  → `TicketParts` の `SAFE`。★**避けるのは文字だけ。写真は逆に欠けへ噛ませる。**
- ★★★**マスクを掛けた要素に `box-shadow` を書いても出ない。** マスクは描画結果を
  切り抜くので、枠の外へ出る影ごと消える。第78巡から第80巡まで**券の影はずっと
  出ていなかった**（色の紙のうちは色で縁が分かるので気づかなかった）。
  **`filter: drop-shadow` を親に掛ける** ―― 描いたあとの α の形に落ちるので、
  ギザギザと切り欠きの輪郭にそのまま沿う（`TICKET_SHADOW`）。
- ★★★**だから BRIEF の札では、切り抜くのは写真だけ**（第94巡）。
  **札そのものには掛けない** ―― 掛けると `box-shadow` が消え、クリームの地に
  黄の面（比 1.27）の縁が1本も出なくなる。札の縁は**影**が持っている。
- ★★★**札の写真に CSS のマスクを使わない。`clip-path` で切る**（第95巡）。
  実機（iOS）で **`mask-size: 100% 100%` が効かず**、SVG が固有の 1:1 の比のまま
  置かれた（実測 … 切り抜かれた領域が器の幅より 254 デバイス画素 狭い正方形）。
  ★★**プロパティの書き漏らしではない**（`maskImage`/`maskSize`/`maskRepeat`/
  `maskPosition` を `Webkit` 付きと対で8本書いてあった）。
  ★★**Chromium でも Playwright の WebKit でも再現しない。** 実機の WebKit だけ。
  → **`clipPathUnits="objectBoundingBox"` のパスを 0〜1 の器で書く**
  （`lib/cardShape.ts` ＋ `components/explore/CardShapeDefs.tsx`）。器の比へ
  そのまま伸びるので、**画像としての寸法計算が存在しない**。
  `clipPath` と `WebkitClipPath` は**必ず対で**書く。効かない環境では素の矩形へ戻る。
  ★★★**id は札ごとに変える**（`useId()`）―― `AppShell` はタブを全部載せたまま
  横へ送るので、BRIEF の札と `DEV` の見本帳が**同時に存在する**。

## 3-d. 写真 — **デュオトーン**（券）

★★★**生のフルカラー写真を版面へ落とさない**（第81巡・参照5枚のうち4枚がデュオトーン、
残り1枚がグレースケールで、**生の写真を置いている参照は1枚も無かった**）。
写真は**明暗だけ**にして、「**影の色 → 紙の色**」の1本の帯へ**写す**。

- ★★★**やり方は SVG のフィルタ1本** ―― `feColorMatrix type="saturate" values="0"` で
  彩度を落とし、`feComponentTransfer` の `type="table"` で各チャンネルを2点補間する
  （＝**グラデーションマップ**。0 が影の色、1 が紙の色で、あいだは線形）。
- ★★★**混色（`lighten`/`darken`/`multiply`/`screen`）ではできない。** 両方試して
  両方失敗した ―― `lighten`＋`darken` は**切り捨て**なので影の色より暗い画素が
  全部その色で潰れ（実測 … 明度の中央値 0.70 の写真で 96% が単色）、
  `multiply`＋`screen` は**線形でない**ので中間調が一様に浅い方へ寄る。
  **デュオトーンは「切り捨て」でも「掛け算」でもなく、写像である。**
- ★★`color-interpolation-filters="sRGB"` を必ず書く（既定の linearRGB では中間調が
  持ち上がる）。
- ★★**白い紙では 影＝ドメインの色の「暗い側」／光＝紙**。色そのものを影にすると
  いちばん暗い所がその色の明るさ（0.44〜0.66）で止まり、**写真が暗くなれない**。
  ★★★**暗くするのは掛け算**（`deepen`）。`lib/helpers.ts` の `shade()` は各
  チャンネルに同じ数を足し引きするので**色相が飛ぶ**（実測 … 朱 → 純赤 `#7F0000`、
  緑 → 純緑 `#004200`。掛け算なら `#813522` / `#0B5F33` で色相は動かない）。
- ★★**色の紙では 影＝地そのもの**。写真の影が地に溶け、**写真が色の面から
  浮き上がる**（Post Familiar の見え方の正体）。

## 4. 動き — 曲線4本。時間は「何が動くか」で4つ

| 何が動くか | 時間 | 曲線 |
|---|---|---|
| **面**（シート・ページ・カメラ） | `--t-in` 700 / `--t-out` 600 | `--ease-settle`（下から出る面は `--ease-sheet`） |
| **要素**（面の中のひとつ） | `--t-item` 420 | `--ease-settle` |
| **押下** | 沈み `--t-press` **90** ／ 戻り `--t-out` 600 | 沈み `--ease-press` ／ 戻り `--ease-settle` |
| **環境ループ**（無限） | `--t-amb-*` | `linear` / `ease-in-out` |

`--ease-settle`(0.16,1,0.3,1) ★主役 ／ `--ease-sheet`(0.32,0.72,0,1) 下から出る面 ／
`--ease-press`(0.4,0,1,1) 加速して沈む ／ `--ease-exit`(0.4,0,0.2,1) 加速してから減速する唯一の形

- ★★**押下だけが非対称**（即座に沈み、ゆっくり戻る）。**沈みを 0.6s にしない** ―
  押した感触が消える。
- 新しい `cubic-bezier` を書かない。対称な `ease` / `ease-in-out` は環境ループ以外で使わない。
- 時間差は `--t-step`(50ms) の倍数。合成は可（`calc(var(--t-in) + var(--t-item))`）。
- インラインの `transition` は必ず `var(--t-*)` と `var(--ease-*)` を含む文字列で書く。
  語彙から引いた埋め込み（`` `opacity ${ms(T_OUT)}ms var(--ease-press)` ``）も可。
- JS のタイマーも語彙から引く（`ms(T_OUT)`）。数字を書き写すと CSS だけ変えたときにずれる。
- 押せる面は `components/Button.tsx` だけ（`Button` ／ 入力画面は `Press`）。
- ★★★**打ち消しの層で消してよいのは回転と平行移動だけ**（第119巡）。CSS は
  `transform` を**成分ごとに線形に補間する**ので、外が `scale(s)` で内が `scale(1/s)`
  でも**途中では逆行列にならない**（実測 … 真ん中で積 **2.04** ＝ 2倍に膨らんだ）。
  **回転と平行移動は足し算なので厳密に打ち消し合う。** 大きさを変えながら中身を
  据え置きたいときは、**大きさをレイアウト（`width`/`height`/`margin`）で育てる**。
  ★★★**第120巡に「打ち消しの層」そのものをやめた** ―― **形が変わる遷移は、
  毎フレーム自分でパスを書く**ほうが確実（`lib/cardMorph.ts`）。CSS の
  `clip-path` の補間はコマンド列が揃ったときだけで、実機の版に依る。
- ★★★**CSS に任せられない動きは `easeAt(EASE_SETTLE, t)` で引く**（第120巡）。
  **曲線の数字を写経しない** ―― 写すと `lib/motion.ts` と `app/globals.css` の
  `:root` に3か所目ができる。★canvas と、毎フレーム組み直すパスだけが対象。
- ★★★**全画面の面は `createPortal` で `body` 直下へ出す** ―― `AppShell` のアプリの列は
  `transform` を持つので**独立した重なりの文脈**を作り、`zIndex` をいくつにしても
  タブバー（z 25）の下に潜る。★**そこでは `--nav-h` が未定義**なので読まないこと
  （未定義の変数を `calc()` に入れると**一括指定ごと無効になり四方が 0**）。
- **`primary` は1画面に1つ。** 並び立つ選択肢は `secondary`、取り消し・あとでは `ghost`、
  図だけは `icon`（`aria-label` 必須）。

---

## 5. 情報の階層 — レイアウトを決める順番

**「余白が多い」の正体は、たいてい情報が足りないことではなく、束ね方が決まって
いないこと。** 大きさや色を触る前に、まず役を決める。

### 5-1. 決める順番

1. **数える** … 画面に載せる項目を全部書き出し、**役**で束ねる（主／従／付帯）。
2. **段を割り当てる** … 役の数だけ `TYPE` の段を使う。★**4段まで**。5段以上要ると
   感じたら、それは役が整理できていない合図。
3. **主役は1画面に1つ** … いちばん大きい文字はひとつだけ。同率一位を作らない。
4. **色は最後** … 階層は**大きさ・太さ・余白**で作る。色は階層の道具ではなく、
   **1か所だけの合図**（このアプリでは図形の面）。
5. **同じ役は同じ形** … ALIGN の「時刻＋日数」と TIMELINE の詳細の「時刻」は
   同じ組み方にする。画面が違っても、役が同じなら形も同じ。

### 5-2. 差は「見て分かる」だけ離す

| | 決まり |
|---|---|
| 大きさ | 隣り合う段は **1.3倍以上**。26/20(1.3)はぎりぎり、26/16(1.63)は明確 |
| 太さ | `bold` と `text` の2値で足りる。**中間を作らない** |
| 色 | `INK`(主) → `SECOND`(従) → `MUTED`(付帯) の3段。**同じ段の中で色を変えない** |
| 余白 | 束の**中**は `hair`〜`xs`、束の**間**は `md` 以上 |

★★**束の間が束の中より狭くなったら、階層は壊れている。** 距離の差そのものが
「これらは仲間」「これらは別」を伝えている。

### 5-3. 縦に積むか、高さを合わせるか

一覧は上から順に積む。ただし**別の絵と対応している文字**は、順番ではなく
**その絵の高さに合わせる**（TIMELINE の詳細＝レーンの図形と、その説明）。

★このとき重なりは**二段階**で解く … ①上から順に「上の行を追い越さない」
②下から順に「下端と次の行を越えない」ように押し戻す。
**前へ詰めるだけにすると、入り切らないときに末尾の行が下端で潰れ合う**
（第67巡に実際に起きた）。

### 5-4. 例 — TIMELINE の詳細（第67巡）

```
名刺を刷り直す              ← 主   SANS bold  lead   INK    snug
14:00–16:00  #WORK         ← 従   LATIN bold small  MUTED  ＋ 唯一の色（ピル）
版下を確認する ・ 紙を選ぶ    ← 付帯 SANS text  body   SECOND
TAKE  名刺入れ・見本         ← 付帯 ラベルは micro caps MUTED、中身は body SECOND
─────────────────
8/27                3 TASKS ← 日の見出し（帯のすぐ上に固定）
```

段は `head / lead / small / body / micro` の5段、色は3段、束の中は `hair`〜`xs`、
束の間は `md`。**ALIGN より項目が多いのに散らからないのは、段と色を増やして
いないから。**

## 6. 機械チェック（作業の前後に必ず走らせる）

★**#1〜#7・#10〜#13 は 0 件。#8・#9 は件数を見て目視。**
ルールと検出は必ず対で持つこと ― 検出の無い性質は例外なく劣化する。

```bash
# 1 迂回した cubic-bezier
grep -rn "cubic-bezier" components lib --include=*.tsx --include=*.ts | grep -v "var(--" | grep -v "GROUND_EASE" | grep -v "^components/Button.tsx:9:"
# 2 インラインの生の時間（" と ` の両方）
grep -rnE 'transition[A-Za-z]*: *("|`)[^"`]*[0-9]+m?s' components app --include=*.tsx | grep -v "var(--" | grep -vE '\$\{ms\('
# 3 生の fontSize
grep -rhoE 'fontSize: [0-9.]+' components app --include=*.tsx | sort | uniq -c
# 4 globals.css の生の時間（var(--) と同居していても拾う。0ms / 0s は除く）
grep -nE '\b(transition|animation)\b[^;]*(^|[^-\w(])(0*[1-9][0-9]*(\.[0-9]+)?|0*\.[0-9]*[1-9][0-9]*)m?s\b' app/globals.css
# 5 生の余白（★三項やテンプレの陰も見る。0 と、理由を書いた例外は除く）
grep -rnE '\b(padding|margin|gap|rowGap|columnGap)[A-Za-z]*:[^,}]*[^\w.$-]-?[1-9][0-9]*\b' components app --include=*.tsx | grep -v 目盛りの外 | grep -vE ':[0-9]+: *(//|\*)'
# 6 生の行間・字間・太さ（★lineHeight: 0 は行ボックスのリセットなので除く）
grep -rnE '\b(lineHeight|letterSpacing|fontWeight): *"?-?[1-9]' components app --include=*.tsx | grep -v 目盛りの外
# 7 16 進の直書き
grep -rn '#[0-9A-Fa-f]\{3,8\}\b' components app --include=*.tsx | grep -v 目盛りの外 | grep -vE ':[0-9]+: *(//|\*)'
# 8 左右パディングの持ち主（目視。最上位の器だけか）
grep -rnE '\b(paddingLeft|paddingRight|paddingInline): ' components --include=*.tsx
# 9 横並びの center（目視。baseline であるべきものが無いか）
grep -rn 'alignItems: "center"' components --include=*.tsx | wc -l
# 10 位置の生の数値（隅のバッジなど）。★下の FIG は図形・物理のファイル（§7）
FIG='GravityTab|DriftTab|SolidCanvas|GeoType|TabIcons|LeafletMap|Binder|TaskSpace'
grep -rnE '\b(top|left|right|bottom|inset)[A-Za-z]*: *-?[1-9][0-9.]*' components app --include=*.tsx | grep -vE "$FIG" | grep -v 目盛りの外 | grep -vE ':[0-9]+: *[(/*]'
# 11 fontSize と fontWeight は必ず対（style オブジェクト単位なので grep では書けない）
#    ★これだけは件数ではなく**終了コード**で見る（0 なら OK）
node tools/check-weight.mjs
# 12 globals.css の 16進と生の余白（★色の持ち主は lib/constants.ts の1か所だけ）
grep -nE '#[0-9A-Fa-f]{3,8}\b|(padding|margin)[a-z-]*: *[^;]*[1-9][0-9]*px' app/globals.css | grep -v 目盛りの外 | grep -vE '^[0-9]+: *(/\*|\*)'
# 13 hair(2) の役の誤用（罫と光学的な詰め専用。器の左右の余白に使わない）
grep -rn 'px \${SPACE.hair}px' components app --include=*.tsx
```

---

## 7. 目盛りの外（絵と物理の寸法）

**画面の余白ではなく「絵と物理の寸法」であるものは、4の倍数へ丸めると
物理法則が変わるか、絵が歪む。** だから目盛りに乗せない。

| 種類 | 具体例 |
|---|---|
| **matter.js の力と場** | 初速・`frictionAir`・アトラクタの係数・`TAP_MOVE`(8px)。★山の器の番人 … `WALL_MIN_H`(1200) / `LOST_BELOW`(900) / `LOST_SIDE`(120) / `THROW_MAX`(18) / `FLOOR_CHECK_MS`(2000)。★★★**落とす順を「時間」で作るなら、`Composite.add` の直前に
重なりを解くこと**（`clearOverlap`。第109巡）―― `DROP_EVERY_MS`(60ms) で落ちる距離は
**2.5px**、散らばりの幅は **200px**（79倍）なので、**時間差は位置を分けていない**
（実測 16組が最大 82px めり込んで world に入っていた）。
★★★**質量は全部の体で同じ密度にする**（箱の面積 ÷ `unit²`）―― 密度がばらつくと
**位置ソルバ（質量を見ない）と速度ソルバ（見る）が毎ステップ食い違う**。
★★★**`positionIterations` は触らない（matter の既定 6。`GravityTab` と同値）** ―― 第106巡に 10 へ上げたのは逆効果で、第108巡に差し戻した。`POS_ITER` は削除。**復活させない**。★★★**眠りは最初から許す**（`enableSleeping: true`）。**大域の門（`SETTLE_MS`/`CALM_V`/`CALM_W`/`STILL_FRAMES`/`SLEEP_EPS`）は第108巡に削除。復活させない**。★★**刻みと歩数** … `STEP_MS`(1000/60) / `MAX_STEPS`(2)（第104巡。**`requestAnimationFrame` ごとに1歩だと 120Hz の実機で物理が2倍速になる**。★第105巡に 3 → 2 ―― **1歩が重くなると「遅れ → 歩数が増える → さらに遅れる」の悪循環**に入り、頭打ちのまま張り付く） |
| **ALIGN / TIMELINE の寸法** | `ALIGN_MAX_W/H`・`PITCH_TIGHT/SPREAD`・`ARC_APEX_X`・`ARC_SWING`・`ROW_H`。★互いに縛り合っている（`H × 0.75 ≤ PITCH` を割ると図形が重なる）ので**一つだけ動かせない** |
| **ニュースの札** | `components/home/NewsCard.tsx` … `PULL_OPEN`(64 ＝ 引き切る距離) / `PULL_TRIP`(0.45 ＝ 離して開く境目) / `CARD_MAX`(0.6 ＝ 高さの上限。**高さそのものは中身を測って決める** ―― 決め打ちだと下半分が空っぽの札になる)。★★★**指が動かす `t` に曲線を掛けない** ―― 運ぶ側（`run`）が**すでに `easeAt` を通した**値を作るので、描く側でもう一度掛けると**二重に効く**（実測 … 6px 引いただけで 45% 開いた。`EASE_SETTLE` は出だしの傾きが大きい） |
| **canvas の描画座標** | ★★★**画面の細かさの上限は 2**（`DPR_MAX` ／ `GravityTab` ／ `SolidCanvas` の**3か所とも同じ**。ホームだけ 3 にしていた第92〜106巡は、実機で**GRAVITY の 2.5倍の画素**を毎フレーム塗っていた）／★**塗る箱の余白 `PAINT_PAD`(4) と `GHOST_PAD`(24)**（`components/home/`。変わった図形の箱だけ塗るため）／`SolidCanvas` / `GeoType` / `lib/solidPaint.ts` / `lib/paperTexture.ts` / `lib/spring.ts` のバネ係数（**5組 … 運ぶ・吸い付く・揺れる・追いつく・ひらく**。★第113巡に `K_OPEN`/`D_OPEN`（**90% まで 117ms・行き過ぎ 0.0%**）を足した ―― 帯の隙間は「**速い**」と「**跳ね返らない**」を同時に要求され、既存の4組はどちらか片方しか満たせない。表は `lib/spring.ts`。**これ以上増やさない**） |
| **図形そのものの寸法** | 綴じ穴の直径・装飾図形の `-height/2`（縦の中央合わせ）・アイコン内部の `gap`・✕印の2本の線の交点。★★★**タスクの形は「角丸四角」**（第123巡にユーザー指定で「ピルの積み」から替えた）… `lib/solid.ts` の `RECT_R`(0.32) ＝ **角の半径 ÷ 段の高さ**。段の高さは px でどの図形も同じ（`rowSpecOf`）なので、**この比1つで全図形の角の丸みが px で揃う**。★**段の数も箱の寸法も `STACK_INK` も変えていない**（ユーザー指定「元のピルを元にした大きさとかを維持して」）。★★★**札の4つの形（`lib/cardShape.ts`）の正規化は一様**（第123巡）―― **両軸を別々に伸ばすと形の比がレイアウトの都合で変わる**。実測の生の外接箱（高さ÷幅）… 四つ葉 1.000／**六角形 0.897**（＝正六角形 √3/2 ＋ 角の丸み）／波打つ四角 1.000／トゲトゲ 1.000 ―― **正方形でないのは六角形だけ**なので、一様にしても他は1点も変わらない |
| **グリフの焼き段と線の太さ** | `lib/textFit.ts` の `BUCKETS`(32/64/96/144/192)、輪郭線の `EDGE`(1)、リールの芯の比（`lib/reelHub.ts` の `HUB`）。★**画素の刻み**なので、4の倍数へ丸めると字が拡大されるか線が太る |
| **表示専用の巨大欧文** | `SWISS_XL`(72)／ホームの上限 `PILE_WORD_MAX`(72)／その行間 `0.86`／**焼く箱の余り `PLATE_BLEED`(0.16)**（★**生の px で持たない** ―― 大きい字ほど効かなくなり、書体を替えた巡に必ず切れる）／**段の中の字の取り分 `ROW_FILL`(0.62) と左右の倍率 `ROW_SIDE`(2)**（`lib/textFit.ts`。ベゼルはここから四方へ回る） |
| **引き下ろしの手ざわり** | `PULL_ARM`(**44** ＝ `BAND_H.plain`) / `PULL_RESIST`(**1.0** ＝ 指と 1:1) / `PULL_GIVE`(1.6) / ★★★**近さの摘みは3つに割れている**（第120巡にユーザー指定「**帯に戻すのと、右側に寄せて予定を変えるのの判定を小さくして誤動作したりしないように**」）… **右端の ASSIGN `RAIL_NEAR`(72 → 36)** ／ **帯へ戻す幽霊が生まれる `BAND_NEAR`(72 → 48)** ／ **帯を狙っていると見なす `BAND_CATCH`(`PULL_ARM` を借りていた 44 → 24)**。★**右端は「寄せにいく」ので狭くてよく、帯は「上へ運ぶ途中に必ず通る」ので少し広い。** ★★第119巡までは3つとも同じ 72／44 で、**山の図形を右へ運ぶだけで ASSIGN が出て**、**帯の下を横切るだけで並びが動いた** / ★★★**境目の遊び `RAIL_HYST`(22 → 12)** ―― **遊びは閾値より小さいこと**（36 の閾値に 22 の遊びだと、一度入ったら 58px まで出られない） と **姿の掛け金 `PILL_EXIT`(0.2)** ―― **境目を1本の線にすると、指がその上に居るあいだ毎フレーム反転して震える**（実測 … 10往復で `tTo` の反転 19回 → 0回）。**ばねは 0 を跨ぐので、姿を生の比較で分けると必ず点滅する** / ★★★**変形と吸い付きは別のばね**（第109巡）… **変形 `K_SWING`/`D_SWING`**（実測 36% 行き過ぎ・1212ms で静止。「物理が働いている柔らかさ」）／**吸い付き `K_CATCH`/`D_CATCH`**。★★**姿の掛け金はばねの値で分けない**（`tTo` で分ける）―― 係数を柔らかくすると1歩目が閾値に届かず**スナップが黙って消える**。★★**振れは「絵そのものの速さ」で振る**（`mo.catchX.v`。指の速さだと**指を止めた瞬間に、絵が飛んでいる最中でも 0 になる**）。`SWING_MAX` は 0.30（実測 片側 21.6°）。**`MORPH_SPAN`(距離)・`MORPH_STEPS`(時計)・`shownRows`(段の階段)・`STEP_POP`・`SNAP_POP`・`pop` は第106〜107巡に削除。`STRETCH_MAX`（図形そのものの伸び縮み）は第109巡に削除。復活させない**（★バネを2本掛けると面積が 1.2秒 揺れ続けた。**一発 ＝ ばね1本**）/ **`BEND_TOP`(0.55)・`PILL_PRESS`(0.97)・`PILL_EDGE`(1)**（第105巡の「輪ゴム → ばちん → 追いつき」。★**`PULL_TAUT` は第105巡に削除。復活させない** ―― ピルは1px も動かず、垂れるのは掴んだ一点だけ。垂れの上限 **70.4px** は `PULL_ARM × PULL_GIVE × PULL_RESIST` から**式で出る**）/ `HANG_T`(0.6)・`SWING_MAX`(0.38)・`STRETCH_MAX`(0.22)（`lib/pullDrag.ts`）。★★**ゴムの側は「指が引いた距離」**なので曲線4本・時間の4分割は使えない（あれは時間の語彙）。★★★**弾けたあとだけは時間**（`T_ITEM` をそのまま読む）―― ユーザー確定「**距離によって変わってるのは不自然**」。ゴムの式は `rubber()`（`lib/spring.ts`。**GravityTab の TIMELINE と同じ1本**）。★★ぶら下がりと伸び縮みは **`K_SWING`/`D_SWING`**（周期 ≒ 30フレーム・減衰比 ≒ 0.3。`K_TRAVEL` では**減衰が強すぎて揺れて見えない** ―― 実測で行き過ぎが 3%）。★★★**弾けたあと指へ追いつくのは `K_OPEN`/`D_OPEN`**（2026-09-18・第122巡にユーザー指摘「**弾けるように手に吸い付いてくるのが強すぎるので、もっと弱めて**」で `K_CATCH` から差し替えた。★★「強い」の正体は2つ … ① **4フレーム（67ms）で届く**＝ほぼ瞬間移動 ② **10.5% 指を追い越す**＝弾ける。`K_OPEN` は **7フレーム（117ms）・行き過ぎ 0.0%** なので**どちらも同時に弱まる**。★**揺れは支点まわりの振れ**（`mo.swing`／`K_SWING`）が持っているので消えない。★係数は5組のまま ―― **役の割り当てを変えただけ**）|
| **帯の他のピルの動き** | `components/home/bandMotion.ts` … `BAND_PAD`(`SPACE.sm`) / `NUDGE_MAX`(24。**帯ぜんたいのずれだけ**に掛ける) / `STOP_KICK`(4) / `BUMP_KICK`(7)。★★★**隙間と席は `transform` のずれでは書けない。レイアウト（`.band-pad` の幅）が持つ**（第113巡）―― 帯は**同じ id のピルが2周ぶん居る**ので、id で引くずれには**1周目と2周目に別の値を書けない**。実測 … 挿し込んだ瞬間に**2周目の先頭が 152px 飛んだ**（1周目は 3px）。★**両側 ±w/2 も片側 +w も同じ理由で直らない。復活させない。** ★★器なら**2周とも同じ列を描く**ので隙間も席も自動的に2周ぶん・同じ index に生まれ、**挿し込みでレイアウトが増える幅＝開けていた幅**なので受け渡しの計算が要らない（実測 飛び **2.0px**）。★★★**帯ぜんたいを `lead`（指の器より前で開いたぶん）＋ 隙間の半分だけ左へ戻す** ―― 戻さないと**指のそばの境目が画面の外へ逃げる**（実測 指 x=200 に対し隙間 405〜563）。★**この一様なずれは挿し込みの瞬間に「送り（px）」へ畳み替える。`currentTime` を直接ずらさない**（1周ぶんで折り返すので、古い幅で丸めた送りを新しい幅で読み直すことになる。実測 156px の飛び）。★★★**バネは役で分ける**（第111巡にユーザー確定「**跳ね返りは全部やめる／再開のときだけ軽い慣性**」）… **挿し口（`open`/`shut`）＝`K_OPEN`（速いのに行き過ぎない）／帯ぜんたいの一発＝`K_SWING`**。★★★**席（引き抜いた穴）を畳むときだけ `K_SETTLE`**（2026-09-18・第122巡にユーザー指摘「**引っ張り出した時に開く穴が閉じるスピードが速すぎて、ガクッと急に動いた感じがして、全く洗練された印象を受けない**」）―― **挿し口と席は速さの要求が正反対**で、挿し口は**指が居るあいだ（0.1〜0.3秒）に開き切らないと意味が無い**のに対し、席は**もう指が離れたあと**にピル1つぶん（150px 級）を畳む。117ms でやると「畳む」ではなく「消える」。`K_SETTLE` は **90% まで 467ms・行き過ぎ 0.0%** ＝ **跳ねないまま4倍ゆっくり**。★★**素の幅へ戻すとき（挿し込み・引き戻し）は `K_OPEN` のまま**（そちらは指がまだ居る）＝**向きで分ける**（`m.hole.to > m.hole.p`）。★**`K_SETTLE` は遅すぎる**（90% まで 467ms ＝ 指が離れるまでに 150px のうち 15px しか開かない）、**`K_CATCH` は 10.5% 行き過ぎる**。★★**`STAGGER`（追突のずらし）は第113巡に削除**（席はレイアウトなので後ろは剛体で詰む）。★★`stepBandMotion` は**実時間を貯めて固定の刻み**（段ごとの rAF が2つとも呼ぶので、**同じフレームの2度目は 0 歩**＝門を別に作らない。120Hz の2倍速も同時に消える） |
| **掴んでいる図形の代理の体** | `components/home/Pile.tsx` の `syncProxy`／`HANDOFF_MS`(300)。★**サーボの利得は 1**（`GRAB_K` を重ねると2極のフィルタになる）、**頭打ちは `THROW_MAX`(18)**（2つ目の数を作らない）。★`setMass` の**あとに** `setInertia(Infinity)`。★★**`piecesRef` に混ぜない**（代理は眠らないのでループが二度と止まらない） |
| **ピル ⇄ 図形の変形** | `stackOutline(rows, ar, waist)` の `waist`（0〜1。`lib/solid.ts`）。★**段の半幅を1段と n 段で lerp するだけ**なので点の数が変わらず、再標本化が要らない |
| **物理の当たり判定** | **絵より外へ出す量 `PHYS_GAP`(1px)**（`lib/solid.ts`。文字の板は `PLATE_PAD`／`PLATE_PAD_Y` も 1）／**等角に測り直す本数 `PHYS_VERTS`(12。★偶数)**。★★★**費用は「軸の本数 × 頂点数」** ―― 第104巡の「左右へ折り返す」は**細かい辺を大量に生んで最大 16倍**にし、着地でフレームが落ちた。**極座標で等角に測り直し、いちばん外の点だけ足す**（偶数本なら対称性は式が保証する）。★★★**渡す前に `atan2` で並べること** ―― 並べ忘れると多角形が自己交差し、**SAT が「当たっていない」と答えて床をすり抜ける**（第105巡） |
| **帯の物理っぽい動き** | `components/home/bandMotion.ts`（第109巡）… `NUDGE_MAX`(=`SPACE.xl` 24) ／ `STOP_KICK`(4 ＝ 行き過ぎ 11.5px) ／ `BUMP_KICK`(7) ／ `KNOCK_KICK`(6)。★★**バネは `K_SWING`/`D_SWING` 1組だけ**。★★★**帯の環境の動きは曲線4本の対象外**（すでに `linear` が例外。`components/home/Band.tsx` の頭）―― 慣性・追突・弾きも同じ側に置く。★★**実際の流れの速さ（0.25px/フレーム）は使わない**（1px にも満たず見えない）。**見える量を設計して置く。** |
| **提案の図形の当たり判定と塗り直す箱** | ★★★**`p.r` は「絵の半径」ではない**（第121巡）。絵は `traceCardShape(ctx, shape, p.r * 2)` ＝ **2r 四方いっぱい**なので、中心からいちばん遠い点は `p.r × cardShapeReach(shape)`（実測 … 四つ葉 **1.169**／六角形 1.116／**波打つ四角 1.292**／トゲトゲ 1.000）。★**指の判定は点の列で**（`inCardShape`。円で見ると出っ張りを押しても掴めず、へこみで掴めた ―― 実測の誤り 四つ葉 368/1440・波打つ四角 236/1440 → **0/1440**）／★**塗り直す箱にも同じ倍率を掛ける**（掛けないと波打つ四角で四方 13px 足りない）／★**物理の光線は `OFFER_VERTS`(28)**（`PHYS_VERTS`(12) だと波打つ四角の体が絵より **10% 小さい**＝隣とめり込む。**タスクは 12 のまま** ―― 費用は「軸の本数 × 頂点数」） |
| **札 → 角丸四角の変形** | `lib/cardMorph.ts` … 等角に測り直す本数 `RAYS`(144。★**偶数**)／角丸の弧の分割 `ARC_SEG`(14)／**半回転で着地する `MORPH_TURN`(180°)**（札の4つの形も角丸四角も**点対称**なので、始めと終わりの見た目が揃う）。★`components/explore/CardDetail.tsx` の版面 … ベゼル `SPACE.lg`／写真の高さ `PHOTO_VH`(0.58 ＝「画面の上半分より大きいくらい」)。★★★**作法は `lib/solid.ts` の `waist` と同じ ―― 点の数を変えずに半径を lerp する**（再標本化が要らない）。★★**曲線は `EASE_SETTLE` を `easeAt()` で引く。写経しない** |
| **端末が決める値** | `env(safe-area-inset-*)` を含む式（`TAB_PAD_TOP` / `NAV_BOTTOM_GAP` / `NAV_H` / `NAV_OFFSET`） |
| **外部APIの引数** | Leaflet の `fitBounds({ padding: [40,40] })` |
| **マスク** | `#000` は「色」ではなく「不透明」の意味 |

**ファイルまるごと図形の座標系**なので検査 #10 から外すもの …
`GravityTab` / `DriftTab` / `SolidCanvas` / `GeoType` / `TabIcons` / `LeafletMap` /
`Binder` / `TaskSpace`。

★**それ以外は、その行に理由を書く**（`// ★目盛りの外（…）` ／ JSX の中では `/* … */`）。
検査はこの印だけで除外する。**印の無い例外は例外ではない。**

---

## 8. 検査の書き方（過去に2度やった失敗）

★★**除外を「行ごと」にしないこと。** 行に他のトークンがあるだけで行全体を捨てると、
同じ行に同居した生の値を見逃す（#4 と #6 が実際にそれで取りこぼしていた）。
**トークンは数字で始まらない**ので、`プロパティ: 数字` を直接見れば行ごとの除外は要らない。

★★**値が「見えているか」で判定を分けないこと。** #5 は `プロパティ: 数字` の形しか
見ておらず、**三項の陰**（`marginBottom: x ? 22 : 0`）とテンプレの中の12件を見逃していた。

★★**突き合わせの検証は「比較できた件数」を必ず表示すること。** 移行前後の
computed style を突き合わせるとき、キーを「木の中の位置」で作ると、要素が1つ
増減しただけで全キーがずれ、**比較0件のまま「差分なし」と誤報する**（第66巡に
実際に起きた）。キーは**タグ＋中身＋文字サイズ＋同名の何番目**で作る。
