// ★★★**凍結した元データ（第21巡）。誰も import しない。絶対に消さない。**
//   22巡目に「パスのクリーンアップと最適化」を始めるにあたり、その直前の
//   座標データをそのまま保存したもの。整理でシルエットが崩れたときは、
//   ここへ戻せば必ず 21巡目の形に復帰できる。
//
//   由来 … tools/trace-nipper.mjs が部品ごとの平面図から生成
//   right 32辺 / left t0 17辺 / left t1 13辺
//
// （以下、当時の lib/nipperShape.ts のまま）
//   `node tools/trace-nipper.mjs <右の図> <左の図> <左の厚み図> <組み立て図> <out.json>`
//   が作る。形を変えたいときは**図を描き直して生成し直す**（目で数値を打ち込まない）。
//
//   右: f3c4e966-image.png／左: ff98e3f1-image.png
//   左の厚み: f2c479e8-image.png／組み立て: e47be4e8-image.jpg
//
//   ★★★21巡目に**部品ごとの図**から起こすようにした。16〜20巡目は組み立て図1枚から
//     割っていたが、そこでは**左の部品が右に隠れている**ので断片しか取れず、
//     2部品の**重なりが存在しなかった**（ユーザー指摘「重なる部分がおかしい」）。
//   ・右の面 23769px ／ 左の面 16496px ／ **重なり 4571px**。
//   ・厚みは左だけ塗り分け（薄い 7856px ／ 厚い 7965px）。右は一定。
//   ・バネは**組み立て図のインクから両部品を引いた残り**（1992px）。
//   ・線の太さ 2px の半分だけ太らせて、輪郭を線の中心へ置いた。
//   原点は**支点 × 全体の天** = 図の (210, 34)。1px = 2.2 単位。+y は上。
//   縦横比 0.7059（図の実測）。

/** 平面の点。★この表は**描き方から独立している**ので、ここで持つ。 */
export interface P2 { x: number; y: number }

/**
 * 辺。`r` があれば**円弧**（`c` は中心）、無ければ**直線**。
 * ★★20巡目に、なぞった点の羅列をやめて**直線と円弧の並び**にした。
 * ★`inner` は**段の境目**の印。そこは面取りしない ―― 面取りを回すと段差が
 *   坂に見える。印が**辺ごと**なのは、1本の辺がまるごと外の輪郭かまるごと境目の
 *   どちらかだから。
 */
export interface NipperEdge { to: P2; r?: number; c?: P2; ccw?: boolean; inner?: boolean }

/**
 * ★★**押し出す単位**。`tier` は 0=厚 / 1=薄。
 * **実際の厚みは `lib/nipperRig.ts` が決める**（ここは「どこがどの段か」だけ）。
 * ★右の部品は一定の厚みなので1枚。左は厚みの塗り分けで2枚に割れる。
 */
export interface NipperPiece { tier: 0 | 1; start: P2; edges: NipperEdge[] }

const L = (x: number, y: number, inner?: 1): NipperEdge =>
  ({ to: { x, y }, inner: inner === 1 });
const A = (
  x: number, y: number, r: number, cx: number, cy: number, ccw: 0 | 1, inner?: 1,
): NipperEdge => ({ to: { x, y }, r, c: { x: cx, y: cy }, ccw: ccw === 1, inner: inner === 1 });
const piece = (tier: 0 | 1, sx: number, sy: number, edges: NipperEdge[]): NipperPiece =>
  ({ tier, start: { x: sx, y: sy }, edges });

/** 右の部品（先端の箱と右の持ち手）。**動かない**。 */
export const NIPPER_RIGHT_PIECES: NipperPiece[] = [
  piece(0, 202.4, 0, [
    L(222.2, 0),
    L(235.4, -24.2),
    L(286, -202.4),
    A(288.2, -270.4, 180.2, 110.2, -242.9, 0),
    L(294.8, -321.2),
    A(387.2, -576.4, 2673.2, -2166.4, -1366.4, 0),
    A(411.4, -765.6, 1016.8, -605.9, -796.6, 0),
    A(402.6, -866.8, 275.5, 136.7, -796.1, 0),
    L(391.7, -873.4),
    L(378.4, -873.4),
    L(367.4, -860.2),
    A(347.8, -726.8, 558.4, 907.3, -718.8, 0),
    A(323.4, -594, 690.7, -340.3, -788.9, 1),
    A(222.2, -385, 1134.7, -745.9, -976.9, 1),
    A(145.2, -323.4, 217.1, 51.7, -519.4, 1),
    A(24.2, -303.6, 265.3, 45.2, -569.5, 1),
    L(2.2, -235.4),
    L(-48.4, -239.8),
    A(-66, -191.4, 123.3, 57, -175.4, 0),
    A(-68.2, -143, 95.9, 26.1, -161.8, 0),
    L(-26.4, -132),
    A(-46.2, -35.2, 251.6, 205.5, -29.3, 0),
    L(85.8, -19.8),
    A(132, -52.8, 72.8, 69.7, -90.7, 0),
    L(161.2, -151.5),
    L(175.6, -200.9),
    L(180.6, -217.3),
    L(191.4, -228.8),
    A(170.7, -142.3, 259.8, -67.7, -248.7, 1),
    A(151.8, -55, 229.8, 381.3, -47.5, 0),
    A(200.2, -2.2, 260, 364.5, -204.5, 0),
    L(202.4, 0),
  ]),
];

/** 左の部品（先端の板と左の持ち手）。★支点まわりに回る。赤の中へ挟み込まれる。 */
export const NIPPER_LEFT_PIECES: NipperPiece[] = [
  piece(0, -107.8, -286, [
    L(-81.4, -288.2, 1),
    L(-48.4, -323.4, 1),
    A(-44, -378.4, 93.1, -135.5, -356.8, 0, 1),
    L(-55.6, -391.7),
    L(-61.9, -408.5),
    L(-78.6, -440.5),
    L(-103.4, -488.4),
    L(-140.8, -594),
    A(-162.3, -746.3, 958.7, 795.1, -802.1, 1),
    A(-173.8, -882.2, 562.6, -725.6, -773.7, 0),
    L(-189.2, -899.8),
    L(-202.6, -901.8),
    L(-215.6, -897.6),
    A(-231, -842.6, 108.5, -122.4, -842.3, 0),
    A(-211.2, -580.8, 1211.6, 980.4, -802.2, 0),
    A(-107.8, -288.2, 1845.4, 1570.6, -1059.3, 0),
    L(-107.8, -286, 1),
  ]),
  piece(1, -8.8, 4.4, [
    A(81.4, -4.4, 291.3, 9.4, -286.5, 0),
    A(44, -283.8, 1288.1, 1333.3, -314.7, 1),
    A(17.6, -347.6, 138.4, -93.5, -265.2, 0),
    L(-11, -371.8),
    A(-55, -382.8, 94.8, -10.7, -466.6, 1),
    A(-57.2, -334.4, 91.8, -145.1, -361.5, 1, 1),
    L(-85.8, -303.6, 1),
    L(-110, -297, 1),
    A(-46.2, -44, 2800.9, -2789.6, 518.2, 1),
    L(-33, -11),
    L(-23.2, -2.3),
    L(-11, 2.2),
    L(-8.8, 4.4),
  ]),
];

/** 紙が入るスリット（右の箱の天で口が開く切れ込み）。 */
export const NIPPER_SLOT = {
  x0: 83.6, x1: 198, y0: -235.4, y1: -24.2,
};

/**
 * バネ。★★**輪は二重丸で描かれている**ので、放射に走査して帯を2本取り、
 * その中間を**芯の半径**、差の半分を**針金の半径**とする。
 * 18〜20巡目は内側の円を芯にしていたので、輪が 12% 小さかった。
 * 実測 … 内 28.15px ／ 外 35.77px（120 方向で一致）。
 */
export const NIPPER_COIL = {
  cx: 181.5, cy: -762.3,
  r: 70.3, wire: 8.4,
  /** 左の部品から輪まで（付け根 → 輪の縁）。 */
  legFar: [{ x: -25.5, y: -373.8 }, { x: 97.5, y: -754.4 }],
  /** 輪から右の部品まで（輪の縁 → 付け根）。 */
  legNear: [{ x: 282.5, y: -775.3 }, { x: 336.8, y: -597.3 }],
};

/**
 * ★支点。**2部品が重なっているところの下の端**（＝腕が分かれる蝶番）。
 * ★★部品ごとの図をもらって初めて測れるようになった（21巡目）。
 * 重なり 4571px・範囲 x 180〜243 / y 45〜171。
 */
export const NIPPER_PIVOT = { x: 0, y: -235.4 };

/** 道具全体の広がり（2つの面の和）。枠と接地を決めるのに使う。 */
export const NIPPER_EXTENT = {
  x0: -226.6, x1: 407, y0: -897.6, y1: 0,
};
