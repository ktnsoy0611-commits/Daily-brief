# Explore 刷新（確定版・2026-08-28）

いまの Explore を「提案」と「実行」の2本の柱に組み直す。
**生成の仕組み・データモデル・KEEP/SKIP の記録・今日を終える処理は一切変えない。**
変えるのは見せ方と操作だけ。

---

## 0. 言葉の方針（最優先）

- **画面に出る文字は一般語**。「提案 / ストック / マップ / プラン」。
- メタファー（鉄道・券・案内標）は**視覚だけ**で表現する。
  「本日の販売は終了しました」のような言い回しは使わない。
  空の状態は「今日の提案は以上です」程度の普通の文にする。
- 英字のラベル（`STOCK` / `VIA` / `TERM` など）はサインの語彙として使ってよい。

---

## 1. 場所

| 場所 | 画面の英字 | 券の縮尺 | 役割 |
|---|---|---|---|
| 提案 | `TODAY` | 原寸1枚 | 1件ずつ見て、残す／流す |
| ストック | `STOCK` | 縮小・格子 | 残したものを画像主体で見返す |
| マップ | `NETWORK` | 点（駅） | どこにあるか・どう行くか |
| プラン（重ねる） | `BOARD` | 行 | 今日の行程・プラン生成・保存したプラン |

- Explore のタブは**この3つ**（従来の4タブから減る）。プランは NETWORK に重ねる。
- 設定の入口は従来どおり右下の輪の SETTING。

---

## 2. 券 ── 唯一の共通部品

1件＝1枚の券。場所によって**縮尺だけ**が変わる。

### ★組み方の正は **Vitsœ のラベル**（2026-08-28 にユーザー確定）

参照は3枚（AuBe のポスター／**Vitsœ のラベル**／Grilli Type の名刺）。
3枚に共通しているのは次の5つで、券はこれに従う。

1. 地は**紙か、ベタ1色**。券は**その色の紙そのもの**になる。
2. **文字は全部黒**。階層は色ではなく**大きさだけ**が作る。
3. **太い黒バー**か**極細の罫**で区画を割る。箱で囲わない。
4. **巨大な要素が1つだけ**。券では**題**（ユーザー確定）。
5. 色は幾何形か全面のどちらか。**文字には使わない**。

### 版面（幅 300 / 比 13:21 → 高さ 485）

```
　　　　　　　　　　　　　　　　　　  ← 全面がドメイン色の紙（紙の目を焼いてある）
  EXHIBITION              Nº 0143    nano caps ／ 通し番号
  ──────────────────────────────     罫（黒・1px）

  ┌────────────────────────────┐
  │           写真              │     ★周囲に余白をとった四角（裁ち落とさない）
  └────────────────────────────┘

  見えないものたちの庭                ★主役。display(26)/bold、最大2行
  音と光だけで構成された9つの部屋を…    body(13)、最大2行

        （余白は題の上下へ 3:5 で割る）

  ──────────────────────────────     罫
  会期      12.02 – 03.16            ラベル(和 micro ＋ 欧 nano) ／ 内容(body)
  SOON
  会場      東京都現代美術館・清澄白河
  VENUE
  ∘ ∘ ∘ ∘ ∘ ∘ ∘ ∘ ∘ ∘ ∘ ∘ ∘ ∘ ∘    ミシン目（穿孔）
  EXPLORE                 ▮▯▮▮▯     半券
```

- **段は3つだけ** … 題 `display`(26) ／ 内容 `body`(13) ／
  ラベル 和 `micro`(9)＋欧 `nano`(7)。比は 2.0 と 1.44 で design.md §5-2 を満たす。
- **余白は題の上下へ 3:5 で割る。** 片側だけに逃がすと、写真が無い券で
  中央がぽっかり空く（3巡目に実際にそうなった）。
- 期限が近いことは**色ではなくラベルの語**で伝える（`PERIOD` → `SOON`）。
  有彩色は紙の1色しか使わない。
- ★題と要約は `flexShrink: 0`。縮むと `-webkit-box` が**字の途中で切れる**。

### 紙の色 ── 黒が載る明るい4色

券は色紙になり黒い文字が載るので、**暗い色は使えない**
（forest / violet / wine の上では黒が沈む）。`SCHEME` の明るい4色を当てる。

| ドメイン | 色 |
|---|---|
| バショ `place` | sky `#509BF5` |
| タイケン `experience` | pink `#F76FA1` |
| ジョウホウ `info` | yellow `#F5E837` |
| モノ `thing` | orange `#FA6E31` |

### 紙のテクスチャは**下地**に

`public/paper-kraft.webp`（図形が焼き込んでいるのと同じ1枚）を
**色のすぐ上・文字の下**に `mix-blend-mode: multiply` で敷く。等倍・繰り返しなし
（★縮小しない。紙の目は高周波なので消える）。
★最前面に `overlay` で乗せると**写真も文字も霞む**（2巡目に実際にそうなった）。

### 券種（データの出自）
| 券種 | ラベル |
|---|---|
| 提案 | 券種の欧文（`EXHIBITION` / `BOOK` / `FOOD` …） |
| 入力（自分で書いた／声から拾った） | `SELF ISSUED` |

★「自分で書いた」と「声から拾った」は**同じもの**。手動か AI かの違いだけ。

### 鋏痕（入鋏の痕）

★**券の縁を切り欠く**形。大きさは**券の幅の 13%**。
★★券が彩度の高い紙になったので、切り欠きは**地との差だけで読む**。

**形の正は旧国鉄の鋏こん一覧**（46種・2026-08-28 にユーザー指定）。
一覧を見ると、鋏こんは例外なく「**券の縁が形の上辺**で、そこから下へ食い込む」
形で、輪郭は**直線と単純な弧が2〜4本だけ**。この規律から外れると鋏こんに見えない。

| ドメイン | 形 | 一覧の出典 | 輪郭の性格 |
|---|---|---|---|
| バショ `place` | **アーチ** | #4 大井町 / #6 蒲田 | 弧 |
| タイケン `experience` | **台形** | #26 藤沢 / #27 洋光台 | 斜め |
| ジョウホウ `info` | **角** | 予備2 / 鶴見 | 直角 |
| モノ `thing` | **二又** | 品川 / #22 恵比寿 | 切れ込み |

★4つは**輪郭の性格が全部違う**ものを選んである。小さく描いても混ざらないのは
このため。形だけ差し替えないこと。
★割り当ては**4ドメインで固定**（ユーザー確定）。一覧は駅ごとに46種あるが、
鋏痕は**マップのノードの形**と**ストックの絞り込み**でも同じ意味を持つので、
細かく分けるとその約束が壊れる。

**鋏痕は3か所で同じ意味を持つ**（文字を使わない分類）:
1. 券の縁の切り欠き 2. マップ上のノードの形 3. ストックの絞り込みアイコン

### 入鋏は垂直でなくてよい

`TicketPunch` は `edge`（4辺）・`t`（辺のどこか）・`tilt`（傾き・度）を持つ。
**切りたい辺を、好きな位置に、好きな傾きで**切り欠ける（ユーザー確定）。

★物理的に正しく描くための仕掛け … 鋏痕の形の**口から外側へ板（`NOTCH_LIP`）を
伸ばして**おき、その塊ごと**口の中点まわりに回す**。斜めに入れば口はそのぶん
広がり、券の縁は左右とも直線のまま残る＝実物の斜め入鋏。券の外へはみ出た分は
券の `overflow: hidden` が切る。
★板を伸ばさずに回すと、**縁に三角の紙のヒゲが残る**。

★陰は**口を除いた輪郭**（`notchContour`）にだけ落とす。閉じた輪郭を縁取ると
**券の縁そのものにも線が乗り**、切れ目ではなく「置かれた図形」に見える。

---

## 2-b. 改札鋏（`components/explore/Nipper.tsx` ＋ `lib/nipperSolid.ts`）

★**SVG で描く**。CSS の 3D 変形は使わない（design.md 冒頭）。
★★9巡目に**ローポリ（面取りした多角柱の集合）＋フラットシェーディング**へ
作り替えた。8巡目までは「輪郭を掃引して階調を塗る」作りで、**面を持たない**ので
稜線が立たず、どこを直しても塗り絵に見えた（ユーザー評「すごくチープ」）。
**輪郭を描くのをやめ、立体を組んで面を落とす。**

### 立体（`lib/nipperSolid.ts`）

局所座標は **鋲（中央の関節）が原点／`+y` が上（刃先）／`+x` が右／`+z` が手前**。

- 断面は**八角形ひとつだけ**（角を落とした四角）。刃も柄も鋲もこれを押し出す。
  **曲線は一度も出てこない。**
- `extrude(駅, z の中心)` … 駅 `[y, 半幅, 半分の厚み, x の中心]` に沿って押し出す。
  駅の間の側面は四角形の面、両端は蓋。**面は外を向いた法線を持つ**。
- `bossZ(r, z0, z1)` … z 軸まわりの短い多角柱（鋲だけ y に沿っていないので別）。

### 彩色 ── フラットシェーディング（手で塗らない）

面の階調は**法線と固定光の内積を6段に量子化する**。グラデーション禁止。

- 光は**ほぼ真横（左）から、わずかに上・手前**。面の x の向きがそのまま段になる。
  正面 = 2（基本）／左を向く面 = 4／右を向く面 = 0。
- ★★段5（最も明るい）は**光へ正面から向いた面にしか出ない**ので、絞りの途中の
  面取りだけが光る。光を正面寄りにすると**正面も面取りも一緒に白く飛ぶ**
  （9巡目に実際に飛んだ。面積で測ったら 44% が最明の段だった）。
- ★階調は**動きで変えない**。回転角が小さいので、変えると段が飛んでチラつく。
- 色は `NIPPER_PAINT.ramp`（暗い順の6段）。**有彩色は使わない**（画面の有彩色は券の紙だけ）。
  指定は寒色のグレーだったが、**明度関係だけ保って色相をクラフト紙寄りへ寄せた**。

### 隠れ ── 裏の面を落とし、奥から手前へ描く

- 裏の面を落とす（この投影の視線は `(away.x, away.y, −1)`）。
- 残りを**平均 z の遠い順**に描く（画家のアルゴリズム）。この投影では奥行きは z そのもの。
- ★これで腕どうしの隠れが**自動で**正しくなる。8巡目に手で奥の腕を細くして
  誤魔化していた仕掛けは要らなくなった。

### 投影 ── 一点透視（7巡目に確定。**触らない**）

```
proj(p) = { x: p.x + p.z·away.x,  y: −p.y + p.z·away.y }
away = (鋏の位置 − 画面の中心) ÷ カメラの高さ
```

★★鋏は**掴んで画面内を自由に動かす**ので、この仕組みは必ず残す。
消失点は画面の中心で、**手前の面ほど中心から外へ逃げる**。だから置いた場所で
見える側面が入れ替わる（右に置けば明るい左面、左に置けば暗い右面）。

### 形 ── 縦置き（刃先が上・持ち手が下）・シザー型

```
   ▲ ▲     刃    y +80〜+258   顎から薄く終える
   ┃ ┃     顎    y   0〜 +80   四角い塊
    ╳      鋲    y     0       ★ここで腕が交差する
   ┃ ┃     首
  ┃   ┃    柄    y −390〜−100  ペンチのように太い
  ┃⌇┃     バネ  y  −250      柄のあいだ
```

- ★**内側の縁は鋲を通る直線**（`x = JAW · y`、`JAW = 0.062`）。この直線でないと、
  回しても刃が**全長で噛み合わない**（先だけ当たる）。
- 閉じ切る角は `2·atan(JAW)` ＝ 約 7.1°。**開きの角度がそのまま閉じる角**。
- 2枚の刃は**厚みの方向（z）に ±13 ずらして並ぶ**。だから交差できる。

### 群と描画順（4つ）

`shadow`（床の影）→ `left_part`（固定部：左の柄・左の刃・鋲の台）→
`right_part`（可動部：右の柄・右の刃・鋲の頭）→ `spring`（バネ）。

- 影は**ぼかさない**。輪郭だけの多角形を**左下へ14枚ずらして掃く**。
  ★重ね塗りで濃淡が出ないよう、**面は不透明の単色・群に1度だけ透かす**。
- バネは**角ばった4折れの帯**。前後の折れを z で分けてあるので、画家の順で
  勝手に絡んで見える。

### 動き ── Framer Motion は使わない

指定は `framer-motion` の `type: "spring"` だったが、design.md 冒頭で禁止
（未インストール）。**`lib/spring.ts` の減衰振動＋rAF で同じ物理を作る**
（`stiffness` ＝ 引き寄せ `k`、`damping` ＝ 減衰 `d`）。★係数は既存の4つだけ。

| 局面 | 係数 | 手ざわり |
|---|---|---|
| 閉じる（入鋏） | `K_TRAVEL` / `D_TRAVEL` | わずかに行き過ぎる＝**ガチャンの衝撃**（7.13° > 7.10°） |
| 戻る | `K_SETTLE` / `D_SETTLE` | 行き過ぎずに復帰 |

- 毎フレーム **React を再レンダーしない**。ref に持った群の `transform` を直接書く
  （`right_part` は鋲を軸に `rotate`、`spring` は左の柄側を支点に横へ `scale`）。
- ★JSX 側の `transform` は **`want` ではなく「いまのバネの値」**で書く。`want` で
  書くと、押した瞬間の再レンダーで**終わりの姿勢へ一度飛んでから**動き出す。

### 見えないもの

★**穴を開ける部分（ダイの窓）は描かない。** この画角では物理的に見えない
（7巡目まで、それを見せようとしていたのが形の歪みを固定していた）。

## 3. 提案（TODAY）

- 券が1枚。右下に**改札鋏**（SVG で立体的に描く。CSS の 3D 変形は使わない）。
- 鋏をドラッグして券に近づけると**一番近い縁に吸い付き**、離すとそこが切り欠かれる＝残す。
- 券は落ちて次が出る。何もしなければ送って流す。
- タップで券が伸びて詳細。
- 下端に**束の厚み**が見えていて、入鋏した券がそこへ落ちる。

## 4. ストック（STOCK）

- 入鋏済みの券が格子に並ぶ。**画像主体**、数百件に耐える。
- 絞り込みは**鋏痕の形**。
- 会期が近い券は縁が変わり、切れた券は落ちる。
- 券をタップ＝原寸に戻る。

## 5. マップ（NETWORK）

- 目標とする見た目は Vignelli の NYC 路線図系：
  **太い等幅の線が平行に走り、角は大きな円弧、駅は黒丸、乗換は白い抜き。**
- ノードの形＝鋏痕。1駅に複数件が付く場合に対応する。
- 駅をタップ→券のピンが立つ。**中央ほど大きく写真つき、端ほど小さい（LOD）**。
- 絞り込むと図が**必要な分だけに変形**する（レイアウトは決定的。前後を補間）。
- **県が違えば別の島**にして破断線で繋ぐ（距離では判定しない）。

## 6. プラン（BOARD・重ねる）

- 下から引き上げるオーバーレイ。
- 今日の行程／プラン生成（3案）／保存したプラン。
- 3案は図の上に薄く重なって見比べられる。

---

## 7. デザインの制約（design.md より）

- **CSS の 3D 変形（`perspective` / `rotateX` / `rotateY`）は使わない。**
  立体感は SVG / canvas で「描く」。
- Framer Motion・`layoutId`・Tailwind も使わない。
- 券の紙は既存の `PAPER`（中性のグレー）。第33巡で暖色のクリームは廃止済みなので
  戻さない。紙らしさは**地紋と繊維の目**だけで出す。
- 余白・文字・角丸は `lib/tokens.ts`、色は `lib/constants.ts` から引く。
  図形の座標は「目盛りの外」として印を付ける。

---

## 8. 実装の順番

1. ~~**券の部品**（券面・券種・鋏痕・紙）＋ 開発用の確認画面~~ 済
2. ~~券と鋏を作り直す（§2 の4条件・§2-b の3条件）~~ 済
3. 提案（TODAY）── 入鋏の操作、券が落ちる、次が出る ← いまここ
4. ストック（STOCK）── 格子・絞り込み・期限
5. ここで一度実機で確認（提案側が完成する）
6. 駅データを用意し、NETWORK のレイアウトエンジン
7. BOARD を作り、既存のプラン生成を移す
8. 券種の残り（記事・継続）

---

## 9. 未決

- 場所ごとに地の色を変えてよいか（`design.md` は「地色は列が持つ」）。
  ★提案（TODAY）の地を少し暗くすると**切り欠きがはっきり読む**。要判断。
- 提案とストックを1画面にするか、2つに分けるか
- 駅データ（ekidata）の同梱可否
