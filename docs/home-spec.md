# ホーム画面 実装仕様書（2026-09-06 確定 ／ 第18〜33便の結論）

このファイルだけ読めば実装に入れるように書いてある。
経緯と却下案は `docs/redesign-ideas.md`、参照の分析と測定値は `docs/redesign-references.md`。
**UI の規約は `design.md` が正**で、この仕様はそれに従う（衝突したら `design.md` が勝つ）。

---

## 0. これは何か

**ホーム** ―― 起動時に最初に出る画面。3つのアプリ（EXPLORE / TASK / JOURNAL）の**入口**であり、
3アプリの枠を越えて情報が一元化される場所。

**主題は時間管理ではない。** 「AI が先回りして、いま自分にとって最適なものを差し出す」場所。
仕事も週末も余暇も、同じ種類の提案として扱う。

---

## 1. 置き場の規則（★これ1本で全部決まる）

> **日付が無いものは帯（上）。日付が付いたものは山（下）。それだけ。**

- 提案・フォローアップ・声から拾った候補・いつかやる ―― **ぜんぶ「まだ日付が無い」で一致する**ので
  4種類ではなく1種類。置き場は帯ひとつ。
- 山にいるものは**全部「今日」**。だから山は説明が要らない。
- ★**日付を聞く正規の入口は「帯から下ろす手つき」1つだけ。** 編集画面でも変えられるが、
  入口を増やさない（増やすと「どこで決めたか」が散る）。

### 日付は2つある
| | 意味 |
|---|---|
| **帯の日付** | **見つけた日**（今日）。日付は中身ではなく**帯そのもの**が持つ |
| **山の日付** | **やる日**（今日） |

だから下ろす手つきは**2つの決断を同時にしている**（引き受ける ＋ いつ）。

---

## 2. 配色（★2026-09-17・第118巡が最新）

★★★**タスクの図形＝無彩色のグレー `#BFBBB2`（`TASK_GREY`）＋ 黒の字**／
**ドメイン4は「色と形の組」**（バショ＝緑・四つ葉／タイケン＝赤・六角形／
ジョウホウ＝青・波打つ四角／モノ＝黄・トゲトゲ）／**JOURNAL ＝ ラベンダー
`#B2AFCE`**（4色の原色寄りの列に入らない＝別枠）。表と実測は `design.md` §3。

### 2-z. 旧稿（2026-09-06 ユーザー確定）

出どころ … 参照画像 `SKY / STORM / SEA / CREAM / ROSE` のパレット
（`tools/extract-palette.py` で画素から測定。**印字 HEX との差は最大 1**）。
そこへユーザー指定で**墨（黒寄りの soft black）**を1つ足す。

### 2-a. 値

```ts
// lib/constants.ts ―― PALETTE の6行をこれに差し替える
export const PALETTE = {
  sky:   "#C3BFE0",  // SKY   ── 藤
  storm: "#858AC2",  // STORM ── 青紫
  sea:   "#144474",  // SEA   ── 紺
  cream: "#FEE1D1",  // CREAM ── 生成りの桃
  rose:  "#E15442",  // ROSE  ── 珊瑚
} as const;

export const WHITE = "#FCF7F4";  // 地。★参照の中の白（純白ではない）
export const INK   = "#23252B";  // 墨。★黒寄りの soft black（純黒ではない）
```

**`INK` は既存の `#2C2627`（暖色）から `#23252B`（寒色）へ変える。**
パレットが寒色に寄ったので、暖色の墨だと墨だけが浮く。
より柔らかくしたい場合の代案は `#2A2C33`（白地との比 13.11。`#23252B` は 14.41）。

無彩色の段（`MUTED` / `SECOND` / `SURFACE` など）は **`WHITE` と `INK` の混色で導出する**。
表に書かない ―― 書くと墨を替えた人が片方だけ直す。

### 2-b. 実測のコントラスト比（`#23252B` 墨・`#FCF7F4` 地）

| 面 | 墨を載せる | 白を載せる | 面として地の上 |
|---|---|---|---|
| WHITE `#FCF7F4` | **14.41** | — | — |
| CREAM `#FEE1D1` | **12.33** | — | 1.17 |
| SEA `#144474` | 1.54 | **9.37** | 9.37 |
| STORM `#858AC2` | **4.68** | 3.08 | 3.08 |
| SKY `#C3BFE0` | **8.64** | 1.67 | 1.67 |
| ROSE `#E15442` | **4.05** | 3.56 | 3.56 |

★★★**ROSE だけが AA（4.5）に届かない（4.05）。**
ROSE の面に載せる和文は **`TYPE.lead`(16) ／ `WEIGHT` 700 以上**とし、
その行に `// ★目盛りの外（ROSE の比 4.05。面としての役が要るため）` を書く（`design.md` §6）。
**ROSE に `TYPE.body`(13) 以下を載せない。**

### 2-c. 色を出してよい場所（★これ以外は白と墨だけ）

| 出る | 出ない |
|---|---|
| 帯の**上段**（提案のカプセル・急ぎのカプセル） | 帯の中段・下段 |
| 山の**提案の丸** | 山の**タスクの四角** |
| 山の**未読のバッジ** | タブバー |
| 写真のデュオトーン | 持ち物のアイコン |

★これで**骨格が色から独立する**（配色を替えても構造が動かない）。

### 2-d. 役 → 色

| 役 | 色 |
|---|---|
| 急ぎ（フォローアップ） | **ROSE** |
| 提案 ── 1枚目 | **STORM** |
| 提案 ── 2枚目以降 | **SKY** → **SEA** の順に循環 |
| 未読のバッジ | **ROSE** |
| 2つ目の明るい面（中段のピル・タスクの一部） | **CREAM** |
| 大きな英語（SUNDAY） | `mix(WHITE, INK, 0.13)` ＝ 地より少し濃い灰 |

---

## 3. 画面の構造（★上から下への並びだけ。寸法と位置は `design.md` に従って決める）

1. **小さな見出し** … `HOME ／ 06 SEP ／ SUN`。★**大きな英語はここに置かない**（§8）
2. **持ち物のアイコン ＋ 数字のバッジ**（画面の右上に固定）
3. **帯 3段**（§4）── 互い違いに逆方向へ流れ、**左右とも画面の外へ切れる**
4. **山（GRAVITY）**（§5）── 大きな英語（SUNDAY）／未読のバッジ／タスクの四角／提案の丸
5. **タブバー** … `EXPLORE ／ TASK ／ JOURNAL` ＋ 右端に作るの丸

**左右のパディングはページ最上位の器だけが持つ**（`design.md`）。
★**帯だけは例外で、器のパディングの外へはみ出す** ―― はみ出しが「まだ続きがある」を言うため。
その行に `// ★目盛りの外（帯は器の外へ出る。続きがあることを形で言う）` を書く。

## 4. 帯（上）

### 4-a. 段 ＝ 出どころ（★★★2026-09-17・第119巡が最新）

**段は2つ**（`lib/homeBand.ts` の `BAND_ROW` が正）。

| 段 | 中身 | 見た目 |
|---|---|---|
| **0（上）** | **ストックの中で「今日これから行ける」もの**（`today-`。`lib/todayPick.ts`） | **写真つきの太いピル**。★**塗り**（日付を持つ側） |
| **1（下）** | 声から出た候補（`voice-`）／フォローアップ（`follow-`）／期限なしのタスク（`someday-`） | **縁だけのピル**。塗られていない＝時間を割り当てていない |

★★★**未読の提案（`offer-`）は帯から外した**（第119巡にユーザー指定
「**帯に流れるのはすでにストックしてあるものの中で今行けるもの**」）。
**提案は山へ落ちる**（§5-d のおすすめ3件）。★`BandKind` の `"offer"` は
**山から帯へ運んで KEEP する道**が同じ id の形を使うので型としては残っている。

★**塗り／輪郭の合図は「日付があるか／ないか」の1つだけ**（第93巡。`isOutlined`）。
★★**旧稿の3段（AI／声／いつかやる）は死文**。段を増やさないこと。

### 4-b. 大小の関係（★具体的な値は `design.md` の段から引く）

- ★**上の段ほど厚く、濃く、速い。** 3つの規則が同じ向きを向いているので説明が要らない。
- 段1のカプセルは**段2・3のピルより明確に厚い**（第26便で「もっと大きく強調」と指定）。
- 段1の写真の丸は**カプセルの高さいっぱい**、`border-radius:50%`、左端に密着。
- 文字は段1 > 段2 = 段3。**すべて `TYPE` の段から選ぶ**（`lead`(16) / `body`(13)）。
- 段3の縁は 1.5px、色は `mix(WHITE, INK, .30)`。

### 4-c. 動き（★確定）
- **自動でゆっくり流れ続けて循環する。**
- **段ごとに向きが互い違い** … 段0 = 左へ ／ 段1 = **右へ**（`direction: "reverse"`）。
- 一周 … 段0 **44秒** ／ 段1 **58秒**（★3段だった頃の 72秒 の段はもう無い）。
- **左右とも画面の外へ切れる**（＝まだ続きがある、を形で言う）。
- 実装 … 中身を**2回並べて** `translateX(-50%)` を線形で無限に。逆向きは `animation-direction: reverse`。
- **指で触れている間は止まる。離すと流れ出す。**
- `prefers-reduced-motion: reduce` で停止。
- ★代償 … 動くものは目を引き続けるので、速度は**1秒に 8px 前後**（読めるが追わなくていい）まで
  落としてある。実機で速ければ**さらに半分**まで落とせる。

### 4-d. 操作
- **タップ** … 詳細へ（編集できる）。
- **掴んで下へ** … 日付を割り当てて山へ落とす（§6。**実装済み**）。
  ★**右端へ寄せれば ASSIGN の帯**が出て、離すとカレンダー（§6-d）。
- ★**ピルに印（アイコン）を付けない。** 列に印が並ぶと**印が「模様」になって印でなくなる**。

---

## 5. 山（下 ／ GRAVITY）

### 5-a. 山は何か（★確定）
> **上の帯 ＝ まだ自分の時間を割り当てていないもの**
> **下の山 ＝ 自分の時間を使うと決めたもの**

展覧会も**入る**（行くと決めた時点で「自分の時間の使い道」だから）。
**違いは分類ではなく形が言う。種類でレーンを分けない。**

### 5-b. 形 ＝ ジャンルの札（★意味づけはしない）
| 形 | 中身 | 備考 |
|---|---|---|
| **ピルの積み**（段の数＝題の文字数・最大3段） | タスク | **文字が組める唯一の形**。★**1件が1つ**（まとめない）。★★第99巡に「辺の数＝埋まり具合」から替えた。`lib/solid.ts` の `stackOutline` |
| **札の形**（4つ。四つ葉／六角形／波打つ四角／トゲトゲ） | 提案（今日行くと決めたもの） | ★**写真を入れられるのはこれだけ**。形は**ジャンル**が決める（`lib/cardShape.ts`。BRIEF の札と同じ1か所） |
| **ギザギザの円**（12頂点・内半径 0.40） | 未読の提案の枚数 | **1画面に1つだけ**。0 になると消える |
| **カセット**（青い本体＋墨のリール2つ＋黒い下の段） | まだ声を録っていない日 | ★★★**JOURNAL のタブのアイコンそのまま**（第94巡に円から替えた）。**文字は載せない**。寸法は `lib/cassette.ts`・リールの芯は `lib/reelHub.ts` ＝ `VoiceStudio` と同じ部品 |
| **大きな英語** | 今日の日付と曜日 | ★**2枚の板として山と一緒に落ちる**（2026-09-07 ユーザー指定で「版面の柱」から変更）。作りは `lib/wordPlate.ts` ＝ GRAVITY と同じ部品 |

★★★**塗り分けは「日付があるか／ないか」の1軸だけ**（2026-09-12・第93巡）。
**日付あり＝メインカラーの塗り／日付なし＝輪郭線**（中は地と同じ色、縁と字は
メインカラー）。**帯のピルと図形で同じ言い方**にしてあるので、帯から掴んで
日付を割り当てた瞬間に塗りへ変わる。★タグは廃止した。

★**形に物語を持たせない。** 持たせた瞬間に当てはまらないものが出る
（「返事待ち」は義務か誘いかで詰まった）。★**ジャンルが増えたら色で分ける。形は増やさない。**
半円・八角・16角・アーチ・封筒・時計は**すべて却下済み**。

### 5-c. 積み方
- ★★★**大きさの正は §7-c**（第116巡に「段の高さ」1つへ変わった）。
  **以下の「大きさ ＝ 重要度」は TASK（GRAVITY）にだけ残っている死文**。
- **大きさ ＝ 重要度**（文字数ではない。ユーザーが決めた重要度）。
- **傾きは 3〜6° だけ**。★**0° だと格子になって「積もった」に見えず、10° を超えると漫画になる。**
- 図形の中の文字は**左上に寄せる**。★**中央揃えにした瞬間にバッジ・シールになる**（`design.md` の版面の規則）。
- 物理は既存の matter.js（`components/tabs/GravityTab.tsx`）を使う。
- ★★★**器は GRAVITY とまったく同じ箱**（2026-09-11・第92巡）。左右は
  `lib/pileBox.ts` の `PILE_INSET`（＝タブバーと同じ 16px）、床は
  `floorYOf(h) = h − navHeightPx() − GROUND_LIFT`。
  ★★**この式は「器が画面の底まで伸びている」前提**なので、山の器には
  **`.bleed-x-b`**（左右 ＋ 下のタブバーぶん）を付ける ―― タブの器は
  `paddingBottom: var(--nav-h)` を持つので、付けないと**タブバーの高さが二重に
  引かれて**床が浮く（実測 … 実機で 164px。狙いは 32px）。
  ★**式に旗を足して逃げないこと。器を揃えるのが正。**
- ★★★**器が変わったときの約束は3つ**（2026-09-14・第103巡。ユーザー報告
  「図形が地面の下に落ちる」「横画面にして戻すと図形が消える」の根治）…
  1. **床が動いたら壁を作り直す。** 床は `navHeightPx()` に依存するので、
     **器の寸法が変わらなくても動く**（`env(safe-area-inset-bottom)` が
     遅れて 0→34 になる）。`ResizeObserver` だけでは足りない。
  2. **床が上がったぶんだけ山ごと動かす**（`refitPile`）。床の板は 200px と厚く、
     板の中に入った図形は**いちばん浅い軸＝下へ**押し出されて抜ける。
  3. **器が大きく変わったら大きさを決め直し、外へ出た図形は拾い直す**
     （`isLost` → `respawn`）。器には天井も底も無いので、一度外へ出た図形は
     **二度と戻らない**。★山が空にならない限り入れ直しの番人も撃たない。
  4. ★★★**沈んだだけなら「その場で床へ戻す」**（`sink`。第104巡）。
     **上から落とし直さない** ―― 沈む原因が残っているかぎり
     「沈む → 降る → また沈む」を繰り返し、**雨のように見える**。
  5. ★★★**物理の歩数は実時間から決める**（第104巡）。`requestAnimationFrame` ごとに
     1歩だと、**120Hz の実機で物理が2倍速**になって図形が床を突き抜ける。
  6. ★★★**重さの目盛りは1つ**（第104巡）。**`setMass` を呼び忘れた体だけが
     matter の既定の密度（px²）になり、他の全部（solid²）と桁が違う。**

### 5-d. ★★★おすすめ3件が山に落ちる（2026-09-17・第119巡にユーザー指定）

> 「**その日とか、その時の Explore で溜まっている（もしくは新着の）もののうち
> 最もおすすめなのを3件ほど抽出し、ホームに落とすようにしたい。それをタップすると
> Explore のそのカードに飛べるように**」
> 「**おすすめ3件は時間とか関係なく私が一番好みそうなものを選べばよい**」

- **選び方は `lib/offerPick.ts` の `pickOffers(state, 3)` の1か所**。
  ★★★**AI を使わない**（`lib/todayPick.ts` と同じ理由 ―― 材料はクライアントに
  全部あり、夜間生成が止まっていても効き、嘘をつかない）。
- **点の付け方**（重みは同ファイルの定数が正）… **願いに応えたか `W_WISH`(3.0) ＞
  ゴール由来か `W_GOAL`(2.0) ＞ 好みの語に当たったか `W_INTEREST`(0〜2.0) ＞
  ドメインの KEEP 率の差 `W_HISTORY`(±1.0) ＞ 新着 `W_FRESH`(0.6)**。
  ★★**同じドメインは `PER_DOMAIN`(2) 枚まで**（3枚が同じ色になると「おすすめ」に
  見えない）。★**時間帯は見ない**（ユーザー確定。`todayPick` とはここが違う）。
  ★KEEP 率は `PRIOR`(4) で均した比なので、**履歴が薄いうちは効かない**。
- **山での見た目** … 提案は**円**（`OFFER_D`）で、面は**ドメインの色**、
  中は**札の形にマスクした写真**（`lib/cardShape.ts`）。
- **押すと Explore のそのカードへ** … `Piece.nav`（`TabId`）に加えて
  **`Piece.card`（カードの id）**を持たせ、`AppShell.goTab(tab, card)` が
  `focusCard` を立てる。`BriefTab` は `focusCard` を**次に出る札の位置へ差し込む**
  （`pinned`）。★**デッキの並びを永続化しない**（見えている間だけの割り込み）。
- **帯へ運ぶと KEEP** … 山の提案を帯へ戻すと `lib/keepCard.ts` が `Item` を作る
  （＝ストックへ入る）。★**カード → `Item` の道はこの1本だけ**（`BriefTab` の
  KEEP も同じ関数を呼ぶ）。

### 5-e. ★★★Explore の詳細の画面（2026-09-18・第120巡が最新）

> 【第119巡】「**Explore のカードの画面で、画像のところをタップすると、画像のマスクの
> 形が回転しながら拡大して、画像が画面全体のような画面アニメーションして遷移する**」
>
> 【第120巡・**上を撤回**】「**写真部分をタップすると、マスクの枠だけが回りながら
> アニメーションし、画像の表示領域が大きくなっていきます。広がるのですが最終的に、
> 画像の周りに色のついたベゼルが残るようにし、画面的には画面全体そのジャンルの
> ベタ塗りに、角丸の四角でマスクされた写真が画面上半分より大きいくらいに大きく
> 表示され、下に詳細文がかかるような形です。（…）画像を大きくしすぎると粗が
> 目立つのでそれの対策でもある変更です**」

- **着いた先の版面** … 画面いっぱいが**そのジャンルの色**（`colorOfKind`。押した札と
  同じ色）。その上に**角丸の四角でマスクした写真**が、四方 `SPACE.lg` のベゼルを
  残して**画面の高さの 0.58**。**下に詳細文**（指で送ると写真ごと上へ抜ける）。
- ★★★**「画面いっぱいへ広げる」は第119巡で作って第120巡に撤回した。復活させない。**
  理由は版面ではなく**素材** ―― 写真は `img()` 越しの 1000px 級なので、390×844 の
  実機（dpr 3）で全画面にすると足りない（ユーザー「**粗が目立つ**」）。
  ベゼルを残せば 358×490 ＝ デバイス 1074×1470 で釣り合う。
- **遷移** … **札の形のまま、枠だけが回りながら角丸四角へ変わり、同時に育つ**。
  **写真は回らない**（器がまっすぐ育つだけ）。地の色は同じ進みで濃くなる。
- ★★★**変形は `lib/cardMorph.ts` の1か所**。**両端を「中心から等角に測った半径の
  列」に直して lerp する**ので、**点の数が変わらず再標本化が要らない**
  （`lib/solid.ts` の `waist` と同じ作法）。★**回るのは「測る向き」**。
  ★★**半回転（180°）で着地する** ―― 札の4つの形も角丸四角も点対称なので、
  始めと終わりの見た目が揃う。
- ★★★**着いたら「ただの角丸四角」に戻す** ―― 最後のフレームの `clip-path` は
  行き先の角丸四角そのものなので、`border-radius` の `<img>` と**入れ替えても
  継ぎ目が出ない**。入れ替えたあとは普通に流れる版面になる。
- ★★**本文は `BriefCard.detail`**（`briefPipeline` がスクレイピングから生成済み）。
- ★★★**`createPortal` で `body` 直下へ**／★★★**`--nav-h` を読まない**
  （どちらも第119巡に踏んだ。理由は `design.md` §4）。

---

## 6. 下ろす手つき ── 日付の割り当て（★★★2026-09-14・第102巡に確定・実装済み）

★★★**K1「一週間が地面から7本の柱になる」はやめた**（ユーザー確定）。
柱は**遠い日が選べない**のが致命で、しかもカレンダーの盤は
`components/tasks/WhenSheet.tsx` に**すでに戦い抜いた実物がある**。
**柱を作るより、その盤を持ち上げて使い回すほうが正しい。**
★K2（点の集まり）・K3（下から暗いシート）も**採らない**。**この節が正。**

### 6-a. ユーザーの説明（★これがそのまま仕様）

> ピルを触ると**少し柔らかいような感触**がして、少し下に引っ張ると最初は**少しゴムの
> ような反発**する感触がして、そのまま引っ張っていくと**ピルの形から、ホームに落ちて
> くる時の図形の形に変化しながら滑らかにアニメーションして指に吸い付いて**くる。
> そのまま**ホームのところで指を離すと、日付が今日になってシステム的にも今日に
> 割り当てられたことになって、そのままホームに落とされます**。
> **別の日付を割り当てる場合**は、掴んだまま**画面の右端に近づけると右端から黒い帯が
> 出てきて、「ASSIGN」と書いてあります**（帯の出方もベジエ曲線でスムーズに）。
> そこで**離すとカレンダーの画面**が出てきて、上部に**今日・明日・来週**のボタン、
> カレンダーは**横にスワイプ**でき、**割り当てたい日付をタップすると登録が完了**。
> また**ホームの図形も長押しで掴んで自由に移動**させられるようにして、**同じ操作**
> （右端に近づけたときの動き）が行えるように。

### 6-b. 状態機械（★一本道。分岐は「離す先」だけ）

| 段 | 何が起きるか | 出どころ |
|---|---|---|
| 触る | ピルが**わずかに沈む**（`scale(0.97)`） | `--t-press` / `--ease-press`（**押下だけ非対称**） |
| 1px でも下へ | ★★★**そこから先は canvas の写し取り**（DOM のピルは消える。ただのタップでは切り替えない） | `pillGhost.drawPillGhost()` |
| 少し下へ（**〜44px**） | ★★★**両端は帯に残り、掴んだ一点だけがゴムのように垂れる**（＝**輪ゴムを引っ張る**）。★★**垂れは指と 1:1**（第107巡。実測 … 指を 6px 下げるごとに +5.5〜6.0px）。上限 **70.4px** | `bendAt()` / `PULL_RESIST`(1.0) / `BEND_TOP`(0.55) |
| **44px**（＝ピル1つぶん）を越える | ★★★**ばちん** … ピルが図形になり、**指へ「一気に、でも滑らかに」追いつく**（★瞬間移動しない。実測 … 28px を **4フレームで届き、10.5% 行き過ぎてから戻る**）＋`haptic(12)` | `K_CATCH`/`D_CATCH` ＋ `SNAP_POP` |
| **弾けてから 450ms** | ★★★**ばね一発** … くびれが**一息に開き、8% 行き過ぎてから収まる**（実測 … 150ms で開き、224ms が山、450ms で完全に静止）。**段は1つずつ生えない**（第107巡にユーザー確定「**ぽこぽこは気持ちよくない**」） | `K_CATCH`/`D_CATCH` ＋ `Ghost.waist` |
| 引いている間ずっと | **指の下にぶら下がって揺れる**（指が右へ動けば図形は左へ残り、止めると揺り返してから静かに戻る） | `K_SWING`/`D_SWING`（`lib/spring.ts`） |
| 帯へ戻しているあいだ | ★★★**位置は最後まで指に 100% 追従する**。行き先は**帯に開く隙間**と、**図形が少しだけピルへ戻ること**（`tTo = PILL_HINT` 0.55）が言う。**離して初めて `tTo = 0`** | `Ghost.aim`（**boolean**）／`PILL_HINT` |
| 同上 | **引く速さで伸び、止めると一度潰れて戻る**。★**面積は保つ**（`sx·sy = 1`）ので大きさは変わって見えない | 同上 |
| 右の縁から **36px**（★第120巡に 72 から） | 右端から **ASSIGN の帯**が滑り込む | `RAIL_NEAR` ／ `--t-in` / `--ease-sheet` |
| ホームで離す | **今日**が入って**そのまま山へ落ちる** | `dueDate` / `plannedFor` に今日 |
| 帯の上で離す | **カレンダーの画面**（`AssignSheet`） | ― |
| 途中で離す | **何も起きない**（帯へ戻る。ユーザー確定） | ― |

★★★**「かなり帯に近づいたら滑らかに吸い付く」は第109巡に採り、第118巡に
ユーザーが撤回した。復活させない。** ―― `aim` が絵の座標を奪うので、
**指を離していないのに縦に動かなくなる**（「**強制的に帯の列に吸い寄せられて
横にしか動かなくなる**」）。真因と実測は `docs/project_knowledge.md`。

★★★**進みは「時間」ではなく「指が引いた距離」で決まる。** だから曲線4本
（`lib/motion.ts`）を使わない ―― あれは**時間**の語彙。ここは物理と同じ
**目盛りの外**（`design.md` §6）。摘みは …
`PULL_ARM`(**44** ＝ `BAND_H.plain`) / `PULL_RESIST`(**1.0**) / `PULL_GIVE`(1.6) /
`BEND_TOP`(0.55)。
★★★**弾けたあとは「ばね1本」**（`K_CATCH`/`D_CATCH`）。
★★★**削除して復活させないもの** … `MORPH_SPAN`(距離・第105巡) ／
`MORPH_STEPS`(線形の時計・第106巡) ／ `shownRows`(段の階段) ／ `STEP_POP` ／
**`SNAP_POP` と `pop`**（★バネを2本掛けると**面積が 1.2秒 揺れ続けた**。
**一発 ＝ ばね1本**）。
★垂れの上限 **28.8px** は `PULL_ARM × PULL_GIVE × PULL_RESIST` から**式で出る**
（生の上限を別に置かない）。
★★★**第103巡の 56px・42% は「最初の抵抗が強すぎる」と差し戻された**（第104巡）。
**引き始めが死んでいると、そのあと何をしても取り返せない。**
★★★**第104巡の「ピルを `scaleY` で伸ばす」（`PULL_TAUT`）は第105巡に廃止した。
復活させない** ―― **全体が一様に伸びる**ので「掴んだところが曲がる」にならず、
しかも `.band-row` の `overflow: hidden` の外へ垂れられなかった（ユーザー指摘
「**ゴム感／流体感が足りません**」）。

★★**「入口は1つだけ」の決まりは守れている。** 右端の ASSIGN は**別の入口ではなく、
引き下ろしという一本道の途中の分岐**。掴まずに触れる入口は増えていない。

★★★**ぶら下がりと伸び縮みは 2026-09-14・第103巡にユーザーが選んだ2つ**。
**「糸を引いて千切れる」と「着地で山が沈む」は採らないと確定した。足さないこと。**
★**支点は指そのもの**（図形はその下へぶら下がる）―― 中心を指に置くと「ただ回る」
に見える。★**振れは指に遅れる**（符号が負）。先回りすると引きずられて見えない。
★摘みは `HANG_T` / `SWING_MAX` / `STRETCH_MAX`（`lib/pullDrag.ts`）。

### 6-c. 形の変形（★新しい形を作らない）

★★★**帯のピルは「段が1つのピルの積み」**。`borderRadius: RADIUS.pill` の角丸長方形
＝ **`stackOutline(1, ar)` そのもの**で、山の図形は `stackOutline(rows, ar)`。
→ **`waist` を 0〜1 で送るだけ**で連続変形になる（`lib/solid.ts`）。
★★**点の数が一定なので再標本化が要らない**。★`waist = 1` は既存の呼び手と
**まったく同じ点の列を返す**（実測 … 9件・最大差 3.5e-9）。
★★**第16便の「48点の `clip-path` を補間する」案は使っていない**（§12-c は死文）――
`canvas` に描いているので、**輪郭の点の列をそのまま毎フレーム作り直せる**。

**提案（上の段）だけは違う**。行き先は札の形（`lib/cardShape.ts`）なので
`waist` ではなく、**`t > 0.5` で `traceCardShape` へ切り替える**（`pilePaint.drawGhost`）。

★★★**絵は山の canvas に描く。新しい canvas を足さない** ―― 同じ座標系・同じ dpr・
同じ焼き方なので、**離した瞬間の継ぎ目が無い**。

### 6-c2. ★★★帯のピルの写し取り（2026-09-14・第105巡にユーザー確定）

**触った瞬間（下へ 1px）に、帯のピルとまったく同じ見た目を canvas へ写し取る。**
理由は1つ ―― **`.band-row` は `overflow: hidden`** なので、DOM のままでは
**帯の外へ垂れられない**。以後その絵が**曲がり → 弾け → 図形になり → 山へ落ちる**まで、
**手つきが一本の続きになる**。

★★**寸法は `Band` が実測した矩形**（`PillLook`）。書体・余白・色は DOM と同じ
トークンを読むので、**数を二重に持たない**。
★★★**踏んだ罠は3つ**（どれも画素で突き合わせて見つけた）…
① **掴んだ瞬間の矩形では 1.2px ずれる**（帯が止まるのは段の `onPointerDown` ――
ピルのあとに走る）→ **写し取る瞬間に測り直す。**
② **`border` は余白の外側に積む**（輪郭だけのピルは中身が `PILL_EDGE` ぶん内へ寄る）。
③ **`offsetWidth` は整数へ丸まる**（0.02px 足りないだけで題が1文字切り詰められた）
→ **矩形 ÷ 実測の押下の倍率**で割り戻す。
★写真は**帯の `<img>` そのもの**を渡す（URL で取り直すと、写し取った瞬間はまだ届かない）。
★★**引いているあいだは山の canvas を帯の上へ**（`HomeTab` の `zIndex`）。

★実測（WebKit 390×844・dpr 2。1px 引いた前後）… **中身（縁の内側）の違いは
提案 1.43%（平均差 0.99）／タスク 0.43%（平均差 0.23）**。
**違うのは縁だけで、それは狙いどおりの垂れ**（1px の時点で 0.75px）。

### 6-c3. ★★★帯へ戻す（2026-09-15・第106巡にユーザー指定）

**山の図形を長押しで掴み、帯まで運んで離すと、ピルへ戻りながら吸い込まれる。**
★★★**`tTo` を 0 にするだけの逆再生**（6-c の作りをそのまま逆へ回す）。

| 段 | 何が起きるか |
|---|---|
| 帯の下端 ＋ `RAIL_NEAR`(72) より上 | **幽霊が生まれる**（絵は図形のまま）。**支点＝体**＝指にぴったり追従（実測 差 1〜2px） |
| ＋ `PULL_ARM`(**44**) より上 | ★★★**「捕まえる」** … `Ghost.aim`（帯の下の段の中心・指の x）へ **`K_SETTLE`**（行き過ぎない）で寄りながら、`tTo = 0` でピルへ畳む |
| 掛け金が落ちる（`tTo` 0 でばねが静止） | **帯のピルの顔**で描かれる |
| 帯から出る（`+ RAIL_HYST`(22) より下） | また**図形へ膨らむ**（往復できる） |
| 帯の上で離す | **日付を消す**＋**左右のピルを弾く**（`bandKnockAt`）。`haptic` |

★★★**戻りが2段なのはユーザー確定**（2026-09-15・第109巡）「**戻す時は指に基本的には
追従して、かなり帯の位置に近づいたら滑らかに帯に吸い付いていく**」／距離は
**ピル1つぶん**。★**1段（第108巡）だと「少し近づけると急に指から離れて戻る」**。

### 6-c4. ★★★帯の他のピルも動く（2026-09-15・第109巡にユーザー指定／第113巡に作り直し）

★★★**跳ね返り（バウンド）は全部やめる／再開のときだけ軽い慣性**（第111巡にユーザー確定）。

| | 何を | 合図 | 実測（第113巡） |
|---|---|---|---|
| ④a | 掴んで流れが止まるとき、**慣性で行き過ぎて戻る** | `.band-row` の `pointerdown` | 行き過ぎ 11.5px |
| ① | **完全に引き抜いた瞬間**、**流れが再開**し、**席が畳まれて後ろが詰める** | **`armed`**（`taken` ではない） | `paused` → `running`／離した前後の飛び **0px**（3/3 件） |
| ② | 帯へ寄せているあいだ、**指が指している所に隙間が開く**（段は止まる） | `Pile` の `bandAim({row, x, w})` | 90% まで **5〜10フレーム**／幅 **149/150px**／**2周ぶんに開き、画面に 1 件見える** |
| ⑤ | 挿し込まれた瞬間、**開いた隙間へそのまま収まる** | `sig` の照合（`useLayoutEffect`） | 見えているピルの飛び **最大 2.0px**（第112巡は **152px**）／戻ったピルは指の x=200 に対し **189**（＝指の下） |

★★★**隙間は「レイアウトそのもの」が持つ**（2026-09-16・第113巡に土台から作り直した）。
ピルとピルのあいだに**幅を持つ器**（`.band-pad`）を1つずつ置き、その幅を毎フレーム書く。

★★★**第109〜112巡の「`transform` のずれで隙間を作る」は、継ぎ目で原理的に破綻する。
復活させない。** 帯は同じ列を**2周ぶん**並べて `translateX(-1周)` で送っているので、
**同じ id のピルが1周ぶん離れて2つ居る**。ところが `transform` のずれは **id で引く**ので、
2つに別々の値を書けない。挿し口より後ろを `+w` 押すと **1周目の尻は `+w`・2周目の頭は 0** に
なり、受け渡しでは**2周目の全員がレイアウトで `+w` 動く**のに `-w` しか戻せない。
★**実測（第112巡）… 離した瞬間に2周目の先頭が 152px 飛んだ**（1周目は 3px）。
ユーザー報告「**入れ込んだ時にピルのスクロールがおかしくなる**」「**ピルは離した瞬間に
消えてしまいます**」の正体。★両側 ±w/2（第110巡）も片側 +w（第111巡）も同じ理由で直らない。

★★★**器なら、2周とも同じ列を描くので隙間も席も自動的に2周ぶん・同じ index に生まれる**
＝継ぎ目の辻褄が**原理的に**合う。**挿し込まれた瞬間にレイアウトが増える幅
（`pad + ピル`）は、開けていた幅と厳密に同じ**なので、**受け渡しの計算がそもそも要らない**。
★おまけに、周と周のあいだの間隔も周の内側と厳密に同じになった（前は `SPACE.sm` 多かった）。

★★★**帯ぜんたいを `lead`（指の器より前で開いたぶん）と「隙間の半分」だけ左へ戻す** ――
戻さないと**1周目に開いた隙間が2周目を丸ごと押す**ので、**指のそばにあったはずの境目が
画面の外へ逃げる**（実測 … 指が x=200 なのに隙間は 405〜563 ＝ どちらの周の隙間も画面に
出ていない。ユーザー報告「**近づけてもピルがどきません**」）。
★★**この2つは一様なずれなので、挿し込まれた瞬間に「送り（位相）」へ畳み替えられる。**
★★★**時計（`currentTime`）を直接ずらしてはいけない** ―― 1周ぶんで折り返すので、
**古い1周の幅で丸められた送りを新しい1周の幅で読み直す**ことになる（実測 770.5 → 70.4px
＝ 156px の飛び）。**送りは px のまま、丸めずに持ち越す**（`holdRef` → `phaseRef`）。
★★★**`transform` から外す瞬間と送りへ畳む瞬間は同じ 1回**（`build()` の末尾で `paint()`）
―― 別のフレームに分かれると、そのあいだ 72px 飛ぶ（実測）。

★★★**バネは `K_OPEN`（第113巡に足した5組目。90% まで 117ms・行き過ぎ 0.0%）**。
★★★**第111〜112巡の `K_SETTLE` は遅すぎた**（90% まで 467ms）―― 指が挿し口に居るのは
0.1〜0.3秒なので、**150px の隙間が 15px しか開いていなかった**＝「どかない」の正体。
★`K_CATCH` は速いが **10.5% 行き過ぎる**ので、跳ね返りを禁じられた帯には使えない。
表と理由は `lib/spring.ts`。

★★★**境目は「まだ何も開いていなかったときの位置」で測る**（開いた器の中心で測らない）
―― 器は**左の縁が動かないまま右へ育つ**ので、中心が指から逃げて**挿し口が勝手に
となりへ移る**（実測 同じ指の位置で index が 1 → 0 へ飛んだ）。
★★★**挿し口が2つに割れたときは受け皿へ預ける**（`open`／`shut` の2本）―― 1本だと、
指がピルの境目を跨いだ瞬間に**隙間がとなりへ瞬間移動する**。★**勢いは渡さない**
（渡すと閉じるはずの隙間が 130 → 165px まで育った）。

★★★**自分が開けている隙間では `ResizeObserver` で組み直さない** ―― 隙間はレイアウトなので
毎フレーム鳴る。組み直すと `track.animate` が**新しく作られて走り出す**ので、
**止めていたはずの段が指の下で流れ始める**（実測 `paused` のはずが `running`、離したあと 19px ずれた）。

★★★**再開と席は別の合図**（第111巡）―― 第110巡は「後ろにピルが無ければ return」の
**中**に `play()` を置いていたので、**引き抜いたのが段の最後のピルだと再開ごと落ちていた**。
★★★**流れの速さでは再開が見えない**（26秒で画面1枚＝**0.25px/フレーム**）。
**見えるのは一発の慣性だけ**なので、`bandResume` を必ず撃つ。

★★★**隙間は「指が指している所」に開き、並びのほうが合わせる**
（2026-09-16・第112巡にユーザー確定「**順番がいくら入れ替わっても問題ない。
どんな時でも、任意のピルとピルの間に戻せるように**」）。
★**x → index は段が出す**（ピルの居場所は流れの `transform` が決めている）→
`bandBus.slot.after`（**index ではなく左どなりの id**）→ 離したときに `pinBand` が
**留め金**を打つ（第114巡。`tasks` の並びは触らない）。
★★**帯は輪なので index は 0〜n-1 で足りる**（「n 番目の後ろ」＝「0 番目の前」）。
★★**狙われている段は流れを止める** ―― 止めないと、指を止めていてもピルのほうが
動いて挿し口が勝手に変わる（実測 同じ x で index が 3 → 1 へ飛んだ）。第113巡は理由が
もう1つ増えた ―― **隙間はレイアウトなので、開いているあいだ1周の幅が変わる**。

★★★**戻したタスクは段の上限（9）で切り落とさない**（`bandRows` の `keepId`）――
前は「帯へ戻しました」と出るのに**黙って消えていた**。

★★★**開ききる前に離されたら、新しいピルは「開いていたぶんの大きさ」から生える**
（`bandGapDone` が `hole` の掛け金へ畳み替える）。`pad + ピル` を `part` 倍した幅は
**開いていた幅そのもの**なので、レイアウトが増えたぶんと帳尻が合う。

★★**`stepBandMotion` は実時間を貯めて固定の刻みで進める** ―― 段ごとの rAF が
2つとも呼ぶので、**同じフレームの2度目は貯金が足りず 0 歩**（＝倍速の門）。
120Hz の2倍速も同時に消える。
★★★**流れの位相は px で持ち、下の段は `reverse` を折り返す** ―― 割合で持つと
1周の幅が変わった瞬間に帯が丸ごと飛ぶ（実測 送りが -302.6 → -502.8px）。

★★★**絵は `hx`/`hy` に描かれる**（2026-09-16・第108巡）―― `stepGhost` の `home` の枝で
**`hx`/`hy` を毎フレーム体へ置くこと**。
★★★**姿は掛け金（`Ghost.pill`）で持つ。`t <= 0` の生の比較で分けない** ――
変形のばねは**0 を挟んで行き過ぎる**ので、跨ぐたびに点滅する。
★★★**境目には遊びを持たせる**（`RAIL_HYST`）。
★★**声の候補・フォローアップから生まれたタスクも「日付なしタスク」に戻る**。

### 6-c5b. ★★★どこからでも、どこへでも（2026-09-16・第114巡にユーザー指定）

> **どんな時でも、帯に近づけるとその場所で、ピルの列に間が空いて、図形を
> 入れ込めるようにしてください。これは絶対です。**

**帯へ入れる道は2本ある。第113巡まで、実装が在ったのは片方だけだった。**

| 出どころ | 指を捕まえているのは | 第113巡まで | いま |
|---|---|---|---|
| 山の図形 → 帯 | `Pile` | 狙い・隙間・挿し込み **あり** | 同じ |
| 帯のピル → 引き抜く → 帯の別の場所 | **`Pill`（帯の要素）** | **何も無い**（`Pile.onMove` は1度も走らない） | 同じ1本に乗せた |

★★★**だから「引き出して別のところに入れようとしても元の場所に戻る」のは
当然だった** ―― 戻す先を選ぶコードが存在しなかった。
★★**狙いの出し方は1本**（`bandAim` ＋ `PullHost.bandBottom`/`rowCenter`）。
「どこから来たか」で手つきが変わってはいけない。

#### ★★★弾けは掛け金（`pullFrame` の `latched`）

`armed = pulled > PULL_ARM` を**毎フレーム測り直していた**ので、指を上へ戻すと
**弾けが取り消され、ピルが元の席へ瞬間移動して戻っていた**。
→ **一度弾けたら、その指のあいだは弾けたまま**。輪ゴムから千切れたものが、
指を戻したら勝手に繋がり直すのはおかしい。
★取り消しは消えていない ―― **元の場所で離せば元に戻る**（入れ直しがそれを兼ねる）。

#### ★★★並びは「留め金」で憶える（`AppState.bandPins`）

1つの留め金は「**この id は、この id の次に置く**」（`after` が空なら段の先頭）。
★★★**第112巡は `tasks` の並びそのものを替えていた** ―― それだと
① GRAVITY の山と ALIGN の一覧まで動き、② 声の候補（`inbox`）とフォローアップ
（`Task.suggestions`）は**持ち主が別なので動かせず**、
**「任意のピルとピルの間」が原理的に作れなかった**。
★★**留め金は表示の並びだけ**を持つので、**5種類すべて・2段とも**置ける。
★★**隣が消えたら自然な位置へ戻るだけ**（全件の並びを持つと翌日には消えた id の列になる）。
★★**帯に居なくなった留め金は捨てる**（溜めない）。

#### ★★★挿し口は index ではなく「左どなりの id」で渡す

離した瞬間に `items` は組み替わる（引き抜いたピルが消える／新しいピルが入る）ので、
**index は指した場所を指し続けない**。id なら指した「あいだ」が動かない。

#### ★★★入れ直しも「受け渡し」に乗せる

引き抜いたピルを別の場所へ入れ直すと**件数は変わらない**ので、「増えたか」だけを
見ていると受け渡しが走らず、**畳んでいた席が古い所で開き直し、新しい所の隙間が
別に閉じる** ―― 実測 **1フレームで 139px 飛んだ**。
→ 並びが変わったときも `bandGapDone` を通し、**開いていた隙間をそのまま
そのピルの幅へ畳み替える**（実測 **1px**）。
★そのピルの DOM はいま幅 0 を書かれているので、**幅は測り直さず `holeW`（畳む前に
測った値）を使う**。

#### ★★★戻したものを、時間帯で隠さない

`pickTodayItems` は `kind` ごとの時間帯で候補を落とす。山の提案を帯へ戻すと
`plannedFor` が消えてストックへ返るが、**行き先でない種類（本・モノ）や、もう
閉まっている時間だと、そこで落とされて帯に現れなかった** ―― それでも画面には
「帯へ戻しました」と出ていた（ユーザー報告「**メッセージだけ出てどこかに消える**」）。
→ **留め金の付いたものは時間帯の門を通さない**（`force`）。
**表はおすすめの並べ方であって、ユーザーの意思を却下する門ではない。**

#### ★★★掴んでいる間は山の canvas を帯の上へ

幽霊は山の canvas に描かれるので、上げないと**帯の裏へ回って消えて見える**
（実測 … canvas には描かれているのに画面には出ていない）。
「どこかに消える」の、**目に見えるほうの半分**がこれ。

### 6-c6. ★★★白抜き ⇄ 塗りは「量」で渡る（2026-09-16・第113巡にユーザー指定）

> **帯のピルは白抜きで、図形は塗りですが、これもアニメーションなく
> 切り替わってしまいます。これも UX として全くリッチに感じない原因なので直してください。**

★★★**掛け金（`Ghost.pill`）で切り替わっていた** ―― `t` が `PILL_EXIT`(0.2) を跨いだ
1フレームで、**輪郭の版面から塗りの図形へ絵ごと入れ替わる**。形は `waist` で連続に
変わっているのに、**面の塗り方だけが段差**だった。
★★★**`inkMix(g)`（`components/home/pillGhost.ts`）が「塗りの量」を1つ返す** ――
地を敷いてから面を `α = mix` で重ね、輪郭を `α = 1 − mix` で引く。**字の色も同じ量で混ぜる**
（`mixHex`。面だけ渡って字が飛ぶのを防ぐ）。
★★★**両端は決め打ちにしない** ―― 「帯のピルが塗りか」と「山の図形が塗りか」から導く。
決め打ちにすると**日付の無い図形（山でも輪郭）を戻すときに勝手に塗られる**。
★★**同じ式を2つの絵が読む**（`drawPillGhost` と `drawGhost`）ので、掛け金が跳ぶフレームでも
塗りの量は飛ばない。★実測 … 掛け金が跳ぐ前後の色の差 **157 → 21**（うち 21 は
その瞬間の `t` の進み 0.125 ぶんそのもの＝**段差ではない**）。

### 6-d. カレンダーの画面（`components/home/AssignSheet.tsx`）

★★★**盤は `components/DateGrid.tsx`**（`WhenSheet` から持ち上げた**同じ1つ**）。
**2か所に書くと、次に画素を直す人が片方しか直さない。**
★地は**墨**（`CHARCOAL`。タスクの入力画面・声の録音・設定と同じ側）。
★早押しは **今日・明日・来週**（★入力画面の「今週末」も**来週へ揃えた**）。
★**日をタップしたらその場で確定**（✓ を押させない。手つきを2回に割らない）。
★★**`body` 直下へポータルで出す** ―― 祖先の `.app-track` が `transform` を持つので、
そのままだと `position: fixed` が**3列ぶんの幅（1170px）**に広がる（実測。
曜日が2列しか見えなくなった）。

### 6-e. 書き込み先（★帯の段ごとに違う）

| 帯の段 | 元 | 日付の入れ方 |
|---|---|---|
| `someday` | `Task` | `dueDate = iso` |
| `voice` | `InboxCandidate` | `DriftTab.accept` と同じ形で `Task` にして `dueDate` |
| `followup` | `TaskSuggestion` | 題から `Task` を作り、親の `suggestions` から外す |
| `offer` / `today` | **`Item`（Explore）** | ★**タスクにしない**。**`Item.plannedFor = iso`** |

★★★**`Item.plannedFor` を第102巡に足した**（ユーザー確定）。それまで山の提案の円は
**常に0個**だった ―― 唯一の入れ物 `AppState.magazine` は**今日1日ぶんしか持てず**、
しかも**書き込む呼び手がアプリに1つも無かった**。
★★**実行・ログ・アーカイブ・my-brain は1行も変えていない**（`doneAt` が正のまま）。
★★**「今日を終える」は、その日に割り当てた提案も綴じる**（ユーザー確定）――
`AppShell.finishDay` と `Dashboard` の両方で `selection.itemIds` に
`plannedFor === todayKey()` のものを足す（**片方だけだとボタンが死ぬ**。実際に踏んだ）。

### 6-c5. ★★★掴んでいる図形には当たり判定が在る（2026-09-15・第110巡にユーザー指定）

ユーザー確定「**山の図形を押しのける ＋ 壁と床にも当たる**」（帯のピルは物理では当てない）。

- **弾けたら「代理の体」が1つ world に入る**（`pileWorld.ghostBodyOf`。形も重さも
  `buildPieces` と同じ1本から）。**絵は体そのもの**（`stepGhost(mo, g, pin)`）なので、
  「壁で止まる」「山を押しのけて止まる」がそのまま絵に出る。
- **体は絵の中心に置く**（支点ではない ―― `ay` は `h/2` まで育つので、支点に置くと
  **絵の下半分に当たり判定が無い**）。おかげで `landing` の受け渡しは1行も変わらない。
- ★実測 … 山へ押し込むと**7/7 個が動き（最大 157.4px）**、器の外へ **0px**、
  離して 1.6秒後に**代理は残っていない**。

★★★**山の大きさは中身が変わっても決め直さない**（同巡。ユーザー確定「**最初は山の
大きさを変えずに、次に開いた時とかに調整する**」）。1枚増えるだけで**全員が別の
大きさで作り直され、山が丸ごと浮いていた**のが「離した瞬間に画面が一気に変わる」の
正体。実測 … **大きさが変わった 0 件・倍率 31.234 → 31.234**。

★★★**そして体そのものも作り直さない**（2026-09-15・第111巡）。形の署名が一致したら
**前の体をそのまま使う** ―― 作り直すと `Composite` から出し入れすることになり、
**接触も眠りも切れる**うえ、入れ直しの `clearOverlap` が**落ち着いた山を
「AABB が1つも重ならない縦の塔」へ積み直す**。これが残っていた「**リセットが入って
落とし直しが発生する**」の正体。
★★★**`clearOverlap` を掛けてよいのは「新しく湧いた体」だけ**（`Piece.fresh`）。
**着地の1枚にも掛けない** ―― 指を離した所がそのまま正。
★実測 … **体を使い回した 7/7 件**・**組み直しで位置が動いた 0 件（最大 0.00px）**・
world の動く体 **7 → 8**（1つ増えただけ）。★前は山が最大 **77px** 浮いていた。

★★★**引き出している最中は、字も輪郭と同じだけ曲がる**（同巡）―― 第110巡までは
**文字列の左端 1点**で測った垂れを行ごとに1回足していたので、**字は平行に下がるだけ**
だった。**1文字ずつ自分の x で `bendAt` を読む。**

★★★**掴んで運ぶ図形は回さない**（2026-09-16・第112巡にユーザー指摘「**何回転もする。
暴れないように**」）―― `setVelocity` で引きずると接触が毎歩 回りを注ぎ込むので、
**角速度を 0 にするだけでは足りない**（実測 0.0234 rad/歩 ＝ 約 80°/秒が残って漂う）。
**角度そのものを「掴んだときの値」に留める。** 帯へ吸い込まれるときは
`g.angle *= g.waist` が絵のほうを立て直す。

### 6-f. 山の図形も同じ操作（`components/home/Pile.tsx`）

★★★**「掴めない」の正体は眠り** ―― 山は落ち着くと `engine.enableSleeping` が真になるが、
**`Body.setVelocity` は眠った体を起こさない**。掴んだときと動かしている間に
**`M.Sleeping.set(body, false)` を呼ぶ**のが正（`GravityTab` は呼んでいた）。
★実測 … 起こさないと **0.0px**／起こすと **156.7px** 動いた。
★掴んだ図形を右端へ運んで離せば、**すでに入っている日が選ばれた状態**でカレンダーが開く
（＝**付け直し**。ユーザー確定）。

★★★**日付と曜日の板も掴める**（2026-09-18・第120巡にユーザー指定「**日付もつかめる
ようにしてください**」）。第119巡までは `pickAt` が板を**見ないことにして**いた
（下の図形へ譲るため）。
- ★★**板は「遊びなし」で勝つ** ―― 板は**いちばん最後に塗られる**ので画面では必ず
  上に居る。だから**板の内側を押したら板**以外は嘘になる。
  ★★★**`TOUCH_SLOP` を足さないのが肝** ―― 足すと板は山でいちばん大きな塊なので
  **まわりの図形を軒並み飲み込む**（実測 … 板の中心を押したのに隣のタスクが掴まれ、
  板は **295px** 置き去りになった）。縁のそばは下の図形へ譲る。
- ★★★**板には行き先が無い**（`dragRef.place`。日付の板に割り当てる日付は無い）
  ―― 右端へ運んでも **ASSIGN は出ず**、帯へ近づけても**隙間は開かない**。
  判定は `pillOf()` が版面を返すかどうか**1か所**から引く。
- ★実測 … 板を掴んで運ぶと**指との差 19px**／ASSIGN は画面の外のまま／
  帯の器の幅は素の `BAND_PAD`(8) のまま。

### 6-g. ★★★**山は落ち直さない**（2026-09-14・第103巡にユーザー確定「いつでも」）

第102巡は日付を1つ付けるたび**山が丸ごと落ち直していた**（実測 … 離した直後に
画素が **0** になり、元へ戻るまで 2.9 秒）。
★★★**体は作り直すしかない** ―― 一括の倍率 `unit` は**全体の面積の予算**から
出るので、1つ増えれば全部の大きさが変わる。落ち直して見えていたのは
「新しい体を**上から落とし直していた**」からで、そこだけをやめればよい。
→ `buildPieces(…, prev)` が**同じ id の位置・角度・速度・回りを写す**。
★**新しい1つだけ**が落ちてきて、それも**指を離した所から、投げた勢いのまま**
（`pullBus.landing`）。★実測 … 離したあとの画素の最小が**離す前の 118%**
（＝一度も減らない）。

---

## 7. 部品

### 7-a. 印 ── 円の中に記号1つ（★触れる部品にだけ付く）
> **山の図形は情報であって、ボタンではない。** 矢印は「押すと動く」の合図なので、
> 図形に付けると全部がボタンに見える。

付く場所は **3か所だけ** … ①持ち物（右上） ②タブバーの作るの丸（右下） ③下ろす手つきの最中。
**帯のピルには付けない**（第33便で確定）。

- 直径は **18 / 28 / 46** の3段だけ。中身は必ず1つ（矢印・数字・点）。**真円以外にしない。**
- ★「円＝提案」と衝突しない ―― **大きい円は物、小さい円は印**で、**大きさが役を決める**（46 以下は必ず印）。
- ★**矢印は同じ向きにしない** ―― 下ろす合図は**右下 45°**、進むは**右**。**形は同じ、角度だけが情報。**
- 採る印は4つだけ … **矢印45° ／ 数字 ／ 点 ／ 進捗のリング**。器は**円だけ**
  （四角はタスクの形なので、小さくても「タスク」と読まれる）。
- ★**進捗のリング**は会期のあるものに「残り」を文字なしで付けられる唯一の印。
- 採らない … ✕（閉じる・消すと衝突。**掴んだときだけ出る「捨てる口」に限定**）／
  ✓（完了は「口に放り込む」動作が担う）／＋（第36巡に撤去済み）／▶（録音と衝突）／
  時計（時間管理アプリではないという主題に反する）／波・縞（純粋な飾り）。

### 7-b. 持ち物
- **画面右上に固定した 38px の面のアイコン ＋ 数字のバッジ。**
  既存の `TabIcons`（面で描いたアイコン）と同じ作り。**山の物理に参加しない。**
- **タップすると図形が扇に散る。** シートも一覧も出さない。
  詰めたものは**口へ放る**（＝完了と同じ動作）。もう一度タップで吸い込まれて閉じる。
- ★開いた状態も**図形の語彙のまま**なので、山と地続きに見える。

### 7-c. タブバー
- **ホームは3アプリの入口** … `EXPLORE ／ TASK ／ JOURNAL` ＋ 右端に**作るの丸**（46px）。
- 白い面に墨の縁、文字は `TYPE.nano`(7)〜`micro`(9) の大文字・字間 .18em。

---

## 7-c. ★★★図形の形と大きさ（2026-09-17・第116巡にユーザー指定）

> **やっぱり、図形の形はピルが積み上がったような形に戻してください。** ／
> **一段だけの図形の大きさと二段の図形の大きさと三段の図形の大きさが全く違う。
> 文字の大きさや図形の大きさを揃えてほしい。1段ごとのピルの大きさは同じで、
> 2段の時はそれが2個で3段の時はそれが3個そのまま積み上がったような形にして
> ください。で文字の大きさを揃えてください。**

- **形** … **幅の等しいピル（角丸＝段の高さ/2）を縦に積む**。段は隙間なく接する。
  左右対称なので `halfWidthAtStack` は `stackEdge` の値そのもの。
  ★★★**第115巡の S 字の折りたたみ（`foldsAt`／`sideFolds`／2段を覆う弧）は削除した。
  復活させない。**
- **大きさ** … **段の高さを固定する**（`lib/taskSize.ts` の `rowSpecOf`）。
  `h = 段の数` ／ `w = rowAspect`。**`unit` の意味は「段の高さ（px）」**。
  → どの図形でも**段の厚みが同じ＝字の大きさも同じ**
  （実測 … 段の高さの幅 **2.25倍 → 1.00倍**。比較できた件数 9）。
  ★★**幅だけは題の長さで伸びる** ―― 字を同じ大きさで折り返さずに収めるには
  文字数ぶんの幅が要る。**幅も固定にすると字が縮むか `…` で消える。**
  ★★**提案の円 `OFFER_D`(3.2) とカセット `CASSETTE_ROWS`(2.2) も段の高さの倍数**。
  ★★★**TASK（GRAVITY）は今までどおり重要度で大きさが変わる**（`specOf` は1行も
  触っていない）。
- **山の高さ**（第118巡／★**第120巡に緩めた**）… **帯が覆っている高さは予算から
  引く**（`buildPieces` の `bandH`。帯は山の器へ重ねてあるので、引かないと
  **山が帯の裏へ伸びて触れなくなる**）＋ **混み具合の係数 `crowdOf(n)`**。
  ★★**`crowd` は「長さ」の倍率**なので、面積の予算には `crowd²`、日付と曜日の板には
  `crowd` をそのまま掛ける（**板だけ据え置くと混むほど板が巨大になる**）。
  ★★★**板は `room` と上限 `PILE_WORD_MAX` の両方に掛けること**（第120巡）――
  字の大きさは `min(幅に収まる値, 上限)` なので、**上限が効いている間は `room` だけ
  縮めても 1px も変わらない**。ユーザー報告「**日付だけ変わらない**」はこれ。
  ★★★**第120巡に `CROWD_N0` 10 → **18**、`CROWD_MIN` 0.85 → **0.94****
  （ユーザー「**山全体のスケールが効き過ぎています。小さすぎるようになって
  しまっているので緩めてください**」）。★★**第118巡の見立てが逆だった** ――
  実測すると**件数が増えるほど山は低い**（小さい図形は密に積むので同じ面積でも
  高さが要らない）。⑥を直したのは `bandH` であって、この係数ではなかった。
  ★実測（390×844。タスク 4／9／14／22 件）… 帯からの余裕 **112／164／220／254px**、
  塗った面積は 24831／24970／25018／22136 で**ほぼ一定**。

## 7-d. ★★★日付と曜日の板（2026-09-16・第114巡にユーザー指定）

> **日付は 9.15 のようにしてください。曜日は WED や TUE のような感じに。
> そしてどちらも黒いピルに白の文字に。サイズは今の文字くらいでよく、
> ピルのサイズに対してそれぞれの文字が大きく入っている感じに。**

- 日付 `9.15`（`.` 区切り）／曜日 `WED`（3文字。`WD_SHORT`）。
- **面 ＝ `INK`／字 ＝ `PAPER`**。角丸は**高さの半分** ―― 帯のピルとまったく同じ形。
- 遊びは**字の高さに対する比**（`lib/wordPlate.ts` の `PILL_PAD` 0.46 ／
  `PILL_PAD_Y` 0.24）。★生の px を置かない ―― 字の大きさが日によって変わっても、
  ピルと字の関係が動かない。
- ★★★**大きさの物差しは長いほうの綴り（`WD_FULL`）のまま**（「サイズは今の文字
  くらいでよく」）―― 3文字で測ると器いっぱいまで太って**今までの倍近い字**になる。
- ★★★**TASK（GRAVITY）の板は変えていない**（あちらは「文字そのものが図形」のまま）。
  ★**揃えるかどうかは未確認**（ユーザーの指定はホームの山についてのもの）。

## 8. 文字

★**参照の迫力は欧文の黒さから来ている。和文で同じ黒さを出そうとすると必ず潰れる。**

| | 規則 |
|---|---|
| 欧文（`LATIN` / Archivo） | weight **900** ／ 字間 **−0.045em** ／ 行間 0.8〜1.0。**迫力はここが担当** |
| 和文（`SANS` / Noto Sans JP） | weight **500〜700** ／ 字間 **+0.02〜0.04em** ／ 行間 **1.6〜1.9**。**太らせない・詰めない・大きくしない** |
| 小さいラベル | **8〜9px・字間 .18〜.22em・大文字**。★**中くらいの文字を作らない** |

★このアプリは**すでに大きな英語（`Masthead` / SUNDAY）を持っている**ので構造は正しい。直すのは和文側。

★**大きな英語は1画面に1つ。** ごちゃごちゃの正体は第24〜25便で**大きな英語が2つあったこと**だった
（`TASK` と `SUNDAY`）。ホームの見出しは小さな `HOME ／ 06 SEP ／ SUN` だけにする。

---

## 9. 触ってはいけないもの

- **iOS の「上下の帯」**（`design.md` ／ `docs/project_knowledge.md` §3）。`statusBarStyle: "default"` のまま。
- **地の色を替えたら `app/manifest.ts` の `background_color` / `theme_color` も同時に直す。**
- **Tailwind 不使用**（すべてインライン style）。
- **CSS の 3D 変形は禁止**（WebGL は可）。

---

## 10. 実装の段取り

1. **配色を差し替える** … `lib/constants.ts` の `PALETTE`(6行→5行) ／ `INK` ／ `PAPER`。
   `lib/palette.ts` の `SCHEME`（役→色）を新しい5色へ張り直す。
   ★**色そのものを持つのはこの2か所だけ**なので、呼び出し側は触らない。
   ★`app/manifest.ts` も同時。
2. **`/dev` にホームを作る** … `components/tabs/DevStageTab.tsx` の隣に置き、
   **実機（iPhone の PWA）で見る**。4タブは手つかずのまま。
3. **帯を作る** … 3段の循環（§4-c）。まず**動きだけ**を実機で確かめる（速さの判断が先）。
4. **山を繋ぐ** … 既存の `GravityTab` の物理をそのまま使い、形を §5-b に合わせる。
5. **下ろす手つき** … §6 の K1。TIMELINE と同じ仕掛けなので `GravityTab` の中で完結する。
6. **持ち物** … §7-b。
7. **本物にする** … 4タブへ展開し、`/dev` を消す。

### 触るファイル
| 目的 | ファイル |
|---|---|
| 色 | `lib/constants.ts` ／ `lib/palette.ts` ／ `app/manifest.ts` |
| 寸法・文字 | `lib/tokens.ts`（**増やさない**） |
| ホームの器 | `components/AppShell.tsx` ／ `lib/apps.ts` |
| 帯 | `components/home/Band.tsx` ／ `lib/homeBand.ts` ／ `components/home/bandMotion.ts` |
| 山 | `components/home/`（`Pile.tsx`＋`pileWorld.ts`＋`pilePaint.ts`）／ `lib/solid.ts` ／ `lib/taskSize.ts` |
| おすすめ3件 | `lib/offerPick.ts` ／ カード → `Item` は `lib/keepCard.ts` の**1本だけ** |
| 詳細の画面 | `components/explore/CardDetail.tsx`（本文は `BriefCard.detail`） |
| 動き | `lib/motion.ts` ／ `app/globals.css` の `:root`（**数字はこの2つだけ**） |

---

## 11. 検証（★省略しない）

1. `design.md` §5 の**機械チェック13本**を走らせる。**#1〜#7・#10〜#13 は 0 件**。
   0 にできない値は**目盛りの外**として、その行に `// ★目盛りの外（理由）` を書く。
   ★**ROSE の比 4.05 はここに該当する**（§2-b）。
2. **移行前後の computed style を突き合わせ、「比較できた件数」を必ず出す。**
   件数を出さない検証は、検証していないのと区別がつかない。
3. **実機の iPhone で見る。** Chromium で問題なくても実機で直っていないことがある。
   特に**帯の流れる速さ**は実機でしか判断できない。

---

## 12. まだ決まっていないこと

| | 内容 |
|---|---|
| ~~A~~ | ~~**下ろす手つき**~~ … **第102巡に確定・実装済み**（§6。K1「7本の柱」は**やめた**） |
| B | **山の構造** … 今日でないものをどこに置くか（S1 今日だけの山／S2 高さ＝日の遠さ／S3 日ごとの引き出し／S4 日付を持たない）。第19便に4案 |
| C | **帯の速さ** … 44／58／72秒。実機で決める |
| D | **墨の値** … `#23252B`（比 14.41）か `#2A2C33`（13.11）か |
| E | **ROSE の扱い** … 16/700 で使うか、文字を持たない面に限るか |
| F | **記録（JOURNAL のログ）の見た目** … 「角丸のカプセルで描く」とだけ決まっている（第28便） |

---

## 12-b. 動きの全表（★数字は `lib/motion.ts` と `app/globals.css` の `:root` からしか引かない）

既存の語彙 … 曲線4本 `--ease-settle` / `--ease-sheet` / `--ease-press` / `--ease-exit`、
時間 `--t-press`(90ms) / `--t-item`(420ms) / `--t-out`(600ms) / `--t-in`(700ms) / `--t-step`(50ms)、
環境の動き5本 `--t-amb-*`。**★増やさない。** 帯の循環だけは新しい役なので
`--t-amb-band-1/2/3`（44s / 58s / 72s）を**環境の動きの段に足す**（対称な linear を許すのはこの段だけ）。

| # | 何が | どう動く | 曲線 | 時間 |
|---|---|---|---|---|
| 1 | 帯の3段 | 左／右／左へ等速で無限に流れる。中身を2回並べ `translateX(-50%)` | `linear` | 44s / 58s / 72s |
| 2 | 帯に指が触れる | その段だけ止まる（`animation-play-state: paused`） | — | 即座 |
| 3 | 指を離す | 流れ出す。**位置は戻さない**（`paused` を解くだけ） | — | 即座 |
| 4 | ピルを押す | 沈む（`scale .97`）。★**押下だけが非対称** ―― 沈みは速く、戻りは遅い | 沈み `--ease-press` ／ 戻り `--ease-settle` | 90ms ／ 420ms |
| 5 | ピルを掴む | **沈む**（`scale .97`）。★**柱は立たない**（第102巡に K1 を廃止） | `--ease-press` | 90ms |
| 6 | 引き下ろしている間 | ゴム（〜56px）→ 図形へ変形しながら指へ吸い付く。★**時間ではなく距離で送る** | ★目盛りの外（`lib/pullDrag.ts`） | — |
| 6-b | 右の縁へ 72px | 右端から **ASSIGN の帯**が滑り込む | `--ease-sheet` | 700ms（`--t-in`） |
| 7 | 指を離す | ホーム＝**今日**が入って物理へ引き継ぐ／帯の上＝**カレンダー**／途中＝**何も起きない** | 落下は物理 | — |
| 8 | 図形が山に着地 | matter.js の物理に引き継ぐ。**跳ねは1回だけ**（`restitution` は既存値） | 物理 | — |
| 9 | 山を長押しして掴む | 既存の `GravityTab` の挙動をそのまま使う | 既存 | 既存 |
| 10 | 持ち物を押す | **アイコンから小さい図形が弧を描いて扇に開く** | `--ease-settle` | 420ms（1つずつ `--t-step` 50ms 遅らせる） |
| 11 | 持ち物をもう一度押す | 逆再生で吸い込まれる | `--ease-exit` | 600ms |
| 12 | 未読のバッジが 0 になる | 縮んで消える（`scale 0` ＋ `opacity 0`） | `--ease-exit` | 600ms |
| 13 | 朝ひらいたとき | その日のものが**上から落ちてきて山になる**（`--t-step` で順に） | 物理 | — |
| 14 | 画面に入る／出る | 既存の `AppShell` の横スライドに乗る | 既存 | 既存 |

★`prefers-reduced-motion: reduce` … 1・13 を止め、4〜12 は時間を 0 にする（**挙動は残す**）。

## 12-c. ピル → 図形の変形（★★★**死文**。第102巡に実装した中身は §6-c）

★★★**下の「48点の `clip-path` を補間する」案は採らなかった。** canvas に描いて
いるので、**輪郭の点の列（`stackOutline` の `waist`）をそのまま毎フレーム作り直す**
ほうが素直で、しかも**山の物理とまったく同じ形**が保証される。
**戻すときのための記録として残すだけ。読むのは §6-c。**


**帯のピル（角丸 999）から、山の図形（角丸 12 の四角／円）へ、連続的に変形する。**

- 実装 … **`clip-path: polygon()` を同じ頂点数で補間する**。第16便に検証済み ――
  **48点**を弧長で等間隔に取り直せば、ピルから四角へも円へも**ライブラリ無しで**滑らかに繋がる。
  頂点数が違うと補間できないので、**どの形も必ず 48点で持つ**。
- ばね感 … 落ちるときだけ `cubic-bezier(.34,1.5,.5,1)` を使ってよい（**目盛りの外**。
  `--ease-settle` には行き過ぎ（overshoot）が無く、「ぎゅっと変形する」が出ないため。
  ★その行に `// ★目盛りの外（変形の行き過ぎ。settle では出ない）` を書く）。
- 同時に … 文字は**フェードで差し替える**（ピルの1行 → 図形の中の左上の2行）。
  形が変わり終わる前に文字を動かすと読めなくなるので、**文字は 420ms の後半 210ms** で入れ替える。

## 13. 絵（Artifact）

| 便 | 内容 | URL |
|---|---|---|
| 18 | 山は何か（5つの答え） | https://claude.ai/code/artifact/377f070d-9690-4c50-9d49-b37088e164e7 |
| 19 | ふたつの日付・カレンダー3案 | https://claude.ai/code/artifact/dd8323be-8352-4bd9-a7b7-74a8ffdd403b |
| 21 | 置き場の規則 | https://claude.ai/code/artifact/b3a75296-d523-42e6-9220-fd2502082a39 |
| 25 | 印の見本帳48個 | https://claude.ai/code/artifact/72cade82-e255-4734-9cff-63210a4d8745 |
| 32 | 名前つきパレット（06 の出どころ） | https://claude.ai/code/artifact/72bd86f0-e456-4f7a-8579-cd943ed08fb5 |
| **33** | **★確定した骨格。10配色の比較** | https://claude.ai/code/artifact/84d0dd0f-af3d-48b4-8f25-95e50a6f3f16 |
