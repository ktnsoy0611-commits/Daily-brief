# プロジェクトナレッジ（現行仕様の正）

このファイルは**いま有効な設計・確定仕様・データモデル**だけを載せる。
過去の経緯・却下された案・不具合の修正史は `docs/archive/` にある。
「いまどこまで進んでいるか」は `handoff_current.md`。

- 生成パイプラインの設計思想 … `SYSTEM-DESIGN.md`
- Cowork（週次の発掘・分析、毎晩の仕分け）に渡すプロンプト … `COWORK-ROUTINES.md`
- 初期要件定義書（**データモデルとタブ構成は古い**。目的と思想の参照用）
  … `docs/archive/implementation-handoff-2026-07.md`

---

## 0. このアプリは何か

ユーザー本人 1 人のための PWA。タスク管理アプリではなく、
**「やりたいこと」を集め・実行し・記録として綴じる**ための道具。

**絶対に守るユーザーの設計思想**
- タスク化しない（義務や TODO の消化にしない）
- 設定画面を増やさない（`ProfileTab` が唯一の設定で、これ以上増やさない）
- 説明文を置かない（本人専用アプリなので、使い方の案内は不要）
- OS 標準のフォーム UI（ドロップダウン・標準テキストボックス・チェックボックス）を
  使わない。**純粋な幾何学図形・ベタ塗り・太いタイポグラフィだけ**で画面を構成する。
  ★**角丸と円は使ってよい**（2026-08-16 にユーザー指定で改訂）。むしろ
  アプリの語彙は「丸」— タブバーのピル、丸ボタン、`borderRadius: 22` の
  カード。ここから外れた「角の立った矩形だけの画面」は他から浮くので不可。
  使ってよい丸みは **22（板）/ 999（ピル）/ 50%（丸）** の 3 つだけ。
  影は `SOFT_SHADOW` / `SOFT_SHADOW_LG`。
  ★ただし**タスクの図形そのもの**（円・半円・三角・四角）は角を立てたまま。

---

## 1. アプリの構造

3 つのアプリを、**タブバーの帯を横に払って**行き来する（`lib/apps.ts` が正）。

| 順 | アプリ | `en` | タブ |
|---|---|---|---|
| 0 | ジャーナル `journal` | JOURNAL | レコード `journal-record` / 今日 `journal-today` / アーカイブ `journal-archive` |
| 1 | タスク `tasks` | TASK | 候補 `tasks-drift` / タスク `tasks-gravity` |
| 2 | ブリーフ `life` | EXPLORE | ブリーフ `brief` / ゴール `goals` / ストック `stock` / プラン `execute` |

- 右端からさらに右へ払うと先頭へ**回り込む**（一周ループ）。位置は 0〜2 に
  丸めない**通し番号 `pos`** で持つ。丸めると端で位置が飛ぶ。
- **3 つの列は常にマウントされたまま**。初回描画後に `requestIdleCallback` で
  残り 2 つを先読みし、以後アンマウントしない。
- アプリごとに最後に見ていたタブを覚える（`tabByApp`）。
- 画面左上（`Masthead`）は**アプリ名**（EXPLORE / TASK / JOURNAL）を
  幾何アルファベットで。タブ名はタブバーのアイコンの下に小さく出す。

### ダッシュボード
画面下から引き上げる共通の引き出し（`components/Dashboard.tsx`）。
どのアプリからでも開ける。中身は「選んでいるカード」と「今日のタスク」、
下端に **「今日を終える」**。これで選んだカードは `done` になりアーカイブへ移る。
開き具合は 0〜1 の連続値 `--dash` で、指の位置と 1 対 1。

**メタファー（§38 で確定した体験の構造）**
- Journal … 音声の抽出（テープを巻き、切り出す）
- Task … 落下と粉砕
- Explore … 立体の展開と結合
- 1 日の終わりに 3 要素が 1 枚の幾何学ポスターへプレスされる（**未実装**）

---

## 2. データモデル（`lib/types.ts` が正）

- `Item` … ストック／プラン／アーカイブに出る「願望の対象物」。
  `kind`（place/exhibition/live/activity/food/movie/book/album/info/thing）×
  `area`（場所の有無は種類と独立の直交軸）。
  `status: candidate → planned → done`、`origin: brief | manual | wish | info`、
  `lat/lng/placeId`、`goalId?`、`expiresAt?`。
  分類の入口は `domainOf(item)`（`lib/helpers.ts`）ただ 1 つ。
  4 ドメイン `ItemDomain = place | experience | info | thing`
  （バショ／タイケン／ジョウホウ／モノ）。
- `Wish` … ✨で書く受信箱。`category: ItemDomain`。KEEP されて初めて Item になる。
- `Goal` / `CheckIn` … ゴールのバインダーと記録。
- `Task extends TaskSides` … §4 参照。面は `SIDE_KEYS = title / due / context /
  belongings` の 4 つで、埋まった数がそのまま図形になる。
  ★2 番目は **`dueDate`（YYYY-MM-DD）**。自由文の `when` は 2026-08-16 に廃止し、
  `migrate()` が「日付なら `dueDate` へ / それ以外は `context` の先頭へ」畳んだ。
  同じ「いつ」が自由文と期日の 2 箇所にあると、大きさ（切迫度）に効くのが
  どちらか読めなくなるため。
- `InboxCandidate extends TaskSides` … Cowork が声のメモから作った候補。
  `dueDate` を持ち、確定するとそのままタスクの期日になる。
- `JournalEntry` / `VoiceNote` … ジャーナルと声のメモ。
- `BriefCard` / `GrowthCard` … ブリーフのデッキのカード。
- `AppState` … 上記すべての入れ物。
  - `generatedDecks: Record<"YYYY-MM-DD", BriefCard[]>` と `cronStatus` は
    **サーバー（夜間 Cron）が所有**する（`lib/dataStore.ts` の
    `SERVER_OWNED_KEYS`）。クライアントは読むが上書きしない。
  - `magazine` / `weekendMeta` / `shelfOrder` は**もう更新されない**
    （それを使っていた UI が廃止済み）。型は互換のために残してある。

**永続化** … `lib/dataStore.ts`。3 モード（cloud / persistent / memory）。
Supabase の環境変数があり、かつログイン済みのときだけ cloud。
localStorage のキーは `qol-app-state-v1`。

★`migrate()` は `wishes` が無い state を**最古の形式とみなして全部捨てる**。
テストで localStorage に state を差し込むときは必ず `wishes` を置くこと。

---

## 3. デザイン言語

**テクスチャを排除し、ベタ塗りの色面・直線・円などの純粋幾何学**（§38 で確定）。
角度は 0 / 45 / 90 / 180 度のみ。

### 色（`lib/constants.ts`）

**無彩色**（地とタイトル）— ここは色を持たない。カラースキームの対象外。
| 用途 | 定数 | 値 |
|---|---|---|
| 地（EXPLORE / TASK） | `BD_GREY` | `#ECECEA` |
| 地（JOURNAL） | `JOURNAL_BG` | `#B3B3AE` |
| 紙 | `PAPER` | `#FAFAF9` |
| 墨 | `INK` | `#1A1A18` |
| 控えめな字 | `MUTED` | `#8E8E88` |

**カラースキーム `SCHEME`**（2026-08-16 確定・参照画像＝Spotify の配色見本）
— **色の出どころはここ 1 つ**。9 つの「地の色 × その上に載る文字の色」の組で、
使うのは**地の色**の方。文字の色は、その地の上に何かを載せるときの相方。

| 名前 | 地 | 相方の文字 |
|---|---|---|
| `pink` | `#F76FA1` | `#4B2438` |
| `orange` | `#FA6E31` | `#1B2B4F` |
| `sky` | `#509BF5` | `#C4F0C5` |
| `red` | `#EE1B33` | `#F9C3C9` |
| `violet` | `#4100F5` | `#FFFFFF` |
| `yellow` | `#F5E837` | `#EF3E23` |
| `navy` | `#1E3264` | `#F573A0` |
| `wine` | `#8C1932` | `#A3E3C8` |
| `forest` | `#04624A` | `#F7D6D3` |

前からある 6 つのアクセントは**名前を変えずに中身を差し替えて**ある
（`BLUE`=violet / `RUST`=red / `GREEN`=forest / `GOLD`=orange /
`PLUM`=wine / `SLATE`=navy）。呼び出し側は数十箇所あるので、名前は動かさない。
★`GOLD` に黄ではなく橙を当てているのは、その面に**白い ✓ が載る**ため
（黄だと白が消える）。

★**新しい色を hex で直書きしない。** 必ず `SCHEME` から取る。地と字面は
**必ず同じ組から**取ること（片方だけ差し替えると、明るい地に明るい字が
乗る事故が起きる）。

### 幾何アルファベット（`components/GeoType.tsx`）
見本から**画像解析して転写**した書体。1 文字＝162×162 の正方形を
「セル 78px＋溝 6px の 2×2 グリッド」に割り、`full` / 四半円 / 端丸帯 / 空 の
4 種を置き、**繋がっているところだけ溝を塗る**。等幅。A–Z と 0–9。
**使うのは `Masthead` のアプリ名だけ**。タグ・棚の見出しは本文の書体
（`SectionLabel`、`components/common.tsx`）。

### ★画面の地色（`lib/ground.ts`）— 持ち主はここだけ

**背景が途切れたら、まずここを見る。**

- `html` の `background-color` と **`<meta name="theme-color">` を必ずセットで**
  書く。ホーム画面へ追加した PWA では、**iOS が自分で塗る領域**
  （キーボードの手前の帯・画面のいちばん下・セーフエリア）に theme-color を
  使うので、片方だけ合わせても帯が残る（2026-08-17 に
  `themeColor: "#1A1A18"` の固定値のままだったのが原因で、
  「キーボードのところで背景が途切れる」「全タブの下端で途切れる」の両方が出た）。
- 積み重ね式（`pushGround(color)` が**戻す関数**を返す）。いちばん上が勝つので、
  全画面の面（声の録音・タスクの入力）が開いている間はその色、閉じたら
  アプリの地色へ自然に戻る。**書き手を2つにしないこと**（以前 `AppBackdrop` と
  `TaskComposer` が両方 `html` を書いていて、閉じても戻らないことがあった）。
- ★新しく全画面の面を作るときは必ず `pushGround` を呼ぶ。
- ★★**積む色は「その画面のいちばん下に見えている面の色」**（2026-08-17）。
  入力画面は地が `CHARCOAL` でも**下端に見えているのは帯（`LIFT`）**なので、
  `pushGround(LIFT)` を積む。`CHARCOAL` を積んでいた頃は、キーボードの手前
  （次候補の帯のあたり）と画面の下端に色の違う帯が出て「境目が見える」と
  報告された。下敷き（全画面のオーバーレイ）も同じ色にする。
  **検証は「画面の最下 1px の色 == `html` の色」**（Playwright で
  `elementFromPoint` の背景を辿る。全タブ＋入力画面で一致することを確認済み）。

### ★構造的な約束（破ると必ず壊れる）

1. **CSS の 3D 変形を使わない。** `perspective` / `preserve-3d` / `rotateX/Y` は
   Safari の合成で 5 回崩れた（archive: ui-binder §5 / §7.9 / §7.14、
   shell-redesign §7.32 / §7.35）。
2. **`position: fixed` は必ず `createPortal` で `document.body` 直下へ。**
   `.app-track` が `transform` を持つので、その中の fixed は画面ではなく
   トラックを基準に解決され、画面外へ飛ぶ（archive: shell-redesign §26.1）。
3. **アプリの地色を塗ってよいのは `AppColumn` だけ。** 出どころが 2 つあると
   遷移中に混ざって境目が出る（同 §51）。地色を変えるときは
   `components/AppBackdrop.tsx` の `groundOf` だけを直す。
4. **`body` に `background` を書かない。** body 直下の負の z-index にある地を
   塗りつぶす。地色は `html` にだけ書く（同 §27.1）。
5. **タブバーはフローから外す**（`position: absolute; bottom: 0`、`fixed` は
   iOS の URL バー伸縮でズレるので禁止）。本文の領域は常に画面いっぱい。
   画面の四隅まで敷きたいタブは **`.full-bleed` を付けるだけ**で、
   タブバーの高さの計算をタブ側に書かない。高さを知っているのは
   `lib/constants.ts` の `NAV_H` / `TAB_PAD_TOP` と、それを CSS 変数
   `--nav-h` / `--pad-top` として配る `AppShell` だけ（同 §50）。
6. **`document.querySelector("[data-tab-scroll-root]")` は使わない。**
   スクロールルートは 3 つ同時に存在し、先頭は必ずタスクの列。
   自分の列は `el.closest("[data-tab-scroll-root]")` で引く。
7. **共有する定数は `lib/constants.ts` へ。** `AppShell` に置くと
   `Dashboard → AppShell → Dashboard` の循環参照でビルドが落ちる。
8. ★★★**全画面の器は、一度置いたら二度と寸法を書き換えない。**
   `position: fixed; inset: 0; width: 100vw; height: 100lvh; min-height: 100%`
   （録音のオーバーレイ `VoiceOverlay` と同じ書き方。実機でなめらかだと
   ユーザーが認めた唯一の全画面）。キーボードのぶんは **`--kb` に入れ、
   中の要素の `transform` だけ**で追う。
   - 器を `visualViewport` の矩形へ貼り直すやり方（第 9〜12 巡）は、
     キーボードが動くたびに**画面まるごとのレイアウトをやり直す**。
     実機で「ガクッとなる／幅が変わる／スクロールの判定が入る」と 3 度報告された。
   - `100lvh` 固定＋`transform` は第 8 巡でも試して崩れたが、**原因は
     `--kb` の測り方と `top` の扱い**であって、この形自体ではなかった。
   ★★**`--kb` は「器の下端が、見えている下端よりどれだけ下か」で測る**:
   `max(0, 器の rect.bottom − (vv.offsetTop + vv.height))`。
   器を実測するので、**レイアウトビューポートが縮む端末（iOS スタンドアロン）
   でも、縮まない端末でも、同じ式が正しい**。
   `innerHeight - vv.height`（第 8 巡）は前者で 0 になるので使ってはいけない。
   ★`vv.offsetTop > 0` になったら `window.scrollTo(0, 0)` で押し戻す
   （器は画面に貼り付いているので、ずれると上のバーが画面の外へ出る）。
9. ★★**スクロールする器の中に `Press` を置かない。**
   `Press`（`components/tasks/Popover.tsx`）は iOS のフォーカス移動を止める
   ために**非 passive な `touchstart` で `preventDefault()`** している。
   **それはスクロールも打ち消す。** 時刻のダイアルの各値を `Press` にしたら
   指で回せなくなった（2026-08-17 に実機で「操作できません」と報告）。
   スクロールで選ぶものは**押せる面にしない**（`<span>` で置く）。
10. ★**題が空のタスク・候補は保存しない／読み込みで捨てる。** 「＋」は題が
   空のまま作って開くので、閉じる処理を通らずに終わる（背面へ回る・アプリを
   落とす）と残骸になり、円環に**無題の図形**として並び続ける
   （2026-08-18 に報告）。`migrate()` が読み込み時に捨て、背面での保存
   （`visibilitychange`）も題が空なら行わない。

### ★性能の作法
- **毎フレーム変わる値を React の state に入れない。** ref と、CSS カスタム
  プロパティ（`--drag` / `--dash`）への直接書き込み、canvas で扱う。
  ドラッグ中の React のレンダーは 0 回が正しい。
- **`@property` は使わない**（Safari 16.4 未満に無い）。トランジションは
  カスタムプロパティ自身ではなく、そこから `calc()` で導かれる
  transform / width / opacity の側に掛ける。
- **重いものは合成レイヤーへ上げる**（`will-change: transform`）。巨大な図形の
  隣で canvas を毎フレーム描くと、同じレイヤーを汚して図形ごと塗り直される。
- **`animation-direction: reverse` では再生し直されない。** 逆再生させたい
  ときは**別名のキーフレーム**を用意して `animation-name` ごと差し替える。
- **`animation-fill-mode`** … `both` にすると最後のコマの transform が
  残り続け、JS が書く値を上書きする。遅延の間だけ見せたいなら `backwards`。

---

## 4. タスクアプリ（§53 で確定）

**アイソメも 3D も持たない。断面の形をベタ塗りで描くだけ。**

図形は**ひとつ**。ビューを切り替えても**形も位置も変わらず、載る文字だけ**が
変わる（2026-08-16 確定）。

| ビュー | 載る文字 |
|---|---|
| ネーム `name` | **タスクのタイトル**（中央・最低 16px ＋ 面積に比例） |
| タグ `tag` | **タグの英字**（図形いっぱい） |

★以前は FRONT を長方形にしていたが、それだと FRONT だけで情報が完結し、
「埋まっている側面の数 → 形」が何も意味しなくなっていた。両ビューとも同じ
断面の形で描くことで、形が常に効く。**切り替えで落とし直さない**
（山は 1px も動かない。文字だけ差し替えて描き直す）。

**書体はタグごとに 1 つ**。`tagFace()` が `FONT_FACES` の番号を引く
（同じタグ＝同じ書体）。★**明朝は使わない**（2026-08-16 確定）。
ゴシック系だけで骨格の違う 5 つ（太 / 丸ゴ / 極太 / 極太斜体 / 太斜体）を当てる。
★SOCIAL（赤）は ゴシック 400 →（細くて色面の上で消える）→ Dela 斜体 →
（**iOS は和文の斜体を合成しない**ので黄の Dela と同じに見えた）→ Reggae One →
**M PLUS 1 (800)** と替えた。**斜体で見分けを作らないこと。**
実測の画素差（同じ字を 200px で焼いて総当たり）: M PLUS vs Dela 19.3% /
M PLUS vs ゴシック 700 15.3% / 使っている組でいちばん近いのは
ゴシック 700 と丸ゴ 500 の 8.8%（色が違うので実用上は分かる）。

**タグを持たない図形は作らない**。`resolveTag()` が
①本人／Cowork が決めたタグ → ②題や側面の言葉から見立て（`inferTag`）→
③id から決定的に 5 つのどれかへ、の順で必ず 1 つ与える。

**図形の色と文字の色はカラースキームの組そのまま**で、タグと**一対一対応**。
明度から白黒を選ぶやり方（`inkOn`）は削除した。

| タグ | 図形 | 文字 | 書体 |
|---|---|---|---|
| `WORK` | `sky` `#509BF5` | `#C4F0C5` | ゴシック 700 |
| `LIFE` | `forest` `#04624A` | `#F7D6D3` | 丸ゴシック 500 |
| `WELLNESS` | `yellow` `#F5E837` | `#EF3E23` | Dela（極太） |
| `SOCIAL` | `red` `#EE1B33` | `#F9C3C9` | M PLUS 1（800） |
| `GROWTH` | `orange` `#FA6E31` | `#1B2B4F` | ゴシック 700 斜体 |

### 寸法の対応
```
塗られる面積 = 重要度 … weightArea(小1.6/中3.6/大8.0) × 期限の倍率
  期限の倍率 … 今日・過ぎ 2.2 / 1〜2日 1.6 / 3〜7日 1.1
               8〜30日 0.7 / 31日〜 0.45 / 期日なし 0.55
  → 面積の幅 24.4:1（辺の比 4.9:1）。★大きさに効くのはこれだけ
  形ごとの塗り率(inkRatio)で外接箱を広げるので、**形が違っても同じ重要度なら
  色の量が同じ**（円/半円 0.78・三角 0.50・四角 1.00）
縦横比 … **四角だけ**題の長さのはしご 1:2 / 3:4 / 1:1 / 3:2 / 5:2 / 4:1
        円 1:1・半円 2:1・三角 1.155:1 は**必ずその比を保つ**
形     … 埋まっている側面の数（1..4）      lib/solid.ts
        題だけ=円 / ＋日付=半円 / ＋メモ=三角 / ＋持ち物=四角
        ★2 番目の面は **dueDate**（自由文の `when` は 2026-08-16 に廃止）
スラブ … 残っている手順（大きさには効かない）
色・書体 … タグ                            lib/taskTags.ts
落ちる順 … 切迫度（期日）
```
★**幅に上限を置かないこと**。以前は `LEN_MAX 4.0` で頭打ちにして余りを高さへ
回していたため、本来 2:1 の半円が **1.06:1 のドームに潰れていた**。画面に
収める役目は一括スケールが持つ。
★`sectionOutline` は**縦横とも −0.5〜0.5** に正規化。`(w, h)` を掛けた外接箱が
ちょうど `w × h` になり、「箱の面積 × 塗り率 ＝ 塗られる面積」が形によらず
成立する。その形の本来の比は `naturalRatio()` が別に持つ。

### タイトルの組み方
`layoutInShape()` が **`max(16px, 0.34 × √面積 × unit)`**（上限 64px）から始め、
折り返し、それでも入らなければ縮める（最大 3 行）。
★基準に √面積 を使うのは**縦横比に左右されないため**。

★**折り返す幅は行ごとに違う**（2026-08-16 確定）。断面のその高さでの半幅を
`halfWidthAt(sides, y)` から引き、行の**狭い方の端**で測る。矩形 1 つ
（`innerBox`）に収めていた頃は三角が外接箱の 23% しか使えず（四角は 79%）、
同じ面積でも**三角だけ文字が半分以下**になっていた。いまは 1.5 倍。
縦の置き場所も形で決める — **三角は下寄せ**（広い方へ寄せる）、他は中央。
`innerBox` はタグビュー（`fitText` で英字を図形いっぱい）だけが使う。

### 入力画面（`components/tasks/TaskComposer.tsx`）

TickTick 型の**即時入力**（2026-08-16 確定。方眼の展開図 `TaskSheet` は削除）。
地はチャコール（`CHARCOAL`。声の録音と同じ墨）。

```
✕                    DELETE  COMPLETE   ← 完了・削除は常時ここ
        ● いまの図形（実寸で変わる）
──────────────────────────
タスクの名前                            ← 開いたら即キーボード
▪ 手順1  ▪ 手順2                       ← Enter で行が増える＝手順
──────────────────────────
日付  メモ  持ち物  重要度  タグ          ← 値が入ると印が灯る
```

- **1 行目＝題 / 2 行目以降＝手順**。行はそれぞれ独立した `<textarea>`
  （1 つの textarea に混ぜると行ごとに字を変えられず、行頭の点も合わない）。
  Enter でキャレットから割って次の行へ、行頭の Backspace で前の行とつなげる。
- **下書きはこの画面が持ち、確定は閉じるときの 1 回**。1 文字ごとに親へ返すと
  保存（localStorage ＋ クラウド）と山の作り直しが毎打鍵走る。背面へ回った
  ときだけ保険で保存する（`visibilitychange`）。
- ★★★**器は一度置いたら二度と動かさない**（2026-08-18・第13巡。ここに 5 巡ぶん
  費やした）。寸法は録音のオーバーレイ（`VoiceOverlay`）と**同じ書き方**にする —
  `position: fixed; inset: 0; width: 100vw; height: 100lvh; min-height: 100%`。
  あれは実機でなめらかだとユーザーが認めた唯一の全画面で、その理由は
  **器の寸法を JS が一度も書き換えないこと**にある。

  第 8〜12 巡はここで `top` / `height` を `visualViewport` に合わせて書き替えて
  いた。キーボードが動くたびに**画面まるごとのレイアウトをやり直す**ので、
  「ガクッとなる」「幅が変わる」「スクロールの判定が入る」が全部ここから出た。

  **キーボードのぶんは `--kb` に入れ、動かすのは 2 つの transform だけ**
  （＝レイアウトも塗り直しも起きず、合成のやり直しだけで済む）:
  - 帯（`[data-dock]`）… `translateY(calc(-1 * var(--kb)))`
  - 図形（`ShapeStage`）… `translateY(calc(var(--kb) / -2))`
    舞台は「上のバーの下〜帯の上」。帯が kb 上がると見えている領域の中心は
    kb/2 上がるので、絵も kb/2 上げればちょうど真ん中に居続ける。
  - ポップオーバーの外側 … `translateY(calc(-1 * var(--kb)))`＋
    `max-height: calc(100% - var(--kb) - 8px)`。
  どれも `transition: transform 280ms cubic-bezier(0.32,0.72,0,1)`。iOS は
  キーボードの 250ms のあいだ resize を数回しか飛ばさないので、その間を埋める。
- ★★★**`--kb` は「器の組版上の高さ」と「見えている高さ」の差で測る**
  （2026-08-18・第18巡で確定。ここは 5 通り試して 4 通り外している）。

  ```
  kb = max(0, 器の offsetHeight − vv.height)
  ```

  **`offsetHeight` は組版上の高さ**なので、出入りの transform も
  `offsetTop` の端末ごとの解釈も、見えている矩形のずれ（後述）も**一切混ざらない**。
  高さには曖昧さが無い — これが選んだ理由。

  **端末がどちらの流儀でも正しい**:
  - レイアウトごと縮む（iOS のスタンドアロン）… 器も縮むので差は 0。
    帯はもともと見えている下端にいる。
  - `visualViewport` だけ縮む（通常の Safari）… 器は 844 のまま、
    見えている高さは 470。差 374 ぶん持ち上げればよい。

  **外した 4 通りと、外した理由**（同じ轍を踏まないために全部残す）:

  | 式 | 巡 | 何が起きたか |
  |---|---|---|
  | `innerHeight − vv.height` | 8 | スタンドアロンではレイアウトごと縮むので**常に 0** |
  | 器の `rect.bottom − (offsetTop + vv.height)` | 13〜15 | 出入りの transform が乗る／**ずれが引き算に紛れ込む**（帯だけ合って上のバーが画面の外へ） |
  | `offsetTop + offsetHeight` | 16 | `position: fixed` かつ `offsetParent` が null のときの **`offsetTop` は実装まかせ** |
  | 0×0 の物差しの `rect.bottom` | 17 | 物差しも画面に貼るので、結局**ずれが混ざる** |
- ★★★**`--kb` を動かしてよい場面はごく限られる**（2026-08-18・第15〜16巡。
  「メモや持ち物のアイコンで画面が動く」を 2 度差し戻された）。
  1. **何かが開いているあいだは一切聞かない**。日程のシートは**こちらから**
     キーボードを閉じているし、ポップオーバー（メモ・持ち物・重要度・タグ）は
     **キーボードを出したまま**にする約束なので、変わる理由が無い。
     `whenRef.current !== "" || toolRef.current` なら `return`。
  2. **小さな揺れは無視する**（`KB_MIN_STEP = 60px`）。iOS は欄を移るたびに
     予測変換の帯（≒40px）を出し入れする。
  3. **上がるのは即座に、下がるのは 200ms 待って測り直してから**。
  ★凍らせるので、**シートの側は `--kb` を見てはいけない**（`bottom: 0` のまま）。
- ★★★**見えている矩形の「ずれ」（`vv.offsetTop`）は `--vvtop` で追う**
  （2026-08-18・第18巡）。iOS はキーボードを出すとき、**見えている矩形そのものを
  下へずらす**ことがある（`vv.offsetTop > 0`）。器は `position: fixed; top: 0` で
  画面に貼り付いたままなので、追わないと**器の上端が見えている範囲の外へ出る** —
  実機で「上のバー（DELETE / COMPLETE）が消え、図形が上で切れる」として出た。

  **`components/BottomSheet.tsx` には同じ教訓が先に書いてある**。入力画面だけが
  落としていた。

  ★**器そのものは動かさない約束は守る**。器の子を 1 枚の面（`[data-fit]`）で
  包み、そこに `transform: translateY(var(--vvtop, 0px))` を掛ける。包むのは
  上のバー / 図形の領域 / 帯のドック / 日程のシートの 4 つぜんぶ。合成の
  やり直しだけで済み、下へはみ出したぶんは器の `overflow: clip` が切る。
  ★包む面は**出入りのアニメーションを持たない**（スライドは器が持つ）が、
  **ずれの追従には帯と同じ間合い**（`KB_EASE`）を掛ける。ずれは本来
  キーボードと一緒に動くものなので、瞬間移動させると跳ねて見える。

  ★★★**ずれは凍らせない。いつでも追う**（2026-08-19・第21巡で決着）。
  ここは 3 巡かけて行き来したので、結論と外した理由を全部残す:

  | 巡 | やったこと | 何が起きたか |
  |---|---|---|
  | 18 | ずれを追い始めた | 上のバーが画面の外へ出るのが直った |
  | 19 | 開いている間は**凍らせた** | 「4 回に 1 回ガクッと動く」を抑えたつもり |
  | 20 | 日程シートだけ凍結を外した | シートの崩れは直った。**ポップオーバーは残した** |
  | 21 | **凍結を全部やめた** | 「アイコンをタップすると崩れる」が直った |

  第19〜20巡の判断は「ポップオーバーはキーボードを出したままにする約束だから、
  凍らせても取り残されない」だった。**この前提が誤り** — 動くのはキーボードの
  高さではなく**見えている矩形のずれ**の方。iOS はポップオーバーが開いた拍子に
  （焦点が行を出入りする・キーボードの上の帯が変わる）ずれを付け替える。
  凍らせていたので**古いずれのまま取り残され**、器の中身が画面の上へ突き抜けた
  — 上のバーが画面の外、帯がキーボードの遥か上、あいだが大きく空く。
  ずれ方がポップオーバーごとに違うので、帯の高さも一つずつ違って見えた。

  ★**跳ねないための手当ては「追わないこと」ではなく「間合いを持って追うこと」**。
  第19巡の跳ねは `[data-fit]` に間合いが無かったから。いまは帯と同じ
  `KB_EASE`（280ms）が掛かっているので、ずれが変わっても滑って追いつく。
  1〜2px の往復で動きが走り続けないよう `SHIFT_MIN_STEP = 8px` の段だけ置く。

  ★**凍らせるのは `--kb`（持ち上げ量）だけ**。あれはキーボードの高さの話で、
  ポップオーバー中に動く理由が本当に無い（日程のシートは自分でキーボードを
  閉じているので、追うと帯が落ちて閉じると上がり直す）。

- ★★**文書のスクロールも見る**（2026-08-19・第21巡）。iOS はキャレットを
  見せるために**文書ごと動かす**ことがあり、`position: fixed` の器はそれに
  引きずられる。ずれは `vv.offsetTop + window.scrollY` で測り、
  **`window` の `scroll` も購読する**（ここを聞いていなかった）。
  `html`/`body` は `overflow: hidden` なので通常 `scrollY` は 0 — 0 のときは
  何も変わらない保険。

  ★`window.scrollTo(0,0)` で押し戻すのはやめた（html は `overflow: hidden` なので
  効かないうえ、動いている最中に割り込むとつっかえる）。
- ★**図形の canvas は測り直さない**。`ShapeStage` は**それまでに見た一番大きい箱**
  を持ち続け、canvas の寸法も倍率も動かさない（器が動かなくなったいまは
  そもそも変わらないが、保険として残す）。焼き直しは 4× 絞りで 1 枚 50ms 級。
  測るのは **`ResizeObserver` の `contentRect`**（`getBoundingClientRect` は
  出るときの animation の scale が乗った値を返し、**7% 小さい箱**を掴む）。
- ★★**図形は「後から、きちんと登場させる」**（2026-08-19・第21巡にユーザー確定）。
  焼くのは板が滑り終わってから（下）だが、ただ現れるのではなく
  **0.86 倍・透明から実寸・不透明へ 260ms**（`tc-solid-in`）。「遅れて出た」
  ではなく「あとから現れる」と読めるようにするため。
  ★同時に出す案は採らなかった — 焼きは 4× 絞りで 130ms 級で、滑りの最後に
  重なると必ずフレームが落ちる（第17巡で実際にそうなっている）。
- ★★**情報を足して図形が変わるときは、前の絵を残したまま重ねて入れ替える**
  （同巡にユーザー指定）。canvas を 2 枚重ね、前の絵を薄れさせながら新しい絵を
  濃くする（`tc-solid-swap-out` / `tc-solid-swap-in`・280ms）。
  ★**どちらの層もいつも同じ見た目の大きさ**になるように scale を当てる
  （`--sc-from` / `--sc-to` ＝ 面積の平方根の比）。こうするとディゾルブではなく
  **1 つの図形が大きくなっていく**ように見え、「大きさ＝重要度×期限」が
  操作しながら読める。★透ける瞬間を作らないので**点滅にならない**
  （第17巡の教訓）。動きが終わったら層は 1 枚に戻す。
- ★**出るアニメーションのあいだは図形を焼かない**（`STAGE_DELAY_MS = 300`）。
  器の登場・帯のせり上がり・キーボードの追従が同時に走っているところへ
  焼きを重ねると 1 フレーム落ちて「引っ掛かり」として見える。落ち着いてから
  出す（ユーザー確定「必ずしも早く起動するのを優先しなくてよい。安定的に、
  かつスムーズに引っ掛かりなく」）。
- ★★**キーボードは閉じない**（日付以外。実機で 4 度差し戻された）。閉じると器の
  高さが変わり、ボタンの位置ごと飛ぶ。**4 段構えで守る**（`Popover.tsx` の
  `Press` ＋ `TaskComposer` の 2 つの受け皿）:

  1. **押せる面は `<button>` ではなく `<div role="button">`**（`Press`）。
     iOS は `<button>` をタップすると、いま focus している欄を blur する。
     `div`（tabindex なし）はフォーカスを受け取れないので**移動先が無い**。
  2. `pointerdown` で `preventDefault` して**その場で処理する**（`click` を
     待たない）。＋ **素の非 passive な `touchstart` でも `preventDefault`** —
     iOS でフォーカス移動を確実に止められるのはこちらで、React は root の
     `touchstart` を passive で張るため React の `onTouchStart` では止められない。
     処理を走らせるのは `pointerdown` 側だけ（両方だと二重になる）。
  3. 行の `onBlur` の受け皿 … フォーカスが外れたら**その場で同期的に**戻す
     （非同期だと、もう閉じたあとなので開き直せない）。別の入力欄へ移るとき
     （`relatedTarget` が TEXTAREA / INPUT）は通す。
  4. **`useLayoutEffect` の受け皿**（毎レンダー） … `activeElement` が `body` に
     落ちていたら戻す。★**フォーカスしている要素が消える**とき（メモ・持ち物を
     閉じる、別の項目へ切り替える）は、その欄の focusout であって行の focusout
     ではないので 3 では拾えない。layout effect は**同じコミットの中**で走るので、
     描かれる前に戻せる。これが最後の砦で、**構造的に閉じられなくなる**。

  `onClick` は使わない。文字を打つ欄（題・手順・メモ・持ち物）だけがフォーカスを
  受ける。`keepKeyboard`（器のルートの `onMouseDown`）は余白を叩いたときの保険。

  ★★★**押せる面には 3 つ揃って要る**（2026-08-18・第19巡。実機で
  「キーボード上のアイコンの反応が悪い」と報告された）:

  1. **`touch-action: none`**。これが無いと iOS は「この指はページを送るための
     ものか」を見極めるまで `pointerdown` を握る。送る余地が無いことを先に
     宣言しておけば、指が触れた瞬間に飛ぶ。`Press` の既定に入れてある。
  2. **当たり判定は 44、描く丸は 32**。下の帯のアイコンは丸そのものを押せる面に
     していたので、指の腹が 6px 外れると帯に当たって**何も起きなかった**。
     `margin: -6px` で場所は 32 のままにし、当たり判定だけ広げる。
  3. **沈む合図は自分で出す**（`data-pressed`）。`:active` は
     **`touchstart` の既定を止めた面では iOS が当てないことがある**ので、
     押しても何も起きないように見える。`.tc-lamp` は `:active` と
     `[data-pressed="1"]` の両方を見る。

  ★★**ただし 3・4 は「画面の中を押したとき」だけ**（2026-08-18）。
  フォーカスが外れる理由は 2 つあり、**逆のことをしなければならない**:
  - **画面の中を押した** → 取りこぼし。**その場で戻す**（上のとおり）。
  - **画面の外**（キーボードの「完了」・下へスワイプ）→ 「閉じたい」という意思。
    **戻さない**。器が受けて**入力画面ごと閉じる**。

  ページの中に iOS の「完了」を知る手がかりは無いので、`Press` が
  **最後に押された時刻**（`pressedRecently()`）で見分ける（500ms）。
- ★★**キーボードが引っ込んだら入力画面も閉じる**（2026-08-18 にユーザー指定
  「キーボード上のバーのチェックマークで閉じたら、タスク入力画面も閉じて」）。

  ★★★**閉じる合図は `--kb` ただ 1 つ**（2026-08-18・第18巡で約束を裏返した）。
  第 11〜17 巡は `focusout` も合図に数えていた（260ms たっても focus がどこにも
  無ければ閉じる）。これが**「＋を押すと立ち上がってすぐ閉じる」の主因**だった —
  **主スレッドが詰まっていると `focus()` が居着かない**ので、山が動いている
  gravity で＋を押したときほど起きた。

  いまの `focusout` の受け皿は**焦点を戻すだけで、決して閉じない**:

  ```
  focusout → 260ms 後に見る
    どこかの欄に focus がある     → 何もしない
    キーボードがまだ出ている      → 題の行へ焦点を戻す（閉じない）
    キーボードが引っ込んでいる    → 閉じたい意思。--kb 側が閉じる
  ```

  ★★★**時計ではなく「いまの状態」で決める**。第 17 巡までは
  `setTimeout(260)` の中で「開いてから 600ms 以内か」を見ていたが、
  6× に絞った主スレッドでは**その 260ms のタイマー自体が 940ms や 3195ms に
  遅れて発火する**ので、壁時計の窓は用をなさない。見るのは
  「キーボードはまだ出ているか」——これは遅れても正しい。

  `--kb` 側の条件は 2 つ:
  1. 120px 以上出たことがある（`kbSeen`）→ 60px を下回った。
  2. **下がりは 200ms ごとに 2 回続けて確かめる**。一瞬の谷（欄の移動・予測変換の
     出入り）で閉じないため。確かめ 400ms ＋ 出ていくスライド 300ms なので、
     キーボードを閉じてから消えるまで実測 700ms。

  **日程のシートとポップオーバーが開いている間は `--kb` を凍らせる**
  （あそこはわざと閉じている／わざと開けたままにしている）。
  書いたものはそのまま持って閉じる（`onClose(draft)`）ので、何も失わない。
- ★**開いた直後の 600ms は閉じない**（`OPEN_GUARD_MS`）。＋ボタンは `Press` では
  ない素の `<button>` なので、押した直後は `pressedRecently()` が false になる。
  守りは**入力画面の側 1 か所に集める**（＋はタブ側の部品で、あそこで
  `touchstart` を `preventDefault` する理由が無い）。
- ★★★**閉じる動きは「触れた瞬間から動く曲線」で**（2026-08-19・第20巡。
  実機で「閉じる時のアニメーションがワンテンポ遅れる」と報告された）。
  出ていく `cubic-bezier(0.32, 0, 0.67, 0)` は**制御点が 2 つとも y=0** で、
  最初の 3 分の 1 を 0 に張り付いたまま過ごす（60ms たっても **2%** しか
  動かない）。動き出しを待たせる曲線は、そのまま「間」として見える。
  `cubic-bezier(0.4, 0, 0.2, 1)` にすると同じ 60ms で **16%**。
  実測（6× に絞って）**20px 下がるまで 127ms → 52ms**。
  ★出るときは 2 フレーム待つのが正しい（下）が、**閉じるときは逆** —
  待つ理由が無いので、触れたそのフレームでクラスを直に付ける（React の
  描き直しを待たない）。`blur()` はそのあと（iOS はここで矩形を作り直す）。
- ★★★**閉じると決めた瞬間から「1 枚の板」にする**（2026-08-19・第21巡。
  実機で「キーボードが閉じ切ったときにガクッと不安定な挙動をしながら下に
  スライドする」と報告された）。

  `commit()` は `--kb` を**書いてから** `leave()` を呼んでいた。`--kb` には
  帯（-kb）・図形（-kb/2）・ポップオーバーが 280ms の transition で追従するので:

  ```
  キーボードが閉じた
   → --kb: 374 → 0 を書く … 帯が 374px、図形が 187px 下へ動き出す
   → その同じ瞬間に leave() … 板ぜんぶが下へ滑り出す
   ＝ 板の中で中身が動きながら、板が滑る
  ```

  **去ると決めたら `--kb` は書かない**（`commit` は `leave` を呼んで `return`）。
  帯の下は `LIFT` の板が `100lvh` 敷いてあるので、帯が浮いたままでも地色は
  途切れない（見た目は 1px も変わらない）。念のため
  `.tc-shell-out [data-fit] / [data-dock] / [data-stage]` の transition も止める。
- ★★**`will-change: transform` は「出入りの最中だけ」置く**（同巡）。
  第17巡に「置かない」と決めたのは、`both` でクラスが動き終わっても外れず
  **692×1380 の canvas を抱えた画面いっぱいの層が居座る**からだった。
  いまは外れる — 出るときは `animationend`（`settled`）で**クラスごと落とし**、
  閉じるときは 300ms 後に要素ごと消える。滑っているあいだだけ層に上がるので、
  canvas と文字の塗り直しが滑りに混ざらない。
- ★**閉じる合図の「確かめ」に 400ms は要らない**（同巡）。第18巡は
  200ms × 2 で確かめていたが、iOS がキーボードを畳むのに 250ms しか
  かからないので、待ちきる前にユーザーが「遅い」と感じる。**90ms × 2**。
  実測、キーボードを閉じてから滑り出すまで **196ms**。
- ★**出るアニメーションは 2 フレーム待ってから掛ける**（第17〜18巡）。
  CSS のアニメーションは**時計で進む**ので、画面を組み立てる作業で主スレッドが 283ms
  詰まると、手が空いたときには 320ms のアニメーションが 88% 進んでいて
  **板が飛んで見える**。`requestAnimationFrame` を 2 回重ねてからクラスを付け、
  それまでは `translate3d(0,100%,0)` で画面の外に置いておく。
- ★**閉じる ✕ は置かない**（同指定）。閉じ方は**画面の余白をタップ**するか
  **キーボードを閉じる**の 2 つ。どちらも指がもう居る場所にある。
- ★**iOS のキーボード上の `^ ∨ ✓` は消せない**（2026-08-18 に決着）。Web に API は
  無く、`contenteditable` へ作り替えても出た。**ユーザーが「諦める」と確定**。
  作り替えは revert したが、そのとき見つけた **IME の不具合だけは残してある** —
  `onKeyDown` は `e.nativeEvent.isComposing` を見ること。見ないと**日本語の変換を
  確定するたびに手順が 1 行増える**。
- ★**左右だけ画面いっぱいにしたいときは `.bleed-x`**（`app/globals.css`）。
  列は左右に 16px の padding を持っているので、**横へ出ていくものは
  そのままだと 16px 内側で切れる**（候補の輪で 3 巡ぶん誤診した）。
- ★ポップオーバーを閉じたら行へキャレットを戻す（`backToRow`）。**閉じる道は
  4 つある**（✕ / 背面板 / ツールバーの丸をもう一度 / 別の項目へ切り替え）ので、
  どれも同じ処理を通す。次に開くものが自分でフォーカスを取る（メモ・持ち物）
  ときだけ戻さない。
- ★**html の地色も墨に**する（`VoiceStudio` と同じ手）。器が届かなかった
  隙間から明るい地が帯になって見える。
- **開いたら即フォーカス**は `useLayoutEffect`（rAF ではない）。iOS は
  タップと同じ処理の流れの中でしかキーボードを開かない。
- ポップオーバーは共通の器（`Popover`）＋中身。**地は墨**（2026-08-17 確定。
  白い紙だと入力画面の墨地から浮いた）。重要度は**円の大きさ**、タグは
  **色の円**を 5 つ。
  ★**置き場所は「上のバーの下 〜 下の帯の上」の領域そのもの**（図形のステージを
  親にした `position:absolute`）。高さは中身のぶんだけで、下の帯のすぐ上に
  貼り、`maxHeight: calc(100% - 8px)` でその領域を**構造的に超えられない**。
  以前は帯を親にして `bottom: 100%` で上へ伸ばしていたため、中身が高いと
  画面の上へはみ出してタブの下に潜り、下へスワイプしないと触れなかった。
  ★**縦スクロールは持たせない**（2026-08-17 指定）。溢れそうなときは中身の側
  （カレンダー）が痩せて収まる。題と手順の並びだけは件数が不定なので例外
  （ダイアログではなくリスト）。
- ★**上のバーと下の帯は `zIndex: 2`**。ポップオーバーの背面板（`zIndex: 1`）が
  上に来ると、完了も閉じるもツールバーも叩けない。`.tc-sheet` は transform の
  アニメーションを持ち続けるので**帯自身が重なりの文脈になり**、帯の中の要素に
  `zIndex` を振っても背面板には勝てない（帯ごと上げること）。
- 寸法（実測。下の帯 150px → 96px → **81px**）… 上のバーの丸／ピル 36 /
  ツールバーの丸 32・アイコン 18 / 帯の上下 7・6 / 題 21px・手順 15px。
- ★★**キーボードが出ているあいだは `env(safe-area-inset-bottom)` を使わない。**
  iOS は**キーボードが出ていても 34px のまま報告する**ので、そのぶんの死んだ
  帯がキーボードの真上に残る（2026-08-17・18 に「キーボードとアイコンの隙間が変」
  と報告）。**JS の分岐は要らない** — `--kb` を引けば CSS だけで切り替わる:

  ```css
  padding-bottom: max(6px, calc(env(safe-area-inset-bottom) - var(--kb, 0px)));
  ```

  ★★帯の下を埋める板は**置かない**（2026-08-18 に撤去）。下敷き（zIndex 59）が
  すでに画面ぜんぶを `LIFT` で塗っているので、器の下端より下は放っておいても
  帯と同じ色になる。置いていたころは **器の中に 60vh のはみ出し**を作っていて、
  iOS がキャレットを見せようと**器ごとスクロール**できてしまっていた
  （実機で「アイコンとタイトルのウィンドウがスクロールして動く」と報告）。
- ★★★**全画面のオーバーレイは「送れる箱を減らす」ではなく「動かせるものを
  名指しで許す」側から書く**（2026-08-18・第14巡。「まだスクロールできる」が
  3 巡続いた）。器の中を `clip` にしても消えなかったのは、**送っているのが器では
  なかった**から。iOS は指の下に送れる箱が見つからないと、**その先（後ろのタブの
  列・ページ・見えている矩形そのもの）を送りにいく**。だから 2 段構えで止める:

  1. `document.documentElement.dataset.overlay = "1"` を立て、CSS で
     **後ろのタブの列を止める**（`[data-overlay] [data-tab-scroll-root] { overflow: hidden !important; }`）。
  2. **`touchmove` を素の非 passive で受け、本当に送れる箱の中でなければ
     その場で `preventDefault()`**。「本当に送れる箱」＝ 祖先をたどって
     `overflow-y/x: auto|scroll` かつ**実際にはみ出している**もの
     （行の並び・カレンダー・ダイアル・タイムライン）。二本指も止める。

  ★`touchmove` の `preventDefault` は**スクロールだけ**を止め、`pointermove` は
  流れ続ける。カレンダーの横払いも時刻のダイアルも生きたままである（実測済み）。
- ★**器の中に「はみ出し」を作らないこと**（2026-08-18）。
  `overflow: hidden` の箱は**送れる箱**なので、iOS はキャレットを見せるために
  そこを送ってくるし、右端にスクロールバーも出す。**`overflow: clip`** は
  箱そのものを作らないので、構造的に送れない。
  **器（`[data-composer-shell]`）自身も `clip`**。図形の canvas のように
  器より大きいものを置く面も `clip`。行の並びだけは `auto`（手順は増える）だが、
  `scrollbar-width: none` でバーは出さない。
  検証は「器と、その中のどの箱も `scrollTop` を書いても 0 のまま」
  （`scrollHeight` の大小ではない — `clip` ははみ出しても送れない）。
- ★★**閉じるときは追従そのものを止める**（`leftRef`）。閉じるとキーボードも
  引っ込むので、放っておくと帯と図形が下りてくる動きが見えてしまう
  （実機で「閉じた時に画面が上下に動く」と報告）。
- ★★★**出入りは「1 枚の板が下から出て、下へ帰る」だけ**
  （2026-08-18・第16巡にユーザー指定）。
  `.tc-shell-in` = `translateY(100%) → 0`（320ms）/
  `.tc-shell-out` = `0 → translateY(100%)`（280ms）。**下敷きも同じだけ動かす**。
  ★**中の部品は自前の出入りを持たせない**（帯の `.tc-sheet` も外した）。
  持たせると板の動きと二重になる。

  不透明度で出し入れする形は 4 度作り直して 4 度とも差し戻された。薄くしている
  最中は**器も中身も半透明**になるので、後ろの画面と図形が重なって
  「図形が残っている」ように見えるのが避けられない。スライドなら中身と地が
  一枚の板として動くので、**それが構造的に起きない**。`LEAVE_MS` は 300。
  ★器に `transform` を掛けてよい — 器は画面いっぱいで寸法が動かないので、
  中の `position: fixed`（ポップオーバーの背面板）が器基準になっても同じ矩形。
  **ただし `--kb` の測定は「物差し」で行うこと**（上記）。
- ★★★**動き出しは「組み立てが終わってから」**（2026-08-18・第17巡）。
  CSS のアニメーションは**時計で進む**。組み立て（React のマウント・レイアウト・
  フォーカス）に 283ms かかっているあいだも時計は進むので、手が空いたときには
  320ms のうち 88% が終わっている ＝ **板がいきなり所定の位置に現れる**。
  これが「ガクッと動く」の正体だった。時間で誤魔化さず、**組み立てが済んだ
  次のフレーム（`rAF` を 2 回）で初めてクラスを付ける**。それまでは
  inline の `translateY(100%)` で画面の下に置いておく。
  → 実測（4× 絞り）板の位置は 844→605→269→119→62→34→17→8→0 と段階を踏む。
- ★**図形を焼くのは `animationend` を待ってから**（同巡）。1 枚 130ms 級なので、
  時間で待つと（第13巡の 300ms）スライド 320ms の**最後の数フレームに重なる**。
  合図で待てばずれない。→ 実測で焼きは 600〜700ms 台へ移った。
- ★`will-change` は置かない。`both` なのでクラスは動き終わっても外れず、
  **画面いっぱいの層が居座る**（中に 692×1380 の canvas を抱えている）。
- ★★★**入力画面は「ただのオーバーレイ」。開け閉めで後ろを動かさない**
  （2026-08-18 にユーザー確定「タスク UI はレコードと同じでオーバーレイの扱い。
  背景とは関係なく動作し、閉じたら背景もそのまま維持されていて欲しい」）。
  - **drift** … ＋ で作ったものは**保存せずタブの state に持つだけ**。以前は
    作った瞬間に候補として保存していたので、開いた瞬間に輪が 4→5 件になって
    回り、閉じるとまた戻っていた（「閉じる瞬間と閉じた後の画面が違う」）。
    題が付いて確定したときに初めて輪へ入る。
  - **gravity** … drift と**同じ形**（`addTask` も保存せずタブの state に持つ）。
    ★★**落とし直すのは「まだ在るのに山から押し出された」ときだけ**
    （2026-08-18・第15巡）。以前は**件数が違うだけ**で落とし直していたので、
    1 件増えるだけで全部が上から降ってきた。純粋な追加なら既存は `rescale` で
    その場のまま倍率だけ合わせ、**新しい 1 つだけが落ちる**。完了・削除は
    `alive` の道で世界から外れる。中身が変わっただけのときも**いまの位置のまま
    body を作り直す**（位置・角度・速度を引き継ぐので山は崩れない）。
    ★入力画面が開いているあいだは rAF を止める。
- **出入りは必ず対にする**（2026-08-18）。出るだけで消えるのが一瞬だと安っぽい。
  入力画面 `tc-shell-in` 180ms / `tc-shell-out` 200ms ／
  ポップオーバー `tc-pop-out` 160ms ／
  日程シート `tc-sheet-out` 180ms ／ 日を選ぶと `tc-pick` で 1 度弾む。
  **イージングはアプリ共通のものだけ**（`cubic-bezier(0.16,1,0.3,1)` と、
  追従・シェルの `cubic-bezier(0.32,0.72,0,1)`）。増やさないこと。
- ★★**日程だけはキーボードを閉じる**（`components/tasks/WhenSheet.tsx`。
  2026-08-17 にユーザーが方針転換）。カレンダーに高さが要るため、他の項目
  （メモ・持ち物・重要度・タグ）とは扱いが違う。

  - アイコンを押した瞬間に `blur()` して、**画面いっぱいのシート**を下から出す。
  - **左上に ✕（取り消し）・右上に ✓（確定）**。開いた時点の値を控え、✕ は
    そこへ戻す。
  - ★どちらを押しても、**その `pointerdown` の中で同期的に `textarea.focus()`**
    してからシートを閉じる。iOS はユーザー操作の流れの中でしかキーボードを
    開かないので、アニメーションの後に focus してももう開かない。
  - 閉じるのは **180ms で下へ抜かす**（`.tc-sheet-out`）。「シュッと閉じる」。

  ★**シートの高さは固定**（`WhenSheet` の `SHEET_BODY_H` ＝「期日」の並びの
  合計から導く）。中身なりにすると、中身の少ない「期間」で**上端が下がって
  押す位置が変わり**、さらにホバーがシートの外へはみ出していた
  （2026-08-17 に指摘）。**上端を動かさないことを優先**し、中身は上から並べる。
  「期間」では下が空くが、それでよい。
  中身の根は `height: 100%` にすること — 中身なりの高さだと、ホバーの
  置き場所を決める「使える高さ」まで中身の高さになり、下に余裕があるのに
  上へ返って見出しを覆う。

  見た目は**参照画像（TickTick）から起こす**。幅いっぱいに伸ばさないこと:

  ```
  (✕)      [ 期日 | 期間 ]      (✓)     ← ピルは幅 200px を中央に。✓ は塗った丸 40
  今日   明日   今週末                   ← 早押し(高さ 62)。時刻を決めるものは入れない
  ‹  8月  ›                              ← 月は 17px 太字・中央、山形は小さく
  日 月 火 水 木 金 土  ＋ 6週の方眼      ← 1週 40px・丸 34・数字 15px
  🕐 時刻                     終日  ✕    ← カード(高さ 52)
                クリア
  ```

  - 期間・**終日ON** … `開始 › 終了` を**1枚のカードの中で 2 つに割る**（`Half` ＋
    `Wedge`）。期間・**終日OFF** … `期日` と `時刻` は**別々の 2 枚**（`Card`）。
    どちらも下に「終日」のスイッチ。
  - カードは ラベル 11.5px（沈んだ色）/ 値 17px 太字（**アクセント色**）/
    注記 10.5px の 3 段。
  - ★**アクセント色 = そのタスクのタグの色**（2026-08-17 にユーザー確定）。
    ただし LIFE の深緑のように墨地で沈む組があるので、**`tagColor` と `tagInk` の
    うち地とのコントラストが 3:1 以上の方**を選ぶ（`tagAccent`。新しい色は
    作らない — 必ず `SCHEME` の対の中から）。
  - ★**モードは値から決まる**（`endDate` か `endTime` があれば期間）。状態を
    二重に持たない。大きさに効くのは今までどおり `dueDate` だけ。
  - ★カレンダーの月送りは**面ごと滑らせる**（前月・当月・翌月を横に並べて
    `translateX`）。数字だけ差し替えると変わったことに気づけない。
    隣の月は**指が触れるまで作らない**（3 か月ぶんを最初から並べると開く瞬間に
    200ms 級のひっかかり）。
    - ★**指の軸は最初の 8px で決める**。`pointerdown` した時点で横取りしていた
      ため、縦や斜めの指でも月が動いた。縦と決まった指はその場で捨てる
      （`touchAction: "none"`）。
    - ★**送ったあとに逆へ動かさない**。中央へ戻す**1 フレームだけ transition を
      切る**（`snapBack`。`.app-track` の再センタリングと同じ手）。
    - ★**曜日の行は送らない**（月ごとに描くと、送っている途中の並びがずれる）。
    - ★**常に 6 週ぶん描く**（5 週の月と混ざると盤の高さが跳ねて下の行まで動く）。
      1 週の高さは残りに合わせる（`ResizeObserver`。24〜40px）。
  - ★★**期日は一点・期間は範囲**（2026-08-18・第12巡にユーザー確定）。
    入れるものが違うので UI も分ける。**刻みはどちらも 15 分**
    （「5 分は操作しづらい」）。
    - **期日＝締め切りの時刻**（8:00 など）。**iPhone のアラームと同じダイアル**
      （時／分の 2 本の輪）。`endTime` は持たない。
      - **一周する**。値を 5 周ぶん並べ、止まったら外側の周から中央の周へ
        差し戻す（`scrollTop` を 1 周ぶん動かすだけなので見た目は動かない）。
      - **待たない**。スクロール中に中央の値が変わった瞬間に確定する。
      - ★★**スクロール中に React を 1 度も走らせない**。以前は中央の値が変わる
        たび **120＋60 個の inline style を書き換えて**いた（「反応が遅い」の正体）。
        上下の薄れは CSS の `mask`（`.tc-dial`）、選択は中央のピルに任せ、
        マスは `useMemo` ＋ `Column` を `memo`（`value`/`onPick` は比較から外す。
        渡す関数は `Dial` 側で ref に固める ＝ でないと古い時刻を掴む）。
    - **期間＝始まりと終わり**（`components/tasks/TimeRange.tsx`）。
      タイムラインの上で帯をつまむ。長さがそのまま帯の長さになる。
      - **1 時間 = 56px**、見えるのは 264px。帯をつまむと動く、上下の端で伸び縮み。
        最短 15 分。★**既定は 1 時間**（時刻が無い状態で開いたらその場で置く）。
      - ★**帯の左右に `LANE`（34px）空ける**（2026-08-18 指定）。ここを触れば
        帯を掴まずに時間帯を送れる。
      - ★指が動いているあいだ **React を回さない**。目盛りは `useMemo` で
        作り置き、帯だけ ref 越しに style を書く。親へ返すのは刻みをまたいだ瞬間だけ。
      - ★**`Press` は使わない**（恒久ルール 9）。送れる面の中で `touchstart` を
        止めると、送ること自体ができなくなる。帯だけ `touch-action: none`。
  - ★★**期日か期間かは「日付」だけで決まる**（2026-08-18）。`endDate` の有無で
    見る。`endTime` を見ていると、**時刻を入れただけで期間へ飛ぶ**（実際そうなった）。
  - ★**ホバーは押した相手の隣に出す**（`Float` の `anchor`）。自分の高さを
    測り、下に入るなら相手の下、入らないなら**上へ返す**（すき間 6px）。
    見出しのすぐ下に固定していた頃は、下端の「時刻」から遠かった。
  - ★**中身が小さいホバーは幅も絞る**（`Float` の `fit`。2026-08-18 指定
    「触る部分に対して幅が大きすぎる」）。`width: max-content` にして
    **相手の中心へ寄せ**、シートの縁 4px の手前で止める
    （ダイアル 182px / タイムライン 280px）。
    **カレンダーは今までどおり幅いっぱい**（方眼が痩せると押せなくなる）。
  - ★★**役割ごとに手段を分ける**（2026-08-18 にユーザー確定。それまでは
    今日も選択日も「塗りの円」で、どちらがどちらか読めなかった）。
    - **塗りの円＝選んだ日**（と期間の両端）。塗りはこの意味だけに使う。
    - **今日＝数字がアクセント色＋丸の下に小さな点**。塗らない。
    - **期間の両端＝丸の下に「開始」「終了」の極小の文字**（8px）。
      1 日だけの期間には書かない（ただの選択と同じなので）。
    - あいだの日はアクセント 22% の帯。**両端のマスは半分だけ敷く**ので、
      丸から帯が伸びて見える（帯が途中で切れていると範囲が読み取りにくい）。
    - 札の場所は `paddingBottom: LABEL_H` で**必ず空ける**（有無で高さが跳ねない）。
      札そのものは**要る マスにだけ**作る — 42 マス全部に空の札を置くと
      日程シートの開きが 430→650ms になる（この画面の重さは**要素数**が支配的）。
  - ★**期間は開始・終了の両方が見える**（2026-08-18 指摘）。`MonthGrid` に
    `range` を渡すので、開始側のカレンダーでも終了側でも同じ範囲が見える。
  - ★**日を選んでも浮きは閉じない**（2026-08-18 にユーザー指定「押した瞬間に
    閉じてしまって何日を選んだか分からない」）。閉じるのは ✕・外側タップ・
    カードをもう一度押す、の 3 つ。開始⇄終了の切り替えは一度閉じてから。
  - ★**「クリア」は塗った赤のボタン**（2026-08-18・第19巡にユーザー指定
    「クリアはボタンにして塗りにしてください」）。文字だけの行だったころは
    押せると分からなかった。幅いっぱいのピル（高さ 36）を `RUST`（#EE1B33）で
    塗り、文字はその相方の淡い赤（`SCHEME.red.ink`）。塗りと相方の一対は
    タグの丸と同じ語彙。
  - ★**「クリア」は消すものを 1 つずつ名指しする**（2026-08-18。空の `{}` を
    渡していて**何も消えなかった**）。呼び側は受け取ったものを下書きへ
    **混ぜる**ので、`{}` では古い値がそのまま残る。
  - ★**週の始まりは月曜**（2026-08-17 にユーザー確定）。見出しは `WD_HEAD`、
    先頭の空きマスは `(getDay() + 6) % 7`。`label()` の曜日名は
    `WD[d.getDay()]`（日曜始まりの添字）のままにすること。

- 動きは `app/globals.css` の `tc-*`（器が下から上がる / ポップオーバーが
  立ち上がる / 形が変わった瞬間だけ図形が弾む / 灯りの色が滑らかに変わる）。
  イージングはアプリ共通の `cubic-bezier(0.16, 1, 0.3, 1)`。

### 一括スケールと間引き（`pileOf`）
「小さすぎるものは出さない」と「混雑しても大きい図形を残す」を同じ仕組みで解く。
面積の降順に並べ、先頭 N 件だけで縮尺を出しながら N を増やし、次のどちらかで止める。

- **これ以上入れると `SCALE_MIN 0.70` より縮めないと収まらない**
  → 縮めるのではなく**間引く**
- **その 1 件自身が `CULL_PX 30` を割る** → どうせ読めないので入れない

★最後に、**いちばん大きい 1 枚が画面に収まる**ところまで縮める
（幅 `FIT_W 0.86` / 高さ `FIT_H 0.62`）。面積の合計だけで決めていた頃は、
数件しか無いと縮尺が上限に張り付き、重要度の高い 1 枚が画面より大きくなって
壁に挟まったまま宙に浮いていた。渡す高さからは**見出しのぶん**
（`MASTHEAD_H 108`）も引く — 引かないと山が見出しに重なる。

含める数を増やすほど縮尺は単調に下がるので、この走査は一意に決まる。
実測: 4 件→4 件表示・最大 278px / 16 件→16 件・146px / 40 件→10 件・137px /
80 件→9 件・137px。★**間引かれたタスクは画面から触れなくなる**（ユーザー了承済み）。

LOD は代表寸法 `s = √面積 × unit` で決める。`>=48` 3 行 / `30〜48` 1 行 /
`<30` 文字なし / `<16` 切れ目なし。

### ファイルの役割
- `lib/solid.ts` … 幾何。純粋関数のみ。`section` / `sectionOutline` /
  `innerBox`（文字を置いてよい矩形）/ `frontRect` / `slabRects` / `boundsOf`。
- `lib/textFit.ts` … 1 文字ごとの書体割り当て、箱に最大で収める、白黒判定、
  **グリフのアトラス**。
- `lib/solidPaint.ts` … `paintShape`（立面 2 枚）＋図形 1 枚のビットマップ
  キャッシュ（LRU、フレームごとの焼き予算つき）。
- `components/tasks/` … `SolidCanvas`（図形 1 つ）/ `TaskComposer`（入力画面）/
  `ComposerToolbar`・`ComposerFields`・`WhenSheet`（日程）・
  `TimeRange`（時刻のタイムライン）・`Popover`（器と `Press`）/
  `ViewToggle` / `TaskAddButton`。
- `components/tabs/GravityTab.tsx`（落として積む）/ `DriftTab.tsx`（円環の
  カバーフロー）。
  ★★**「左右が見切れる」の正体は、輪の面が画面より 16px 内側だったこと**
  （2026-08-18・第12巡でようやく特定）。列の左右パディング（`padding: … 16px`）が
  効いていたので、隣の図形が**画面の端ではなく 16px 手前で直線に切れて**いた。
  直しを 2 回外している — 図形と半径を縮めて収める（第10巡）、`mask` で
  端を薄れさせる（第11巡）。どちらも**切れ目の位置**を見ていなかった。
  いまは `.bleed-x`（左右だけ画面いっぱい）＋ `overflow: clip`。切れ目が
  画面の端と重なるので、見た目は「そのまま外へ出ていく」になる。
  ★`hidden` ではなく **`clip`**（`hidden` は送れる箱を作り、はみ出したぶんを
  iOS が横に送れてしまう）。これで**大きさも半径も画面幅に縛られない**。
  いまは `SOLID_W 300 / SOLID_H 190 / SPREAD 300`。
  ★検証は「輪の面が 0〜画面幅であること」（`getBoundingClientRect`）。
  ★**輪は一周する**（同指定）。並びの差を `wrap(i - pos, n)` で輪の長さに
  折り返してから角度にする。端の抵抗も上限も持たない。手前の番号は
  `((round(pos) % n) + n) % n`。

### ★★書体が揃ったときの捨て方（2026-08-18・第17巡）

**`onFontsReady` は「購読しただけ」で呼び返してはいけない。**
以前は `document.fonts.ready.then(cb)` を置いていた。これは**もう揃っていれば
即座に解決する**ので、`SolidCanvas` が 1 枚マウントされるたびにコールバックが
走っていた。そのコールバックは `clearSolidBitmaps()` ＝ **みんなで使っている
焼き上がりの全消し**なので、

- 入力画面を開くだけで**山の絵が全部消え**、閉じたあとに焼き直されて
  **文字が点滅**する（実機で報告。Chromium でも閉じた 350ms 後に
  色数 241→51 と再現できた）。
- タブを行き来するたびに焼き直しが走る ＝「全体的に処理が間に合っていない」。

揃っていない面は `requestGlyphs` の `load().then` が拾うので、これは要らない。
`loadingdone` の安全網も、**面の顔ぶれ（`document.fonts.check` の並び）が実際に
変わったときだけ**動かす。

### ★守ること
- **`ctx.font` は CSS 変数を解決できない。** `var(--font-…)` を含む文字列を
  代入すると黙って失敗し、既定の 10px サンセリフのままになる。
  `resolveFamily()` で `getComputedStyle(document.documentElement)` から
  一度だけ解決する。
- **文字はグリフのアトラス経由で描く。** 和文の Web フォントは unicode-range で
  数百の `@font-face` に分割されており、`fillText`/`measureText` は
  **(書体, 文字) の組ごとに初回 5〜10ms** かかる。素直に描くと落下中に
  `fillText` だけで 2.4 秒。同じ (書体, 文字, 色) は一度だけ焼いて
  `drawImage` で使い回し、**1 フレームに数枚だけ**焼く（`GLYPH_BUDGET = 4`）。
  ★1 枚では足りない — 16 個 × 8 文字 ≒ 130 枚を 1 枚/フレームで焼くと 2 秒以上
  かかり、そのあいだに断片が届いて振り出しに戻るため文字が一度も出なかった。
  ★**焼く大きさをバケツ `[32, 64, 96]` に丸める**。常に 128px で焼いて
  13px へ縮めていた頃は 9.5 倍の縮小になり、`drawImage` が標本を落として
  文字がジャギーになっていた。ただし**段を増やしすぎない** — 同じ文字が段の
  数だけ焼き直されるので、7 段にしたら落下中の `fillText` が 868ms へ増えた。
  図形の絵は `bakeDpr = min(dpr, 1.5)` で焼く（1.25 では画面へ 1.6 倍に
  拡大されて滲み、2.0 では画素が 2.56 倍になって 800ms 級のひっかかりが出た）。
- **和文の送り幅は測らない。** 全角（かな・漢字・全角記号）の `advance` は
  必ず 1em なので、`measureText` を飛ばす。題の文字数ぶんが 1 フレームに
  まとまって走ると 666ms 止まった。
- **欧文（タグの英字）の送り幅は暇な時間に先に測っておく**
  （`primeAdvances` を `requestIdleCallback` で数組ずつ）。BOTTOM へ
  切り替えた瞬間に 5 書体ぶんがまとめて走ると 328ms 止まる。
- 実測（4× CPU 絞り・本番ビルド、16 個）… 落下 合計 1300ms 前後 / 最大 190ms、
  ビュー切替 最大 130ms、積み終わったあとと横スワイプは 0ms。
- **和文の Google フォントは 1 書体につき 1 ウェイトまで。** 3 書体 × 3
  ウェイトで turbopack が `Module not found` を出す。太字・斜体はブラウザの
  合成に任せる（`app/layout.tsx`）。
- **`document.fonts.load()` の Promise は必ず `.catch()` する**
  （unhandled rejection で pageerror が飛ぶ）。書体が届いたら
  `onFontsReady` でアトラスとビットマップを捨てて描き直す。
- ★★**「書体が届くまで文字を描かない」は必ず時間で門を開けること**
  （2026-08-17。実機で「文字が表示されない・アプリ全体が不安定」）。
  `textDrawable(face, text)` は「届いた **または** 頼んでから 600ms 過ぎた」で真。
  待つだけにしていたら、次の 3 つのどれか 1 つで**文字が永久に出なかった**:
  - **WebKit の `document.fonts.check()` が偽を返し続ける**
    （unicode-range で分割された和文の family に対し Blink と挙動が違う）。
    → `computeTextPlan` が `null` を返し、その `null` が `planCache` に載る。
  - **判定結果のキャッシュに「取りに行く前の偽」が焼き付く** →
    届いたあとの再判定も偽を読むので、**焼き直しの通知が一度も飛ばない**。
    取りに行く前後の比較は**キャッシュを通さない生の `check()`** で行う。
  - **`loadingdone` のたびにアトラスを全消し**していた → 断片は 100 以上
    あるので、1 フレーム数枚しか焼けないアトラスが焼き切る前に振り出しへ
    戻る。`pendingBake` が立ち続けて **rAF も止まらない**。
  → 捨てるのは **400ms にまとめて、届いた面のぶんだけ**（`dropFace`）。
  安全網の `loadingdone` は **`document.fonts.status === "loaded"`（＝読み込みが
  落ち着いた）ときだけ**動かす。
  ★**「何も出ない」より「一度 fallback で出てから本物に差し替わる」**方がまし。
  検証は `document.fonts.check` を偽で固定して実機を再現し、山の画素の色数を
  数える（修正前 125 色・文字の画素 19 → 修正後 336 色・6779）。
- **当たり判定は描く形そのもの**。断面の多角形
  （`Bodies.fromVertices`）。長方形で当てていた頃は、形の周りの見えない余白
  どうしがぶつかって図形が宙に浮いて見えた。**丸だけは `Bodies.circle`**
  （36角形は衝突判定が重く、転がりもカクつく）。物理の頂点は 12 点まで
  間引く — 半円の 37 点を 16 個ぶんまとめて `fromVertices` に通すと、
  凸包の計算で 400ms 級のひっかかりが出た。
  ★`fromVertices` は**重心**を `body.position` に置くが、絵は図形の原点を
  中心に焼いてある。三角形のように両者がずれる形では、そのズレ（`ox`/`oy`）を
  描画側で戻すこと。戻さないと絵と当たり判定が食い違う。
- 手順を済ませて形が変わったときは、位置と速度を引き継いで body を差し替える
  （ビュー切替は全部作り直すので、こちらは編集のときだけ）。
- **落下位置の hash に連番をそのまま使わない。** `"t1","t2",…` は hash も
  連番になり、全部が同じ x に落ちて 1 本の塔になる。黄金比の定数を掛けて混ぜる。
- ★★**`document.fonts` には「先頭の family だけ」を渡す。**
  `next/font` は `--font-dela-gothic-one` を
  `"Dela Gothic One", "Dela Gothic One Fallback"` に展開する。後ろの
  *Fallback* は**端末のフォントに寸法だけ合わせた `local()`** なので
  **常に読み込み済み**。スタックごと `load()` に渡すと、それで即 resolve して
  **本物の断片を取りに行かない**。実測（2026-08-17）:
  スタックで load → 塗り率 **17%**（素のサンセリフ）のまま /
  先頭だけで load → **37.9%**（本物の Dela）。
  ★これを間違えていたため、**和文のディスプレイ書体は一度も表示されて
  いなかった**（それまでの「書体の違い」は太さと斜体の合成だけ。だから
  「赤と黄が同じに見える」ことになった）。`primaryFamily()` / `checkFace()` /
  `textDrawable()` / `ensureGlyphs()` が唯一の窓口。`ctx.font` の方はスタックのままでよい
  （届いていない間は fallback で描くため）。
- **書体は待たずに「取りに行く」。** 断片の取得が始まるのは**その文字を
  初めて描いた瞬間**なので、`document.fonts.ready` も `loadingdone` も
  「鳴る前に焼いてしまった絵」は救えない。焼く直前に `ensureGlyphs()` を
  呼び、**まだ使えないときだけ**、届いた時点でアトラスと割り付けを捨てて
  購読者へ知らせる。
- ★**書体が届くまで文字を描かない**（`computeTextPlan` が `textDrawable` で
  門番をする）。グリフは 1 フレームに数枚しか焼かない（落下のガクつき対策）
  ので、焼いている途中で書体が届くと**前半 fallback・後半 本物**の絵になる
  （実機で「1 つの図形に 2 書体」として報告された）。
  **fallback で焼かなければ混ざった絵は存在し得ない。**
  ★★ただし**門は必ず時間で開ける**（600ms）。理由と再現手順は上の
  「性能の作法」の節に書いた — 待つだけにしたら実機で文字が永久に出なかった。
- **入力画面のプレビューは倍率を固定する**（`SolidCanvas` の `unit`）。
  器いっぱいに拡大すると、重要度や期限を変えても絵の大きさが変わらず
  「大きさ ＝ 重要度」が読めなくなる。

---

## 5. 声の記録（Journal）

`components/VoiceStudio.tsx` 1 つで完結。ジャーナルの RECORD タブと、
タブバー右端の録音ボタンから開くオーバーレイが**同じ部品**を使う。

- 画面下部に**画面をはみ出す巨大な円が 2 つ**（カセットのリールの抽象）。
  直径＝器の幅の 1.30、中心 x = ∓0.30、中心 y は
  「タイトルの下端とタブバーの上端の中間」。
- 操作は**トグル**。REC キーか**円の上**をタップで開始／停止。それ以外の
  余白は反応しない。止めても送らず `review` で待つ。
- 停止後、同じ 2 つの円が**トリミングのダイヤル**になる。左＝始点、右＝終点。
  1 回転で全体の 50%。惰性つき（指を止めてから離したら速さは 0）。
- 物理キー 4 つ（丸・直径 42・出っ張り 4）… `REC` / `PAUSE` / `SEND` / `CANCEL`。
  `MediaRecorder.pause()/resume()` で続きから録れる。止まっている間は数えない。
- 実際に切る … `lib/audioTrim.ts`。`decodeAudioData` で波形に戻し、必要な範囲
  だけを 16bit PCM モノラル WAV へ組み直す。デコードできない端末では
  **元の音声をそのまま送る**。
- 文字起こし … `app/api/transcribe/route.ts`。`OPENAI_API_KEY` があれば
  Whisper 系、無ければ `GEMINI_API_KEY` で Gemini の音声入力。結果は
  `AppState.voiceNotes` と my-brain の `days/YYYY-MM/voice.md` へ。

### ★守ること
- **マイクは必ず解放する。** 生ストリームを自分で持ち（`streamRef`）、
  二重起動を防ぎ（`startingRef`）、`onstop` が来なかったときの保険
  （1500ms）も置く。どの終わり方でも `releaseStream()` を通す。
- **波形の棒は「本数 × 等間隔」で置かない。** 測定の `setInterval` は
  45ms ちょうどでは来ないので、増えるたびに 1 本ぶん飛ぶ。測った瞬間の
  経過時刻を記録し、**時刻から位置を出す**。
- **`navigator.vibrate` は iOS Safari に無い。** 手応えはハプティクスでは
  返せないので、掴んだ合図は `transform: scale()` の視覚で出す
  （**塗りの色は変えない**。巨大な面が十数フレーム塗り直される）。
- **入場アニメーションの再生の合図は AppShell から prop で渡す。**
  IntersectionObserver で見え方から推測すると実機で閾値に達せず、
  円が出てこない。

---

## 6. ブリーフ生成（運用ポリシー）

パイプラインの詳細は `SYSTEM-DESIGN.md`、実装は `lib/briefPipeline.ts`。
**ここに書くのはユーザーが明言した「これを正とする」運用だけ。**

### 情報源とカードの関係（§8.15）
- **情報源の本体は、雑多で高品質なネットマガジン**（カルチャー・技術・生活・
  音楽・ファッション）。淘汰・入れ替えでゆっくり好みに追従する。
  ここから、興味に合致する＝**提案カード**、合致しない新着＝**情報カード**。
  **カルチャー誌の「記事」を第一級で扱う**のが中核の体験。
- 展覧会・映画・イベントの集約サイトは、**情報が集約されているから固定に
  しているだけ**で、他の情報源と完全に平等。**抽出・分類・枚数配分のどこにも
  「展覧会を優先する」構造を作らない。**

### 更新と枚数（§8.18）
- **1 日 2 回**（朝 06:30 JST / 夕 13:30 JST の Cron）、各 **10 枚くらい**。
  事前に生成してストックし、アプリを開いたら即出る。
- 未消化カードの保持は **3 日**。ストック上限 **40 枚**（超えている間は追加生成
  しない）。キーは**日付だけ**（`YYYY-MM-DD`。朝刊／夕刊の概念は撤去済みで、
  Cron は置き換えではなく **id 重複を除いた積み増し**）。
- **「今すぐ生成」はテスト用**。完成時に削除する。

### 構造（§8.19）
2 層＋コード検証。**AI に「落とす」判断をさせない**ので、候補が 1 件でもあれば
構造的に 0 枚にならない。
- 層 1（サイトごと・並列）… 抽出と関連度付け（0〜100）を 1 回で。
- 選抜（コードのみ）… 出典 URL の実在検証・終了済み／圏外／重複の除外の後、
  関連度順にサイト横断ラウンドロビン。`>=80` ストレート／`50〜79` 派生／
  `<50` 情報カード。**trigger もコードが関連度から決める。**
- 層 2 … 採用分だけ個別ページを取得して本文を詳細化。

### ★絶対の原則
- **LLM に URL を抽出させない。** 抽出のつもりが推測創作になる（§8.12.5 で
  実在しない展覧会を丸ごと捏造した）。URL は必ず**コードが実 HTML／Markdown
  から機械的に取り出した集合**で照合し、外れたカードは機械的に捨てる。
- **retrieval は Jina Reader 経由**（`r.jina.ai`）。生 fetch は IP ブロック・
  JS 描画・正規表現依存で繰り返し失敗した（§8.12.13）。
- **機械的な処理はコードで、LLM に投機させない**（日付の期限切れ判定、距離の
  計算、枚数の配分、重複の統合）。

### 情報源の淘汰（§8.20）
個別のスキップは記録しないので、打率は**集計値として別に持つ**。
`lib/sourceStats.ts` が `generatedDecks` と `briefs` からドメインごとの
`shown / kept / flagged` を積算し（`countedIds` で二重計上を防ぐ）、
my-brain の `sources/stats.md` へ書く。Cowork の発掘タスクはこの数字だけを
見て並べ替え・淘汰する。

---

## 7. 基盤

- **Supabase** … `supabase/schema.sql`。`app_state`（キーごとの行＝書き込みを
  分離）/ `context_notes` / `api_usage` / `content_cache`。RLS は全テーブル
  `auth.uid()` 一致。★`value` 列に `not null` を付けないこと（`magazine` など
  正当に null になる値があり、付けると upsert 全体が 400 で落ちる）。
- **認証** … **6 桁コードの入力**（`components/SignInGate.tsx`）。マジックリンクは
  使わない。iOS ではホーム画面 PWA と Safari が別の保存領域を持つため、リンクを
  踏むと永遠にログインできない。Supabase のメールテンプレートに `{{ .Token }}`
  が必要で、独自 SMTP（Resend）は**ポート 587**（465 は失敗する）。
- **Vercel** … ★**ハッシュ入りのデプロイ固定 URL を実機で常用しない**
  （そのビルドに永久に凍結される）。安定 URL は
  `daily-brief-lyart-six.vercel.app`。Supabase Auth の Site URL /
  Redirect URLs にもこれを登録してある。
  ★★**本番ブランチは `main`。毎回ここへ push する**（2026-08-18 にユーザー確定・
  確認不要）。作業ブランチへ push しただけでは**実機に何も届かない** —
  第 9〜11 巡はこれで 3 巡ぶん届いておらず、「変更が確認できません」と報告された。
  作業ブランチへ push したら続けて
  `git checkout main && git merge --ff-only <branch> && git push -u origin main`。
- **地図** … Leaflet + CartoDB Positron タイル（`components/LeafletMap.tsx`）。
  座標は Places API（New）で解決（`app/api/resolve-place/route.ts`）。
  Google マップ URL に埋まった `@lat,lng` を正規表現で先に抜き、API 呼び出しを省く。
- **my-brain**（別リポジトリ。人間が編集する自己モデルの真実源）
  … パスを知っている場所は `lib/myBrainPaths.ts` **だけ**。以後どのコードも
  ここ経由でしかパスを書かない。
  ```
  me/taste.md      興味・好み・生活圏・願い・ゴールに効くキーワード
  me/patterns.md   手配漏れ・物忘れの傾向（毎晩の仕分けが育てる）
  me/goals.md      ゴールとチェックイン
  days/YYYY-MM/    facts.md / voice.md / summary.md / feedback.md
  inbox/candidates.md
  sources/         list.md / stats.md / proposed.md / dismissed.md
  analysis/taste.md
  ```
  front-matter の `owner: app | cowork | human` で所有者を分ける。1 ファイルを
  共同編集するのは `me/taste.md` と `sources/list.md` だけで、
  `<!-- BEGIN/END app-managed:… -->` のゾーン方式で衝突を避ける。

---

## 8. 検証ワークフロー

```bash
# 開発サーバー（★性能の計測は必ず本番ビルドで。dev は桁違いに遅く数字が無意味）
npm run build && npx next start > /tmp/.../server.log 2>&1 &

# Playwright はグローバル install 済みのものを使う
NODE_PATH=/opt/node22/lib/node_modules node script.mjs
#   import { chromium } from "/opt/node22/lib/node_modules/playwright/index.mjs";
#   chromium.launch({ executablePath: "/opt/pw-browsers/chromium" })
```

締め … `npx tsc --noEmit` / `npx eslint .`（`common.tsx` などの `<img>` 警告
3 件は既知・容認）/ `npm run build`。

### ★検証スクリプトの落とし穴
- **3 つの列と nav は常に DOM にある。** グローバルな `getByLabel` は別の
  アプリにヒットする。`getBoundingClientRect().x ≈ 0` で見えている列を特定し、
  その中に限定して探す。タブの `aria-label` は**日本語**。
- **`migrate()` は `wishes` の無い state を全部捨てる**（§2）。
- **見た目のスクリーンショットは「効果が出ている」証明にならない。** 影・
  グラデーションなど微弱な効果は Pillow などで**ピクセル値を直接サンプリング**
  して数値で確かめる。
- **WebGL / canvas のある画面は `page.screenshot()` 1 枚に 0.5〜1 秒かかり、
  その間 rAF が止まる。** 位相ごとの絵が要るときはアニメーションの定数を
  一時的に伸ばして撮る。CSS アニメーションなら `getAnimations()` で止めて
  `currentTime` を指定するのが確実。

### ★★単体テストの前に「写し」を焼き直す（第19巡で発覚）

`scratchpad/*.test.mjs` は `lib/*.ts` を **esbuild で書き出した写し**
（`taskSize.mjs` など）を読む。写しは**焼いた時点で固まる**ので、
`lib` を直しても勝手には追いつかない。

第19巡で焼き直したところ、**3 つのテストが消えた API を試していた**ことが分かった
（`lenOf` / `radiusOf` の 3D 寄りの寸法、旧タグ id の `body`/`people`/`learn`/`shopping`、
自由文の `when`）。つまり**通っていたのは化石**で、出しているコードは
一度も試されていなかった。`lib/prism.ts` に至ってはファイルごと無く、
196 件が丸ごと死んだモジュールを試していた（`prism.test.mjs` は撤去した）。

**必ず走らせる前に焼き直すこと**:

```bash
S=<scratchpad>
for m in solid taskSize taskTags inboxImport; do
  npx esbuild lib/$m.ts --bundle --format=esm --outfile=$S/$m.mjs
done
```

いまの単体テストは **90 件**（solid 73 / tag 9 / inbox 8）。

### ★★入力画面は「4 通りの型」で見る（第18巡・`scratchpad/geo4.mjs`）

**これまでの検証には「ずれた場合」が無かった。だから 3 巡続けて同じ所を壊した。**
キーボードの出方は端末で 3 通りあり、さらに**見えている矩形がずれる**型がある。

| 型 | 作り方 |
|---|---|
| A キーボード無し | そのまま |
| B レイアウトごと縮む | `setViewportSize` で縮める |
| C `visualViewport` だけ縮む | `vv.height` だけ差し替える |
| **D ずれる** | `vv.height` を縮め、さらに `vv.offsetTop` を 156 にする |

**どの型でも次の 3 つが成り立つこと**（入力画面の不変条件）:

1. 上のバーの上端 == 見えている上端（`vv.offsetTop`）
2. 帯の下端 == 見えている下端（`vv.offsetTop + vv.height`）
3. 図形の中心が見えている範囲の中にある

あわせて **D の型で入力画面が閉じないこと**。

### ★★入力画面の回帰は `pop21.mjs` で見る（第21巡）

ポップオーバーが開いている最中に**ずれを付け替えて**、①ずれを追っているか
②上のバーの上端が見えている上端と一致するか ③`--kb` は凍ったままか を
5 種すべてで見る。あわせて ④小さい揺れでは動かないか ⑤**閉じるあいだ帯が
板の中で動かないか**（＝1 枚の板で滑っているか）⑥図形の登場と入れ替え
（**色数が一度も落ちないこと** ＝ 点滅の回帰）。

### ★★「開いてすぐ閉じる」は詰まらせて見る（`scratchpad/plus.mjs`）

主スレッドが空いていると再現しない。**`Emulation.setCPUThrottlingRate` を 6 に**
してから、gravity の山が動いている最中に＋を押し、開いた直後に `blur()` を
起こして 1.5 秒後に器が残っていることを見る。時計に頼った守りはここで必ず落ちる。

### ★性能の測り方
`PerformanceObserver` で `longtask` を拾い、CDP の
`Emulation.setCPUThrottlingRate { rate: 4 }` を掛けて**本番ビルド**で測る。
- 比べてよいのは「**同じスクリプトで、変更あり／なしを続けて測った値**」だけ。
  スクリプトが変われば数字の意味も変わるので、過去の記録と比較しない。
- 重いときは Paint を疑う前に、まず **Layout かどうか**を CDP の `Tracing`
  （`devtools.timeline`）で確定させる。`textContent` への代入は**値が同じでも**
  レイアウトを汚し、`font-size` の遷移は遷移中ずっとレイアウトを走らせる。
- 「1 つずつ外して数字が動かない」ときは、**原因が 1 つではない**可能性を先に疑う。

### ★開発用「画面の数値を出す」（★直ったら撤去する）

`lib/debugViewport.ts` / `components/tasks/ViewportProbe.tsx` / 設定の
「画面の数値を出す」。ON のあいだだけ入力画面の左上に実測値を出す
（`--kb` / `lvh` / `vv.height @ offsetTop` / `scrollY` / `--vvtop` /
上のバーの実際の上端 / セーフエリア上下 / 日程シートの下端）。

**この環境では iPhone を動かせない**ので、レイアウトの崩れを推測で直しては
外し続けていた（同じ場所で 3 巡）。崩れた瞬間の写真 1 枚で決められるように
置いたもの。**`bar` が 0 でなければ器がずれている** — そこが見分けの要。

### 動作確認用のダミー（★完成時に撤去する）
- `lib/constants.ts` の `CARDS`（ダミーのブリーフデッキ）
- `components/tabs/ExecuteTab.tsx` の `injectDemo`（「デモ用データを投入」）
- `lib/taskDemo.ts`（タスク 16・候補 12）と「デモのタスク／候補を入れる」ボタン。
  設定 →「その他」→「データの整理」の**「デモのタスク・候補を削除」**で、
  id が `demo-` で始まるものだけを消せる（手で作ったタスクと、声のメモから
  来た候補は残る）
- `ProfileTab` の「ブリーフ生成の実験（開発用）」カード・「今すぐ生成」

---

## 9. ドキュメントの構成と運用

| ファイル | 役割 | 上限 |
|---|---|---|
| `CLAUDE.md` | 普遍のルール・ファイル地図・恒久ルール。**毎セッション自動で読まれる** | 100 行 |
| `handoff_current.md` | いまどこにいるか・直近完了・次の一手・重要パス | 200 行 |
| `docs/project_knowledge.md` | 現行の設計・確定仕様・データモデル（このファイル） | — |
| `docs/archive/*.md` | 経緯と教訓。**自動では読まない**。必要なときだけ `grep` する | — |
| `SYSTEM-DESIGN.md` | 生成パイプラインの設計思想 | — |
| `COWORK-ROUTINES.md` | Cowork に渡すプロンプト（アプリは読まない） | — |

**変更を加えたら、コミットの前に必ず**：価値の無い記述を消し、教訓は
`docs/archive/` へ退避し、確定した仕様はこのファイルの該当章を**上書き**し、
`handoff_current.md` を書き直す。手順は `CLAUDE.md` の「恒久ルール」が正。
