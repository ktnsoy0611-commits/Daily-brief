# いまどこにいるか（第69巡・2026-08-29／Explore の刷新の途中）

3アプリ（JOURNAL / TASK / EXPLORE）は動いている。第68巡までで TASK は一段落。
**第69巡から Explore の刷新を始めた。設計の正は `docs/explore-redesign.md`。**

★★新しい**券と改札鋏は `/dev/explore` にしか無い**（本番の導線からは辿れない）。
**Explore の4タブはまだ手つかず**で、現状のまま動いている。

---

## Explore 刷新の骨子（`docs/explore-redesign.md` が正）

- **2本の柱**は提案と実行。それを貫く共通部品が**券**で、場所によって縮尺だけが変わる。
- 場所は3つ … 提案（1枚）／ストック（格子）／マップ（点）＋ プランを重ねる。
- **言葉は一般語**。メタファーは視覚だけ。「本日の販売は終了しました」のような
  言い回しは使わない。
- 入鋏＝残す。**鋏痕の形は4ドメインに対応**し、3か所（券の穴／マップのノードの形／
  ストックの絞り込み）で同じ意味を持つ。
- **CSS の 3D 変形は使えない**（design.md 冒頭）。立体感は SVG で「描く」。

---

## 直近完了（第69巡の 9〜20巡目 ＝ 改札鋏）

### ★★★20巡目 ── 輪郭を**直線と円弧**に組み直し／段を色で見せ／バネを図から測る

**① なぞった点の羅列をやめた**（ユーザー「トレースで点をとってきただけで、整理されて
いない」）。角と角のあいだを**1本の辺**にする ―― 弦からのずれ ≤1px なら直線、
最小二乗で円を当ててずれ ≤1px なら円弧、駄目なら半分に割る。
生成物は `L(x,y,inner)` / `A(x,y,r,cx,cy,ccw,inner)` の並びになった（**89辺・直62/弧27**）。
点に開くのは `flattenPiece`（約4°刻み）。
- ★★**円弧の向きは「区間の中点が掃きの中に入るか」で決める**。弦と中心の左右で
  推理すると、画像の y が下向きなのと合わさって裏返り、長いほうの弧を掃いて破綻する
  （実測 面積 +1044%）。
- ★★**当てはめは「角と角」に掛ける**。細かい間引きの点で切ると全部直線になる
  （実測 37辺中36本）。
- ★**次のデフォルメは、この辺の並びを間引く／半径を丸めるだけで済む。**

**② 厚みの段を色でも見せる**（ユーザー確定）。厚い片も薄い片も正面の法線は +z なので
同じ色になり、違いは段差の壁だけ。その壁は 17単位 × 画角 0.22 ＝ **画面上 4px** しか
なく、**角度の問題なので形を綺麗にしても見えない**（走査して実測）。
アプリの規則「陰の手当ては群に1度だけ段を下げる」で見せる ―― `toneRamp(dim)` が
**段の表を dim 段ずらして焼く**（シェーダーは触らない）。`DIM = [0, 1, 1]`。
実測 … 一段暗い色の画素が **536 → 9700**（18倍）。

**③ バネの脚も図から測る**（ユーザー「リングの辺りの形が捉えられていない」）。
19巡目まで脚の4点だけ手で置いていた。脚は2本線で描かれ、**あいだが閉じた領域**として
残っているので、輪の帯を除いて**主成分**を取れば端が出る。
★領域は**地のものだけ**を採る（部品側まで見ると右の持ち手の細部を脚と取り違える）。
★輪は**脚が触れている角度から**1周する。


### ★★★19巡目 ── なぞった形を**整形してから**モデルに入れる／段差／輪

ユーザー指摘3点。原因はどれも実測で特定できた。

**① エッジがギザギザ**（実測 最短の辺 2.2単位＝1画素・短辺37本・急な折れ33か所）
… 画素の階段をそのまま多角形にしていた。整形を入れた:
マスクに open→close → 輪郭を追う → **粗い DP で本物の角を拾う** → 角の間を平滑
→ **弧長で等間隔に取り直す**（辺の長さの下限を作る）→ 細かい DP → 近すぎる点を溶かす。
- ★★**DP は開いた列に使う**。閉じた列（先頭＝末尾）に使うと最初の segment が長さ0に
  なり、どの点も距離0と判定されて**両端しか残らない**。
- ★平滑の回数と取り直しの間隔は**片の寸法に合わせる**（一律だと細い片が13%痩せた）。
- 結果 … 短辺 37→7本／最短の辺 2.2→8.9単位／元の輪郭からのずれ 最大2.4画素／
  面積の変化は −0.7〜+1.0%（高さ14画素の細い先だけ −8.6%）。

**② 段差が坂に見える**（面取り7 に対し段差は17 しかなく41%が斜面）… 2つ直した:
- **食い込むのは薄い側だけ**にした。18巡目は全部の片を同じだけ太らせていたので
  厚い片が薄い領域へ庇のようにはみ出していた。
- ★★**段の境目は面取りしない**（`NipperPiece.inner`）。境目かどうかは
  「すぐ外に同じ部品の別の段が居るか」で決める（外の縁からの距離で見ると、
  段が縁まで届いているところを外と数えて 96点中9点しか拾えなかった）。

**③ リングが筒に見える** … 2巻きを半径差3・z差4で重ねていた。針金の直径17に対して
差が小さすぎて融合していた。**1本の輪**にし、半径と太さを図から実測して使う
（芯の半径 61.8／針金の半径 8.5。18巡目は穴の bbox 58.3 を半径にし、太さは手書きの7）。


### ★★18巡目 ── 厚みを**3段の塗り分け**から読むようにした／輪を滑らかに

ユーザーが平面図を3色に塗り分けて厚みを指定（**部品分けは現状で正しいので維持**）。

- 段は **厚 45 / 中 40 / 薄 28**（半分の厚み。ユーザー確定）。表は `lib/nipperRig.ts` の
  `HALF` 1か所だけ。**どこがどの段かは平面図が持つ**（`lib/nipperShape.ts`）。
- ★★**押し出す単位は「部品」ではなく「片」**（`NIPPER_*_PIECES`）。
  部品の画素と段の画素を**交差**させて割る。段を線画の閉じた領域へ割り当てては
  いけない ―― 塗りが領域の境目に乗っている保証が無い。
- ★18巡目まで厚みを **y の関数**（`STEP_Y`）で決めていたが、塗り分けは y の帯では
  ない（中と薄が y で重なる）ので表せなかった。関数は捨てた。
- ★★厚み図の判定式は**実測した色**で書く。ユーザーは**こちらの重ね絵の上に**塗るので、
  注記の色と混ざらないこと（輪郭の緑 `#30D158` は B=88、継ぎ目の紫 `#BF5AF2` は G=90）。
- ★片を捨てるかは**太らせる前**の画素数で決める。太らせた後で見ると幅3画素の筋が
  180画素の塊に化けて残る（実測で欠片が16枚出た）。
- バネの輪は1周 10 → **48** 折れ（「リングがリングに見えない」）。断面は8角形のまま。
- 検証 … 段の画素は 厚 29601 / 中 905 / 薄 2946 で塗り分けと一致。
  片は6枚すべて狙いどおりの半厚で組めた（**比較6件・食い違い0件**）。


### ★★★17巡目 ── **three.js（WebGL）へ移した**

> これはやはりちゃんと3Dモデルにした方がいいかもしれません（ユーザー）

形はトレースで合ったのに、なお**板に見えた**。原因は描き方ではなく**立体そのもの**
（角の立った、上から下まで同じ厚みの角柱）。立体を作り直し、光・遮蔽・影を本物に任せた。

- `lib/nipperMesh.ts`（新規）… 面取り1段（`CHAMFER` = 7）＋**高さで変わる厚み**。
  ★巻きを反時計回りに揃えてから使う（逆だと面が裏になって**中が透ける**）。
- `lib/nipperRig.ts`（新規）… **組み立てと光**。本番と三面図が**同じものを見る**ための1か所。
- `lib/nipperSolid.ts`（自前の SVG 投影）は**撤去**。
- ★段は `NIPPER_PAINT.ramp` の6色のまま。`MeshToonMaterial` の段の出し方を
  `applyAppTones` で差し替え、16巡目までの `toneOf` と同じ式にした
  （three の既定は ①`n·光` を 0 で切る ②段の**赤だけ**を読む、の2点で合わない）。
- ★`DirectionalLight` の強さは **π**（three は拡散を 1/π するので戻す）。環境光は入れない。
- ★三面図は**光をカメラと一緒に回す**（世界に固定すると側面と上面が真っ黒になる）。
- ★★回す向きの符号は SVG と three.js で**逆**（y の向きが違う）。移したとき
  開きと閉じが入れ替わった。
- 禁じられているのは **CSS の 3D 変形**であって WebGL ではない（`design.md` 冒頭に明記した）。


### ★★★16巡目 ── **目測をやめ、平面図をトレースする**ようにした

9〜15巡目は写真を目で読んで数値を打ち込んでいた。機構は13巡目で合ったが、
**プロポーションは何巡やっても合わなかった** ―― 目測の精度以上にはならない。
`tools/trace-nipper.mjs` が平面図＋ユーザーの塗り分けから `lib/nipperShape.ts`
（**生成物。手で直さない**）を作る。部品分けは図形では決められないので**色で決める**
（赤＝右／青＝左）。支点・スリット・バネの輪も図から拾う。

### 9〜13巡目の教訓（詳細は `docs/explore-redesign.md` §2-b）

- **13巡目**… 機構は**ペンチ**で確定（先端が繋がった箱ひとつ＋ハの字に大きく開く腕）。
  9〜12巡目は頭を2つの塊として並べていたので「2本の棒」にしか見えなかった。
- **12巡目**… ★**厚みを削って解決しない**。薄く・浅くすると面が減って板に見える。
  正しい直し方は段の幅を広げて見える面を真ん中へ寄せること。
- **11巡目**… 裏面判定の y の符号が逆で、上を向いた面が常に落ちていた（「形が違う」の主因）。
- **9巡目**… 輪郭を描くのをやめ、立体を組んで面を落とす作りへ。動きは `lib/spring.ts`
  の減衰振動＋rAF（framer-motion は入れない）。★JSX 側は「いまのバネの値」で書く。

### ★★アプリ全体のハイドレーションが壊れていたのを直した（`app/layout.tsx`）

`<style>` を `<html>` の直下に `precedence` 無しで置いていた。不正な HTML なので
**ハイドレーションが失敗し、クライアント側が丸ごと動かなくなる**。
`href="tokens" precedence="default"` を付けて React に `<head>` へ持ち上げさせた。
★これは Explore とは無関係の**全画面に効く**修正。

---

## 次の一手

1. **鋏をユーザーに見てもらう**（`/dev/explore` の先頭の1体と三面図）。
   形の直しは**平面図を描き直して生成し直す**。厚みの直しは**厚み図を塗り直す**か、
   `lib/nipperRig.ts` の `HALF`（`[45, 40, 28]`）を変える。数値を散らかさない。
2. **提案（TODAY）** … 鋏を右下に置き、掴んで券へ寄せる。券の縁に**吸い付き**、
   離すとそこが切り欠かれ、券が落ちて次が出る。下端に束の厚み。
3. **ストック（STOCK）** … 格子・鋏痕での絞り込み・期限。
4. ここで一度**実機で確認**（提案側が完成する）。
5. マップのレイアウトエンジン（駅データの調達から）。

## 未解決

- ★**側面図では厚みの段が見えない**。薄い片は厚い片の中へ挟み込まれているので、
  正射影の輪郭には出ない（＝それが「挟み込む」ということ）。段の確認は
  **重ね絵**と、`buildPart` の z の広がりを数で突き合わせる方でやる。
- ★**デフォルメ（持ち手を単純な幾何へ）は次の巡**。辺の並びになったので、
  間引く／半径を丸めるだけで済むはず。
- 頭の天に出る先（高さ14画素）は整形で面積が 4.2% 痩せる。ずれ自体は 1.1画素で
  線の太さの中だが、細いものほど割合が大きく出る。気になるなら平面図を描き直す。
- 持ち手の厚みは先端まで一定のまま（塗り分けが「厚」なので）。絞るなら塗り直す。
- ★`/dev/explore` は WebGL の器を**10個**開く（本番は1個）。iOS は同時に開ける数が
  少ないので、実機で確かめるときは落ちることがある。本番の導線には影響しない。
- バネの脚は図の細い線を拾えないので**まだ手で置いている**（`COIL.legFar/legNear`）。
  閉じたとき脚の先が腕から離れる。
- 提案とストックを1画面にするか、2つに分けるか。
- 駅データ（ekidata）の同梱可否。

---

## 重要パス

- ★Explore 刷新の設計 … `docs/explore-redesign.md`
- ★UI の規約と機械チェック13本 … `design.md`（作業の前後に必ず走らせる）
  ／ 目盛り … `lib/tokens.ts` ／ 色 … `lib/constants.ts`
- 券 … `components/explore/Ticket.tsx` / `PunchMark.tsx` / `lib/ticket.ts`
- 鋏（★three.js）… `components/explore/Nipper.tsx`（画角・器・動き）
  ＋ `lib/nipperMesh.ts`（立体の作り方）＋ `lib/nipperRig.ts`（組み立てと光）
  ＋ ★`lib/nipperShape.ts`（**生成物**）＋ `tools/trace-nipper.mjs`（生成する道具）
  ／ 三面図 `components/explore/NipperViews.tsx` ／ 確認画面 `app/dev/explore`
- タスクの本体 … `components/tabs/GravityTab.tsx`
- 入口の輪 … `components/CreateMenu.tsx`（RECORD / TASKS / SETTING）
- カメラ … `components/tasks/TaskSpace.tsx`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite` / `fetchDirect` / `jinaSlot`）
  ／ 単体チェック `npx tsx tools/jina-check.mjs`
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちてページが 500）。
  Playwright は `executablePath: "/opt/pw-browsers/chromium"`、
  **`localhost` で開く**（`127.0.0.1` だと Next が開発用の資材を弾く）。
  ★**WebGL を撮るには** `--use-gl=angle --use-angle=swiftshader
  --enable-unsafe-swiftshader` が要る（無いと canvas が空で返る）。
  ★★測り方の罠4つ … ①dispatch 直後に DOM を読むと rAF 前の値が返る
  ②遠い行を測ると「連鎖」の遅れを不具合と読み違える（**焦点の行**を測る）
  ③`waitForTimeout(40)` は 60fps に対して 2.4 コマなので**測り方のせいで**ばらつく
  ④★**同じ式から書いた2つの値を突き合わせても検証にならない** ―
  「見えている幾何」を測り、**比較できた件数を必ず出す**
