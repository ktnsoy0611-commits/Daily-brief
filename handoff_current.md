# 現在地（2026-08-19）

現行仕様は `docs/project_knowledge.md` が正。経緯は `docs/archive/`。
このファイルは**常に200行以内**に保つ（更新手順は `CLAUDE.md` の「恒久ルール」）。

- ブランチ … `claude/brief-tab-bind-bugs-9ijd62`。
  ★**毎回 `main` へも push する**（2026-08-18にユーザー確定・確認不要）。
  `main` が Vercel の本番ブランチで、実機はここを見る。作業ブランチへ
  push しただけでは**実機に何も届かない**。

---

## いまどこにいるか

タスクアプリ（TASK）は**図形**（断面ひとつのベタ塗り）と**入力画面**の両方が
確定した。入力は TickTick 型の即時入力（チャコール地・ツールバー＋ポップオーバー）。
3 つのアプリのうち**ブリーフ（EXPLORE）は実運用中**、タスクとジャーナルは
UI が出来た段階で、Cowork の仕分けとの往復はこれから。

★★★**第13〜23巡（11巡）ぶんの再発は、第24巡で根から作り直して終わった。**
原因は正典に載っていた**成り立たない類推**だった。いまのキーボード追従は
**1行で言える**:

> **器（入力画面の全画面）の `top` と `height` を、`visualViewport` の値
> そのものにする。それだけ。**

第26巡でその上に**モーションを作り直した**。曲線と時間は
`app/globals.css` の `:root` と `lib/motion.ts` の**2か所だけ**が持つ
（§3「モーションの語彙」）。

---

## 直近で完了したこと（第27巡）

実機の写真5枚から、3つとも原因を特定して直した。**推測ではなく、写真の症状と
コードの構造が1対1で対応している。**

- ★★★★**画面のいちばん下の帯 … 帆布が積み木の勝者を見ていなかった**。
  `lib/ground.ts` は勝者（app < overlay）を html と theme-color に書くのに、
  帆布（`AppBackdrop`）だけが `groundOf(appId)` を**直に見ていた**。だから
  入力画面が開くと **html は暗いのに帆布はアプリの明るい色**。帆布は
  `zIndex: -1` で html の上にあるので、**html の色は帆布の外側 — 画面の
  いちばん下 — にしか出られない**。第26巡は「時間」だけ揃えて、
  **色そのものが2つある**ままだった。いまは `onGround` で購読して勝者を塗る
- ★★★**帆布の寸法を `position: fixed; inset: 0` に**。`100lvh` は iOS の
  スタンドアロンで下のセーフエリアを含まないことがあり、そのぶん**画面の下まで
  届かない**。届かない所は端末が manifest の色で塗る。
  `app/manifest.ts` の `theme_color` / `background_color` も地色へ揃えた
  （★**反映にはホーム画面から一度消して追加し直す必要がある** — ユーザー作業）
- ★★★★**開閉が悪化した原因は Framer Motion の共有要素**（第26巡で入れた）。
  layout animation は「測った矩形へ合わせる transform」を**焼き付ける**。
  器は `place()` が高さを書き換え続けるので**焼き付けた値がすぐ古くなる**。
  実機の症状はすべてこれ1つ … キーボードの後ろに地が無い（**アイコンを叩くと
  直る＝そこで測り直される**）／開き終わりに背景がパッと出る／閉じるときに
  要素が3つリレーされて「2段階ガクッ」
- ★★★**器を円で切り抜く形へ作り直した**（`clip-path: circle()`）。
  **切り抜きは寸法を持たない**ので、器がどう動いても地の面も中身も常に器そのもの。
  要素の受け渡しもゼロ。覆い終わったら切り抜きごと外す。`framer-motion` は
  依存ごと撤去した
- ★★**閉じる動きが半分で打ち切られていた**。`LEAVE_MS`(300) < `--t-out`(600)。
  いまは `lib/motion.ts` の `T_OUT` から引いて必ず同じ。
  ★`--t-out` は **600ms → 350ms**（ユーザー確定「きびきび」）
- ★覆う曲線を `--ease-settle` から **`--ease-sheet`** へ。`--ease-settle` は
  350ms で 99% まで行って残り 1% に 350ms かけるので、覆う動きに使うと
  「端の細い余白だけが長く残る」。半径は四隅より 8% 大きくして角を作らない
- 実測（本番ビルド・CPU 4倍遅く）… 落ちたフレームは**増えていない**
  （器ごと切り抜いても数字が動かない）。覆い切るまで約 330ms

## その前の巡（要点だけ）

- **第26巡** … モーションの語彙を4本の曲線・5つの時間へ統合（これは残っている）。
  地色の積み木を「後勝ち」から「高い層が勝つ」へ。背後の後退・中身の時間差・
  押した手ざわり。★ただし**＋から広がる仕掛けを Framer Motion で作ったのが
  失敗**で、第27巡で作り直した
- **第25巡** … キーボードを閉じても閉じない（`window.innerHeight` は
  スタンドアロンで**一緒に縮む**ので「出た」と一度も判定できていなかった。
  いちばん大きく見えた高さを持ち続ける形へ）／帯の下のセーフエリアを
  キーボード中は取らない／日程シートで舞台を凍らせる／
  図形の点滅は `className` の付け外しで直す（`key` の入れ替えは canvas ごと
  作り直され、まっさらな1フレームが出るので禁物）
- **第24巡** … ★★★★正典に載っていた**成り立たない類推**（「録音の
  オーバーレイと同じ書き方にする」— **あの画面には入力欄が1つも無い**）の上に
  11巡ぶん代用品を積んでいた。器を `visualViewport` の矩形そのものにして
  222行を削除。検証を**位置だけ**を見る形へ作り直した（`rect24.mjs`）
- **第19〜23巡** … `--vvtop`（ずれの補正）を入れては外しの繰り返し。
  アイコンの反応（`touch-action: none` ＋ `data-pressed` ＋ 当たり判定44）
- **第17〜18巡** … 閉じる合図は1つだけ・時計ではなく状態で決める／
  `onFontsReady` が購読しただけで呼び返して文字が点滅していた
- **単体テストは lib の写し**。焼き直さないと固まったまま

---

## 次に着手すること

1. **実機（iOS Safari）での確認**（この環境で WebKit を動かせないため構造的な制約）
   - ★★**画面のいちばん下の帯**… まだ出るか。★出る場合は
     **ホーム画面から一度消して追加し直してから**もう一度見ること
     （`manifest.ts` の色は追加した時点で端末に焼き込まれる）
   - ★★**開閉**… ＋から円で広がるか／＋へ吸い込まれるか／
     **キーボードの後ろまで地があるか**（第27巡の本題）／
     閉じる 0.35 秒がちょうどよいか
   - ★**キーボードと一緒に正しく表示されるか**。上のバーが上端に・
     帯がキーボードのすぐ上に・図形がその間の真ん中に
   - ★**キーボードを閉じたら閉じるか**／**アイコンを押しても画面が動かないか**／
     **日程のときだけキーボードが閉じて別窓が出るか**
   - ★崩れていたら **設定 →「画面の数値を出す」**を ON にして写真を1枚。
     **「器 上/下」が両方 0 なら器は正しい**
2. **Cowork のプロンプト更新**（`COWORK-ROUTINES.md`）… 候補の項目を
   `いつ`（★**日付で書かせる**。YYYY-MM-DD）/ `道具・場所` / `持ち物` /
   `タグ`（英字5つ）へ揃える。日付で書かれないと期日にならず、
   `lib/inboxImport.ts` がメモへ回す。
   ★**プロンプトの全文を提示して承認を得てから**実装する。

---

## 未解決・持ち越し

- **実機 Safari の未検証** … タスクアプリ全般、ジャーナルの円のドラッグと
  マイクの解放。
- **候補のうちジャーナル・ウィッシュ・ストックの行先が未定**／**1日の終わりに
  3アプリの要素を1枚のポスターへプレスする**（§38）は未着手。
- **完成時に撤去するもの** … `lib/taskDemo.ts` と「デモを入れる」ボタン、
  `ProfileTab` の「ブリーフ生成の実験」カードと「今すぐ生成」、
  ★**「画面の数値を出す」**（`lib/debugViewport.ts` / `ViewportProbe.tsx`）。

---

## いま触っている領域のファイル

★全体のファイル地図は `CLAUDE.md`。ここは**いま手を入れている所だけ**。

```
lib/motion.ts                      ★動きの語彙(JS)。曲線・時間・＋の丸の場所
lib/ground.ts                      ★地色。優先度つきの積み木・時間の統一・onGround
components/AppBackdrop.tsx         ★帆布。勝者を塗る／fixed inset:0 で下まで届く
app/globals.css                    ★動きの語彙(CSS)の :root ／振付／沈む合図
components/AppShell.tsx            ★シェルを絶対にスクロールさせない見張り
components/tasks/TaskComposer.tsx  入力画面。★器の top/height 追従・共有要素・図形の舞台
components/tasks/TaskAddButton.tsx ★共有要素の起点(＋の丸)
components/tasks/Popover.tsx       ポップオーバーの器と ★`Press`(押せる面)
components/tasks/WhenSheet.tsx     日程（★キーボードを閉じるシート）
components/tasks/ViewportProbe.tsx ★開発用の数値表示（直ったら撤去）
```

## 検証（`scratchpad/`）

★**単体テストの前に必ず写しを焼き直す**（`lib/*.ts` → `*.mjs`。焼かないと
固まったままで、消えた API を試し続ける）:
`for m in types constants solid taskSize taskTags inboxImport; do npx esbuild lib/$m.ts --format=esm --outfile=$S/$m.mjs; done`
（`./types` などの相対 import に `.mjs` を足すこと）

★★**入力画面とキーボードを触ったら、まず `rect24.mjs`。**
`visualViewport` の getter を差し替えて**実機と同じ条件**を作り、
**位置だけ**を見る（代用品の値は見ない）。

```
ground26.mjs ★★地色。列・帆布・html が同じ合図で動くか／theme は折り返しか／
             ★★**画面のいちばん下 1px == html == 帆布**(閉じた状態と開いた状態)／
             入力画面の地色が奪われないか／閉じたら戻るか
motion26.mjs ★★語彙が1か所か／切り抜きの円が＋から四隅を覆い切るか／
             ★**最初から最後まで同じ1つの要素**か(受け渡しが無い)／
             閉じる動きが打ち切られていないか／時間差／押し 90ms・戻り 350ms
rect24.mjs ★★4通りの型で器・上のバー・帯・図形・シートの位置／閉じる合図／
           ★★**キーボードが出たあとも 地の面 == 器**(第27巡の本題)／
           ★レイアウトごと縮む端末での開閉／帯の下の隙間
when25.mjs ★日程シートを開閉しても図形が飛ばないか（塗られた画素で測る）
pop21.mjs  閉じるあいだ板が1枚か・図形の登場と入れ替え（色数が落ちないか）
when20.mjs 日程シート・閉じる出足（引数=CPUの絞り）
tap.mjs    アイコンの当たり判定・沈む合図・クリアの塗り
geo4.mjs plus.mjs v15/v14/v12/v10/v8/v7/v6.mjs  第6〜18巡ぶんの回帰
smooth.mjs 落ちたフレーム / blink.mjs 文字の点滅 / bake.mjs 焼き直し
swipe seam junk text drift2.mjs
solid.test / tag.test / inbox.test.mjs  単体 90 件
```

★**画面の中央の列を掴むときは、位置が止まるのを待つこと**（第26巡）。
`|x| < 5` の決め打ちだと、横スライド(380ms)や入力画面を閉じた後の戻り(600ms)の
途中で**隣の列を掴んで**しまい、無関係な TimeoutError になる。

## ユーザー側の作業（アプリの外）

- Cowork に**毎晩のタスク「1日を仕分ける」**を設置する（例: 02:00 JST）。
- 任意: `OPENAI_API_KEY` を Vercel に登録すると文字起こしが Whisper 系になる。
