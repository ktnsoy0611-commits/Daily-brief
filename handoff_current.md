# 現在地（2026-08-16）

現行仕様は `docs/project_knowledge.md` が正。経緯は `docs/archive/`。
このファイルは**常に200行以内**に保つ（更新手順は `CLAUDE.md` の「恒久ルール」）。

- ブランチ … `claude/brief-tab-bind-bugs-9ijd62`。`main` も同じ先頭へ早送り済み
  （Vercel の本番ブランチ。以後もユーザーの許可を得てから統合する）。

---

## いまどこにいるか

タスクアプリ（TASK）の作り直しが 2 巡して確定した。
いまは**断面の形ひとつ**（円/半円/三角/四角）のベタ塗り。3D・アイソメは持たない。
ネーム／タグの切り替えは載る文字が変わるだけで、山は動かない。

3 つのアプリのうち、**ブリーフ（EXPLORE）は実運用中**（夜間 Cron が毎日 2 回
デッキを生成し、実機で使えている）。**タスクとジャーナルは UI が出来た段階**で、
中身の運用（Cowork の仕分けとの往復）はこれから。

---

## 直近で完了したこと

- タスク: **ネーム / タグビュー**へ。切り替えは載る文字が変わるだけ（山は動かない）
- タスク: 円・半円・三角は**本来の比を必ず保つ**（幅の上限が半円を潰していた）
- タスク: 期限で大きさを強く動かす（今日 2.2 倍〜先 0.45 倍。面積の幅 24:1）
- タスク: 混雑したら**縮めずに間引く**（40 件→10 件・最大 137px）
- タスク: 書体を**全部ゴシック**へ（明朝の読み込みごと削除）
- タスク: 断面の形（丸/半円/三角/四角）へ。伸びるのは四角だけ
- タスク: 文字を中央揃えにし、**最低 16px＋面積に比例**（13.4px 固定をやめた）
- タスク: グリフをサイズのバケツで焼き、和文の実測を省いて**ジャギーを解消**
- タスク: 塗り率で外接箱を広げ、**形が違っても同じ重要度なら色の量が同じ**に
- 全体: **カラースキームを差し替え**（参照画像の「地 × 文字」9 組。`SCHEME` が唯一の出どころ）
- タスク: タグの 5 色を レッド/グリーン/イエロー/ブルー/オレンジ へ（ピンクと濃紺をやめた）
- タスク: **書体もタグと一対一**（同じタグ＝同じ書体。id から引くのは廃止）
- タスク: **面積＝重要度**（WEIGHT × 期限）、**横幅＝タイトルの長さ**へ作り直し
- タスク: タイトルは一定の大きさで**左下**（「できるだけ大きく」は廃止）
- タスク: 数に応じた**一括スケール**と **LOD**（小さい図形から文字が消える）
- タスク: 重要度の面積比を 1 : 2.25 : 5 に広げ、差をはっきりさせた
- タスク: 図形の色と文字の色を**タグと一対一**でスキームの組に（`inkOn` は削除）
- タスク: **タグを持たない図形を無くす**（`resolveTag` が必ず 1 つ割り当てる）
- タスク: **ビュー切替のたびに落とし直す**（上から順に縮んで消えてから再落下）
- タスク: 当たり判定をそのビューで描く形に（BOTTOM は断面の多角形）。宙に浮くのを解消
- タスク: ダミーを増量（タスク 16・候補 12）＋設定から `demo-` だけを消せるように
- タスク: 立面 2 枚（FRONT=長方形＋タイトル / BOTTOM=断面＋タグ英字）へ作り直し（`d53f935`）
- タスク: グリフのアトラスで落下時の描画コストを 1/3 に
- タスク: 設定画面を「方眼を強調した立方体の展開図」へ（`components/tasks/TaskSheet.tsx`）
- タスク: タグを 5 つ（WORK/LIFE/WELLNESS/SOCIAL/GROWTH）に。色は全面へ適用
- タスク: 重要度＝厚みと物理の重さ、タイトルの文字数＝横幅、手順＝スラブの枚数
- ジャーナル: 声の記録を巨大な円 2 つのダイヤル UI で確定（§38〜§49）
- シェル: タブバーをフローから外し、`.full-bleed` で「タブバー付近の途切れ」を構造的に解決
- シェル: アプリの地色を `AppColumn` が持つようにし、遷移中の境目を解消
- ブリーフ: 生成を 3 層→2 層へ再構築。0 枚問題を解消し、1 日 2 回・各 10 枚前後で稼働
- ブリーフ: 情報源の打率を集計値（`lib/sourceStats.ts`）として持ち、淘汰ループを修復
- ドキュメント: 7,489 行の旧 handoff を `docs/archive/` へ退避し、自動読み込みを撤廃（今回）

---

## 次に着手すること

1. **実機（iOS Safari）での確認** … タスクアプリはまだ一度も実機で見ていない。
   - 和文フォントの読み込みの重さと、書体が届くまでの見え方（fallback で焼いた絵）
   - 図形 16 個ぶんの落下の重さ（Chromium・4× 絞りでは合計 1757ms / 最大 226ms）
   - 間引きの手ざわり（40 件で 10 件しか出ない。隠れたタスクに触れないのが不便でないか）
   - 期限による大きさの差（今日 2.2 倍〜先 0.45 倍）が強すぎないか
2. **Cowork のプロンプト更新**（`COWORK-ROUTINES.md`）… 候補の項目を
   `いつ` / `道具・場所` / `持ち物` / `タグ`（英字 5 つ）へ揃える。
   ★**プロンプトの全文を提示して承認を得てから**実装する。
   それまでは `lib/inboxImport.ts` が新旧どちらの書式も受ける。
3. **`main` への統合の判断**（Vercel の本番ブランチ。ユーザーに確認する）

---

## 未解決・持ち越し

- **実機 Safari の未検証**（この環境で WebKit を動かせないため構造的な制約）
  … タスクアプリ全般、ジャーナルの円のドラッグとマイクの解放、
  `.full-bleed` でタブバーの下へ潜る見え方。
- **タスクを手で足す入口が弱い**。いまは声のメモ → Cowork の候補が主。
- **候補のうちジャーナル・ウィッシュ・ストックの 3 種の行先が未定**
  （データとしては `AppState.inbox` に残っている）。
- **1 日の終わりに 3 アプリの要素を 1 枚のポスターへプレスする**という
  最終出力（§38 の体験の構造）は未着手。
- **完成時に撤去するもの** … `lib/taskDemo.ts` と「デモを入れる」ボタン、
  `ProfileTab` の「ブリーフ生成の実験」カードと「今すぐ生成」。

---

## いま触っている領域のファイル

```
components/tabs/GravityTab.tsx   落として積む（matter.js・描画ループ・焼き予算）
components/tabs/DriftTab.tsx     候補の円環カバーフロー
components/tasks/TaskSheet.tsx   方眼の設定画面（展開図）
components/tasks/SolidCanvas.tsx 図形1つを描く
lib/solid.ts                     幾何（純粋関数）
lib/solidPaint.ts                図形の描画＋ビットマップのキャッシュ・LOD
lib/textFit.ts                   グリフのアトラス(サイズのバケツ)・fitText / layoutText
lib/taskSize.ts                  面積=重要度×期限 / 比ははしご(四角だけ) / 落下順
lib/taskTags.ts                  5つのタグ → 色の組と書体・`resolveTag`
lib/inboxImport.ts               Cowork の候補を取り込む（新旧の書式を受ける）
app/layout.tsx                   和文フォントの読み込み（★1書体1ウェイトまで）
```

## ユーザー側の作業（アプリの外）

- Cowork に**毎晩のタスク「1 日を仕分ける」**を設置する（例: 02:00 JST）。
- Cowork の**一度だけの移行タスク**（my-brain を新パスへ）が済んでいるか確認。
  アプリは新旧どちらのパスからも読めるので、済んでいなくても動く。
- 任意: `OPENAI_API_KEY` を Vercel に登録すると文字起こしが Whisper 系になる。
