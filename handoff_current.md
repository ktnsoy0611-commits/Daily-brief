# いまどこにいるか（2026-09-12／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。3アプリは手つかず。
★★★**Explore の刷新（券・改札鋏）は保留**。コードは消していない（`DEV` タブごと最後に処分）。

★★★**いま試している大きな変更は1つ。実機で見て決めてもらう段階。**
**アプリごとのアクセント配色**（`lib/appAccent.ts` の `ACCENT_TEST`）。
**`false` の1行で元の 6色パレットへ丸ごと戻る。**

仕様の正 … `docs/home-spec.md`（ホーム）／`docs/project_knowledge.md` §3-h（確定したこと）
／`design.md`（UI の規約と機械チェック13本）。

---

## 直近完了

### ★★★第93巡 ── タグを全廃し「日付あり＝塗り／なし＝輪郭」へ／録音の円を色へ

ユーザーの指摘は「タスクの図形関係が整理されていない」。**分類の道具が多すぎた。**

- ★★★**タグを全面廃止**（`lib/taskTags.ts` と `lib/tagPattern.ts` をファイルごと削除）。
  経緯 … 場面で割った5つ（必ず重なる）→ 第91巡に動機の2つ＋柄 → 第87巡の配色替えで
  **2つの色がすでに同じ**になり、残る差は柄と書体だけ ＝ **意味を持たない2択**だった。
- ★★★**タスクの性質は「日付があるか／ないか」の1軸だけ**。言い方も1つ ――
  **日付あり＝メインカラーの塗り／日付なし＝輪郭線**（中は地と同じ色、縁と字は
  メインカラー）。**もとはホームの帯のピルの見え方**で、それを図形
  （GRAVITY／DRIFT／ALIGN／入力画面の下書き／ホームの山）へ広げた。
  ★★**合図は `isDated(spec)`** ＝ `spec.sides.includes("due")`。`sidesOf()` は
  埋まっている側面だけを返すので、**日付の有無は最初から図形の仕様の中に在った**
  （フィールドを足していない）。
  ★★★**`paintKey` に日付の有無と地の色を入れた** ―― `sides` は**数**しか見ていない
  ので、`["title","due"]` と `["title","context"]` はどちらも 2＝**形は同じで塗りが逆**。
  ★★**地の色は呼ぶ側が渡す**（`SolidPaint.ground`）。入力画面だけ地が墨なので、
  決め打ちにすると下書きの中だけクリームになる。
  ★★**線は内側に引く**（道を `EDGE/2` 内側へ寄せて `EDGE` で引く）。実測の実効の
  太さ **2.15 デバイス画素／倍率2 ＝ 1.08 CSS 画素**。
- ★★**書体を1つに固定**（`SHAPE_FACE`。ユーザー確定）。`FONT_FACES` は 6 → **1**、
  `app/layout.tsx` から **Zen Maru / Dela / M PLUS の3本を外した**（誰も使わない
  Web フォントを実機で取りに行くだけだった）。
  ★**書体を「分類の道具」にしない。** 増やしたくなったら新しい分類を持ち込む合図。
- ★★**帯の下の段はまるごと輪郭に**（`isOutlined` ＝ 提案以外）。下の段の3種
  （声の候補・フォローアップ・期日未割当）は**3つとも日付を持たない**。
  ★★**前巡の指定「フォローアップは塗りのまま」は今回の1軸に置き換わった。**
  ★上の段（EXPLORE の提案）は**塗りのまま** ―― 黄は地との比 **1.27** で線にすると消える
  （ユーザー確定「輪郭はタスクだけ」）。
- ★★★**録音の大きな円を `JOURNAL_FACE`（メインカラー）へ**（ユーザー指定）。
  **明るいタブも暗いオーバーレイも同じ色**で、画面ごとの反転（黒／白）をやめた
  ＝ 第78巡の「暗いのは機械」はここで終わり。縁の目盛りは `bodyInkOn(円)` の**ベタ**。
- ★★★**ホームの山の JOURNAL の図形を「録音の円そのもの」へ**（ユーザー指定・文字なし）。
  角丸の四角＋「今日を録る」は廃止。寸法は**`lib/dial.ts` の1か所**（SVG と canvas が
  同じ数を読む）。★**当たり判定に `dial` を足すのを忘れないこと**（円は半径で見る枝に
  入れないと押しても飛ばない）。
- ★入力画面 … タグの列を削除、ツールバーの丸を **5つ → 4つ**、`WhenSheet` の
  アクセントを TASK のメインカラーへ。ALIGN／TIMELINE の**タグのピルも廃止**
  （日数のラベルが同じことを言っていて二重だった）。
- 検証（件数）… 帯のピル **上6=塗り／下7=輪郭**（0 取りこぼし）／DRIFT は
  塗り 88,590 画素・輪郭の中 128,205 画素で**両方出る**／GRAVITY は 198,992 と 47,636／
  入力画面は日付を入れた瞬間に**オレンジ 3,360 → 127,503 画素**（＝輪郭→塗り）／
  輪郭線の実効 **1.08 CSS 画素**／録音の円 `rgb(158,178,239)`＋目盛り5本 `#2C2627`／
  山の青い円をタップ → **RECORD へ遷移**（スクリーンショットで確認）／
  旧タグ 26 件を読ませて **tag を持つ行 0 件**（`migrate` の単体チェック）／
  タグ関連の参照 **0 件**（grep）／機械チェック13本 **0件**／`tsc` 0・eslint 0・`build` 成功。

### ★★★第92巡 ── ホームの山を土台から組み直した

- ★★★**ぼやけの正体 ＝ 同じ器に ResizeObserver が2つ**。先に走る①が `sizeRef` を
  書き換えるので、②（実解像度を直す唯一の場所）が毎回「差 0」で return していた。
  実測 … 前は **CSS 1px あたり 2.00 画素**のまま固定、後は 4/4 で **3.00 画素**。
- ★★★**下が空いていた正体 ＝ 床がタブバーのぶん二重引き**。タブの器は
  `paddingBottom: var(--nav-h)` を持つので、`.bleed-x` だと器がタブバーの上で終わる。
  → **`.bleed-x-b`（左右＋下）**。実測 164px 浮き → **31.7px**（狙い 32）。
- ★★★**「少し変えるたび不安定」＝ 中身が1文字変わると世界ごと作り直し**。
  → **effect を2本に割った**（世界＝`[measured]` で1度だけ／中身＝図形だけ差し替え）。
- ★★**眠りを許すのは時間ではなく「みんな止まったら」**（床を下げると時間の見積もりが
  外れて宙で固まる）／**画面の細かさは端末そのまま**（`min(3, dpr)`）＋ グリフの
  焼き段に **192** を追加／`onFontsReady` を購読／**紙の目（`paperize`）を全廃**。
- ★**提案カードの上限を `BRIEF_POOL_CAP`(15) の1つへ**（表示だけ。下の「未解決」参照）。

### ★★★第89〜91巡までの要点

- **`lib/wordPlate.ts`**（日付・曜日の板）… GRAVITY のコードをそのまま移した1か所。
  ★**DOM で組み直さないこと**（板だけが物理と別の座標系に居ると挙動が違う）。
- **山が「塔」だった正体** … ①面積の予算は「横に2つ並ぶ」前提なのに1枚が器の幅の
  68% まで取れた（`FIT_W`/`FIT_H` を 0.52/0.28）②出どころの高さが全部同じ
  （`DROP_SCATTER` 200・間隔 60ms）。
- **日付と曜日の字間を詰めて塊に**（`PLATE_TRACK` -0.06em。★目盛りの外）。
- **ホームは4つ目の列**／**帯**はゆっくり流れ続けて循環（継ぎ目を消すには2周の幅が
  寸分たがわず同じ。速さは「画面1つ分を横切る時間」で持つ）。
- ★★★**刻みは固定（1000/60）**／★★★**焼き込まれた色を信じない**
  （`BriefCard.color` は生成した夜のパレット）／★★★**タブバーの高さは
  `navHeightPx()` から引く**（実機 132px／Chromium 81px）。

---

## 次の一手

1. ★★★**実機で見てもらう** … ①**塗りと輪郭**の見分け（とくに輪郭の字は地の上で
   比 2.55。細く見えないか）②**録音の円の青**（明るい画面では地との比 2.02 で
   輪郭が弱い）③**ホームの山の録音の円**（目盛りの細さ）
   ④**アクセント配色を採るか捨てるか**（捨てるなら `ACCENT_TEST` を `false`）。
2. **引き下ろし**（★ホームの軸。まだ無い）… 帯のピルを掴んで引き下ろすと
   **7本の柱が地面から立ち上がり**（高さ＝その日の量）、離した日がその日付になる。
   外で離すと候補（ストック）へ。**ピルは落ちながら図形へ連続的に変わる**。
   ★**塗り／輪郭の規則はもう通してある**ので、日付が入った瞬間に塗りへ変わる。
3. **持ち物** … 右上のアイコン＋数の印（警告）。押すと印のすぐ下に小さな一覧。
4. **生成器が2つ足りない** … ①「今日行くのがおすすめ」（新しいサーバー関数）
   ②フォローアップの夜間まとめ生成（`Task.suggestions`）。
   ★★**どちらもプロンプトを先に文面で出して承認を得る。**
5. ★ピルを押したときの詳細（まだ無い）。★山の図形はもう押せる（`Piece.nav`）。

## 未解決

- ★★★**提案の生成は止まったまま。** 15 は**表示だけ**の上限（ユーザー確定）なので、
  裏の未読 41 枚は減らず、夜間の生成は `POOL_CAP`(40) の番に掛かり続ける
  ＝**新しい提案が入ってこない**。実際に減らす側へ切り替える場所は
  `app/api/cron/build-brief/route.ts` の `POOL_CAP` にコメントで明記した。
- ★**帯の②（今日行くのがおすすめ）は生成器が無いので今は出ない。**
- ★★**輪郭の字は地の上で比 2.55**（本文の下限を割る。★目盛りの外・ユーザー確定）。
- ★★**録音の円と地の比は 2.02**（元の黒い円は 11.25）。明るい画面では円の輪郭が
  弱い。淡いアクセントをクリームに置く以上どうにもならない（色の選択の帰結）。
- ★★**券の大きな英語が読めなくなる恐れ** ―― アクセントの段は白い紙の上で
  4.85/3.21/2.20/1.54 で、下2つは大きな文字の下限 3.0 を割る。**券は `DEV` タブと
  `/dev/explore` にしか無い**ので4タブには影響しない。
- ★★**地 `#FFFBF5` と紙 `#FAFAF9` の差が比 1.01**（第76巡から持ち越し）。
- ★`SectionLabel`（`MUTED`）は明るい地で 3.2 ＝ AA 割れ。**既知・未対応。**

---

## 重要パス

- ホーム … `components/tabs/HomeTab.tsx`／帯 `components/home/Band.tsx` ＋
  `lib/homeBand.ts`／山 `components/home/Pile.tsx` ＋ `pileWorld.ts` ＋ `pilePaint.ts`
  ／器の寸法 `lib/pileBox.ts`／列の配線 `components/AppShell.tsx`／仕様 `docs/home-spec.md`
- ★★★**塗り／輪郭は `lib/solidPaint.ts` の `paintShape` と `isDated` が正**。
  ホームの山だけ形が角丸の四角なので `components/home/pilePaint.ts` にも同じ分岐がある
  （**片方を直したらもう片方も**）。
- ★★★**録音のダイヤルの寸法は `lib/dial.ts` の1か所**（`VoiceStudio` の SVG と
  ホームの山の canvas が同じ数を読む）。
- ★★★**山の器は `.bleed-x-b`**（`app/globals.css`）。左右＋**下**を列の外へ出す
  ―― 下を出さないと `floorYOf` が**タブバーの高さを二重に引く**。
- ★★★**器を見る ResizeObserver は1つだけ。** 2つ付けると先に走ったほうが
  もう一方の番人を黙らせ、**canvas の実解像度が初回のまま固定される**。
- ★★★**文字の板は `lib/wordPlate.ts` の1か所**。★**DOM で組み直さないこと。**
- ★★**アクセントのテスト** … `lib/appAccent.ts`（`ACCENT_TEST` の1行で戻る）。
  効いている先は `lib/constants.ts` の `TASK_FACE`／`JOURNAL_FACE`／`STUDIO_KEY` と
  `lib/palette.ts`（ドメイン4）。
- ★**色そのものは `lib/constants.ts` の `PALETTE`（6行）だけ**／役との対応は
  同ファイルの `SCHEME` と `lib/palette.ts`
  ／★**面から導く3つ** … `bodyInkOn`・`redOn`・`inkVarsOn`。**表を持たず、導かせる。**
- ★UI の規約と機械チェック13本 … `design.md`（作業の前後に必ず走らせる）
  ／目盛り `lib/tokens.ts`／動き `lib/motion.ts` ＋ `app/globals.css` の `:root`
- タスク … `components/tabs/GravityTab.tsx`（★山の物理の出どころ。ホームと**対**）
  ／カメラ `components/tasks/TaskSpace.tsx`／入口の輪 `components/CreateMenu.tsx`
- 券・鋏（★保留中）… `components/explore/samples/`／`Ticket.tsx`／`lib/nipperShape.ts`
  （生成物。手で直さない）／設計 `docs/explore-redesign.md`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite` / `fetchDirect` / `jinaSlot`）
  ／ 単体チェック `npx tsx tools/jina-check.mjs`
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちて 500）。Playwright は
  `executablePath: "/opt/pw-browsers/chromium"`、**`localhost` で開く**。
  ★★**山の測定は 8 秒待つ**（落ちる距離が伸びた。4.5秒だとまだ空中に居る）。
  ★★**canvas の倍率は場所で違う** … ホームの山 3／GRAVITY・DRIFT・入力画面 2。
  線の太さを画素で測るときは**その canvas の倍率**で割ること（3 で割って誤った）。
  ★`migrate` の単体チェックは、state に **`wishes: []` を入れておくこと**
  （無いと「最古の形式」と見なされて state ごと作り直される）。
  ★`next build` は Google フォントの取得に**ときどき失敗する**（環境側。数回やり直す）。
