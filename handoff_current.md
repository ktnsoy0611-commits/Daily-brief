# いまどこにいるか（第69巡・2026-08-29／Explore の刷新の途中）

3アプリ（JOURNAL / TASK / EXPLORE）は動いている。第68巡までで TASK は一段落。
**第69巡から Explore の刷新を始めた。設計の正は `docs/explore-redesign.md`。**

★★新しい**券と改札鋏は `/dev/explore` にしか無い**（本番の導線からは辿れない）。
**Explore の4タブはまだ手つかず**で、現状のまま動いている。

---

## Explore 刷新の骨子（`docs/explore-redesign.md` が正）

- **2本の柱**は提案と実行。それを貫く共通部品が**券**で、場所によって縮尺だけが変わる。
- 場所は3つ … 提案（1枚）／ストック（格子）／マップ（点）＋ プランを重ねる。
- **言葉は一般語**。メタファーは視覚だけ。「本日の販売は終了しました」のような
  言い回しは使わない。
- 入鋏＝残す。**鋏痕の形は4ドメインに対応**し、3か所（券の穴／マップのノードの形／
  ストックの絞り込み）で同じ意味を持つ。
- **CSS の 3D 変形は使えない**（design.md 冒頭）。立体感は SVG で「描く」。

---

## 直近完了（第69巡の 9〜22巡目 ＝ 改札鋏）

### ★★★22巡目 ── **パスの整理（リトポロジー）**。なぞると整えるを分けた

ユーザー指示「シルエットは非常に良い。**座標データは絶対に破棄せず**、点が多すぎて
ガタつく線と歪んだ曲線を整理して」。**元データを先に凍結してから**着手した。

```
図4枚 → tools/trace-nipper.mjs → lib/nipperShapeRaw.ts（生）
                                → tools/clean-nipper.mts → lib/nipperShape.ts（整）
```

- ★**元データは `docs/archive/nipper-shape-21.ts` に凍結**（誰も import しない）。
- ★整理は**再実行できるプログラム**。形を直すときは規則（`tools/clean-nipper.mts`
  の先頭の目盛り 7つ）を直して走らせ直す。図を描き直しても整理はそのままかかる。
- 型と `L`/`A`/`piece` は `lib/nipperPath.ts` へ（生成物2つが共有）。import 先は
  変わらないので `lib/nipperRig.ts` 以降は無変更。
- **辺 62 → 51**。潰れた辺 3→**0**、軸平行 3→**12**、シルエットのずれは最大
  **2.55**（許容 6）。最小のくびれ 37.0（面取り2倍 = 14 なので bevel は破綻しない）。
- スリットは**軸平行の長方形**に作り直した（左 x=155／右 x=175／底 y=−229）。
  元の溝は右へ傾いているのでずれ 16.4 が出るが、**指定どおりの造形**なので許容の外。
  正面図を走査した実測で、溝の本体 72行すべて**左 x=194・右 x=204（標準偏差 0.00）**。
- ★★**急な折れ（40°超）は「カーブ」ではなく角**。見ないと直角3つの長方形に円を
  当ててしまう（実測でずれ 106）。★★**スリットはいちばん最後に作る**（先に作ると
  あとの規則が直角を丸める）。★★**溝の範囲は生の `NIPPER_SLOT` の枠から取る**
  （「上のほうの点」で探すと始点まで巻き込み、ずれ 674・辺 32→9 に落ちた）。
- 検証 … `buildPart` の z の広がりと `HALF[tier]` の突き合わせ **比較3件・食い違い
  0件**、寄せた輪郭の自己交差 **0件**、`tsc` 0・eslint エラー 0・`npm run build` 成功、
  機械チェック13本（#1〜#7・#10〜#13 = **0件**）。


### ★★★21巡目 ── **部品ごとの図**から組み立て直した（入力が4枚になった）

あわせてユーザー指摘3点を直した:
- **色の塗り分けをやめた**（`DIM = [0, 0]`）。段は**形**で見せる。仕掛けは残してある。
- ★★**塗り残しを「いちばん近い塗り」へ配る**（多源の幅優先）。手で塗った図は輪郭の
  黒線の上を塗れないので、ふちに 2〜4画素の塗り残しが残る（実測 956px）。既定を
  「厚い」にしていたため、**薄いはずの先端のふちだけが厚いまま立ち上がって**いた。
- ★★**バネの脚は輪の接線方向に入る**。付け根から円への接線2本のうち、**測った
  当たり所に近い側**を採る。輪は**閉じた1本の丸**として別に作る（1本の管では
  枝分かれできない）。針金は3本（輪・脚・脚）。実測で脚と半径のなす角 90.00°。

ユーザー指摘「左の部品と右の部品が重なる部分がおかしい／リングを含む針金の形が
かなりおかしい」。**根本原因は入力**だった。

- 16〜20巡目は**組み立て図1枚**から2部品を割っていたが、そこでは**左が右に隠れて
  いる**ので断片しか取れず、**2部品の重なりが存在しなかった**。部品ごとの図を
  もらって解消（右の面 23769px／左 16496px／**重なり 4571px**）。
- 入力は4枚（**同じ枠なので位置合わせ不要**）… 右の図／左の図／左の厚み図／組み立て図。
- 段は**2つ**に（厚45／薄28）。以前の「中40」は重なった図の上で塗ったための取り違え。
  ★**右の中では左は必ず薄い段**（塗り分けより優先）。厚いままだと面が重なって
  ちらつく（実測 192px の筋が頭を縦に走った）。
- ★**バネは引き算で取る**（組み立てのインク − 両部品の面 ＝ 1992px）。19〜20巡目は
  「地の閉じた領域」を探していて右の持ち手の細部と取り違えた。
  ★脚は両端が開いているので**閉じ演算**で2本線を1本の帯に繋いでから主成分を取る。
- ★★**輪は二重丸で描かれている**。放射走査で帯を2本取り（内28.2／外35.8）その中間を
  芯にする。18〜20巡目は内側を芯にしていたので**輪が12%小さかった**。
- ★★**輪の中を弦が横切っていた**（1周したあと反対側の脚へ飛んでいた）。
  **もう片方の脚が触れる角＋1周**まで掃くようにして解消。
- ★支点は**重なりの下の端**（＝蝶番）。部品ごとの図があって初めて測れるようになった。


### ★★★20巡目 ── 輪郭を**直線と円弧**に組み直し／段を色で見せ／バネを図から測る

**① なぞった点の羅列をやめた**（ユーザー「トレースで点をとってきただけで、整理されて
いない」）。角と角のあいだを**1本の辺**にする ―― 弦からのずれ ≤1px なら直線、
最小二乗で円を当ててずれ ≤1px なら円弧、駄目なら半分に割る。
生成物は `L(x,y,inner)` / `A(x,y,r,cx,cy,ccw,inner)` の並びになった（**89辺・直62/弧27**）。
点に開くのは `flattenPiece`（約4°刻み）。
- ★★**円弧の向きは「区間の中点が掃きの中に入るか」で決める**。弦と中心の左右で
  推理すると、画像の y が下向きなのと合わさって裏返り、長いほうの弧を掃いて破綻する
  （実測 面積 +1044%）。
- ★★**当てはめは「角と角」に掛ける**。細かい間引きの点で切ると全部直線になる
  （実測 37辺中36本）。
- ★**次のデフォルメは、この辺の並びを間引く／半径を丸めるだけで済む。**

**② 厚みの段を色でも見せる**（ユーザー確定）。厚い片も薄い片も正面の法線は +z なので
同じ色になり、違いは段差の壁だけ。その壁は 17単位 × 画角 0.22 ＝ **画面上 4px** しか
なく、**角度の問題なので形を綺麗にしても見えない**（走査して実測）。
アプリの規則「陰の手当ては群に1度だけ段を下げる」で見せる ―― `toneRamp(dim)` が
**段の表を dim 段ずらして焼く**（シェーダーは触らない）。`DIM = [0, 1, 1]`。
実測 … 一段暗い色の画素が **536 → 9700**（18倍）。

**③ バネの脚も図から測る**（ユーザー「リングの辺りの形が捉えられていない」）。
19巡目まで脚の4点だけ手で置いていた。脚は2本線で描かれ、**あいだが閉じた領域**として
残っているので、輪の帯を除いて**主成分**を取れば端が出る。
★領域は**地のものだけ**を採る（部品側まで見ると右の持ち手の細部を脚と取り違える）。
★輪は**脚が触れている角度から**1周する。


### 16〜19巡目の要点（詳細は `docs/explore-redesign.md` §2-b）

- **19巡目**… なぞった輪郭を**整形してから**モデルに入れる（マスクに open→close →
  粗い DP で角を拾う → 角の間を平滑 → 弧長で等間隔に取り直す → 細かい DP）。
  ★**DP は開いた列に使う**（閉じた列だと両端しか残らない）。
  ★★**段の境目は面取りしない**（面取り7に対し段差は17しかなく、41%が斜面になる）。
  ★**食い込むのは薄い側だけ**（厚い片が庇のようにはみ出していた）。
- **18巡目**… 厚みを**塗り分け**から読む。★片を捨てるかは**太らせる前**の画素数で。
- **17巡目**… **three.js（WebGL）へ移した**。「板に見える」原因は描き方ではなく立体
  そのものだった。段は `NIPPER_PAINT.ramp` の6色のまま（`MeshToonMaterial` の段の
  出し方を `applyAppTones` で差し替え）。★`DirectionalLight` の強さは **π**。
  ★回す向きの符号は SVG と three.js で**逆**。
- **16巡目**… 目測をやめてトレースへ。`lib/nipperShape.ts` は**生成物。手で直さない**。

### 9〜13巡目の教訓（詳細は `docs/explore-redesign.md` §2-b）

- **13巡目**… 機構は**ペンチ**で確定（先端が繋がった箱ひとつ＋ハの字に大きく開く腕）。
  9〜12巡目は頭を2つの塊として並べていたので「2本の棒」にしか見えなかった。
- **12巡目**… ★**厚みを削って解決しない**。薄く・浅くすると面が減って板に見える。
  正しい直し方は段の幅を広げて見える面を真ん中へ寄せること。
- **11巡目**… 裏面判定の y の符号が逆で、上を向いた面が常に落ちていた（「形が違う」の主因）。
- **9巡目**… 輪郭を描くのをやめ、立体を組んで面を落とす作りへ。動きは `lib/spring.ts`
  の減衰振動＋rAF（framer-motion は入れない）。★JSX 側は「いまのバネの値」で書く。

### ★★`app/layout.tsx` のハイドレーションが壊れていた（全画面に効く修正）

`<style>` を `<html>` 直下に `precedence` 無しで置いていた（不正な HTML → 水和が
失敗し、クライアントが丸ごと動かない）。`href="tokens" precedence="default"` で解決。

## 次の一手

1. **整えた鋏をユーザーに見てもらう**（`/dev/explore` の先頭の1体と三面図）。
   直し方は3つ。**形**＝平面図を描き直して `trace` からやり直す／**整理の具合**＝
   `tools/clean-nipper.mts` の先頭の目盛りを変えて走らせ直す／**厚み**＝厚み図を
   塗り直すか `lib/nipperRig.ts` の `HALF`（`[45, 28]`）。数値を散らかさない。
   ★ここから先の「デフォルメ（持ち手を単純な幾何へ）」も、整理の規則を足す形で行う。
2. **提案（TODAY）** … 鋏を右下に置き、掴んで券へ寄せる。券の縁に**吸い付き**、
   離すとそこが切り欠かれ、券が落ちて次が出る。下端に束の厚み。
3. **ストック（STOCK）** … 格子・鋏痕での絞り込み・期限。
4. ここで一度**実機で確認**（提案側が完成する）。
5. マップのレイアウトエンジン（駅データの調達から）。

## 未解決

- ★**側面図では厚みの段が見えない**。薄い片は厚い片の中へ挟み込まれているので、
  正射影の輪郭には出ない（＝それが「挟み込む」ということ）。段の確認は
  **重ね絵**と、`buildPart` の z の広がりを数で突き合わせる方でやる。
- ★**デフォルメ（持ち手を単純な幾何へ）はまだ**。辺の並びになったので、
  間引く／半径を丸めるだけで済むはず（右32辺・左40辺のうち円弧28本）。
- 頭の天に出る先は細いので整形で 4.2% 痩せる（ずれ自体は線の太さの中）。
- 持ち手の厚みは先端まで一定のまま（塗り分けが「厚」なので）。絞るなら塗り直す。
- ★`/dev/explore` は WebGL の器を**10個**開く（本番は1個）。iOS は同時に開ける数が
  少ないので、実機で確かめるときは落ちることがある。本番の導線には影響しない。
- バネの脚は図の細い線を拾えないので**まだ手で置いている**（`COIL.legFar/legNear`）。
  閉じたとき脚の先が腕から離れる。
- 提案とストックを1画面にするか、2つに分けるか。
- 駅データ（ekidata）の同梱可否。

---

## 重要パス

- ★Explore 刷新の設計 … `docs/explore-redesign.md`
- ★UI の規約と機械チェック13本 … `design.md`（作業の前後に必ず走らせる）
  ／ 目盛り … `lib/tokens.ts` ／ 色 … `lib/constants.ts`
- 券 … `components/explore/Ticket.tsx` / `PunchMark.tsx` / `lib/ticket.ts`
- 鋏（★three.js）… `components/explore/Nipper.tsx`（画角・器・動き）
  ＋ `lib/nipperMesh.ts`（立体の作り方）＋ `lib/nipperRig.ts`（組み立てと光）
  ＋ ★形の生成 … `lib/nipperPath.ts`（語彙・手書き）→ `tools/trace-nipper.mjs`
  → `lib/nipperShapeRaw.ts`（生）→ `tools/clean-nipper.mts` → `lib/nipperShape.ts`
  （**どちらも生成物。手で直さない**）／元データの凍結 `docs/archive/nipper-shape-21.ts`
  ／ 三面図 `components/explore/NipperViews.tsx` ／ 確認画面 `app/dev/explore`
- タスクの本体 … `components/tabs/GravityTab.tsx`
- 入口の輪 … `components/CreateMenu.tsx`（RECORD / TASKS / SETTING）
- カメラ … `components/tasks/TaskSpace.tsx`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite` / `fetchDirect` / `jinaSlot`）
  ／ 単体チェック `npx tsx tools/jina-check.mjs`
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちてページが 500）。
  Playwright は `executablePath: "/opt/pw-browsers/chromium"`、
  **`localhost` で開く**（`127.0.0.1` だと Next が開発用の資材を弾く）。
  ★**WebGL を撮るには** `--use-gl=angle --use-angle=swiftshader
  --enable-unsafe-swiftshader` が要る（無いと canvas が空で返る）。
  ★★測り方の罠4つ … ①dispatch 直後に DOM を読むと rAF 前の値が返る
  ②遠い行を測ると「連鎖」の遅れを不具合と読み違える（**焦点の行**を測る）
  ③`waitForTimeout(40)` は 60fps に対して 2.4 コマなので**測り方のせいで**ばらつく
  ④★**同じ式から書いた2つの値を突き合わせても検証にならない** ―
  「見えている幾何」を測り、**比較できた件数を必ず出す**
