# 現在地（2026-08-19）

現行仕様は `docs/project_knowledge.md` が正。経緯は `docs/archive/`。
このファイルは**常に200行以内**に保つ（更新手順は `CLAUDE.md` の「恒久ルール」）。

- ブランチ … `claude/brief-tab-bind-bugs-9ijd62`。
  ★**毎回 `main` へも push する**（2026-08-18にユーザー確定・確認不要）。
  `main` が Vercel の本番ブランチで、実機はここを見る。作業ブランチへ
  push しただけでは**実機に何も届かない**。

---

## いまどこにいるか

タスクアプリ（TASK）は**図形**（断面ひとつのベタ塗り）と**入力画面**の両方が
確定した。入力は TickTick 型の即時入力（チャコール地・ツールバー＋ポップオーバー）。
3 つのアプリのうち**ブリーフ（EXPLORE）は実運用中**、タスクとジャーナルは
UI が出来た段階で、Cowork の仕分けとの往復はこれから。

★★★**第13〜23巡（11巡）ぶんの再発は、第24巡で根から作り直して終わった。**
原因は正典に載っていた**成り立たない類推**だった（下記）。いまのキーボード
追従は**1行で言える**:

> **器（入力画面の全画面）の `top` と `height` を、`visualViewport` の値
> そのものにする。それだけ。**

帯・図形・ポップオーバー・日程シートの位置は**全部ただのレイアウト**になり、
`--kb` / `--vvtop` / ずれ残りの閉ループ / 上限 / 物差し — 代用品は全部消えた。

---

## 直近で完了したこと（第25巡）

実機で「かなり安定した」。残っていた4件を、原因を1つずつ突き止めて直した。

- ★★★**キーボードを閉じても閉じない** … 「キーボードが無いときの高さ」を
  `window.innerHeight` から取っていた。**iOS のスタンドアロンはレイアウト
  ビューポートごと縮む**ので `innerHeight` も一緒に小さくなり、
  「出ていない高さ」＝「出ている高さ」になって**一度も「出た」と判定できて
  いなかった**。いちばん大きく見えた高さを持ち続ける形に変えた。
  ★検証にこの型（レイアウトごと縮む端末での開閉）が無かったので追加した
- ★★**アイコンとキーボードの隙間が大きすぎる** … 器の下端は**キーボードの
  上端**でホームバーは無いのに、帯の下にセーフエリア 34px を取っていた。
  `place()` が `data-kb` を立て、CSS が 6px に切り替える
- ★★**when の開閉で後ろの地が一瞬見える／画面がガクッと動く** … 2つあった。
  ①出入りの移動量が `100%`（自分の高さ）だったので、**高さの違う器と下敷きで
  滑る距離が変わって**ずれ、隙間から後ろが見えた → `100lvh`（絶対量）に。
  下敷きは画面いっぱいのまま動かさない。
  ②シートが開くと器が画面いっぱいへ伸び、`flex: 1` の舞台も広がって
  **図形が 187px 下へ飛びシートの裏に隠れていた** → 開く直前の高さで舞台を凍らせる
- ★★**図形が更新された後に点滅** … 動きを `className` で持たせていたので、
  入れ替えが終わって `prev` が消えた瞬間に**登場の動き（不透明度 0→1）が
  掛け直されて**いた。`key` の入れ替えも同じ理由で禁物（canvas ごと作り直され、
  まっさらな1フレームが出る）。**要素は据え置き、動きだけを付け外しする**

## その前の巡（第24巡）

- ★★★★**根本原因の特定**。正典 §1-8 と §4 に第13巡から
  「器は一度置いたら二度と寸法を書き換えない。録音のオーバーレイ
  （`VoiceOverlay`）と同じ書き方にする。あれが実機で滑らかなのは寸法を JS が
  書き換えないからだ」と書いてあった。**`VoiceOverlay` には入力欄が1つも無い**
  （`components/VoiceStudio.tsx`）。**キーボードが出ない画面**である。
  滑らかなのは「書き換えないから」ではなく**追う相手が居ないから**。
  この一文の上に11巡ぶん代用品を積み、そのたび実機で崩れた
- ★★★★**器を `visualViewport` の矩形そのものにした**。`top` と `height` を
  書く（`height` は `resize` のときだけ・`transition` は掛けない・`width` は
  一度も触らない）。**消えたもの**: `--kb` / `--vvtop` / `[data-fit]` /
  `SHIFT_*` / `CORRECT_*` / ずれ残りの閉ループ / 上限 / `legacyShift.ts` /
  「ずれの補正を切る」「旧方式を試す」のスイッチ。**295行のうち222行が削除**
- ★★**下敷きも器と同じ箱にした**。出入りのスライドは `translate3d(0,100%,0)`
  ＝**自分の高さの100%**なので、片方だけ画面いっぱいだと滑る距離が違って
  2枚がずれる
- ★★**閉じにいく気配のときは器を広げない**。キーボードが引っ込むと器は画面
  いっぱいへ伸びるが、直後に滑り降りるので**伸びてから滑る＝一度跳ねる**
  （実機「キーボードが閉じ切った時にガクッと不安定な挙動をしながら下に
  スライド」）。閉じないと決まったときだけ追いつかせる
- ★★★**検証を「位置だけ」に作り直した**（`scratchpad/rect24.mjs`）。
  第18〜23巡の判定は `--kb` や `--vvtop` という**代用品の値**を見ていたので、
  代用品が壊れていても「正しい」と判定できた。**実機の崩れを14巡ぶん通した
  直接の原因**。いまは4通りの型すべてで器・上のバー・帯・図形・シートの
  **位置そのもの**を見る
- 実測（本番ビルド）… 器が縮んでも図形は焼き直さない（`v12` / `bake` / `blink`）。
  出入りの落ちたフレームは悪化していない

## その前の巡（要点だけ）

- **第21〜23巡** … `--vvtop`（ずれの補正）を入れては外しを繰り返した。
  第21巡で凍結をやめ、第22巡で「測って直す閉ループ」にし、第23巡で
  上限が効いていないと判明。**第24巡で仕組みごと撤去**
- **第20巡** … 閉じる曲線 `cubic-bezier(0.32,0,0.67,0)` は制御点が2つとも y=0 で
  最初の3分の1が動かない（60ms で 2%）。`cubic-bezier(0.4,0,0.2,1)` へ
- **第19巡** … アイコンの反応が悪いのは3つ重なっていた（`touch-action` 未宣言／
  当たり判定が丸そのもの32px／`:active` が当たらず沈む合図が出ない）。
  `touch-action: none` ＋ `data-pressed` ＋ 当たり判定44（丸の場所は32のまま）
- **第18巡** … 閉じる合図は1つだけ。`focusout` は焦点を戻すだけで閉じない。
  **時計ではなく状態で決める**（6×に絞ると `setTimeout(260)` が 940ms 遅れる）
- **第17巡** … `onFontsReady` が購読しただけで呼び返し、`SolidCanvas` が1枚
  マウントされるたび全消しが走って文字が点滅していた
- **図形** … 登場は 0.86倍・透明から実寸へ 260ms。情報を足して変わるときは
  canvas を2枚重ねてクロスフェード＋scale（透ける瞬間が無いので点滅しない）
- **キーボードが閉じない4段構え** … `<div role=button>`（`Press`）＋非passiveな
  `touchstart` の `preventDefault` ＋ 行の `onBlur` ＋ layout effect の受け皿
- **単体テストは lib の写し**。焼き直さないと固まったまま（`prism.test` の196件は
  存在しないモジュールが相手だった → 撤去して90件）

---

## 次に着手すること

1. **実機（iOS Safari）での確認**（この環境で WebKit を動かせないため構造的な制約）
   - ★★**キーボードと一緒に正しく表示されるか**（第24巡の本題）。
     上のバーが上端に・帯がキーボードのすぐ上に・図形がその間の真ん中に
   - ★**開閉が滑らかか**／**キーボードを閉じたら閉じるか**／
     **アイコンを押してもキーボードが閉じず画面が動かないか**／
     **日程のときだけキーボードが閉じて別窓が出るか**（ユーザーの4つの希望）
   - ★**日程シートの下が見切れないか**
   - ★崩れていたら **設定 →「画面の数値を出す」**を ON にして写真を1枚。
     **「器 上/下」が両方 0 なら器は正しい**。0 でなければそこが原因
   - **文字が出るか**（第6巡の本題。Chromium では2〜4秒で全部出る。実機が本番）
   - 時刻（ダイアル・帯をつまむ）／カレンダーの読みやすさ／drift の見切れ
2. **Cowork のプロンプト更新**（`COWORK-ROUTINES.md`）… 候補の項目を
   `いつ`（★**日付で書かせる**。YYYY-MM-DD）/ `道具・場所` / `持ち物` /
   `タグ`（英字5つ）へ揃える。日付で書かれないと期日にならず、
   `lib/inboxImport.ts` がメモへ回す。
   ★**プロンプトの全文を提示して承認を得てから**実装する。

---

## 未解決・持ち越し

- **実機 Safari の未検証** … タスクアプリ全般、ジャーナルの円のドラッグと
  マイクの解放。
- **候補のうちジャーナル・ウィッシュ・ストックの行先が未定**／**1日の終わりに
  3アプリの要素を1枚のポスターへプレスする**（§38）は未着手。
- **完成時に撤去するもの** … `lib/taskDemo.ts` と「デモを入れる」ボタン、
  `ProfileTab` の「ブリーフ生成の実験」カードと「今すぐ生成」、
  ★**「画面の数値を出す」**（`lib/debugViewport.ts` / `ViewportProbe.tsx`）。

---

## いま触っている領域のファイル

★全体のファイル地図は `CLAUDE.md`。ここは**いま手を入れている所だけ**。

```
components/tasks/TaskComposer.tsx  入力画面。★器の top/height 追従・出入り・図形の舞台
components/tasks/Popover.tsx       ポップオーバーの器と ★`Press`(押せる面)
components/tasks/WhenSheet.tsx     日程（★キーボードを閉じるシート）
components/tasks/ViewportProbe.tsx ★開発用の数値表示（直ったら撤去）
app/globals.css                    出入り・図形の登場と入れ替え・沈む合図
lib/textFit.ts / solidPaint.ts     グリフのアトラス・★書体の門・ビットマップ
```

## 検証（`scratchpad/`）

★**単体テストの前に必ず写しを焼き直す**（`lib/*.ts` → `*.mjs`。焼かないと
固まったままで、消えた API を試し続ける）:
`for m in solid taskSize taskTags inboxImport; do npx esbuild lib/$m.ts --bundle --format=esm --outfile=$S/$m.mjs; done`

★★**入力画面とキーボードを触ったら、まず `rect24.mjs`。**
`visualViewport` の getter を差し替えて**実機と同じ条件**を作り、
**位置だけ**を見る（代用品の値は見ない）。

```
rect24.mjs ★★4通りの型で器・上のバー・帯・図形・シートの位置／閉じる合図／
           ★レイアウトごと縮む端末での開閉／帯の下の隙間／器と下敷きの滑り
when25.mjs ★日程シートを開閉しても図形が飛ばないか（塗られた画素で測る）
pop21.mjs  閉じるあいだ板が1枚か・図形の登場と入れ替え（色数が落ちないか）
when20.mjs 日程シート・閉じる出足（引数=CPUの絞り）
tap.mjs    アイコンの当たり判定・沈む合図・クリアの塗り
geo4.mjs plus.mjs v15/v14/v12/v10/v8/v7/v6.mjs  第6〜18巡ぶんの回帰
smooth.mjs 落ちたフレーム / blink.mjs 文字の点滅 / bake.mjs 焼き直し
swipe seam junk text drift2.mjs
solid.test / tag.test / inbox.test.mjs  単体 90 件
```

## ユーザー側の作業（アプリの外）

- Cowork に**毎晩のタスク「1日を仕分ける」**を設置する（例: 02:00 JST）。
- 任意: `OPENAI_API_KEY` を Vercel に登録すると文字起こしが Whisper 系になる。
