# いまどこにいるか（2026-09-20／ホームを作っている最中）

**ホームが動いている**（帯3段＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。★第110〜114巡に「落とし直し」を根絶して
**帯の隙間をレイアウトへ作り直し**（並びは**留め金**）、第115〜118巡に**起動の代金を
払い、大きさを「段の高さ」1つ**にして**手ざわりの4つの真因を根治**、
第119〜120巡に**「おすすめ3件」と Explore の詳細の画面**、
第121〜123巡に**提案の図形・帯の3段目（ニュース）・六角形の歪みと当たり判定**、
第124巡に**「カーブ」を1つへ揃えた**（タスク＝ひとつの大きなピル／2段＝日付の板の
高さ／提案の札の凹凸も同じ半径へ）、★★★**第125巡に配色を総入れ替えして、和文を
LINE Seed JP へ替え、帯の送りを循環させ、Explore の詳細を2段階で開くようにした**。
★Explore の刷新（券・改札鋏）は保留（`DEV` タブごと最後に処分）。仕様の正 …
`docs/home-spec.md`（**§6 が引き下ろしの正**）／`docs/project_knowledge.md` §3-h／`design.md`。

## 直近完了

### ★★★第125巡 ── 配色の総入れ替え／和文 LINE Seed JP／帯の循環／2段階で開く

★指示の全文は `docs/project_knowledge.md` §3-h の同名の節。

- **①1段に入る文字数を `ROW_WIDE` 倍** … `rowsOf` の閾値 **10／24**。
  ★★★**同時に `rowSpecOf` の幅を「掛け算」から「下限」へ直すこと**
  （`Math.max(ROW_AR × ROW_WIDE, rowAspect)`）―― `rowAspect` は文字数ぶんの幅を
  もう返しているので、掛けると**二重に広がって `cap` が全体を押し下げる**
  （実測 … 10文字で 514px 対 山の内寸 358px）。
- **②帯の循環** … ★★★**真因は「中身を2周ぶんしか並べていない」** ―― 第124巡は
  送りを `holdRef`（`transform` のずれ）へ足していて**上限が無かった**。→ **送りを
  「流れの時計」（`anim.currentTime`）へ畳む**（`iterations: Infinity` は**勝手に
  循環する**）。★実測（6000px 送る・画面の空き）… **4130〜4886px → 8px で一定**。
- **③配色** … ドメイン4＝**緑／ピンク／ティール／黄**（色と形は組のまま）、
  **JOURNAL＝ラベンダー**、**TASK＝オレンジ**、**NEWS＝水色**（唯一の寒色の
  パステル＝退く）。★★★**赤はドメインから外して「危険」だけ**（オレンジと
  **色相が 8° しか違わない**）。**未読の円も墨へ。**★★★**「タスクは無彩色」
  （第117〜124巡）は撤回**（`TASK_GREY` は残す）。★表と実測は `design.md` §3-2。
- **④ピルは全部「輪郭だけ・黒の字」** … `isOutlined` が**常に真**（消さない ――
  呼び手が全部ここを読むので**塗りに戻すのも1行**／`inkMix` の両端はここから導く）。
  ★★**帯＝全部 輪郭／山＝日付があれば塗り**という**上と下の対比**に変わった。
- **⑤和文 LINE Seed JP** … `app/fonts/` ＋ `next/font/local`。★★★**戻し方は1行**
  （`lib/constants.ts` の `JP` → `JP_NOTO`）。★★**太さは4本を別ファイルで**（1本だと
  **合成ボールド**。Anton で踏んだ罠）。★★**図形の字（`GOTHIC`）も同じ書体**。
  ★★★**代償** … 断片配信ではないので **Rg と Bd の2本＝約 4.3MB**（`display: "swap"`
  で届くまでは代替）。★実測で **Noto は1つも取りに行かれていない**。
- **⑥2段階で開く**（`lib/motion.ts` の `twoStage`）… ★★★**曲線も時間も増やして
  いない**（`EASE_SETTLE` 1本・`T_IN` 1つの中の**割り振りだけ**）。★**「とどまる」は
  等速**（曲線を掛けると「また動き出した」に見える）／★**慣性は「行き過ぎて戻る」で
  出す**（ばねを足さない）／★**閉じるのは1本のまま**。★実測 … 0〜45ms で 1/3 ／
  45〜278ms は**ほぼ静止しながら 9° ゆっくり戻る**／278ms から一気に／700ms で着地。
- **検証** … 機械チェック13本 0 件・`tsc`・`eslint`（警告は HEAD と同じ16件）・
  `build`・Chromium と WebKit で `pageerror` **0**。

### ★★★第124巡 ── カーブを1つに／タスクは大きなピル／帯を指で送る

- **①提案は写真があるものだけ**（`offerPick` と `HomeTab.pileOffers` の**2か所**）。
- **②写真が無いときは英語の1語**（`categoryOfKind`。★**書体は `DISPLAY`**、
  ★★**大きさは版面の幅に合わせて組む** ―― 段から選ぶと短い語だけ間延びする）。
- **③ニュース 8:2** … 主要と検索の2本を同時に取り、好みを**等間隔で差し込む**
  （末尾に固めると**趣味の塊が1周に1度**通り過ぎる）。
- **④ニュースのタップ** … ★**段の `onPointerDown` が慣性の一発を出していた**／
  ★**札を開けているあいだも段が流れていた** → **掛け金 `lockRef`**。
  実測 … 1フレームの飛び **3.36 → 0.25px**／360ms の移動 **3.01 → 0.00px**。
- **⑥タスク＝ひとつの大きなピル** … `pillEdge` 1本（角丸＝高さの半分）。
  ★★★**段の高さ `unit` ＝ 日付の板の高さ ÷ 2**（`PLATE_ROWS`）。幅は `ROW_WIDE`(2) 倍で、
  **同時に `FIT_W` 0.52 → 0.86**（上げないと `cap` が先に効いて**小さくなる**）。
  実測（4／9／14／22件）… 2段 **222×74／153×51／129×43／97×32** ＝ 幅 1.43倍
  （**2.0倍は器に入らない**）。帯からの余裕 **154／108／90／194px**。
- **⑦カーブを1つに** … 凹凸を減らして半径を上げ（波打つ四角 16→**8個**／トゲトゲ
  12→**5個**／六角形の角 0.07→**0.154**／四つ葉 1.45a→**1.25a**）、**平均の比 0.2611 の
  逆数を `OFFER_D`(3.83) に置く** → **提案のカーブ ＝ 1.000 unit ＝ ピルの角**。
  実測の比 0.2778／0.2665／0.2436／0.2563 ＝ **幅 1.14倍**（第123巡は 2.55倍）。
  ★★★**四つ葉は `k > 1` の制約で 0.25 より下へ行けない**ので完全には揃わない。
  ★★★**`OFFER_D` を手で振らない**（大きさを変えるなら凹凸の数のほう）。


### 第120〜123巡（要点。全文は `docs/project_knowledge.md` §3-h）

- **第123巡** … ★★★**六角形の歪みの真因は形の定義**（`fitBox` ではない）―― 頂点が
  正方形いっぱいで**縦に 1.155 倍**（自然な比は √3/2）。生の外接箱は 1.000／
  **0.897**／1.000／1.000 ＝**六角形だけ**／**第121巡の「提案だけ傾けない」は撤回**／
  ★★★**当たり判定は「いちばん近い中心」を採っていた**＋**「板が必ず勝つ」規則の
  根拠（板は最後に塗られる）が事実と逆** → **塗る順と拾う順を同じ配列から引き、
  末尾から探す**＋**遊びは2周目でだけ**／カセットの高さ＝日付の板の高さ／
  ニュースのピルと広がる札（★**指が動かす `t` に曲線を掛けない**。二重だと 6px で
  45% 開く。★**札の高さは中身の高さ**）。
- **第122巡** … ★★★**写真が付かない真因は `crossOrigin="anonymous"`**（OGP 画像は
  CORS ヘッダを返さないので**読み込みそのものが失敗**する）／**崩れたピルは
  `photo` ごと落とす**／**文章は 0 件か 1 件**／**板 0.80倍**（`WORD_W` と上限の
  **両方に**）／**ばねは役の割り当てだけ**（席の畳み `K_SETTLE`・指へ追いつく
  `K_OPEN`。係数は5組）／**ニュースの段**（Google ニュース RSS）。
- **第121巡** … ★★★**`p.r` は「絵の半径」ではない**（遠い点は `p.r × cardShapeReach`）／
  **提案だけ `OFFER_VERTS`(28)**／**夜は明日の分へ**（**そのことは帯の文章が言う**）／
  **帯の文章**（`lib/bandNotes.ts`。★`homeBand` から呼ばない ―― 輪になる）。
- **第120巡** … **件数が増えるほど山は低い**（第118巡の見立てが逆）／**字は
  `min(幅, 上限)` なので上限にも `crowd`**／**近さを3つに割る**（遊びは**閾値より
  小さく**）／**Explore の詳細は `lib/cardMorph.ts`**（全画面は撤回）。

### 第113〜119巡（要点）

- **第119巡** … 帯の上の段は**ストックの「今行ける」だけ**、未読の提案は**山へ**
  （`lib/offerPick.ts`。★AI を使わない）。★★★`createPortal` で `body` 直下へ／
  **`--nav-h` を読まない**（列にしか無い変数を `calc()` に入れると四方が 0）。
- **第118巡** … ①**引き下ろす＝KEEP**（`lib/keepCard.ts`）／②`stepGhost` の
  `if (g.aim)` が**絵の座標を奪っていた**（**第109巡の確定を本人が撤回**）／
  ③挿し口 `at` に**遊び `GAP_HYST`**／④落とし所は**絵の中心 `hx`/`hy`**。
- **第117巡** … 配色の総入れ替え（★**第125巡にもう一度 入れ替わった**）。

- **第115〜116巡** … ★**GRAVITY が軽いのは「最初の画面ではない」から**（ホームは
  **起動の代金**を払っていた。先読みは1つずつ／見えていない `VoiceStudio` は測らない／
  焼くのは1フレーム2枚／書体は落ちる前に頼む＝フレーム **58 → 115〜127**）／
  ★**大きさは「段の高さ」1つ**（`rowSpecOf`。`specOf` は 1 行も変えない）。
- **第113〜114巡** … ★★★**隙間と席は `transform` のずれでは書けない。レイアウトが
  持つ**（同じ id のピルが2周ぶん居るので別々の値を書けない＝2周目が **152px 飛ぶ**）／
  **帯ぜんたいを `lead`＋隙間の半分だけ左へ戻し、挿し込みで「送り」へ畳み替える**／
  ★★★**並びは「留め金」（`AppState.bandPins`）**（`tasks` は触らない ―― GRAVITY と
  ALIGN まで動く／声の候補は持ち主が別）。★**挿し口は index ではなく左どなりの id**。

### 第89〜112巡の要点（全文は `docs/project_knowledge.md` §3-h）

- ★★★**落とし直しの真因は `clearOverlap` が据え置きの山にも掛かっていたこと**
  （→ `Piece.fresh` だけ）／**めり込みの真因は「湧いた瞬間から重なっている」**
  （→ 投入の直前に `clearOverlap`。372件 → 10件）／**眠りは `GravityTab` と同じ**／
  **光線は「正規化した空間」で飛ばす**／**幽霊の寿命は3重**。
- ★★★**canvas の `ctx.font` は読み込みを頼まない**し `document.fonts.ready` は**頼んで
  いないものを待たない**（→`ensureWordFont`）／**実機のバグ ＝ `mask-size`** → `clip-path`。

## 次の一手

1. ★★★**実機で見てもらう**（第125巡）… ①**和文の LINE Seed JP**（気に入らなければ
   `JP` を `JP_NOTO` に戻すだけ。**初回の重さ**も一緒に見てもらう）／②**配色**
   （タスクのオレンジは**山で面積がいちばん大きい**ので、強すぎないか）／
   ③**輪郭だけのピル**（黄・水色の線が実機で見えるか）／④**帯を何周でも送れるか**／
   ⑤**Explore の詳細の2段階**（**とどまる長さ**と**行き過ぎの量**が肝。`lib/motion.ts` の
   `STAGE_A`/`STAGE_H`/`STAGE_OVER` で振れる）／⑥**1段に入る文字数**（10／24）。
2. ★**第124巡の宿題** … 提案の大きさ（`OFFER_D` はカーブから逆算した数なので、
   大きくしたいなら**凹凸の数**を変える）／タスクの幅は **1.43倍が器の限界**。
2. ★**ニュースの札に本文は無い**（RSS は題と出典だけ）。本文が要るなら
   **記事を取りに行く**話になる。★**`magazine` と `ItemStatus "planned"` も死んだ欄**。

## 未解決

- ★**帯の3段目（ニュース）だけ引き下ろせない**（手つきは `NewsPill` が自分で持つ）。
- ★★★**提案の生成は止まったまま**（未読 41 枚が減らず `POOL_CAP`(40) に掛かる。
  おすすめ3件の母集団もこの池 ―― **生成が戻るまで新着は増えない**）。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も焼く予算も直していない**（山だけ）。
  ★**起動の代金は残っている**。★`SectionLabel` は 3.2 ＝ AA 割れ。

## 重要パス

★★**ファイルの地図と、踏んだ罠の全文は `CLAUDE.md`。ここは「どこを開くか」だけ。**

- ホーム … `HomeTab.tsx`／帯 `Band.tsx`＋`lib/homeBand.ts`＋`bandMotion.ts`／
  山 `components/home/`（`Pile`＋`pileWorld`＋`pilePaint`）／器 `lib/pileBox.ts`／
  引き下ろし `lib/pullDrag.ts`＋`pillGhost.ts`＋`AssignRail`＋`AssignSheet`＋`DateGrid`。
- 帯の中身 … **「今日行ける」`lib/todayPick.ts`**（夜は明日へ）／**「文章」
  `lib/bandNotes.ts`**（0 件か1件）／**ニュース `lib/newsFeed.ts`＋`app/api/news/route.ts`
  ＋`components/home/NewsCard.tsx`**（ピルと広がる札）／**おすすめ3件
  `lib/offerPick.ts`**／**カード → `Item` は `lib/keepCard.ts` の1本**。
- Explore … **詳細 `CardDetail.tsx` ＋ 変形 `lib/cardMorph.ts`**／**札の形
  `lib/cardShape.ts`**（★`cardShapeReach`・`inCardShape`・`fitBox` は一様）。
- 図形 … `lib/solid.ts`（`stackOutline`＝**ひとつの大きなピル**）／`lib/taskSize.ts`
  （`ROW_WIDE`）／
  `lib/wordPlate.ts`（板）／`lib/cassette.ts`＋`lib/reelHub.ts`。
- 語彙 … 色 `lib/constants.ts` の `PALETTE`(8行)／**和文の掛け金 `JP`**（戻すなら
  `JP_NOTO`。実体は `app/fonts/`）／動き `lib/motion.ts`（★**CSS に任せられない曲線は
  `easeAt(EASE_SETTLE, t)`。写経しない**／★**2段階で開くのは `twoStage`**）／
  バネ `lib/spring.ts`／目盛り `lib/tokens.ts`／★規約13本 `design.md`。

### ★ここだけは忘れると必ず壊れる

- **絵は `hx`/`hy`**（落とし所にも支点を渡さない）／**姿は掛け金 `Ghost.pill`**／
  **位置は最後まで指**（`Ghost.aim` は `boolean`）／**近さは3つ**（`RAIL_NEAR` 36／
  `BAND_NEAR` 48／`BAND_CATCH` 24。遊び `RAIL_HYST` 12 ＝ **閾値より小さく**）。
- **帯の隙間と席はレイアウト（`.band-pad` の幅）が持つ**／**挿し口は `after`（左どなりの
  id）＋ 遊び `GAP_HYST`**／**狙われている段は流れを止める**／**位相は px で引き継ぐ**。
- **山**… **`clearOverlap` は `Piece.fresh` だけ**／**体も倍率も作り直さない**／**質量は
  同じ密度**／**`atan2` で並べてから `fromVertices`**／**dpr の上限は 2**／**ループは
  止める**（`wake()` を忘れない）／**帯が覆う高さは予算から引く**＋`crowdOf`（板は
  `room` と**上限の両方に**）／**塗るのは変わった図形の箱だけ**。
- **カーブは1つ** … 2段のタスク ＝ 板 ＝ 帯のピル ＝ 提案の凹凸 ＝ **`unit`**。
  ★**`OFFER_D` も `FIT_W` も `ROW_WIDE` も、どれか1つだけ動かすと崩れる**
  （`ROW_WIDE` は `rowsOf` の閾値と `rowSpecOf` の**下限**の両方に効く）。
- **帯** … **ピルは全部 輪郭だけ・黒の字**（`isOutlined` は常に真）／**指の送りは
  `anim.currentTime` へ畳む**（`transform` のずれへ足すと**2周ぶんで尽きる**）。
- **赤はもう分類の色ではない** … オレンジ（タスク）と色相が 8° しか違わない。
- **提案の図形** … **`p.r` は絵の半径ではない**（`cardShapeReach` 倍まで届く）／
  **体の光線は `OFFER_VERTS`(28)**／**写真に `crossOrigin` を付けない**／
  **傾きも回転も他の図形と同じ**（第121巡の固定は撤回）。
- **当たり判定** … 形は全部「絵と同じ」＋**重なったら「画面でいちばん上」**（末尾から
  探す。**遊びは2周目でだけ**）。★**実機は 120Hz**／**iOS の上下の帯は触らない**。

### 検証の作法

★★**WebKit は `executablePath` 無し／Chromium だけ `/opt/pw-browsers/chromium`**／
★★**種に `keeps: []` を入れない**（`migrate` が `items` を丸ごと捨てる）／
★**`generatedDecks` は `Record<号, BriefCard[]>`**／★★**山の測定は 8〜10 秒待つ**／
★★**帯を出す種には「日付の無いタスク」が要る**／★★★**写真が乗った山の canvas は
汚染されて `getImageData` で読めない** → **画面の写しを別のページで読む**か写真なしで／
★★★**帯のピルは2周ぶん DOM に居る**ので、指で触る検証は**画面に出ているほう**を選ぶ／
★★**`bandBus.rows` の添字は論理の段**（0＝提案／1＝タスク／2＝ニュース）／
★★**WebKit は続けて何回も起動すると落ちる**（1プロセス1ケース）／
★★**`next dev` は初回のコンパイルに 45 秒**（`domcontentloaded` ＋ 先に1度叩いて温める）。
