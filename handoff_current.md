# いまどこにいるか（2026-09-11／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。3アプリは手つかず。
★★★**Explore の刷新（券・改札鋏）は保留**。コードは消していない（`DEV` タブごと最後に処分）。

★★★**いま試している大きな変更が2つある。どちらも実機で見て決めてもらう段階。**
1. **アプリごとのアクセント配色**（`lib/appAccent.ts` の `ACCENT_TEST`）。
   **`false` の1行で元の 6色パレットへ丸ごと戻る。**
2. **タグ2つ＋柄**（MUST＝べた塗り／WANT＝網点）。

仕様の正 … `docs/home-spec.md`（ホーム）／`docs/project_knowledge.md` §3-h（確定したこと）
／`design.md`（UI の規約と機械チェック13本）。

---

## 直近完了

### ★★★第92巡 ── ホームの山を土台から組み直した／紙の目を外した／柄を大きく／提案を15に

ユーザーの指摘は「なぜ少し変えるたびにホームだけ不安定なのか。根本から立て直して」。
**測って特定した。3つとも構造の欠陥だった。**

- ★★★**ぼやけの正体 ＝ 同じ器に ResizeObserver が2つ**。①が先に `sizeRef` を
  新しい値へ書き換え、②（実解像度と壁を直す唯一の場所）は「差 0」を見て**必ず**
  return していた（observer は**作った順**に呼ばれるので必然）。canvas は
  **実解像度も CSS の箱も初回のまま固定**。
  **実測（倍率3・器を 844/800/760/844）… 前は4回とも CSS 690px・実解像度 1378
  ＝ CSS 1px あたり 2.00 画素。後は 4/4 で 3.00 画素。**
  → **observer は1つ**＋ **`draw()` の頭で毎フレーム照合**（`GravityTab` と同じ）。
- ★★★**下が空いていた正体 ＝ 床がタブバーのぶん二重引き**。タブの器は
  `paddingBottom: var(--nav-h)` を持つので `.bleed-x` だと器がタブバーの上で終わる。
  そこへ `floorYOf`（nav を引く式）を当てていた。**実測 … 実機で 164px 浮いていた。**
  第91巡の「タブバーの上 116px」は**二重引きを測っていた**。
  → **`.bleed-x-b`（左右＋下）** で器を画面の底まで。**後は 31.7px（狙い 32）。**
- ★★★**「少し変えるたび不安定」の正体 ＝ 中身が1文字変わると世界ごと作り直し**。
  370行が1本の async effect で、`stop` の確認が1か所しかなく**前の走行が後の走行の
  ref を上書き**し得た。→ **effect を2本に割った**（世界＝`[measured]` で1度だけ／
  中身＝`[sig, worldGen]` で図形だけ差し替え。中身側に await が無いので競合しない）。
- ★★**眠りを許すのは時間ではなく「みんな止まったら」** ―― 床を下げて落ちる距離が
  伸びた途端、2.5秒の見積もりが外れて**宙で固まる**（第90巡の形の再発）。
- ★★**画面の細かさは端末そのまま**（`min(3, dpr)`）＋ `lib/textFit.ts` の
  グリフの焼き段に **192** を足した（題 57×3＝171px。144 で頭打ちだと拡大されて滲む）。
- ★★**`onFontsReady` を購読**（ホームだけ購読しておらず、和文が代替書体のまま
  固定され得た）／**焼く作法を `solidBitmap` に合わせた**（回転の余白 1px・整数の箱・
  鍵と実値を一致）／**写真は倍率ぶん大きく取りに行く**（240 の決め打ちをやめた）。
- ★**`Pile.tsx` を3つに割った** … `Pile.tsx`（器・寿命・指）/ `pileWorld.ts`（物理・
  壁・図形）/ `pilePaint.ts`（焼き・描き）。758行 → 366/312/170行。
- ★★★**図形の紙の目（`paperize`）を外した**（ユーザー指定）。呼び先3か所すべて。
  `lib/paperTexture.ts` と `crumple.webp` は残す（券の CSS が同じ画像を使う）。
  ★副産物 … `destination-in` で α を2乗していたので**網点の縁が痩せていた**。
- ★★**網点を大きく**（`DOT_PITCH` 7→**12**／`DOT_R` 1.85→**3.2**）。
  **直径÷刻み 0.53 を保つ**ので濃さは変わらない。CSS 側のぼかしも比例へ。
- ★★**提案カードの上限を `BRIEF_POOL_CAP`(15) の1つに**（`lib/homeBand.ts`）。
  これまで `BriefTab` に 30・夜間の生成に 40 と**2つあってずれていた**（＝ホームが
  41 でデッキが 30）。★★**いまは「表示だけ」**（ユーザー確定）。
- 検証（件数）… 実解像度の一致 **8/8**（前は器を動かしても canvas が動かない）／
  CSS 1px あたり **2.00 → 3.00 画素**／床はタブバーの上 **31.7px**（前 113px）／
  左右は画面の端から **16px**＝タブバー／タスク 3・8・12・16件で山の上端
  322/211/151/182px・帯との余裕 **262/151/91/122px ＝ 重ならない**／震え **0.00px**／
  器 844→800→844 で塗り 750109→749933（**0.02% ＝ 落ち直さない**）／
  柄はべた塗りの行 **272**・網点の行 **728**（両方読める）／網点の刻み
  **36.5 デバイス画素**（狙い 36）／CSS 側も **12px タイル・点 3.2px**／
  未読 41 枚を入れてホームの数字が **15**／`paperize` の呼び出し **0 件**／
  機械チェック13本 **0件**／`tsc` 0・eslint 0（警告7は既存）・`build` 成功。

### ★★★第91巡 ── タグを2つ＋柄へ／日付を塊に

- ★★★**タグを5つ → 2つ**（**MUST＝やねば／WANT＝やりたい**）。ユーザー確定。
  旧五つは**生活の場面**で割っていたので必ず重なった（「仕事のために英語を学ぶ」）。
  **動機**で割れば、どんなタスクも必ずどちらかに入る。
  ★旧タグは**機械的に読み替え**（**16 件すべて読み替えできた／0 件取りこぼし**）。
- ★★★**見分けは色ではなく「柄」**（`lib/tagPattern.ts`）。第87巡の配色替えで
  タグ5色が**オレンジの濃淡5段**になり区別が消えていた（ユーザー指摘）。
  ★★**これは質感ではなく塗り**（`design.md` §3-b の外）。ただし**網点の機械の
  規則は守る** ―― `pattern.setTransform(1/dpr)` で **1タイル画素＝1デバイス画素**。
- ★★**日付と曜日の字間を詰めて塊に**（`PLATE_TRACK` -0.06em。★目盛りの外）。
  `ctx.letterSpacing` は Safari の対応が新しいので**使わず、1文字ずつ送る**。
  **測る側も同じ送りで測る**ので当たり判定は塗りのまま。ホームと TASK の両方。

### ★★★第89〜90巡 ── 「gravity と同じに動かない」の答えは**値ではなく構造**だった

- **`lib/wordPlate.ts`**（日付・曜日の板）を作り、**GRAVITY のコードをそのまま移した**。
  それまで板だけ DOM で組んでいたので、**板だけが物理と別の座標系に居た**。
  ★**DOM で組み直さないこと。**
- **山が「塔」だった正体** … ①**面積の予算は「横に2つ並ぶ」前提**なのに1枚が器の幅の
  68% まで取れた（`FIT_W`/`FIT_H` を 0.52/0.28 へ）②**出どころの高さが全部同じ**で
  一列に降りてきた（`DROP_SCATTER` 200・間隔 60ms）。
- **線だけのピルの中を地と同じ色で塗った**／**字も線と同じ色**（ユーザー指定）。

### ★★★第82〜88巡までの要点

- **ホームは4つ目の列**（起動して最初に出る）。タブバーの中身だけが3アプリの名前へ。
- **帯** … ゆっくり流れ続けて循環。★継ぎ目を消すには**2周の幅が寸分たがわず同じ**。
  速さは「画面1つ分を横切る時間」で持つ（件数が変わっても速さが変わらない）。
- ★**提案の帯が空だった根本原因** … `generatedDecks[today]` しか見ていなかった
  （BriefTab は**日をまたいだ未読の池**を見る）。`unreadCards` を同じ規則へ。
- ★★★**刻みは固定（1000/60）**／★★★**`paperize` を毎フレーム掛けない**。
- ★★★**焼き込まれた色を信じない**（`BriefCard.color` は生成した夜のパレット）。
- ★★★**タブバーの高さは `navHeightPx()` から引く**（実機 132px／Chromium 81px）。

---

## 次の一手

1. ★★★**実機で見てもらう** … ①山の**画質と落ち方と床の位置**（今回の本体）
   ②**網点の大きさ**（12px で足りるか）③**アクセント配色を採るか捨てるか**
   （捨てるなら `ACCENT_TEST` を `false`）。
2. **引き下ろし**（★ホームの軸。まだ無い）… 帯のピルを掴んで引き下ろすと
   **7本の柱が地面から立ち上がり**（高さ＝その日の量）、離した日がその日付になる。
   外で離すと候補（ストック）へ。**ピルは落ちながら図形へ連続的に変わる**。
3. **持ち物** … 右上のアイコン＋数の印（警告）。押すと印のすぐ下に小さな一覧。
4. **生成器が2つ足りない** … ①「今日行くのがおすすめ」（新しいサーバー関数）
   ②フォローアップの夜間まとめ生成（`Task.suggestions`）。
   ★★**どちらもプロンプトを先に文面で出して承認を得る。**
5. ★ピルを押したときの詳細（まだ無い）。★山の図形はもう押せる（`Piece.nav`）。

## 未解決

- ★★★**提案の生成は止まったまま。** 15 は**表示だけ**の上限（ユーザー確定）なので、
  裏の未読 41 枚は減らず、夜間の生成は `POOL_CAP`(40) の番に掛かり続ける
  ＝**新しい提案が入ってこない**。実際に減らす側へ切り替える場所は
  `app/api/cron/build-brief/route.ts` の `POOL_CAP` にコメントで明記した
  （`generatedDecks` はサーバーのものなので、**本当に減らせるのはそこだけ**）。
- ★**帯の②と④は生成器が無いので今は出ない**（③⑤は出る）。
- ★★**券の大きな英語が読めなくなる恐れ** ―― アクセントの段は白い紙の上で
  4.85/3.21/2.20/1.54 で、下2つは大きな文字の下限 3.0 を割る。**券は `DEV` タブと
  `/dev/explore` にしか無い**ので4タブには影響しない。
- ★★**地 `#FFFBF5` と紙 `#FAFAF9` の差が比 1.01**（第76巡から持ち越し）。
- ★`SectionLabel`（`MUTED`）は明るい地で 3.2 ＝ AA 割れ。**既知・未対応。**
- ★**帯の線だけのピルの字は地の上で比 2.55**（ユーザー指定の配色。目盛りの外）。

---

## 重要パス

- ホーム … `components/tabs/HomeTab.tsx`／帯 `components/home/Band.tsx` ＋
  `lib/homeBand.ts`／山 `components/home/Pile.tsx` ＋ `pileWorld.ts` ＋ `pilePaint.ts`
  ／器の寸法 `lib/pileBox.ts`／列の配線 `components/AppShell.tsx`／仕様 `docs/home-spec.md`
- ★★★**山の器は `.bleed-x-b`**（`app/globals.css`）。左右＋**下**を列の外へ出す
  ―― 下を出さないと `floorYOf` が**タブバーの高さを二重に引く**。
- ★★★**器を見る ResizeObserver は1つだけ。** 2つ付けると先に走ったほうが
  もう一方の番人を黙らせ、**canvas の実解像度が初回のまま固定される**。
- ★★★**文字の板は `lib/wordPlate.ts` の1か所**（GRAVITY の日付・曜日／TIMELINE の
  「自由」／ホームの山が同じ関数を読む）。★**DOM で組み直さないこと。**
- ★★**アクセントのテスト** … `lib/appAccent.ts`（`ACCENT_TEST` の1行で戻る）。
- ★**色そのものは `lib/constants.ts` の `PALETTE`（6行）だけ**／役との対応は
  同ファイルの `SCHEME` と `lib/palette.ts`
  ／★**面から導く3つ** … `bodyInkOn`・`redOn`・`inkVarsOn`。**表を持たず、導かせる。**
- ★タグの柄 `lib/tagPattern.ts`（刻み 12／点 3.2）／タグの定義 `lib/taskTags.ts`。
- ★UI の規約と機械チェック13本 … `design.md`（作業の前後に必ず走らせる）
  ／目盛り `lib/tokens.ts`／動き `lib/motion.ts` ＋ `app/globals.css` の `:root`
- タスク … `components/tabs/GravityTab.tsx`（★山の物理の出どころ。ホームと**対**）
  ／カメラ `components/tasks/TaskSpace.tsx`／入口の輪 `components/CreateMenu.tsx`
- 券・鋏（★保留中）… `components/explore/samples/`／`Ticket.tsx`／`lib/nipperShape.ts`
  （生成物。手で直さない）／設計 `docs/explore-redesign.md`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite` / `fetchDirect` / `jinaSlot`）
  ／ 単体チェック `npx tsx tools/jina-check.mjs`
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちて 500）。Playwright は
  `executablePath: "/opt/pw-browsers/chromium"`、**`localhost` で開く**。
  ★★**山の測定は 8 秒待つ**（床を下げて落ちる距離が伸びた。4.5秒だとまだ空中に居る）。
  ★`next build` は Google フォントの取得に**ときどき失敗する**（環境側。数回やり直す）。
