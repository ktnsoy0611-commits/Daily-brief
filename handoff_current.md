# いまどこにいるか（2026-09-19／ホームを作っている最中）

**ホームが動いている**（帯3段＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。★第110〜114巡に「落とし直し」を根絶して
**帯の隙間をレイアウトへ作り直し**（並びは**留め金**）、第115〜116巡に**起動の代金を
払い、大きさを「段の高さ」1つ**にし、第117〜118巡に**配色を総入れ替えして手ざわりの
4つの真因を根治**、第119〜120巡に**「おすすめ3件」と Explore の詳細の画面**、
第121〜122巡に**提案の図形と、帯の3段目（ニュース）と文章**、第123巡に**六角形の
歪みを根治して当たり判定を「見たまま」へ直し**、★★★**第124巡に「カーブ」を1つへ
揃えた**（タスク＝ひとつの大きなピル／2段＝日付の板の高さ／提案の札の凹凸も同じ
半径へ）＋**帯を指で左右に送れる**ように。
★Explore の刷新（券・改札鋏）は保留（`DEV` タブごと最後に処分）。
仕様の正 … `docs/home-spec.md`（**§6 が引き下ろしの正**）／
`docs/project_knowledge.md` §3-h／`design.md`。

## 直近完了

### ★★★第124巡 ── カーブを1つに／タスクは大きなピル／帯を指で送る

> 「**提案は画像があるもの限定に。写真がないときは英語でしっかりレイアウト。
> ニュースは一般を基本に 2割だけ私に合わせて。ニュースをタップした時に帯のピルが
> 不安定な動きをするのを直して。ピルは自分で左右にスクロールできるようにも。
> タスクは一つの大きいピルの形に。2段の時の大きさを日付と曜日の図形の高さと合わせ、
> 横幅を今の2倍くらいに。ピルの角のカーブを基準に形を構成できますか？提案の図形の
> カーブが合っていない。凹凸の数を減らしてカーブを大きくし、調和をとって**」

- **①写真があるものだけ** … `offerPick` と `HomeTab.pileOffers` の**2か所**を同じ条件で。
- **②写真が無いときは英語の1語** … `lib/deckStyle.ts` の `categoryOfKind`（券と同じ
  語彙）。`Piece.glyph` → **`label`**。★**書体は `DISPLAY`**、★★★**大きさは版面の幅に
  合わせて組む**（`drawOfferLabel`。段から選ぶと短い語だけ間延びする）。
- **③ニュース 8:2** … **主要と検索の2本を同時に取り、好みを等間隔で差し込む**
  （`blend`。★末尾に固めると**趣味の塊が1周に1度**通り過ぎる）。第122巡は
  **好みの語が1つでもあれば全件を検索に振っていた**＝世の中の見出しが 0 本だった。
- **④ニュースのタップ** … 真因は2つ ―― ★★★**段の `onPointerDown` が `bandStop`
  （慣性の一発）を出していた**（タップは「掴んで止めた」ではない）／★★★**札を
  開けているあいだも段が流れていた**（元のピルは `opacity: 0` なので**列に穴が動き**、
  閉じる先もずれる）→ **掛け金 `lockRef`**（閉じる・横へ送った・unmount で必ず下ろす）。
  ★実測 … 1フレームの飛び **3.36 → 0.25px**／札を開けている 360ms の移動 **3.01 → 0.00px**。
- **⑤指で左右に送る** … 遊び `PAN_SLOP`(10) を越えたら **`holdRef`**（受け渡しの一様な
  ずれと**同じ入れ物**）へ足す。★★★**ピルが捕捉しても祖先へ上がる**ので口を増やさない。
  ★実測 … 指 +144px で **+123.8／+132.0px**（第123巡は **−8.2／−5.0px** ＝ 動かなかった）。
- **⑥タスク＝ひとつの大きなピル** … `lib/solid.ts` は **`pillEdge` 1本**（角丸＝高さの
  半分）。**`rectEdge`／`RECT_R` は削除**。★★★**段の高さ `unit` ＝ 日付の板の高さ ÷ 2**
  （`PLATE_ROWS`。板は `unit` より先に決まるので循環しない）。**幅は `ROW_WIDE`(2) 倍**。
  ★★★**同時に `FIT_W` 0.52 → 0.86** ―― 上げないと `cap` が先に効いて**タスクは
  小さくなる**（実測 4件で unit 26.4 対 板の 37.0）。
  ★実測（4／9／14／22件）… 2段 **222×74／153×51／129×43／97×32**（第123巡は
  154×103／109×72／91×61／68×46）＝**幅 1.43倍**。**2.0倍は器に入らない**（308px 対
  内寸 358px）。★**4件では板がそのまま効いて 2段 ＝ 板の高さ 74px に一致**／
  9件からは予算が安全網として効く／帯からの余裕は **154／108／90／194px**。
- **⑦カーブを1つに** … **凹凸を減らして半径を上げ**（波打つ四角 16→**8個**／トゲトゲ
  12→**5個**／六角形の角 0.07→**0.154**／四つ葉 1.45a→**1.25a**）、**平均の比 0.2611 の
  逆数を `OFFER_D`(4.4 → **3.83**) に置く** → **提案のカーブ ＝ 1.000 unit ＝ ピルの角**。
  ★実測の比 … 0.2778／0.2665／0.2436／0.2563 ＝ **幅 1.14倍**（第123巡は **2.55倍**）。
  ★★★**四つ葉は `k > 1` の制約で 0.25 より下へ行けない**ので完全には揃わない。
  ★★★**`OFFER_D` を手で振らない**（大きさを変えるなら凹凸の数のほう）。
- **検証** … 機械チェック13本 0 件・`tsc`・`eslint`（警告は HEAD と同じ16件）・`build`・
  Chromium と WebKit で `pageerror` **0**。

### ★★★第123巡 ── 六角形／回転／当たり判定／カセット／ニュースの札

- **①六角形** … ★★★**真因は形の定義**（`fitBox` ではない）―― 頂点が正方形いっぱいで
  **縦に 1.155 倍**（自然な比は √3/2）。→ 正六角形へ＋**`fitBox` を一様な倍率へ**。
  ★「他の図形も調べて」… 生の外接箱は 1.000／**0.897**／1.000／1.000 ＝**六角形だけ**。
- **②回転** … **第121巡の「提案だけ傾けない」は撤回**（犯人は回転ではなく①）。
- **③当たり判定** … ★★★**「いちばん近い中心」を採っていた**＋**「板が必ず勝つ」規則の
  根拠（板は最後に塗られる）が事実と逆**（積む順は 板→タスク→カセット→提案→未読）。
  → **塗る順と拾う順を同じ配列から引き、末尾から探す**＋**遊びは2周目でだけ**。
- **④タスクの形** … 角丸四角へ（★**第124巡に「ひとつの大きなピル」へ上書き**）。
- **⑤カセット** … **高さ＝日付の板の高さ**（px）。`CASSETTE_ROWS` は削除。
- **⑥ニュース**（`components/home/NewsCard.tsx`）… 輪郭のピル＋**引くと広がる札**。
  ★★★**指が動かす `t` に曲線を掛けない**（二重だと 6px で 45% 開く）／
  ★★★**札の高さは中身の高さ**（開いた幅で組んだ写しを測る）。

### 第120〜122巡（要点。全文は `docs/project_knowledge.md` §3-h）

- **第122巡** … ★★★**写真が付かない真因は `crossOrigin="anonymous"`**（OGP 画像は
  CORS ヘッダを返さないので**読み込みそのものが失敗**する。付ける理由は canvas を
  読み返すときだけ＝このアプリには 0 件）／**崩れたピルは `photo` ごと落とす**／
  **文章は 0 件か 1 件**／**板 0.80倍**（`WORD_W` と上限の**両方に**）／**ばねは役の
  割り当てだけ**（席の畳み `K_SETTLE`・指へ追いつく `K_OPEN`。係数は5組のまま）／
  **ニュースの段**（Google ニュース RSS。★サーバーで取る／`AppState` に入れない）。
- **第121巡** … ★★★**`p.r` は「絵の半径」ではない**（遠い点は `p.r × cardShapeReach`）
  ―― 円で見ていたので**出っ張りを押しても掴めず、へこみで掴めた**／**提案だけ
  `OFFER_VERTS`(28)**（12 だと谷ばかり拾って体が 10% 小さい）／**夜は明日の分へ**
  （今日の分があるうちは混ぜない。**そのことは帯の文章が言う**）／**帯の文章**
  （`lib/bandNotes.ts`。★`homeBand` から呼ばない ―― 輪になる）。
- **第120巡** … **件数が増えるほど山は低い**（第118巡の見立てが逆）→ `CROWD_N0` 18・
  `CROWD_MIN` 0.94／**字は `min(幅, 上限)` なので上限にも `crowd`**／**近さを3つに割る**
  （`RAIL_NEAR` 36／`BAND_NEAR` 48／`BAND_CATCH` 24。遊びは**閾値より小さく**）／
  **Explore の詳細は `lib/cardMorph.ts`**（全画面は撤回。写真の粗が出る）。

### 第117〜119巡（要点。詳しくは `docs/project_knowledge.md` §3-h）

- **第119巡** … 帯の上の段は**ストックの「今行ける」だけ**、**未読の提案は山へ**
  （`lib/offerPick.ts`。★**AI を使わない**）。★★★**`createPortal` で `body` 直下へ**／
  **`--nav-h` を読まない**。
- **第118巡** … ①**引き下ろす＝KEEP**（`lib/keepCard.ts`）／②`stepGhost` の
  `if (g.aim)` が**絵の座標を奪っていた** → 削除（**第109巡の確定を本人が撤回**）／
  ③挿し口 `at` に**遊び `GAP_HYST`**／④落とし所は**絵の中心 `hx`/`hy`**。
- **第117巡** … 配色の総入れ替え。**タスク＝無彩色の塗り ＋ 黒の字**／**ドメイン4は
  色と形の組**／★★★**`lib/appAccent.ts` を削除**。

### 第113〜116巡 ── 起動の代金／大きさ／帯の隙間

- ★★★**GRAVITY が軽いのは「最初の画面ではない」から**（ホームは**起動の代金**を
  払っていた）… 先読みは1つずつ／見えていない `VoiceStudio` は測らない／焼くのは
  1フレーム2枚／書体は落ちる前に頼む（**フレーム 58 → 115〜127**）。
- ★★★**大きさは「段の高さ」1つ**（`rowSpecOf`）／**`specOf` は 1 行も変えない**。
- ★★★**隙間と席は `transform` のずれでは書けない。レイアウトが持つ** ―― 同じ id の
  ピルが2周ぶん居るので別々の値を書けない（2周目が **152px 飛ぶ**。両側 ±w/2 も
  片側 +w も同じ理由で直らない）。
- ★★★**帯ぜんたいを `lead`＋隙間の半分だけ左へ戻し、挿し込みで「送り（px）」へ畳み
  替える**／**境目は開く前の位置で測る**／**挿し口が移るときは受け皿 `shut` へ**／
  **白抜き ⇄ 塗りは `inkMix` の「量」で渡る**。
- ★★★**並びは「留め金」（`AppState.bandPins`）**。`tasks` は触らない ―― ① GRAVITY と
  ALIGN まで動く ② 声の候補は持ち主が別で動かせない。★**挿し口は index ではなく
  「左どなりの id」**。★**日付・曜日は黒いピルに白い文字**。

### 第89〜112巡の要点（全文は `docs/project_knowledge.md` §3-h）

- ★★★**落とし直しの真因は `clearOverlap` が据え置きの山にも掛かっていたこと**
  （→ `Piece.fresh` だけ）／**めり込みの真因は「湧いた瞬間から重なっている」**
  （→ 投入の直前に `clearOverlap`。372件 → 10件）／**眠りは `GravityTab` と同じ**／
  **光線は「正規化した空間」で飛ばす**／**幽霊の寿命は3重**。
- ★★★**canvas の `ctx.font` は読み込みを頼まない**し `document.fonts.ready` は**頼んで
  いないものを待たない**（→`ensureWordFont`）／**実機のバグ ＝ `mask-size` が効かず
  SVG が 1:1** → `clip-path`。

## 次の一手

1. ★★★**実機で見てもらう**（第124巡）… ①**タスクの大きなピル**（2段＝日付の板の
   高さ・幅 1.43倍。**2.0倍は器に入らない**と伝える）／②**4つの札の形のカーブ**が
   ピルの角と揃って見えるか（凹凸が減っている）／③**提案の大きさ**（`OFFER_D` は
   カーブから逆算した数なので、大きくしたいなら凹凸の数を変える）／④**帯を指で
   左右に送る手ざわり**（`PAN_SLOP` 10）／⑤**ニュースのタップで帯が跳ねないか**／
   ⑥**写真が無い提案の英語の版面**／⑦**ニュースが一般 8割になったか**。
2. ★**ニュースの札に本文は無い**（RSS は題と出典だけ）。本文が要るなら
   **記事を取りに行く**話になる。★**`magazine` と `ItemStatus "planned"` も死んだ欄**。

## 未解決

- ★**帯の3段目（ニュース）だけ引き下ろせない**（`pull` を渡していない。手つきは
  `NewsPill` が自分で持つ）。日付の割り当てが要るなら設計から。
- ★★★**提案の生成は止まったまま**（未読 41 枚が減らず `POOL_CAP`(40) に掛かる。
  おすすめ3件の母集団もこの池 ―― **生成が戻るまで新着は増えない**）。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も焼く予算も直していない**（山だけ）。
  ★**起動の代金は残っている**。★`SectionLabel` は 3.2 ＝ AA 割れ。

## 重要パス

★★**ファイルの地図と、踏んだ罠の全文は `CLAUDE.md`。ここは「どこを開くか」だけ。**

- ホーム … `HomeTab.tsx`／帯 `Band.tsx`＋`lib/homeBand.ts`＋`bandMotion.ts`／
  山 `components/home/`（`Pile`＋`pileWorld`＋`pilePaint`）／器 `lib/pileBox.ts`／
  引き下ろし `lib/pullDrag.ts`＋`pillGhost.ts`＋`AssignRail`＋`AssignSheet`＋`DateGrid`。
- 帯の中身 … **「今日行ける」`lib/todayPick.ts`**（夜は明日へ）／**「文章」
  `lib/bandNotes.ts`**（0 件か1件）／**ニュース `lib/newsFeed.ts`＋`app/api/news/route.ts`
  ＋`components/home/NewsCard.tsx`**（ピルと広がる札）／**おすすめ3件
  `lib/offerPick.ts`**／**カード → `Item` は `lib/keepCard.ts` の1本**。
- Explore … **詳細 `CardDetail.tsx` ＋ 変形 `lib/cardMorph.ts`**／**札の形
  `lib/cardShape.ts`**（★`cardShapeReach`・`inCardShape`・`fitBox` は一様）。
- 図形 … `lib/solid.ts`（`stackOutline`＝**ひとつの大きなピル**）／`lib/taskSize.ts`
  （`ROW_WIDE`）／
  `lib/wordPlate.ts`（板）／`lib/cassette.ts`＋`lib/reelHub.ts`。
- 語彙 … 色 `lib/constants.ts` の `PALETTE`(7行)／動き `lib/motion.ts`（★**CSS に
  任せられない曲線は `easeAt(EASE_SETTLE, t)`。写経しない**）／バネ `lib/spring.ts`／
  目盛り `lib/tokens.ts`／★規約13本 `design.md`。

### ★ここだけは忘れると必ず壊れる

- **絵は `hx`/`hy`**（落とし所にも支点を渡さない）／**姿は掛け金 `Ghost.pill`**／
  **位置は最後まで指**（`Ghost.aim` は `boolean`）／**近さは3つ**（`RAIL_NEAR` 36／
  `BAND_NEAR` 48／`BAND_CATCH` 24。遊び `RAIL_HYST` 12 ＝ **閾値より小さく**）。
- **帯の隙間と席はレイアウト（`.band-pad` の幅）が持つ**／**挿し口は `after`（左どなりの
  id）＋ 遊び `GAP_HYST`**／**狙われている段は流れを止める**／**位相は px で引き継ぐ**。
- **山**… **`clearOverlap` は `Piece.fresh` だけ**／**体も倍率も作り直さない**／**質量は
  同じ密度**／**`atan2` で並べてから `fromVertices`**／**dpr の上限は 2**／**ループは
  止める**（`wake()` を忘れない）／**帯が覆う高さは予算から引く**＋`crowdOf`（板は
  `room` と**上限の両方に**）／**塗るのは変わった図形の箱だけ**。
- **カーブは1つ** … 2段のタスク ＝ 板 ＝ 帯のピル ＝ 提案の凹凸 ＝ **`unit`**。
  ★**`OFFER_D` も `FIT_W` も `ROW_WIDE` も、どれか1つだけ動かすと崩れる。**
- **提案の図形** … **`p.r` は絵の半径ではない**（`cardShapeReach` 倍まで届く）／
  **体の光線は `OFFER_VERTS`(28)**／**写真に `crossOrigin` を付けない**／
  **傾きも回転も他の図形と同じ**（第121巡の固定は撤回）。
- **当たり判定** … 形は全部「絵と同じ」＋**重なったら「画面でいちばん上」**（末尾から
  探す。**遊びは2周目でだけ**）。★**実機は 120Hz**／**iOS の上下の帯は触らない**。

### 検証の作法

★★**WebKit は `executablePath` 無し／Chromium だけ `/opt/pw-browsers/chromium`**／
★★**種に `keeps: []` を入れない**（`migrate` が `items` を丸ごと捨てる）／★**`generatedDecks`
は `Record<号, BriefCard[]>`**／★★**山の測定は 8〜10 秒待つ**／
★★**帯を出す種には「日付の無いタスク」が要る**／★★★**写真が乗った山の canvas は
汚染されて `getImageData` で読めない** → **画面の写しを別のページで読む**か写真なしで／
★★★**帯のピルは2周ぶん DOM に居る**ので、指で触る検証は**画面に出ているほう**を選ぶ／
★★**`bandBus.rows` の添字は論理の段**（0＝提案／1＝タスク／2＝ニュース）／
★★**WebKit は続けて何回も起動すると落ちる**（1プロセス1ケース）／
★★**`next dev` は初回のコンパイルに 45 秒**（`domcontentloaded` ＋ 先に1度叩いて温める）。
