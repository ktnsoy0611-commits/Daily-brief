# いまどこにいるか（2026-09-19／ホームを作っている最中）

**ホームが動いている**（帯3段＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。★第110〜114巡に「落とし直し」を根絶して
**帯の隙間をレイアウトへ作り直し**（並びは**留め金**）、第115〜116巡に**起動の代金を
払い、大きさを「段の高さ」1つ**にし、第117〜118巡に**配色を総入れ替えして手ざわりの
4つの真因を根治**、第119〜120巡に**「おすすめ3件」と Explore の詳細の画面**、
第121〜122巡に**提案の図形と、帯の3段目（ニュース）と文章**、★★★**第123巡に六角形の
歪みを根治して、提案を回るようにし、当たり判定を「見たまま」へ直し、タスクの形を
角丸四角に、ニュースを「引くと広がるピル」にした**。
★Explore の刷新（券・改札鋏）は保留（`DEV` タブごと最後に処分）。
仕様の正 … `docs/home-spec.md`（**§6 が引き下ろしの正**）／
`docs/project_knowledge.md` §3-h／`design.md`。

## 直近完了

### ★★★第123巡 ── 六角形／回転／当たり判定／形と大きさ／ニュースの札

> 「**提案の図形も自由に回転したり動くように。やはりまだ当たり判定がおかしい。特に
> 六角形の図形が縦に引き伸ばされているのを修正して。同様に他の図形も調べて**」
> 「**Journal の図形は日付と曜日ぐらいに**」「**タスクの図形は…形だけ角丸の四角に
> （元のピルを元にした大きさとかを維持して）**」「**ニュースもピルに。引き出した時に
> 画面上で広がって角丸の四角になって展開し、詳細が見れるように**」

- **①六角形** … ★★★**真因は形の定義**（`fitBox` ではない）―― 頂点が
  `(0,0.5)…(1,0.5)` で**外接箱がちょうど 1×1** ＝ **正方形いっぱいに広げた六角形**
  （自然な比は **√3/2** ＝ **縦に 1.155 倍**伸びていた）。→ **正六角形へ**＋
  **`fitBox` を一様な倍率へ**。★**「他の図形も調べて」** … 生の外接箱（高さ÷幅）は
  四つ葉 **1.000**／六角形 **0.897**／波打つ四角 **1.000**／トゲトゲ **1.000** ＝
  **歪みは六角形だけ**（タスク・カセット・板・未読も歪める箇所は無い）。
- **②回転** … ★★★**第121巡の「提案だけ傾けない」は撤回**（`setInertia(Infinity)` と
  `respawn` の枝を削除）。犯人は回転ではなく①だった。
- **③当たり判定** … ★★★**「いちばん近い中心」を採っていたのが真因**（大きな図形の
  上に小さな図形が乗ると**中心が近い大きいほうが掴めた**）。★★★**そのうえ「板が必ず
  勝つ」規則があり、根拠の「板はいちばん最後に塗られる」が事実と逆**（積む順は
  **板 → タスク → カセット → 提案 → 未読**＝板はいちばん下）。→ **塗る順と拾う順を
  同じ配列から引き、末尾から探す**＋**遊びは2周目でだけ**。★実測 … 絵と判定の食い違いは
  提案4形とも **0/14641**、タスクは **4/4/0**（縁の丸め）。
- **④タスクの形** … **角丸四角**へ。★★★**変えたのは縁の式（`rectEdge`）だけ** ――
  段の数も箱の寸法も `STACK_INK` も 1px も動かしていない。角丸は **`RECT_R`(0.32) ×
  段の高さ**（段の高さは px で全図形共通なので**角が px で揃う**）。★`waist` は
  生きているが **`n === 1` の近道は削除**（1段でもピルとは別の形）。★点の刻みは
  不変（角に点が集まる）。★GRAVITY と DRIFT も同じ関数なので同時に変わった。
- **⑤カセット** … **高さ＝日付の板の高さ**（px）。`CASSETTE_ROWS` は削除。★`unit` の
  倍数では板と揃わない（予算から出るので比が毎回ずれる）＝**px の側へ移すしかない**。
- **⑥ニュース**（新設 `components/home/NewsCard.tsx`）… **輪郭のピル**＋**下へ引くと
  角丸の四角へ広がって詳細**（タップでも開く）。★★**形の補間は要らない**（両端が
  どちらも角丸の四角）。★★★**指が動かす `t` に曲線を掛けない**（`run()` が既に通して
  いる。二重だと **6px で 45% 開く**）。★★★**札の高さは中身の高さ**（決め打ちだと
  下半分が空っぽ。**開いた幅で組んだ写しを測る**）。
- **検証** … 引き 6/20/40/64px → 札 165×48／212×72／278×109／**358×154**（開き切り・
  角丸 28px）・幕を押すと閉じる（両ブラウザ）／機械チェック13本 0 件・`tsc`・
  `eslint`・`build`・`pageerror` **0**。

### ★★★第122巡 ── 帯3段（ニュース）／文章1件／写真・板・提案

- **①写真が付かない** … ★★★**真因は `crossOrigin="anonymous"`** ―― 提案の写真は
  他所の **OGP 画像**で、多くは CORS ヘッダを返さないので**読み込みそのものが
  失敗**していた。付ける理由は「canvas を読み返すとき」だけで**このアプリには 0 件**。
- **②崩れたピル** … `onError` が**丸だけ**消していたので `photo` は真のまま＝左の余白が
  6px のままで**題が丸い角に食い込んでいた** → **URL を憶えて `photo` ごと落とす**。
- **③文章は 0 件か 1 件**（＝ピル6個に文章1つ）。★**主語＋助詞で言い切る**／
  ★★**題は文に入れない**。**④板 0.80倍**（`WORD_W` 0.67 ＋ `PILE_WORD_MAX` 58。
  **両方を同じ比で**）＋当たり判定は**丸い角の矩形**へ。
- **⑤提案を大きく** … `OFFER_D` **3.2 → 4.4**。★★★**代償はタスク**（`unit` は
  「予算 ÷ 全員の面積」）。実測 … **外接箱 112 → 131px（+17%）／タスクの塗り面積
  0.71倍／合計 −2.7%／帯からの余裕 46 → 131px**。
- **⑥⑦ばねは役の割り当てだけ**（係数は5組のまま）… **席の畳み `K_OPEN` →
  `K_SETTLE`**（**117 → 467ms**。★素の幅へ戻すときは `K_OPEN` のまま＝向きで分ける）／
  **指へ追いつく `K_CATCH` → `K_OPEN`**（**67 → 117ms**・行き過ぎ **10.5% → 0%**）。
- **⑧ニュースの段**（`app/api/news/route.ts` ＋ `lib/newsFeed.ts`）… **Google ニュースの
  RSS**。★**サーバーで取る**（CORS ヘッダが無い）／30分キャッシュ／**好みの語で検索
  し、0 件なら主要ニュースへ**／★★★**`AppState` に入れない**。

### ★★★第121巡 ── 提案の図形／夜のおすすめ／帯の文章

- **①歪み** … **第123巡に真因が別だったと判明**（当時は「形は正しく 150° 回っていた」
  と見立てて提案だけ正立で固定したが、犯人は**六角形の定義**）。
- **②③当たり判定と塗り直す箱** … ★★★**`p.r` は「絵の半径」ではない**（遠い点は
  `p.r × cardShapeReach`）。円で見ていたので**出っ張りを押しても掴めず、へこみで
  掴めた**（誤り 368・236/1440 → **0/1440**）。**④めり込み** … `PHYS_VERTS`(12) では
  札の形の谷ばかり拾う → **提案だけ `OFFER_VERTS`(28)**（タスクは 12 のまま）。
- **⑤夜に帯が空** … 仕様どおり（嘘を出さない）→ **今日が1件も無いときだけ明日へ**。
  ★★**今日の分があるうちは混ぜない**／**そのことは帯の文章が言う**。
- **⑥帯の文章**（`lib/bandNotes.ts`）… **面も縁も持たない地の上の字**／**上限の数に
  入れない**／**`homeBand` から呼ばない**（輪になる。`HomeTab` が渡す）。

### ★★★第120巡 ── 山のスケール／日付の板／近さの判定／Explore の遷移

- **①スケール** … **第118巡の見立てが逆だった**（件数が増えるほど山は低い）→
  `CROWD_N0` 18・`CROWD_MIN` 0.94。**②日付だけ変わらない** … 字は `min(幅, 上限)` で
  **上限が効いていた** → **上限にも `crowd`**（第122巡も同じ罠）。**③日付も掴める**／
  **④判定を3つに割った**（`RAIL_NEAR` 36／`BAND_NEAR` 48／`BAND_CATCH` 24／
  遊び `RAIL_HYST` 12 ＝ **閾値より小さく**）。
- **⑤Explore の遷移**（全画面は**撤回**。写真は 1000px 級で実機では粗が出る）…
  **`lib/cardMorph.ts`**（両端を「中心から等角に測った半径の列」へ直して lerp）。
  ★★★**光線が頂点をちょうど通る**ので `u` に **1e-9 の遊び**（無いと**切れ込みが
  1本走る**）／**着いたら `border-radius` の `<img>` と入れ替える**／**器に
  `opacity` を掛けない**／**閉じる1フレーム目は `useLayoutEffect`**。

### 第117〜119巡（要点。詳しくは `docs/project_knowledge.md` §3-h）

- **第119巡** … 帯の上の段は**ストックの「今行ける」だけ**、**未読の提案は山へ**
  （`lib/offerPick.ts`。★**AI を使わない**）。★★★**`createPortal` で `body` 直下へ**／
  **`--nav-h` を読まない**。
- **第118巡** … ①**引き下ろす＝KEEP**（`lib/keepCard.ts`）／②`stepGhost` の
  `if (g.aim)` が**絵の座標を奪っていた** → 削除（**第109巡の確定を本人が撤回**）／
  ③挿し口 `at` に**遊び `GAP_HYST`**／④落とし所は**絵の中心 `hx`/`hy`**。
- **第117巡** … 配色の総入れ替え。**タスク＝無彩色の塗り ＋ 黒の字**／**ドメイン4は
  色と形の組**／★★★**`lib/appAccent.ts` を削除**。

### 第113〜116巡 ── 起動の代金／大きさ／帯の隙間

- ★★★**GRAVITY が軽いのは「最初の画面ではない」から**（ホームは**起動の代金**を
  払っていた）… 先読みは1つずつ／見えていない `VoiceStudio` は測らない／焼くのは
  1フレーム2枚／書体は落ちる前に頼む（**フレーム 58 → 115〜127**）。
- ★★★**大きさは「段の高さ」1つ**（`rowSpecOf`）／**`specOf` は 1 行も変えない**。
- ★★★**隙間と席は `transform` のずれでは書けない。レイアウトが持つ** ―― 同じ id の
  ピルが2周ぶん居るので別々の値を書けない（2周目が **152px 飛ぶ**。両側 ±w/2 も
  片側 +w も同じ理由で直らない）。
- ★★★**帯ぜんたいを `lead`＋隙間の半分だけ左へ戻し、挿し込みで「送り（px）」へ畳み
  替える**／**境目は開く前の位置で測る**／**挿し口が移るときは受け皿 `shut` へ**／
  **白抜き ⇄ 塗りは `inkMix` の「量」で渡る**。
- ★★★**並びは「留め金」（`AppState.bandPins`）**。`tasks` は触らない ―― ① GRAVITY と
  ALIGN まで動く ② 声の候補は持ち主が別で動かせない。★**挿し口は index ではなく
  「左どなりの id」**。★**日付・曜日は黒いピルに白い文字**。

### 第89〜112巡の要点（全文は `docs/project_knowledge.md` §3-h）

- ★★★**落とし直しの真因は `clearOverlap` が据え置きの山にも掛かっていたこと**
  （→ `Piece.fresh` だけ）／**めり込みの真因は「湧いた瞬間から重なっている」**
  （→ 投入の直前に `clearOverlap`。372件 → 10件）／**眠りは `GravityTab` と同じ**／
  **光線は「正規化した空間」で飛ばす**／**幽霊の寿命は3重**。
- ★★★**canvas の `ctx.font` は読み込みを頼まない**し `document.fonts.ready` は**頼んで
  いないものを待たない**（→`ensureWordFont`）／**実機のバグ ＝ `mask-size` が効かず
  SVG が 1:1** → `clip-path`。

## 次の一手

1. ★★★**実機で見てもらう**（第123巡）… ①**六角形の比**（他の3形も）／
   ②**提案が自由に回ること**／③**当たり判定が「見たまま」になったか**／
   ④**タスクの角丸四角**（角の丸みは `RECT_R`）／⑤**カセットの大きさ**／
   ⑥**ニュースのピルを引くと広がるか**（速さ・高さ・閉じ方）。
2. ★**ニュースの札に本文は無い**（RSS は題と出典だけ）。本文が要るなら
   **記事を取りに行く**話になる。★**`magazine` と `ItemStatus "planned"` も死んだ欄**。

## 未解決

- ★★★**提案の生成は止まったまま**（未読 41 枚が減らず `POOL_CAP`(40) に掛かる。
  おすすめ3件の母集団もこの池 ―― **生成が戻るまで新着は増えない**）。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も焼く予算も直していない**（山だけ）。
  ★**起動の代金は残っている**。★`SectionLabel` は 3.2 ＝ AA 割れ。

## 重要パス

★★**ファイルの地図と、踏んだ罠の全文は `CLAUDE.md`。ここは「どこを開くか」だけ。**

- ホーム … `HomeTab.tsx`／帯 `Band.tsx`＋`lib/homeBand.ts`＋`bandMotion.ts`／
  山 `components/home/`（`Pile`＋`pileWorld`＋`pilePaint`）／器 `lib/pileBox.ts`／
  引き下ろし `lib/pullDrag.ts`＋`pillGhost.ts`＋`AssignRail`＋`AssignSheet`＋`DateGrid`。
- 帯の中身 … **「今日行ける」`lib/todayPick.ts`**（夜は明日へ）／**「文章」
  `lib/bandNotes.ts`**（0 件か1件）／**ニュース `lib/newsFeed.ts`＋`app/api/news/route.ts`
  ＋`components/home/NewsCard.tsx`**（ピルと広がる札）／**おすすめ3件
  `lib/offerPick.ts`**／**カード → `Item` は `lib/keepCard.ts` の1本**。
- Explore … **詳細 `CardDetail.tsx` ＋ 変形 `lib/cardMorph.ts`**／**札の形
  `lib/cardShape.ts`**（★`cardShapeReach`・`inCardShape`・`fitBox` は一様）。
- 図形 … `lib/solid.ts`（`stackOutline`＝**角丸四角**・`RECT_R`）／`lib/taskSize.ts`／
  `lib/wordPlate.ts`（板）／`lib/cassette.ts`＋`lib/reelHub.ts`。
- 語彙 … 色 `lib/constants.ts` の `PALETTE`(7行)／動き `lib/motion.ts`（★**CSS に
  任せられない曲線は `easeAt(EASE_SETTLE, t)`。写経しない**）／バネ `lib/spring.ts`／
  目盛り `lib/tokens.ts`／★規約13本 `design.md`。

### ★ここだけは忘れると必ず壊れる

- **絵は `hx`/`hy`**（落とし所にも支点を渡さない）／**姿は掛け金 `Ghost.pill`**／
  **位置は最後まで指**（`Ghost.aim` は `boolean`）／**近さは3つ**（`RAIL_NEAR` 36／
  `BAND_NEAR` 48／`BAND_CATCH` 24。遊び `RAIL_HYST` 12 ＝ **閾値より小さく**）。
- **帯の隙間と席はレイアウト（`.band-pad` の幅）が持つ**／**挿し口は `after`（左どなりの
  id）＋ 遊び `GAP_HYST`**／**狙われている段は流れを止める**／**位相は px で引き継ぐ**。
- **山**… **`clearOverlap` は `Piece.fresh` だけ**／**体も倍率も作り直さない**／**質量は
  同じ密度**／**`atan2` で並べてから `fromVertices`**／**dpr の上限は 2**／**ループは
  止める**（`wake()` を忘れない）／**帯が覆う高さは予算から引く**＋`crowdOf`（板は
  `room` と**上限の両方に**）／**塗るのは変わった図形の箱だけ**。
- **提案の図形** … **`p.r` は絵の半径ではない**（`cardShapeReach` 倍まで届く）／
  **体の光線は `OFFER_VERTS`(28)**／**写真に `crossOrigin` を付けない**／
  **傾きも回転も他の図形と同じ**（第121巡の固定は撤回）。
- **当たり判定** … 形は全部「絵と同じ」＋**重なったら「画面でいちばん上」**（末尾から
  探す。**遊びは2周目でだけ**）。★**実機は 120Hz**／**iOS の上下の帯は触らない**。

### 検証の作法

★★**WebKit は `executablePath` 無し／Chromium だけ `/opt/pw-browsers/chromium`**／
★★**種に `keeps: []` を入れない**（`migrate` が `items` を丸ごと捨てる）／★**`generatedDecks`
は `Record<号, BriefCard[]>`**／★★**山の測定は 8〜10 秒待つ**／
★★**帯を出す種には「日付の無いタスク」が要る**／★★★**写真が乗った山の canvas は
汚染されて `getImageData` で読めない** → **画面の写しを別のページで読む**か写真なしで／
★★★**帯のピルは2周ぶん DOM に居る**ので、指で触る検証は**画面に出ているほう**を選ぶ／
★★**`bandBus.rows` の添字は論理の段**（0＝提案／1＝タスク／2＝ニュース）／
★★**WebKit は続けて何回も起動すると落ちる**（1プロセス1ケース）／
★★**`next dev` は初回のコンパイルに 45 秒**（`domcontentloaded` ＋ 先に1度叩いて温める）。
