# いまどこにいるか（第66巡・2026-08-26／UI 規約を作り、目盛りへ移行し、縦のラインを揃えた）

3アプリ（JOURNAL / TASK / EXPLORE）は動いている。
第65巡は実機確認の4件（うち2件は**私の作り込みが行き過ぎた／足りなかった**もの）。
第66巡は UI 規約 `design.md` の策定と、その目盛りへのコード全体の移行。

---

## 直近完了（第66巡）

### ★★画面の縦のラインを1本にした（第1段・見た目が変わる）
- **マストヘッドが本文より 4px 内側だった**（全タブ・全画面）。AppShell が 16px を
  持っているのに `Masthead` がさらに `SPACE.xs`(4) を足していた。→ 0 にして
  題字を柵へ（実測 20→16、歯車 370→374）。
- **GOALS のバインダーが柵から 18px 内側**。「16 + 8(main) + 9.8(88% を中央寄せ)」の
  3段重ね。→ main の左右を外し `justifyItems:stretch` / 幅 100%。左端 16 / 右端 374。
  幅が変わって題が1文字溢れたので `wordBreak:"auto-phrase"`（和文は**文節で折る**。
  `textWrap:"balance"` は行長を揃えるので語中で折れて逆効果）。
- **カード内オーバーレイの端からの距離**が 8/10/11/12/14/16/18/20 の8種類 →
  `SPACE.md`(12)。★判子だけ `SPACE.lg`(16)（12°傾いていて外接矩形が広がるため、
  同じ 12 だと目には「他より角に寄っている」と映る）。
- `HOLE_CLEAR = 33`（生の数字）→ `HOLE_X + HOLE_D + SPACE.md`(34) の式へ。

### ★★見た目を変えずに片づけた（第2段）
- `fontSize` があって `fontWeight` が無い **93件**に `WEIGHT.text` を明示。
  第66巡に「継承していると壊れる」として残していたが、**継承経路は Button の
  children だけで、そこに fontSize を渡す呼び出しは 0件**と確認できたので実施。
- `SPACE.hair`(2) を器の左右余白に使っていた 12件を 0 へ（**役の誤用**。
  トークンは正しく引かれているので検査 #5 は通ってしまう）。
- **globals.css の色と余白の二重持ちを断った**。`app/layout.tsx` が
  `lib/constants.ts` から `:root` の CSS 変数を注入し、CSS は変数だけを読む。

### ★検査を 9本 → 13本、`design.md` に §6「目盛りの外」を新設（第3段）
#10 位置の生の数値 / #11 `fontSize` と `fontWeight` の対（`tools/check-weight.mjs`）/
#12 `globals.css` / #13 `hair` の役の誤用。

---


### ★★目盛りへ移行した（余白384・書体278・色55＝計717件）
`design.md` §1〜§3 を実装へ。**機械チェック #1〜#7 がすべて 0**。
- `lib/tokens.ts` に `LEAD`(3段)/`TRACK`(4段)/`WEIGHT`(3段)。`SWISS_MD` は廃止。
- `lib/constants.ts` に `SECOND`/`WHITE`/`VOID`/`TAB_ICON_OFF`/`STUDIO`/`BINDER_COLORS`。
- ★**一括置換には台帳を添えた** … 1件ごとに解決後の値を記録し、**元と違う行が
  事前に宣言した「意図した差」以外に無いこと**を機械で確認してから書き込む。
- ★検査の穴を2つ塞いだ … #5 が**三項やテンプレの陰**を見ていなかった(12件見落とし)／
  #6 が**行ごと除外**していた(#4 と同じ誤り)。**除外を行ごとにしない。**
- ★目盛りの外は**その行に理由を書く**（`// ★目盛りの外（…）`）。印だけで除外する。
- 検証 … 移行前後で全要素の computed style を突き合わせ、差は**字間126・太さ45・
  行間18**とその幾何的帰結だけ。**色と余白の差は 0**。

### ★UI 規約 `design.md`（405行 → 119行）
「チェックが有る性質だけが揃い、無い性質は例外なく劣化していた」という実測から、
規約と grep を**必ず対**で持つ形にした。初版は7割が調査結果・移行表・**他ファイルとの
重複**（`SPACE` の表は4か所目）だったので、表と grep だけに絞った。
根拠は `docs/archive/shell-redesign-2026-08.md` §65・§66。`CLAUDE.md` の章も 69→30行。

---

## 直近完了（第65巡）

### ★★UI 規約 `design.md` を作り、毎回読む量を減らした
全数調査で「**チェックが有る性質だけが揃い、無い性質は例外なく劣化していた**」と判明
（`fontSize` 生の数字 0.4% ⇔ `letterSpacing` 18種・余白のトークン化 2.6%）。
規約と grep を**必ず対**で持つ形にし、機械チェックを4本→**9本**へ。
★初版は405行あり、その7割が調査結果・移行表・**他ファイルとの重複**だった
（`SPACE` の表は4か所目のコピー＝design.md 自身の規約に違反）。**113行**（表と grep だけ）
へ絞り、根拠は `docs/archive/shell-redesign-2026-08.md` §65 へ、移行表は下の「次の一手」へ、
寸法の段は `docs/project_knowledge.md` §3 へ一本化。`CLAUDE.md` の章も 69→30行。

### ★★紙を「写真そのもの」に戻した
第64巡は写真を使ったつもりで**3つ手を加えていた**（色を捨てる／粒を2倍に増幅／
ハイパス）。測ると写真の低周波は std 1.7〜2.6 しか無く、**ハイパスはほとんど何も
取り除いていない**のに、そのために色と諧調を捨てていた。増幅のせいで圧縮も効かず
121KB。→ **等倍で切って書き出すだけ**にした（768px角・**83KB**・粒 std 11.2＝本物）。
乗せるのは**凹凸だけ**（無彩色。ユーザー確定）。強さは写真のまま＝第64巡の約半分。
★**縮小しないこと**（5.4倍に縮めたら std が 11→6.9 に落ちた）。軽くするなら品質を下げる。

### ★★★ALIGN を作り直した（2回目・1回目でスクロールを壊したので直した）

**1回目でやってしまったこと** … 「上下の空きを釣り合わせる」つもりで、円弧の中心の
高さ `mid` を**スクロール位置の関数**にした。指を動かす → 原点が動く → **場ぜんぶが
動く** → その動く的をバネが追いかける、という輪で**スクロールが壊れた**。
★教訓: **レイアウトの原点を、レイアウトの中身から決めてはいけない。**

2回目で入れたもの:

- **道を「縦はまっすぐ、横だけほんのり弓なり」に**（本物の円をやめた）。円だと
  ①端で縦が詰まって行が重なる ②遠い行が横へ逃げる、が必ず起きる。
  縦を道のりそのものにすると間隔が常に一定で、**指と 1:1** が素直に出る。
  横の膨らみは `ARC_SWING`(32) で**頭打ち**（二次関数は外で暴れるので必ず止める）。
- **寸法は互いに縛り合っている** … `PITCH_TIGHT`(88) は**行の高さ(≒50px)より大きく**、
  かつ**図形の高さ×0.75 より大きく**なければ重なる。1回目は 176/64 で派手に重なった。
  第64巡までの 44 も実は重なっていたが、**霧が隠していた**だけ。
- **図形を小さく左へ、文字を右へ** … `ALIGN_MAX_W` 176→**116**。
  文字の幅 194 → **226px**（1回目は 166px しか無かった）。
- **焦点↔隣を広げた** 81 → **116px**（ユーザー指定。1回目は詰めすぎだった）。
- **横のラインを揃えた** … 残り日数とタグを**同じ高さの箱**(24px)に入れて上下中央へ。
  題と帯のすき間は 2px。題と残り日数の左端は**インクで一致**（実測 0px）。
- **指に 1:1** … バネの硬さを距離で落とす（焦点 0.34 → 遠く 0.016）。第64巡までは
  全部が 0.016 で、**焦点の行ですら指の3割しか付いてこなかった**。
  実測: 120px 払って**116px＝ちょうど1行**／払っている最中も 112/120px 付いてくる。

### ★★DRIFT の掴みを直した（起動直後に掴めない／変な挙動）
原因は**1つ**。DRIFT は無重力で、表に出るたび全部に初速が入り**2〜3秒漂う** ―
ユーザーが最初に触るのがちょうどその間で、**掴む相手が指の下から逃げていた**。
→ **`onDown` で当たった図形を止める**（速度・角速度を 0）。追わなくてよくなるので
「8px で掴みが捨てられる」「上へ払ったと解釈されて GRAVITY へ飛ぶ」が根元から消える。
あわせて `enterHold` で `ox/oy` を取り直し（すっ飛び）、角速度を 0（回り続ける）、
`onDown` に門番。★**DRIFT は第60・61・63・65巡と4回続けて壊れた。毎回
「DRIFT だけが GRAVITY と違うことをしていた」。**

### ★口／ゴミ箱の当たり判定
`scale(0.18)→scale(1)` の**変形中の箱**を測っていたので、掴み始めの 0.7 秒は
的が最大 1/5.5 の大きさでずれていた。→ `offsetLeft/Width` から**素の矩形**を出す。

## 次の一手・未解決

### ★TIMELINE の「自由」のワープ — 解決（第67巡）
横スクロールは `syncLanes` が**物体そのものを動かして**いるので、画面外の列の
物体は物理座標で `0..w` の**外**に居る。閉じるときにそこへ側壁を張り直すと、
matter が「壁にめり込んでいる」と解いて**画面の中へ押し戻す**。
→ 壁を張る前に片づける（「自由」は消す／タスクは画面の下へ移していつもの道へ）。
★前回「再現しない」と報告したのは、**横にパンした状態で閉じていなかった**ため。
**再現手順に「何をした状態か」が要る**という教訓。

- ★**第1段は見た目が変わる**（題字とバインダーの位置、隅のバッジ、キーボード時の
  帯の下 6→8px）。実機で確認すること。第2段・第3段は computed style の
  突き合わせで**差 0** を確認済み。

- **実機（iPhone / PWA）で確認**。★ホーム画面から追加し直すこと。
- **`JINA_API_KEY` は触らなくてよい**（第64巡に鍵なし既定へ。トークンを消費しない）。
  次の夜間実行のあと、設定タブ「ブリーフ生成の状況」に
  「取得成功10・鍵なし」と出れば EXPLORE も復旧している。0 なら理由が出る。
- 開発環境は外向き通信がすべてプロキシで 403 になるため、**実サイトの取得は試せていない**。

## 重要パス

- ★**UI の規約と機械チェック13本** … `design.md`（作業の前後に走らせる）
  ／ 目盛りの定義 … `lib/tokens.ts`（`SPACE`/`TYPE`/`LEAD`/`TRACK`/`WEIGHT`/`RADIUS`）
  ／ 色 … `lib/constants.ts`（グレーは `INK`/`CHARCOAL`/`SECOND`/`MUTED` の4段）

- タスクの本体 … `components/tabs/GravityTab.tsx`
  （`arcGeom` / `offField` / `layoutAlign` / `flow` / `streamAt` / `homeAt`）
- DRIFT … `components/tabs/DriftTab.tsx`（`onDown` で止める / `enterHold`）
  ／ 的 … `components/tasks/DropTargets.tsx`（`restRect`）
- カメラ … `components/tasks/TaskSpace.tsx`
- 紙 … `lib/paperTexture.ts` ＋ `public/paper-kraft.webp`（作り直しは `tools/make-paper.py`）
  ／ 焼く所 … `lib/solidPaint.ts`（`SolidView = "none"` もここ）
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite` / `fetchDirect` / `sendsJinaKey` / `jinaSlot`）
  ／ `components/tabs/BriefTab.tsx`（`decidedOf` / `dueCandidate`）
  ／ 単体チェック `npx tsx tools/jina-check.mjs`
- 検証 … `scratchpad/r65.mjs`（DRIFT の掴み・ALIGN のレイアウト・**指と 1:1**・入り／閉じ）
  ★★測り方の罠3つ … ①dispatch 直後に DOM を読むと rAF 前の値が返る
  ②遠い行を測ると「連鎖」の遅れを不具合と読み違える（**焦点の行**を測る）
  ③`waitForTimeout(40)` は 60fps に対して 2.4 コマなので**測り方のせいで**ばらつく
