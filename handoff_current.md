# いまどこにいるか（2026-09-15／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。★第109巡に**落とす瞬間の重なりを解き**、
**柔らかさを「変形」と「支点まわりの振れ」に置き**、**帯の他のピルも動く**ように
した（`components/home/bandMotion.ts`）。
★★★**Explore の刷新（券・改札鋏）は保留**（`DEV` タブごと最後に処分）。
★★★**試している大きな変更は1つ** … **アプリごとのアクセント配色**
（`lib/appAccent.ts` の `ACCENT_TEST`。**`false` の1行で元へ戻る**）。
仕様の正 … `docs/home-spec.md`（**§6 が引き下ろしの正**）／
`docs/project_knowledge.md` §3-h／`design.md`（UI の規約と機械チェック13本）。

---

## 直近完了

### ★★★第109巡 ── めり込みの真因／手ざわりを Fluid へ／帯の他のピルも動かす

**① めり込んで震える（6巡目の指摘）… 真因は「湧いた瞬間から重なっている」**
★★★**GRAVITY には `i × (110+…)` の幾何のはしごがあり、HOME には無かった。**
HOME は順番を**時間**（`DROP_EVERY_MS` 60ms）で作っていたが、**60ms で落ちる距離は
2.5px**、散らばりの幅は **200px**（79倍）―― **時間差は位置を 1px も分けていない**。
★実測 … **16組が最大 82px めり込んだ状態**で world に入っていた（GRAVITY は **0組**）。
- → **出どころは変えず、`Composite.add` の直前に重なりを解く**（`clearOverlap`）。
  ★はしごへ替えると 2500px 上から降ってきて絵が変わるので替えない。
- → **板の質量も箱から出す**（全部同じ密度に）。`specOf({title})` を通していたので
  **大きさと無関係な定数**（10倍 軽い）だった。★★★**「`GravityTab` は板に `setMass`
  を呼んでいた」という第104巡の記録は誤り** ―― 呼んでいない。**第104巡の「修正」が
  密度の幅を 1.6 → 9.5倍に広げていた。**

**② 吸い付きが硬い → 柔らかさは「変形」と「振れ」に置く**（ユーザー確定）
- ★★★**変形と吸い付きを別のばねへ** … 変形 `K_SWING`（36% 行き過ぎ・1212ms）／
  吸い付き `K_CATCH` のまま。**係数は4組のまま増やしていない。**
- ★★★**姿の掛け金はばねの値で分けない**（`tTo` で分ける）―― `t >= PILL_EXIT` は
  **`K_CATCH` の1歩目が 0.274 だから**成立していただけで、**柔らかくすると
  スナップが黙って消える**罠だった。
- ★★★**振れは「絵そのものの速さ」で振る** ―― 指の速さだと**指を止めた瞬間に、
  絵がまだ飛んでいる最中でも 0 になる**（＝「硬い」の一因）。`SWING_MAX` 0.30。
- ★★★**図形そのものの伸び縮みは削除**（ユーザー「図形自体は柔らかくしなくて良い」。
  **第103巡のユーザー確定を今回の指示で上書きした**）。

**③ 戻りが逆再生 → 2段へ**（ユーザー確定。距離は**ピル1つぶん**）
`RAIL_NEAR`(72) で幽霊が生まれて**指に追従**（実測 差 1〜2px）、**`PULL_ARM`(44) で
捕まえて** `Ghost.aim`（帯の段の中心・指の x）へ `K_SETTLE` で吸い付く。

**④⑤ 帯の他のピルも動かす**（新規 `components/home/bandMotion.ts`）
★★★**前提のバグを先に潰した** … 帯は `items` の**同一性**が変わるたび `cancel()`
され、**`cancel()` は `translateX(0)` へ戻す**。`bandRows(appState)` は毎回新しい配列を
返すので、**`appState` に何か書かれるだけで先頭へ飛んでいた**。→ 引き金を**中身の
署名**へ／**位相を割合で引き継ぐ**。★★★**控えるのは専用の `snapPhase()`** ―― React は
**クリーンアップを新しい効果の本体より先に**走らせるので `build()` の中では手遅れ。

**検証（件数つき）**
- ① 落下＋着地5秒の接触の深さ … p95 **9.70 → 2.02px**／最大 **72.8 → 18.2px**／
  **8px 超 372件 → 10件**／2px 超 1240 → 225件／**密度の幅 15.56 → 2.64倍**。
- ② 変形（指を止めたまま）… 行き過ぎ **36.1%**・静止 1212ms。見える行き過ぎは
  箱の側（1段で高さ 33.8 → 30.1 → 33.8 ＝ 約11%。`waist` は 1 で頭打ちなので）。
- ② 振れ（左右3往復）… 片側 **21.6°**（0.38 のままだと 27° で「やりすぎ」）。
- ③ 戻り（8px ずつ9段階）… 44px の外は支点が指と **1〜2px**、内は着地点へ。切替 **1回**。
- ④a 慣性 … 行き過ぎ **11.5px**・静止 **496ms**／④b 追突 … **15.3px・8枚**／
  ⑤ 弾き … **±15.9px・8枚**（外向き）。
- 位相 … ピルを1枚抜いたとき **6633 → 300ms（先頭へ戻る）** が **6533 → 5777ms（続く）**。
- 回帰 … 床より下 0（4条件）／横のはみ出し 0px／横→縦で図形が残る／落ち直さない／
  幽霊の寿命（塗り 0回）／帯へ戻すと `dueDate` が消える。機械チェック13本 **0件**・
  `tsc` 0・`eslint` 0（既存の img 警告3件のみ）・`build` 成功。

### 第102〜108巡 ── 要点（詳しくは `docs/project_knowledge.md` §3-h）

- ★★★**眠りは `GravityTab` と同じ**（`enableSleeping: true`・**大域の門は全削除**）。門は
  **全体の最大値**で測るので**1つでも転がっていれば開かない**＝誰も凍らない。代わりに
  **体を world へ入れるフレームで全員を起こす**。
- ★★★**`positionIterations` は既定の 6** ―― matter は `clamp(20/iterations, 0, 1)` なので
  **6 でも 10 でも減衰は 1**。上げても柔らかくならず、**全力の押し戻しを4回余計に回して
  振動が持続する**（第106巡に 10 へ上げたのは逆効果だった）。
- ★★★**当たり判定の光線は「正規化した空間」で飛ばす**（px だと横長の形の丸い端が潰れて
  6頂点・体が絵より 10% 小さい → 16頂点）。★**`GravityTab` の「順番どおりの間引き」は
  借りない**（重心が最大 2.87px ずれる）／**`fromVertices` へ渡す前に `atan2` で並べる**
  （交差すると SAT が「当たっていない」と答えて床をすり抜ける）。
- ★★★**GRAVITY は canvas を dpr 2。ホームだけ 3 だった**（実機 1.09M 対 **2.77M**）。
  ★「2 で止めると 1.5倍」（第92巡）は**当時のバグの記録**（1.5 ＝ 3÷2）。
- ★★★**ループは止める**（`runningRef`/`wake()`）。**`dirtyRef` や `dragRef` を立てる所では
  必ず `wake()`**（眠りが速くなったら**落ち着いた山が掴めなくなった**。自分の退行）。
- ★★★**`ghostKey()` 1か所から取る。手で並べない**（第106巡に `bend` を入れ忘れて
  引き出しが1フレーム目で固まった）／**幽霊の絵は `hx`/`hy`**（`dx`/`dy` だけでは動かない）／
  **姿は掛け金 `Ghost.pill`**／**境目には遊び `RAIL_HYST`**。
- ★★★**`pullBus.ghost` の寿命は3重**（`lostpointercapture`／unmount／`window` の
  `pointerup`）。★**隠すのは `opacity: 0`**（`visibility: hidden` は捕捉が外れる原因）／
  **`navHeightPx()` を rAF から呼ばない**／**塗るのは「変わった図形の箱」だけ**／
  **`stepGhost` と物理は同じ固定の刻み**（`rAF` ごとに1歩にしない）。
- ★★**測る窓を間違えない**（第105巡の「32ms 超 0 件」は「落ち着いたあと」を測っていた）。
  ★★★**実機は 120Hz。Chromium も WebKit も 60fps なのでこの環境では再現しない。**
- ★★★**沈んだだけなら `sink`**（上から落とすと「雨」）／**指の当たり判定も
  `halfWidthAtStack`**／**セーフエリアは左右対称**（**もう振らない**）／**`setVelocity`
  は眠った体を起こさない**／**列は `overflowX: "clip"`**。

### ★★★第89〜101巡までの要点（寸法は上書き済み。教訓だけ）

- ★★★**canvas の `ctx.font` は読み込みを頼まない**し、`document.fonts.ready` は**頼んで
  いないものを待たない**（→`ensureWordFont`）。**待つなら締切**／★**中身を入れる引き金は
  「世界を作り終えた最後の1行」だけ**（途中で抜けると空）。
- ★★★**参照の形は全部「円をつないだ形」。正弦波で円に似せない**（角で √2 倍）／
  **`objectBoundingBox` は 0〜1 のパスを器へそのまま掛ける**（器は正方形。→`fitBox`）／
  **実機のバグの正体 ＝ `mask-size` が効かず SVG が 1:1 で置かれていた** → `clip-path`
  （Playwright の WebKit でも再現しない）／**切るのは写真だけ。札には掛けない**。
- ★★★**タグを全廃**（タスクの性質は「日付があるか／ないか」だけ。`isDated`）／
  **ぼやけ ＝ ResizeObserver が2つ**／**下が空く ＝ 床の二重引き**（`.bleed-x-b`）／
  **effect を2本に割る**（1本だと題を1文字直すたび山ごと落ち直す）／
  **タブバーの高さは `navHeightPx()`**（実機 132px／Chromium 81px）。

---

## 次の一手

1. ★★★**実機で見てもらう**（第109巡）…
   ★★★①**落ちてぶつかった時のめり込みと震え**（今回の主目的。**8px 超の接触が
   372件 → 10件**。残っていたら「未解決」の項へ）
   ②**変形が「物理が働いている」柔らかさか**（速すぎ／柔らかすぎは `K_SWING`）
   ③**左右に振ったとき、掴んだ所を支点に揺れるか**（強さは `SWING_MAX` 0.30）
   ④**戻すとき、44px まで指に追従して、そこから帯へ吸い付くか**
   ⑤**帯の慣性・追突・弾きがやりすぎていないか**（`bandMotion.ts` の3つの `KICK`）
   ⑥**アクセント配色を採るか捨てるか**（捨てるなら `ACCENT_TEST` を `false`）。
2. ★**`magazine` と `ItemStatus "planned"` を消す**（★**死んだ欄**。`plannedFor` が正。
   起動時の後片付けと永続化の移行に絡むので単独の巡で）。
3. **持ち物** … 右上のアイコン＋数の印（警告）。押すと印のすぐ下に小さな一覧。
4. **フォローアップの夜間生成がまだ無い**（★プロンプトは先に文面で出して承認を得る）。
   ★ピルを押したときの詳細も無い（山の図形はもう押せる ―― `Piece.nav`）。

## 未解決

- ★★★**提案の生成は止まったまま。** 15 は**表示だけ**の上限なので裏の未読 41 枚は減らず、
  夜間の生成は `POOL_CAP`(40) に掛かり続ける（`app/api/cron/build-brief/route.ts`）。
  ★**`VoiceStudio` の `ResizeObserver` がホームでも走る**（未調査）。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も直していない**（山だけ直した）。**TASK も
  120Hz では物理が2倍速**のはずだが、塗る画素が少なく眠りも効いていて症状は出ていない。
- ★★★**震えが実機で本当に消えたかは未確認**（第109巡で**めり込みは 8px 超 372→10件**
  まで落ちたが、実機は 120Hz で Chromium は 60Hz）。★残っていたら次に疑うのは
  **持ち越した山が重なったまま再投入される**こと ―― `buildPieces` は前の位置を
  丸写しするのに、`unit` が変われば寸法が変わり、しかも `releaseAt: 0` で
  **全部同じフレームに入る**（＝`clearOverlap` が効かない道が1つ残っている）。
- ★★**札の比は `BRIEF_CARD_ASPECT` が正**／**黄の札と地の比 1.27**（縁は**影**が持つ）／
  `SectionLabel` は明るい地で 3.2 ＝ AA 割れ／**札の中の隙間**。**未対応。**

---

## 重要パス

- ホーム … `HomeTab.tsx`／帯 `Band.tsx` ＋ `lib/homeBand.ts`／山 `components/home/`
  （`Pile.tsx` ＋ `pileWorld.ts` ＋ `pilePaint.ts`）／器 `lib/pileBox.ts`／`AppShell.tsx`
- ★★★**引き下ろし … `lib/pullDrag.ts`（算数と `pullBus`）／`pillGhost.ts`（写し取りと
  曲げ）／`AssignRail.tsx`／`AssignSheet.tsx`／盤は `components/DateGrid.tsx`**。
  ★★**手つきは「輪ゴム(44px・指と1:1) → ばちん → 変形」**。
  ★★★**絵は `hx`/`hy` に描かれる**（`dx`/`dy` だけ書いても動かない）／
  **姿は掛け金 `Ghost.pill`**（ばねは 0 を跨ぐので生の比較は必ず点滅する）／
  **境目には遊び `RAIL_HYST`**（1本の線だと毎フレーム反転する）。
  ★★★**戻りは2段**（第109巡）… `RAIL_NEAR`(72) で幽霊が生まれて**指に追従**し、
  `PULL_ARM`(44) で**捕まえて** `Ghost.aim` へ `K_SETTLE` で吸い付く。
  ★★★**変形と吸い付きは別のばね**（変形 `K_SWING`／吸い付き `K_CATCH`）。
  **掛け金はばねの値で分けない**（`tTo` で分ける）／**振れは絵そのものの速さで振る**／
  **図形自体の伸び縮みは第109巡に削除**（ユーザー確定）。
- ★★★**帯の物理っぽい動きは `components/home/bandMotion.ts` の1か所**（第109巡）。
  慣性・追突・弾き。**`transform` だけでレイアウトは動かさない**／`data-pill-id` で
  **2周ぶんに同じ値**／ずれは `.band-track` ではなく **`.band-shift`** が持つ
  （`transform` は流れの WAAPI が占有している）。
  ★★★**帯は `items` の同一性が変わるたび先頭へ飛んでいた** ―― 引き金は**中身の
  署名**、位相は**割合で引き継ぐ**。★**控えるのは `snapPhase()`**（React は
  クリーンアップを新しい効果の本体より先に走らせる）。
- ★★★**セーフエリアは全画面で `SPACE.lg`、左右対称**（`lib/pileBox.ts` の `PILE_INSET`。
  タブバーは右下の丸まで含む。**もう振らない**）。
- ★★★**器が変わったときの4つ** … `FLOOR_CHECK_MS`／`refitPile`／**`sink`**／`isLost`→`respawn`。
- ★★★**当たり判定の多角形は「軸の本数 × 頂点数」**（`PHYS_VERTS` 12・等角・偶数。
  **光線は正規化した空間で飛ばす**）／★★★**落とす順を時間で作るなら、投入の直前に
  重なりを解く**（`clearOverlap`。60ms は 2.5px しか稼げないのに散らばりは 200px）／
  ★★★**質量は全部同じ密度に**（箱 ÷ `unit²`。**位置ソルバは質量を見ない**）。
  ★★★**`fromVertices` へ渡す前に `atan2` で並べる** ―― 交差すると SAT が
  「当たっていない」と答えて**床をすり抜ける**。
- ★★★**山の眠りは `GravityTab` と同じ**（`enableSleeping: true`・`positionIterations` 6・
  大域の門なし）。**`dirtyRef` / `dragRef` を立てる所では必ず `wake()`。**
- ★★★**タスクの形は `stackOutline`（ピルの積み）が正**／**段の数は `rowsOf(title)`**
  （純粋な関数。絵も物理も山も `shapeRowsOf` から）／★**`waist` が「ピル ⇄ 図形」**／
  **割り付けは `layoutInRows` だけ**／**塗り／輪郭は `paintShape` と `isDated`**
  （`pilePaint.ts` にも同じ分岐。**両方直す**）。
- ★**カセットは `lib/cassette.ts`**／**芯は `lib/reelHub.ts`**／**札の形は
  `lib/cardShape.ts`**（点の列が正。割り当ては `PUNCH_BY_DOMAIN` から導く。
  `clip-path`・器は必ず正方形）。
- ★★★**書体は3つ** … `SANS`／`LATIN`／**`DISPLAY`**（Anton）。★★**canvas は
  `font-variation-settings` を受けない**（**偽コンデンスは撤回済み**）。
- ★★★**山の器は `.bleed-x-b`**／**ResizeObserver は1つだけ**／**文字の板は
  `lib/wordPlate.ts`**／**帯の「今日行ける」は `lib/todayPick.ts`**。
- ★**色は `lib/constants.ts` の `PALETTE`（6行）だけ**／役は `SCHEME` と `lib/palette.ts`／
  ★**面から導く3つ** … `bodyInkOn`・`redOn`・`inkVarsOn`。
- ★規約13本 `design.md`／目盛り `lib/tokens.ts`／動き `lib/motion.ts`／タスク `GravityTab.tsx`。
- ★★★**WebKit がこの環境で動く**（`executablePath` 無し。**Chromium だけ**
  `/opt/pw-browsers/chromium`）。★**万能ではない**（第95巡の札のバグは再現しなかった）。
  ★**CPU を遅くできるのは Chromium だけ**（`Emulation.setCPUThrottlingRate`）。
- ★★★**試験の種に `keeps: []` を入れない**（`migrate` が旧形式とみなして **`items` を
  丸ごと捨てる**）。`wishes: []` は**必要**（無いと全部捨てる）。
- 検証 … `localhost` で開く。★★**山の測定は 8 秒待つ**／★**dpr 上限は山もタスクも 2**／
  ★★**帯を出す種には「日付の無いタスク」が要る**（全部に `dueDate` を付けると帯が空に
  なり、`bandBottom()` が 0 を返して**戻しの判定にいちども入らない**）／
  ★★**帯のピルを掴む試験は「画面の中に居る」ものを選ぶ**（流れているので外に居る）。
