# いまどこにいるか（2026-09-13／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★第94〜95巡に **JOURNAL（録音画面）と
EXPLORE の BRIEF の札**も作り直した（TASK は手つかず）。
★★★**Explore の刷新（券・改札鋏）は保留**。コードは消していない（`DEV` タブごと最後に処分）。

★★★**いま試している大きな変更は1つ**（実機で見て決めてもらう段階）… **アプリごとの
アクセント配色**（`lib/appAccent.ts` の `ACCENT_TEST`。**`false` の1行で元へ戻る**）。

仕様の正 … `docs/home-spec.md`（ホーム）／`docs/project_knowledge.md` §3-h（確定したこと）
／`design.md`（UI の規約と機械チェック13本）。

---

## 直近完了

### ★★★第95巡 ── 録音画面をレコーダーに／札の実機のバグを根治し、形を参照へ

**★★★この巡でいちばん大事なこと ―― WebKit がこの環境で動くようになった。**
`npx playwright install webkit` ＋ `install-deps webkit`（`libgtk-4` などが要る）。
**実機のバグを、ここで再現して直せる。**「Chromium で 0 件＝直った」はもう言わない。
★ただし**万能ではない**（下の札のバグは Playwright の WebKit でも再現しなかった）。

**① 録音画面 ―― 円を小さくし、下の段がキーの穴になった**（ユーザー指定
「円を小さくして、全体が図形のデザインと統一して見えるように」「下のバーを、
全体の幅を変えず**左に円・右にバー**へ」「**ボタン類をバーに当てはめる**」）。

- ★★★**組み立ての順が逆になった** … 前は「円 → 本体」、いまは
  **「キーの列 → 下の段 → 本体 → 円」**。★**キーの列を動かさない**（`NAV_H` に
  追従している）。**下の段のほうが行く。**
- ★★★**腰（砂時計の締まり）を式で縛った** … `DIAL_CX` を定数で持たず
  `(DIAL_RATIO − (1 − DIAL_WAIST)) / 2` から導く。**直径だけ動かすと腰が広がる**
  という事故が原理的に起こせない（前はコメントの約束だけだった）。
  実測 … 円 507 → **390px**、腰は **117px のまま**（＝波形はそのまま）。
- ★★★**下の段（黒い円＋バー）がそのままキーの穴** ―― キーは「白い面・黒い穴・
  墨の窓」（第79巡）なので、**新しい材料を1つも足していない**。
  左の円＝REC／バーの中に PAUSE・SEND・CANCEL。ラベルは `bodyInkOn(下の段)`＝紙色。
- ★**本体は左右へはみ出したまま**（685.7px ＝ 片側 147.8px。ユーザー確定
  「画面端から縁がはみ出ている今の感じを残して欲しい」）。
- ★`lib/cassette.ts` の `bar` を **`knob`（左の円）＋ `bar`** へ割った。**総幅は 9.6 のまま。**
  タブのアイコンとホームの山も**同時に**変わる（出どころが1つだから）。

**② BRIEF の札 ―― 実機のバグを根治し、形を参照の語彙へ**
- ★★★**実機のバグの正体 ＝ `mask-size: 100% 100%` が効いていなかった**。
  切り抜かれた領域は**器の高さと同じ辺の正方形**で、器の幅より **254 デバイス画素
  狭かった** ＝ SVG が固有の 1:1 の比のまま置かれていた。
  ★★**プロパティの書き漏らしではない**（`Webkit` 付きと対で8本書いてあった）。
  ★★★**Chromium でも Playwright の WebKit でも再現しない。実機の WebKit だけ。**
- ★★★**だから仕組みごと替えた** … `clip-path` ＋ `clipPathUnits="objectBoundingBox"`。
  パスを **0〜1 の器**で書くと器の比へそのまま伸びるので、**画像としての寸法計算・
  `preserveAspectRatio`・データURIの解析・マスクの合成が全部いなくなる**。
  ★★★**id は札ごとに変える**（`useId()`）―― `AppShell` はタブを全部載せたまま
  横へ送るので、BRIEF の札と `DEV` の見本帳が**同時に存在する**。
- ★★★**形を参照（Google Labs）の語彙へ** … ユーザー指摘「形が写真＝参照と違うもの
  ばかり」。**ドームと台形は参照に1枚も無かった**ので捨てた。
  弧＝バショ **四つ葉** ／ 斜め＝タイケン **六角形** ／ 直角＝ジョウホウ
  **波打つ四角** ／ 切れ込み＝モノ **トゲトゲ**。
  ★**割り当ては今までどおり `PUNCH_BY_DOMAIN` から導く**（約束を2つ持たない）。
  ★トゲトゲはホームの山の EXPLORE のバッジ（`zigVerts`）と同じ性格。

**検証（件数）** ―― ★**すべて WebKit（＝実機と同じ系列）で取った。**
- 録音画面 … 円 390px の墨 ×2／本体 685.7×500 の青／下の段は黒い円 78 ＋ バー 243.4／
  キー4つとも下の段の中（はみ出し 0）／ラベル4つとも紙色／**腰 117px（移行前と同じ）**／
  円は掴んで回る・**バーの上（キーの外）を引いても回らない**。
- 札 … 4枚とも正しい形／**写真が器を埋めている**（308×275 ＝ 器と完全一致）／
  綴じ穴 0／出典の円は 3枚に有り・1枚に無し／左右 16/16／`clipPath` の id 16個で重複 0。
- ホームの山 … 青い箱の中の墨の塊が**4つ**（リール2・円1・バー1）。
- 機械チェック13本 **0 件**／`tsc` 0 ／`eslint` 0 ／`next build` 成功。

### ★★★第94巡 ── JOURNAL をカセットへ／BRIEF の札を「形が分類を言う」札へ

★**多くは第95巡に上書きされた**（録音画面の組み立て・札の切り抜きの仕組み・
4つの形）。残る要点だけ:

- ★★★**録音画面はもともとカセットだった** … アイコンの `リール半径 ÷ 中心間 =
  2.9/7.2 = 0.403`、録音画面の円は `0.65w/1.60w = 0.406`。**実測でほぼ一致**。
- **新設 `lib/cassette.ts`** … 同じ形を**3つの技術**が描く（タブの SVG／山の canvas／
  録音画面の DOM）。★**タブバーの中だけは1色**（地が選択状態で変わる）。
- ★★★**形の約束は `lib/ticket.ts` の `PUNCH_BY_DOMAIN` が持っていた**。
  `lib/cardShape.ts` は**それを写すだけ**。約束を2つ持たない。
- ★★★**切るのは写真だけ。札には掛けない**（掛けると `box-shadow` が消え、
  黄とクリームの比 1.27 では縁が1本も出ない）。
- BRIEF から**綴じ穴を外した**（育成カードには残る）。**ジャンルの文字も置かない**。
  **写真が余りを全部取る**。操作は下の1列に集め、**高さを `LINK_D` で固定**。
- ホームの山の JOURNAL は `kind: "cassette"`（`Bodies.rectangle`）。
  ★**当たり判定の「円は半径で見る」枝から外すこと。**

### ★★★第93巡 ── タグを全廃し「日付あり＝塗り／なし＝輪郭」へ

- ★★★**タグを全面廃止**（`lib/taskTags.ts`・`lib/tagPattern.ts` をファイルごと削除）。
  分類の道具が多すぎた ―― 残っていた差は柄と書体だけ＝**意味を持たない2択**。
- ★★★**タスクの性質は「日付があるか／ないか」の1軸だけ**（**日付あり＝塗り／
  日付なし＝輪郭**）。合図は `isDated(spec)`。
  ★★**`paintKey` に日付の有無と地の色を入れること**（`sides` は数しか見ていない）。
- ★★**書体を1つに固定**（`SHAPE_FACE`）。**書体を分類の道具にしない。**
- ★上の段（EXPLORE の提案）は**塗りのまま** ―― 黄は地との比 1.27 で線にすると消える。

### ★★★第92巡 ── ホームの山を土台から組み直した

- ★★★**ぼやけ ＝ 同じ器に ResizeObserver が2つ**（先に走るほうがもう一方を黙らせる）。
- ★★★**下が空く ＝ 床がタブバーのぶん二重引き** → **`.bleed-x-b`**。
- ★★★**「少し変えるたび不安定」＝ 中身が1文字変わると世界ごと作り直し**
  → **effect を2本に割った**（世界は1度だけ／中身は図形だけ差し替え）。
- ★★**眠りを許すのは時間ではなく「みんな止まったら」**／**紙の目（`paperize`）を全廃**。

### ★★★第89〜91巡までの要点

- **`lib/wordPlate.ts`**（日付・曜日の板）… ★**DOM で組み直さないこと**
  （板だけが物理と別の座標系に居ると挙動が違う）。
- **山が「塔」だった正体** … 面積の予算が「横に2つ並ぶ」前提でなかったことと、
  出どころの高さが全部同じだったこと。
- ★★★**刻みは固定（1000/60）**／★★★**焼き込まれた色を信じない**／
  ★★★**タブバーの高さは `navHeightPx()` から引く**（実機 132px／Chromium 81px）。

---

## 次の一手

1. ★★★**実機で見てもらう** … ①**録音画面**（リールの大きさ・下の段とキーの収まり）
   ②**札の4つの形**が実機でも正しく切り抜かれるか（`DEV` → 「札」。**ここが直って
   いなければ、次は写真を `<svg><image preserveAspectRatio="xMidYMid slice"
   clip-path=…>` の中へ入れる** ―― DOM の `<img>` と CSS を使わず、SVG エンジン
   1つに描かせる）③**塗りと輪郭**の見分け（輪郭の字は地の上で比 2.55）
   ④**アクセント配色を採るか捨てるか**（捨てるなら `ACCENT_TEST` を `false`）。
2. **引き下ろし**（★ホームの軸。まだ無い）… 帯のピルを掴んで引き下ろすと
   **7本の柱が地面から立ち上がり**（高さ＝その日の量）、離した日がその日付になる。
   外で離すと候補（ストック）へ。**ピルは落ちながら図形へ連続的に変わる**
   （★塗り／輪郭の規則はもう通してある）。
3. **持ち物** … 右上のアイコン＋数の印（警告）。押すと印のすぐ下に小さな一覧。
4. **生成器が2つ足りない** … ①「今日行くのがおすすめ」②フォローアップの夜間生成。
   ★★**どちらもプロンプトを先に文面で出して承認を得る。**
5. ★ピルを押したときの詳細（まだ無い）。★山の図形はもう押せる（`Piece.nav`）。

## 未解決

- ★★★**提案の生成は止まったまま。** 15 は**表示だけ**の上限なので、裏の未読 41 枚は
  減らず、夜間の生成は `POOL_CAP`(40) の番に掛かり続ける。切り替える場所は
  `app/api/cron/build-brief/route.ts` の `POOL_CAP` にコメントで明記した。
- ★**帯の②（今日行くのがおすすめ）は生成器が無いので今は出ない。**
- ★★**輪郭の字は地の上で比 2.55**（本文の下限を割る。★目盛りの外・ユーザー確定）。
- ★★**録音画面の本体は左右へはみ出したまま**（片側 147.8px）。ユーザー確定
  「はみ出ている今の感じを残して欲しい」なので**仕様**。
- ★★**黄の札とクリームの地の比は 1.27**。縁は**影**が持っているので、
  **札を切り抜くと縁が消える**（切っていない）。
- ★★★**札の実機のバグは「直したはず」の段階**。Chromium と Playwright の WebKit
  では**もともと再現していなかった**ので、**直った証明は実機でしか取れない**。
- ★★**券の大きな英語が読めなくなる恐れ**（白い紙の上で下2つが 3.0 を割る）。
  **券は `DEV` タブと `/dev/explore` にしか無い**ので4タブには影響しない。
- ★★**地 `#FFFBF5` と紙 `#FAFAF9` の差が比 1.01**／`SectionLabel`（`MUTED`）は
  明るい地で 3.2 ＝ AA 割れ。**どちらも既知・未対応。**

---

## 重要パス

- ホーム … `components/tabs/HomeTab.tsx`／帯 `Band.tsx` ＋ `lib/homeBand.ts`／
  山 `components/home/Pile.tsx` ＋ `pileWorld.ts` ＋ `pilePaint.ts`／器 `lib/pileBox.ts`
  ／列の配線 `components/AppShell.tsx`／仕様 `docs/home-spec.md`
- ★★★**塗り／輪郭は `lib/solidPaint.ts` の `paintShape` と `isDated` が正**。
  ホームの山だけ角丸の四角なので `pilePaint.ts` にも同じ分岐がある（**片方を直したら両方**）。
- ★★★**カセットの寸法は `lib/cassette.ts` の1か所**（タブの SVG／山の canvas／
  録音画面の DOM）。目盛りだけ `lib/dial.ts`（`drawDial` は削除済み）。
- ★★★**札の形は `lib/cardShape.ts` ＋ `components/explore/CardShapeDefs.tsx`。
  ただし `lib/ticket.ts` の `PUNCH_BY_DOMAIN` から導いている**（対応表を2つ持たない）。
  ★**CSS のマスクを使わない。`clip-path` ＋ `objectBoundingBox`。**
  見本帳は `DevStageTab` の「札」。
- ★★★**山の器は `.bleed-x-b`**（`app/globals.css`）。左右＋**下**を列の外へ出す
  ―― 下を出さないと `floorYOf` が**タブバーの高さを二重に引く**。
- ★★★**器を見る ResizeObserver は1つだけ。** 2つ付けると先に走ったほうが
  もう一方の番人を黙らせ、**canvas の実解像度が初回のまま固定される**。
- ★★★**文字の板は `lib/wordPlate.ts` の1か所**。★**DOM で組み直さないこと。**
- ★★**アクセントのテスト** … `lib/appAccent.ts`（`ACCENT_TEST` の1行で戻る）。
  効く先は `lib/constants.ts` の `TASK_FACE`/`JOURNAL_FACE`/`STUDIO_KEY` と `lib/palette.ts`。
- ★**色そのものは `lib/constants.ts` の `PALETTE`（6行）だけ**／役との対応は
  同ファイルの `SCHEME` と `lib/palette.ts`
  ／★**面から導く3つ** … `bodyInkOn`・`redOn`・`inkVarsOn`。**表を持たず、導かせる。**
- ★UI の規約と機械チェック13本 … `design.md`／目盛り `lib/tokens.ts`／
  動き `lib/motion.ts` ＋ `app/globals.css` の `:root`
- タスク … `components/tabs/GravityTab.tsx`（★山の物理の出どころ。ホームと**対**）
  ／カメラ `components/tasks/TaskSpace.tsx`／入口の輪 `components/CreateMenu.tsx`
- 券・鋏（★保留中）… `components/explore/samples/`／`Ticket.tsx`／`lib/nipperShape.ts`
  （生成物。手で直さない）／`docs/explore-redesign.md`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite`/`fetchDirect`/`jinaSlot`）／
  単体チェック `npx tsx tools/jina-check.mjs`
- ★★★**WebKit（＝実機と同じ系列）がこの環境で動く**（第95巡に入れた）…
  `npx playwright install webkit` ＋ `npx playwright install-deps webkit`
  （`libgtk-4` などが要る。**入れないと「Executable doesn't exist」ではなく
  「Missing libraries」で落ちる**）。`import { webkit } from "playwright"` で
  `executablePath` 無しに起動する（Chromium だけは
  `executablePath: "/opt/pw-browsers/chromium"` が要る ―― 版が食い違うため）。
  ★★**万能ではない** … 第95巡の札のバグは **Playwright の WebKit でも再現しなかった**
  （実機の WebKit だけ）。**「Chromium で 0 件＝実機で直った」は今も言えない。**
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちて 500）。Playwright は
  `executablePath: "/opt/pw-browsers/chromium"`、**`localhost` で開く**。
  ★★**山の測定は 8 秒待つ**（落ちる距離が伸びた。4.5秒だとまだ空中に居る）。
  ★★**canvas の倍率は場所で違う** … ホームの山 3／GRAVITY・DRIFT・入力画面 2
  （画素で測るときは**その canvas の倍率**で割る）。
  ★`migrate` の単体チェックは state に **`wishes: []` が要る**（無いと作り直される）。
  ★`next build` は Google フォントの取得に**ときどき失敗する**（環境側。数回やり直す）。
