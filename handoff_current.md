# 現在地（2026-08-19）

現行仕様は `docs/project_knowledge.md` が正。経緯は `docs/archive/`。
このファイルは**常に200行以内**に保つ（更新手順は `CLAUDE.md` の「恒久ルール」）。

- ブランチ … `claude/brief-tab-bind-bugs-9ijd62`。
  ★**毎回 `main` へも push する**（2026-08-18にユーザー確定・確認不要）。
  `main` が Vercel の本番ブランチで、実機はここを見る。作業ブランチへ
  push しただけでは**実機に何も届かない**。

---

## いまどこにいるか

タスクアプリ（TASK）は**図形**（断面ひとつのベタ塗り）と**入力画面**の両方が
確定した。入力は TickTick 型の即時入力（チャコール地・ツールバー＋ポップオーバー）。
3 つのアプリのうち**ブリーフ（EXPLORE）は実運用中**、タスクとジャーナルは
UI が出来た段階で、Cowork の仕分けとの往復はこれから。

★★★**第13〜23巡（11巡）ぶんの再発は、第24巡で根から作り直して終わった。**
原因は正典に載っていた**成り立たない類推**だった。いまのキーボード追従は
**1行で言える**:

> **器（入力画面の全画面）の `top` と `height` を、`visualViewport` の値
> そのものにする。それだけ。**

第26巡でその上に**モーションを作り直した**。曲線と時間は
`app/globals.css` の `:root` と `lib/motion.ts` の**2か所だけ**が持つ
（§3「モーションの語彙」）。

---

## 直近で完了したこと（第31巡）★実機の実測から判明・要ユーザー確認

実機の数値（第30巡で足した「画面の数値を出す」）を初めて読めた。
**`vv`/`inner` が 797（画面は 844）で、2分置いても変わらず固定。**
第30巡の「起動直後だけ短く、そのうち自己修復する」という仮説とは合わない
（自己修復するなら時間とともに変わるはず）。

★★★決定的な手がかり: `はみ出し`（844−797＝47）が `safe`（セーフエリア
上/下=47/34）の**「上」と完全に一致**。これは
`statusBarStyle: default`（第29巡で一度試して実機で悪化し、差し戻した設定）
のときに起きる**決まった計算**（画面の高さ − ステータスバーの高さ）そのもの。
サーバー配信は `black-translucent` で正しいと `curl` で確認済み。

- **もっとも疑わしいのは、ホーム画面のアイコンが古い設定のまま焼き込まれて
  いること。** メタタグを何度か変えたが、そのたびに「消して追加し直す」を
  徹底できていなかった可能性がある。★★**次に試すこと**: ホーム画面の
  アイコンを完全に削除 → 新規に追加し直し → `vv`/`満尺` が 844 になるか確認
- 844 に戻れば解決。797 のままなら「この iOS で `black-translucent` 自体が
  もう機能していない」可能性が高く、そのときはメタタグでの解決を諦めて
  `viewport-fit=cover` とセーフエリアだけを頼りにする方針転換が要る
- `kickViewport()`（第30巡）はこの症状には効かない可能性が高い（値が
  安定しているなら「やり直す」対象がない）。害は無いので残した
- ★「画面の数値を出す」が**タブを見ているだけでも出る**ように直した
  （以前は設定画面か入力画面を開かないと出ず、「作る」の輪をタップした
  ときの下の帯が数値で確認できないと報告された）。入力画面が開くと
  `[data-app-shell]` に `filter` が掛かり、その中の `position: fixed` は
  **器を基準に測られる**（CSS 仕様）ので、その間は入力画面自身の表示に譲る
- 検証は `probe31.mjs` を新設

## その前の巡（第27〜30巡）

- **第30巡** … ①画面の下端が切れる ②入力画面のアイコンを最初の2〜3回押すと
  画面がガクッと動く、の2件が実機で報告された。「iOS が起動直後だけ画面を
  短く報告し自己修復する」という WebKit の既知の不具合を疑い
  `lib/viewportKick.ts` を実装したが、**第31巡の実測でこの仮説は否定的**
  （値が2分経っても変わらず安定していた）。上の第31巡を見ること
- **第29巡** … `apple-mobile-web-app-capable` が出ていなかった（Next は
  Android 用の名前しか出さない）ので明示。`statusBarStyle` を一度 `default`
  へ変えたが実機で悪化し `black-translucent` へ差し戻した（第31巡の実測は、
  この差し戻しが端末へちゃんと反映されているかを疑っている）。
  `theme-color` を毎フレーム自分で混ぜる形にした（1回だけ書いていたので
  そこだけ遅れてパッと切り替わっていた）
- **第27〜28巡** … 下端の帯は帆布が積み木の勝者を見ていなかった（`onGround`
  で購読）。開閉の悪化は Framer Motion の共有要素が原因（測った矩形へ合わせる
  transform を焼き付けるので動き続ける器では必ず古くなる）。**器を円で
  切り抜く**形へ作り直し依存ごと撤去。キーボードの後ろにアプリが見えていた
  のは器の外(`visualViewport`の外側)に何も塗られていなかったため — 画面
  いっぱいの板を敷いて器をその子にした。作るものを選ぶ輪（`CreateMenu.tsx`）
  を新設

- **第28巡** … キーボードの後ろにアプリが見えていた本当の原因は、器の外
  （`visualViewport` の外側・キーボードの高さぶん）に何も塗られていなかった
  こと。**画面いっぱいの板**（`[data-composer-plate]`。帯と同じ色）を敷き、
  器をその子にして、板と器を同じ円で切り抜く形に。閉じたあと一瞬止まって
  見えたのは、後ろが明るくなり始めるのが「閉じ終わってから」だったから —
  閉じ始めと同時に戻す。後ろの縮小はやめて明度だけに。
  **作るものを選ぶ輪**（`CreateMenu.tsx`）を新設 — タブバー右端の丸から
  RECORD/TASK が広がり、タスクの追加がどのアプリからでもできる
- **第27巡** … 下端の帯は帆布が積み木の勝者を見ていなかった（`onGround` で
  購読する形に）。開閉の悪化は Framer Motion の共有要素が原因（測った矩形へ
  合わせる transform を焼き付けるので、動き続ける器では必ず古くなる）。
  **器を円で切り抜く**形へ作り直し、依存ごと撤去した

## さらに前の巡（要点だけ）

- **第26巡** … モーションの語彙を曲線4本・時間5つへ統合。地色を「後勝ち」から
  「高い層が勝つ」へ
- **第25巡** … `innerHeight` がスタンドアロンで縮むのでキーボードの開閉を
  一度も判定できなかった／図形の点滅は `className` の付け外しで直す
- **第24巡** … ★★★★正典の成り立たない類推（「録音のオーバーレイと同じ
  書き方に」— あの画面には入力欄が無い）の上に11巡代用品を積んでいた。
  器を `visualViewport` の矩形そのものにして解消
---

## 次に着手すること

1. ★★★★**ホーム画面のアイコンを完全に削除 → 新規に追加し直してもらう**
   （第31巡）。そのうえで「画面の数値を出す」を ON にし、タブを見ている
   普通の状態で `vv`/`満尺` を確認。**844 になれば解決**（キャッシュが
   原因だった）。**797 のままなら、この iOS で `black-translucent` が
   もう機能していない可能性が高く**、メタタグでの解決を諦めて方針転換が要る
   （`docs/project_knowledge.md` の該当節を参照）
   - ★★**キーボードの後ろ**… 操作バー（`^ v ✓`）の裏まで帯と同じ色か
   - ★★**閉じる動き**／**作るものを選ぶ輪**（右端の丸から RECORD/TASK）
2. **Cowork のプロンプト更新**（`COWORK-ROUTINES.md`）… 候補の項目を
   `いつ`（★**日付で書かせる**。YYYY-MM-DD）/ `道具・場所` / `持ち物` /
   `タグ`（英字5つ）へ揃える。日付で書かれないと期日にならず、
   `lib/inboxImport.ts` がメモへ回す。
   ★**プロンプトの全文を提示して承認を得てから**実装する。

---

## 未解決・持ち越し

- **実機 Safari の未検証** … タスクアプリ全般、ジャーナルの円のドラッグとマイクの解放。
- ジャーナル・ウィッシュ・ストックの行先が未定／1日の終わりに3アプリを
  1枚のポスターへプレスする（§38）は未着手。
- **完成時に撤去** … `lib/taskDemo.ts`「デモを入れる」ボタン、`ProfileTab` の
  「ブリーフ生成の実験」、★「画面の数値を出す」（`debugViewport.ts`/`ViewportProbe.tsx`）。

---

## いま触っている領域のファイル

★全体のファイル地図は `CLAUDE.md`。ここは**いま手を入れている所だけ**。

```
lib/viewportKick.ts                ★iOS の起動直後だけ縮む不具合への対処(未確証)
lib/motion.ts                      ★動きの語彙(JS)。曲線・時間・＋の丸の場所
lib/ground.ts                      ★地色。優先度つきの積み木・時間の統一・onGround
components/AppBackdrop.tsx         ★帆布。勝者を塗る／fixed inset:0 で下まで届く
components/CreateMenu.tsx          ★作るものを選ぶ輪(RECORD / TASK)
app/globals.css                    ★動きの語彙(CSS)の :root ／振付／沈む合図
components/AppShell.tsx            ★輪の入口／どこからでも作るタスク／スクロールの見張り
components/tasks/TaskComposer.tsx  入力画面。★画面いっぱいの板＋器の top/height 追従
components/tasks/TaskAddButton.tsx ＋の丸(その場で足す近道。輪とは別入口)
components/tasks/Popover.tsx       ポップオーバーの器と ★`Press`(押せる面)
components/tasks/WhenSheet.tsx     日程（★キーボードを閉じるシート）
components/tasks/ViewportProbe.tsx ★開発用の数値表示（直ったら撤去）
```

## 検証（`scratchpad/`）

★**単体テストの前に必ず写しを焼き直す**（`lib/*.ts` → `*.mjs`。焼かないと
固まったままで、消えた API を試し続ける）:
`for m in types constants solid taskSize taskTags inboxImport; do npx esbuild lib/$m.ts --format=esm --outfile=$S/$m.mjs; done`
（`./types` などの相対 import に `.mjs` を足すこと）

★★**入力画面とキーボードを触ったら、まず `rect24.mjs`。**
`visualViewport` の getter を差し替えて**実機と同じ条件**を作り、
**位置だけ**を見る（代用品の値は見ない）。

```
ground26.mjs ★★地色。列・帆布・html が同じ合図で動くか／theme は折り返しか／
             ★★**画面のいちばん下 1px == html == 帆布**(閉じた状態と開いた状態)／
             入力画面の地色が奪われないか／閉じたら戻るか
motion26.mjs ★★語彙が1か所か／切り抜きの円が＋から四隅を覆い切るか／
             ★**最初から最後まで同じ1つの要素**か(受け渡しが無い)／
             閉じる動きが打ち切られていないか／時間差／押し 90ms・戻り 350ms
probe31.mjs ★数値表示がタブを見ているだけでも出るか／輪を開いても出るか／
            入力画面を開いたら重複せず1つだけか
kick30.mjs ★viewport-fit のキックが起動直後・復帰時に1フレームだけ起きて戻るか
menu28.mjs ★★作るものを選ぶ輪。円が丸から広がるか／RECORD・TASK／
           ★どのアプリからでもタスクが足せるか／外を触れば閉じるか
rect24.mjs ★★4通りの型で器・上のバー・帯・図形・シートの位置／閉じる合図／
           ★★**器の外(キーボードの裏)も帯と同じ色**(第28巡の本題)／
           ★★**キーボードが出たあとも 地の面 == 器**(第27巡の本題)／
           ★レイアウトごと縮む端末での開閉／帯の下の隙間
when25.mjs ★日程シートを開閉しても図形が飛ばないか（塗られた画素で測る）
pop21.mjs  閉じるあいだ板が1枚か・図形の登場と入れ替え（色数が落ちないか）
when20.mjs 日程シート・閉じる出足（引数=CPUの絞り）
tap.mjs    アイコンの当たり判定・沈む合図・クリアの塗り
geo4.mjs plus.mjs v15/v14/v12/v10/v8/v7/v6.mjs  第6〜18巡ぶんの回帰
smooth.mjs 落ちたフレーム / blink.mjs 文字の点滅 / bake.mjs 焼き直し
swipe seam junk text drift2.mjs
solid.test / tag.test / inbox.test.mjs  単体 90 件
```

★**画面の中央の列を掴むときは、位置が止まるのを待つこと**（第26巡）。
`|x| < 5` の決め打ちだと、横スライド(380ms)や入力画面を閉じた後の戻り(600ms)の
途中で**隣の列を掴んで**しまい、無関係な TimeoutError になる。

## ユーザー側の作業（アプリの外）

- Cowork に**毎晩のタスク「1日を仕分ける」**を設置する（例: 02:00 JST）。
- 任意: `OPENAI_API_KEY` を Vercel に登録すると文字起こしが Whisper 系になる。
