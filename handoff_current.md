# 現在地（2026-08-19）

現行仕様は `docs/project_knowledge.md` が正。経緯は `docs/archive/`。
このファイルは**常に200行以内**に保つ（更新手順は `CLAUDE.md` の「恒久ルール」）。

- ブランチ … `claude/brief-tab-bind-bugs-9ijd62`。
  ★**毎回 `main` へも push する**（2026-08-18にユーザー確定・確認不要）。
  `main` が Vercel の本番ブランチで、実機はここを見る。作業ブランチへ
  push しただけでは**実機に何も届かない**（第9〜11巡はこれで届いていなかった）。

---

## いまどこにいるか

タスクアプリ（TASK）は**図形**（断面ひとつのベタ塗り）と**入力画面**の両方が
確定した。入力は TickTick 型の即時入力（チャコール地・ツールバー＋ポップオーバー）で、
書いた情報の量がそのまま図形になる。3D・アイソメは持たない。

3 つのアプリのうち、**ブリーフ（EXPLORE）は実運用中**（夜間 Cron が毎日 2 回
デッキを生成し、実機で使えている）。**タスクとジャーナルは UI が出来た段階**で、
中身の運用（Cowork の仕分けとの往復）はこれから。

第10〜21巡は**入力画面（`TaskComposer`）だけ**を直し続けている。再発の原因は
ほぼ全部**キーボードと「見えている矩形」の関係の扱い**で、第21巡でようやく
1 本の決まりに落ち着いた:

- **持ち上げ量（`--kb`）＝ `器の offsetHeight − vv.height`。開いているあいだは凍らせる。**
- **ずれ（`--vvtop`）＝ `vv.offsetTop`。凍らせず 280ms の間合いで追い、
  そのうえで「印の rect と見えている上端の差」を測って 0 に寄せる。**
- **出入りの最中は、どちらも書かず動かさない。1 枚の板として滑らせる。**

★ここは**式で当てようとしたぶんだけ外してきた**。実機を動かせない環境で
式は確かめられない。第22巡から**測って直す**形にしてある — 式が正しければ
何もせず、間違っていれば原因が何であれ自動で戻る。

---

## 直近で完了したこと（第22巡）

- ★★★**崩れ方が逆になったのが決め手**。第20巡の版は中身が**上へ**突き抜け、
  第21巡の版は**下へ**落ちた（上のバーが画面の真ん中）。下へ落とせるのは
  第21巡で私が足した `window.scrollY` しかない。**外した。**
  「`overflow: hidden` なので通常 0」という前提が誤りで、あれは**指で送るのを
  止めるだけ**、ブラウザが自分で送るのは止められない（`app/globals.css` に
  同じ記録があった）。しかも器はその送りに引きずられていなかった
- ★★★**式で当てるのをやめ、測って直す形にした**。実機を動かせない以上、式は
  永久に確かめられない。`[data-fit]` の先頭に 0 高の印を置き、
  **`印の rect.top − vv.offsetTop` を 0 に寄せる**。式が正しければ何もしない。
  器が余計に動いていれば**原因が何であれ**戻る。逆に**根拠なく押し下げることが
  構造的にできない**。備えは3つ ①直しは最大2回 ②段は `SHIFT_MIN_STEP` と
  同じ値（小さいと追従が見送ったぶんを拾い直して段の意味が消える）
  ③上限で頭打ち（`vv.offsetTop + 160`）
- ★**日程シートの見切れも同じ原因**。シートは `[data-fit]` の中で `bottom: 0`
  なので、中身が下へずれればその分だけ下端が画面の外へ出る
- ★**出てくる最中も追従の動きを止めた**（閉じる側は第21巡で対応済み）。
  滑っている最中にキーボードが立ち上がると板の中で中身がもう一度滑る

## その前の巡（第21巡）

- ★★★**「アイコンをタップすると崩れる」は、第19巡で入れた凍結**。ずれを
  ポップオーバー中だけ凍らせていたので、iOS が付け替えたずれに取り残された。
  **凍結を全部やめた**（凍らせるのは `--kb` だけ）。跳ねない手当ては
  「追わない」ではなく「280ms の間合いで追う」
- ★★★**閉じるときのカクつきは「板の中で中身が動いていた」から**。
  `commit()` が `--kb` に 0 を書いてから `leave()` を呼んでいたので、帯（374px）と
  図形（187px）が下へ動き**ながら**板が滑っていた。**去ると決めたら書かない**
- ★**図形は「後から、きちんと登場」**（0.86倍・透明から実寸・不透明へ 260ms）。
  同時に出す案は採らない（焼きは 4× 絞りで 130ms 級で、滑りの最後に重なると
  必ずフレームが落ちる）
- ★**情報を足して図形が変わるときの動き**。canvas を2枚重ね、前の絵を残した
  まま入れ替える。どちらの層も同じ見た目の大きさになるよう scale を当てるので
  「1つの図形が大きくなっていく」ように見える。透ける瞬間が無く点滅しない
- ★**開発用「画面の数値を出す」**（設定。**直ったら撤去**）

### 第20巡

- ★★**「閉じる動きがワンテンポ遅れる」の正体は曲線**。出ていく
  `cubic-bezier(0.32,0,0.67,0)` は**制御点が2つとも y=0** で、最初の3分の1を
  0 に張り付いたまま過ごす（60ms で 2%）。`cubic-bezier(0.4,0,0.2,1)` へ
  （実測 20px 下がるまで 127ms → 52ms）。閉じる合図の確かめも 90ms×2 に。
  滑り出しのクラスは React の描き直しを待たず直に付け、`blur()` は後
- ★**日程シートの崩れ** … ずれの凍結を外した（第21巡で凍結の考え自体をやめた）

### 第19巡

- ★**アイコンの反応が悪いのは3つ重なっていた** … ①`touch-action` 未宣言で
  iOS が `pointerdown` を握る ②当たり判定が丸そのもの（32px）③`:active` が
  当たらず沈む合図が出ない。`touch-action: none` と `data-pressed`、
  当たり判定を 44 に（丸の場所は 32 のまま）。「クリア」を塗ったボタンへ
- ★★**単体テストは「化石」を試していた** … `scratchpad/*.mjs` は `lib` の写し。
  焼き直さないと固まったまま（`prism.test` の196件は存在しないモジュール相手）

### 第18巡

- ★★★**`--kb` の式を確定**。`max(0, 器の offsetHeight − vv.height)`。組版上の
  高さなので transform も `offsetTop` の解釈もずれも混ざらない。外した4通りは
  `docs/project_knowledge.md` に表で残した
- ★★★**器の子を1枚の面 `[data-fit]` で包んでずれを追う**。器は動かさない
- ★★★**閉じる合図は `--kb` ただ1つ**。`focusout` は焦点を戻すだけで閉じない
- ★★★**時計ではなく状態で決める**。6× に絞ると `setTimeout(260)` が 940ms や
  3195ms に遅れる。見るのは「キーボードはまだ出ているか」
- 検証を **4通りの型**へ（`geo4.mjs`）／「開いてすぐ閉じる」は **6× に絞って**
  見る（`plus.mjs`）— 空いていると出ない

### 第10〜17巡（要点だけ）

- ★**出入りは「1枚の板が下から出て下へ帰る」だけ**（ユーザー指定）。不透明度で
  出し入れする形は4度作って4度差し戻された
- ★**出るときの動き出しは組み立てが終わってから**。CSS の動きは時計で進むので、
  マウントの 283ms の間に 320ms の 88% が過ぎ、**板がいきなり現れて**いた
- ★★**書体の「全消し」が効きすぎていた**。`onFontsReady` が**購読しただけで
  呼び返して**いたため、入力画面を開くだけで山の絵が消え、文字が点滅していた
- ★**入力画面は「ただのオーバーレイ」**。drift も gravity も**＋は保存せず手元で
  持つ**（落とし直すのは、まだ在るのに山から押し出されたときだけ）
- ★**「まだスクロールできる」を根治**（3巡）。送っていたのは器ではなく**その先**
  （後ろのタブの列・ページ）。`touchmove` を非passive で受け、**本当に溢れて
  いる箱の中でなければその場で止める**
- ★**器は一度も動かさない**（録音オーバーレイと同じ書き方に固定）
- タスク: **期日は一点(アラーム型のダイアル)・期間は範囲(タイムライン)**・15分刻み。
  期日か期間かは **`endDate` だけ**で決める。日程だけキーボードを閉じる
- タスク: **キーボードが閉じない**を4段構えに（`<div role=button>`＋非passive な
  touchstart ＋ 行の onBlur ＋ layout effect の受け皿）
- ★**左右だけ画面いっぱいにするのは `.bleed-x`**（列の 16px padding で切れる）
- 全体: **文字が出ない／不安定**を根治（第6巡）。①`loadingdone` の全消しを
  400ms にまとめる ②判定キャッシュの偽の焼き付きを外す ③門は600msで必ず開ける

---

## 次に着手すること

1. **実機（iOS Safari）での確認**（この環境で WebKit を動かせないため構造的な制約）
   - ★**アイコンをタップしても崩れないか**（第21巡の本題。上のバーが画面の
     外へ出る／帯がキーボードの遥か上に浮く、が直ったか）
   - ★**＋を押してすぐ閉じないか**（とくに gravity で山が動いている最中）
   - ★**閉じる動きが1枚の板で滑るか**（第21巡。ガクッとしないか）／
     **図形の登場と、情報を足したときの大きさの変化**が気持ちよいか
   - ★**キーボードを閉じたら入力画面が閉じるか**（iOS の「完了」・下スワイプ）。
     逆に、重要度やタグを押しただけで閉じてしまわないこと
   - ★**指で動かないか**（スクロール）／★**時刻**（ダイアル・帯をつまむ）／
     ★**カレンダーの読みやすさ**／★**drift**（端で切れないか・一周するか）
   - **文字が出るか**（第6巡の本題。Chromium では2〜4秒で全部出る。実機が本番）
   - ★**まだ崩れていたら、設定 →「画面の数値を出す」を ON にして
     崩れた瞬間の写真を1枚**。**`res`（ずれ残り）が 0 でなければ器がずれて
     いる**。`sheet` が 0 でなければシートが画面の外へ出ている
2. **Cowork のプロンプト更新**（`COWORK-ROUTINES.md`）… 候補の項目を
   `いつ`（★**日付で書かせる**。YYYY-MM-DD）/ `道具・場所` / `持ち物` /
   `タグ`（英字5つ）へ揃える。日付で書かれないと期日にならず、
   `lib/inboxImport.ts` がメモへ回してしまう。
   ★**プロンプトの全文を提示して承認を得てから**実装する。

---

## 未解決・持ち越し

- **実機 Safari の未検証** … タスクアプリ全般、ジャーナルの円のドラッグと
  マイクの解放。
- **候補のうちジャーナル・ウィッシュ・ストックの行先が未定**／**1日の終わりに
  3アプリの要素を1枚のポスターへプレスする**（§38）は未着手。
- **日程シートの下の見切れ** … 第22巡で「器の中身が下へずれていたから」と
  見立てて直した。まだ残っていたら「画面の数値を出す」の `sheet` で確かめる。
- **完成時に撤去するもの** … `lib/taskDemo.ts` と「デモを入れる」ボタン、
  `ProfileTab` の「ブリーフ生成の実験」カードと「今すぐ生成」、
  ★**「画面の数値を出す」**（`lib/debugViewport.ts` / `ViewportProbe.tsx`）。

---

## いま触っている領域のファイル

★全体のファイル地図は `CLAUDE.md`。ここは**いま手を入れている所だけ**。

```
components/tasks/TaskComposer.tsx  入力画面。--kb / --vvtop / 出入り / 図形の舞台
components/tasks/Popover.tsx       ポップオーバーの器と ★`Press`(押せる面)
components/tasks/WhenSheet.tsx     日程（★キーボードを閉じるシート）
components/tasks/ViewportProbe.tsx ★開発用の数値表示（直ったら撤去）
app/globals.css                    出入り・図形の登場と入れ替え・沈む合図
lib/textFit.ts / solidPaint.ts     グリフのアトラス・★書体の門・ビットマップ
```

## 検証（`scratchpad/`）

★**単体テストの前に必ず写しを焼き直す**（`lib/*.ts` → `*.mjs`。焼かないと
固まったままで、消えた API を試し続ける）:
`for m in solid taskSize taskTags inboxImport; do npx esbuild lib/$m.ts --bundle --format=esm --outfile=$S/$m.mjs; done`

```
fix22.mjs  ★★わざと 300px 壊して戻るか／scrollY で押し下げないか（壊し方を試す）
pop21.mjs  ★ポップオーバー中にずれを追うか・閉じるあいだ板が1枚か・図形の動き
when20.mjs ★日程シートでずれを取り残さないか・閉じる出足（引数=CPUの絞り）
tap.mjs    ★アイコンの当たり判定・沈む合図・クリアの塗り
geo4.mjs   ★入力画面の幾何を4通りの型で / plus.mjs ★＋で即閉じないか（6倍絞り）
v15/v14/v12/v10/v8/v7/v6.mjs  第6〜15巡ぶんの回帰
smooth.mjs 落ちたフレーム / blink.mjs 文字の点滅 / swipe seam junk text drift2
solid.test / tag.test / inbox.test.mjs  単体 90 件
```

## ユーザー側の作業（アプリの外）

- Cowork に**毎晩のタスク「1日を仕分ける」**を設置する（例: 02:00 JST）。
- 任意: `OPENAI_API_KEY` を Vercel に登録すると文字起こしが Whisper 系になる。
