# いまどこにいるか（2026-09-13／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★第94巡に **JOURNAL（録音画面）と
EXPLORE の BRIEF の札**も作り直した（TASK は手つかず）。
★★★**Explore の刷新（券・改札鋏）は保留**。コードは消していない（`DEV` タブごと最後に処分）。

★★★**いま試している大きな変更は1つ。実機で見て決めてもらう段階。**
**アプリごとのアクセント配色**（`lib/appAccent.ts` の `ACCENT_TEST`）。
**`false` の1行で元の 6色パレットへ丸ごと戻る。**

仕様の正 … `docs/home-spec.md`（ホーム）／`docs/project_knowledge.md` §3-h（確定したこと）
／`design.md`（UI の規約と機械チェック13本）。

---

## 直近完了

### ★★★第94巡 ── JOURNAL をカセットへ／BRIEF の札を「形が分類を言う」札へ

**① JOURNAL の顔をカセットに揃えた**（ユーザー指定「図形を現在の journal のタブの
アイコンにして落として。**四角い部分がブルーで他が黒**」「録音の UI も**円と角丸の
四角が組み合わさった形**に。**その時、円の大きさや配置は絶対に変えないでください**」）。

- ★★★**録音画面はもともとカセットだった** ―― アイコンの
  `リールの半径 ÷ 中心間 = 2.9/7.2 = 0.403`、録音画面の円は `0.65w/1.60w = 0.406`。
  **実測でほぼ一致**。足りなかったのは**本体の面**だけ。
- **新設 `lib/cassette.ts`** … 同じ形を**3つの技術**が描く（タブの SVG／山の canvas／
  録音画面の DOM）。塗り分けは**四角＝`JOURNAL_FACE`／リールと帯＝`INK`**。
  ★**タブバーの中だけは1色**（地が選択状態で変わるので色を決め打ちできない）。
- 録音画面 … 円は**墨へ戻り**（第93巡の青をやめた。地との比 **2.02 → 11.25**）、
  青い本体を**兄弟**として `zIndex: 0`・`pointerEvents: "none"` で敷いた。
  ★中に入れると `.vs-reel-spin` で一緒に回る。★入退場は**不透明度だけ**
  （本体は画面より広いので滑らせると縁が見える）。
  ★★★**四隅は画面の外**（円の直径 507px ／ 画面幅 390px ＝ 1.3 倍。片側 175px
  はみ出す）。「**円を動かさない**」の帰結で逃げ道は無い。
  ★**アイコンの余白の比は使えない**（当てると本体が 1224px 高＝全面青）。
- ホームの山 … `kind: "dial"` → **`"cassette"`**、体は `Bodies.rectangle`、
  **面積は据え置き**（外接箱を `√(面積×比)` で取る）。
  ★★**当たり判定の「円は半径で見る」枝から外した**（`drawDial` も削除）。

**② BRIEF の札を作り直した**（ユーザー指定「参照画像と**全く同じになるまで**。
画像が**図形でマスク**される。右下に**黒の円のアイコン**でリンクへ。色は
**Explore のメインカラーで統一**し、形は**ジャンルに対応**」）。

- ★★★**形の約束はすでに在った** ―― `lib/ticket.ts` の `PUNCH_BY_DOMAIN`（券の鋏痕）。
  **新設 `lib/cardShape.ts` はそれを写すだけ**（`SHAPE_BY_PUNCH`）。
  弧＝バショ `dome` ／ 斜め＝タイケン `wedge` ／ 直角＝ジョウホウ `wave` ／
  切れ込み＝モノ `clover`。★★**約束を2つ持たない。**
- 面は `accentOf("life").main` の**1色**、字は `bodyInkOn(面)`（黄の上の墨 **11.31**）。
  ★★★**マスクは写真だけ。札には掛けない**（掛けると `box-shadow` が消え、
  黄とクリームの比 1.27 では縁が1本も出ない）。
- **やめたもの** … 綴じ穴（提案カードだけ。育成カードには残る）／写真のグラデーション
  ／「写真 N を見る ⤢」のピル／**ジャンルの文字**（形が言うことを字で二度言わない）。
- **写真が余りを全部取る** … 52% の決め打ちだと 3:4 の札で**黄色い空白が 300px**
  残った。★**操作（旗・記事を読む・黒い円）は下の1列**に集め、**高さは `LINK_D`
  で固定**（`isTop` で中身が増減しても本文の折り返しが動かない）。
- **`DevStageTab` に「札」モード**を足した（実機で見る見本帳。★**本番の `CardFace`
  をそのまま**並べる ―― 見本用の別実装を作ると形の割り当てが2か所になる）。

**検証（件数）**
- 録音画面 … 円 `rgb(44,38,39)` ×2（`left/top/w/h` が明るいタブと暗いオーバーレイで
  **8値すべて一致**）／本体 `rgb(158,178,239)` 739×539・角丸 100.1px・`zIndex 0`・
  `pointerEvents: none`／目盛り**紙色 `rgb(250,250,249)`**／円は掴んで回る
  （`scale(1.028)` が出る）／**四角の側を引いても回らない**（円の外）。
- ホームの山 … 青い画素 19,880（倍率2）／外接箱 73×100 ＝ 比 1.37（＝`CASSETTE_ASPECT`）
  ／カセットを押す → **JOURNAL の列が x 390 → 0**（＝RECORD へ遷移）。
- 札 … 4形のマスク面積比 **dome 0.89／wedge 0.78／wave 0.96／clover 0.59**
  （4件すべて 1.00 未満＝形が効いている）、**四隅の α は 4形×4隅すべて 0**、
  `maskSize: 100% 100%`・`WebkitMaskImage` **4形すべて有り**／
  面 `rgb(237,228,123)`・角丸 28px・影あり・題 `rgb(44,38,39)`／
  **左右の余白 16px / 16px（対称）**／黒い円 44×44、出典あり3枚＝**1個**・
  出典なし1枚＝**0個**／円を押すと**タブが 1 → 2**（新しいタブ）で
  **先頭の題は変わらない**（＝スワイプが走っていない）／
  写真のある札もマスクされる（`naturalWidth 192` が形の中に出た）／
  デッキ4枚すべて `holes: 0`（綴じ穴が消えた）。
- 機械チェック13本 … #1〜#7・#10〜#13 **すべて 0 件**（#11 は終了コード 0）。
  #8 の左右パディングの持ち主は **3 → 2**（残るのは育成カードの `HOLE_CLEAR` だけ）。
- `tsc` 0 ／ `eslint` 0 ／ `next build` 成功。

### ★★★第93巡 ── タグを全廃し「日付あり＝塗り／なし＝輪郭」へ

- ★★★**タグを全面廃止**（`lib/taskTags.ts`・`lib/tagPattern.ts` をファイルごと削除）。
  分類の道具が多すぎた ―― 残っていた差は柄と書体だけ＝**意味を持たない2択**。
- ★★★**タスクの性質は「日付があるか／ないか」の1軸だけ**。**日付あり＝塗り／
  日付なし＝輪郭**（もとはホームの帯のピルの見え方）。合図は `isDated(spec)`。
  ★★**`paintKey` に日付の有無と地の色を入れること**（`sides` は数しか見ていない）。
- ★★**書体を1つに固定**（`SHAPE_FACE`）。**書体を分類の道具にしない。**
- ★上の段（EXPLORE の提案）は**塗りのまま** ―― 黄は地との比 1.27 で線にすると消える。
- 旧タグ 26 件を読ませて **tag を持つ行 0 件**（`migrate` の単体チェック）。

### ★★★第92巡 ── ホームの山を土台から組み直した

- ★★★**ぼやけの正体 ＝ 同じ器に ResizeObserver が2つ**（先に走る①が `sizeRef` を
  書き換え、②が毎回「差 0」で return）。実測 2.00 → **3.00 画素/CSS px**。
- ★★★**下が空いていた正体 ＝ 床がタブバーのぶん二重引き** → **`.bleed-x-b`**。
- ★★★**「少し変えるたび不安定」＝ 中身が1文字変わると世界ごと作り直し**
  → **effect を2本に割った**（世界は1度だけ／中身は図形だけ差し替え）。
- ★★**眠りを許すのは時間ではなく「みんな止まったら」**／**画面の細かさは端末そのまま**
  （`min(3, dpr)`）／**紙の目（`paperize`）を全廃**。

### ★★★第89〜91巡までの要点

- **`lib/wordPlate.ts`**（日付・曜日の板）… GRAVITY のコードをそのまま移した1か所。
  ★**DOM で組み直さないこと**（板だけが物理と別の座標系に居ると挙動が違う）。
- **山が「塔」だった正体** … ①面積の予算は「横に2つ並ぶ」前提なのに1枚が器の幅の
  68% まで取れた（`FIT_W`/`FIT_H` を 0.52/0.28）②出どころの高さが全部同じ
  （`DROP_SCATTER` 200・間隔 60ms）。
- **日付と曜日の字間を詰めて塊に**（`PLATE_TRACK` -0.06em。★目盛りの外）。
- **ホームは4つ目の列**／**帯**はゆっくり流れ続けて循環（継ぎ目を消すには2周の幅が
  寸分たがわず同じ。速さは「画面1つ分を横切る時間」で持つ）。
- ★★★**刻みは固定（1000/60）**／★★★**焼き込まれた色を信じない**
  （`BriefCard.color` は生成した夜のパレット）／★★★**タブバーの高さは
  `navHeightPx()` から引く**（実機 132px／Chromium 81px）。

---

## 次の一手

1. ★★★**実機で見てもらう** … ①**録音画面のカセット**（本体の**下の縁が物理キーの列を
   横切る**。Chromium では縁 y=693.5・キーの列 672〜729。実機は `NAV_H` が 132px で
   位置が変わるので、詰めるなら `PLATE_PAD` の1つ）②**札の4つの形**（とくに `wave` と
   `clover` の見分け）③**塗りと輪郭**の見分け（輪郭の字は地の上で比 2.55）
   ④**アクセント配色を採るか捨てるか**（捨てるなら `ACCENT_TEST` を `false`）。
2. **引き下ろし**（★ホームの軸。まだ無い）… 帯のピルを掴んで引き下ろすと
   **7本の柱が地面から立ち上がり**（高さ＝その日の量）、離した日がその日付になる。
   外で離すと候補（ストック）へ。**ピルは落ちながら図形へ連続的に変わる**。
   ★**塗り／輪郭の規則はもう通してある**ので、日付が入った瞬間に塗りへ変わる。
3. **持ち物** … 右上のアイコン＋数の印（警告）。押すと印のすぐ下に小さな一覧。
4. **生成器が2つ足りない** … ①「今日行くのがおすすめ」（新しいサーバー関数）
   ②フォローアップの夜間まとめ生成（`Task.suggestions`）。
   ★★**どちらもプロンプトを先に文面で出して承認を得る。**
5. ★ピルを押したときの詳細（まだ無い）。★山の図形はもう押せる（`Piece.nav`）。

## 未解決

- ★★★**提案の生成は止まったまま。** 15 は**表示だけ**の上限（ユーザー確定）なので、
  裏の未読 41 枚は減らず、夜間の生成は `POOL_CAP`(40) の番に掛かり続ける
  ＝**新しい提案が入ってこない**。実際に減らす側へ切り替える場所は
  `app/api/cron/build-brief/route.ts` の `POOL_CAP` にコメントで明記した。
- ★**帯の②（今日行くのがおすすめ）は生成器が無いので今は出ない。**
- ★★**輪郭の字は地の上で比 2.55**（本文の下限を割る。★目盛りの外・ユーザー確定）。
- ★★**録音画面のカセットの四隅は画面の外**（円の直径が画面幅の 1.3 倍）。
  「**円を動かさない**」の帰結で、逃げ道は無い。見えるのは上下の縁と青い面だけ。
- ★★**黄の札とクリームの地の比は 1.27**。札の縁は**影**（`SOFT_SHADOW_LG`）が
  持っているので、**札にマスクを掛けると縁が消える**（掛けていない）。
- ★★**券の大きな英語が読めなくなる恐れ** ―― アクセントの段は白い紙の上で
  4.85/3.21/2.20/1.54 で、下2つは大きな文字の下限 3.0 を割る。**券は `DEV` タブと
  `/dev/explore` にしか無い**ので4タブには影響しない。
- ★★**地 `#FFFBF5` と紙 `#FAFAF9` の差が比 1.01**（第76巡から持ち越し）。
- ★`SectionLabel`（`MUTED`）は明るい地で 3.2 ＝ AA 割れ。**既知・未対応。**

---

## 重要パス

- ホーム … `components/tabs/HomeTab.tsx`／帯 `components/home/Band.tsx` ＋
  `lib/homeBand.ts`／山 `components/home/Pile.tsx` ＋ `pileWorld.ts` ＋ `pilePaint.ts`
  ／器の寸法 `lib/pileBox.ts`／列の配線 `components/AppShell.tsx`／仕様 `docs/home-spec.md`
- ★★★**塗り／輪郭は `lib/solidPaint.ts` の `paintShape` と `isDated` が正**。
  ホームの山だけ形が角丸の四角なので `components/home/pilePaint.ts` にも同じ分岐がある
  （**片方を直したらもう片方も**）。
- ★★★**カセットの寸法は `lib/cassette.ts` の1か所**（タブの SVG／山の canvas／
  録音画面の DOM）。目盛り5本だけ `lib/dial.ts`（`drawDial` は削除済み）。
- ★★★**札の形は `lib/cardShape.ts`。ただし `lib/ticket.ts` の `PUNCH_BY_DOMAIN`
  から導いている**（対応表を2つ持たない）。見本帳は `DevStageTab` の「札」。
- ★★★**山の器は `.bleed-x-b`**（`app/globals.css`）。左右＋**下**を列の外へ出す
  ―― 下を出さないと `floorYOf` が**タブバーの高さを二重に引く**。
- ★★★**器を見る ResizeObserver は1つだけ。** 2つ付けると先に走ったほうが
  もう一方の番人を黙らせ、**canvas の実解像度が初回のまま固定される**。
- ★★★**文字の板は `lib/wordPlate.ts` の1か所**。★**DOM で組み直さないこと。**
- ★★**アクセントのテスト** … `lib/appAccent.ts`（`ACCENT_TEST` の1行で戻る）。
  効いている先は `lib/constants.ts` の `TASK_FACE`／`JOURNAL_FACE`／`STUDIO_KEY` と
  `lib/palette.ts`（ドメイン4）。
- ★**色そのものは `lib/constants.ts` の `PALETTE`（6行）だけ**／役との対応は
  同ファイルの `SCHEME` と `lib/palette.ts`
  ／★**面から導く3つ** … `bodyInkOn`・`redOn`・`inkVarsOn`。**表を持たず、導かせる。**
- ★UI の規約と機械チェック13本 … `design.md`（作業の前後に必ず走らせる）
  ／目盛り `lib/tokens.ts`／動き `lib/motion.ts` ＋ `app/globals.css` の `:root`
- タスク … `components/tabs/GravityTab.tsx`（★山の物理の出どころ。ホームと**対**）
  ／カメラ `components/tasks/TaskSpace.tsx`／入口の輪 `components/CreateMenu.tsx`
- 券・鋏（★保留中）… `components/explore/samples/`／`Ticket.tsx`／`lib/nipperShape.ts`
  （生成物。手で直さない）／設計 `docs/explore-redesign.md`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite` / `fetchDirect` / `jinaSlot`）
  ／ 単体チェック `npx tsx tools/jina-check.mjs`
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちて 500）。Playwright は
  `executablePath: "/opt/pw-browsers/chromium"`、**`localhost` で開く**。
  ★★**山の測定は 8 秒待つ**（落ちる距離が伸びた。4.5秒だとまだ空中に居る）。
  ★★**canvas の倍率は場所で違う** … ホームの山 3／GRAVITY・DRIFT・入力画面 2。
  線の太さを画素で測るときは**その canvas の倍率**で割ること（3 で割って誤った）。
  ★`migrate` の単体チェックは、state に **`wishes: []` を入れておくこと**
  （無いと「最古の形式」と見なされて state ごと作り直される）。
  ★`next build` は Google フォントの取得に**ときどき失敗する**（環境側。数回やり直す）。
