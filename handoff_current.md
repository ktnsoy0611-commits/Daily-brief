# いまどこにいるか（2026-09-13／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★第94〜97巡に **JOURNAL（録音画面）と
EXPLORE の BRIEF の札**も作り直した（TASK は手つかず）。
★★★**Explore の刷新（券・改札鋏）は保留**（`DEV` タブごと最後に処分）。
★★★**いま試している大きな変更は1つ**（実機で見て決めてもらう段階）… **アプリごとの
アクセント配色**（`lib/appAccent.ts` の `ACCENT_TEST`。**`false` の1行で元へ戻る**）。
仕様の正 … `docs/home-spec.md`（ホーム）／`docs/project_knowledge.md` §3-h／
`design.md`（UI の規約と機械チェック13本）。

---

## 直近完了

### ★★★第97巡 ── 形を器いっぱいに／リールを大きく遠く／キーをバーの中心線へ

ユーザー指摘は3つとも**実測で裏が取れた欠陥**だった（感覚の違いではない）。

**① 札の形が器に届いていなかった ＝ ベゼルが4形バラバラ**
- ★★★**原因 ＝ `objectBoundingBox` は 0〜1 のパスを器の寸法へそのまま掛けるのに、
  パスの外接箱が 0〜1 でなかった。** 実測（器 276・指定ベゼル 32）… 四つ葉 **80.5%**
  （ベゼル **58.9**）／六角形 幅 **97.2%**（左右 **35.9**）／波打つ四角 **107.5%**
  （**はみ出して切れていた**）／トゲトゲ 100%（32）。
  **「図形が縦長」「四辺でばらばら」「ベゼルの太さと合っていない」は全部ここから。**
- → `cardShapePoints()` の最後に **`fitBox`（外接箱を 0〜1 へ正規化）**。
  ★**点の列で直したので、SVG の `clip-path` も canvas の輪郭も同時に直る。**
  ★**形の性格の目盛り（`CLOVER_AMP` など）は1つも触っていない。**

**② カセットのリールが小さく、中央に固まっていた**
- 実測（24 の器）… 左の縁 2.8 ／ **リール間 0.8** ／ 右の縁 2.8（**間が縁の 1/3.5**）。
- → ★★★**半径 3.6・中心 7.6/16.4 は式の解**（`5.2 − R = (9.6 + R)/1.5 − 2R`）。
  「左の縁 ＝ リール間 ＝ 右の縁 ＝ 段までの隙間」が**全部 1.6**。**手で振らない。**

**③ 録音画面のキーがバーの中心線に乗っていなかった（3つの独立した欠陥）**
- ★`KEY_D = 42` は**生の数**で、段の高さ（アイコンの比から出る 67.1）と無関係。
- ★★★`KEY_LABEL_H = 12` と書いてあったが実際の行は `TYPE.nano × LEAD.flat` ＝ **7**。
  余り 5px が `flex-start` で捨てられ**列ごと 5px 上**。
- ★`alignItems: center` は**沈みの溝を含めた箱**を中央に置くので**さらに 2px 上**。
- → 合計 **7px 上**（黒い余白 上 5.6 / 下 19.6）で、押しても **一度も中心線を
  通らなかった**。★`KEY_D = deckH − KEY_LIP×2`／**円の中心を `deckH/2` へ絶対配置**／
  **3つのキーはバーの端の丸と同心**（`space-evenly` を捨てた）／REC は穴と同心。
  ★`KEY_LABEL_H` は**消した**（ラベルはキーと同じ中心の x へ絶対配置）。

**④ 札が画面の中で上へずれていた**（「下に変な隙間」）
- `arena` の下にフッターの予約枠（58）が常に居るので、枠の中で中央＝**画面では
  29 上**。実測 **45 / 103**。→ 余っている高さの範囲でだけ `translateY` で下げる
  （`lift = min(slack, 58/2)`）。★★**予約枠は消さない**（育成カードの昇格で札の実寸が
  変わる）／★★**札を縮めてまで中心へは寄せない**。

**検証（すべて WebKit・件数つき）** … 外接箱 **8件（4形×2軸）すべて 0〜1**／
見えるベゼル **16件（4形×4辺）すべて 32**／カセットの4つの隙間が**全部 1.6**・
`D/H 0.5143`・`中心間/W 0.4583`・リールの中心 y ＝ 本体の中心 y（差 0）／
**キーの中心 y ＝ 段の中心 y（差 0。4件）**・黒い余白 **7.95 / 8.05**・押すと
**4.04px** 沈む・径 **51.1 ＝ deckH − 16**・3キーがバーの丸と同心・REC が穴と同心／
札の上下 **74 / 74**（340×510 のまま）／明暗2画面／機械チェック13本 **0件**・
`tsc` 0・`eslint` 0・`build` 成功。

### ★★★第96巡 ── 比を正す（札の器を正方形へ／録音画面をアイコンの拡大へ）

★寸法は第97巡に上書きされた。残る要点だけ:

- ★★★**`objectBoundingBox` は 0〜1 のパスを器の比へ引き伸ばす** → **器は必ず正方形**
  （`aspectRatio: "1 / 1"`。**文の塊が余りを取る**）。ベゼルは **`SPACE.xxl`**（9.4%。
  参照の実測 10.3%）。判（KEEP/SKIP）も同じ値。
- ★★★**札の比は `ITEM_CARD_ASPECT` から来ていない** → **`BRIEF_CARD_ASPECT`**
  （"2 / 3"）を新設。**1つの割り算から2か所を導く。**
- ★★★**録音画面は「アイコンを拡大して画面に嵌めた絵」** ―― リールの寸法は全部
  `lib/cassette.ts` の比から。**画面側に数を1つも置かない。**
  ★★大きさを決める条件は**下の段が画面に収まること**の1つだけ＝本体は必ずはみ出す。
  ★★★**`DIAL_RATIO`/`DIAL_CX`/`DIAL_WAIST` は消えた。復活させない。**
- ★★★**山に落ちる5種のうちジャンルを持てるのは提案だけ**／**物理は凸の12角形**
  （`poly-decomp` が無い）／**形の正は「点の列」**（SVG も canvas もそこから導く）。

### ★★★第94〜95巡 ── 札の実機のバグを clip-path で根治／JOURNAL をカセットへ

★寸法も仕組みも第96〜97巡に上書きされた。残る要点だけ:

- ★★★**WebKit がこの環境で動く**（`npx playwright install webkit` ＋
  `install-deps webkit`）。★★**万能ではない** ―― 下のバグは Playwright の WebKit
  でも**再現しなかった**（実機の WebKit だけ）。
- ★★★**実機のバグの正体 ＝ `mask-size: 100% 100%` が効かず、SVG が固有の 1:1 の比の
  まま置かれていた**（切り抜きが器の幅より 254 デバイス画素 狭い正方形）。
  ★**プロパティの書き漏らしではない**（`Webkit` 付きと対で8本書いてあった）。
- ★★★**だから仕組みごと替えた** … `clip-path` ＋ `clipPathUnits="objectBoundingBox"`。
  ★**id は札ごとに `useId()`**（`AppShell` はタブを全部載せたまま横へ送る）。
- ★★**形は参照画像に在るものだけ**（ドームと台形は参照に1枚も無かったので捨てた）。
  割り当ては `PUNCH_BY_DOMAIN` から導く（**約束を2つ持たない**）。
- ★★★**切るのは写真だけ。札には掛けない**（掛けると `box-shadow` が消え、黄と
  クリームの比 1.27 では縁が1本も出ない）。
- **新設 `lib/cassette.ts`** … 同じ形を**3つの技術**が描く（タブの SVG／山の canvas／
  録音画面の DOM）。★**タブバーの中だけは1色**（地が選択状態で変わる）。
  ★下の段を「左に円・右にバー」へ割り、**そのままキーの穴**にした。
- BRIEF から**綴じ穴を外した**（育成カードには残る）。**ジャンルの文字も置かない**。
  操作は下の1列に集め、**高さを `LINK_D` で固定**。

### ★★★第93巡 ── タグを全廃し「日付あり＝塗り／なし＝輪郭」へ

- ★★★**タグを全面廃止**（`lib/taskTags.ts`・`lib/tagPattern.ts` を削除）。残っていた
  差は柄と書体だけ＝**意味を持たない2択**だった。
- ★★★**タスクの性質は「日付があるか／ないか」の1軸だけ**（合図は `isDated(spec)`）。
  ★★**`paintKey` に日付の有無と地の色を入れること**（`sides` は数しか見ていない）。
- ★★**書体を1つに固定**（`SHAPE_FACE`）。**書体を分類の道具にしない。**
- ★上の段（EXPLORE の提案）は**塗りのまま**（黄は地との比 1.27 で線にすると消える）。

### ★★★第89〜92巡までの要点

**山の作り替え**（第92巡）… ★★★**ぼやけ ＝ 同じ器に ResizeObserver が2つ**／
★★★**下が空く ＝ 床がタブバーのぶん二重引き** → `.bleed-x-b`／
★★★**「少し変えるたび不安定」＝ 中身が1文字変わると世界ごと作り直し** →
**effect を2本に割った**／**紙の目（`paperize`）を全廃**。
**`lib/wordPlate.ts`**（文字の板。★DOM で組み直さない）／**山が「塔」だった正体**／
**刻みは固定 1000/60**／**焼き込まれた色を信じない**／**タブバーの高さは
`navHeightPx()`**（実機 132px／Chromium 81px）。詳しくは `docs/project_knowledge.md`。

---

## 次の一手

1. ★★★**実機で見てもらう** … ①**録音画面**（大きくなったリールと段の釣り合い・
   キーとバーの縁 8px）②**札の4つの形**（`DEV` → 「札」。**まだ潰れていたら、次は
   写真を `<svg><image preserveAspectRatio="xMidYMid slice" clip-path=…>` の中へ
   入れる** ―― DOM の `<img>` と CSS を使わず SVG エンジン1つに描かせる）
   ③**ホームの山の提案の4つの形**（多角形にしたので転がり方が変わった）
   ④**アクセント配色を採るか捨てるか**（捨てるなら `ACCENT_TEST` を `false`）。
   ★★**札の中の「文の下から操作の列までの隙間」は手をつけていない**（本文が短い札で
   28px 空く）。**見た目の変更なので、直すなら先に文面を出して承認を得る。**
2. **引き下ろし**（★ホームの軸。まだ無い）… 帯のピルを掴んで引き下ろすと
   **7本の柱が地面から立ち上がり**（高さ＝その日の量）、離した日がその日付になる。
   外で離すと候補（ストック）へ。**ピルは落ちながら図形へ連続的に変わる**。
3. **持ち物** … 右上のアイコン＋数の印（警告）。押すと印のすぐ下に小さな一覧。
4. **生成器が2つ足りない** … ①「今日行くのがおすすめ」②フォローアップの夜間生成。
   ★★**どちらもプロンプトを先に文面で出して承認を得る。**
5. ★ピルを押したときの詳細（まだ無い）。★山の図形はもう押せる（`Piece.nav`）。

## 未解決

- ★★★**提案の生成は止まったまま。** 15 は**表示だけ**の上限なので裏の未読 41 枚は
  減らず、夜間の生成は `POOL_CAP`(40) に掛かり続ける（切り替える場所は
  `app/api/cron/build-brief/route.ts` にコメント済み）。
- ★**帯の②（今日行くのがおすすめ）は生成器が無いので今は出ない。**
- ★★**輪郭の字は地の上で比 2.55**（本文の下限を割る。★目盛りの外・ユーザー確定）。
- ★★★**札の比は `ITEM_CARD_ASPECT` から来ていない。`BRIEF_CARD_ASPECT` が正。**
  `ITEM_CARD_ASPECT` を変えると**ストック・ゴールのバインダー・表紙・コーヴァー
  フロー**が動くのに、**札は 1px も動かない**（第96巡に踏みかけた罠）。
- ★★**録音画面の本体は左右へはみ出したまま**（片側 163px）＝**仕様**（ユーザー確定。
  式から自然に出る）。
- ★★**黄の札とクリームの地の比は 1.27**。縁は**影**が持つので、**札を切り抜くと
  縁が消える**（切っていない）。
- ★★★**札の実機のバグは「直したはず」の段階**。Chromium と Playwright の WebKit
  では**もともと再現していなかった**ので、**直った証明は実機でしか取れない**。
- ★★**券の大きな英語が読めなくなる恐れ**（白い紙の上で下2つが 3.0 を割る）。
  券は `DEV` と `/dev/explore` にしか無いので4タブには影響しない。
- ★★**地と紙の差が比 1.01**／`SectionLabel`（`MUTED`）は明るい地で 3.2 ＝ AA 割れ。
  **どちらも既知・未対応。**
- ★★**札の中の「文の下から操作の列までの隙間」**（本文が短い札で 28px）。**未着手。**

---

## 重要パス

- ホーム … `HomeTab.tsx`／帯 `Band.tsx` ＋ `lib/homeBand.ts`／山
  `components/home/Pile.tsx` ＋ `pileWorld.ts` ＋ `pilePaint.ts`／器 `lib/pileBox.ts`
  ／配線 `AppShell.tsx`／仕様 `docs/home-spec.md`
- ★★★**塗り／輪郭は `lib/solidPaint.ts` の `paintShape` と `isDated` が正**
  （山は `pilePaint.ts` にも同じ分岐。**片方を直したら両方**）。
- ★★★**カセットの寸法は `lib/cassette.ts` の1か所**（タブの SVG／山の canvas／
  録音画面の DOM）。目盛りだけ `lib/dial.ts`（`drawDial` は削除済み）。
- ★★★**札の形は `lib/cardShape.ts`（点の列が正）＋ `CardShapeDefs.tsx`。
  ただし `lib/ticket.ts` の `PUNCH_BY_DOMAIN` から導いている**（対応表を2つ持たない）。
  ★**CSS のマスクを使わない。`clip-path` ＋ `objectBoundingBox`。**
  ★★★**器は必ず正方形**（`objectBoundingBox` は器の比へ引き伸ばす）。
  ★★★**点の列は最後に外接箱を 0〜1 へ正規化する**（`fitBox`。しないと形ごとに
  見えるベゼルが変わる）。
  ★canvas（ホームの山の提案）は `traceCardShape`。見本帳は `DevStageTab` の「札」。
- ★★★**山の器は `.bleed-x-b`**（下を出さないと `floorYOf` がタブバーを二重引き）／
  ★★★**器を見る ResizeObserver は1つだけ**（2つだと canvas の実解像度が固定される）／
  ★★★**文字の板は `lib/wordPlate.ts` の1か所**（★DOM で組み直さない）。
- ★★**アクセントのテスト** … `lib/appAccent.ts`（`ACCENT_TEST` の1行で戻る）。
- ★**色そのものは `lib/constants.ts` の `PALETTE`（6行）だけ**／役との対応は
  `SCHEME` と `lib/palette.ts`／★**面から導く3つ** … `bodyInkOn`・`redOn`・
  `inkVarsOn`。**表を持たず、導かせる。**
- ★UI の規約と機械チェック13本 … `design.md`／目盛り `lib/tokens.ts`／動き
  `lib/motion.ts` ＋ `app/globals.css` の `:root`
- タスク … `GravityTab.tsx`（★山の物理の出どころ。ホームと**対**）／カメラ
  `TaskSpace.tsx`／入口の輪 `CreateMenu.tsx`
- 券・鋏（★保留中）… `components/explore/samples/`／`Ticket.tsx`／`lib/nipperShape.ts`
  （生成物。手で直さない）／`docs/explore-redesign.md`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite`/`fetchDirect`/`jinaSlot`）／
  単体チェック `npx tsx tools/jina-check.mjs`
- ★★★**WebKit（＝実機と同じ系列）がこの環境で動く**… `npx playwright install webkit`
  ＋ `install-deps webkit`（`libgtk-4` などが要る）。`executablePath` 無しに起動
  （**Chromium だけ** `/opt/pw-browsers/chromium` が要る）。
  ★★**万能ではない** … 第95巡の札のバグは Playwright の WebKit でも再現しなかった。
  **「Chromium で 0 件＝実機で直った」は今も言えない。**
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちて 500）。**`localhost` で開く**。
  ★★**山の測定は 8 秒待つ**（落ちる距離が伸びた。4.5秒だとまだ空中に居る）。
  ★★**canvas の倍率は場所で違う**（山 3／GRAVITY・DRIFT・入力画面 2）。
  ★`migrate` の単体チェックは state に **`wishes: []` が要る**。
  ★`next build` は Google フォントの取得に**ときどき失敗する**（数回やり直す）。
