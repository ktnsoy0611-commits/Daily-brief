# いまどこにいるか（第70巡・2026-08-30／Explore の刷新の途中）

3アプリ（JOURNAL / TASK / EXPLORE）は動いている。第68巡までで TASK は一段落。
**第69巡から Explore の刷新中。設計の正は `docs/explore-redesign.md`。**

★★新しい**券と改札鋏は `/dev/explore` と `DEV` タブにしか無い**。
**Explore の4タブはまだ手つかず**で、現状のまま動いている。

---

## Explore 刷新の骨子（`docs/explore-redesign.md` が正）

- **2本の柱**は提案と実行。それを貫く共通部品が**券**で、場所によって縮尺だけが変わる。
- 場所は3つ … 提案（1枚）／ストック（格子）／マップ（点）＋ プランを重ねる。
- **言葉は一般語**。メタファーは視覚だけ。「本日の販売は終了しました」のような
  言い回しは使わない。
- 入鋏＝残す。**鋏痕の形は4ドメインに対応**し、3か所（券の穴／マップのノードの形／
  ストックの絞り込み）で同じ意味を持つ。
- ★**CSS の 3D 変形は使えない**（design.md 冒頭）が、**WebGL は使ってよい**。
  券と鋏は `components/explore/TicketStage.tsx` の**同じ3D空間**に居る。

---

## 直近完了

### ★★★第70巡 ── 券と鋏を**同じ3D空間**へ（`components/explore/TicketStage.tsx`）

- ★★★**券の面は「深さだけ書いて色は書かない」板**（`colorWrite: false`）。canvas は
  券のところだけ穴が空き、**下の DOM の券がそのまま見える**（版面は無傷）。深さは
  書くので**鋏が奥へ回れば隠れる**。
- ★★**券の面は z = 0**。そこで「1単位 = 1px」になる距離にカメラを置いたので、
  **DOM の券は素の CSS の座標のままで必ず一致する**（投影の計算が要らない）。
- 入っているもの … 券1枚／小口／**水平な床に落ちる影**／鋏。★★**束は作らない**
  （**スキップしたら次の券が上から降りてくる**形にする）。
- ★場の組み方（ユーザー確定）… **床は水平・券はそれに垂直に浮く・カメラは真正面**
  （床は塗らず**影だけが床を教える**）。**カメラの焦点＝券の真ん中**。鋏は
  **券の右下（被らない）**で、頭が画面の中・柄は画面外へ抜ける。
- ★★★**券の版面は DOM のまま**。ただし券の中に `zIndex` を持つ要素があるので、
  器の側で **`isolation: isolate`** を掛けて文脈を閉じる（閉じないと**文字だけが
  canvas を突き抜けて鋏の手前に浮く**）。★焼き付けは不要になった。
- ★鋏の**厚みは 2 倍**（`HALF` 45／28 → **90／56**。比は保つ。1.5倍でも薄いとの
  指摘で二度上げた）。バネの z（`COIL_Z`）も同じだけ（26 → 52）。
- ★★★**金属の質感**（`applySteel`）… 段（6つ）は土台に残し、**面のむら・光沢・
  すり傷・券の映り込み**を重ねる。目盛りは `NIPPER_PAINT` の
  `gloss`／`shine`／`scratch`／`bounce`。
  - ★**ramp は地の色から導出**（`STEEL_DARK` → `BD_GREY` の6段）。手で置いた
    寒色の灰は青が7ほど強く、金属に見えなかった。実測で**青い段 0/6**。
  - ★★**傷は「セルに1本」**（横切る軸をセルに割り、位置・太さ・長さ・濃さを
    セル番号のハッシュから引く）。**軸は3次元で散らす** ―― 図の xy 面だけで回すと
    小口の面で長い直線になる。太さと長さは**図の単位**で決める（刻みに対する割合
    だと点々にしか見えない）。
  - ★★★**うねりは2次元の雑音**。1次元の雑音を「x と y の一次結合」で引くと
    **等値線が必ず平行な直線**になる ＝ これが「規則的な斜めのライン」の正体だった。
    実測（平らな一面の内側 8064 画素）… 前は 8° 方向のばらつきだけが突出
    （0.473／他は 0.166〜0.302・比較 76〜91 本）。後は 0.265 で突出が消えた。
  - ★**ワントーン明るく**（`ramp` の混ぜ具合を1段ずつ上げた）。同じ面の平均の
    明るさ **83.6 → 100.2**。
  - ★**券の色が乗る**（`SteelBounce`。券の色と中心を**視点座標**で毎フレーム）。
    A/B 実測 … 約59000画素で **R−B 2.74 → 4.80**、暖色の画素 **13.5% → 30.3%**。
  - ★**ベベルは四分円**（`BEVEL_STEPS`）で半径 7 → 11。**頂点ごとにその場所の
    狭さの34%まで**に抑える（実測 169頂点中52頂点が上限に当たる。左 t1 は
    46中29が 3.8 まで）。スリットは狭まらず**広がる**ので、守るのは細い材料のほう。
  - ★踏んだ穴7つは `docs/explore-redesign.md` の表へ。
    ★★**GLSL のコメントにバッククォートを書かない**（2度やった。TS の
    テンプレート文字列が閉じて工具が消える）。
- ★★鋏は**平らな面を左（券のほう）へ回す**（`yaw`。縦の軸まわり）。指定は **80°**。
  ★実測を報告済み … 80° では見えている面の幅が `cos80° = 17%` まで痩せ、頭・
  スリット・バネの形はほとんど読めない。**意図した造形**として採っている。
- ★★**実機で見るための `DEV` タブ**（`components/tabs/DevStageTab.tsx`。EXPLORE の
  タブバーの末尾）。構図は `/dev/explore` の「3D の場・定位置」と同じで、値は
  390 × 844 に対する**割合**で持つ。★★**完成したら4か所まとめて消す**
  （このファイル／`lib/apps.ts` の `life-dev`／`lib/types.ts` の `LifeTabId`／
  `components/TabIcons.tsx` の `ticket`）。
  ★★タブバーの高さは**実寸を測る** ―― `--nav-h` は `calc`／`env()` 混じりの
  文字列で `parseFloat` は 0 を返し、鋏がタブバーの裏に隠れた。
  検証 … タブ5つは各 56.4px ＋ 丸 52px ＝ 334px（390px に収まる）。
  版面は素の券と **682500 画素すべて一致**（中央値の比 1.0000）。
- 券の紙の色を **場所=黄 / 体験=橙 / 情報=空 / もの=桃** に直した（`TICKET_DOMAIN_COLOR`）。
  アプリは `lib/deckStyle.ts` で kind ごとに色を持っていて、**同じものが2色**に
  なっていた。もの→桃・情報→空は `deckStyle` と完全に一致する。
- 踏んだ穴3つ（詳細は `docs/explore-redesign.md` §3）… ①three.js は不透明な面を
  **「材質の番号 → 奥行き」**で並べる（穴あきの面に `renderOrder = -1`）②既定の
  深さ比較は `LessEqual` なので**同じ深さだと後勝ち** ③地を遠くに置くと影が流れる。
- `appTone`（段の表を JS からも引く）と `poseNipper`（腕とバネの姿勢）を足した。

## 改札鋏（第69巡の 9〜23巡目）

### ★★★23巡目 ── **持ち手の内側を削った**（図面のパースぶん）

`tools/clean-nipper.mts` の**規則8**。`min( 12·smoothstep(0,0.7,t), 0.30·(幅−面取り2つ) )`

- ★★★**青い線は「正面の暗い面」の上にある**。シルエットの内端ではなく**正面の面の
  内端から**測る（取り違えると右の削る量が幅の 60% と出る。実際に出た）。
  ★★**線の中心ではなく内側の縁で測る**（中心だと全体に約5単位多く出る）。
- 実測は**右15／左9**と食い違ったが**パースの見え方の差**。ユーザー確定で**平均12**。
- ★★**上限が先端を守る**。★**削る場所は走って見つける**（判定は両端とも内側）。
- 検証 … 辺の数は不変、脚の刺さり 13→4.1、描画で測った削り量と青い線の差は
  右 比較482件・平均 −0.1／左 513件・平均 +3.0 単位。

### ★★★22巡目 ── **パスの整理**。なぞる工程と整える工程を分けた

```
図4枚 → tools/trace-nipper.mjs → lib/nipperShapeRaw.ts（生）
                               → tools/clean-nipper.mts → lib/nipperShape.ts（整）
```

- ★**元データは `docs/archive/nipper-shape-21.ts` に凍結**（誰も import しない）。
- ★整理は**再実行できるプログラム**。形を直すときは規則の目盛りを直して走らせ直す。
- **辺 62 → 51**。潰れた辺 3→0、軸平行 3→12、ずれ 最大 2.55（許容 6）。
- ★★**急な折れ（40°超）は角**。★★**スリットは最後に作る**（枠は生の `NIPPER_SLOT`）。

### ★★★21巡目 ── **部品ごとの図**から組み立て直した（入力が4枚になった）

「2部品の重なりがおかしい」の**根本原因は入力**だった ―― 16〜20巡目は
**組み立て図1枚**から2部品を割っていたので、左が右に隠れて重なりが存在しなかった。
入力を4枚（右／左／左の厚み／組み立て。**同じ枠なので位置合わせ不要**）にして解消。

- 段は**2つ**に。★**右の中では左は必ず薄い段**（塗り分けより優先）。
- ★★**塗り残しは「いちばん近い塗り」へ配る**（既定を「厚い」に倒していたため、
  薄いはずの先端のふちだけが厚いまま立ち上がっていた。実測 956px）。
- ★★**バネの脚は輪の接線方向**。★支点は**重なりの下の端**（＝蝶番）。

### ★★★20巡目 ── 輪郭を**直線と円弧**に組み直した

なぞった点の羅列をやめ、角と角のあいだを**1本の辺**にした。生成物が `L(...)` /
`A(...)` の並びになり、22・23巡目はこの並びの上で動いている。
★同巡の「段を色で見せる」と脚の取り方は**21巡目に撤回済み**。

### 9〜19巡目の要点（詳細は `docs/explore-redesign.md` §2-b）

- **19巡目**… ★**DP は開いた列に使う**。★★**段の境目は面取りしない**。
- **18巡目**… 厚みを**塗り分け**から読む。★捨てるかは**太らせる前**の画素数で。
- **17巡目**… **three.js（WebGL）へ移した**。「板に見える」原因は描き方ではなく立体
  そのものだった。★光の強さは **π**。★回す向きの符号は SVG と three.js で**逆**。
- **16巡目**… 目測をやめてトレースへ。`lib/nipperShape.ts` は**生成物。手で直さない**。
- **13巡目**… 機構は**ペンチ**で確定（9〜12巡目は「2本の棒」に見えていた）。
  ★**厚みを削って解決しない**。

### ★★`app/layout.tsx` のハイドレーションが壊れていた（全画面に効く修正）

`<style>` を `<html>` 直下に `precedence` 無しで置いていた（不正な HTML → 水和が
失敗し、クライアントが丸ごと動かない）。`href="tokens" precedence="default"` で解決。

## 次の一手

1. **実機（iPhone）で `DEV` タブを見てもらう**。★見るのは3つ ―― 金属の質感／
   斜めの縞が消えたか／**滑らかに動くか**（下の未解決を参照）。
   鋏の直し方は3つ。**形**＝平面図を描き直して `trace` からやり直す／**整理と削り**
   ＝`tools/clean-nipper.mts` の先頭の目盛り（`SHAVE` など）を変えて走らせ直す／
   **厚み**＝厚み図を塗り直すか `lib/nipperRig.ts` の `HALF`。数値を散らかさない。
2. **券のデザインの精査**（★ユーザー指定。この巡の次はこれ）。
3. **券が上からひらりと降りてくる**（★ユーザー指定）。スキップ／入鋏のあと、
   次の1枚が上から舞い降りる。`TicketStage` は券を場所で受け取るので、
   `card.x/y` と `z` を動かすだけで書ける（★動きは `lib/spring.ts`）。
4. **入鋏とスキップの動き**（★ユーザー指定）… 鋏で穴を開ける／券をスキップする。
5. **提案（TODAY）の操作** … 鋏を掴んで券へ寄せると縁に**吸い付き**、離すと切り欠かれる。
6. **ストック（STOCK）** … 格子・鋏痕での絞り込み・期限。そのあとマップ。

## 未解決

- ★**券1枚の小口は、この画角では1画素に満たない**（ほぼ正射影のため）。厚みを
  読ませるには `CARD_T` を上げるか画角を広げるしかない。いまは**影で浮かせて**いる。
- ★★**`applySteel` の計算量は未確認**。1画素あたり雑音を約55回引く（傷の傾きを
  物の座標の差分で取るため）。**実機の iPhone で滑らかに出るか要確認**。
  重ければ、傷を**テクスチャに焼いて三面から引く**形に替えれば1画素3回で済む。
- ★**デフォルメ（持ち手を単純な幾何へ）はまだ**。`tools/clean-nipper.mts` に規則を
  足せばよい（いまは計51辺）。側面図では厚みの段が見えない（薄い片が厚い片に
  挟み込まれているため）。絞るなら厚み図を塗り直す。
- ★`/dev/explore` は WebGL の器を**13個**開く（`DEV` タブは1個）。iOS は同時に
  開ける数が少ないので、実機で見るときは **`DEV` タブのほう**を使う。
- バネの脚は図の細い線を拾えないので**まだ手で置いている**（`COIL.legFar/legNear`）。
  閉じたとき脚の先が腕から離れる。
- 提案とストックを1画面にするか。駅データ（ekidata）の同梱可否。

---

## 重要パス

- ★Explore 刷新の設計 … `docs/explore-redesign.md`／★UI の規約と機械チェック13本
  … `design.md`（前後に必ず走らせる）／目盛り `lib/tokens.ts`／色 `lib/constants.ts`
- 券 … `components/explore/Ticket.tsx` / `PunchMark.tsx` / `lib/ticket.ts`
- ★券と鋏を同じ3D空間に置く器 … `components/explore/TicketStage.tsx`
- 鋏（★three.js）… `Nipper.tsx`（画角・動き）＋ `lib/nipperMesh.ts`（立体と質感）
  ＋ `lib/nipperRig.ts`（組み立てと光）／三面図 `NipperViews.tsx`／`app/dev/explore`
  ＋ ★形の生成 … `lib/nipperPath.ts`（手書き）→ `tools/trace-nipper.mjs` →
  `lib/nipperShapeRaw.ts` → `tools/clean-nipper.mts` → `lib/nipperShape.ts`
  （**どちらも生成物。手で直さない**）／凍結 `docs/archive/nipper-shape-21.ts`
- タスク … `components/tabs/GravityTab.tsx`／カメラ `components/tasks/TaskSpace.tsx`
  ／入口の輪 `components/CreateMenu.tsx`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite` / `fetchDirect` / `jinaSlot`）
  ／ 単体チェック `npx tsx tools/jina-check.mjs`
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちて 500）。Playwright は
  `executablePath: "/opt/pw-browsers/chromium"`、**`localhost` で開く**。
  ★**WebGL を撮るには** `--use-gl=angle --use-angle=swiftshader
  --enable-unsafe-swiftshader`（無いと canvas が空で返る）。
  ★★測り方の罠 … ①dispatch 直後に DOM を読むと rAF 前の値 ②遠い行を測ると
  「連鎖」の遅れを不具合と読み違える ③`waitForTimeout(40)` は 2.4 コマぶんしか
  無い ④★**同じ式から書いた2つの値を突き合わせても検証にならない** ―
  「見えている幾何」を測り、**比較できた件数を必ず出す**
