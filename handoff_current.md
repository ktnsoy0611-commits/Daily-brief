# いまどこにいるか（2026-09-18／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。★第110〜112巡に「落とし直し」を根絶、
★★★**第113〜114巡に帯の隙間をレイアウトへ作り直して「どこからでも・どこへでも
入る」を通し**（並びは**留め金**）、★★★**第115〜116巡に起動の代金を払い、形を
「ピルの積み」・大きさを「段の高さ」1つ**にし、★★★**第117〜118巡に配色を総入れ替え
して手ざわりの4つの真因を根治**、★★★**第119巡に「おすすめ3件」と Explore の詳細の
画面**を入れ、★★★**第120巡に山のスケールを緩めて Explore の遷移を作り直し**、
★★★**第121巡に提案の図形（傾き・当たり判定・体）を直して帯に文章を流した**。
★Explore の刷新（券・改札鋏）は保留（`DEV` タブごと最後に処分）。
仕様の正 … `docs/home-spec.md`（**§6 が引き下ろしの正**）／
`docs/project_knowledge.md` §3-h／`design.md`。

## 直近完了

### ★★★第121巡 ── 提案の図形／夜のおすすめ／帯の文章

> 「**提案の図形が歪んでいる／当たり判定がずれる／他の図形とめり込む／端っこが
> ずれて表示される**」「**上部におすすめが出なくなった。夜だからなら、次の日に
> 行けそうなやつを出して**」「**ピル以外に文章みたいなのも一緒に流して提案する**」

- **①歪み** … ★★★**測ったら形は正しかった** ―― 実機の写真から輪郭を拾って正規の
  点の列と突き合わせ、**半径のずれ 平均 1.1%・最大 5.1%**。違っていたのは**向き**で
  **150° 回っていた**。四つ葉も波打つ四角も**正方形に収まる形**なので傾くと読めない。
  → **提案だけ正立で固定**（`setInertia(Infinity)` を `setMass` の**あと**に。
  合図は体の `inverseInertia === 0` ―― 旗を持ち回ると `respawn` で付け忘れる）。
  ★**タスクは今までどおり傾く。**
- **②当たり判定／③塗り直す箱** … ★★★**`p.r` は「絵の半径」ではない** ―― 絵は
  **2r 四方いっぱい**なので、いちばん遠い点は `p.r × cardShapeReach`（波打つ四角
  **1.292**）。指の判定が円だったので**出っ張りを押しても掴めず、へこみで掴めた**
  （誤り 368・236/1440 → **0/1440**）。塗り直す箱も同じ倍率を掛ける。
- **④めり込み** … `PHYS_VERTS`(12) は「ピルの積み」用で、**札の形は縁に山と谷がある**
  ので谷ばかり拾う（体÷絵 … 波打つ四角 **0.900**）→ **提案だけ `OFFER_VERTS`(28)**
  （→ **1.011**）。★**タスクは 12 のまま**（費用は軸×頂点）。
- **⑤夜に帯が空** … 仕様どおりだった（`todayPick` は「今日もう閉まったものは
  出さない」）。→ **今日が1件も無いときだけ明日行けるものへ替える**。
  ★★**今日の分があるうちは混ぜない**／**そのことは帯の文章が言う**（印を付けずに
  並べると嘘になる）。
- **⑥帯の文章**（新設 `lib/bandNotes.ts`）… **面も縁も持たない地の上の字**／
  **指のイベントは素通し**／**幅を決めない**。出すのは**選ばれた3つだけ** …
  ①明日の案内 ②おすすめの理由（いちばん点の高い1件）③タスクの残り。
  ★**AI を使わない**／★**理由の判定は `offerPick`、言葉は `bandNotes`**／
  ★**上限の数に入れない**／★**`homeBand` から呼ばない**（輪になる。`HomeTab` が渡す）。
- **検証** … 22時 → 「今日はもう閉まっています。明日ならここへ行けます」＋明日行ける
  2件／14時 → その文は出ず今日の分／指の判定の誤り **0/1440**（4形）／機械チェック
  13本 0 件・`tsc`・`eslint`・`next build`・WebKit `pageerror` 0。

### ★★★第120巡 ── 山のスケール／日付の板／近さの判定／Explore の遷移

- **①スケール** … ★★★**第118巡の見立てが逆だった** ―― 実測すると**件数が増えるほど
  山は低い**（小さい図形は密に積む）。→ **`CROWD_N0` 10→18・`CROWD_MIN` 0.85→0.94**
  （4/9/14/22 件で帯からの余裕 **112/164/220/254px**・塗った面積はほぼ一定）。
- **②日付だけ変わらない** … 字は `min(幅に収まる値, 上限)` で、390 幅では**上限が
  効いている**ので `room` だけ縮めても 1px も変わらなかった → **上限にも `crowd`**
  （`min(a·c, M·c) = c·min(a, M)` ＝ どちらが効いても厳密に `crowd` 倍）。
- **③日付も掴める** … `pickAt` が板も見る。★★**`TOUCH_SLOP` を足さない**（足すと板が
  まわりを飲み込む）。★★**板には行き先が無い**（`dragRef.place`）。
- **④判定を小さく** … 1つの `RAIL_NEAR`(72) を**3つに割った** … 右端 **36**／幽霊
  `BAND_NEAR` **48**／狙い `BAND_CATCH` **24**。遊び `RAIL_HYST` **22→12**
  （★**遊びは閾値より小さく**）。
- **⑤Explore の遷移**（第119巡の全画面は**撤回**。理由は**素材** ―― 写真は 1000px 級
  なので実機の全画面では粗が出る）… **新設 `lib/cardMorph.ts`**。**両端を「中心から
  等角に測った半径の列」へ直して lerp**（`waist` と同じ作法。点の数が変わらない）。
  ★**回るのは測る向きだけ**／★★**半回転で着地**／★★★**光線が頂点をちょうど通る**
  ので `u` に **1e-9 の遊び**が要る（無いと**切れ込みが1本走る**）／★**遠い頂点より
  外は採らない**／★★★**着いたら `border-radius` の `<img>` と入れ替える**／
  ★★★**器に `opacity` を掛けない**／★★★**閉じる1フレーム目は `useLayoutEffect` で
  今すぐ書く**／★曲線は **`easeAt(EASE_SETTLE, t)`**（新設）。
- **検証** … 遷移は**最初のフレームが押した写真そのもの**・最後が
  **(16,16,358,490)・角丸 28px** に着地・閉じると札の写真へ戻って消える。

### ★★★第119巡 ── おすすめ3件を山へ／Explore の詳細の画面

- **場所の入れ替え** … 帯の上の段は**ストックの「今行ける」だけ**（`todayPick`）。
  **未読の提案は山へ**（おすすめ3件）。★押すと Explore のそのカードへ。
- **新設 `lib/offerPick.ts`** … ★**AI を使わない**。**願い 3.0 ＞ ゴール 2.0 ＞
  好みの語 0〜2.0 ＞ ドメインの KEEP 率の差 ±1.0 ＞ 新着 0.6**、**同じドメインは
  2枚まで**。★**時間帯は見ない**（ユーザー確定）。
- ★★★**CSS は `scale` を線形に補間する**ので `scale(1/s)` は途中で逆行列にならない
  （真ん中で積 2.04）。**回転と平行移動だけが厳密に打ち消し合う。**
- ★★★**`createPortal` で `body` 直下へ**（列の `transform` が独立した重なりの文脈を
  作るので **`zIndex` 70 でも nav の下に潜る**）／★★★**`--nav-h` を読まない**。

### ★★★第118巡 ── 手ざわりの4つの真因を根治／色／山のスケール

- **①** `offer-` に対応する `Item` が無く `items.find` が原理的に当たらなかった
  → **引き下ろす＝KEEP**（式は **`lib/keepCard.ts` の1か所**）。
- **②** `stepGhost` の `if (g.aim)` が**絵の座標を奪っていた** → **枝ごと削除。
  `Ghost.aim` は `boolean`**（役は `tTo` を `PILL_HINT`(0.55) へ寄せる合図だけ）。
  ★★**第109巡にユーザー確定で入れたものを本人が撤回。復活させない。**
- **③** 挿し口 `at` に**遊びが無く**、変わるたび `m.open.p = 0` に戻していた →
  **`GAP_HYST`(22) ＋ `bandSettling`**。実測 飛び **11〜12 → 5 回**／幅 150 で一定。
- **④** 落とし所に**支点**を渡していた（正は**絵の中心 `hx`/`hy`**）。**第108巡と
  同じ罠の裏返し** → ずれ **44.93px → 0.00px**。★**⑤**色／**⑥**`bandH`。

### ★★★第117巡 ── 配色の総入れ替え（承認済み）

- 参照画像（Jardins de Métis の入場券）から**画素を測って 7色**。**タスク＝無彩色の
  塗り ＋ 黒の字**（帯・山・GRAVITY・DRIFT・入力画面すべて追従）。
- **ドメイン4は色と形の組** … バショ＝緑・四つ葉／タイケン＝赤・六角形／ジョウホウ＝
  青・波打つ四角／モノ＝黄・トゲトゲ。★★★**`lib/appAccent.ts` を削除。復活させない。**

### 第113〜116巡 ── 起動の代金／形と大きさ／帯の隙間

- ★★★**GRAVITY が軽いのは「最初の画面ではない」から**（ホームは**起動の代金**を
  払っていた）… 先読みは1つずつ／見えていない `VoiceStudio` は測らない（524ms）／
  焼くのは1フレーム2枚／書体は落ちる前に頼む。実測 **フレーム 58 → 115〜127**。
- ★★★**形は「ピルの積み」**（S 字は撤回。`foldsAt`/`sideFolds` 削除）／★★★**大きさは
  「段の高さ」1つ**（`rowSpecOf`。「面積を1つ」では段が増えるほど段が痩せた ――
  幅 2.25倍 → 1.00倍・9件）／★★★**`specOf` は 1 行も変えない**。
- ★★★**隙間と席は `transform` のずれでは書けない。レイアウトが持つ** ―― 同じ id の
  ピルが2周ぶん居るので別々の値を書けない（2周目が **152px 飛ぶ**）。
  **両側 ±w/2 も片側 +w も同じ理由で直らない。**
- ★★★**帯ぜんたいを `lead`＋隙間の半分だけ左へ戻し、挿し込みで「送り（px）」へ
  畳み替える**／**境目は開く前の位置で測る**／**挿し口が移るときは受け皿 `shut` へ**／
  **開けているあいだ組み直さない**／★★★**白抜き ⇄ 塗りは `inkMix` の「量」で渡る**。
- ★★★**並びは「留め金」（`AppState.bandPins`）**。`tasks` は触らない ―― ① GRAVITY と
  ALIGN まで動く ② 声の候補とフォローアップは持ち主が別で動かせない。★**挿し口は
  index ではなく「左どなりの id」**。★**日付・曜日は黒いピルに白い文字**。

### 過去の巡の要点（詳しくは `docs/project_knowledge.md` §3-h）

- ★★★**第111〜112巡** … 落とし直しの真因は **`clearOverlap` が据え置きの山にも
  掛かっていた**こと → **`Piece.fresh` だけ**＋**体を作り直さない**／**下の段の9件の
  打ち切りが戻したタスクを消していた**／**運ぶ図形は角度を留める**。
- ★★★**第109巡 … めり込みの真因は「湧いた瞬間から重なっている」**（60ms で落ちる
  距離 2.5px に対し散らばり 200px）→ 投入の直前に `clearOverlap`（372件 → 10件）／
  ★★★**眠りは `GravityTab` と同じ**（大域の門は全削除）／`positionIterations` は 6／
  ★★★**光線は「正規化した空間」で飛ばす**／**幽霊の寿命は3重**。
- **第89〜101巡** … ★★★**canvas の `ctx.font` は読み込みを頼まない**し
  `document.fonts.ready` は**頼んでいないものを待たない**（→`ensureWordFont`。締切を
  必ず）／**形は「円をつないだ形」。正弦波で円に似せない**／**器は必ず正方形**／
  **実機のバグ ＝ `mask-size` が効かず SVG が 1:1** → `clip-path`。

## 次の一手

1. ★★★**ニュースの段を足す**（第121巡にユーザー確定・**次の巡**）… **Google
   ニュース RSS**（鍵が不要・無料・日本語。プロフィールの「好みの語」で検索して
   絞る）。★サーバー関数で取りに行って（CORS）、**帯に3段目**を作る。
   ★★**帯の段は2段という前提が `BAND_ROW`・`Band.tsx`・`bandMotion.ts` に
   入っている**ので、そこを3段へ広げるのが本体の作業。
2. ★★★**実機で見てもらう**（第118〜121巡）… ①**提案の図形が正方形に見えるか**／
   ②**当たり判定とめり込み**／③**夜のおすすめと帯の文章**／④**山の大きさ**／
   ⑤**日付の板が掴めるか**／⑥**ASSIGN と帯の判定の誤動作**／
   ⑦**写真を押したときの遷移**。
3. ★**`magazine` と `ItemStatus "planned"` を消す**（死んだ欄。`plannedFor` が正）。

## 未解決

- ★★★**提案の生成は止まったまま**（未読 41 枚が減らず `POOL_CAP`(40) に掛かる）。
  ★おすすめ3件の母集団もこの池 ―― **生成が戻るまで新着は増えない**。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も焼く予算も直していない**（山だけ）。
  ★TASK の板は「文字そのものが図形」のまま／重要度で大きさが変わるまま。
  ★**起動の代金は残っている**。★`SectionLabel` は 3.2 ＝ AA 割れ。
## 重要パス

★★**ファイルの地図と、踏んだ罠の全文は `CLAUDE.md`。ここは「どこを開くか」だけ。**

- ホーム … `HomeTab.tsx`／帯 `Band.tsx`＋`lib/homeBand.ts`＋`bandMotion.ts`／
  山 `components/home/`（`Pile`＋`pileWorld`＋`pilePaint`）／器 `lib/pileBox.ts`／
  引き下ろし `lib/pullDrag.ts`＋`pillGhost.ts`＋`AssignRail`＋`AssignSheet`＋`DateGrid`。
- 帯の中身 … **「今日行ける」`lib/todayPick.ts`**（夜は明日へ）／**「文章」
  `lib/bandNotes.ts`**（理由の判定は `offerPick`、言葉は `bandNotes`）／
  **おすすめ3件 `lib/offerPick.ts`**／**カード → `Item` は `lib/keepCard.ts` の1本**。
- Explore … **詳細 `components/explore/CardDetail.tsx` ＋ 変形 `lib/cardMorph.ts`**／
  **札の形 `lib/cardShape.ts`**（★**届く倍率 `cardShapeReach`・中か `inCardShape`**）。
- 図形 … `lib/solid.ts`（`stackOutline`）／`lib/taskSize.ts`（`rowsOf`・`rowSpecOf`）／
  `lib/wordPlate.ts`（板）／`lib/cassette.ts`＋`lib/reelHub.ts`（カセット）。
- 語彙 … 色 `lib/constants.ts` の `PALETTE`(7行)／動き `lib/motion.ts`（★**CSS に
  任せられない曲線は `easeAt(EASE_SETTLE, t)`。写経しない**）／バネ `lib/spring.ts`／
  目盛り `lib/tokens.ts`／★規約13本 `design.md`。

### ★ここだけは忘れると必ず壊れる

- **絵は `hx`/`hy`**（落とし所にも支点を渡さない）／**姿は掛け金 `Ghost.pill`**／
  **位置は最後まで指**（`Ghost.aim` は `boolean`）／**近さは3つ**（右端 `RAIL_NEAR` 36／
  幽霊 `BAND_NEAR` 48／狙い `BAND_CATCH` 24。遊び `RAIL_HYST` 12 ＝ **閾値より小さく**）。
- **帯の隙間と席はレイアウト（`.band-pad` の幅）が持つ**／**挿し口は `after`（左どなりの
  id）＋ 遊び `GAP_HYST`**／**狙われている段は流れを止める**／**位相は px で引き継ぐ**。
- **山**… **`clearOverlap` は `Piece.fresh` だけ**／**体も倍率も作り直さない**／
  **質量は同じ密度**／**`atan2` で並べてから `fromVertices`**／**dpr の上限は 2**／
  **ループは止める**（`wake()` を忘れない）／**帯が覆う高さは予算から引く**＋`crowdOf`
  （板は `room` と**上限の両方に**）／**塗るのは変わった図形の箱だけ**。
- **提案の図形** … **`p.r` は絵の半径ではない**（`cardShapeReach` 倍まで届く ―― 指の
  判定も塗り直す箱もこれを掛ける）／**正立で固定**（`setInertia(Infinity)` は `setMass`
  のあと）／**体の光線は `OFFER_VERTS`(28)**（タスクは 12 のまま）。
- **実機は 120Hz**（この環境では再現しない）／**iOS の上下の帯は解決済み。触らない**。

### 検証の作法

★★**WebKit は `executablePath` 無し／Chromium だけ `/opt/pw-browsers/chromium`**
（CPU を遅くできるのは Chromium だけ＝速さの測定はそちら）／★★**種に `keeps: []` を
入れない**（`migrate` が `items` を丸ごと捨てる。`wishes: []` は必要）／
★**`generatedDecks` は `Record<号, BriefCard[]>`**／★★**山の測定は 8〜10 秒待つ**／
★★**帯を出す種には「日付の無いタスク」が要る**／★★**帯の飛びは「見えているピル」
だけで測る**／★★**`bandBus.rows` の添字は論理の段**（0＝提案／1＝タスク）／
★★**WebKit は続けて何回も起動すると落ちる**（1プロセス1ケース）／
★★**`next dev` は初回のコンパイルに 45 秒かかる**（`networkidle` は待ち切れない。
`domcontentloaded` にして、先に1度叩いて温めておく）。
