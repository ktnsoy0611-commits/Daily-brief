# いまどこにいるか（第76巡・2026-08-31／Explore の刷新の途中）

3アプリ（JOURNAL / TASK / EXPLORE）は動いている。第68巡までで TASK は一段落。
**第69巡から Explore の刷新中。設計の正は `docs/explore-redesign.md`。**

★★新しい**券と改札鋏は `/dev/explore` と `DEV` タブにしか無い**。
**Explore の4タブはまだ手つかず**で、現状のまま動いている。
★★いま**券のデザインを選んでもらう待ち**（`DEV` タブ →「券」に5案）。

---

## Explore 刷新の骨子（`docs/explore-redesign.md` が正）

- **2本の柱**は提案と実行。それを貫く共通部品が**券**で、場所によって縮尺だけが変わる。
- 場所は3つ … 提案（1枚）／ストック（格子）／マップ（点）＋ プランを重ねる。
- **言葉は一般語**。メタファーは視覚だけ（「本日の販売は終了しました」は使わない）。
- 入鋏＝残す。**鋏痕の形は4ドメインに対応**し、3か所（券の穴／マップのノードの形／
  ストックの絞り込み）で同じ意味を持つ。
- ★**CSS の 3D 変形は使えない**（design.md 冒頭）が、**WebGL は使ってよい**。
  券と鋏は `components/explore/TicketStage.tsx` の**同じ3D空間**に居る。

---

## 直近完了

### ★★★第76巡 ── 「濁り」は好みではなく**2つのバグ**だった

ユーザー報告「色を反映させているはずなのに、すごく濁った色に見える。おそらく
テクスチャののせ方のせい」。そのとおりで、原因は測って出た。

- **図形** … `paperTexture.ts` が **赤チャンネル**を読みながら**輝度**の中央値と
  比べていた。クラフト紙は赤の平均が 175 なので**ほぼ全画素が「白」側へ倒れ**、実測
  **96% が白** ＝ 凹凸ではなく**白の薄塗り**。★★**読む量と比べる量が揃っていない**。
- **券** … `Ticket.tsx` が**茶色いカラー写真**を `multiply` 0.42 で敷いたまま
  （R×0.868 / G×0.840 / B×0.809 ＝ 13〜19% 暗く、青だけ 6pt 余計に落ちる）。
  第71巡に見本帳だけ直し、本体が取り残されていた。
- ★★**色相は動いていなかったので気づきにくかった。動いていたのは彩度。**
- ★★★直し … **平均 128 のタイルを `soft-light`**（128 が恒等点）。実測 142,884 画素で
  **ΔE 0.135 → 0.001**、彩度 −20% → **変化なし**。★canvas は透明な地にも乗るので
  `destination-in` で元の α に切り抜く。★**`multiply` は原理的に暗くしかできない**。

### ★★★第76巡 ── 網点のタイル（券と図形で同じ1枚）

- `tools/make-halftone.mjs` → `public/halftone.webp`（126px 角・8.5KB・可逆）。
- ★★**継ぎ目は消すのではなく最初から作らない** … 網点は**周期 9px 角**の格子
  （自己相関 0.94）なので**整数周期 14×9 = 126 で切る**。実測の継ぎ目 15.0 に対し
  タイル内の隣どうし 33.0 ―― 継ぎ目のほうが小さい。本数 126 / 15,750。
- ★平均は**書いてから測って戻す**（頭打ちで平均が逃げる。狙い 128 → 実測 127.99）。
  ★「券は板紙・図形は切った紙。混ぜない」は**この巡で撤回**（ユーザー指定）。

### ★★★第73→76巡 ── 有彩色 **9つ**、メイン × サブの組

- ★★★**役 → 色の対応表は `lib/palette.ts` 1か所**。色は `SCHEME`。参照画像を
  持ってくるたび**`SCHEME` を書き換えるだけ**で全部追従した（第74・75・76巡）。
- ★★★**多いタグから先に離す**（ユーザー指定「work と life と growth が多いので
  被らないように、かつパレットの雰囲気をとらえた主要な色に」）→ その3つはこの盤で
  **彩度の高い上位3色**（fiery / limeade / deepred）。見分けは最小 ΔE **0.25**、
  図形5色でも **0.22**（第75巡は 0.116）。★★★**「前の色にいちばん近い」で解かない
  こと** ―― 第75巡はそれで近い色が山に並び「図形の色の選定が良くない」と言われた。
- ★10色に対し役は11。ユーザー確定で **BLACK CHERRY が墨と SOCIAL を兼ねる**。
  ★その結果**山に出る色は5つ**（日付の板と SOCIAL が同じ色相。板は文字・図形は面）。
- ★★★**状態の色をそのまま文字に使わないこと**（この巡に踏んだ）。紙の上で本文の
  4.5 を満たすのは `RUST`(8.3) と `INK`(15.7) だけ。`GREEN`(1.2) / `BLUE`(3.1) は
  **面**として使い、載せる色は `GREEN_INK` / `BLUE_INK` から。**9か所直した。**
- 地 … `BD_GREY` `#FFFBF5`（CLOUD）／`JOURNAL_BG` `#D1EAF1`（AQUA）。
  ★`app/manifest.ts` の `background_color`/`theme_color` も同時に直す。
- ★★**紙 `#FAFAF9` と地 `#FFFBF5` の差は比 1.01**（前は 1.16）。カードは
  `SOFT_SHADOW` と罫だけで読ませている。**次に見直す第一候補**（下の未解決）。
- 検証（件数）… 比 **21/21**／10 kind すべて一致 **10/10**／旧パレットの16進 0件／
  異なり16進 61 → **51**／9タブ＋券5案＋図形の山を撮って 0エラー／機械チェック 0件。
  ★ドメイン4色の見分けは **0.057**（盤の淡い3色が三つ子なので避けられない。
  券は1枚ずつ出るのと、鋏痕の形が別の手がかりを持つ）。

### ★★★第73巡 ── 和文の 800 が出ていなかった

- `Noto Sans JP` を固定3本（400/500/700）で読んでいたため **`WEIGHT.heavy`(800) が
  和文で出ていなかった**（13箇所）。**可変で読む**ように直した（`weight` を渡さない）。
- ★★**確かめ方**：`document.fonts.check` は**近い太さへ落ちても true**。
  **描いたインクの量**を測る（和文は幅では測れない ―― どの字も 1em 幅）。
- ★**`Noto Sans JP` ＝ 源ノ角ゴシック**。源ノ角でないのは**欧文だけ**（Archivo）。
- ★★第74巡に **`WEIGHT.black`(900) を券の題**へ。`design.md` §5-2 の「中間を作らない」
  に反しないのは、900 が**中間ではなく上の端**だから。**増やさない**。
- `SERIF` / `DISPLAY` の別名を消した（中身は `SANS`。12箇所）。`--font-anton` も撤去。

### ★★★第72巡 ── 券を**組み立てから**作り直した（5案・`samples/`）

- ★★★**役と段を1対1に**。旧版も段は4つだったが、1つの役に2段・1つの段に2役だった。
  新は **20/13/11/7** で `design.md` §1 の表そのまま。★★**写真が余りを全部取る**
  （`flex: 1`）ので spacer は 0本。要約は3行。★`SOON` と `Nº` は消した。
- 5案は**色がどれだけ見えるか**で振る … N2 全面 → N1 帯 → N5 挟み → N3 額 → N4 札。
- **縦横比 3:4**。ミシン目と半券は廃止し、**上下の縁のギザギザ**が「切る」を担う。
  ★★★**間隔ではなく「数」で持つ**（`calc(100% / N)`）。px だと幅で割り切れず右端が
  切れる。★★`mask-composite` が無い環境では**ひとりでに素の矩形へ戻る**。
- **場から影と床を撤去**。**鋏の厚みは 1.85倍**（`HALF` 83／52。★`COIL_Z` 48 と
  必ず同時に動かす）。**光沢 0.85 → 0.14**（すり傷の照りは `scratch` から引く）。

### ★★★第71巡 ── DEV タブがタブバーに被っていた

- ★★★**タブバーの高さは `navHeightPx()` から引く。自分で測らない。**
  `.app-nav` の矩形は `NAV_H` と一致しない ―― **Chromium 81px／実機 132px**。
  器も `full-bleed` ＋ `100dvh` にして**器＝画面**にした。
- ★紙の質感の経緯（印刷の粒 → 網点）は `docs/project_knowledge.md` の質感の章へ。

## 改札鋏（第69巡の 9〜23巡目）

### ★★★23巡目 ── **持ち手の内側を削った**（図面のパースぶん）

`tools/clean-nipper.mts` の**規則8**。`min( 12·smoothstep(0,0.7,t), 0.30·(幅−面取り2つ) )`

- ★★★**青い線は「正面の暗い面」の上にある**。シルエットの内端ではなく**正面の面の
  内端から**測る（取り違えると右の削る量が幅の 60% と出る。実際に出た）。
  ★★**線の中心ではなく内側の縁で測る**。実測は右15／左9と食い違ったが
  **パースの見え方の差**で、ユーザー確定で**平均12**。
- 検証 … 辺の数は不変、脚の刺さり 13→4.1、描画と青い線の差は
  右 比較482件・平均 −0.1／左 513件・平均 +3.0 単位。

### ★★★22巡目 ── **パスの整理**。なぞる工程と整える工程を分けた

`図4枚 → trace-nipper.mjs → nipperShapeRaw.ts（生）→ clean-nipper.mts → nipperShape.ts（整）`

- ★**元データは `docs/archive/nipper-shape-21.ts` に凍結**（誰も import しない）。
  ★整理は**再実行できるプログラム**。形を直すときは規則の目盛りを直して走らせ直す。
- **辺 62 → 51**。★★**急な折れ（40°超）は角**。★★**スリットは最後に作る**。

### ★★★21巡目 ── **部品ごとの図**から組み立て直した（入力が4枚になった）

「2部品の重なりがおかしい」の**根本原因は入力**だった ―― 組み立て図1枚から2部品を
割っていたので、左が右に隠れて重なりが存在しなかった。入力を4枚にして解消。
段は**2つ**に。★**右の中では左は必ず薄い段**。★★**塗り残しは「いちばん近い塗り」へ**。
★★**バネの脚は輪の接線方向**。★支点は**重なりの下の端**（＝蝶番）。

### 9〜20巡目の要点（詳細は `docs/explore-redesign.md` §2-b）

- **20巡目**… 輪郭を**直線と円弧**に組み直した。22・23巡目はこの並びの上で動く。
  **19巡目**… ★**DP は開いた列に使う**。★★**段の境目は面取りしない**。
  **18巡目**… 厚みを**塗り分け**から読む。★捨てるかは**太らせる前**の画素数で。
- **17巡目**… **three.js（WebGL）へ移した**。★光の強さは **π**。回す向きの符号は
  SVG と three.js で**逆**。★**16巡目**… `lib/nipperShape.ts` は**生成物。手で直さない**。

### ★★`app/layout.tsx` のハイドレーションが壊れていた（全画面に効く修正）

`<style>` を `<html>` 直下に `precedence` 無しで置いていた（不正な HTML → 水和が
失敗し、クライアントが丸ごと動かない）。`href="tokens" precedence="default"` で解決。

## 次の一手

1. ★★**実機で `DEV` →「券」を見て、5案から選んでもらう**。選んだ1案を
   `Ticket.tsx` へ畳み、**`components/explore/samples/` をディレクトリごと消す**。
   ★「場」では鋏の質感も見てもらう。鋏の直し方は3つ … **形**＝平面図を描き直して
   `trace` から／**整理と削り**＝`tools/clean-nipper.mts` の目盛り／**厚み**＝
   厚み図か `lib/nipperRig.ts` の `HALF`。数値を散らかさない。
2. **券が上からひらりと降りてくる**（★ユーザー指定）。スキップ／入鋏のあと、
   次の1枚が上から舞い降りる。`TicketStage` は券を場所で受け取るので、
   `card.x/y` と `z` を動かすだけで書ける（★動きは `lib/spring.ts`）。
3. **入鋏とスキップの動き**（★ユーザー指定）… 鋏で穴を開ける／券をスキップする。
4. **提案（TODAY）の操作** … 鋏を掴んで券へ寄せると縁に**吸い付き**、離すと切り欠かれる。
5. **ストック（STOCK）** … 格子・鋏痕での絞り込み・期限。そのあとマップ。

## 未解決

- ★★**地 `#FFFBF5` と紙 `#FAFAF9` の差が比 1.01** ―― カードとタブバーが地に
  ほぼ溶けている。ユーザー確定「CLOUD は背景に使う」の結果。**次に決めること。**
- ★**券の小口はこの画角では1画素に満たない**（ほぼ正射影）。影も消したので浮遊の
  手がかりが無い。
- ★★**`applySteel` の計算量は未確認**（1画素あたり雑音 約55回）。**実機で滑らかに
  出るか要確認**。重ければ傷を**テクスチャに焼く**形に替えれば1画素3回で済む。
- ★**デフォルメ（持ち手を単純な幾何へ）はまだ**（計51辺）。側面図では厚みの段が
  見えない（薄い片が厚い片に挟み込まれている）。
- ★`/dev/explore` は WebGL の器を**13個**開く（`DEV` タブは1個）。実機は `DEV` で見る。
  ★バネの脚は図の細い線を拾えず**まだ手で置いている**（`COIL.legFar/legNear`）。
- 提案とストックを1画面にするか。駅データ（ekidata）の同梱可否。

---

## 重要パス

- ★Explore 刷新の設計 … `docs/explore-redesign.md`／★UI の規約と機械チェック13本
  … `design.md`（前後に必ず走らせる）／目盛り `lib/tokens.ts`／色 `lib/constants.ts`
- 券 … ★**見本帳 `components/explore/samples/`（5案。選んだら消す）**／
  旧版 `Ticket.tsx`／`PunchMark.tsx`／`lib/ticket.ts`／紙 `lib/printGrain.ts`
- ★**色の役 … `lib/palette.ts`**（ドメイン4＋タグ5＝9色）／色そのものは `SCHEME`
- ★券と鋏を同じ3D空間に置く器 … `components/explore/TicketStage.tsx`
- 鋏（★three.js）… `Nipper.tsx`（画角・動き）＋ `lib/nipperMesh.ts`（立体と質感）
  ＋ `lib/nipperRig.ts`（組み立てと光）／三面図 `NipperViews.tsx`／`app/dev/explore`
  ＋ ★形の生成 … `lib/nipperPath.ts`（手書き）→ `tools/trace-nipper.mjs` →
  `lib/nipperShapeRaw.ts` → `tools/clean-nipper.mts` → `lib/nipperShape.ts`
  （**どちらも生成物。手で直さない**）／凍結 `docs/archive/nipper-shape-21.ts`
- タスク … `components/tabs/GravityTab.tsx`／カメラ `components/tasks/TaskSpace.tsx`
  ／入口の輪 `components/CreateMenu.tsx`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite` / `fetchDirect` / `jinaSlot`）
  ／ 単体チェック `npx tsx tools/jina-check.mjs`
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちて 500）。Playwright は
  `executablePath: "/opt/pw-browsers/chromium"`、**`localhost` で開く**。
  ★**WebGL を撮るには** `--use-gl=angle --use-angle=swiftshader
  --enable-unsafe-swiftshader`（無いと canvas が空で返る）。
  ★★測り方の罠 … ①dispatch 直後に DOM を読むと rAF 前の値 ②遠い行を測ると
  「連鎖」の遅れを不具合と読み違える ③`waitForTimeout(40)` は 2.4 コマぶんしか
  無い ④★**同じ式から書いた2つの値を突き合わせても検証にならない** ―
  「見えている幾何」を測り、**比較できた件数を必ず出す**
