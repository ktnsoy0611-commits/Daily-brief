# いまどこにいるか（第70巡・2026-08-30／Explore の刷新の途中）

3アプリ（JOURNAL / TASK / EXPLORE）は動いている。第68巡までで TASK は一段落。
**第69巡から Explore の刷新中。設計の正は `docs/explore-redesign.md`。**

★★新しい**券と改札鋏は `/dev/explore` にしか無い**（本番の導線からは辿れない）。
**Explore の4タブはまだ手つかず**で、現状のまま動いている。

---

## Explore 刷新の骨子（`docs/explore-redesign.md` が正）

- **2本の柱**は提案と実行。それを貫く共通部品が**券**で、場所によって縮尺だけが変わる。
- 場所は3つ … 提案（1枚）／ストック（格子）／マップ（点）＋ プランを重ねる。
- **言葉は一般語**。メタファーは視覚だけ。「本日の販売は終了しました」のような
  言い回しは使わない。
- 入鋏＝残す。**鋏痕の形は4ドメインに対応**し、3か所（券の穴／マップのノードの形／
  ストックの絞り込み）で同じ意味を持つ。
- ★**CSS の 3D 変形は使えない**（design.md 冒頭）が、**WebGL は使ってよい**。
  券と鋏は `components/explore/TicketStage.tsx` の**同じ3D空間**に居る。

---

## 直近完了

### ★★★第70巡 ── 券と鋏を**同じ3D空間**へ（`components/explore/TicketStage.tsx`）

- ★★★**券の面は「深さだけ書いて色は書かない」板**（`colorWrite: false`）。canvas は
  券のところだけ穴が空き、**下の DOM の券がそのまま見える**（版面は無傷 ―― 実測で
  **比較 36450 件中 36181 件が素の券と一致**）。深さは書くので**鋏が奥へ回れば隠れる**。
- ★★**券の面は z = 0**。そこで「1単位 = ステージの 1px」になる距離にカメラを置いたので、
  **DOM の券は素の CSS の座標のままで必ず一致する**（投影の計算が要らない）。
- 入っているもの … 券1枚／小口（紙の厚み）／**水平な床に落ちる影**／鋏。
  ★★**束は作らない**（ユーザー確定）。**スキップしたら次の券が上から降りてくる**形にする。
- ★場の組み方（ユーザー確定）… **床は水平・券はそれに垂直に浮く・カメラは真正面**。
  床は塗らず**影だけが床を教える**。**カメラの焦点＝券の真ん中**なので券は画面の
  ど真ん中。鋏は**券の右下（被らない）**で、頭が画面の中・柄は画面外へ抜ける。
- ★★★**券の版面は DOM のまま**。ただし券の中に `zIndex` を持つ要素があるので、
  器の側で **`isolation: isolate`** を掛けて文脈を閉じる（閉じないと**文字だけが
  canvas を突き抜けて鋏の手前に浮く**）。★焼き付けは不要になった。
- ★鋏の**厚みは 2 倍**（`HALF` 45／28 → **90／56**。比は保つ。1.5倍でも薄いとの
  指摘で二度上げた）。バネの z（`COIL_Z`）も同じだけ（26 → 52）。
- ★★★**金属の質感**（`applySteel`）… 段は土台に残し、**面のむら・光沢・すり傷**を
  重ねる。`NIPPER_PAINT` に `gloss`／`shine`／`scratch` の3つの目盛り。
  ramp は**鈍い鉛色**へ寄せた。★踏んだ穴5つは `docs/explore-redesign.md` の表へ ――
  ①平らな面では光沢が一定なので**法線を揺らしてから**当てる ②段は6つなので
  **むらは段を通さず直接足す** ③等間隔の縞は**波板**に見える ④傾きを画面微分で
  取ると**線が点線に切れる**（物の座標の差分で取る）⑤うねりとすり傷は刻みが
  2桁違うので**別々の強さ**で効かせる。
- ★★鋏は**平らな面を左（券のほう）へ回す**（`yaw`。縦の軸まわり）。指定は **80°**。
  ★実測を報告済み … 80° では見えている面の幅が `cos80° = 17%` まで痩せ、頭・
  スリット・バネの形はほとんど読めない。**意図した造形**として採っている。
- 券の紙の色を **場所=黄 / 体験=橙 / 情報=空 / もの=桃** に直した（`TICKET_DOMAIN_COLOR`）。
  アプリは `lib/deckStyle.ts` で kind ごとに色を持っていて、**同じものが2色**に
  なっていた。もの→桃・情報→空は `deckStyle` と完全に一致する。
- 踏んだ穴3つ（詳細は `docs/explore-redesign.md` §3）…
  ①three.js は不透明な面を**「材質の番号 → 奥行き」**で並べる（穴あきの面に
  `renderOrder = -1` が要る）②既定の深さ比較は `LessEqual` なので**同じ深さだと
  後勝ち**（小口を1単位下げる）③地を遠くに置くと影が流れる（**横のずれ = 深さ × 2.05**）。
- `lib/nipperMesh.ts` に `appTone(法線, 光)` を足した（段の表を JS からも引ける）。
  `lib/nipperRig.ts` に `poseNipper(rig, p)`（腕とバネの姿勢。`Nipper` と共用）。

## 改札鋏（第69巡の 9〜23巡目）

### ★★★23巡目 ── **持ち手の内側を削った**（図面のパースぶん）

`tools/clean-nipper.mts` の**規則8**。`min( 12·smoothstep(0,0.7,t), 0.30·(幅−面取り2つ) )`

- ★★★**青い線は「正面の暗い面」の上にある**。右の持ち手だけ**内側の側面が明るい帯
  （29単位）として見えて**いるので、**シルエットの内端ではなく正面の面の内端から**
  測る。取り違えると右の削る量が幅の 60% と出る（実際に出た）。
- ★★**線の中心ではなく内側の縁で測る**（太さ13px＝約9.7単位）。中心だと全体に
  約5単位多く出て、削る量 0 のはずの上端でも 6 出る。
- 実測は**右15／左9**と食い違ったが**パースの見え方の差**。ユーザー確定で**平均12**。
- ★★**上限が先端を守る**（右の内側のベベルは横に11単位しか走っていない）。
- ★**削る場所は走って見つける**。判定は**両端とも内側**。★★**run の始点も動かす**。
- 検証 … 辺の数は不変、最小のくびれ 51.0／53.3、脚の刺さり 13→4.1、
  描画で測った削り量と青い線の差は 右 比較482件・平均 −0.1／左 513件・平均 +3.0 単位。

### ★★★22巡目 ── **パスの整理**。なぞる工程と整える工程を分けた

```
図4枚 → tools/trace-nipper.mjs → lib/nipperShapeRaw.ts（生）
                               → tools/clean-nipper.mts → lib/nipperShape.ts（整）
```

- ★**元データは `docs/archive/nipper-shape-21.ts` に凍結**（誰も import しない）。
- ★整理は**再実行できるプログラム**。形を直すときは規則（`tools/clean-nipper.mts`
  の先頭の目盛り）を直して走らせ直す。図を描き直しても整理はそのままかかる。
- 型と `L`/`A`/`piece` は `lib/nipperPath.ts` へ（生成物2つが共有）。
- **辺 62 → 51**。潰れた辺 3→0、軸平行 3→12、ずれ 最大 2.55（許容 6）。
- スリットは**軸平行の長方形**（左 x=155／右 x=175／底 y=−229）。走査した実測で
  溝の本体72行すべて**左 x=194・右 x=204（標準偏差 0.00）**。
- ★★**急な折れ（40°超）は角**（見ないと直角3つの長方形に円を当てる）。
  ★★**スリットは最後に作る**。★★**溝の範囲は生の `NIPPER_SLOT` の枠から取る**。

### ★★★21巡目 ── **部品ごとの図**から組み立て直した（入力が4枚になった）

ユーザー指摘「2部品が重なる部分がおかしい／リングを含む針金の形がおかしい」の
**根本原因は入力**だった ―― 16〜20巡目は**組み立て図1枚**から2部品を割っていたが、
そこでは左が右に隠れているので断片しか取れず、**2部品の重なりが存在しなかった**。
入力を4枚（右／左／左の厚み／組み立て。**同じ枠なので位置合わせ不要**）にして解消。

- 段は**2つ**に（厚45／薄28）。★**右の中では左は必ず薄い段**（塗り分けより優先）。
- **色の塗り分けはやめた**（`DIM = [0, 0]`）。段は**形**で見せる。仕掛けは残してある。
- ★★**塗り残しは「いちばん近い塗り」へ配る**（多源の幅優先）。既定を「厚い」に
  していたため、薄いはずの先端のふちだけが厚いまま立ち上がっていた（実測 956px）。
- ★★**バネの脚は輪の接線方向に入る**。輪は**閉じた1本の丸**として別に作る。
  針金は3本（輪・脚・脚）。バネ自体は**引き算**で取る。
- ★支点は**重なりの下の端**（＝蝶番）。部品ごとの図があって初めて測れる。

★数値と、踏んだ穴の一覧は `docs/explore-redesign.md`（二重丸の輪・弦が輪を横切る等）。

### ★★★20巡目 ── 輪郭を**直線と円弧**に組み直した

なぞった点の羅列をやめ、角と角のあいだを**1本の辺**にした（ユーザー「トレースで
点をとってきただけで、整理されていない」）。生成物が `L(...)` / `A(...)` の並びに
なり、22巡目の整理・23巡目の削りはどちらもこの並びの上で動いている。
踏んだ穴2つ（円弧の向き・当てはめる区間）は `docs/explore-redesign.md` の表へ。
★同巡の「段を色で見せる」（`DIM=[0,1,1]`）と脚の取り方は**21巡目に撤回済み**。

### 16〜19巡目の要点（詳細は `docs/explore-redesign.md` §2-b）

- **19巡目**… なぞった輪郭を**整形してから**モデルに入れる。★**DP は開いた列に使う**。
  ★★**段の境目は面取りしない**（面取り7に対し段差は17しかない）。
- **18巡目**… 厚みを**塗り分け**から読む。★捨てるかは**太らせる前**の画素数で。
- **17巡目**… **three.js（WebGL）へ移した**。「板に見える」原因は描き方ではなく立体
  そのものだった。段は `applyAppTones` でアプリの式に差し替え。★光の強さは **π**。
  ★回す向きの符号は SVG と three.js で**逆**。
- **16巡目**… 目測をやめてトレースへ。`lib/nipperShape.ts` は**生成物。手で直さない**。

### 9〜13巡目の教訓（詳細は `docs/explore-redesign.md` §2-b）

- **13巡目**… 機構は**ペンチ**で確定。9〜12巡目は頭を2つの塊として並べていたので
  「2本の棒」にしか見えなかった。★**厚みを削って解決しない**（12巡目）。
- 動きは `lib/spring.ts` の減衰振動＋rAF（framer-motion は入れない）。

### ★★`app/layout.tsx` のハイドレーションが壊れていた（全画面に効く修正）

`<style>` を `<html>` 直下に `precedence` 無しで置いていた（不正な HTML → 水和が
失敗し、クライアントが丸ごと動かない）。`href="tokens" precedence="default"` で解決。

## 次の一手

1. **3D の場をユーザーに見てもらう**（`/dev/explore` の「Today — 3D の場」）。
   鋏の直し方は3つ。**形**＝平面図を描き直して `trace` からやり直す／**整理と削り**
   ＝`tools/clean-nipper.mts` の先頭の目盛り（`SHAVE` など）を変えて走らせ直す／
   **厚み**＝厚み図を塗り直すか `lib/nipperRig.ts` の `HALF`。数値を散らかさない。
2. **券が上からひらりと降りてくる**（★ユーザー指定）。スキップ／入鋏のあと、
   次の1枚が上から舞い降りる。`TicketStage` は券を場所で受け取るので、
   `card.x/y` と `z` を動かすだけで書ける（★動きは `lib/spring.ts`）。
3. **提案（TODAY）の操作** … 鋏を掴んで券へ寄せると縁に**吸い付き**、離すと切り欠かれる。
4. **ストック（STOCK）** … 格子・鋏痕での絞り込み・期限。
5. ここで一度**実機で確認**（提案側が完成する）。そのあとマップ。

## 未解決

- ★**券1枚の小口は、この画角では1画素に満たない**（カメラが遠く画角が狭いので
  ほぼ正射影）。厚みを読ませたいなら **`CARD_T` を上げる**か画角を広げるしかない。
  いまは券が**影で浮いて見える**ことで奥行きを出している。
- ★**デフォルメ（持ち手を単純な幾何へ）はまだ**。`tools/clean-nipper.mts` に規則を
  足せばよい（いまは 右29・左12・左10 の計51辺）。
- 側面図では厚みの段が見えない（薄い片が厚い片に挟み込まれているため）。
  持ち手の厚みも先端まで一定のまま。絞るなら厚み図を塗り直す。
- ★`/dev/explore` は WebGL の器を**13個**開く（本番は1個）。iOS は同時に開ける数が
  少ないので、実機で確かめるときは落ちることがある。本番の導線には影響しない。
- バネの脚は図の細い線を拾えないので**まだ手で置いている**（`COIL.legFar/legNear`）。
  閉じたとき脚の先が腕から離れる。
- 提案とストックを1画面にするか、2つに分けるか。
- 駅データ（ekidata）の同梱可否。

---

## 重要パス

- ★Explore 刷新の設計 … `docs/explore-redesign.md`
- ★UI の規約と機械チェック13本 … `design.md`（作業の前後に必ず走らせる）
  ／ 目盛り … `lib/tokens.ts` ／ 色 … `lib/constants.ts`
- 券 … `components/explore/Ticket.tsx` / `PunchMark.tsx` / `lib/ticket.ts`
- ★券と鋏を同じ3D空間に置く器 … `components/explore/TicketStage.tsx`
- 鋏（★three.js）… `components/explore/Nipper.tsx`（画角・器・動き）
  ＋ `lib/nipperMesh.ts`（立体の作り方）＋ `lib/nipperRig.ts`（組み立てと光）
  ＋ ★形の生成 … `lib/nipperPath.ts`（語彙・手書き）→ `tools/trace-nipper.mjs`
  → `lib/nipperShapeRaw.ts`（生）→ `tools/clean-nipper.mts` → `lib/nipperShape.ts`
  （**どちらも生成物。手で直さない**）／元データの凍結 `docs/archive/nipper-shape-21.ts`
  ／ 三面図 `components/explore/NipperViews.tsx` ／ 確認画面 `app/dev/explore`
- タスクの本体 … `components/tabs/GravityTab.tsx`
- 入口の輪 … `components/CreateMenu.tsx`（RECORD / TASKS / SETTING）
- カメラ … `components/tasks/TaskSpace.tsx`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite` / `fetchDirect` / `jinaSlot`）
  ／ 単体チェック `npx tsx tools/jina-check.mjs`
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちてページが 500）。
  Playwright は `executablePath: "/opt/pw-browsers/chromium"`、
  **`localhost` で開く**（`127.0.0.1` だと Next が開発用の資材を弾く）。
  ★**WebGL を撮るには** `--use-gl=angle --use-angle=swiftshader
  --enable-unsafe-swiftshader` が要る（無いと canvas が空で返る）。
  ★★測り方の罠4つ … ①dispatch 直後に DOM を読むと rAF 前の値が返る
  ②遠い行を測ると「連鎖」の遅れを不具合と読み違える（**焦点の行**を測る）
  ③`waitForTimeout(40)` は 60fps に対して 2.4 コマなので**測り方のせいで**ばらつく
  ④★**同じ式から書いた2つの値を突き合わせても検証にならない** ―
  「見えている幾何」を測り、**比較できた件数を必ず出す**
