# いまどこにいるか（2026-09-07／ホームの実装に着手）

3アプリ（JOURNAL / TASK / EXPLORE）は動いている。**ホームを新設している最中。**
実装の正は `docs/home-spec.md`。段取りは同 §10。

## ★★★2026-09-07 にユーザーが決めたこと（仕様書に無い決定）

1. **新しい5色は3アプリ全部に当てる。同時にタグの色は廃止する。**
   理由は測定 ―― 新5色は SKY / STORM / SEA が**同じ色相の3段**（隔たり **5.4°**。
   前のパレットは 25.3°）で、**分類には使えない**。代わりに明度の幅が 63.2（前 44.2）
   と広いので、**役の階段**としては前より強い。だから
   **色は「役」に使い、分類（タグ）は濃さ5段（`TAG_STEPS`）で言う**。
2. **ホームは4つ目の列**（HOME → JOURNAL → TASK → EXPLORE → HOME の横循環）。
   ホームの列だけタブバーの中身が**3アプリの名前**になる。新しい仕掛けは足さない。
3. **山の図形** … **タスクは全部「角丸の四角」**（形＝入力の埋まり具合＝円/半円/三角は
   廃止）。**円＝提案／ギザギザの円＝未読の枚数は仕様どおり残す**。
   ★**大きさは「重要度 × 締切の近さ」の現行のまま**（仕様の「重要度だけ」は不採用）。

## 直近完了

### ★★★ホームの器と帯と山（§10-2〜4）

- **ホームは4つ目の列**。タブバーの中身だけが「3アプリの名前」に変わる。
  起動時はホーム。★列の仕組み（横の無限循環・地色・`--ink-on`）は足していない。
- **帯**（`components/home/Band.tsx` ／ 中身は `lib/homeBand.ts`）… 3段が
  互い違いに流れて循環。速さは `--t-amb-band-1/2/3`（44/58/72秒）**だけ**が持つ。
  ★★★**継ぎ目** … 中身を2回並べて `translateX(-50%)` するので、半分と半分は
  必ず同じ幅でなければならない。隙間を器の `gap` で作ると半分どうしの間にも
  隙間が1つ入り、`-50%` が**その半分（4px）だけずれる**。隙間は半分の内側だけが
  持ち、末尾にも同じ幅の隙間を置く。
- ★**急ぎ（フォローアップ）は出さない**（ユーザー確定）。印だけ残してある。
- **山**（`components/home/Pile.tsx`）… 今日のものだけ。matter.js。
  ★★★**転がらせない**（`inertia` を無限に）―― 自由に回る物体は積まれるうちに
  60°まで倒れ、**戻そうとすると衝突の解決と綱引きになって固まる**（実際に起きた）。
  回転を止めれば最初に与えた 3〜6°が残り、紙がずれて積まれた見た目になる。
- ★**大きな英語（曜日）は版面の柱**。物理に参加せず、**版面の幅に合わせて組む**
  （固定すると SUNDAY と WEDNESDAY で柱の太さが変わる）。
- **タスクの形は角丸の四角へ統一**（`lib/taskSize.ts` の `specOf`）。円・半円・
  三角は使わない。角丸は `RADIUS.lg`(12) で、**`unit` で伸縮させない**。

### ★★★配色の差し替え（`docs/home-spec.md` §10-1）

- `PALETTE` を**5色**へ（SKY / STORM / SEA / CREAM / ROSE。参照画像の実測値）。
  墨 `#23252B`（寒色）／地 `#FCF7F4`。
- ★★**無彩色は地と `INK` の混色で導き、比は前と同じ値に合わせた** ―― 色みだけが
  寒色へ移り、**明暗の階層は1段も動いていない**。
- **タグの色を廃止** → `TAG_STEPS`（L\* を等間隔に切った濃さ5段）。書体の対応は不変。
- **盤の赤は1つに**（新5色に赤は ROSE だけ）。`redOn()` は面によらず ROSE を返す
  ―― 関数は残す（赤が2つに戻ったとき直す場所を1つに保つ）。
- ★**危険は「面・縁・印」だけ**。ROSE は地の上 3.56 で AA を割るので、
  **エラーや削除の文言は墨で書く**（前の Magenta 6.07 は文章に使えた）。
- 検証 … **比較できた件数 3566／3566**（照合できなかった 0）。
  **寸法・文字の差 0件**／色の変化はすべて意図どおり（墨 4113・非活性 182・紙 168 …）。
  機械チェック13本 **#1〜#7・#10〜#13 は 0件**／`tsc` 0・eslint エラー 0・`build` 成功。

## 次の一手

1. ★★**実機で見る** … ①帯の速さ（44/58/72 秒。速ければ `--t-amb-band-*` を倍に）
   ②墨の値 ③山の傾きと詰まり具合。
2. **下ろす手つき**（§10-5・K1）… 帯から掴んで下ろすと地面が7本の柱に割れて立つ。
   ★ここが**帯と山をつなぐ唯一の操作**なので、次に作るならこれ。
3. **提案の丸**（§5-b）… 「今日行くと決めた提案」を表すデータが無い（下の未解決）。
4. **持ち物**（§7-b）… いまは数だけ。扇に開くのは未着手。

## 未解決（★ユーザーに聞くこと）

- ★★**「今日行くと決めた提案」を表すデータが無い** ―― `Item` は `status`
  （candidate / planned / done）しか持たず、**日付を持たない**。山の**円**が作れない。
- **帯の見本** … 実データが無い段には見本のピルが出る（`lib/homeBand.ts` の
  `SAMPLES`）。★**段取り7 で定数ごと消す。**
- 段1の写真に**デュオトーン**を掛けるか（§2-c は色が出る場所に挙げている）。
- **持ち物**は今のデータではタスクの1項目（`belongings`）で、独立した物が無い。
  「持った」の印を新しく持つ必要がある。
- **墨の値**（`#23252B` か `#2A2C33`）と**帯の速さ**は実機で決める。
- ★**Explore のドメイン4のうち「モノ」は SKY 1.67** で、大きな文字の 3.0 に届かない
  （新5色で 3.0 を満たすのは3つだけ）。**目盛りの外**として明記。券の再開時に見直す。
- ★`SectionLabel`（`MUTED`）は明るい地で 3.2 ＝ AA 割れ。**既知・未対応**（前から）。

---

## ★Explore 刷新（★保留中。いま進めない）

正は `docs/explore-redesign.md`。2本の柱は提案と実行で、貫く共通部品が**券**。
入鋏＝残す。鋏痕の形は4ドメインに対応。★**CSS の 3D 変形は使えないが WebGL は可**。
コードは消していない（`DEV` タブごと最後に処分）。

要点（詳細は `design.md` §3-b〜§3-d と `docs/archive/`）:

- ★★★**デュオトーンは「写像」**（SVG フィルタ1本＝グラデーションマップ）。
  混色（`lighten`/`darken`＝切り捨て、`multiply`/`screen`＝非線形）では**できない**。
- ★★★**暗くするのは掛け算**（`deepen`）。`shade()` は足し引きなので**色相が飛ぶ**。
- ★★★**大きな英語は版面の幅に合わせて組む**（`FitLine`。段から選ばない＝目盛りの外）。
  測る箱は **`inline-block`**。`textLength` で伸ばさない。
- ★★★**マスクした要素に `box-shadow` は出ない**。券の影は親の `filter: drop-shadow`。
- ★★★**マスクの層を小さな箱に `no-repeat` で置くと `intersect` が券を丸ごと消す。**
- ★★★**欠けの安全域は「幅の％」の縦余白1つで解ける**（`%` の padding は幅で解決）。
- ★★★**テクスチャは明暗だけを足すもので、色を変えるものではない**（`soft-light`）。
  **しわは拡大縮小してよい。網点はいけない**（テクスチャの刻みで規則が決まる）。
- ★★★**タブバーの高さは `navHeightPx()` から引く。自分で測らない**
  （`.app-nav` の矩形は `NAV_H` と一致しない ―― Chromium 81px／実機 132px）。
- ★★鋏の形は `lib/nipperShape.ts` が正だが**2段の生成物で、手で直さない**。

## 重要パス

- ホームの正 … `docs/home-spec.md`／経緯と却下案 … `docs/redesign-ideas.md`／
  参照と測定値 … `docs/redesign-references.md`（道具 `tools/extract-palette.py`）
- UI の規約と機械チェック13本 … `design.md`（作業の前後に必ず走らせる）
- 目盛り `lib/tokens.ts`／★**色そのものは `lib/constants.ts` の `PALETTE`（5行）だけ**
  ／役との対応は同ファイルの `SCHEME` と `lib/palette.ts`
  ／★**面から導く3つ** … `bodyInkOn`・`redOn`・`inkVarsOn`。**表を持たず、導かせる。**
- タスク … `components/tabs/GravityTab.tsx`／カメラ `components/tasks/TaskSpace.tsx`
  ／入口の輪 `components/CreateMenu.tsx`
- ブリーフ … `lib/briefPipeline.ts`／単体チェック `npx tsx tools/jina-check.mjs`
- 券（保留）… `components/explore/samples/`／`Ticket.tsx`／`lib/ticket.ts`
- 鋏（保留・three.js）… `Nipper.tsx`／`lib/nipperMesh.ts`／`lib/nipperRig.ts`
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` ＋ `NODE_OPTIONS=--use-env-proxy` ＋
  **`SSL_CERT_FILE`**（3つとも要る。無いと next/font/google が落ちて 500）。
  Playwright は `executablePath: "/opt/pw-browsers/chromium"`、**`localhost` で開く**。
  ★**WebGL を撮るには** `--use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader`。
