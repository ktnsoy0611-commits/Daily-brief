# いまどこにいるか（2026-09-15／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。★第109巡に**落とす瞬間の重なりを解いた**
（ユーザー確認済み）。★第110巡に**掴んでいる図形へ当たり判定を持たせ**（代理の体）、
**山の倍率を据え置き**、**帯の穴と隙間を「行き先を持つずれ」で作り直した**。
★★★**Explore の刷新（券・改札鋏）は保留**（`DEV` タブごと最後に処分）／
**試している大きな変更は1つ** … **アプリごとのアクセント配色**（`lib/appAccent.ts` の
`ACCENT_TEST`。**`false` の1行で元へ戻る**）。仕様の正 … `docs/home-spec.md`
（**§6 が引き下ろしの正**）／`docs/project_knowledge.md` §3-h／`design.md`。

---

## 直近完了

### ★★★第110巡 ── 掴んでいる図形に当たり判定／帯の穴と隙間／着地の滑らかさ

ユーザーは第109巡で「**落下の揺れやぶつかった時のバグはなくなりました**」と確認。
残っていた**引っ張り系の5点**を直した。

**③ 掴んでいる図形に当たり判定が無い** → **代理の体**（確定「山を押しのける ＋ 壁と床」）。
弾けたら matter の体を1つ world に入れ、**絵は体そのもの**（`stepGhost(mo, g, pin)`）。
- ★★★**一言でいうと「代理の体が絵の中心。追いつきのばねは毎歩そこへ繋ぎ直す」**
  ―― ばねが行き先を言い、体が行ける所を決め、**ばねの位置だけ上書きして速さは残す**
  （何にも当たっていなければ**恒等**なので二重の遅れが出ない＝「硬く」ならない）／
  **作るのは出来事ではなく条件**（`ghost && !home && !pill`。`armed` は遊びが無く、
  閾値の上で止まると**毎 pointermove で体が生まれては消える**）。
- ★★★**`clearOverlap` の「前」に消す**（world の全部を見るので、着地点に重なった
  代理を見て**本番を1体ぶん真上へ持ち上げる**＝`landing` が消したはずの跳ねが戻る）／
  **ループの停止条件に `proxyRef` を足す**（止めると代理を消せる唯一のコードも止まり、
  **見えない体が永久に刺さる**）／**締切 `HANDOFF_MS`(300) は必須**（カレンダー経由・
  早期 return・閾値未満の離上では**着地が来ない**）。

**④ 離した瞬間に画面が一気に変わる** → **`unit` を据え置く**（確定「次に開いた時に調整」）。
`buildPieces` は毎回まっさらに倍率を出すので**1枚増えれば全員が別の大きさで作り直され**、
位置は前から丸写しなので `clearOverlap` が持ち上げて**山が丸ごと浮く**。
→ `buildPieces(…, hold)`。決め直すのは**世界を作るとき**と**器が 15% 以上変わったとき**だけ。

**① 引き抜いた瞬間に穴が閉じて流れが再開しない** → **仕込む場所が遅すぎた**。`bandBump` は
`items` から消えた**あと**に走っていて、そこでは後続は**もう瞬間移動を終えている**。
→ **`armed`（まだ席が在るうち）に仕込む**＋同じ瞬間に `play()`。

**② 帯へ寄せても隙間が開かない／⑤ 上へ動かすと止まる** → **ずれに行き先を持たせた**。
第109巡の `Off` は `{p,v}` だけで `springTo(s, 0, …)` が直書き ＝ **「開いたまま静止する
隙間」が原理的に書けない**。しかも上限 24px に対し**ピルは 120〜250px**（追突は8割が
切り捨てられていた）。→ `Off = Spring & { hole, gap, wait }`、**頭打ちは帯ぜんたいだけ**。
挿し口は `bandAim()` で伝え、段の rAF が**左は −w/2・右は +w/2**（指に付いてくる）。

**ついでに潰した5つ**（詳しくは `docs/project_knowledge.md` §3-h）
- **`stepBandMotion` が1フレームに2回進んでいた**（段ごとの rAF が同じ大域の step を
  呼ぶ）→ **実時間を貯めて固定の刻み**（2度目は貯金が足りず 0 歩＝門を別に作らない。
  **120Hz の2倍速も同時に消える**）／**ずれをピルの `transform` に混ぜていた**
  （押下の縮みと 420ms の transition と同居）→ **`.band-slot`（包み）へ出す**／
  **受け渡しが DOM に載るのが1フレーム遅れて帯が飛んでいた**（実測 186.5px）→
  `useLayoutEffect` の中で `paint()` まで／**流れの位相を「割合」で引き継いでいた** →
  1周の幅が変わると帯が丸ごと飛ぶ（-302.6 → -502.8px）→ **px で持ち、下の段
  （`reverse`）は折り返す**（差 **0.8px**。幅は `lapRef` から ―― クリーンアップ時の
  DOM は**もう新しい幅**）／**`today` のピルが帯から消えなかった**（`put` は
  `plannedFor` を書くのに `pickTodayItems` が見ていなかった）→ 1行追加。

**検証（件数つき）**
- ③ 押し込み … **山の 7/7 個が動く（最大 157.4px）**・接触 4 件・めり込み 14.4px／
  壁と床の6点で**器の外へ 0px**／離して 1.6秒後に**代理は残っていない**。
- ④ 着地（比較できた 7 件）… **大きさが変わった 0 件（0.00%）**・倍率 31.234 → 31.234。
- ① 穴 … ずれの最大 **−149.7px**（行き先 −110 に 36% 行き過ぎ）・**8枚**・`paused` → `running`／
  ② 隙間 … 左右5か所で **5/5 件**中心が付いてくる／⑤ 上へ 8px ずつ6回で **6/6 件**
  （前は 4/6 で止まった）／⑦ 段が**2つ**でも慣性の頂点は **9フレーム**（1倍速）／
  ⑧ `today` のピルは引き出すと**消えた**。
- ⑨ 回帰 … 床より下 **0**／横のはみ出し **0.0px**／眠り **7/7**／8px 超のめり込み **0件**
  （最大 0.8px）／world の動く体 ＝ 山の数／Chromium と WebKit で **pageerror 0**／
  機械チェック13本 **0件**・`tsc` 0・`eslint` 0（既存の img 警告3件）・`build` 成功。

### 過去の巡の要点（詳しくは `docs/project_knowledge.md` §3-h）

- ★★★**第109巡（ユーザー確認済み）… めり込みの真因は「湧いた瞬間から重なっている」**
  ―― GRAVITY の幾何のはしごが HOME に無く、時間（60ms）で落ちる距離は **2.5px** なのに
  散らばりは **200px**（79倍）。→ `Composite.add` の直前に `clearOverlap`
  （8px 超 **372件 → 10件**）。★**板の質量も箱から**（「`GravityTab` は板に `setMass` を
  呼んでいた」という第104巡の記録は**誤り**）。★変形 `K_SWING`／吸い付き `K_CATCH` へ分離、
  **振れは絵そのものの速さ**、**図形自体の伸び縮みは削除**。
- ★★★**眠りは `GravityTab` と同じ**（`enableSleeping: true`・**大域の門は全削除**）。門は
  **全体の最大値**で測るので**1つでも転がっていれば開かない**＝誰も凍らない。代わりに
  **体を world へ入れるフレームで全員を起こす**。★**`positionIterations` は既定の 6**
  （matter は `clamp(20/n, 0, 1)` なので **6 でも 10 でも減衰は 1**。上げると
  **全力の押し戻しを4回余計に回して振動が持続する**）。
- ★★★**当たり判定の光線は「正規化した空間」で飛ばす**（px だと横長の形の丸い端が潰れて
  6頂点・体が絵より 10% 小さい → 16頂点）／**`GravityTab` の「順番どおりの間引き」は
  借りない**（重心が最大 2.87px ずれる）／**`fromVertices` へ渡す前に `atan2` で並べる**
  （交差すると SAT が「当たっていない」と答えて床をすり抜ける）／**GRAVITY は canvas を
  dpr 2。ホームだけ 3 だった**（実機 1.09M 対 **2.77M**。「2 で止めると 1.5倍」は
  当時のバグの記録で 1.5 ＝ 3÷2）。
- ★★★**ループは止める**（`runningRef`/`wake()`。**`dirtyRef` や `dragRef` を立てる所では
  必ず `wake()`** ―― 眠りが速くなったら**落ち着いた山が掴めなくなった**。自分の退行）／
  **`ghostKey()` 1か所から取る。手で並べない**／**幽霊の絵は `hx`/`hy`**／
  **姿は掛け金 `Ghost.pill`**／**境目には遊び `RAIL_HYST`**／**寿命は3重**
  （`lostpointercapture`／unmount／`window` の `pointerup`）／**隠すのは `opacity: 0`**／
  **`navHeightPx()` を rAF から呼ばない**／**塗るのは「変わった図形の箱」だけ**／
  **`stepGhost` と物理は同じ固定の刻み**（`rAF` ごとに1歩にしない）。
- ★★**測る窓を間違えない**（第105巡の「32ms 超 0 件」は「落ち着いたあと」を測っていた）／
  ★★★**実機は 120Hz。Chromium も WebKit も 60fps なのでこの環境では再現しない**／
  **沈んだだけなら `sink`**（上から落とすと「雨」）／**指の当たり判定も
  `halfWidthAtStack`**／**`setVelocity` は眠った体を起こさない**／**列は `overflowX: "clip"`**。
- **第89〜101巡** … ★★★**canvas の `ctx.font` は読み込みを頼まない**し、`document.fonts.ready` は**頼んで
  いないものを待たない**（→`ensureWordFont`。**待つなら締切**）／**中身を入れる引き金は
  「世界を作り終えた最後の1行」だけ**／**参照の形は全部「円をつないだ形」。正弦波で
  円に似せない**／**`objectBoundingBox` の器は必ず正方形**（→`fitBox`）／**実機のバグの
  正体 ＝ `mask-size` が効かず SVG が 1:1 で置かれていた** → `clip-path`／
  **切るのは写真だけ。札には掛けない**／**タグを全廃**（`isDated` だけ）／
  **ぼやけ ＝ ResizeObserver が2つ**／**下が空く ＝ 床の二重引き**（`.bleed-x-b`）／
  **effect を2本に割る**／**タブバーの高さは `navHeightPx()`**（実機 132px／Chromium 81px）。

## 次の一手

1. ★★★**実機で見てもらう**（第110巡）
   ①**引き出した図形が山を押しのけ、壁と床で止まるか**（当たり判定）
   ②**引き抜いた瞬間、後ろのピルが追突して穴を詰め、スクロールが再開するか**
   ③**帯へ近づけているあいだ、挿し口が指に付いてきて左右が開くか**
   ④**離した瞬間に画面が一気に変わらないか**（山の大きさは据え置きにした）
   ⑤**帯の中で指を上へ動かしても動きが止まらないか**
   ⑥**アクセント配色を採るか捨てるか**（捨てるなら `ACCENT_TEST` を `false`）。
2. ★**`magazine` と `ItemStatus "planned"` を消す**（★**死んだ欄**。`plannedFor` が正。
   起動時の後片付けと永続化の移行に絡むので単独の巡で）。
3. **持ち物** … 右上のアイコン＋数の印（警告）。押すと印のすぐ下に小さな一覧。
4. **フォローアップの夜間生成がまだ無い**（★プロンプトは先に文面で出して承認を得る）／
   ★ピルを押したときの詳細も無い（山の図形はもう押せる ―― `Piece.nav`）。

## 未解決

- ★★★**提案の生成は止まったまま。** 15 は**表示だけ**の上限なので裏の未読 41 枚は減らず、
  夜間の生成は `POOL_CAP`(40) に掛かり続ける（`app/api/cron/build-brief/route.ts`）。
  ★**`VoiceStudio` の `ResizeObserver` がホームでも走る**（未調査）。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も直していない**（山だけ直した）。**120Hz では
  物理が2倍速**のはずだが、塗る画素が少なく眠りも効いていて症状は出ていない。
- ★★★**帯の上の段の `offer` は引き下ろしても消えない**（第110巡に判明）。`BandItem.id` は
  `offer-<card.id>` だが、そのカードは `generatedDecks` に居るだけで **`Item` が存在しない**
  （KEEP して初めて `Item` が生まれ、同時に decision が付いて帯から消える）。つまり
  `HomeTab.put` の `items.find` は**原理的に当たらず、何も書かずに return する**。
  ★★**直すなら「引き下ろす ＝ KEEP ＋ `plannedFor`」**（`BriefTab` の KEEP と同じ `Item` を
  作り decision も記録する）。**中身の作り方が変わるので、勝手にやらない。**
  ★`today`（ストック由来）は第110巡に直した。
- ★**帯へ戻したときの挿し込み位置は「挿し口」ではなく `bandRows` の順が決める**（第110巡）。
  隙間は指に付いてくるが並びは列の順なので、受け渡しの補正が効き切らない
  （実測 … 1フレームの飛び 232px → **73.9px**）。★直すなら「落とした所で並べ替える」だが、
  **帯の順の意味が変わる**ので要相談。
- ★★**札の比は `BRIEF_CARD_ASPECT` が正**／**黄の札と地の比 1.27**（縁は**影**が持つ）／
  `SectionLabel` は明るい地で 3.2 ＝ AA 割れ／**札の中の隙間**。**未対応。**

---

## 重要パス

- ホーム … `HomeTab.tsx`／帯 `Band.tsx` ＋ `lib/homeBand.ts` ＋ `bandMotion.ts`／
  山 `components/home/`（`Pile.tsx`＋`pileWorld.ts`＋`pilePaint.ts`）／器 `lib/pileBox.ts`
- ★★★**引き下ろし … `lib/pullDrag.ts`（算数・`pullBus`・`armOffset`・`Pin`）／
  `pillGhost.ts`／`AssignRail.tsx`／`AssignSheet.tsx`／盤は `components/DateGrid.tsx`**。
  ★**手つきは「輪ゴム(44px・指と1:1) → ばちん → 変形」**／**絵は `hx`/`hy`**
  （`dx`/`dy` だけ書いても動かない）／**姿は掛け金 `Ghost.pill`**／**境目には遊び
  `RAIL_HYST`**。★★★**戻りは2段** … `RAIL_NEAR`(72) で幽霊が生まれて**指に追従**し、
  `PULL_ARM`(44) で**捕まえて** `Ghost.aim`（**段の中心は `bandRowAt` が実測**。
  空の段は描かれないので決め打ちできない）へ `K_SETTLE` で吸い付く。
  ★★★**変形 `K_SWING`／吸い付き `K_CATCH`**。掛け金は `tTo` で分ける／
  **振れは絵（代理の体が在れば体）の速さ**／**図形自体の伸び縮みは削除**。
- ★★★**帯の物理っぽい動きは `components/home/bandMotion.ts` の1か所**（第109→110巡）。
  慣性・**穴（追突）**・**隙間（指に付いてくる）**・バウンド。**`transform` だけ**／
  `data-pill-id` で**2周ぶんに同じ値**／帯ぜんたいは **`.band-shift`**、ピルごとは
  **`.band-slot`**（ピル自身には**混ぜない**）。
  ★★★**ずれには行き先が要る**（`hole + gap`）／**頭打ちは帯ぜんたいだけ**／**バネは
  `K_SWING` 1組**（`K_CATCH` は4フレームで着くので瞬間移動になる）／**穴は `armed` に
  仕込む**／**受け渡しは `useLayoutEffect` で `paint()` まで**／**`bandAim()` を通す**
  （段の rAF は止まっていることがある）／**位相は px で引き継ぎ、下の段（`reverse`）は
  折り返す**（幅は `lapRef` から。控えるのは `snapPhase()`）。
- ★★★**掴んでいる図形の代理の体**（第110巡。`Pile.syncProxy`／`pileWorld.ghostBodyOf`）
  … **絵は体そのもの**（`stepGhost(mo, g, pin)`）／**体は絵の中心**（支点ではない）／
  **`piecesRef` に混ぜない**／**`clearOverlap` の前に消す**／**停止条件に入れる**／
  **締切 `HANDOFF_MS`**。★★**山の倍率は中身が変わっても決め直さない**（`hold`）。
- ★★★**セーフエリアは全画面で `SPACE.lg`、左右対称**（`lib/pileBox.ts` の `PILE_INSET`。
  タブバーは右下の丸まで含む。**もう振らない**）／**器が変わったときの4つ** …
  `FLOOR_CHECK_MS`／`refitPile`／**`sink`**／`isLost`→`respawn`／**器は `.bleed-x-b`**／
  **ResizeObserver は1つ**。
- ★★★**当たり判定の多角形は「軸の本数 × 頂点数」**（`PHYS_VERTS` 12・等角・偶数。
  **光線は正規化した空間で飛ばす**）／**落とす順を時間で作るなら投入の直前に重なりを
  解く**（`clearOverlap`）／**質量は全部同じ密度に**（箱 ÷ `unit²`）／**`fromVertices` へ
  渡す前に `atan2` で並べる**（交差すると SAT が「当たっていない」と答えて**床をすり
  抜ける**）／**眠りは `GravityTab` と同じ**（`enableSleeping: true`・
  `positionIterations` 6・大域の門なし。**`dirtyRef` / `dragRef` を立てる所では
  必ず `wake()`**）／**タスクの形は `stackOutline`・段の数は `rowsOf(title)`・
  `waist` が「ピル ⇄ 図形」**／**塗り／輪郭は `paintShape` と `isDated`**
  （`pilePaint.ts` にも同じ分岐。**両方直す**）。
- ★**カセット `lib/cassette.ts`**／**芯 `lib/reelHub.ts`**／**札の形 `lib/cardShape.ts`**
  （点の列が正。`PUNCH_BY_DOMAIN` から導く。`clip-path`・器は必ず正方形）／
  **文字の板 `lib/wordPlate.ts`**／**帯の「今日行ける」 `lib/todayPick.ts`**／
  ★★★**書体は3つ**（`SANS`／`LATIN`／**`DISPLAY`**。canvas は
  `font-variation-settings` を受けない）／★**色は `lib/constants.ts` の `PALETTE`
  （6行）だけ**（役は `SCHEME` と `lib/palette.ts`。面から導く `bodyInkOn`・`redOn`・
  `inkVarsOn`）／★規約13本 `design.md`／目盛り `lib/tokens.ts`／動き `lib/motion.ts`。
- 検証 … `localhost` で開く。★★★**WebKit がこの環境で動く**（`executablePath` 無し。
  **Chromium だけ** `/opt/pw-browsers/chromium`。CPU を遅くできるのは Chromium だけ）／
  ★★★**種に `keeps: []` を入れない**（`migrate` が **`items` を丸ごと捨てる**。
  `wishes: []` は**必要**）／★★**山の測定は 8 秒待つ**／★**dpr の上限は 2**／
  ★★**帯を出す種には「日付の無いタスク」が要る**（全部に `dueDate` を付けると帯が空に
  なり、`bandBottom()` が 0 を返して**戻しの判定にいちども入らない**）／
  ★★**帯のピルを掴む試験は「画面の中に居る」ものを選ぶ**（流れているので外に居る）。
