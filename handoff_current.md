# いまどこにいるか（第72巡・2026-08-31／Explore の刷新の途中）

3アプリ（JOURNAL / TASK / EXPLORE）は動いている。第68巡までで TASK は一段落。
**第69巡から Explore の刷新中。設計の正は `docs/explore-redesign.md`。**

★★新しい**券と改札鋏は `/dev/explore` と `DEV` タブにしか無い**。
**Explore の4タブはまだ手つかず**で、現状のまま動いている。
★★いま**券のデザインを選んでもらう待ち**（`DEV` タブ →「券」に5案）。

---

## Explore 刷新の骨子（`docs/explore-redesign.md` が正）

- **2本の柱**は提案と実行。それを貫く共通部品が**券**で、場所によって縮尺だけが変わる。
- 場所は3つ … 提案（1枚）／ストック（格子）／マップ（点）＋ プランを重ねる。
- **言葉は一般語**。メタファーは視覚だけ。「本日の販売は終了しました」のような
  言い回しは使わない。
- 入鋏＝残す。**鋏痕の形は4ドメインに対応**し、3か所（券の穴／マップのノードの形／
  ストックの絞り込み）で同じ意味を持つ。
- ★**CSS の 3D 変形は使えない**（design.md 冒頭）が、**WebGL は使ってよい**。
  券と鋏は `components/explore/TicketStage.tsx` の**同じ3D空間**に居る。

---

## 直近完了

### ★★★第72巡 ── 券を**組み立てから**作り直した（5案）

> 全くオーガナイズされていません。並列に情報が並んでいるだけで、写真も小さいし、
> 無駄が非常に多いです（…）design.md なども参考にもう一度作り変えてください（ユーザー）

参照は**現代のカードUI 3枚**（構成・配置・文字の大きさ・情報の整理だけを見る）。
役と段の表は `docs/explore-redesign.md` §2。

- ★★★**役と段を1対1にした**。旧版も段は4つだったが、**1つの役に2段**（対ラベルが
  micro＋nano の2行）、**1つの段が2つの役**（要約と欄の値がどちらも body）だった。
  新は **20 / 13 / 11 / 7**。★★`design.md` §1 の表そのままなので、
  **要約（13・細い）より会期と会場（11・太い）のほうが小さくて太い**
  ―― 文章の段とラベルの段は性格が違う。
- ★★**写真が余りを全部取る**（`flex: 1`）。spacer は **2本 → 0本**。要約を3行に
  したぶん写真は **43〜49%**、崩れない下限は 220px → **260px**。
- ★★**ラベル列（64px）を捨てた**。日付は**罫で囲った升**に（ラベル4行 → 0行）。
- ★★**`SOON` と `Nº 0143` を消した**（ユーザー指定「役割がよく分かりません」）。
  券らしさは**ギザギザ**が担うので記号は要らない。
- ★★**題は `display`(26) → `head`(20)**。26 では和文が9文字で折り返し、2行目に
  1文字だけ残る。主役は**写真**なので、題が最大の文字であれば足りる。
- 5案は**色がどれだけ見えるか**で振ってある（ユーザー指摘「アクセントカラーしか
  見えないデザインしかない」）… **N2 全面**（板だけ）→ **N1 帯**（下半分）→
  **N5 挟み**（上下2か所）→ **N3 額**（縁）→ **N4 札**（7割）。
  ★N2 の色の板は**紙の縁から離す** ―― 縁まで伸ばすと N1 と見分けが付かない。

### ★★★第72巡 ── 縦横比 **3:4**、紙の形は**上下のギザギザ**

- `TICKET_ASPECT` を **13/21（1.62倍）→ 3/4（1.33倍）**。数で要るときは
  `TICKET_H_PER_W`。★現行の `Ticket.tsx` は差し替わるまで**自分で旧い比を持つ**。
- **ミシン目と半券は廃止**。上下の縁の**ギザギザ**が「切る」を担う。
- ★★★**間隔ではなく「数」で持つ**（`calc(100% / N)`）。px の間隔だと券の幅で
  割り切れず**右端の1個が切れる**（ユーザー指摘「割り付けが合っておらず汚い」）。
- ★★上下の穴のマスクを **`intersect`** で掛け合わせる。**`mask-composite` が無い
  環境ではひとりでに素の矩形へ戻る**（既定の `add` が穴を埋める。JS の判定が不要）。

### ★★★第72巡 ── 場から**影と床を消し**、鋏をマットにした

- **影と床を撤去**（`ShadowMaterial` 2枚と `renderer.shadowMap` ごと）。
- **厚みは 2.0倍 → 1.85倍**（`HALF` 83／52）。★★**`HALF` と `COIL_Z` は必ず
  同時に動かす**（26 × 1.85 = 48）。片方だけだとバネが本体から浮く。
- **光沢 `gloss` 0.85 → 0.14**。★★すり傷の照りは `gloss` ではなく **`scratch`**
  から引く ―― そうしないと光沢を落としたとき傷まで消える。
- ★GLSL のコメントにバッククォートを書かない（**4度目**で踏んだ）。

### ★★★第71巡 ── 紙の質感を「**印刷の粒**」へ（券だけ）

`tools/make-grain.mjs` → `public/print-grain.webp`（128px 角）。知っているのは
`lib/printGrain.ts` の1か所だけ。**白色雑音から低い刻みを引く**＝ムラが消えて粒が残る。

- ★★**タイルの中央は白のすぐ下（247）**。真ん中（128）で作ると multiply で
  地が半分まで沈む（クリームが 250 → 222 の灰色になった）。
- ★★★**1画像画素 = 1デバイス画素**（`background-size: 128 / dpr`）。等倍だと
  3x の実機で粒が3倍に太り「砂」に見える。ぼかしを**輪**で掛けたので継ぎ目が無い。
- 実測（紙そのもの）… **沈み 15.5% → 3.8%／粒 3.53 → 13.53／ムラ 1.73 → 0.36**。
- ★★**タスクの図形のクラフト紙は触らない**（第65巡にユーザー確定）。

### ★★★第71巡 ── DEV タブが実機で下の UI に被っていた

★★★**タブバーの高さは `navHeightPx()` から引く。自分で測らない。**
`.app-nav` の矩形は `NAV_H`（`77px + max(4px, safe-area-bottom × 2.382 − 26px)`）と
一致しない ―― **Chromium 81px／実機 132px**。開発機では気づけないずれだった。
器も `full-bleed` ＋ `100dvh` にして**器＝画面**に（`absolute; inset: 0` だと祖先の
箱で大きさが決まる）。DEV タブは「券」（見本帳）と「場」（3D）を切り替える。

## 改札鋏（第69巡の 9〜23巡目）

### ★★★23巡目 ── **持ち手の内側を削った**（図面のパースぶん）

`tools/clean-nipper.mts` の**規則8**。`min( 12·smoothstep(0,0.7,t), 0.30·(幅−面取り2つ) )`

- ★★★**青い線は「正面の暗い面」の上にある**。シルエットの内端ではなく**正面の面の
  内端から**測る（取り違えると右の削る量が幅の 60% と出る。実際に出た）。
  ★★**線の中心ではなく内側の縁で測る**。
- 実測は右15／左9と食い違ったが**パースの見え方の差**。ユーザー確定で**平均12**。
- 検証 … 辺の数は不変、脚の刺さり 13→4.1、描画と青い線の差は
  右 比較482件・平均 −0.1／左 513件・平均 +3.0 単位。

### ★★★22巡目 ── **パスの整理**。なぞる工程と整える工程を分けた

`図4枚 → trace-nipper.mjs → nipperShapeRaw.ts（生）→ clean-nipper.mts → nipperShape.ts（整）`

- ★**元データは `docs/archive/nipper-shape-21.ts` に凍結**（誰も import しない）。
- ★整理は**再実行できるプログラム**。形を直すときは規則の目盛りを直して走らせ直す。
- **辺 62 → 51**。★★**急な折れ（40°超）は角**。★★**スリットは最後に作る**。

### ★★★21巡目 ── **部品ごとの図**から組み立て直した（入力が4枚になった）

「2部品の重なりがおかしい」の**根本原因は入力**だった ―― 組み立て図1枚から2部品を
割っていたので、左が右に隠れて重なりが存在しなかった。入力を4枚にして解消。

- 段は**2つ**に。★**右の中では左は必ず薄い段**。★★**塗り残しは「いちばん近い塗り」へ**。
- ★★**バネの脚は輪の接線方向**。★支点は**重なりの下の端**（＝蝶番）。

### ★★★20巡目 ── 輪郭を**直線と円弧**に組み直した

点の羅列をやめ、角と角のあいだを**1本の辺**に。22・23巡目はこの並びの上で動いている。

### 9〜19巡目の要点（詳細は `docs/explore-redesign.md` §2-b）

- **19巡目**… ★**DP は開いた列に使う**。★★**段の境目は面取りしない**。
- **18巡目**… 厚みを**塗り分け**から読む。★捨てるかは**太らせる前**の画素数で。
- **17巡目**… **three.js（WebGL）へ移した**。★光の強さは **π**。
  ★回す向きの符号は SVG と three.js で**逆**。
- **16巡目**… `lib/nipperShape.ts` は**生成物。手で直さない**。
- **13巡目**… 機構は**ペンチ**で確定。★**厚みを削って解決しない**。

### ★★`app/layout.tsx` のハイドレーションが壊れていた（全画面に効く修正）

`<style>` を `<html>` 直下に `precedence` 無しで置いていた（不正な HTML → 水和が
失敗し、クライアントが丸ごと動かない）。`href="tokens" precedence="default"` で解決。

## 次の一手

1. ★★**実機で `DEV` →「券」を見て、5案から選んでもらう**。選んだ1案を
   `Ticket.tsx` へ畳み、**`components/explore/samples/` をディレクトリごと消す**。
   ★「場」では鋏の質感と滑らかさも見てもらう（下の未解決を参照）。
   鋏の直し方は3つ。**形**＝平面図を描き直して `trace` からやり直す／**整理と削り**
   ＝`tools/clean-nipper.mts` の先頭の目盛り（`SHAVE` など）を変えて走らせ直す／
   **厚み**＝厚み図を塗り直すか `lib/nipperRig.ts` の `HALF`。数値を散らかさない。
2. ★★**色と書体の精査**（ユーザー指定「アプリ全体を見ながら決めていきます」）。
   選ぶための**比較盤**を作る。★いまの `SCHEME` は仮。★クリームの温度も
   ここで決める（参照のクリームは `PAPER` #FAFAF9 より暖かい）。
   ★書体は `app/layout.tsx` が7つ読み込んでいるが、`SANS`/`LATIN` が使うのは
   **Archivo と Noto Sans JP だけ**（`anton` は使い手 0 件）。整理も一緒に。
3. **券が上からひらりと降りてくる**（★ユーザー指定）。スキップ／入鋏のあと、
   次の1枚が上から舞い降りる。`TicketStage` は券を場所で受け取るので、
   `card.x/y` と `z` を動かすだけで書ける（★動きは `lib/spring.ts`）。
4. **入鋏とスキップの動き**（★ユーザー指定）… 鋏で穴を開ける／券をスキップする。
5. **提案（TODAY）の操作** … 鋏を掴んで券へ寄せると縁に**吸い付き**、離すと切り欠かれる。
6. **ストック（STOCK）** … 格子・鋏痕での絞り込み・期限。そのあとマップ。

## 未解決

- ★**券1枚の小口は、この画角では1画素に満たない**（ほぼ正射影のため）。厚みを
  読ませるには `CARD_T` を上げるか画角を広げるしかない。★第72巡に影を消したので、
  いまは券が「浮いている」手がかりが無い。
- ★★**`applySteel` の計算量は未確認**。1画素あたり雑音を約55回引く（傷の傾きを
  物の座標の差分で取るため）。**実機の iPhone で滑らかに出るか要確認**。
  重ければ、傷を**テクスチャに焼いて三面から引く**形に替えれば1画素3回で済む。
- ★**デフォルメ（持ち手を単純な幾何へ）はまだ**（いまは計51辺）。側面図では
  厚みの段が見えない（薄い片が厚い片に挟み込まれているため）。
- ★`/dev/explore` は WebGL の器を**13個**開く（`DEV` タブは1個）。実機で見るときは
  **`DEV` タブのほう**を使う。★バネの脚は図の細い線を拾えないので**まだ手で置いて
  いる**（`COIL.legFar/legNear`）。閉じたとき脚の先が腕から離れる。
- 提案とストックを1画面にするか。駅データ（ekidata）の同梱可否。

---

## 重要パス

- ★Explore 刷新の設計 … `docs/explore-redesign.md`／★UI の規約と機械チェック13本
  … `design.md`（前後に必ず走らせる）／目盛り `lib/tokens.ts`／色 `lib/constants.ts`
- 券 … ★**見本帳 `components/explore/samples/`（3案。選んだら消す）**／
  旧版 `Ticket.tsx`／`PunchMark.tsx`／`lib/ticket.ts`／紙 `lib/printGrain.ts`
- ★券と鋏を同じ3D空間に置く器 … `components/explore/TicketStage.tsx`
- 鋏（★three.js）… `Nipper.tsx`（画角・動き）＋ `lib/nipperMesh.ts`（立体と質感）
  ＋ `lib/nipperRig.ts`（組み立てと光）／三面図 `NipperViews.tsx`／`app/dev/explore`
  ＋ ★形の生成 … `lib/nipperPath.ts`（手書き）→ `tools/trace-nipper.mjs` →
  `lib/nipperShapeRaw.ts` → `tools/clean-nipper.mts` → `lib/nipperShape.ts`
  （**どちらも生成物。手で直さない**）／凍結 `docs/archive/nipper-shape-21.ts`
- タスク … `components/tabs/GravityTab.tsx`／カメラ `components/tasks/TaskSpace.tsx`
  ／入口の輪 `components/CreateMenu.tsx`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite` / `fetchDirect` / `jinaSlot`）
  ／ 単体チェック `npx tsx tools/jina-check.mjs`
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちて 500）。Playwright は
  `executablePath: "/opt/pw-browsers/chromium"`、**`localhost` で開く**。
  ★**WebGL を撮るには** `--use-gl=angle --use-angle=swiftshader
  --enable-unsafe-swiftshader`（無いと canvas が空で返る）。
  ★★測り方の罠 … ①dispatch 直後に DOM を読むと rAF 前の値 ②遠い行を測ると
  「連鎖」の遅れを不具合と読み違える ③`waitForTimeout(40)` は 2.4 コマぶんしか
  無い ④★**同じ式から書いた2つの値を突き合わせても検証にならない** ―
  「見えている幾何」を測り、**比較できた件数を必ず出す**
