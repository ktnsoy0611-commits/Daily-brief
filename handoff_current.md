# 現在地（2026-08-18）

現行仕様は `docs/project_knowledge.md` が正。経緯は `docs/archive/`。
このファイルは**常に200行以内**に保つ（更新手順は `CLAUDE.md` の「恒久ルール」）。

- ブランチ … `claude/brief-tab-bind-bugs-9ijd62`。`main` も同じ先頭へ早送り済み
  （Vercel の本番ブランチ。以後もユーザーの許可を得てから統合する）。

---

## いまどこにいるか

タスクアプリ（TASK）は**図形**（断面ひとつのベタ塗り）と**入力画面**の両方が
確定した。入力は TickTick 型の即時入力（チャコール地・ツールバー＋ポップオーバー）で、
書いた情報の量がそのまま図形になる。3D・アイソメは持たない。
ネーム／タグの切り替えは載る文字が変わるだけで、山は動かない。

3 つのアプリのうち、**ブリーフ（EXPLORE）は実運用中**（夜間 Cron が毎日 2 回
デッキを生成し、実機で使えている）。**タスクとジャーナルは UI が出来た段階**で、
中身の運用（Cowork の仕分けとの往復）はこれから。

---

## 直近で完了したこと

- タスク: **文字を打つ面を `contenteditable` へ**（`components/tasks/EditableLine.tsx`）。
  狙いは iOS のキーボード上の **`^ ∨ ✓` の操作バーを出さない**こと
  （消す Web の API は無く、フォーム部品をやめる以外に手が無い）。
  ★実機で消えるかは未確認。効かなければこのファイルごと戻せる形にしてある。
  ついでに**変換中（IME）の Enter で手順が割れる**既存の不具合も直した
- タスク: **時刻のダイアルの「反応が遅い」を根治**。真因は再レンダー —
  中央の値が変わるたび **120＋60 個のマスの inline style を書き換えて**いた。
  薄れは CSS の `mask`、選択は中央のピルに任せ、マスを `useMemo` ＋
  `Column` を `memo`（`value`/`onPick` は比較から外す）。実測 **0 回**
- タスク: **ホバーの幅を中身に合わせた**（`Float` の `fit`。358 → **182px**）。
  相手の中心へ寄せ、シートの縁 4px 手前で止める。カレンダーは幅いっぱいのまま
- タスク: **入力画面の出入りを滑らかに**。器の `height`/`top` に
  **transition 260ms**（キーボードの最中は resize が数回しか飛ばず、
  素のままだと 1〜2 段で跳ねる）。出るときは `tc-shell-in` の薄さだけ
- タスク: それを安くするため **`ShapeStage` は見た最大の箱を持ち続ける**
  （canvas の寸法も倍率も動かさない ＝ **焼き直しゼロ**）。舞台だけが縮み、
  中央寄せの絵は自然に上へ動く。測るのは `contentRect`
  （`getBoundingClientRect` は出るときの scale が乗って 7% 小さい箱になる）
  → 実測（4× 絞り・温めて）開く **204ms** / 和文11文字 97ms / 日程シート **430ms**
- タスク: **第8巡の「合成だけで追従」を撤回**（実機で入力画面が崩れた）。
  器は `visualViewport` の矩形そのものへ戻した。崩れた理由は iOS の
  スタンドアロン特有の2点 — ①`top` に `offsetTop` を入れて高さを固定すると
  ページが少しでもスクロールした瞬間に下がはみ出す ②**レイアウトビューポート
  自体がキーボードで縮む**ので `innerHeight - vv.height` が 0 になり、
  持ち上げるべきものが持ち上がらない。
  ★検証は**画面ごと縮める場合と `vv.height` だけ縮める場合の両方**で行う
- タスク: drift の**左右の見切れ**を解消（`SOLID_W 300→230` / `SPREAD 215→140`）。
  輪を一周させて全位置で実測（12〜377px / 画面 390px）

- タスク: **キーボード追従を「レイアウト」から「合成」へ**組み替えた。器の
  高さは `100lvh` のまま**二度と書かない**。キーボードのぶんは `--kb` に入れ、
  ドックを `translateY(-kb)`・図形を `translateY(-kb/2)` で動かす。
  実測: 器の height の書き換え 0 回・帯の再レンダー 0 回・帯の下端＝見えている下端
- タスク: 帯の下余白を **CSS だけ**で切り替え（`env() - var(--kb)`）。
  帯の下に同じ色を 60vh 伸ばし、動いている最中の隙間も出ないようにした。
  タイトル欄とアイコンのすき間は 0 → **15px**
- タスク: **時刻のダイアルが一周する**（5周ぶん並べ、止まったら見た目を
  動かさずに中央の周へ差し戻す）。**待たずに確定**（指が動いているあいだに変わる）
- タスク: 期間のカレンダーで**開始・終了の両方と、あいだの日**が見えるように。
  **今日は塗りの円**（囲みをやめた）
- 全体: **無題の図形**を断った。「＋」が題の空いたまま保存していたのが原因で、
  ダミーではなく実データの残骸。`migrate()` が読み込み時に捨て、背面での
  保存も題が空ならしない
- タスク: 出入りを対にした（入力画面・ポップオーバー・日程シートの閉じる動き、
  日を選んだときの弾み）

- タスク: **時刻のダイアルが操作できない**のを直した（第6巡で入れた `Press` の
  副作用。**非passiveな touchstart の preventDefault はスクロールも打ち消す**）。
  値は `<span>` に戻し、iPhone のアラームと同じ **5行**見えるように
- タスク: **ホバーは押した相手の隣**（下に入らなければ上へ返す。すき間6px）。
  以前は見出しのすぐ下に固定で、下端の「時刻」から遠かった
- タスク: **シートの高さを固定**（期日⇄期間で**上端が動かない**）。中身は上から
  並べる。ホバーもシートの中に必ず収まる
- タスク: **キーボードとアイコンの隙間**を解消。iOS はキーボードが出ていても
  `env(safe-area-inset-bottom)` を 34px のまま報告するので、出ている間は使わない
- タスク: カレンダーを**月曜始まり**に

- 全体: **文字が出ない／アプリ全体が不安定**を根治（第6巡）。原因は3つ重なり:
  ① `loadingdone` のたびにアトラスを**全消し**していた（断片は100以上あるので
  焼き切る前に振り出しへ戻り、rAF も止まらない）→ **400msにまとめて届いた面
  だけ**捨てる ② 判定キャッシュに「取りに行く前の偽」が焼き付いて**焼き直しの
  通知が一度も飛ばなかった** → 生の `check()` で比べる ③ **門が閉じたまま固まる**
  → **600msで必ず開ける**（`textDrawable`）。`GLYPH_BUDGET` 1→4。
  ★実機（WebKit）の再現は `document.fonts.check` を偽で固定する。
  修正前 125色・文字の画素19 → 修正後 336色・6779
- タスク: **日程だけキーボードを閉じる**方針へ（ユーザーが軌道修正）。
  アイコンを押すと閉じてシートが立ち上がり、左上 ✕ で取り消し・右上 ✓ で確定。
  どちらも**その pointerdown の中で同期的に focus** して 180msで下へ抜ける
- タスク: 日程のシートを**参照画像(TickTick)の寸法で作り直し**。ピルは幅200pxを
  中央／早押し（今日・明日・今週末）／月は太字で中央／1週40px・丸34／
  終日ONは1枚のカードを2つに割る／アクセントは**そのタスクのタグの色**
- タスク: **キーボードが閉じない**を4段構えに（`<div role=button>`＋非passiveな
  touchstart ＋ 面の onBlur ＋ layout effect の受け皿）
- タスク: キーボードのガクつきを解消。`visualViewport` の矩形を
  **setState ではなく ref で style へ直接書く**（resize 20回で再レンダー0回）
- 全体: **境目**を根治。積む地色は「その画面のいちばん下に見えている面の色」＝
  入力画面は `LIFT`（帯の色）。全タブ＋入力画面で「最下1pxの色 == html の色」
- タスク: **入力画面はどこも縦スクロール不要**（器 440〜844px で実測。
  溢れそうなときはカレンダーの1週が痩せる）。帯 96px → **81px**
- タスク: カレンダーの**指の軸を最初の8pxで決める**（縦の指で月が動かない）／
  **送ったあとの逆走を消した**（戻す1フレームだけ transition を切る）／
  **曜日の行は送らない**／**1週の高さを残りから決める**（常に6週・18〜30px）
- 全体: **地色の持ち主を `lib/ground.ts` 1つに**（html の背景 ＋ theme-color を
  セットで書く）。「キーボードのところ／全タブの下端で背景が途切れる」の
  正体は、固定の `themeColor: #1A1A18` を iOS が自分で塗る領域に使っていたこと
- タスク: 形と大きさの確定（丸/半円/三角/四角・比を保つ・期限で 2.2〜0.45 倍・
  混雑したら縮めずに間引く）
- タスク: 図形の見た目の確定（グリフのアトラス／サイズのバケツ／LOD／
  カラースキームの差し替え／タグと色・書体の一対一／タグ無しの図形を無くす）
- ジャーナル: 声の記録を巨大な円 2 つのダイヤル UI で確定（§38〜§49）
- ブリーフ: 生成を 3 層→2 層へ再構築。0 枚問題を解消し、1 日 2 回・各 10 枚前後で稼働

---

## 次に着手すること

1. **実機（iOS Safari）での確認**
   - **文字が出るか**（第6巡の本題）。Chromium では `check()` を偽で固定しても
     2〜4 秒で全部出る。実機は WebKit なのでここが本番。
   - **キーボードが閉じないか**（重要度・タグ・メモ・持ち物を各3回開閉しても
     Chromium では `activeElement` が textarea のまま。強制 blur も受け皿が戻す）
   - **日程のシート**（押した瞬間に閉じて立ち上がるか / ✓✕ で即キーボードが
     戻るか / 一画面に収まるか）
   - **境目**（キーボードの次候補のあたり・画面の下端）
   - カレンダーの横スワイプの手ざわり（軸の判定 8px・逆走なし）
   - 図形 16 個ぶんの落下の重さ（Chromium・4× 絞りでは合計 1300ms 前後 / 最大 190ms）
     入力画面は 開く 204ms / 和文11文字 97ms / 日程シートを開く 430ms（4× 絞り）
     ★数字は**温まってから**測ること。サーバーを起こした直後の1回目は
     1.5〜2倍出る（518 / 218 / 1054 を踏んだ）。
     ★日程シートが重いのは要素数（6週×3か月）。プロファイラでは JS の自己時間は
     ほぼ無く、大半が browser 側のレイアウトと描画。詰めるならまず要素数。
   - 間引きの手ざわり（40 件で 10 件しか出ない。隠れたタスクに触れないのが不便でないか）
   - 期限による大きさの差（今日 2.2 倍〜先 0.45 倍）が強すぎないか
   - **時刻のダイアルの手ざわり**（一周するか・遅れないか。実機は慣性
     スクロールがあるのでここが本番）
   - **入力画面の出入りが滑らかか**（器の transition 260ms ＋ 焼き直しゼロ。
     3 度差し戻されているところ）
   - ★**キーボードの上の `^ ∨ ✓` のバーが消えたか**（`contenteditable` に
     した狙い）。あわせて**打ち心地**（キャレットが飛ばないか・変換で手順が
     割れないか・選択と貼り付け）。ここが駄目なら `EditableLine.tsx` ごと戻す
   - **ホバーの位置**（時刻の行のすぐ上に出るか）と、シートの上端が
     期日⇄期間で動かないこと
2. **Cowork のプロンプト更新**（`COWORK-ROUTINES.md`）… 候補の項目を
   `いつ`（★**日付で書かせる**。YYYY-MM-DD）/ `道具・場所` / `持ち物` /
   `タグ`（英字 5 つ）へ揃える。日付で書かれないと期日にならず、
   `lib/inboxImport.ts` がメモへ回してしまう。
   ★**プロンプトの全文を提示して承認を得てから**実装する。
3. **`main` への統合の判断**（Vercel の本番ブランチ。ユーザーに確認する）

---

## 未解決・持ち越し

- **実機 Safari の未検証**（この環境で WebKit を動かせないため構造的な制約）
  … タスクアプリ全般、ジャーナルの円のドラッグとマイクの解放、
  `.full-bleed` でタブバーの下へ潜る見え方。
- **候補のうちジャーナル・ウィッシュ・ストックの 3 種の行先が未定**
  （データとしては `AppState.inbox` に残っている）。
- **1 日の終わりに 3 アプリの要素を 1 枚のポスターへプレスする**という
  最終出力（§38 の体験の構造）は未着手。
- **完成時に撤去するもの** … `lib/taskDemo.ts` と「デモを入れる」ボタン、
  `ProfileTab` の「ブリーフ生成の実験」カードと「今すぐ生成」。

---

## いま触っている領域のファイル

```
components/tabs/GravityTab.tsx    落として積む（matter.js・描画ループ・焼き予算）
components/tabs/DriftTab.tsx      候補の円環カバーフロー
components/tasks/TaskComposer.tsx 入力画面（下書き・キーボード・行の分割・重なり）
components/tasks/WhenSheet.tsx    日程（★キーボードを閉じるシート。TickTick型）
components/tasks/ComposerFields.tsx 重要度 / タグ / テキスト
components/tasks/ComposerToolbar.tsx 下の帯のアイコン5つ
components/tasks/Popover.tsx      ポップオーバーの器と ★`Press`(押せる面)
components/tasks/EditableLine.tsx ★文字を打つ面(contenteditable。キャレット・IME・貼り付け)
components/tasks/SolidCanvas.tsx  図形1つを描く
lib/ground.ts                     画面の地色（html の背景 ＋ theme-color）
lib/solid.ts                      幾何（純粋関数）
lib/solidPaint.ts                 図形の描画＋ビットマップのキャッシュ・LOD
lib/textFit.ts                    グリフのアトラス・★書体の門(`textDrawable`)・`layoutInShape`
lib/taskSize.ts                   面積=重要度×期限 / 比ははしご(四角だけ) / 落下順
lib/taskTags.ts                   5つのタグ → 色の組と書体・`resolveTag`
lib/inboxImport.ts                Cowork の候補を取り込む（`いつ`を日付として読む）
app/layout.tsx                    和文フォントの読み込み（★1書体1ウェイトまで）
```

## ユーザー側の作業（アプリの外）

- Cowork に**毎晩のタスク「1 日を仕分ける」**を設置する（例: 02:00 JST）。
- Cowork の**一度だけの移行タスク**（my-brain を新パスへ）が済んでいるか確認。
  アプリは新旧どちらのパスからも読めるので、済んでいなくても動く。
- 任意: `OPENAI_API_KEY` を Vercel に登録すると文字起こしが Whisper 系になる。
