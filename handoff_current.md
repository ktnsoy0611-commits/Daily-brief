# いまどこにいるか（2026-09-13／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★第94〜96巡に **JOURNAL（録音画面）と
EXPLORE の BRIEF の札**も作り直した（TASK は手つかず）。
★★★**Explore の刷新（券・改札鋏）は保留**。コードは消していない（`DEV` タブごと最後に処分）。
★★★**いま試している大きな変更は1つ**（実機で見て決めてもらう段階）… **アプリごとの
アクセント配色**（`lib/appAccent.ts` の `ACCENT_TEST`。**`false` の1行で元へ戻る**）。
仕様の正 … `docs/home-spec.md`（ホーム）／`docs/project_knowledge.md` §3-h／
`design.md`（UI の規約と機械チェック13本）。

---

## 直近完了

### ★★★第96巡 ── 比を正す（札の器を正方形へ／録音画面をアイコンの拡大へ）

第95巡の2つは、どちらも**比率**で外していた。

**① 札のマスクが潰れていた**（ユーザー指摘「縦横比がおかしい。潰れて見える」）
- ★★★**原因 ＝ `clipPathUnits="objectBoundingBox"` は 0〜1 のパスを器の比へ
  引き伸ばす。** 器 308×275（比 1.12）で四つ葉とトゲトゲが横に 12% 伸びていた。
- ★★★**参照画像を実測したら器は全部正方形**（218×218／218×217／188×188）。
  **ベゼルも 10.3%**、`SPACE.lg` の 5.2% は**半分しかなかった**。
- → 器を `aspectRatio: "1 / 1"`（**文の塊が余りを取る**）、ベゼルを **`SPACE.xxl`**、
  判（KEEP/SKIP）も同じ値へ。
- ★★★**札の比は `ITEM_CARD_ASPECT` から来ていなかった**（「未解決」に再掲）。
  **`BRIEF_CARD_ASPECT`（"2 / 3"）を新設**し、**1つの割り算から2か所を導く**。

**② 録音画面がアイコンと比が違いすぎた**（ユーザー指摘）
- ★★★**実測 … アイコンの `リールの直径 ÷ 本体の高さ = 0.457` に対し、録音画面は
  0.78 で 2倍近く大きかった。**
- → **「アイコンを拡大して画面に嵌めた絵」**にした。リールの直径も中心間も中心の
  高さも**全部 `lib/cassette.ts` の比から導く**。画面側に数を1つも置かない。
- ★★**大きさを決める条件は1つだけ** ―― 下の段が画面に収まること（左右 `SPACE.lg`）。
  本体は段の2倍の幅なので**必ずはみ出す**＝「はみ出している感じは維持」が式から出る。
  ★★★**第95巡の `DIAL_RATIO`/`DIAL_CX`/`DIAL_WAIST` は消えた。復活させない。**
- ★**ラベルは段の外・キーの上**（ユーザー確定。段が薄くキーと両方は入らない）。

**③ 図形そのもの**（ユーザー指定）… リールを**大きく**（2.9→3.2）・**本体の中心へ**
（y 11→12）・**段を薄く**（2.8→1.8）。★1か所でタブ・山・録音画面が同時に変わる。

**④ ホームの山の提案を4つの形へ**（ユーザー指定）
- ★★★**山に落ちる5種のうちジャンルを持てるのは提案だけ**（他は `ItemDomain` を
  持たない。持たせると形が2つの意味を持つ）。
- ★★★**物理はまん丸をやめて凸の12角形**（`poly-decomp` が無いので凹んだ形を
  `fromVertices` に渡せない）。当たり判定は半径のまま。
- ★★★**形の正を「点の列」へ作り替えた**（`cardShapePoints`）。SVG も canvas も
  **そこから導く**。技術をまたいでも形を2度書かない。

**検証（すべて WebKit）** … 札は4枚とも `340x510`・器 **276×276 の正方形**・
ベゼル 32px（9.4%）・写真が器を埋める／録音画面は `D/H 0.4572`（アイコン 0.4571）・
`中心間/W 0.3749`（0.375）・**リールの中心 y − 本体の中心 y ＝ 0**・本体は片側 163px
はみ出す・ラベルは段の外で青との比 **7.12**・リールは掴めて段とラベルの上では回らない／
山は提案が4つの形で落ちる／ストックとゴールは**ファイルごと無変更**／
機械チェック13本 **0件**・`tsc` 0・`eslint` 0・`build` 成功。

### ★★★第95巡 ── 札の実機のバグを clip-path で根治／形を参照の語彙へ

★寸法は第96巡に上書きされた。残る要点だけ:

- ★★★**WebKit がこの環境で動く**（`npx playwright install webkit` ＋
  `install-deps webkit`）。★★**万能ではない** ―― 下のバグは Playwright の
  WebKit でも**再現しなかった**（実機の WebKit だけ）。
- ★★★**実機のバグの正体 ＝ `mask-size: 100% 100%` が効かず、SVG が固有の 1:1 の
  比のまま置かれていた**（切り抜きが器の幅より 254 デバイス画素 狭い正方形）。
  ★**プロパティの書き漏らしではない**（`Webkit` 付きと対で8本書いてあった）。
- ★★★**だから仕組みごと替えた** … `clip-path` ＋ `clipPathUnits="objectBoundingBox"`。
  画像としての寸法計算・`preserveAspectRatio`・データURIの解析が**全部いなくなる**。
  ★**id は札ごとに `useId()`**（`AppShell` はタブを全部載せたまま横へ送る）。
- ★★**形は参照画像に在るものだけ** … 四つ葉／六角形／波打つ四角／トゲトゲ
  （**ドームと台形は参照に1枚も無かったので捨てた**）。割り当ては
  `PUNCH_BY_DOMAIN` から導く（約束を2つ持たない）。
- ★下の段を「左に円・右にバー」へ割り、**そのままキーの穴**にした。

### ★★★第94巡 ── JOURNAL をカセットへ／札を「形が分類を言う」札へ

★寸法も仕組みも第95〜96巡に上書きされた。残る要点だけ:

- ★★★**録音画面はもともとカセットだった**（アイコンのリールの比と実測が一致）。
- **新設 `lib/cassette.ts`** … 同じ形を**3つの技術**が描く（タブの SVG／山の canvas／
  録音画面の DOM）。★**タブバーの中だけは1色**（地が選択状態で変わる）。
- ★★★**形の約束は `lib/ticket.ts` の `PUNCH_BY_DOMAIN` が持っていた。**
  `lib/cardShape.ts` は**それを写すだけ**。約束を2つ持たない。
- ★★★**切るのは写真だけ。札には掛けない**（掛けると `box-shadow` が消え、
  黄とクリームの比 1.27 では縁が1本も出ない）。
- BRIEF から**綴じ穴を外した**（育成カードには残る）。**ジャンルの文字も置かない**。
  操作は下の1列に集め、**高さを `LINK_D` で固定**。

### ★★★第93巡 ── タグを全廃し「日付あり＝塗り／なし＝輪郭」へ

- ★★★**タグを全面廃止**（`lib/taskTags.ts`・`lib/tagPattern.ts` をファイルごと削除）。
  分類の道具が多すぎた ―― 残っていた差は柄と書体だけ＝**意味を持たない2択**。
- ★★★**タスクの性質は「日付があるか／ないか」の1軸だけ**（**日付あり＝塗り／
  日付なし＝輪郭**）。合図は `isDated(spec)`。★★**`paintKey` に日付の有無と地の色を
  入れること**（`sides` は数しか見ていない）。
- ★★**書体を1つに固定**（`SHAPE_FACE`）。**書体を分類の道具にしない。**
- ★上の段（EXPLORE の提案）は**塗りのまま** ―― 黄は地との比 1.27 で線にすると消える。

### ★★★第92巡 ── ホームの山を土台から組み直した

- ★★★**ぼやけ ＝ 同じ器に ResizeObserver が2つ**（先に走るほうがもう一方を黙らせる）。
- ★★★**下が空く ＝ 床がタブバーのぶん二重引き** → **`.bleed-x-b`**。
- ★★★**「少し変えるたび不安定」＝ 中身が1文字変わると世界ごと作り直し** →
  **effect を2本に割った**（世界は1度だけ／中身は図形だけ差し替え）。
- ★★**眠りを許すのは時間ではなく「みんな止まったら」**／**紙の目（`paperize`）を全廃**。

### ★★★第89〜91巡までの要点

**`lib/wordPlate.ts`**（文字の板。★DOM で組み直さない）／**山が「塔」だった正体**
（面積の予算と出どころの高さ）／**刻みは固定 1000/60**／**焼き込まれた色を信じない**
／**タブバーの高さは `navHeightPx()`**（実機 132px／Chromium 81px）。
詳しくは `docs/project_knowledge.md`。

---

## 次の一手

1. ★★★**実機で見てもらう** … ①**録音画面**（リールと段の釣り合い・本体の上の
   青い余白）②**札の4つの形**（`DEV` → 「札」。**まだ潰れていたら、次は写真を
   `<svg><image preserveAspectRatio="xMidYMid slice" clip-path=…>` の中へ入れる**
   ―― DOM の `<img>` と CSS を使わず SVG エンジン1つに描かせる）
   ③**ホームの山の提案の4つの形**（多角形にしたので転がり方が変わった）
   ④**アクセント配色を採るか捨てるか**（捨てるなら `ACCENT_TEST` を `false`）。
2. **引き下ろし**（★ホームの軸。まだ無い）… 帯のピルを掴んで引き下ろすと
   **7本の柱が地面から立ち上がり**（高さ＝その日の量）、離した日がその日付になる。
   外で離すと候補（ストック）へ。**ピルは落ちながら図形へ連続的に変わる**
   （★塗り／輪郭の規則はもう通してある）。
3. **持ち物** … 右上のアイコン＋数の印（警告）。押すと印のすぐ下に小さな一覧。
4. **生成器が2つ足りない** … ①「今日行くのがおすすめ」②フォローアップの夜間生成。
   ★★**どちらもプロンプトを先に文面で出して承認を得る。**
5. ★ピルを押したときの詳細（まだ無い）。★山の図形はもう押せる（`Piece.nav`）。

## 未解決

- ★★★**提案の生成は止まったまま。** 15 は**表示だけ**の上限なので裏の未読 41 枚は
  減らず、夜間の生成は `POOL_CAP`(40) に掛かり続ける（切り替える場所は
  `app/api/cron/build-brief/route.ts` にコメントで明記）。
- ★**帯の②（今日行くのがおすすめ）は生成器が無いので今は出ない。**
- ★★**輪郭の字は地の上で比 2.55**（本文の下限を割る。★目盛りの外・ユーザー確定）。
- ★★★**札の比は `ITEM_CARD_ASPECT` から来ていない。`BRIEF_CARD_ASPECT` が正。**
  `ITEM_CARD_ASPECT` を変えると**ストック・ゴールのバインダー・表紙・コーヴァー
  フロー**が動くのに、**札は 1px も動かない**（第96巡に踏みかけた罠）。
- ★★**録音画面の本体は左右へはみ出したまま**（片側 163px）＝**仕様**（ユーザー確定。
  式から自然に出る）。
- ★★**黄の札とクリームの地の比は 1.27**。縁は**影**が持つので、**札を切り抜くと
  縁が消える**（切っていない）。
- ★★★**札の実機のバグは「直したはず」の段階**。Chromium と Playwright の WebKit
  では**もともと再現していなかった**ので、**直った証明は実機でしか取れない**。
- ★★**券の大きな英語が読めなくなる恐れ**（白い紙の上で下2つが 3.0 を割る）。
  券は `DEV` と `/dev/explore` にしか無いので4タブには影響しない。
- ★★**地 `#FFFBF5` と紙 `#FAFAF9` の差が比 1.01**／`SectionLabel`（`MUTED`）は
  明るい地で 3.2 ＝ AA 割れ。**どちらも既知・未対応。**

---

## 重要パス

- ホーム … `HomeTab.tsx`／帯 `Band.tsx` ＋ `lib/homeBand.ts`／山
  `components/home/Pile.tsx` ＋ `pileWorld.ts` ＋ `pilePaint.ts`／器 `lib/pileBox.ts`
  ／配線 `AppShell.tsx`／仕様 `docs/home-spec.md`
- ★★★**塗り／輪郭は `lib/solidPaint.ts` の `paintShape` と `isDated` が正**（山は
  `pilePaint.ts` にも同じ分岐。**片方を直したら両方**）。
- ★★★**カセットの寸法は `lib/cassette.ts` の1か所**（タブの SVG／山の canvas／
  録音画面の DOM）。目盛りだけ `lib/dial.ts`（`drawDial` は削除済み）。
- ★★★**札の形は `lib/cardShape.ts`（点の列が正）＋ `CardShapeDefs.tsx`。
  ただし `lib/ticket.ts` の `PUNCH_BY_DOMAIN` から導いている**（対応表を2つ持たない）。
  ★**CSS のマスクを使わない。`clip-path` ＋ `objectBoundingBox`。**
  ★★★**器は必ず正方形**（`objectBoundingBox` は器の比へ引き伸ばす）。
  ★canvas（ホームの山の提案）は `traceCardShape`。見本帳は `DevStageTab` の「札」。
- ★★★**山の器は `.bleed-x-b`**（左右＋**下**）。下を出さないと `floorYOf` が
  **タブバーの高さを二重に引く**。
- ★★★**器を見る ResizeObserver は1つだけ**（2つ付けると **canvas の実解像度が
  初回のまま固定される**）。
- ★★★**文字の板は `lib/wordPlate.ts` の1か所**。★**DOM で組み直さないこと。**
- ★★**アクセントのテスト** … `lib/appAccent.ts`（`ACCENT_TEST` の1行で戻る。効く先は
  `TASK_FACE`/`JOURNAL_FACE`/`STUDIO_KEY` と `lib/palette.ts`）。
- ★**色そのものは `lib/constants.ts` の `PALETTE`（6行）だけ**／役との対応は
  同ファイルの `SCHEME` と `lib/palette.ts`
  ／★**面から導く3つ** … `bodyInkOn`・`redOn`・`inkVarsOn`。**表を持たず、導かせる。**
- ★UI の規約と機械チェック13本 … `design.md`／目盛り `lib/tokens.ts`／動き
  `lib/motion.ts` ＋ `app/globals.css` の `:root`
- タスク … `GravityTab.tsx`（★山の物理の出どころ。ホームと**対**）／カメラ
  `TaskSpace.tsx`／入口の輪 `CreateMenu.tsx`
- 券・鋏（★保留中）… `components/explore/samples/`／`Ticket.tsx`／`lib/nipperShape.ts`
  （生成物。手で直さない）／`docs/explore-redesign.md`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite`/`fetchDirect`/`jinaSlot`）／
  単体チェック `npx tsx tools/jina-check.mjs`
- ★★★**WebKit（＝実機と同じ系列）がこの環境で動く**（第95巡に入れた）…
  `npx playwright install webkit` ＋ `install-deps webkit`（`libgtk-4` などが要る。
  入れないと「Missing libraries」で落ちる）。`executablePath` 無しに起動する
  （**Chromium だけ** `executablePath: "/opt/pw-browsers/chromium"` が要る）。
  ★★**万能ではない** … 第95巡の札のバグは Playwright の WebKit でも再現しなかった。
  **「Chromium で 0 件＝実機で直った」は今も言えない。**
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちて 500）。Playwright は
  `executablePath: "/opt/pw-browsers/chromium"`、**`localhost` で開く**。
  ★★**山の測定は 8 秒待つ**（落ちる距離が伸びた。4.5秒だとまだ空中に居る）。
  ★★**canvas の倍率は場所で違う**（山 3／GRAVITY・DRIFT・入力画面 2）。
  ★`migrate` の単体チェックは state に **`wishes: []` が要る**。
  ★`next build` は Google フォントの取得に**ときどき失敗する**（数回やり直す）。
