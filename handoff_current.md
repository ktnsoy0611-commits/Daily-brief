# いまどこにいるか（2026-09-13／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★第94〜100巡に **JOURNAL（録音画面）・
EXPLORE の札・TASK の図形・書体**を作り直した。
★★★**Explore の刷新（券・改札鋏）は保留**（`DEV` タブごと最後に処分）。
★★★**いま試している大きな変更は1つ**（実機で見て決めてもらう段階）… **アプリごとの
アクセント配色**（`lib/appAccent.ts` の `ACCENT_TEST`。**`false` の1行で元へ戻る**）。
仕様の正 … `docs/home-spec.md`（ホーム）／`docs/project_knowledge.md` §3-h／
`design.md`（UI の規約と機械チェック13本）。

---

## 直近完了

### ★★★第100巡 ── 段に1行／ホームの板を小さく／突起・波形・リールの芯／Anton

**① タスクの図形 ―― 段の中に1行**（ユーザー指定「**一つのピルの段に対して一行**、
**一行が段の中心**に。**文字とピルの輪郭の間のスペーシング**も適切に」）
- ★★★**壊れていた理由は「刻みが2つあった」こと** ―― 文字は **`size × LINE_H`** の
  刻みで塊を組んで**箱の中心**に置き、段は **`boxH / rows`** の刻みで並ぶ。
  一致するのは偶然だけで、**どちらも強制されていなかった**。
- → **`layoutInRows`（`lib/textFit.ts`）に一本化**。段の中心 `ys` を返し、`drawFitted`
  がそこに置く。行は `splitLines(title, rows)` で**必ず段の数**。
- ★★**ベゼルは1つの数から四方へ回る** … 字は `pitch × ROW_FILL`(0.62)、余り
  `(pitch − size×LINE_H)/2` を**上下にも左右にも**使う。箱の比（`rowAspect`）も
  **同じ `ROW_FILL` から**（箱を決める側と字を詰める側が別の数だと箱が足りない）。
- ★★★**`fitText` / `layoutInShape` / `wrapPerLine` は削除。復活させない。**
  **`LOD_ONE_LINE`（48px 未満は1行）も削除** ―― 3段の形に1行だけ置くと、行が
  段ではなく**箱の中心**（2段なら**くびれの上**）に来る。
- ★★**ホームの山も同じ割り付け**（第99巡は `fitText(..., ph×0.7)` で山だけ違う絵だった）。

**② ホームの日付・曜日を小さく**（ユーザー指定「もう少し小さく」＝1割半）
- `PILE_WORD_MAX = 49`（**ホーム専用**。★`SWISS_XL`(72) は TIMELINE と共有なので下げない）。
- ★★★**承認された「72 → 61」では小さくならない** ―― 同じ巡で書体が Anton になり、
  キャップが em に対して **24% 高い**ので 61 だと縦は 5% **大きく**なる。
  **「1割半小さく」が指定**なので、縦が 0.85 倍になる上限を**実測から解いた**。

**③ 録音画面**（ユーザー指定3件）
- **突起は上だけ角丸・下は角**（「四角から**ボタンが出っ張っている**」ので）。
  ★★3技術とも四隅を丸めてしまう（SVG の `rx`／canvas の単一半径／CSS の一括）ので、
  `lib/cassette.ts` に `topRoundRectPath`（SVG の `d`）と `topRoundRadii`（4値）を置いた。
- **波形はリールとリールのあいだの上**（第99巡はリールより上に出ていた）。
  ★位置も高さも**リールの直径から**（`CASSETTE_WAVE_CY_PER_D` 0.44／`_H_PER_D` 0.14）。
  ★★**帯の幅は「その高さでの円と円の隙間」** ―― 上寄りほど広い。**生の 136 の正体。**
- **縁の白い点5つ → リールの芯**（中心から 120° ずつ、半径の 1/3 の**とても太い丸い線**
  が3本。中心は**曲線で滑らかに**繋がる）。★★★**滑らかさは式が作る** ―― 3本の
  カプセルの距離場を **smooth-min** で結ぶ（隅を手で丸めない）。`lib/reelHub.ts`。
  ★**出すのはホームの山と録音画面だけ**（タブのアイコンには出さない＝地が変わるため）。
  ★**`lib/dial.ts` は削除。**

**④ 大きな欧文と数字を Anton へ**（ユーザー指定「**タブの文字は良いが大きい文字と数字が変**」）
- ★★★**「変」の正体は書体ではなく潰し方だった** ―― canvas の `ctx.font` は
  **`font-variation-settings` を受け取らない**ので `"wdth" 88` が届かず、それを補うため
  `wordPlate` が**横を 50% に潰し**（実測 63%）、太さ 900 と合わさっていた。
- → **`DISPLAY`（Anton）**を新設し、**同時に偽コンデンスを撤回**
  （`SQUEEZE_AIM = 1`／`SQUEEZE_MIN` は 0.88 の安全網）。**片方だけでは直らない。**
- ★**Anton は単一ウェイト**なので `WORD_WEIGHT` も **900 → 400**（900 は合成ボールド）。
  `WD_WDTH`(58) は削除（軸が無い）。`WD_ADV` は Anton の実測 **0.56**。
- ★**差し替えは4か所だけ**（山の日付・曜日／GRAVITY の同／TIMELINE の巨大な曜日／
  未読の数）。**`SANS`/`LATIN` の使い手は1つも触っていない。**

**検証（件数つき・WebKit 390×844）**
- 段と行 … 6題すべて**行の数 ＝ 段の数**（1/1/1/2/2/3）。**行の中心 − 段の中心**は
  +1.2〜+3.0 device px（dpr 3 ＝ 0.4〜1.0 CSS px。和文の塗りが em の中心より下に来る
  ぶんで、**段ごとに同じだけ**＝積み上がらない）。四方の余白は 18字の題で 上15/下12/
  左14/右14。
- 板 … `WEDNESDAY` の塗り **300.7×52 → 192.7×44**（縦 **0.846 倍**）。
- 突起 … 77.2×38.6・computed **`12.87px 12.87px 0px 0px`**（上だけ角丸）×2個。
- 波形 … **338×58 → 138×43**、中心はリールの中心の **135.9px 上**、左右のクリアランス
  ちょうど **16 ＝ `SPACE.lg`**。
- 芯 … 峰は **270°/30°/150°**（120° ちょうど）・外へ出るのは半径の **0.44**・
  隣り合う接線の折れの最大 **14.3°**（角が無い）。リールの SVG は `rect 0 / path 1`。
- 書体 … TIMELINE の曜日が **Anton 71px/400**。**タブの文字は 13 個とも
  `Archivo 7px / 700 / 1.12px` のまま**（1つも変わっていない）。
- 機械チェック13本 **0件**（#8=7・#9=120 は前と同じ）・`tsc` 0・`eslint` 0（警告7は既存）・
  `next build` 成功・`pageerror` 0。

### ★★★第99巡 ── カセットに角ボタン／タスクを「ピルの積み」へ／帯に「今日行ける」

- **カセット** … 角丸を 2.6 に戻し、**左上に四角い突起2つ**（`w = D`・`h = D/2`・隔 `D/3`、
  組の中心を左のリールの真上）。★★突起は本体の外へ出るので、録音画面の高さの見張りを
  **外接箱**へ（`CASSETTE_BOX_H_PER_H`）／山の焼き箱を**四方に広げる**（1px では頭が切れる）。
- **タスクの図形 → ピルの積み**（`stackOutline`）。参照の実測 399×392 に3段・1段 131。
  ★★★**「辺の数（1〜4）＝埋まり具合」はやめた。復活させない。**
  ★★★**段の数は `rowsOf(title)`（純粋な関数）。行数の実測から引かない** ―― 実測だと
  ① ALIGN と山で形が変わる ② 書体が届いた瞬間に**絵だけ段数が変わり物理とずれる**。
  ★くびれは物理では**凸包に潰れる**（`poly-decomp` が無い）。**それでよい。**
- **帯の「今日行くのがおすすめ」**（`lib/todayPick.ts`）。★★★**AI を使っていない。
  使えなかった** ―― `Item` に営業時間も `start` も `venue` も無い。→ **`kind` ごとの
  時間帯の表**（＝近似）。★**今日もう閉まったものは出さない**（23時なら 0 件）。

### ★★★第96〜98巡までの要点（寸法は上書き済み。教訓だけ）

- ★★★**参照の形は全部「円をつないだ形」**（半径 profile を実測して解く）。
  **正弦波で円に似せない**（角で振幅が √2 倍に伸びる）。**復活させない。**
- ★★★**`objectBoundingBox` は 0〜1 のパスを器の寸法へそのまま掛ける** ――
  ①器が正方形でないと潰れる ②外接箱が 0〜1 でないと形ごとにベゼルが変わる（→`fitBox`）。
- ★★★**録音画面は「アイコンを拡大して画面に嵌めた絵」**。寸法は全部 `lib/cassette.ts`
  の比から。**画面側に数を1つも置かない。** `barW = 3(D−2·lip)+4·lip = 2.75 D`。
- ★★★**コメントと値が食い違う定数を作らない**（`KEY_LABEL_H = 12`／実際 7 でキーの列が
  5px ずれた）。★**円の光学的な中心を中心線へ置く**（縦に積むと誤差が足し算）。
- 札 … 幅 **358**・ベゼル `SPACE.xl`・器 **310**／題2行・本文2行で頭打ち／
  下の列は **44px の円3つ**／生成の側でも題を刈る（全角30字）。

### ★★★第93〜95巡までの要点

- ★★★**実機のバグの正体 ＝ `mask-size` が効かず SVG が固有の 1:1 で置かれていた**。
  → **`clip-path` ＋ `objectBoundingBox`**。★**id は札ごとに `useId()`**。
  ★★**Playwright の WebKit でも再現しなかった** ―― 実機でしか証明できない。
- ★★★**切るのは写真だけ。札には掛けない**（掛けると `box-shadow` が消える）。
- ★★★**タグを全廃**。**タスクの性質は「日付があるか／ないか」だけ**（`isDated`）。
  ★★**`paintKey` に日付の有無と地の色を入れること**。★**書体を分類の道具にしない。**

### ★★★第89〜92巡までの要点

**山の作り替え** … ★★★**ぼやけ ＝ 同じ器に ResizeObserver が2つ**／★★★**下が空く ＝
床がタブバーのぶん二重引き**（→`.bleed-x-b`）／★★★**effect を2本に割る**（1本だと
題を1文字直すたび山ごと落ち直す）／**紙の目（`paperize`）を全廃**。
**文字の板は `lib/wordPlate.ts`**（★DOM で組み直さない）／**タブバーの高さは
`navHeightPx()`**（実機 132px／Chromium 81px）。

---

## 次の一手

1. ★★★**引き下ろし**（**ユーザーが次に作ると言っている**）… **帯の上のピルを引っ張って
   「今日」やその他の日に追加する**アニメーションと UI。帯のピルを掴んで引き下ろすと
   **7本の柱が地面から立ち上がり**（高さ＝その日の量）、離した日がその日付になる。
   外で離すと候補（ストック）へ。**ピルは落ちながら図形へ連続的に変わる**。
2. ★★**実機で見てもらう** … ①**段に1行**（GRAVITY・DRIFT・ALIGN・TIMELINE・入力画面・
   山。★詰まりすぎ／緩すぎなら **`ROW_FILL`(0.62) の1つ**を振る。箱の比も連動する）
   ②**ホームの板の大きさ**（★横は 0.64 倍まで縮んだ。**縦だけ** 0.85 倍。
   戻すなら `PILE_WORD_MAX`）③**リールの芯**（`HUB` の `arm`/`thick`/`blend` の3つ）
   ④**波形**（138×43。狭ければ `CASSETTE_WAVE_CY_PER_D` を上げる）
   ⑤**Anton**（★**券の大きな英語は Archivo のまま**。`DEV` タブだけなので手を付けていない）
   ⑥**アクセント配色を採るか捨てるか**（捨てるなら `ACCENT_TEST` を `false`）。
3. **持ち物** … 右上のアイコン＋数の印（警告）。押すと印のすぐ下に小さな一覧。
4. **フォローアップの夜間生成がまだ無い**（★プロンプトは先に文面で出して承認を得る）。
5. ★ピルを押したときの詳細（まだ無い）。★山の図形はもう押せる（`Piece.nav`）。

## 未解決

- ★★★**提案の生成は止まったまま。** 15 は**表示だけ**の上限なので裏の未読 41 枚は
  減らず、夜間の生成は `POOL_CAP`(40) に掛かり続ける（切り替える場所は
  `app/api/cron/build-brief/route.ts` にコメント済み）。
- ★★**波形は録音中しか出ない**ので、**帯そのものの見え方は実機でしか確かめていない**
  （位置・寸法・クリアランスは数値で確認済み。マイクが無いと描画は走らない）。
- ★★**輪郭の字は地の上で比 2.55**（本文の下限を割る。★目盛りの外・ユーザー確定）。
- ★★★**札の比は `ITEM_CARD_ASPECT` から来ていない。`BRIEF_CARD_ASPECT` が正。**
- ★★**録音画面の本体は左右へはみ出したまま**（片側 **243px**）＝**仕様**（ユーザー確定）。
- ★★**黄の札とクリームの地の比は 1.27**。縁は**影**が持つので、札を切り抜くと縁が消える。
- ★★★**札の実機のバグは「直したはず」の段階**（Chromium も WebKit も再現しない）。
- ★★**券の大きな英語は Archivo ＋ `wdth 70` のまま**（`DEV` と `/dev/explore` だけ。
  4タブに影響なし）。Anton にするかは未定。
- ★★**地と紙の差が比 1.01**／`SectionLabel` は明るい地で 3.2 ＝ AA 割れ。**既知・未対応。**
- ★★**札の中の「文の下から操作の列までの隙間」**（本文が短い札で空く）。**未着手。**
- ★★★**帯の「今日行ける」は営業時間を知らない**（`kind` ごとの表＝**近似**）。

---

## 重要パス

- ホーム … `HomeTab.tsx`／帯 `Band.tsx` ＋ `lib/homeBand.ts`／山
  `components/home/Pile.tsx` ＋ `pileWorld.ts` ＋ `pilePaint.ts`／器 `lib/pileBox.ts`
  ／配線 `AppShell.tsx`／仕様 `docs/home-spec.md`
- ★★★**タスクの形は `lib/solid.ts` の `stackOutline`（ピルの積み）が正**。
  **段の数は `lib/taskSize.ts` の `rowsOf(title)`**（純粋な関数）。絵も物理も山も
  **そこから引く**（`shapeRowsOf`）。
- ★★★**文字の割り付けは `lib/textFit.ts` の `layoutInRows` だけ**（段に1行。
  ベゼルは `ROW_FILL` から四方へ回る）。**割り付けを2つ持たない。**
- ★★★**塗り／輪郭は `lib/solidPaint.ts` の `paintShape` と `isDated` が正**
  （山は `pilePaint.ts` にも同じ分岐。**片方を直したら両方**）。
- ★★★**カセットの寸法は `lib/cassette.ts` の1か所**（タブの SVG／山の canvas／
  録音画面の DOM）。**リールの芯の形は `lib/reelHub.ts`**（SVG と canvas が同じ点の列）。
- ★★★**書体は3つ** … `SANS`／`LATIN`／**`DISPLAY`（大きな欧文と数字だけ。Anton）**。
  ★★**canvas は `font-variation-settings` を受けない** ―― 大きな欧文は**書体そのもので
  縦長にする**。**偽コンデンスは撤回済み**（`SQUEEZE_AIM = 1`）。
- ★★★**札の形は `lib/cardShape.ts`（点の列が正）＋ `CardShapeDefs.tsx`。
  割り当ては `lib/ticket.ts` の `PUNCH_BY_DOMAIN` から導く**。
  ★**CSS のマスクを使わない。`clip-path` ＋ `objectBoundingBox`。器は必ず正方形。**
- ★★★**山の器は `.bleed-x-b`**／**器を見る ResizeObserver は1つだけ**／
  **文字の板は `lib/wordPlate.ts` の1か所**（★DOM で組み直さない）。
- ★★★**帯の「今日行ける」は `lib/todayPick.ts` の1か所**（`kind` ごとの時間帯＝**近似**）。
- ★**色そのものは `lib/constants.ts` の `PALETTE`（6行）だけ**／役との対応は
  `SCHEME` と `lib/palette.ts`／★**面から導く3つ** … `bodyInkOn`・`redOn`・`inkVarsOn`。
- ★UI の規約と機械チェック13本 … `design.md`／目盛り `lib/tokens.ts`／動き
  `lib/motion.ts` ＋ `app/globals.css` の `:root`
- タスク … `GravityTab.tsx`（★山の物理の出どころ。ホームと**対**）／カメラ
  `TaskSpace.tsx`／入口の輪 `CreateMenu.tsx`
- 券・鋏（★保留中）… `components/explore/samples/`／`Ticket.tsx`／`lib/nipperShape.ts`
- ブリーフ … `lib/briefPipeline.ts`／単体チェック `npx tsx tools/jina-check.mjs`
- ★★★**WebKit がこの環境で動く** … `executablePath` 無しに起動（**Chromium だけ**
  `/opt/pw-browsers/chromium`）。★★**万能ではない**（第95巡の札のバグは再現しなかった）。
- 検証 … 開発サーバーは `localhost` で開く。★★**山の測定は 8 秒待つ**／
  **canvas の倍率は場所で違う**（山 3／他 2）／`next build` は Google フォントの取得に
  **ときどき失敗する**（数回やり直す）。★**入力画面の見本は upright なので、図形の
  文字を画素で測るのに使える**（外側から塗って輪郭の連結成分を除くと字だけ残る）。
