# いまどこにいるか（2026-09-17／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。★第110〜112巡に「落とし直し」を根絶、
★★★**第113巡に帯の隙間をレイアウトへ作り直し**、★★★**第114巡に「どこからでも・
どこへでも入る」を通し**（並びは**留め金**）、★★★**第115巡に「最初の落下が重い」を
根治**（起動の代金だった）、★★★**第116巡に形を「ピルの積み」へ戻し、大きさを
「段の高さ」1つ**にした。
★★★**第117〜118巡に配色を総入れ替え**（タスクは無彩色／ドメイン4は色と形の組／
JOURNAL はラベンダーで別枠）、★★★**第118巡に手ざわりの4つの真因を根治**。
**次は「おすすめ3件」と Explore の詳細画面**。
★Explore の刷新（券・改札鋏）は保留（`DEV` タブごと最後に処分）。仕様の正 …
`docs/home-spec.md`（**§6 が引き下ろしの正**）／`docs/project_knowledge.md` §3-h／
`design.md`。

---

## 直近完了

### ★★★第118巡 ── 手ざわりの4つの真因を根治／色／山のスケール

> ①**提案のピルを離しても追加されず消えた** ②**指を離す前に帯へ吸い寄せられて
> 横にしか動かない** ③**隙間がぎこちない・重なる・ガクガク震える・全然アニメーション
> していない** ④**引き出した図形を離すと瞬間移動する** ⑤**ジャーナルのピンクをやめ、
> タスクのグレーをもっと薄く** ⑥**多すぎると操作できないので多さでスケールを調整**

- **①** `offer-` に対応する `Item` が無く `items.find` が原理的に当たらず黙って
  return していた → **引き下ろす＝KEEP**。式は新設 **`lib/keepCard.ts` の1か所**
  （`BriefTab` から持ち上げた）＋ `unreadEntries`（号のキー）＋ `BandItem.itemKind`。
  実測 `items` 0→1・`plannedFor` 1・決定 1（**3/3 回**）。
- **②** `stepGhost` の `if (g.aim) { springTo(catchX/Y, aim.x/y) }` が**絵の座標を
  奪っていた**（`aim.y` ＝ 段の中心の1本の線）→ **枝ごと削除。`Ghost.aim` は
  `boolean`**。役は `tTo` を **`PILL_HINT`(0.55)** へ寄せる合図だけ。
  ★★**第109巡にユーザー確定で入れたものを本人が撤回。復活させない。**
  実測 **絵と体の座標の差 0.000px**／`tTo` は 1〜0.55 で止まる。
- **③** 挿し口 `at` に**遊びが1つも無く**、変わるたび `m.open.p = 0` に戻していた
  → **`GAP_HYST`(22) ＋ `bandSettling`（閉じ切るまで乗り換えない）**。
  実測（±25px を3往復）… 飛び **11〜12 → 5 回**／**席の合計の幅 38〜150 のばたつき
  → 150 で一定**（8px 以上縮んだ回数 **3〜4 → 0**）。
- **④** 落とし所に**支点（`dx`/`dy`）**を渡していた（正は**絵の中心 `hx`/`hy`**）。
  **第108巡と同じ罠の裏返し** → 実測 ずれ **44.93px → 0.00px**。
- **⑤** JOURNAL ＝ **ラベンダー `#B2AFCE`**（地 2.05／黒いリール 7.01）。
  タスク ＝ **`TASK_GREY` `#BFBBB2`**（地 1.86／黒字 7.76。無彩色の**6段目**）。
  ★ピンクは `redOn()` の**暗い面の赤**として残す。
- **⑥** **帯が覆う高さを予算から引いていなかった**（帯は山の器へ重ねてある）→
  `buildPieces(…, bandH)` ＋ **混み具合 `crowdOf(n)`**（長さの倍率。予算は `crowd²`、
  板は `crowd`）。実測 帯からの余裕 **9件 79→218px／14件 108→238px**。
- **検証** … 機械チェック13本 全部 0 件／`tsc` 0・`eslint` 0 error・`next build` 成功／
  WebKit で `pageerror` 0 件。

### ★★★第117巡 ── 配色の総入れ替え（承認済み）

> 「**提案カード以外の色を無彩色にします。タスクに関する図形はグレーを主に使って
> 文字は黒で。提案カードやその他は画像の色を。各ジャンルの形は維持して、色と形が
> セットで印象が合うように。ジャーナルは飛び出た色＝別枠が分かる配色関係に**」

- 参照画像（Jardins de Métis の入場券）から**画素を測って 7色**。
- **タスク＝無彩色の塗り ＋ 黒の字**（輪郭のピルも字は黒 ―― 線と同じ色だと薄すぎる）。
  **帯・山・GRAVITY・DRIFT・入力画面すべて追従**。★色は第118巡に `TASK_GREY` へ。
- **ドメイン4は色と形の組** … バショ＝緑・四つ葉／タイケン＝赤・六角形／
  ジョウホウ＝青・波打つ四角／モノ＝黄・トゲトゲ。**形は1行も変えていない。**
  ★**提案の札の面もドメインの色**（第94巡の「面＝EXPLORE の1色」は撤回）。
- ★★★**`lib/appAccent.ts` を削除**（アプリごとのアクセント配色のテストの決着）。
  **復活させない。** ★`redOn()` は残す（暗い面では赤 3.19 よりピンク 4.09 が読める）。

### 第115〜116巡 ── 最初の落下の重さ／形と大きさ

- ★★★**GRAVITY が軽いのは「最初の画面ではない」から** ―― ホームが払っていたのは
  **起動の代金**（物理は4秒で 35ms、塗り 8ms）。**①**他の2つのアプリの先読みが
  落下に割り込む（`requestIdleCallback` の `timeout` は暇が無くても呼ぶ締切）→
  **落ち着いてから1つずつ**／**②**見えていない `VoiceStudio` が毎フレーム
  `clientWidth`（**524ms**）→ `appActive` で止める／**③**1フレームで全部焼いていた
  → **1フレーム 2枚**／**④**和文は文字ごとに別の断片。**落ちる前に頼む**。
  ★実測 … CPU×4・12体・4秒で **フレーム 58 → 115〜127**／90 パーセンタイル
  **233ms → 50ms**。★外した推測 … 多角形の切り抜き／`imageSmoothingQuality`／書体。
- ★★★**形は「ピルの積み」**（第115巡の S 字は第116巡に撤回。`foldsAt`/`sideFolds` 削除）。
- ★★★**大きさは「段の高さ」1つ**（`rowSpecOf`）。第115巡の「面積を1つ」では
  **段が増えるほど段が痩せた**（倍率 40 で 1段 67.4／2段 46.0／3段 29.9px ＝ 幅 2.25倍）
  → **段の高さを 1 に固定**（`h = 段の数`／`w = rowAspect`）。**`unit` の意味が
  「段の高さ(px)」に変わった**。実測 9件とも 40.0px・字 24.8px（**幅 1.00倍**）。
  ★★**提案の円 `OFFER_D`(3.2)・カセット `CASSETTE_ROWS`(2.2) も段の高さの倍数**。
  ★★★**`specOf` は 1 行も変えない**（TASK は重要度で大きさが変わるまま）。

### 第113〜114巡 ── 帯の隙間をレイアウトへ／どこからでも入る

- ★★★**隙間と席は `transform` のずれでは書けない。レイアウト（`.band-pad` の幅）が
  持つ** ―― 同じ id のピルが2周ぶん居るので別々の値を書けない（実測 2周目が
  **152px 飛ぶ**）。**両側 ±w/2 も片側 +w も同じ理由で直らない。**
- ★★★**帯ぜんたいを `lead`＋隙間の半分だけ左へ戻し、挿し込みで「送り（px）」へ
  畳み替える**（`currentTime` を直接ずらすと 156px 飛ぶ）／**境目は開く前の位置で
  測る**／**挿し口が移るときは受け皿 `shut` へ**／**開けているあいだ組み直さない**。
- ★★★**`K_OPEN`/`D_OPEN`（5組目）** … 90% まで **117ms**・行き過ぎ **0.0%**。
- ★★★**白抜き ⇄ 塗りは `inkMix` が返す「量」で渡る**（実測 色の差 157 → 21）。
- ★★★**帯へ入れる道は2本あるのに実装は片方だけだった** ―― 引き抜いたピルの指は
  **ピルの DOM が捕まえている**ので `Pile.onMove` は1度も走らない。→ 狙いは**1本**。
- ★★★**弾けは掛け金**（`pullFrame` の `latched`）。毎フレーム測り直すと元の席へ
  瞬間移動する。★★**戻した提案が時間帯の門で落ちていた** → 留め金の付いたものは
  門を通さない。★★**幽霊が帯の裏へ回っていた**（掴んでいる間も canvas を上げる）。
- ★★★**並びは「留め金」（`AppState.bandPins`）**。`tasks` は触らない ―― ① GRAVITY と
  ALIGN まで動く ② 声の候補とフォローアップは持ち主が別で動かせない。
  ★**挿し口は index ではなく「左どなりの id」**。
- ★★★**日付 `9.15` ／ 曜日 `WED`、黒いピルに白い文字**（TASK の板は変えていない）。

### 過去の巡の要点（詳しくは `docs/project_knowledge.md` §3-h）

- ★★★**第111〜112巡** … 落とし直しの真因は **`clearOverlap` が据え置きの山にも
  掛かっていた**こと（傾いた図形の AABB は必ず重なる＝縦の塔へ積み直される）→
  **`Piece.fresh` だけ**＋**体を作り直さない**／**下の段の9件の打ち切りが戻したタスクを
  黙って消していた**（`keepId`）／**狙われている段は流れを止める**／
  **字も1文字ずつ `bendAt`**／**運ぶ図形は角度を留める**。
- ★★★**第109巡 … めり込みの真因は「湧いた瞬間から重なっている」**（60ms で落ちる距離
  **2.5px** に対し散らばり **200px**）→ 投入の直前に `clearOverlap`（372件 → 10件）／
  ★★★**眠りは `GravityTab` と同じ**（**大域の門は全削除**。1つでも転がっていれば
  開かない）／**`positionIterations` は既定の 6**。
- ★★★**当たり判定の光線は「正規化した空間」で飛ばす**／**`fromVertices` へ渡す前に
  `atan2` で並べる**（交差すると床をすり抜ける）／**canvas の dpr の上限は 2**／
  **ループは止める**（`dirtyRef`/`dragRef` を立てる所では必ず `wake()`）／
  **`ghostKey()` 1か所から取る**／**幽霊の絵は `hx`/`hy`**／**寿命は3重**／
  **隠すのは `opacity: 0`**／**`navHeightPx()` を rAF から呼ばない**／**塗るのは「変わった
  図形の箱」だけ**／★★★**実機は 120Hz。この環境（60fps）では再現しない**／
  **沈んだだけなら `sink`**／**`setVelocity` は眠った体を起こさない**／
  **列は `overflowX: "clip"`**。
- **第89〜101巡** … ★★★**canvas の `ctx.font` は読み込みを頼まない**し
  `document.fonts.ready` は**頼んでいないものを待たない**（→`ensureWordFont`。**待つなら
  締切**）／**参照の形は全部「円をつないだ形」。正弦波で円に似せない**／
  **`objectBoundingBox` の器は必ず正方形**／**実機のバグの正体 ＝ `mask-size` が効かず
  SVG が 1:1 で置かれていた** → `clip-path`／**切るのは写真だけ**／**タグを全廃**／
  **ぼやけ ＝ ResizeObserver が2つ**／**下が空く ＝ 床の二重引き**／**effect を2本**。

## 次の一手

1. ★★★**配色の総入れ替え（第116巡にユーザー指定・文面の承認待ち）** ――
   **タスクの図形は無彩色**（グレーの塗り＋黒の字）／**提案カードとその他**は
   参照画像（Jardins de Métis の券）から採った **7色**を使う／**ドメイン4の形は
   維持**して色と組にする／**JOURNAL は「飛び出た色」**として別枠にする。
   ★★**`PALETTE` の6行と `SCHEME` が本体**。`ACCENT_TEST` は `false` にして
   `lib/appAccent.ts` を消す（テストの決着）。
2. ★★★**実機で見てもらう**（第113〜116巡）… ①**最初の落下が軽くなったか**／
   ②**ピルの積みの形と、段の高さ・字が揃ったこと**／③**山の図形を帯へ近づけると
   指の下で左右のピルが開いて、離すとそこに入るか**（上の段も）／④**帯のピルを
   引き抜いて別の場所へ入れ直せるか**／⑤**日付と曜日の黒いピル**。
4. ★**`magazine` と `ItemStatus "planned"` を消す**（死んだ欄。`plannedFor` が正）。

## 未解決

- ★★★**提案の生成は止まったまま**（裏の未読 41 枚が減らず `POOL_CAP`(40) に掛かる）。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も焼く予算も直していない**（山だけ）。
- ★★★**帯の上の段の `offer` は引き下ろしても消えない**（第110巡に判明）。`BandItem.id` は
  `offer-<card.id>` だが、そのカードは `generatedDecks` に居るだけで **`Item` が存在しない**
  （KEEP して初めて `Item` が生まれ、同時に decision が付いて帯から消える）。つまり
  `HomeTab.put` の `items.find` は**原理的に当たらず、何も書かずに return する**。
  ★★**直すなら「引き下ろす ＝ KEEP ＋ `plannedFor`」**。**勝手にやらない。**
  ★並べ替えは第114巡に通した。
- ★**帯の並びは `bandPins`（留め金）**。`tasks` を触らないので GRAVITY/ALIGN は動かない。
- ★**TASK（GRAVITY）はホームと揃えていない2つ** … 日付・曜日の板は「文字そのものが
  図形」／重要度で大きさが変わるまま（形だけ共通）。**確認していない。**
- ★**起動の代金は残っている**（最初の1回の大きなレイアウトとスクリプトの評価。
  実測 1フレーム 667ms）。★**落ちた図形が山に当たると 3〜4px 動く**（物理。直さない）。
- ★★`SectionLabel` は明るい地で 3.2 ＝ AA 割れ。**未対応**（配色の入れ替えで直る見込み）。

---

## 重要パス

- ホーム … `HomeTab.tsx`／帯 `Band.tsx` ＋ `lib/homeBand.ts` ＋ `bandMotion.ts`／
  山 `components/home/`（`Pile.tsx`＋`pileWorld.ts`＋`pilePaint.ts`）／器 `lib/pileBox.ts`
- ★★★**引き下ろし … `lib/pullDrag.ts`／`pillGhost.ts`／`AssignRail.tsx`／
  `AssignSheet.tsx`／盤は `components/DateGrid.tsx`**（詳細は `CLAUDE.md` の地図）。
  ★**手つきは「輪ゴム → ばちん → 変形」**／**絵は `hx`/`hy`**／**姿は掛け金 `Ghost.pill`**／
  **遊び `RAIL_HYST`**／**戻りは2段**（`RAIL_NEAR` → `PULL_ARM`）／**変形 `K_SWING`・
  吸い付き `K_CATCH`**。★★★**弾けは掛け金**（`latched`。毎フレーム測り直すと指を戻した
  瞬間に元の席へ瞬間移動する）／★★★**帯へ入れる道は2本**（山の図形 ／ 引き抜いた
  ピル）で**狙いの出し方は1本**／★★**掴んでいる間は山の canvas を上げる**。
- ★★★**帯の物理っぽい動きは `components/home/bandMotion.ts` の1か所**（詳細は
  `CLAUDE.md` のファイル地図）。**隙間と席はレイアウト（`.band-pad` の幅）が持つ**／
  **帯ぜんたいを `lead`＋隙間の半分だけ左へ戻し、挿し込みで「送り（px）」へ畳み替える**／
  **バネは `K_OPEN`（隙間と席）と `K_SWING`（帯ぜんたいの一発）**／**境目は開く前の
  位置で測る**／**開けているあいだ組み直さない**／**挿し口は `after`（左どなりの id）**。
- ★★★**白抜き ⇄ 塗りは `inkMix`（`pillGhost.ts`）の1か所**。掛け金で切り替えない。
- ★★★**速さの3つ**（第115巡）… **先読みは落ち着いてから1つずつ**／**見えていない
  `VoiceStudio` は寸法を測らない**／**1フレーム 2枚まで焼く ＋ 書体は落ちる前に頼む**。
- ★★★**代理の体**（`Pile.syncProxy`／`ghostBodyOf`）… **絵は体そのもの**／**体は絵の
  中心**／**`piecesRef` に混ぜない**／**`clearOverlap` の前に消す**／**停止条件に入れる**／
  **締切 `HANDOFF_MS`**。★★**山の倍率は中身が変わっても決め直さない**（`hold`）。
- ★★★**セーフエリアは全画面で `SPACE.lg`、左右対称**／**器が変わったときの4つ**
  （`FLOOR_CHECK_MS`／`refitPile`／`sink`／`isLost`→`respawn`）／**器は `.bleed-x-b`**／
  **ResizeObserver は1つ**。
- ★★★**当たり判定の多角形は「軸の本数 × 頂点数」**（`PHYS_VERTS` 12・等角・偶数）／
  **投入の直前に `clearOverlap`**／**質量は全部同じ密度に**／**`atan2` で並べてから
  `fromVertices`**／**眠りは `GravityTab` と同じ**（`wake()` を忘れない）／**タスクの形は
  `stackOutline`（**ピルの積み**）・段の数は `rowsOf(title)`・**ホームの大きさは
  `rowSpecOf`（`unit` ＝ 段の高さ）**・`waist` が「ピル ⇄ 図形」**／
  **塗り／輪郭は `paintShape` と `isDated`**（`pilePaint.ts` にも同じ分岐。**両方直す**）。
- ★**カセット `lib/cassette.ts`**／**芯 `lib/reelHub.ts`**／**札の形 `lib/cardShape.ts`**／
  **文字の板 `lib/wordPlate.ts`**（★ホームの日付・曜日は**黒いピルに白い字**）／
  **帯の「今日行ける」 `lib/todayPick.ts`**／★★★**書体は3つ**／★**色は `PALETTE`
  （6行）だけ**（面から導く `bodyInkOn`・`redOn`・`inkVarsOn`・`mixHex`）／
  ★規約13本 `design.md`／目盛り `lib/tokens.ts`／動き `lib/motion.ts`。
- 検証 … ★★★**WebKit は `executablePath` 無し／Chromium だけ `/opt/pw-browsers/chromium`。
  CPU を遅くできるのは Chromium だけ**＝速さの測定はそちら／★★★**種に `keeps: []` を
  入れない**（`migrate` が `items` を丸ごと捨てる。`wishes: []` は必要）／★★**山の測定は
  8 秒待つ**／★★**帯を出す種には「日付の無いタスク」が要る**／★★**ピルを掴む試験は
  「画面の中に居る」ものを選ぶ**／★★★**帯の飛びは「見えているピル」だけで測る**
  （2周目の控えは画面の外なので 152px の偽陽性が出る）／★★**WebKit は続けて何回も
  起動すると落ちる**（1プロセス1ケース）／★★★**速さは `Tracing`（`devtools.timeline`）で
  `Layout`/`FunctionCall`/`EvaluateScript` に割り、`rAF` と `setTimeout` を包んで呼び手
  ごとに測る**（`(program)` だけでは何も分からない）。
