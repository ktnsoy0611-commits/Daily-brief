# いまどこにいるか（第80巡・2026-08-31／Explore の刷新の途中）

3アプリ（JOURNAL / TASK / EXPLORE）は動いている。第68巡までで TASK は一段落。
**第69巡から Explore の刷新中。設計の正は `docs/explore-redesign.md`。**

★★新しい**券と改札鋏は `/dev/explore` と `DEV` タブにしか無い**。
**Explore の4タブはまだ手つかず**で、現状のまま動いている。
★★いま**券のデザインを選んでもらう待ち**（`DEV` タブ →「券」に**4案**）。

---

## Explore 刷新の骨子（`docs/explore-redesign.md` が正）

- **2本の柱**は提案と実行。それを貫く共通部品が**券**で、場所によって縮尺だけが変わる。
- 場所は3つ … 提案（1枚）／ストック（格子）／マップ（点）＋ プランを重ねる。
- **言葉は一般語**。メタファーは視覚だけ（「本日の販売は終了しました」は使わない）。
- 入鋏＝残す。**鋏痕の形は4ドメインに対応**し、3か所（券の穴／マップのノードの形／
  ストックの絞り込み）で同じ意味を持つ。
- ★**CSS の 3D 変形は使えない**（design.md 冒頭）が、**WebGL は使ってよい**。
  券と鋏は `components/explore/TicketStage.tsx` の**同じ3D空間**に居る。

---

## 直近完了

### ★★★第80巡 ── パレットを6色へ／券を「白い紙にタイポグラフィ」へ

**「そもそもなぜ9色必要なんだっけ」への答え ―― 要らない。要るのは5。**

| 何に要るか | 数 | 備考 |
|---|---|---|
| **タグ**（タスクの図形） | **5** | ここだけが「5色ないと足りない」 |
| ドメイン（券・鋏痕・マップ） | 4 | TASK と同じ画面に出ないので**タグと共有** |
| 状態（危険・肯定・選ばれている） | 3 | 上から**借りる** |

**9 は参照画像の色数で、要件ではなかった**（予備の `lilac`・`sky` は使用 0 回）。

- **パレットを6色へ**。★★**色そのものは `lib/constants.ts` の `PALETTE`（6行）
  だけ**で、`SCHEME` は**役 → その箱**の参照。**差し替えは6行**（ユーザー指定
  「すぐ変えられるように」）。★★**「メイン × サブ」の組は廃止** ―― 券が白い紙に
  なって役目が終わった。面に載る字は `bodyInkOn()` が面から導く。
- **割り当て**（6通り全部を4尺度で採点して決定）… WORK/ジョウホウ/選択＝**Azul**／
  LIFE/モノ/肯定＝**Verde**／GROWTH/タイケン＝**Terracota**／WELLNESS＝**Amarillo**／
  SOCIAL＝**Rosa**／**危険/バショ＝Magenta**。色相 **47.7°**（前 28.0°）／
  見分け **0.187**（前 0.144）／多い3つ **0.288**（前 0.169）。
  ★**外した Magenta が危険にちょうど回った**（紙の上 6.07 ＝ 本文が書ける深い赤）。
- **無彩色を5段へ戻した**（ユーザー指定「グレーを復活させて」）… `INK` /
  **`CHARCOAL` `#383432`**（復活）/ `SECOND` / `MUTED` / **`SURFACE` `#EDEAE2`**（新）。
  ★★面のグレーは**半透明の重ねがけをやめる**ために在る（重ねる順で色が動く）。
- **券を作り直した**（ユーザー指摘「まだ幼稚。洗練・ミニマル・タイポグラフィが際立つ」）:
  ①切り欠きを幅の 7% → **14%** ②★★★**安全域は `%` の縦余白1つで解ける**
  （`padding` の `%` は**幅**で解決されるので、切り欠きの半径と正確に一致する。
  円は角から半径までしか食わないので、そこより内側なら横位置がどこでも掛からない）
  ③段を組み直し … **英語 `poster` 38/`black` 900**（新しい段。`black` の持ち主が
  題から英語へ移った）／題 `head` 20／要約 **`small` 11**／会期 **`micro` 9**
  ④**紙は白1色**。色は**大きな英語**と**鋏痕**だけ。★9案 → **4案**へ。
- ★★★**マスクした要素に `box-shadow` は出ない**（影ごと切り抜かれる）。第78巡から
  **券の影はずっと出ていなかった**（色の紙のうちは気づかない。紙を白にして露見）。
  親の **`filter: drop-shadow`** に替えた（輪郭の α に沿うので切り欠きにも回る）。
- 検証（件数）… コントラスト **38/39**（唯一の未達は券のモノ Verde 2.81。
  規約の外として明記）／**文字が欠けに掛からない 16/16**（4案 × 4隅を DOM の
  幾何で実測）／機械チェック13本 **0件**／`tsc` 0・eslint エラー0・
  `npm run build` 成功（フォント取得で1回落ちて再試行）／
  券4案・タスクの山・9タブ・設定3タブ・録音を撮って **pageerror 0**。

### ★★★第79巡 ── キーは「白い面・黒い穴・光る窓」／設定から罫を落とした

- **キーを白系へ戻した**（円は黒のまま）。第78巡は `figure` 1つが円とキーの両方を
  決めていて片方だけ戻せなかった。**別の材料なので割った** …
  `STUDIO.dial*`（画面で反転）と `STUDIO.cap`（**両方の画面で同じ白**）。
- ★★★**押せる／押せないをグレーアウトで言わない**（ユーザー指定）。
  **沈み**（形）＝その機能がいま働いているか／**ランプ**（光）＝いま押せるか。
  **キーは死んで見えない。灯りが消えるだけ。** `capOff` は削除。
- ★★★**明るい面にアクセントを直接置かない ―― 墨の窓の中で灯らせる。**
  白い面では盤の色が 1.2〜1.8 しか出ないが、**墨の窓（`STUDIO.socket` ⌀22）**の
  中なら全部出る。**これで `STUDIO_KEY` の画面ごとの分岐が消えた。**
- ★★★**REC の輪だけ `redOn()` を使わない** ―― 白い面で `redOn` が返す深い赤は
  **待機の墨と見分けが付かない**。**光ったと分かるか**で選ぶ（待機＝墨／録音中＝明るい赤）。
  ★波形の線は地の上なので `redOn` のまま。
- ★★キーの輪郭は**沈む穴の影だけ**。白い面は地と比 1.0 なので、穴を `WELL_LIP`(3px)
  外へ広げて四方に影を回す（下の三日月だけでは物として読めない）。
- ★★★**区切りに線を引かない**（ユーザー指定）。設定から罫を全部落とし、
  **`SectionLabel` ＋ 面のカード ＋ 小区分 `Panel` ＋ 余白**（棚と同じ語彙）にした。
- 検証（件数）… コントラスト **34/34**（★半透明5件は**地と合成してから**測った ――
  生の rgba を測ると全部 1.0 に見える）／機械チェック13本 **0件**。

### ★★★第78巡 ── 暗いのは「地」ではなく「機械」と「オーバーレイ」

第77巡に JOURNAL の地を黒くしたが、3アプリで1つだけ黒く**統一感が無い**との判断。

- **地は3アプリともクリーム `#FFFBF5` の1色**へ戻した。暗さは2つが持つ …
  ①**機械**（`VoiceStudio` のダイヤルとキー。クリームの上の**黒い機械**）
  ②**オーバーレイ**3つ（タスクの入力・声の録音・**設定**）。
  ★**オーバーレイの中の機械は逆に白い**（ユーザー指定）。
- ★★★**盤の赤は2つ。`redOn(面)` が選ぶ** ―― 暗い面には明るい赤、明るい面には
  深い赤（★第79巡に REC の輪だけ例外。上を見よ）。
- ★設定は**すでにオーバーレイ**だったので `TaskComposer` の `LIFT` と同じ作法に。

### ★★★第77〜78巡 ── 色の選び方と券の外形（要点だけ。詳細は `design.md` §3）

- ★★★**5色は4尺度で採点して選ぶ。見分けだけを最大化しない** ―― 第76巡はそれで
  赤が2つになり、**盤の雰囲気が図形に出なかった**。多いタグは先に離す。
- 券は**角丸 0**。丸みは**四隅の切り欠き**が持つ。★★★マスクの層を小さな箱に
  `no-repeat` で置くと、箱の外の α が 0 になり `intersect` が**券を丸ごと消す**。
- 右下の輪は**真ん中にボタンが残り、もう一度押すと閉じる**（ユーザー指定）。

### ★★★第76→77巡 ── 質感は「平均 128 を `soft-light`」

★★★**テクスチャは明暗だけを足すもので、色を変えるものではない。** 乗せたあとの
平均の色が動いていたら、それは質感ではなく色の変更（`multiply` は ΔE 0.135・
**彩度 −20%**／`soft-light` は ΔE 0.001。142,884 画素）。
★★★**しわは低い刻みなので拡大縮小してよい。網点は高い刻みなのでいけない。**
（詳細は `design.md` §3-b と `docs/project_knowledge.md`）

### ★★★第71〜72巡 ── 券を組み立てから作り直し、DEV タブの被りを直した

- ★★★**役と段は1対1**（第80巡に 38/20/11/9 へ組み直した）。★★**写真が余りを
  全部取る**ので spacer は 0本。**縦横比 3:4**。
  ★★★**間隔ではなく「数」で持つ**（px だと幅で割り切れず右端が切れる）。
- **場から影と床を撤去**。**鋏の厚みは 1.85倍**（`HALF` 83／52。★`COIL_Z` と必ず
  同時に動かす）。**光沢 0.85 → 0.14**（すり傷の照りは `scratch` から引く）。
- ★★★**タブバーの高さは `navHeightPx()` から引く。自分で測らない。**
  `.app-nav` の矩形は `NAV_H` と一致しない ―― **Chromium 81px／実機 132px**。

## 改札鋏（第69巡の 9〜23巡目）

### ★★★9〜23巡目の要点（詳細は `docs/explore-redesign.md` §2-b）

- **23巡目**… 持ち手の内側を削った（`clean-nipper.mts` の規則8・平均12）。
  ★★★**青い線は「正面の暗い面」の上にある** ―― シルエットの内端ではなく**正面の面の
  内端から**測る（取り違えると削る量が幅の 60% と出る。実際に出た）。検証 …
  脚の刺さり 13→4.1、描画と青い線の差は右 482件・平均 −0.1／左 513件・平均 +3.0。
- **22巡目**… **パスの整理**（`図4枚 → trace → 生 → clean → 整`。辺 62 → 51）。
  ★整理は**再実行できるプログラム**。形を直すときは規則の目盛りを直して走らせ直す。
- **21巡目**… **部品ごとの図**から組み直した。「重なりがおかしい」の**根本原因は入力**
  （組み立て図1枚から2部品を割っていたので重なりが存在しなかった）。
- **20〜16巡目**… 輪郭を直線と円弧へ／**three.js（WebGL）へ移した**（光の強さは π。
  回す向きは SVG と**逆**）／★`lib/nipperShape.ts` は**生成物。手で直さない**。

★`app/layout.tsx` … `<style>` を `<html>` 直下に `precedence` 無しで置くと**不正な
HTML になり水和が丸ごと失敗する**。`href="tokens" precedence="default"` で解決済み。

## 次の一手

1. ★★**実機で `DEV` →「券」を見て、4案から選んでもらう**。選んだ1案を
   `Ticket.tsx` へ畳み、**`components/explore/samples/` をディレクトリごと消す**
   （そのとき旧版の角丸も新しい外形へ揃える）。★「場」では鋏の質感も見てもらう。
   鋏の直し方は3つ … **形**＝平面図を描き直して `trace` から／**整理と削り**＝
   `tools/clean-nipper.mts` の目盛り／**厚み**＝厚み図か `lib/nipperRig.ts` の `HALF`。
2. **券が上からひらりと降りてくる**（★ユーザー指定）。スキップ／入鋏のあと、
   次の1枚が上から舞い降りる。`TicketStage` は券を場所で受け取るので、
   `card.x/y` と `z` を動かすだけで書ける（★動きは `lib/spring.ts`）。
3. **入鋏とスキップの動き**（★ユーザー指定）… 鋏で穴を開ける／券をスキップする。
4. **提案（TODAY）の操作** … 鋏を掴んで券へ寄せると縁に**吸い付き**、離すと切り欠かれる。
5. **ストック（STOCK）** … 格子・鋏痕での絞り込み・期限。そのあとマップ。

## 未解決

- ★★**地 `#FFFBF5` と紙 `#FAFAF9` の差が比 1.01**（第76巡から持ち越し）。
  **券は影で立てた**（`TICKET_SHADOW`）が、カード・タブバーは未対応。**次に決める。**
- ★`SectionLabel`（`MUTED`）は明るい地で 3.2 ＝ AA 割れ。**既知・未対応。**
- ★**券の「モノ」の英語は Verde 2.81** で大きな文字の 3.0 に 0.19 届かない
  （目盛りの外として明記済み）。代わりに置ける色は 1.43／1.50 でもっと悪い。
- ★**券の小口はこの画角では1画素に満たない**（ほぼ正射影）。浮遊の手がかりが無い。
- ★★**`applySteel` の計算量は未確認**（実機で滑らかに出るか）。★デフォルメと
  バネの脚はまだ手作業。★`/dev/explore` は WebGL の器を**13個**開く。

---

## 重要パス

- ★Explore 刷新の設計 … `docs/explore-redesign.md`／★UI の規約と機械チェック13本
  … `design.md`（前後に必ず走らせる）／目盛り `lib/tokens.ts`／色 `lib/constants.ts`
- 券 … ★**見本帳 `components/explore/samples/`（4案。選んだら消す）**／
  旧版 `Ticket.tsx`（`/dev/explore` だけ。★角丸のまま＝**新しい外形は当てていない**）／
  `PunchMark.tsx`／`lib/ticket.ts`／質感 `lib/printGrain.ts`
  ★外形（角丸0＋ギザギザ＋四隅の切り欠き）は `TicketParts.scallopMask()`。
- ★**色そのものは `lib/constants.ts` の `PALETTE`（6行）だけ**／役との対応は
  同ファイルの `SCHEME` と `lib/palette.ts`
  ／★**面から導く3つ** … `bodyInkOn`（面に載る字）・`redOn`（盤の赤2つ）・
  `inkVarsOn`（地の上）。**表を持たず、導かせる。**
- ★券と鋏を同じ3D空間に置く器 … `components/explore/TicketStage.tsx`
- 鋏（★three.js）… `Nipper.tsx`（画角・動き）＋ `lib/nipperMesh.ts`（立体と質感）
  ＋ `lib/nipperRig.ts`（組み立てと光）／三面図 `NipperViews.tsx`／`app/dev/explore`
  ＋ ★形の生成 … `lib/nipperPath.ts`（手書き）→ `tools/trace-nipper.mjs` →
  `lib/nipperShapeRaw.ts` → `tools/clean-nipper.mts` → `lib/nipperShape.ts`
  （**どちらも生成物。手で直さない**）／凍結 `docs/archive/nipper-shape-21.ts`
- タスク … `components/tabs/GravityTab.tsx`／カメラ `components/tasks/TaskSpace.tsx`
  ／入口の輪 `components/CreateMenu.tsx`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite` / `fetchDirect` / `jinaSlot`）
  ／ 単体チェック `npx tsx tools/jina-check.mjs`
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちて 500）。Playwright は
  `executablePath: "/opt/pw-browsers/chromium"`、**`localhost` で開く**。
  ★**WebGL を撮るには** `--use-gl=angle --use-angle=swiftshader
  --enable-unsafe-swiftshader`（無いと canvas が空で返る）。
  ★★測り方の罠 … ①dispatch 直後に DOM を読むと rAF 前の値 ②遠い行を測ると
  「連鎖」の遅れを不具合と読み違える ③`waitForTimeout(40)` は 2.4 コマぶんしか
  無い ④★**同じ式から書いた2つの値を突き合わせても検証にならない** ―
  「見えている幾何」を測り、**比較できた件数を必ず出す**
