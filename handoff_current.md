# いまどこにいるか（2026-09-14／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**が入り、第105巡に**着地のフレーム落ち**を根治して、手つきを
**「輪ゴム → ばちん → 追いつき」**へ作り直した。
★★★**Explore の刷新（券・改札鋏）は保留**（`DEV` タブごと最後に処分）。
★★★**いま試している大きな変更は1つ**（実機で見て決めてもらう段階）… **アプリごとの
アクセント配色**（`lib/appAccent.ts` の `ACCENT_TEST`。**`false` の1行で元へ戻る**）。
仕様の正 … `docs/home-spec.md`（ホーム。**§6 が引き下ろしの正**）／
`docs/project_knowledge.md` §3-h／`design.md`（UI の規約と機械チェック13本）。

---

## 直近完了

### ★★★第105巡 ── 着地のフレーム落ちの根治／引き出しを「輪ゴム」へ

実機の指摘2件。**①は第104巡で自分が入れた退行。**

**① 着地でフレームが急に落ちる** ―― 原因は2つ重なっていた …
- ★★★**当たり判定の多角形が自己交差していた**（**今回いちばん怖い罠**）。第104巡の
  「間引いて**左右へ折り返す**」は点の順番を崩すので、`Bodies.fromVertices` が
  **凸と誤判定**して交差した多角形を作る。**SAT が「当たっていない」と答え**、
  小さい図形1つが**床をすり抜けて落ち続けた**（`world` にも `detector` にも居るのに
  `pairs: 0`）。その1つが**永久に眠らない**ので**山が毎フレーム塗り直されていた**。
  → **極座標で等角に測り直す**（`PHYS_VERTS` 20→**12**。偶数なら対称性は式が保証）
  ＋**いちばん外の点だけ足す**（2段はくびれなので角0の光線が最広点を通らない）
  ＋**`atan2` で並べてから渡す**。
- ★★★**折り返しで軸の本数が爆発していた**（SAT の費用＝**軸の本数 × 頂点数**）。
- ★★**CPU の profile では 94.9% が `(program)`**（ラスタライズと GPU 送り）。
  **軽くすべきは計算ではなく「塗る回数」だった。** ★`MAX_STEPS` 3→**2**
  （1歩が重くなると「遅れ→歩数が増える→さらに遅れる」の悪循環）。

**② 引き出しのゴム感／流体感が足りない** ―― ユーザー確定は2つ、
**「触った瞬間に canvas へ写し取る」**と**「両端は帯に残る」**（＝**輪ゴムを引っ張る**）。
- ★★★**1px 引いたら canvas の写し取りへ**（`components/home/pillGhost.ts`）。
  **`.band-row` は `overflow: hidden`** なので DOM のままでは帯の外へ垂れられない
  ―― 第104巡が `scaleY` で一様に伸ばすしかなかった理由。
- ★★★**掴んだ一点だけが垂れる**（`bendAt` の `cos²`。**端で厳密に 0**）。
  「そこに止まろうとする抵抗」を**数で足していない**。上の縁は `BEND_TOP`(0.55) 倍。
- ★★★**弾けたら指へ「一気に、でも滑らかに」追いつく**（`K_CATCH`/`D_CATCH`。
  周期12フレーム・減衰比0.5）。第104巡は**瞬間移動**だった。
- ★★★**写し取りで踏んだ罠3つ** … ①**掴んだ瞬間の矩形では 1.2px ずれる**（帯が止まるのは
  段の `onPointerDown` ＝ ピルのあと）→**写し取る瞬間に測り直す** ②**`border` のぶん
  中身が内へ寄る**（`PILL_EDGE`）③**`offsetWidth` は整数へ丸まる**（0.02px 足りず題が
  1文字切れた）→**矩形 ÷ 実測の押下の倍率**。★写真は**帯の `<img>` そのもの**を渡す。
  ★★**引いているあいだは山の canvas を帯の上へ**（`HomeTab` の `zIndex`）。

**検証（件数つき）**
- ① フレーム（Chromium・6倍遅・dpr3・rAF の間隔。**着地を含む**）…
  落下＋着地 p50 **111.6→9.2**／32ms超 **109/109枚 → 81/404枚**。
  落ち着いたあと p50 **113.0→8.8**・p95 **133.7→10.9**・最大 **258.2→26.6**・
  32ms超 **106/106枚 → 0/1324枚**。★遅くしていない実速では**着地を含めて 0件**。
- ② 多角形 6件（費用＝軸×頂点。第103→104→**105**）… 1段ar3 **66→288→18**／
  2段ar1.5 **81→280→72**／3段ar1 **64→176→50**。
  **体÷絵は6件とも 1.0000／重心のずれは 6件とも 0.0000px。**
- ③ 写し取り（WebKit 390×844・dpr2。1px 引いた前後を画素で突き合わせ）…
  **中身（縁の内側）の違いは 提案 1.43%（57096画素・平均差0.99）／
  タスク 0.43%（30456画素・平均差0.23）**。違うのは縁だけで、それは**狙いどおりの垂れ**。
- ④ 曲がり（219×44 を中心で掴む）… **両端は +0.5 / +0.0px**（±1px 以内）、
  掴んだ点は 6/14/22px 引いて **+4.5 / +10.0 / +16.0px**（単調）。
- ⑤ 吸い付き … 28px を **4フレームで届き、2.95px（10.5%）行き過ぎてから戻る**。
- ⑥ 回帰 … 横のはみ出し 0px ／床より下 0 ／横→縦で画素が残る（9921→9877）／
  山は落ち直さない（最小が離す前の 118%）／離して `dueDate` が入る。
- ⑦ 機械チェック13本 **0件**・`tsc` 0・`eslint` 0（警告3は既存）・`build` 成功・
  `pageerror` 0。

### 第104巡 ── 床を抜ける根治／ピルの左右の当たり判定／ゴム→ばちん

- ★★★**実機は 120Hz。`rAF` ごとに1歩進めると物理が2倍速**（めり込みも倍で床を抜ける）
  → **実時間を貯めて `STEP_MS` の整数歩だけ**（実測 60/120/144Hz とも **1秒 59歩**）。
  ★★**Chromium も WebKit も 60fps なので、この環境では絶対に再現しない。**
- ★★★**文字の板だけ `setMass` を呼び忘れていた** ―― matter の既定の密度（px²）になり、
  **他の全部（solid²）と桁が違った**（板 18.8 対 タスク 12.67 → 直して 3.17）。
- ★★★**沈んだだけなら `sink` でその場で床へ戻す**（上から落とし直すと「雨」になる）。
- ★★★**指の当たり判定も `halfWidthAtStack`**（箱のままだと**箱の2割は絵の外**）。
- **引き出しの抵抗を弱めた** … `PULL_ARM` **56→24** ／ `PULL_RESIST` **0.42→0.75**。
  **引き始めが死んでいると、そのあと何をしても取り返せない。**
- **段は 1→2→3 とカクンと生える**（`shownRows`）。★`waist` の連続変形が「普通すぎる」の
  正体だった。**`waist` 自体は残す**（文字の折り返しが読む）。

### 第102〜103巡 ── セーフエリア／引いて日付を割り当てる／器が変わる

- ★★★**セーフエリアは左右対称**（`[16, w−16]`。**タブバーは右下の丸まで含む**。
  第101巡に帯の右端 310 へ寄せたのは誤り ―― `lib/pileBox.ts` に「もう振らない」）。
- **引き下ろしを実装**（`docs/home-spec.md` §6）／**盤を `DateGrid.tsx` へ**／
  **`Item.plannedFor` を足した**（`magazine` は死んでいた）。
- **`Body.setVelocity` は眠った体を起こさない** ―― 山の図形が長押しで動かない正体。
- **器が変わったときの3つ** … 床は器が変わらなくても動く（`FLOOR_CHECK_MS`）／
  **床が上がったぶんだけ山ごと動かす**（`refitPile`）／**外へ出たら拾い直す**（`isLost`）。
- **列は `overflowX` を書かないと横へスクロールする**（ASSIGN が 48px はみ出すだけで
  指で送れて見えた）。**包む側と列（`overflowX: "clip"`）の二重で止める。**
- **山は落ち直さない** … `prev` で居場所を引き継ぐ。新しい1つだけが**指を離した所から
  投げた勢いのまま**落ちる。

### ★★★第89〜101巡までの要点（寸法は上書き済み。教訓だけ）

- ★★★**canvas の `ctx.font` は書体の読み込みを頼まない**し、`document.fonts.ready` は
  **頼んでいないものを待たない**（→`ensureWordFont`）。**待つなら締切。**
- ★★★**中身を入れる引き金は「世界を作り終えた最後の1行」だけ**（途中で抜けると
  山は二度と埋まらない）。
- ★★★**参照の形は全部「円をつないだ形」。正弦波で円に似せない**（角で √2 倍）。
- ★★★**`objectBoundingBox` は 0〜1 のパスを器へそのまま掛ける**（器は正方形。
  外接箱も 0〜1 へ正規化 →`fitBox`）。
- ★★★**実機のバグの正体 ＝ `mask-size` が効かず SVG が固有の 1:1 で置かれていた**
  → **`clip-path`**。★★**Playwright の WebKit でも再現しなかった。**
- ★★★**切るのは写真だけ。札には掛けない**（掛けると `box-shadow` が消える）。
- ★★★**タグを全廃**（タスクの性質は「日付があるか／ないか」だけ。`isDated`）。
- ★★★**ぼやけ ＝ ResizeObserver が2つ**／**下が空く ＝ 床の二重引き**（`.bleed-x-b`）／
  **effect を2本に割る**（1本だと題を1文字直すたび山ごと落ち直す）／
  **タブバーの高さは `navHeightPx()`**（実機 132px／Chromium 81px）。

---

## 次の一手

1. ★★**実機で見てもらう**（第105巡）… ①**着地でフレームが落ちないか**
   （自己交差した多角形と塗る回数を直した）②**引き出しの手つき**
   （掴んだ一点だけが垂れて両端は帯に残る → ばちん → 指へ吸い付いて揺れる。
   垂れが足りなければ `PULL_GIVE`、曲がりが鈍ければ `BEND_TOP`、
   追いつきが速すぎ／遅すぎなら `K_CATCH`/`D_CATCH`）
   ③**写し取った瞬間にカクッとしないか**（この環境では中身の差 0.43〜1.43%）
   ④**すり抜けて落ちないか**（第104巡の3つ＋今回の自己交差）
   ⑤**アクセント配色を採るか捨てるか**（捨てるなら `ACCENT_TEST` を `false`）。
2. ★**`magazine` と `ItemStatus "planned"` を消す**（★**死んだ欄**。`plannedFor` が正。
   起動時の後片付けと永続化の移行に絡むので単独の巡で）。
3. **持ち物** … 右上のアイコン＋数の印（警告）。押すと印のすぐ下に小さな一覧。
4. **フォローアップの夜間生成がまだ無い**（★プロンプトは先に文面で出して承認を得る）。
5. ★ピルを押したときの詳細（まだ無い）。★山の図形はもう押せる（`Piece.nav`）。

## 未解決

- ★★★**提案の生成は止まったまま。** 15 は**表示だけ**の上限なので裏の未読 41 枚は減らず、
  夜間の生成は `POOL_CAP`(40) に掛かり続ける（`app/api/cron/build-brief/route.ts`）。
- ★★★**「器が一度 0×0 になる」道は再現できていない**（第101巡）。**実機でまだ出るなら
  残っているのはこの道** ―― `Pile.tsx` の `read()` と `makeWalls` の下限を疑う。
- ★★**器が大きく変わると図形の大きさが変わる**（横画面では小さくなる）＝**仕様**。
- ★★★**`GravityTab` は `rAF` ごとに1歩進め、体も `PHYS_VERTS` の等角ではない**
  （第104〜105巡は山だけ直した）。**TASK も 120Hz の実機では物理が2倍速**のはず。
  次に TASK を触るとき、**歩数・当たり判定の多角形の両方**を同じ直し方で。
- ★★**GRAVITY の山とホームの山は同じ `PILE_INSET`**（第102巡に揃った）。
- ★★★**札の比は `ITEM_CARD_ASPECT` から来ていない。`BRIEF_CARD_ASPECT` が正。**
- ★★**録音画面の本体は左右へはみ出したまま**（片側 **243px**）＝**仕様**（ユーザー確定）。
- ★★**黄の札と地の比は 1.27**（縁は**影**が持つので、札を切り抜くと縁が消える）／
  `SectionLabel` は明るい地で 3.2 ＝ AA 割れ／**札の中の隙間**（本文が短い札）。**未対応。**
- ★★★**帯の「今日行ける」は営業時間を知らない**（`kind` ごとの表＝**近似**）。

---

## 重要パス

- ホーム … `HomeTab.tsx`／帯 `Band.tsx` ＋ `lib/homeBand.ts`／山 `components/home/`
  （`Pile.tsx` ＋ `pileWorld.ts` ＋ `pilePaint.ts`）／器 `lib/pileBox.ts`／
  配線 `AppShell.tsx`／仕様 `docs/home-spec.md`
- ★★★**引き下ろし … `lib/pullDrag.ts`（算数と `pullBus`）／
  `components/home/pillGhost.ts`（帯のピルの写し取りと曲げ）／`AssignRail.tsx`
  （右端の帯）／`AssignSheet.tsx`（カレンダー）／盤は `components/DateGrid.tsx`
  （`WhenSheet` と共用）**。★★**手つきは「輪ゴム → ばちん → 追いつき」**。
- ★★★**セーフエリアは全画面で `SPACE.lg`、左右対称**（`lib/pileBox.ts` の `PILE_INSET`）。
  **タブバーは右下の丸まで含めてこの幅。もう振らない。**
- ★★★**器が変わったときの4つは `Pile.tsx` の世界の effect と `pileWorld.ts`**…
  床を見張る（`FLOOR_CHECK_MS`）／`refitPile`（床と一緒に動かす）／
  **`sink`（沈んだら床へ戻す。上から落とし直さない）**／`isLost`→`respawn`（本当に外）。
  ★**列は `overflowX: "clip"`**（`AppShell`）。
- ★★★**当たり判定の多角形は「軸の本数 × 頂点数」**（`PHYS_VERTS` 12・等角・偶数）。
  ★★★**`fromVertices` へ渡す前に `atan2` で並べる** ―― 交差すると SAT が
  「当たっていない」と答えて**床をすり抜ける**。
- ★★★**物理の歩数は実時間から**（`STEP_MS`/`MAX_STEPS`(2)。**`rAF` ごとに1歩にしない**）／
  **`setMass` を全部の体に呼ぶ**（呼び忘れると既定の密度で桁が変わる）／
  **指の当たり判定も `halfWidthAtStack`**（絵・物理・指が同じ式）。
- ★★★**タスクの形は `lib/solid.ts` の `stackOutline`（ピルの積み）が正**。
  **段の数は `lib/taskSize.ts` の `rowsOf(title)`**（純粋な関数）。絵も物理も山も
  **そこから引く**（`shapeRowsOf`）。★**`waist` が「ピル ⇄ 図形」の変形**。
- ★★★**文字の割り付けは `lib/textFit.ts` の `layoutInRows` だけ**（段に1行。ベゼルは
  `ROW_FILL` から四方へ）。**2つ持たない。**
- ★★★**塗り／輪郭は `paintShape` と `isDated` が正**（山の `pilePaint.ts` にも同じ分岐。
  **片方を直したら両方**）。
- ★★★**カセットの寸法は `lib/cassette.ts` の1か所**／**芯は `lib/reelHub.ts`**。
- ★★★**書体は3つ** … `SANS`／`LATIN`／**`DISPLAY`（大きな欧文と数字だけ。Anton）**。
  ★★**canvas は `font-variation-settings` を受けない**（**偽コンデンスは撤回済み**）。
- ★★★**札の形は `lib/cardShape.ts`（点の列が正）＋ `CardShapeDefs.tsx`。割り当ては
  `lib/ticket.ts` の `PUNCH_BY_DOMAIN` から導く**。★**`clip-path`。器は必ず正方形。**
- ★★★**山の器は `.bleed-x-b`**／**ResizeObserver は1つだけ**／**文字の板は
  `lib/wordPlate.ts` の1か所**（★DOM で組み直さない）。
- ★★★**帯の「今日行ける」は `lib/todayPick.ts` の1か所**（`kind` ごとの時間帯＝**近似**）。
- ★**色そのものは `lib/constants.ts` の `PALETTE`（6行）だけ**／役との対応は
  `SCHEME` と `lib/palette.ts`／★**面から導く3つ** … `bodyInkOn`・`redOn`・`inkVarsOn`。
- ★UI の規約と機械チェック13本 … `design.md`／目盛り `lib/tokens.ts`／動き
  `lib/motion.ts` ＋ `app/globals.css` の `:root`
- タスク … `GravityTab.tsx`（★ホームと**対**）／`TaskSpace.tsx`／`CreateMenu.tsx`
- 券・鋏（★保留中）… `components/explore/samples/`／`Ticket.tsx`／`lib/nipperShape.ts`
- ブリーフ … `lib/briefPipeline.ts`／単体チェック `npx tsx tools/jina-check.mjs`
- ★★★**WebKit がこの環境で動く** … `executablePath` 無しに起動（**Chromium だけ**
  `/opt/pw-browsers/chromium`）。★★**万能ではない**（第95巡の札のバグは再現しなかった）。
  ★★**CPU を遅くできるのは Chromium だけ**（CDP の `Emulation.setCPUThrottlingRate`。
  フレーム落ちの測定はこれと `Profiler.start/stop` で見る）。
- ★★★**試験の種は `keeps: []` を入れてはいけない**（`migrate` が旧形式とみなして
  **`items` を丸ごと捨てる**）。`wishes: []` は**必要**（無いと全部捨てる）。
- 検証 … `localhost` で開く。★★**山の測定は 8 秒待つ**／**canvas の倍率は場所で違う**
  （山 3／他 2）／`next build` は Google フォントの取得に**ときどき失敗する**。
