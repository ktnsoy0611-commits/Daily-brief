# 現在地（2026-08-18）

現行仕様は `docs/project_knowledge.md` が正。経緯は `docs/archive/`。
このファイルは**常に200行以内**に保つ（更新手順は `CLAUDE.md` の「恒久ルール」）。

- ブランチ … `claude/brief-tab-bind-bugs-9ijd62`。
  ★**毎回 `main` へも push する**（2026-08-18にユーザー確定・確認不要）。
  `main` が Vercel の本番ブランチで、実機はここを見る。作業ブランチへ
  push しただけでは**実機に何も届かない**（第9〜11巡はこれで届いていなかった）。

---

## いまどこにいるか

タスクアプリ（TASK）は**図形**（断面ひとつのベタ塗り）と**入力画面**の両方が
確定した。入力は TickTick 型の即時入力（チャコール地・ツールバー＋ポップオーバー）で、
書いた情報の量がそのまま図形になる。3D・アイソメは持たない。

3 つのアプリのうち、**ブリーフ（EXPLORE）は実運用中**（夜間 Cron が毎日 2 回
デッキを生成し、実機で使えている）。**タスクとジャーナルは UI が出来た段階**で、
中身の運用（Cowork の仕分けとの往復）はこれから。

第10〜19巡は**入力画面（`TaskComposer`）だけ**を直し続けている。同じ症状が
何度も再発したのは、**キーボードと「見えている矩形」の関係の測り方**を
毎回どこかで外していたため。第18巡でその測り方（`--kb`）を確定させ、
第19巡で**ずれ（`--vvtop`）も同じ決まりに揃えた** — 「開いているあいだは
凍らせる／閉じたら測り直す」の 1 本だけにする。

---

## 直近で完了したこと（第19巡）

- ★★★**アイコンの反応が悪いのは 3 つ重なっていた**（実機報告）。
  ①`touch-action` を宣言していないので iOS が「送る指か」を見極めるまで
  `pointerdown` を握る ②**当たり判定が丸そのもの（32px）**で、6px 外れると
  帯に当たって何も起きない ③`:active` が当たらず**沈む合図が出ない**ので
  押せたのかも分からない。`Press` に `touch-action: none` と自前の
  `data-pressed` を入れ、当たり判定は 44 に広げた（**丸の場所は 32 のまま**）
- ★★★**「4回に1回ガクッと動く」の正体は `--vvtop`**。第18巡でずれの追従だけ
  「いつでも追う」にしていたが、iOS は**欄が入れ替わるたびに矩形を数十px
  上下に振る**ので、そのたび中身ごと瞬間移動していた。`--kb` と同じ
  **「開いているあいだは凍らせる／閉じたら測り直す」**1 本の決まりに揃え、
  ずれの追従にも帯と同じ間合い（280ms）を掛けた
- 日程の**「クリア」を塗ったボタンへ**（ユーザー指定）。幅いっぱいのピルを
  赤（#EE1B33）で塗り、文字は相方の淡い赤
- ★★**単体テストは「化石」を試していた**。`scratchpad/*.mjs` は `lib` の写しで、
  焼き直さないと固まったまま。焼き直したら 3 ファイルが消えた API を試して
  おり、`prism.test.mjs` の 196 件は**存在しないモジュール**が相手だった。
  写しを焼き直し、テストをいまのモデルへ直した（撤去して 90 件）

## その前の巡（第18巡）

- ★★★**`--kb` の式を確定**。`max(0, 器の offsetHeight − vv.height)`。
  `offsetHeight` は**組版上の高さ**なので、出入りの transform も `offsetTop` の
  端末ごとの解釈も**矩形のずれ**も一切混ざらない。外した4通り（`innerHeight`
  基準／`rect.bottom` 基準／`offsetTop + offsetHeight`／0×0 の物差し）は
  `docs/project_knowledge.md` に表で残した
- ★★★**見えている矩形のずれ（`vv.offsetTop`）を `--vvtop` で追う**。iOS は
  キーボードを出すとき**矩形そのものを下へずらす**ことがあり、器は
  `fixed; top: 0` のままなので**上のバーが画面の外へ出て図形が上で切れて**いた
  （実機の報告）。器は動かさない約束を守り、**器の子を1枚の面 `[data-fit]` で
  包んで** `translateY(var(--vvtop))` を掛ける。`BottomSheet.tsx` に同じ教訓が
  先に書いてあったのを、入力画面だけが落としていた
- ★★★**閉じる合図を `--kb` ただ1つに**。`focusout` は**焦点を戻すだけで
  決して閉じない**（キーボードが引っ込んでいれば `--kb` 側が閉じる）。
  第11〜17巡はこれが**「＋を押すとすぐ閉じる」の主因**だった —
  **主スレッドが詰まると `focus()` が居着かない**ので、山が動いている gravity で
  ＋を押したときほど起きた
- ★★★**時計ではなく状態で決める**。6× に絞ると `setTimeout(260)` が 940ms や
  3195ms に遅れて発火するので、「開いてから 600ms 以内か」という壁時計の窓は
  用をなさない。見るのは「キーボードはまだ出ているか」
- 下がりは **200ms ごとに 2 回続けて確かめてから**閉じる（一瞬の谷で閉じない）。
  キーボードを閉じてから消えるまで実測 700ms
- 検証を **4通りの型**へ（`scratchpad/geo4.mjs`）… A 無し / B レイアウトごと縮む /
  C `vv.height` だけ縮む / **D ずれる**。どの型でも「上のバーの上端＝見えている
  上端」「帯の下端＝見えている下端」「図形の中心が範囲の中」が成り立つこと
- 「開いてすぐ閉じる」は **6× に絞って**見る（`scratchpad/plus.mjs`）。
  空いている主スレッドでは再現しない

### 第10〜17巡（要点だけ）

- ★**出入りは「1枚の板が下から出て下へ帰る」だけ**（ユーザー指定）。不透明度で
  出し入れする形は4度作って4度差し戻された。中の部品は自前の出入りを持たない
- ★**動き出しは組み立てが終わってから**。CSS のアニメーションは時計で進むので、
  マウントに 283ms かかる間に 320ms の 88% が過ぎ、**板がいきなり所定の位置に
  現れて**いた。rAF を2回待ってからクラスを付ける。図形を焼くのも `animationend` 後
- ★★**書体の「全消し」が効きすぎていた**。`onFontsReady` が
  `document.fonts.ready.then(cb)` で**購読しただけで呼び返して**いたため、
  `SolidCanvas` が1枚マウントされるたびに `clearSolidBitmaps()` が走り、
  **入力画面を開くだけで山の絵が全部消え、閉じたあと焼き直されて文字が点滅**
  していた（Chromium でも色数 241→51 で再現）
- ★**入力画面は「ただのオーバーレイ」**。drift も gravity も**＋は保存せず手元で
  持つ**（押した時点で輪が回り、山が落とし直っていた）。落とし直すのは
  **まだ在るのに山から押し出されたときだけ**＋開いている間は rAF を止める
- ★**「まだスクロールできる」を根治**（3巡）。送っていたのは器ではなく**その先**
  （後ろのタブの列・ページ）。html に印を立てて後ろを止め、`touchmove` を
  非passive で受けて**本当に溢れている箱の中でなければその場で止める**
- ★**`--kb` を動かしてよい場面をごく限った**（メモ・持ち物で「画面が動く」を
  2度差し戻された）。①何かが開いている間は一切聞かない ②60px 未満の揺れは無視
  ③下がるのは待って確かめる
- ★**器は一度も動かさない**（録音オーバーレイと同じ書き方に固定）
- タスク: **期日は一点(アラーム型のダイアル)・期間は範囲(タイムライン)**。刻みは
  どちらも15分。期間の浮きは 280x284、帯の左右に**指を滑らせる余白 34px**
- タスク: **期日か期間かは `endDate` だけで決める**／カレンダーは日を選んでも
  閉じない／「クリア」は消すものを名指しする
- タスク: **キーボードが閉じない**を4段構えに（`<div role=button>`＋非passive な
  touchstart ＋ 行の onBlur ＋ layout effect の受け皿）
- タスク: **日程だけキーボードを閉じる**。左上 ✕ で取り消し・右上 ✓ で確定
- ★**左右だけ画面いっぱいにするのは `.bleed-x`**（列の 16px padding で切れていた。
  drift の輪で3巡ぶん誤診した）
- 全体: **文字が出ない／不安定**を根治（第6巡）。①`loadingdone` の全消しを
  400ms にまとめる ②判定キャッシュの偽の焼き付きを外す ③門は600msで必ず開ける

---

## 次に着手すること

1. **実機（iOS Safari）での確認**（この環境で WebKit を動かせないため構造的な制約）
   - ★**上のバーが見えるか・図形が上で切れないか**（第18巡の本題。
     キーボードを出したときに矩形がずれる端末で崩れていた）
   - ★**＋を押してすぐ閉じないか**（とくに gravity で山が動いている最中）
   - ★**キーボードを閉じたら入力画面が閉じるか**（iOS の「完了」・下スワイプ）。
     逆に、重要度やタグを押しただけで閉じてしまわないこと
   - ★**入力画面の出入りが滑らかか**／**指で動かないか**（スクロール）
   - ★**時刻**（期日＝ダイアルが回るか / 期間＝帯をつまむ・端で伸ばす）
   - ★**カレンダーの読みやすさ**（今日／選んだ日／開始／終了の見分け）
   - ★**drift**（端で切れていないか・一周するか・図形の間隔）
   - **文字が出るか**（第6巡の本題。Chromium では2〜4秒で全部出る。実機が本番）
   - 図形16個ぶんの落下の重さ（Chromium・4×絞りで合計1300ms前後 / 最大190ms）。
     ★数字は**温まってから**測る。起こした直後の1回目は1.5〜2倍出る
   - 間引きの手ざわり（40件で10件しか出ない）／期限による大きさの差（2.2〜0.45倍）
2. **Cowork のプロンプト更新**（`COWORK-ROUTINES.md`）… 候補の項目を
   `いつ`（★**日付で書かせる**。YYYY-MM-DD）/ `道具・場所` / `持ち物` /
   `タグ`（英字5つ）へ揃える。日付で書かれないと期日にならず、
   `lib/inboxImport.ts` がメモへ回してしまう。
   ★**プロンプトの全文を提示して承認を得てから**実装する。

---

## 未解決・持ち越し

- **実機 Safari の未検証** … タスクアプリ全般、ジャーナルの円のドラッグと
  マイクの解放、`.full-bleed` でタブバーの下へ潜る見え方。
- **候補のうちジャーナル・ウィッシュ・ストックの3種の行先が未定**
  （データとしては `AppState.inbox` に残っている）。
- **1日の終わりに3アプリの要素を1枚のポスターへプレスする**という最終出力
  （§38 の体験の構造）は未着手。
- **完成時に撤去するもの** … `lib/taskDemo.ts` と「デモを入れる」ボタン、
  `ProfileTab` の「ブリーフ生成の実験」カードと「今すぐ生成」。

---

## いま触っている領域のファイル

```
components/tasks/TaskComposer.tsx 入力画面（下書き・キーボード・--kb/--vvtop・重なり）
components/tasks/WhenSheet.tsx    日程（★キーボードを閉じるシート。TickTick型）
components/tasks/TimeRange.tsx    ★時刻(タイムラインで範囲を選ぶ)
components/tasks/Popover.tsx      ポップオーバーの器と ★`Press`(押せる面)
components/tasks/ComposerFields.tsx 重要度 / タグ / テキスト
components/tasks/ComposerToolbar.tsx 下の帯のアイコン5つ
components/tasks/SolidCanvas.tsx  図形1つを描く
components/tabs/GravityTab.tsx    落として積む（matter.js・描画ループ・焼き予算）
components/tabs/DriftTab.tsx      候補の円環カバーフロー
lib/ground.ts                     画面の地色（html の背景 ＋ theme-color）
lib/solid.ts / lib/solidPaint.ts  幾何（純粋関数）／描画とビットマップの LOD
lib/textFit.ts                    グリフのアトラス・★書体の門・`layoutInShape`
lib/taskSize.ts / lib/taskTags.ts 面積と落下順 ／ 5つのタグ → 色と書体
lib/inboxImport.ts                Cowork の候補を取り込む（`いつ`を日付として読む）
app/layout.tsx                    和文フォントの読み込み（★1書体1ウェイトまで）
```

## 検証（`scratchpad/`）

★**単体テストの前に必ず写しを焼き直す**（`lib/*.ts` → `*.mjs`。焼かないと
固まったままで、消えた API を試し続ける）:
`for m in solid taskSize taskTags inboxImport; do npx esbuild lib/$m.ts --bundle --format=esm --outfile=$S/$m.mjs; done`

```
tap.mjs    ★アイコンの当たり判定・沈む合図・連打で跳ねないか・クリアの塗り
geo4.mjs   ★入力画面の幾何を4通りの型で（A無し/B縮む/Cvvだけ/Dずれる）
plus.mjs   ★＋を押してすぐ閉じないか（引数=CPUの絞り。6 で見る）
v15/v14/v12/v10/v8/v7/v6.mjs  第6〜15巡ぶんの回帰
smooth.mjs 出入りの落ちたフレーム / blink.mjs 図形の文字の点滅（色数）
swipe / seam / junk / text / drift2.mjs
solid.test / tag.test / inbox.test.mjs  単体 90 件
```

## ユーザー側の作業（アプリの外）

- Cowork に**毎晩のタスク「1日を仕分ける」**を設置する（例: 02:00 JST）。
- 任意: `OPENAI_API_KEY` を Vercel に登録すると文字起こしが Whisper 系になる。
