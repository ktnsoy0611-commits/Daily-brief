# いまどこにいるか（2026-09-17／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。★第110〜112巡に「落とし直し」を根絶、
★★★**第113〜114巡に帯の隙間をレイアウトへ作り直して「どこからでも・どこへでも
入る」を通し**（並びは**留め金**）、★★★**第115〜116巡に起動の代金を払い、形を
「ピルの積み」・大きさを「段の高さ」1つ**にし、★★★**第117〜118巡に配色を総入れ替え
して手ざわりの4つの真因を根治**、★★★**第119巡に「おすすめ3件」と Explore の詳細の
画面**を入れた。★Explore の刷新（券・改札鋏）は保留（`DEV` タブごと最後に処分）。
仕様の正 … `docs/home-spec.md`（**§6 が引き下ろしの正**）／
`docs/project_knowledge.md` §3-h／`design.md`。

---

## 直近完了

### ★★★第119巡 ── おすすめ3件を山へ／Explore の詳細の画面

> 「**帯に流れるのはストックの中で今行けるもの。Explore で溜まっている（もしくは
> 新着の）もののうち最もおすすめなのを3件ほどホームに落とす。タップすると
> Explore のそのカードに飛べるように**」「**画像をタップすると、マスクの形が回転
> しながら拡大して詳細画面に遷移。下の方がブラーで詳細文、下にスクロール**」
> 「**おすすめ3件は時間とか関係なく私が一番好みそうなものを選べばよい**」

- **場所の入れ替え** … 帯の上の段は**ストックの「今行ける」だけ**（`todayPick`）。
  **未読の提案は山へ**（おすすめ3件）。★押すと Explore のそのカードへ／
  ★**帯へ運ぶと KEEP してストックへ**（`lib/keepCard.ts` が生きる）。
- **新設 `lib/offerPick.ts`** … ★**AI を使わない**（材料はクライアントに全部ある／
  夜間生成が止まっていても効く／嘘をつかない）。**願い 3.0 ＞ ゴール 2.0 ＞
  好みの語 0〜2.0 ＞ ドメインの KEEP 率の差 ±1.0 ＞ 新着 0.6**、
  **同じドメインは2枚まで**。★**時間帯は見ない**（ユーザー確定）。
- **新設 `components/explore/CardDetail.tsx`** … ★**本文は `BriefCard.detail`**
  （`briefPipeline` がスクレイピングから生成済み。**新しいパイプラインは要らない**）。
- ★★★**育つのは `scale` ではなく幅と高さ** ―― CSS は `scale` を線形に補間するので
  **`scale(1/s)` は途中で逆行列にならない**（実測 真ん中で積 **2.04** ＝ 写真が2倍に
  膨らむ）。**回転と平行移動は足し算なので厳密に打ち消し合う**ので、その2つだけを
  打ち消しに使い（**順も逆**）、大きさはレイアウトで育てる。
- ★★★**`createPortal` で `body` 直下へ**（列の `transform` が独立した重なりの文脈を
  作るので **`zIndex` 70 でも nav の下に潜る**）／★★★**`--nav-h` を読まない**
  （未定義の変数を `calc()` に入れると **`padding` の一括指定ごと無効で四方が 0**）。
- **検証** … おすすめ3件が**願い／好み／ゴール**の順で落ちる（ドメインも3種）／
  **押すと Explore のそのカードが出る**（題を実測）／詳細が開いて**スクロールできる**
  （950 > 844）／機械チェック13本 0 件・`tsc`・`eslint`・`build`・`pageerror` 0。

### ★★★第118巡 ── 手ざわりの4つの真因を根治／色／山のスケール

> ①**提案のピルを離しても追加されず消えた** ②**指を離す前に帯へ吸い寄せられて
> 横にしか動かない** ③**隙間がぎこちない・重なる・ガクガク震える・全然アニメーション
> していない** ④**引き出した図形を離すと瞬間移動する** ⑤**ジャーナルのピンクをやめ、
> タスクのグレーをもっと薄く** ⑥**多すぎると操作できないので多さでスケールを調整**

- **①** `offer-` に対応する `Item` が無く `items.find` が原理的に当たらず黙って
  return していた → **引き下ろす＝KEEP**。式は新設 **`lib/keepCard.ts` の1か所**
  ＋ `unreadEntries`（号のキー）。実測 `items` 0→1・決定 1（**3/3 回**）。
- **②** `stepGhost` の `if (g.aim) { springTo(catchX/Y, aim.x/y) }` が**絵の座標を
  奪っていた**（`aim.y` ＝ 段の中心の1本の線）→ **枝ごと削除。`Ghost.aim` は
  `boolean`**で、役は `tTo` を **`PILL_HINT`(0.55)** へ寄せる合図だけ。★★**第109巡に
  ユーザー確定で入れたものを本人が撤回。復活させない。**実測 **差 0.000px**。
- **③** 挿し口 `at` に**遊びが1つも無く**、変わるたび `m.open.p = 0` に戻していた
  → **`GAP_HYST`(22) ＋ `bandSettling`（閉じ切るまで乗り換えない）**。
  実測（±25px を3往復）… 飛び **11〜12 → 5 回**／**席の合計の幅 38〜150 のばたつき
  → 150 で一定**（8px 以上縮んだ回数 **3〜4 → 0**）。
- **④** 落とし所に**支点（`dx`/`dy`）**を渡していた（正は**絵の中心 `hx`/`hy`**）。
  **第108巡と同じ罠の裏返し** → 実測 ずれ **44.93px → 0.00px**。
- **⑤** JOURNAL ＝ **ラベンダー `#B2AFCE`**（地 2.05／黒いリール 7.01）。タスク ＝
  **`TASK_GREY` `#BFBBB2`**（地 1.86／黒字 7.76。無彩色の**6段目**）。★ピンクは
  `redOn()` の**暗い面の赤**として残す。
- **⑥** **帯が覆う高さを予算から引いていなかった**（帯は山の器へ重ねてある）→
  `buildPieces(…, bandH)` ＋ **混み具合 `crowdOf(n)`**（長さの倍率。予算は `crowd²`、
  板は `crowd`）。実測 帯からの余裕 **9件 79→218px／14件 108→238px**。

### ★★★第117巡 ── 配色の総入れ替え（承認済み）

- 参照画像（Jardins de Métis の入場券）から**画素を測って 7色**。
- **タスク＝無彩色の塗り ＋ 黒の字**（輪郭のピルも字は黒）。**帯・山・GRAVITY・
  DRIFT・入力画面すべて追従**。★色は第118巡に `TASK_GREY` へ。
- **ドメイン4は色と形の組** … バショ＝緑・四つ葉／タイケン＝赤・六角形／
  ジョウホウ＝青・波打つ四角／モノ＝黄・トゲトゲ。**形は1行も変えていない。**
  ★**提案の札の面もドメインの色**（第94巡の「面＝EXPLORE の1色」は撤回）。
  ★★★**`lib/appAccent.ts` を削除**（テストの決着）。**復活させない。**
  ★`redOn()` は残す（暗い面では赤 3.19 よりピンク 4.09 が読める）。

### 第115〜116巡 ── 最初の落下の重さ／形と大きさ

- ★★★**GRAVITY が軽いのは「最初の画面ではない」から** ―― ホームが払っていたのは
  **起動の代金**。**①**先読みが落下に割り込む → **落ち着いてから1つずつ**／
  **②**見えていない `VoiceStudio` の `clientWidth`（**524ms**）→ `appActive` で止める／
  **③**1フレームで全部焼いていた → **1フレーム 2枚**／**④**和文は文字ごとに別の断片。
  **落ちる前に頼む**。★実測 **フレーム 58 → 115〜127**／90%tile **233ms → 50ms**。
- ★★★**形は「ピルの積み」**（第115巡の S 字は第116巡に撤回。`foldsAt`/`sideFolds` 削除）。
- ★★★**大きさは「段の高さ」1つ**（`rowSpecOf`）。第115巡の「面積を1つ」では
  **段が増えるほど段が痩せた**（倍率 40 で 1段 67.4／3段 29.9px ＝ 幅 2.25倍）→
  **段の高さを 1 に固定**（`h = 段の数`／`w = rowAspect`）。実測 9件とも 40.0px・
  字 24.8px（**幅 1.00倍**）。★★★**`specOf` は 1 行も変えない**（TASK は重要度のまま）。

### 第113〜114巡 ── 帯の隙間をレイアウトへ／どこからでも入る

- ★★★**隙間と席は `transform` のずれでは書けない。レイアウト（`.band-pad` の幅）が
  持つ** ―― 同じ id のピルが2周ぶん居るので別々の値を書けない（実測 2周目が
  **152px 飛ぶ**）。**両側 ±w/2 も片側 +w も同じ理由で直らない。**
- ★★★**帯ぜんたいを `lead`＋隙間の半分だけ左へ戻し、挿し込みで「送り（px）」へ
  畳み替える**（`currentTime` を直接ずらすと 156px 飛ぶ）／**境目は開く前の位置で
  測る**／**挿し口が移るときは受け皿 `shut` へ**／**開けているあいだ組み直さない**／
  ★★★**`K_OPEN`（5組目）** … 90% まで **117ms**・行き過ぎ **0.0%**。
- ★★★**白抜き ⇄ 塗りは `inkMix` が返す「量」で渡る**（実測 色の差 157 → 21）／
  ★★★**帯へ入れる道は2本あるのに実装は片方だけだった** → 狙いは**1本**／
  ★★★**弾けは掛け金**（`latched`）／★★**戻した提案が時間帯の門で落ちていた**
  → 留め金の付いたものは門を通さない。
- ★★★**並びは「留め金」（`AppState.bandPins`）**。`tasks` は触らない ―― ① GRAVITY と
  ALIGN まで動く ② 声の候補とフォローアップは持ち主が別で動かせない。
  ★**挿し口は index ではなく「左どなりの id」**。★**日付 `9.15`／曜日 `WED` は
  黒いピルに白い文字**（TASK の板は変えていない）。

### 過去の巡の要点（詳しくは `docs/project_knowledge.md` §3-h）

- ★★★**第111〜112巡** … 落とし直しの真因は **`clearOverlap` が据え置きの山にも
  掛かっていた**こと（傾いた図形の AABB は必ず重なる）→ **`Piece.fresh` だけ**＋
  **体を作り直さない**／**下の段の9件の打ち切りが戻したタスクを消していた**／
  **字も1文字ずつ `bendAt`**／**運ぶ図形は角度を留める**。
- ★★★**第109巡 … めり込みの真因は「湧いた瞬間から重なっている」**（60ms で落ちる距離
  **2.5px** に対し散らばり **200px**）→ 投入の直前に `clearOverlap`（372件 → 10件）／
  ★★★**眠りは `GravityTab` と同じ**（**大域の門は全削除**）／**`positionIterations` は 6**。
- ★★★**当たり判定の光線は「正規化した空間」で飛ばす**／**`fromVertices` へ渡す前に
  `atan2` で並べる**（交差すると床をすり抜ける）／**canvas の dpr の上限は 2**／
  **ループは止める**（`dirtyRef`/`dragRef` を立てる所では必ず `wake()`）／**寿命は3重**／
  **隠すのは `opacity: 0`**／**`navHeightPx()` を rAF から呼ばない**／★★★**実機は
  120Hz。この環境（60fps）では再現しない**／**`setVelocity` は眠った体を起こさない**。
- **第89〜101巡** … ★★★**canvas の `ctx.font` は読み込みを頼まない**し
  `document.fonts.ready` は**頼んでいないものを待たない**（→`ensureWordFont`。**待つなら
  締切**）／**参照の形は全部「円をつないだ形」。正弦波で円に似せない**／
  **`objectBoundingBox` の器は必ず正方形**／**実機のバグ ＝ `mask-size` が効かず SVG が
  1:1**→`clip-path`／**タグを全廃**／**ぼやけ ＝ ResizeObserver が2つ**／**effect を2本**。

## 次の一手

1. ★★★**実機で見てもらう**（第118〜119巡）… ①**離すまで指に追従するか**／
   ②**帯の隙間の開き方**（震え・重なりが消えたか）／③**離したときに飛ばないか**／
   ④**ラベンダーと薄いグレー**／⑤**図形が多いときの大きさ**（帯の裏へ伸びないか）／
   ⑥**山のおすすめ3件が「好みそう」か**／⑦**押すと Explore のそのカードへ飛ぶか**／
   ⑧**写真を押したときの回りながら開く遷移と詳細の画面**。
2. ★**`magazine` と `ItemStatus "planned"` を消す**（死んだ欄。`plannedFor` が正）。

## 未解決

- ★★★**提案の生成は止まったまま**（裏の未読 41 枚が減らず `POOL_CAP`(40) に掛かる）。
  ★おすすめ3件の母集団もこの池なので、**生成が戻るまで新着は増えない**。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も焼く予算も直していない**（山だけ）。
- ★**TASK（GRAVITY）はホームと揃えていない2つ** … 日付・曜日の板は「文字そのものが
  図形」／重要度で大きさが変わるまま（形だけ共通）。**確認していない。**
- ★**起動の代金は残っている**（実測 1フレーム 667ms）。★**落ちた図形が山に当たると
  3〜4px 動く**（物理。直さない）。★`SectionLabel` は明るい地で 3.2 ＝ AA 割れ。

---

## 重要パス

- ホーム … `HomeTab.tsx`／帯 `Band.tsx` ＋ `lib/homeBand.ts` ＋ `bandMotion.ts`／
  山 `components/home/`（`Pile.tsx`＋`pileWorld.ts`＋`pilePaint.ts`）／器 `lib/pileBox.ts`／
  ★★★**引き下ろし … `lib/pullDrag.ts`／`pillGhost.ts`／`AssignRail.tsx`／
  `AssignSheet.tsx`／盤は `components/DateGrid.tsx`**（詳細は `CLAUDE.md` の地図）。
  ★**手つきは「輪ゴム → ばちん → 変形」**／★★★**絵は `hx`/`hy`（落とし所にも支点を
  渡さない）**／**姿は掛け金 `Ghost.pill`**／**遊び `RAIL_HYST`**／**戻りは2段**
  （`RAIL_NEAR` → `PULL_ARM`）／**変形 `K_SWING`・吸い付き `K_CATCH`**／★★★**位置は
  最後まで指**（`Ghost.aim` は `boolean`。役は `tTo` を `PILL_HINT` へ寄せるだけ）／
  ★★★**弾けは掛け金**（`latched`）／★★★**帯へ入れる道は2本で狙いの出し方は1本**／
  ★★★**白抜き ⇄ 塗りは `inkMix`（`pillGhost.ts`）の1か所**（掛け金で切り替えない）。
- ★★★**帯の物理っぽい動きは `components/home/bandMotion.ts` の1か所**。
  **隙間と席はレイアウト（`.band-pad` の幅）が持つ**／**帯ぜんたいを `lead`＋隙間の
  半分だけ左へ戻し、挿し込みで「送り（px）」へ畳み替える**／**バネは `K_OPEN`（隙間と
  席）と `K_SWING`（一発）**／**境目は開く前の位置で測る**／**開けているあいだ
  組み直さない**／**挿し口は `after`（左どなりの id）＋ 遊び `GAP_HYST`・`bandSettling`**。
- ★★★**速さの3つ**（第115巡）… **先読みは落ち着いてから1つずつ**／**見えていない
  `VoiceStudio` は寸法を測らない**／**1フレーム 2枚まで焼く ＋ 書体は落ちる前に頼む**／
  ★★★**代理の体**（`Pile.syncProxy`／`ghostBodyOf`）… **絵は体そのもの**／**体は絵の
  中心**／**`piecesRef` に混ぜない**／**`clearOverlap` の前に消す**／**締切 `HANDOFF_MS`**／
  ★★**山の倍率は中身が変わっても決め直さない**（`hold`）／★★**帯が覆う高さは予算から
  引く**（`bandH`）＋**混み具合 `crowdOf(n)`**（予算は `crowd²`・板は `crowd`）。
- ★★★**セーフエリアは全画面で `SPACE.lg`、左右対称**／**器が変わったときの4つ**
  （`FLOOR_CHECK_MS`／`refitPile`／`sink`／`isLost`→`respawn`）／**器は `.bleed-x-b`**／
  ★★★**当たり判定の多角形は「軸の本数 × 頂点数」**（`PHYS_VERTS` 12・等角・偶数）／
  **投入の直前に `clearOverlap`**／**質量は全部同じ密度に**／**`atan2` で並べてから
  `fromVertices`**／**眠りは `GravityTab` と同じ**（`wake()` を忘れない）／**タスクの形は
  `stackOutline`（ピルの積み）・段の数は `rowsOf(title)`・大きさは `rowSpecOf`
  （`unit` ＝ 段の高さ）・`waist` が「ピル ⇄ 図形」**／**塗り／輪郭は `paintShape` と
  `isDated`**（`pilePaint.ts` にも同じ分岐。**両方直す**）。
- ★**カセット `lib/cassette.ts`**／**芯 `lib/reelHub.ts`**／**札の形 `lib/cardShape.ts`**／
  **文字の板 `lib/wordPlate.ts`**（★日付・曜日は**黒いピルに白い字**）／**帯の
  「今日行ける」 `lib/todayPick.ts`**／★★★**書体は3つ**／★**色は `PALETTE`（7行）だけ**
  （面から導く `bodyInkOn`・`redOn`・`inkVarsOn`・`mixHex`）／★規約13本 `design.md`。
- ★★★**おすすめ3件 `lib/offerPick.ts`**／**詳細の画面 `components/explore/CardDetail.tsx`**
  （★**育つのは幅と高さ。`scale` の打ち消しは途中で効かない**／★**`createPortal` で
  `body` 直下**／★**`--nav-h` を読まない**）／**カード → `Item` は `lib/keepCard.ts` の1本**。
- 検証 … ★★★**WebKit は `executablePath` 無し／Chromium だけ `/opt/pw-browsers/chromium`。
  CPU を遅くできるのは Chromium だけ**＝速さの測定はそちら／★★★**種に `keeps: []` を
  入れない**（`migrate` が `items` を丸ごと捨てる。`wishes: []` は必要）／★★**山の測定は
  8 秒待つ**／★★**帯を出す種には「日付の無いタスク」が要る**／★★★**帯の飛びは
  「見えているピル」だけで測る**（2周目の控えは画面の外＝152px の偽陽性）／
  ★★**`bandBus.rows` の添字は論理の段**（0＝提案／1＝タスク。DOM の順ではない）／
  ★★**WebKit は続けて何回も起動すると落ちる**（1プロセス1ケース）／★★★**速さは
  `Tracing` で割り、`rAF` と `setTimeout` を包んで呼び手ごとに測る**。
