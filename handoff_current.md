# いまどこにいるか（2026-09-23／ホームを作っている最中）

**ホームが動いている**（帯3段＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。★第110〜118巡に「落とし直し」を根絶して
**帯の隙間をレイアウトへ作り直し**（並びは**留め金**）、**起動の代金を払い**、
**大きさを「段の高さ」1つ**にして**手ざわりの真因を根治**、第119〜120巡に
**「おすすめ3件」と Explore の詳細の画面**、第121〜123巡に**提案の図形・
帯の3段目（ニュース）・六角形の歪みと当たり判定**、
第124巡に**「カーブ」を1つへ揃え**、第125巡に**帯の送りを循環**させ、
第126巡に**帯の払いに滑りを足して Explore の2段階を「優雅」へ作り直し**、
★★★**第127巡に配色を「4色」へ絞り、塗りと文字を「組」で決め、ピルを半透明の
塗りにし、提案を帯へ戻せなくした**。★和文は Noto Sans JP。
★Explore の刷新（券・改札鋏）は保留（`DEV` タブごと最後に処分）。仕様の正 …
`docs/home-spec.md`（**§6 が引き下ろしの正**）／`docs/project_knowledge.md` §3-h／`design.md`。

## 直近完了

### ★★★第127巡 ── 4色のカラースキーム／塗りと文字の組／半透明のピル／提案は戻せない

★指示の全文は `docs/project_knowledge.md` §3-h の同名の節。

- **①有彩色は4色だけ**（参照画像＝Clustr の配色見本）… **TASK ＝ 赤 `#FD5959`**
  （危険も同じ赤）／**JOURNAL ＝ 黄 `#FCFF82`**／**提案 ＝ オレンジ `#FF9C6D`**／
  **その他（NEWS・選択・肯定）＝ 青 `#AFC5FF`**。★**地は替えていない**（`#F7F6FB`）。
  ★★★**提案のジャンルは色で分けない** ―― `DOMAIN_COLOR` の4行は**全部オレンジ**。
  **ジャンルは形が言う**（`lib/cardShape.ts` は 1文字も変えていない）。
  ★★**表は4行のまま残す**（ユーザーの「一旦」＝戻すときはその4行だけ）。
  ★★★**`danger` だけは青にしない** ―― 削除やエラーを青にすると、**押してよいもの
  （選択＝青）と取り返しのつかないものが同じ色**になる。
  ★`GREEN`/`GREEN_INK`/`BLUE_INK` の**名前は変えていない**（`GREEN` はもう青）。
- **②塗りと文字は「組」** … `lib/palette.ts` の **`PAIR`（4行）** を `bodyInkOn()` が
  **先に引く**。**青 ⇄ 赤** ／ **黄 ⇄ オレンジ**。4色以外は今までどおり比で選ぶので
  **判断は増えていない**。★★★**実測 1.82〜1.93 ＝ 目盛りの外**（参照画像は大きな字
  なので成立するが、11〜16px では読めない。**ユーザー指定の上での選択**で
  `design.md` §3-2 に**印**が在る）。★**地の上の小さな字には 4色を使わない**
  （4色とも地との比 1.01〜2.89）。そこは `INK`（15.5）。
- **③半透明のピル**（`lib/pillFill.ts` 新設。`PILL_ALPHA` 0.42）…
  ★★★**α ではなく「地と混ぜた不透明色」** ―― **1px 下へ引いた瞬間に DOM から
  canvas へ写し取る**ので、α のままだと重なる相手が変わって**その瞬間に色が変わる**
  （実測 … 混ぜれば DOM `rgb(250,180,183)` と canvas の 377 画素が**完全に一致**）。
  ★★★**日付なしの図形（山・GRAVITY）も同じ塗り** ―― 山の側だけ地のままだと、
  幽霊がピルから図形へ渡る1フレームで色が飛ぶ。
  ★★★**`lib/halftone.ts`（`HT_STEP`/`HT_DOT`/`PILL_KNOCK`）は削除。復活させない。**
  ★★**`pillWidth()` からも `PILL_KNOCK × 2` を同時に外す**（3か所が同じ数を読む）。
- **④提案は帯へ戻せない** … `Pile.dragRef` に **`band`**（＝`place && kind !== "offer"`）。
  ★★★**`place` と分けたのは、右端の ASSIGN（日付を付け直す）を残すため**。
  ★実測（山の16点を長押しして帯まで運ぶ）… **提案 0/16 回**（器は素の 8.0px のまま）／
  **タスク 1/16 回**（最大 **131.2px**）／**ASSIGN は提案でも 16/16 回 出る**。
- **検証** … 機械チェック13本 0 件・`tsc`・`eslint`（警告は HEAD と同じ）・`build`・
  Chromium と WebKit で `pageerror` **0**・帯のピル 12件で**ハーフトーンの残り 0**。

### 第124〜126巡（要点。全文は `docs/project_knowledge.md` §3-h）

- **第126巡** … **書体を Noto Sans JP へ戻し、図形の字も同じ `JP`**（LINE Seed JP は
  削除。戻すなら commit `0b04755`）／★★★**Explore の「ガクガク」の真因は2つ** ――
  ①**行き過ぎて戻る慣性が回転を逆へ回していた**（`cardMorph` は**進み `p` から測る
  向きを回す**）→ **`p` を単調に**、②**繋ぎ目で速さが飛んでいた** → **2段目を
  `EASE_EXIT`**（始端も終端も速さ 0）。★★★**「とどまる」ために区間を作らない**
  （`EASE_SETTLE` は前のめり＝**1段目の尾がそのまま「とどまる」**）／
  ★★★**帯の払いの真因は3つ** ―― ①**触った瞬間の慣性の一発**（`bandStop`。掴むたび
  11.5px 跳ねる）→ **もう呼ばない**、②**離したあとの滑りが無かった** → **`glide`**
  （**先に `flow(true)` してから**減衰する速さを足す＝止まる瞬間が無い）、
  ③**横へ払う指が「引き下ろし」に化けていた**（`pulled` は `max(0, dy)` で**1px で
  成立**）→ **`|dx| > |dy|` で掴みを捨てる** ＋ **`PULL_LIVE`(3px)**。
  ★実測 … 滑り **0 → 410〜425px**／送りは指と **1:1**／**化けたフレーム 0/16**。
- **第125巡** … **1段に入る文字数を `ROW_WIDE` 倍**（閾値 10／24。★★★**同時に
  `rowSpecOf` の幅を「掛け算」から「下限」へ** ―― `rowAspect` は文字数ぶんの幅を
  もう返しているので、掛けると**二重に広がって `cap` が全体を押し下げる**）／
  ★★★**帯の循環の真因は「中身を2周ぶんしか並べていない」** ―― 送りを `transform` の
  ずれへ足すと**上限が無い**。→ **流れの時計（`anim.currentTime`）へ畳む**
  （実測 … 6000px 送ると画面の空き **4130〜4886px → 8px で一定**）。
- **第124巡** … **提案は写真があるものだけ**（`offerPick` と `pileOffers` の**2か所**）／
  写真が無いときは**英語の1語**／ニュース **8:2**／ニュースのタップは**掛け金
  `lockRef`** で段を止める／**タスク＝ひとつの大きなピル**（`pillEdge` 1本。
  ★★★**段の高さ `unit` ＝ 板の高さ ÷ 2**。幅は `ROW_WIDE`(2) 倍で、**同時に
  `FIT_W` 0.52 → 0.86**）／★★★**カーブを1つに**（凹凸を減らして半径を上げ、
  **平均の比の逆数を `OFFER_D`(3.83) に置く** → 幅 2.55倍 → **1.14倍**。
  ★**四つ葉は `k > 1` の制約で 0.25 より下へ行けない**／★**`OFFER_D` を手で振らない**）。


### 第120〜123巡（要点。全文は `docs/project_knowledge.md` §3-h）

- **第123巡** … ★★★**六角形の歪みの真因は形の定義**（`fitBox` ではない。生の外接箱
  で**六角形だけ 0.897**）／**第121巡の「提案だけ傾けない」は撤回**／★★★**当たり判定は
  「いちばん近い中心」を採り、さらに「板が必ず勝つ」規則の根拠が事実と逆だった**
  → **塗る順と拾う順を同じ配列から引き、末尾から探す**＋**遊びは2周目でだけ**／
  ニュースの札（★**指が動かす `t` に曲線を掛けない**／★**高さは中身の高さ**）。
- **第122巡** … ★★★**写真が付かない真因は `crossOrigin="anonymous"`**（OGP 画像は
  CORS ヘッダを返さず**読み込みそのものが失敗**）／**文章は 0 件か 1 件**。
- **第121巡** … ★★★**`p.r` は「絵の半径」ではない**（`p.r × cardShapeReach` まで届く）／
  **提案だけ `OFFER_VERTS`(28)**／**夜は明日の分へ**（**そのことは帯の文章が言う**）。
- **第120巡** … **件数が増えるほど山は低い**／**字は `min(幅, 上限)` なので上限にも
  `crowd`**／**近さを3つに割る**（遊びは**閾値より小さく**）。

### 第113〜119巡（要点。全文は `docs/project_knowledge.md` §3-h）

- **第118〜119巡** … 未読の提案は**山へ**／★★★`createPortal` で `body` 直下へ・
  **`--nav-h` を読まない**（列にしか無い変数を `calc()` に入れると四方が 0）／
  **引き下ろす＝KEEP**（`lib/keepCard.ts`）／`stepGhost` の `if (g.aim)` が**絵の
  座標を奪っていた**／挿し口 `at` に**遊び `GAP_HYST`**／落とし所は**絵の中心**。
- **第115〜117巡** … ★**GRAVITY が軽いのは「最初の画面ではない」から**（ホームは
  **起動の代金**を払っていた。先読みは1つずつ／見えていない `VoiceStudio` は測らない／
  焼くのは1フレーム2枚／書体は落ちる前に頼む＝フレーム **58 → 115〜127**）／
  ★**大きさは「段の高さ」1つ**（`rowSpecOf`。`specOf` は 1 行も変えない）。
- **第113〜114巡** … ★★★**隙間と席は `transform` のずれでは書けない。レイアウトが
  持つ**（同じ id のピルが2周ぶん居るので別々の値を書けない＝2周目が **152px 飛ぶ**）／
  ★★★**並びは「留め金」（`AppState.bandPins`）**（`tasks` は触らない。**挿し口は
  index ではなく左どなりの id**）。

### 第89〜112巡の要点（全文は `docs/project_knowledge.md` §3-h）

- ★★★**落とし直しの真因は `clearOverlap` が据え置きの山にも掛かっていたこと**
  （→ `Piece.fresh` だけ）／**めり込みの真因は「湧いた瞬間から重なっている」**
  （→ 投入の直前に `clearOverlap`。372件 → 10件）／**眠りは `GravityTab` と同じ**／
  **光線は「正規化した空間」で飛ばす**／**幽霊の寿命は3重**。
- ★★★**canvas の `ctx.font` は読み込みを頼まない**し `document.fonts.ready` は**頼んで
  いないものを待たない**（→`ensureWordFont`）／**実機のバグ ＝ `mask-size`** → `clip-path`。

## 次の一手

1. ★★★**実機で見てもらう**（第127巡）… ①**塗りと文字の組が読めるか**
   （**実測 1.82〜1.93 ＝ 目盛りの外**。読めなければ字だけ `INK` へ戻すのが1手 ――
   `lib/palette.ts` の `PAIR` を空にすれば全部が比で選ばれる）／
   ②**半透明の濃さ**（`PILL_ALPHA` 0.42 の1か所で振れる）／
   ③**黄は地との比 1.01**（明度では地と差が無い。実機で面として立つか）／
   ④**提案がオレンジ1色**でジャンルが形だけで読めるか／
   ⑤**帯の払いの滑り**（`GLIDE_DECAY`）と**Explore の2段階**（`STAGE_A`）。
2. ★**残る宿題** … **起動の代金**（最初の 511ms に長いフレームが 8 件）／
   提案の大きさは `lib/cardShape.ts` の**凹凸の数**で変える（`OFFER_D` は逆算値）／
   **ニュースの札に本文は無い**（RSS は題と出典だけ）／**`magazine` と
   `ItemStatus "planned"` は死んだ欄**。

## 未解決

- ★★★**提案の生成は止まったまま**（未読 41 枚が `POOL_CAP`(40) に掛かる。
  おすすめ3件の母集団もこの池 ―― **生成が戻るまで新着は増えない**）。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も焼く予算も直していない**（山だけ）。
  ★**起動の代金は残っている**。★`SectionLabel` は 3.2 ＝ AA 割れ。

## 重要パス

★★**ファイルの地図と、踏んだ罠の全文は `CLAUDE.md`。ここは「どこを開くか」だけ。**

- ホーム … `HomeTab.tsx`／帯 `Band.tsx`＋`lib/homeBand.ts`＋`bandMotion.ts`／
  山 `components/home/`（`Pile`＋`pileWorld`＋`pilePaint`）／器 `lib/pileBox.ts`／
  引き下ろし `lib/pullDrag.ts`＋`pillGhost.ts`＋`AssignRail`＋`AssignSheet`＋`DateGrid`。
- 帯の中身 … **「今日行ける」`lib/todayPick.ts`**／**「文章」`lib/bandNotes.ts`**／
  **ニュース `lib/newsFeed.ts`＋`app/api/news/route.ts`＋`NewsCard.tsx`**／
  **おすすめ3件 `lib/offerPick.ts`**／**カード → `Item` は `lib/keepCard.ts` の1本**。
- Explore … **詳細 `CardDetail.tsx` ＋ 変形 `lib/cardMorph.ts`**／**札の形
  `lib/cardShape.ts`**（★`cardShapeReach`・`inCardShape`・`fitBox` は一様）。
- 図形 … `lib/solid.ts`（**ひとつの大きなピル**）／`lib/taskSize.ts`（`ROW_WIDE`）／
  `lib/wordPlate.ts`（板）／`lib/cassette.ts`＋`lib/reelHub.ts`。
- 語彙 … 色 `lib/constants.ts` の `PALETTE`(**4行**) ＋ 地 `BD_GREY` ＋
  **組の表 `lib/palette.ts` の `PAIR`**／**和文は `JP`**（`SANS` も `GOTHIC` も読む）／
  **ピルの半透明の塗り `lib/pillFill.ts`**／
  動き `lib/motion.ts`（★**CSS に任せられない曲線は `easeAt(…)`。写経しない**／
  ★**2段階で開くのは `twoStage`**）／バネ `lib/spring.ts`／目盛り `lib/tokens.ts`／
  ★規約13本 `design.md`。

### ★ここだけは忘れると必ず壊れる

- **絵は `hx`/`hy`**（落とし所にも支点を渡さない）／**姿は掛け金 `Ghost.pill`**／
  **位置は最後まで指**（`Ghost.aim` は `boolean`）／**近さは3つ**（`RAIL_NEAR` 36／
  `BAND_NEAR` 48／`BAND_CATCH` 24。遊び `RAIL_HYST` 12 ＝ **閾値より小さく**）。
- **帯の隙間と席はレイアウト（`.band-pad` の幅）が持つ**／**挿し口は `after`（左どなりの
  id）＋ 遊び `GAP_HYST`**／**狙われている段は流れを止める**／**位相は px で引き継ぐ**。
- **山**… **`clearOverlap` は `Piece.fresh` だけ**／**体も倍率も作り直さない**／**質量は
  同じ密度**／**`atan2` で並べてから `fromVertices`**／**dpr の上限は 2**／**ループは
  止める**（`wake()`）／**帯が覆う高さは予算から引く**＋`crowdOf`／**塗るのは変わった
  図形の箱だけ**。
- **カーブは1つ** … 2段のタスク ＝ 板 ＝ 帯のピル ＝ 提案の凹凸 ＝ **`unit`**。
  ★**`OFFER_D`・`FIT_W`・`ROW_WIDE` はどれか1つだけ動かすと崩れる**。
- **帯** … **ピルは半透明の塗り・黒の字**（`isOutlined` は常に真。濃さは
  `lib/pillFill.ts` の1か所で、**DOM・canvas・山の図形の3つが同じ数を読む**）／**指の送りは
  `anim.currentTime` へ畳む**（`transform` のずれへ足すと**2周ぶんで尽きる**）／
  **触っても跳ねさせない**（`bandStop` は呼ばない）／**離したら滑る**（`glide`）／
  **横へ払う指は引き下ろしにしない**（`PAN_SLOP` ＋ `PULL_LIVE`）。
- **色は4つだけ** … TASK＝赤（危険も）／JOURNAL＝黄／提案＝オレンジ（**ジャンルは
  形が言う**）／その他＝青。**塗りと文字は組**（青⇄赤・黄⇄オレンジ。`PAIR`）。
- **4色とも地との比は 1.01〜2.89**。**地の上の小さな字には使わない**（そこは `INK`）。
- **提案は帯へ戻せない**（`dragRef.band`）。**右端の ASSIGN は残っている**。
- **提案の図形** … **`p.r` は絵の半径ではない**（`cardShapeReach` 倍まで届く）／
  **体の光線は `OFFER_VERTS`(28)**／**写真に `crossOrigin` を付けない**。
- **当たり判定** … 形は全部「絵と同じ」＋**重なったら「画面でいちばん上」**（末尾から
  探す。**遊びは2周目でだけ**）。★**実機は 120Hz**／**iOS の上下の帯は触らない**。

### 検証の作法

★★**WebKit は `executablePath` 無し／Chromium だけ `/opt/pw-browsers/chromium`**／
★★**種に `keeps: []` を入れない**（`migrate` が `items` を丸ごと捨てる）／
★**`generatedDecks` は `Record<号, BriefCard[]>`**／★★**山の測定は 8〜10 秒待つ**／
★★**帯を出す種には「日付の無いタスク」が要る**／★★★**写真が乗った山の canvas は
汚染されて読めない** → **画面の写しを別のページで読む**か写真なしで／
★★★**回る形の外接箱は「なめらかさ」の物差しにならない**（形が回るだけで幅が揺れる）
→ **進みに比例する値**（写真の器の幅など）を測ること／
★★★**帯のピルは2周ぶん DOM に居る**ので、指で触る検証は**画面に出ているほう**を選ぶ／
★★**`bandBus.rows` の添字は論理の段**（0＝提案／1＝タスク／2＝ニュース）／
★★**WebKit は続けて何回も起動すると落ちる**（1プロセス1ケース）／
★★**`next dev` は初回のコンパイルに 45 秒**（`domcontentloaded` ＋ 先に1度叩いて温める）。
