# いまどこにいるか（2026-09-10／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。3アプリは手つかず。
★★★**Explore の刷新（券・改札鋏）は保留**。コードは消していない（`DEV` タブごと最後に処分）。

★★★**いま試している大きな変更が2つある。どちらも実機で見て決めてもらう段階。**
1. **アプリごとのアクセント配色**（`lib/appAccent.ts` の `ACCENT_TEST`）。
   **`false` の1行で元の 6色パレットへ丸ごと戻る。**
2. **ホームの山**（落ち方・当たり判定・帯を重ねた層・**日付と曜日が gravity と同じ2枚**）。

仕様の正 … `docs/home-spec.md`（ホーム）／`docs/project_knowledge.md` §3-h（確定したこと）
／`design.md`（UI の規約と機械チェック13本）。

---

## 直近完了

### ★★★第90巡 ── 山が「塔」になっていたのを直し、線だけのピルを塗った

- ★★★**落ちてくるのが変だった正体は2つ**（測って特定）:
  ①**面積の予算は「横に2つ並ぶ」前提**なのに、1枚が器の幅の **68%** まで取れたので
  **1行に1枚しか載らず、山ではなく塔になっていた**（実測 … いちばん広い行が器の幅の
  **89%**／山の高さが器の **74%**／上端 **178px** で帯の裏まで届いた）。
  `FIT_W`/`FIT_H` を **0.52/0.28** へ。→ 山の高さ **54%**・上端 **305px**。
  ★GRAVITY で塔にならないのは器が画面まるごとで**倍の高さがある**から。
  ②**出どころの高さが全部同じ**だったので、**一列に並んで順番に降りてきた**
  （コンベア）。**高さをばらす**（`DROP_SCATTER` 200）＋ 間隔 110 → **60ms**。
  空中に何枚も同時に居る「降ってくる」見え方になった。
- **線だけのピルの中を地と同じ色で塗った**（ユーザー指定）。帯は山の上に重ねて
  あるので、透明だと図形がピルの中を通って見えていた。**字も線と同じ色**へ。
- 検証（件数）… 山の高さ 74% → **54%**／いちばん広い行 89% → 塔が解消／
  タスク 3・8・12・16件で**途切れ 2〜8px＝詰まりなし**／震え **重心 0.11px・板 0px**／
  器 844→800→844 で**塗り 26778 のまま＝落ち直さない**／機械チェック13本 **0件**／
  `tsc` 0・eslint 0・`build` 成功。


### ★★★第89巡 ── 日付と曜日を **GRAVITY と同じ部品**にした（`lib/wordPlate.ts`）

ユーザーの指摘 ―― **「gravity のコードをそのまま流用できないのか。特に日付と曜日が
おかしい。何度も言っている」**。**その通りだった。**

原因は**流用していなかったこと**。板だけホームが独自に **DOM の `<div>`** で組み、
可変フォントの `wdth 62`、当たり判定は近似だった。**板だけが物理とは別の座標系に
居た**ので、板まわりだけ挙動が違い、直すたびに新しい破綻が出ていた
（宙で固まる／壁に噛む／当たり判定が字から離れる）。

- **`lib/wordPlate.ts` を作り、GRAVITY のコードをそのまま移した** …
  `inkBoxOf`（塗りの実測）／`wordFontSize`／`wordSqueeze`／`measureWordPlate`／
  `makeWordBody`（回り慣性 5倍）／`drawWordPlate`（`wordBitmap` で焼いて貼る）。
  ★**GRAVITY もホームも、これを読む。二度と食い違えない。**
- **ホームの板は2枚に**（ユーザー確定「gravity と同じ姿にして」）。
  DOM の板・`wdth 62`・1枚2行・噛み止め（`W·cosθ+H·sinθ`）は**全部消えた**。
- 検証（件数）… **GRAVITY の塗り 73579 → 73600**（0.03%。★重心は毎回ばらつく ――
  GRAVITY は表に出るたび種を変えて落とし直すので、比べるのは**塗りの量**）／
  TIMELINE の「自由」も出る（`pad` 8/4 の経路）／ホームは**震え 重心 0.4px・板 0px**／
  器 844→800→844 で**塗り 26764 のまま＝落ち直さない**／タスク 3・8・12・16件で
  **途切れ 2〜4px＝詰まりなし**／機械チェック13本 **0件**／`tsc` 0・eslint 0・`build` 成功。


### ★★★第88巡 ── 「gravity と同じに動かない」の答えは**値ではなく構造**だった

1. **器が伸び縮みするたびに山ごと落ち直していた**（`size` を effect の依存に入れて
   いた）。gravity は**器を1度だけ作り、伸び縮みでは壁だけ作り直す**。同じ形にした。
   組み直す合図も**中身の署名**にした（配列の同一性だと保存・同期のたびに落ち直す）。
2. **落下中に眠って宙で固まっていた** ―― matter.js は支えが居なくなっても眠っている
   物体を起こさない。落とし終えて 2.5 秒経つまで眠りを許さない。
3. **山が詰まりすぎていた** ―― 板と未読を予算に数えておらず、実測 **80%**。
   数えるようにし、`FILL` を 0.36 へ。
4. **帯を山の上に重ねた**（縦に並べると図形が帯の高さぶん見えないところを落ちる）。

### ★★★第87巡 ── 落下の根治・配色を3色へ・提案のピルを2行に

- **落とす間隔を「高さ」から「時間」へ**（高さだと山が大きいほど出どころが空の
  彼方へ行く。実測 -1571px／1410px/秒／2.98秒 → -57〜-99px／812px/秒／1.70秒）。
- **`Bodies.fromVertices` に凹んだ星を渡していた**（`poly-decomp` が無いので分解に
  失敗し、凹んだ形を1つの凸形として扱う）→ **`Bodies.polygon`（12角形）**。
- **配色を3色へ** … タスク＝オレンジ `#F67D52`／提案＝黄 `#EDE47B`／
  ジャーナル＝青 `#9EB2EF`。サブはメインを地へ 0.55 寄せた淡色。
- ★★★**焼き込まれた色を信じない**（`BriefCard.color` は生成した夜のパレット）。
- **提案のピルを2行に**（題の下にジャンル）／**トゲトゲの数字を焼き付けた**。

### ★★★第86巡 ── 指摘5点（ジャーナルの図形・帯を2段・トゲトゲ・日付）

- **その日まだ声を録っていなければ JOURNAL の図形も落とす**。形は**角丸の四角**
  ＝タスクと同じで、見分けは**JOURNAL の家族のメイン**（形を増やさない）。
  録ってあれば出ない。押すと JOURNAL のレコードへ。
  ★**文面は `HomeTab.tsx` の `JOURNAL_PROMPT`（いまは「今日を録る」）。1行で替わる。**
- **トゲトゲの円を EXPLORE のメインへ**。数えているのが Explore の未読なので、
  **行き先と同じ色**を着る。
- **帯を3段 → 2段**（上＝提案／下＝タスク系）。下の段は**塗り＝③④（まだ提案）**、
  **線と文字だけ＝⑤（登録済み）**で、色は TASK のメイン1色。
  ★下の段の上限を 6 → **9**（3種が同居するので、6 だと⑤が1件も出ない）。
- **日付と曜日をさらに大きく**（`WORD_W` 0.66 → **0.84**。実測 0.841）。
- ★★★**トゲトゲの当たり判定と転がり方を直した** … 絵はトゲ・物理は**まん丸**
  だったので、①当たり判定が絵とずれ ②トゲが床に噛まず玉のように滑っていた。
  **絵と物理を同じ `zigVerts` から作る**ようにした。さらに、掴めないから
  **当たり判定で見ていなかった**のを見るようにした（押すと行き先ができたため）。
- ★`colorOfKind` が知らない kind で `undefined` を返して**落ちた**（`bodyInkOn`）。
  kind は AI の生成物なので表に無い語が来る。`info` へ落とすようにした。
- 検証（件数）… 帯 **2段**／ピル 8件（塗り5・線3。線は `transparent` ＋ `1px`
  ＋ 文字も同色を確認）／板の幅 **0.841**／トゲトゲを押して **EXPLORE のブリーフへ
  遷移**／機械チェック13本 **0件**／`tsc` 0・eslint 0・`build` 成功。

### ★★★第85巡 ── 指摘6点（落ち方・当たり判定・口とゴミ箱・日付・大きさ・配色）

- **回り慣性の細工をやめた**（`setMass` は慣性も比例させるので、質量だけ与えれば
  形から正しい回りにくさが出る）／**当たり判定を作り直した**（触れないものを初めから
  見ない・円は半径で見る・`TOUCH_SLOP` で広げて最寄りを採る）／**口とブラックホールを
  削除**（`DropTargets` は TASK 側が使うので残す）／**日付と曜日を1枚・2行に**／
  **タスクを少し大きく**／**アクセント配色の試しを入れた**（`ACCENT_TEST`）。

### 第82〜84巡 ── ホームを4つ目の列として作った

- **ホームは4つ目の列**（起動して最初に出る）。**タブバーの中身だけ**が3アプリの
  名前に替わる（組み方は他と同じ `TabGlyph`）。既存3アプリの中身は触っていない。
- **帯**（3段・5種のピル）… **ゆっくり動き続けて循環**し、段ごとに向きが互い違い。
  ★★継ぎ目を消すには**2周の幅が寸分たがわず同じ**であること（間隔は各周の中に
  持ち、末尾にも1つ置いて `translateX(-lap)`）。速さは「画面1つ分を横切る時間」で持つ。
- ★**提案の帯が空だった根本原因** … `generatedDecks[today]` しか見ていなかった。
  BriefTab は**日をまたいだ未読の池**を見る。`unreadCards` を同じ規則に揃えた。
- **山**… 形3つ（四角＝タスク／円＝提案／トゲトゲ＝未読の数）。色は帯から引き継ぐ。
  ★★★**刻みは固定（1000/60）**。実時間の差分を渡すと貫通・弾け・震えが起きる。
  ★★★**`paperize` を毎フレーム掛けない**（canvas 全面を合成し直す）。図形ごとに焼く。

### ★★★第81巡までの要点（Explore の券。**保留中**）

- **デュオトーンは「写像」**（SVG のフィルタ1本）。**混色ではできない**
  ―― `lighten`/`darken` は切り捨て、`multiply`/`screen` は線形でない。**両方試した。**
- **暗くするのは掛け算**（`deepen`）。`shade()` は足し引きなので**色相が飛ぶ**。
- **大きな英語は版面の幅に合わせて組む**（`FitLine`。段から選ばない＝目盛りの外）。
  測る箱は **`inline-block`**（`block` だと器を測ってしまう）。
- **マスクした要素に `box-shadow` は出ない**。券の影は親の `filter: drop-shadow`。
- マスクの層を小さな箱に `no-repeat` で置くと、箱の外の α が 0 になり
  `intersect` が**券を丸ごと消す**。
- **要るのは5色**（9 は参照画像の色数で要件ではなかった）。**色は `PALETTE` の6行だけ。**
- ★★★**テクスチャは明暗だけを足すもので、色を変えるものではない**
  （`multiply` は ΔE 0.135・彩度 −20%／`soft-light` は ΔE 0.001）。
- ★★★**タブバーの高さは `navHeightPx()` から引く。自分で測らない**
  （`.app-nav` の矩形は Chromium 81px／実機 132px）。
- ★`app/layout.tsx` … `<style>` を `<html>` 直下に `precedence` 無しで置くと
  **不正な HTML になり水和が丸ごと失敗する**。

---

## 次の一手

1. ★★★**実機で見てもらう2つ** … ①**アクセント配色を採るか捨てるか**
   （捨てるなら `ACCENT_TEST` を `false`。採るなら色を `PALETTE` へ移して
   `lib/appAccent.ts` を消す）②**山の落ち方と日付の板**。
2. **引き下ろし**（★ホームの軸。まだ無い）… 帯のピルを掴んで引き下ろすと
   **7本の柱が地面から立ち上がり**（高さ＝その日の量）、離した日がその日付になる。
   外で離すと候補（ストック）へ。**ピルは落ちながら図形へ連続的に変わる**
   （変形は HTML、着地で canvas へ渡す）。
3. **持ち物** … 右上のアイコン＋数の印（警告）。押すと印のすぐ下に小さな一覧。
   ★図形にはしない。
4. **生成器が2つ足りない** … ①「今日行くのがおすすめ」（新しいサーバー関数）
   ②フォローアップの夜間まとめ生成（`Task.suggestions`）。
   ★★**どちらもプロンプトを先に文面で出して承認を得る。**
5. ★ピルを押したときの詳細（まだ無い）。★山の図形はもう押せる（`Piece.nav`）。

## 未解決

- ★**帯の②と④は生成器が無いので今は出ない**（③⑤は出る）。
- ★★**券の大きな英語が読めなくなる恐れ** ―― アクセントの段は白い紙の上で
  4.85/3.21/2.20/1.54 で、下2つは大きな文字の下限 3.0 を割る。**券は `DEV` タブと
  `/dev/explore` にしか無い**ので4タブには影響しないが、**券を本採用するなら
  ドメインの色をどう取るか決め直しが要る。**
- ★★**地 `#FFFBF5` と紙 `#FAFAF9` の差が比 1.01**（第76巡から持ち越し）。
- ★`SectionLabel`（`MUTED`）は明るい地で 3.2 ＝ AA 割れ。**既知・未対応。**
- ★券と鋏の未解決は全部「保留」（再開時に `docs/explore-redesign.md` から拾い直す）。

---

## 重要パス

- ホーム … `components/tabs/HomeTab.tsx`／帯 `components/home/Band.tsx` ＋
  `lib/homeBand.ts`／山 `components/home/Pile.tsx`／列の配線 `components/AppShell.tsx`
  ／仕様 `docs/home-spec.md`
- ★★★**文字の板は `lib/wordPlate.ts` の1か所**（GRAVITY の日付・曜日／TIMELINE の
  「自由」／ホームの山が同じ関数を読む）。★**DOM で組み直さないこと。**
- ★★**アクセントのテスト** … `lib/appAccent.ts`（`ACCENT_TEST` の1行で戻る）。
  効いている先は `lib/taskTags.ts`（タグ5）／`lib/palette.ts`（ドメイン4）／
  `lib/constants.ts`（`STUDIO_KEY` 3）。
- ★**色そのものは `lib/constants.ts` の `PALETTE`（6行）だけ**／役との対応は
  同ファイルの `SCHEME` と `lib/palette.ts`
  ／★**面から導く3つ** … `bodyInkOn`・`redOn`・`inkVarsOn`。**表を持たず、導かせる。**
- ★UI の規約と機械チェック13本 … `design.md`（作業の前後に必ず走らせる）
  ／目盛り `lib/tokens.ts`／動き `lib/motion.ts` ＋ `app/globals.css` の `:root`
- タスク … `components/tabs/GravityTab.tsx`（★山の物理の出どころ。ホームと**対**）
  ／カメラ `components/tasks/TaskSpace.tsx`／入口の輪 `components/CreateMenu.tsx`
- 券・鋏（★保留中）… `components/explore/samples/`／`Ticket.tsx`／`lib/nipperShape.ts`
  （生成物。手で直さない）／設計 `docs/explore-redesign.md`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite` / `fetchDirect` / `jinaSlot`）
  ／ 単体チェック `npx tsx tools/jina-check.mjs`
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちて 500）。Playwright は
  `executablePath: "/opt/pw-browsers/chromium"`、**`localhost` で開く**。
  ★`next build` は Google フォントの取得に**ときどき失敗する**（環境側。数回やり直す）。
  ★**WebGL を撮るには** `--use-gl=angle --use-angle=swiftshader
  --enable-unsafe-swiftshader`（無いと canvas が空で返る）。
