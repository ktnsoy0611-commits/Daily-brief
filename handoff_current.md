# いまどこにいるか（2026-09-14／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。手つきは**「輪ゴム(指と1:1) → ばちん →
ばね一発」**。★第108巡に**山の眠り方を `GravityTab` と同じにした**（震えの根本原因）。
★★★**Explore の刷新（券・改札鋏）は保留**（`DEV` タブごと最後に処分）。
★★★**試している大きな変更は1つ** … **アプリごとのアクセント配色**
（`lib/appAccent.ts` の `ACCENT_TEST`。**`false` の1行で元へ戻る**）。
仕様の正 … `docs/home-spec.md`（**§6 が引き下ろしの正**）／
`docs/project_knowledge.md` §3-h／`design.md`（UI の規約と機械チェック13本）。

---

## 直近完了

### ★★★第108巡 ── 震えの根本原因は「眠らせていない」ことだった

ユーザーの指摘3件。①は**5巡連続**で「**全く改善されていない**」と言われていた。
指示どおり **GRAVITY と HOME の物理コードを全部読んで突き合わせた。**

**① 図形どうしが当たった瞬間にガクガクブルブル震えて止まらない**
★★★**根本原因 … ホームの山は実質いちども眠りに入れなかった。**
matter で微振動を殺しているのは `enableSleeping` ただ1つ（眠った体は**ソルバの反復から
外れる**＝揺れ得る自由度が消える）。GRAVITY は**フレーム1から `true`**、ホームは
**`false` で始めて「大域の門」が開くまで許さない**設計だった。門は**全体の最大値**で
測るので**1つでもゆっくり転がっていれば開かない** ―― 微振動の `speed` はちょうど
0.3〜0.5 に居座るので、**永久に開かず永久に震えた。**
- → **`enableSleeping: true`。門を丸ごと削除**（`SETTLE_MS`/`CALM_V`/`CALM_W`/
  `STILL_FRAMES`/`SLEEP_EPS`/`settleAt`/`still`/`prev`。**復活させない**）。
  代わりに**体を world へ入れるフレームで全員を起こす**（門の心配はこれで消える）。
- → **`positionIterations` は既定の 6 へ差し戻し**（`POS_ITER` 削除）。matter は
  `clamp(20/iterations,0,1)` なので**6 でも 10 でも減衰は 1** ―― 第106巡に上げたのは
  **柔らかくならず、全力の押し戻しを4回余計に回すだけ**で**逆効果だった**。
- → **当たり判定の光線を「正規化した空間」で飛ばす** … px の空間だと横長の形は
  大半の光線が長い辺に当たって凸包に捨てられ、**180×60 のピルが6頂点・体が絵より
  10% 小さかった**（端が平らな面＝ゆらゆら揺れる接触）。**体÷絵 0.90〜1.02 →
  0.96〜1.02、頂点 6 → 16**（27件）。★**`GravityTab` の「順番どおりの間引き」は
  借りない** ―― **重心が最大 2.87px ずれる**。
- ★★**刻みの貯金（`MAX_STEPS`）だけは意図的に違えたまま**（GRAVITY は 120Hz で半速）。

**② 戻すときに塗り／輪郭を行き来して点滅する・動作が不安定**
- ★★★**`drawGhost` が `t <= 0` の生の比較で分けていた。** 変形のばねは**減衰比 0.5 で
  0 を挟んで行き過ぎる**ので、跨ぐたびに絵の作り方ごと入れ替わる。
  → **掛け金 `Ghost.pill`**（落ちるのは 0、戻るのは `PILL_EXIT`(0.2)）。
- ★★★**帯の口と右端が1本の線だった** → **遊び `RAIL_HYST`(22)**。

**③ 上へ戻そうとすると途中で止まってピルに戻せない**
- ★★★**絵は `hx`/`hy` に描かれるのに、`home` の枝は `dx`/`dy` しか書いていなかった**
  ―― **逆変形を始めた場所に貼り付いて1px も動かない。** → `hx`/`hy` を毎フレーム体へ。
  ★`ghostKey` にも `hx`/`hy` と `pill` を並べた（入れ忘れると塗り直されない）。

**★★★自分で踏んだ退行（直した）** … 眠りが速くなったので、**長押しの 150ms を待つ
あいだに山が眠ってループが止まり、落ち着いた山が掴めなくなった**。→ **掴んだタイマーの
中でも `wake()`。**「`dirtyRef` を立てる所は必ず `wake()`」は**`dragRef` にも要る。**

**検証（件数つき）**
- 落ち切ったあとの塗り直し（4〜18秒）**536 → 299回**／最後に塗った時刻
  **10.2 → 7.9秒**／落ち着いたあとの rAF **0回**（同じ物差し・同じ種）。
- ★**遅くしていない Chromium**（dpr3・11体）… 全員が眠るまで **4.1 → 3.8秒**、
  落ち着いたあとの**1フレームの最大 |Δ位置| 0.0000px（1518件）・塗り 0枠**。
- ★★★**正直に書く … Chromium は 60Hz なので「震えて止まらない」そのものは再現しない。**
  **直したのは仕組みの差であって、この環境の数字ではない。実機での確認が要る。**
- ③ 帯の中で 12px ずつ 6回 動かす … 幽霊の `hy` が**6回とも 365 で固定（0/5）→ 5/5**。
- ② 帯の縁を 6px 幅で 10往復 … **`tTo` の反転 19 → 0回**、姿の入れ替わり 19 → 0回。
  帯の中で 1.5秒 … 姿の入れ替わり **0回**（188件）。
- 回帰 … 横のはみ出し 0px ／床より下 0（4条件）／横→縦で図形が残る（9940→9949）／
  落ち直さない ／幽霊の寿命（塗り 0回）／**帯へ戻すと `dueDate` が消える**。
  機械チェック13本 **0件**・`tsc` 0・`eslint` 0・`build` 成功・`pageerror` 0。

### 第107巡 ── GRAVITY と同じ dpr 2／引き出しを生き返らせる／ばね一発

- ★★★**GRAVITY は canvas を dpr 2。ホームだけ 3 だった**（実機 1.09M 対 **2.77M**）
  → **`DPR_MAX` 2**。★「2 で止めると 1.5倍」（第92巡）は**当時のバグの記録**（1.5 ＝ 3÷2）。
- ★★★**ループを止める**（`runningRef`/`wake()` は `GravityTab` と同じ形）。
- ★★★**`ghostKey()` 1か所から取る。手で並べない** ―― 第106巡に `bend` を入れ忘れ、
  **引き出しが1フレーム目で固まっていた**（自分で入れた退行）。
- **`PULL_RESIST` 1.0**（指と 1:1）／★★★**変形は「ばね一発」**（`MORPH_STEPS`・
  `shownRows`・`STEP_POP`・`SNAP_POP`・`pop` を全部削除。**2本掛けると 1.2秒 揺れ続けた**）。

### 第106巡・第104巡・第102〜103巡 ── 要点（詳しくは `docs/project_knowledge.md` §3-h）

- ★★★**`pullBus.ghost` の寿命は3重**（`lostpointercapture`／unmount／**`window` の
  `pointerup`(once)**）＋ `owner`。★**隠すのは `opacity: 0`**（`visibility: hidden` は
  **捕捉が外れる原因そのもの**）。
- ★★★**`navHeightPx()` を rAF の中から呼ばない**（全文書のレイアウトを強制する）／
  **塗るのは「変わった図形の箱」だけ**（まとめない／前の箱と重なれば畳む）。
- ★★**第105巡の「32ms 超 0 件」は誤り**（測った窓がほとんど「落ち着いたあと」）。
  **落下だけを切り出して測ること。**
- ★★★**実機は 120Hz。`rAF` ごとに1歩だと物理が2倍速**で床を抜ける → **実時間を貯めて
  `STEP_MS` の整数歩**。★★**Chromium も WebKit も 60fps なのでこの環境では再現しない。**
- ★★★**文字の板だけ `setMass` を呼び忘れて密度が桁違いだった**（片方だけ直さない）／
  **沈んだだけなら `sink`**（上から落とすと「雨」）／**指の当たり判定も `halfWidthAtStack`**。
- ★★★**セーフエリアは左右対称**（`[16, w−16]`。**もう振らない**）／
  **`Body.setVelocity` は眠った体を起こさない**（長押しで動かない正体）／
  **列は `overflowX` を書かないと横へスクロールする**（ASSIGN が見えた）。

### ★★★第89〜101巡までの要点（寸法は上書き済み。教訓だけ）

- ★★★**canvas の `ctx.font` は読み込みを頼まない**し、`document.fonts.ready` は**頼んで
  いないものを待たない**（→`ensureWordFont`）。**待つなら締切**／★**中身を入れる引き金は
  「世界を作り終えた最後の1行」だけ**（途中で抜けると空）。
- ★★★**参照の形は全部「円をつないだ形」。正弦波で円に似せない**（角で √2 倍）。
  ★**`objectBoundingBox` は 0〜1 のパスを器へそのまま掛ける**（器は正方形。→`fitBox`）。
- ★★★**実機のバグの正体 ＝ `mask-size` が効かず SVG が固有の 1:1 で置かれていた**
  → **`clip-path`**（★Playwright の WebKit でも再現しなかった）／**切るのは写真だけ。
  札には掛けない**（掛けると `box-shadow` が消える）。
- ★★★**タグを全廃**（タスクの性質は「日付があるか／ないか」だけ。`isDated`）。
- ★★★**ぼやけ ＝ ResizeObserver が2つ**／**下が空く ＝ 床の二重引き**（`.bleed-x-b`）／
  **effect を2本に割る**（1本だと題を1文字直すたび山ごと落ち直す）／
  **タブバーの高さは `navHeightPx()`**（実機 132px／Chromium 81px）。

---

## 次の一手

1. ★★★**実機で見てもらう**（第108巡）…
   ★★★①**震えが本当に消えたか**（今回の主目的。**Chromium は 60Hz で症状そのものが
   再現しないので、実機でしか分からない**。残っていたら「未解決」の項へ）
   ②**落ち着いた山の図形が長押しで掴めるか**（眠りが速くなったので `wake()` を足した）
   ③**帯へ戻せるか**（途中で止まらないか・点滅しないか）
   ④**引き出しが指に付くか**（1:1。重ければ `PULL_GIVE`）
   ⑤**ばね一発が気持ちよいか**（`K_CATCH`/`D_CATCH`。★**係数は4組のまま**）
   ⑥**図形がぼやけて見えないか**（dpr 2 ＝ TASK と同じ細かさ）
   ⑦**アクセント配色を採るか捨てるか**（捨てるなら `ACCENT_TEST` を `false`）。
2. ★**`magazine` と `ItemStatus "planned"` を消す**（★**死んだ欄**。`plannedFor` が正。
   起動時の後片付けと永続化の移行に絡むので単独の巡で）。
3. **持ち物** … 右上のアイコン＋数の印（警告）。押すと印のすぐ下に小さな一覧。
4. **フォローアップの夜間生成がまだ無い**（★プロンプトは先に文面で出して承認を得る）。
   ★ピルを押したときの詳細も無い（山の図形はもう押せる ―― `Piece.nav`）。

## 未解決

- ★★★**提案の生成は止まったまま。** 15 は**表示だけ**の上限なので裏の未読 41 枚は減らず、
  夜間の生成は `POOL_CAP`(40) に掛かり続ける（`app/api/cron/build-brief/route.ts`）。
- ★★**`VoiceStudio` の `ResizeObserver` がホームでも走る**。未調査。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も直していない**（山だけ直した）。
  **TASK も 120Hz では物理が2倍速**のはず。ただし**塗る画素は少なく眠りも効いている**
  ので症状は出ていない。次に触るとき同じ直し方を。
- ★★★**震えが実機で本当に消えたかは未確認。** Chromium は 60Hz なので**症状そのものが
  再現しない**（直したのは仕組みの差）。★実機で残っていたら、次に疑うのは
  **持ち越した山が重なったまま再投入される**こと（`buildPieces` は前の位置を丸写しし、
  `unit` が変われば寸法が変わるのに `releaseAt: 0` で**全部同じフレームに入る**）。
- ★★**札の比は `BRIEF_CARD_ASPECT` が正**（`ITEM_CARD_ASPECT` ではない）。
- ★★**黄の札と地の比 1.27**（縁は**影**が持つ）／`SectionLabel` は明るい地で 3.2 ＝
  AA 割れ／**札の中の隙間**（本文が短い札）。**未対応。**
- ★★★**帯の「今日行ける」は営業時間を知らない**（`kind` ごとの表＝**近似**）。

---

## 重要パス

- ホーム … `HomeTab.tsx`／帯 `Band.tsx` ＋ `lib/homeBand.ts`／山 `components/home/`
  （`Pile.tsx` ＋ `pileWorld.ts` ＋ `pilePaint.ts`）／器 `lib/pileBox.ts`／`AppShell.tsx`
- ★★★**引き下ろし … `lib/pullDrag.ts`（算数と `pullBus`）／`pillGhost.ts`（写し取りと
  曲げ）／`AssignRail.tsx`／`AssignSheet.tsx`／盤は `components/DateGrid.tsx`
  （`WhenSheet` と共用）**。★★**手つきは「輪ゴム(44px・指と1:1) → ばちん → ばね一発」**。
  ★★★**絵は `hx`/`hy` に描かれる**（`dx`/`dy` だけ書いても動かない）／
  **姿は掛け金 `Ghost.pill`**（ばねは 0 を跨ぐので生の比較は必ず点滅する）／
  **境目には遊び `RAIL_HYST`**（1本の線だと毎フレーム反転する）。
  ★★★**幽霊の寿命は3重**（`lostpointercapture`／unmount／**`window` の `pointerup`**）。
  ★★**帯へ戻すのは `tTo` を 0 にするだけ**（逆再生。届く範囲は `RAIL_NEAR`）。
- ★★★**セーフエリアは全画面で `SPACE.lg`、左右対称**（`lib/pileBox.ts` の `PILE_INSET`。
  タブバーは右下の丸まで含めてこの幅。**もう振らない**）。
- ★★★**器が変わったときの4つ** … `FLOOR_CHECK_MS`／`refitPile`／**`sink`（上から落とし
  直さない）**／`isLost`→`respawn`。★**列は `overflowX: "clip"`**（`AppShell`）。
- ★★★**当たり判定の多角形は「軸の本数 × 頂点数」**（`PHYS_VERTS` 12・等角・偶数）。
  ★★★**光線は「正規化した空間」で飛ばす**（第108巡）―― px の空間だと横長の形の
  丸い端が潰れる（180×60 で 6頂点・体が絵より 10% 小さい）。
  ★★★**`fromVertices` へ渡す前に `atan2` で並べる** ―― 交差すると SAT が
  「当たっていない」と答えて**床をすり抜ける**。
- ★★★**山の眠りは `GravityTab` と同じ**（`enableSleeping: true`・`positionIterations` は
  既定の 6・大域の門なし）。**`dragRef` を立てる所でも `wake()`。**
- ★★★**塗るのは「変わった図形の箱」だけ**／**`navHeightPx()` をループから呼ばない**／
  **`stepGhost` と物理は同じ固定の刻み**（`STEP_MS`/`MAX_STEPS`(2)。**`rAF` ごとに
  1歩にしない**）／**`setMass` は全部の体に**／**指の当たり判定も `halfWidthAtStack`**。
- ★★★**タスクの形は `lib/solid.ts` の `stackOutline`（ピルの積み）が正**。**段の数は
  `rowsOf(title)`**（純粋な関数。絵も物理も山も `shapeRowsOf` から引く）。
  ★**`waist` が「ピル ⇄ 図形」の変形**。
- ★★★**文字の割り付けは `lib/textFit.ts` の `layoutInRows` だけ**（**2つ持たない**）／
  **塗り／輪郭は `paintShape` と `isDated` が正**（`pilePaint.ts` にも同じ分岐。**両方**）。
- ★**カセットの寸法は `lib/cassette.ts`**／**芯は `lib/reelHub.ts`**。
- ★★★**書体は3つ** … `SANS`／`LATIN`／**`DISPLAY`**（Anton）。★★**canvas は
  `font-variation-settings` を受けない**（**偽コンデンスは撤回済み**）。
- ★★★**札の形は `lib/cardShape.ts`（点の列が正）＋ `CardShapeDefs.tsx`。割り当ては
  `PUNCH_BY_DOMAIN` から導く**。★**`clip-path`。器は必ず正方形。**
- ★★★**山の器は `.bleed-x-b`**／**ResizeObserver は1つだけ**／**文字の板は
  `lib/wordPlate.ts`**（★DOM で組み直さない）／**帯の「今日行ける」は `lib/todayPick.ts`**。
- ★**色そのものは `lib/constants.ts` の `PALETTE`（6行）だけ**／役との対応は
  `SCHEME` と `lib/palette.ts`／★**面から導く3つ** … `bodyInkOn`・`redOn`・`inkVarsOn`。
- ★UI の規約と機械チェック13本 … `design.md`／目盛り `lib/tokens.ts`／動き `lib/motion.ts`
- タスク … `GravityTab.tsx`（★ホームと**対**）／`TaskSpace.tsx`／`CreateMenu.tsx`
- ブリーフ … `lib/briefPipeline.ts`／単体チェック `npx tsx tools/jina-check.mjs`
- ★★★**WebKit がこの環境で動く** … `executablePath` 無しに起動（**Chromium だけ**
  `/opt/pw-browsers/chromium`）。★★**万能ではない**（第95巡の札のバグは再現しなかった）。
  ★★**CPU を遅くできるのは Chromium だけ**（`Emulation.setCPUThrottlingRate`）。
- ★★★**試験の種は `keeps: []` を入れてはいけない**（`migrate` が旧形式とみなして
  **`items` を丸ごと捨てる**）。`wishes: []` は**必要**（無いと全部捨てる）。
- 検証 … `localhost` で開く。★★**山の測定は 8 秒待つ**／★**山もタスクも dpr 上限 2**／
  `next build` は Google フォントの取得に**ときどき失敗する**。
  ★★**帯を出す種には「日付の無いタスク」が要る**（全部に `dueDate` を付けると帯が
  空になり、`bandBottom()` が 0 を返して**戻しの判定にいちども入らない**）。
