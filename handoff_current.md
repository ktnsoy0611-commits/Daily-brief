# いまどこにいるか（2026-09-18／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。★第110〜112巡に「落とし直し」を根絶、
★★★**第113〜114巡に帯の隙間をレイアウトへ作り直して「どこからでも・どこへでも
入る」を通し**（並びは**留め金**）、★★★**第115〜116巡に起動の代金を払い、形を
「ピルの積み」・大きさを「段の高さ」1つ**にし、★★★**第117〜118巡に配色を総入れ替え
して手ざわりの4つの真因を根治**、★★★**第119巡に「おすすめ3件」と Explore の詳細の
画面**を入れ、★★★**第120巡に山のスケールを緩めて、Explore の遷移を作り直した**。
★Explore の刷新（券・改札鋏）は保留（`DEV` タブごと最後に処分）。
仕様の正 … `docs/home-spec.md`（**§6 が引き下ろしの正**）／
`docs/project_knowledge.md` §3-h／`design.md`。
---

## 直近完了

### ★★★第120巡 ── 山のスケール／日付の板／近さの判定／Explore の遷移

> 「**山全体のスケールが効き過ぎ。小さすぎるので緩めて。日付だけ変わらないのも
> 修正。日付もつかめるように。帯に戻すのと、右側に寄せて予定を変えるのの判定を
> 小さくして誤動作しないように**」「**マスクの枠だけが回りながらアニメーションし、
> 画像の表示領域が大きくなる。最終的に画像の周りに色のついたベゼルが残り、画面全体は
> そのジャンルのベタ塗り、角丸の四角でマスクされた写真が画面上半分より大きいくらい、
> 下に詳細文。画像を大きくしすぎると粗が目立つのでその対策でもある**」

- **①スケール** … ★★★**第118巡の見立てが逆だった** ―― 実測すると**件数が増えるほど
  山は低い**（小さい図形は密に積む）。⑥を直したのは `bandH` であって `crowd` では
  なかった。→ **`CROWD_N0` 10→18・`CROWD_MIN` 0.85→0.94**。実測（4/9/14/22 件）
  帯からの余裕 **112/164/220/254px**・塗った面積はほぼ一定。
- **②日付だけ変わらない** … 字は `min(幅に収まる値, 上限)` で、390 幅では**上限が
  効いている**ので `room` だけ縮めても 1px も変わらなかった → **上限にも `crowd`**
  （`min(a·c, M·c) = c·min(a, M)` ＝ どちらが効いても厳密に `crowd` 倍）。
- **③日付も掴める** … `pickAt` が板も見る。★★**`TOUCH_SLOP` を足さない**（足すと板が
  まわりを飲み込む。実測 隣が掴まれ板は **295px** 置き去り）。★★**板には行き先が
  無い**（`dragRef.place`。ASSIGN も帯の隙間も出ない）。
- **④判定を小さく** … 1つの `RAIL_NEAR`(72) を**3つに割った** … 右端 **36**／幽霊が
  生まれる `BAND_NEAR` **48**／狙いと見なす `BAND_CATCH` **24**（前は `PULL_ARM` 44 を
  借りていた）。遊び `RAIL_HYST` **22→12**（★**遊びは閾値より小さく**）。
- **⑤Explore の遷移**（第119巡の全画面は**撤回**。理由は**素材** ―― 写真は 1000px 級
  なので実機の全画面では粗が出る）… **新設 `lib/cardMorph.ts`**。**両端を「中心から
  等角に測った半径の列」へ直して lerp**（`waist` と同じ作法。点の数が変わらない）。
  ★**回るのは測る向きだけ**／★★**半回転で着地**／★★★**光線が頂点をちょうど通る**
  ので `u` に **1e-9 の遊び**が要る（無いとその向きだけ半径 0 ＝ **切れ込みが1本走る**）／
  ★**遠い頂点より外は採らない**／★★★**着いたら `border-radius` の `<img>` と入れ
  替える**／★★★**器に `opacity` を掛けない**／★★★**閉じる1フレーム目は
  `useLayoutEffect` で今すぐ書く**／★曲線は **`easeAt(EASE_SETTLE, t)`**（新設）。
- **検証** … 遷移は**最初のフレームが押した写真そのもの (40,180,310,310)**・最後が
  **(16,16,358,490)・角丸 28px** に着地・本文 1536 > 844 でスクロール可・閉じると
  **(39,175,313,314)** へ戻って消える（Chromium／WebKit とも `pageerror` 0）。機械
  チェック13本 0 件／`tsc`・`eslint`・`next build` すべて通る。

### ★★★第119巡 ── おすすめ3件を山へ／Explore の詳細の画面

- **場所の入れ替え** … 帯の上の段は**ストックの「今行ける」だけ**（`todayPick`）。
  **未読の提案は山へ**（おすすめ3件）。★押すと Explore のそのカードへ／★**帯へ運ぶと
  KEEP してストックへ**。
- **新設 `lib/offerPick.ts`** … ★**AI を使わない**（材料はクライアントに全部ある／
  夜間生成が止まっていても効く／嘘をつかない）。**願い 3.0 ＞ ゴール 2.0 ＞ 好みの語
  0〜2.0 ＞ ドメインの KEEP 率の差 ±1.0 ＞ 新着 0.6**、**同じドメインは2枚まで**。
  ★**時間帯は見ない**（ユーザー確定）。
- ★★★**CSS は `scale` を線形に補間するので `scale(1/s)` は途中で逆行列にならない**
  （実測 真ん中で積 2.04）。**回転と平行移動だけが厳密に打ち消し合う。**★第120巡に
  **打ち消しの層そのものをやめた**（毎フレーム自分でパスを書く）。
- ★★★**`createPortal` で `body` 直下へ**（列の `transform` が独立した重なりの文脈を
  作るので **`zIndex` 70 でも nav の下に潜る**）／★★★**`--nav-h` を読まない**。

### ★★★第118巡 ── 手ざわりの4つの真因を根治／色／山のスケール

- **①** `offer-` に対応する `Item` が無く `items.find` が原理的に当たらず黙って
  return していた → **引き下ろす＝KEEP**（式は **`lib/keepCard.ts` の1か所**）。
- **②** `stepGhost` の `if (g.aim)` が**絵の座標を奪っていた** → **枝ごと削除。
  `Ghost.aim` は `boolean`**で、役は `tTo` を **`PILL_HINT`(0.55)** へ寄せる合図だけ。
  ★★**第109巡にユーザー確定で入れたものを本人が撤回。復活させない。**
- **③** 挿し口 `at` に**遊びが1つも無く**、変わるたび `m.open.p = 0` に戻していた
  → **`GAP_HYST`(22) ＋ `bandSettling`（閉じ切るまで乗り換えない）**。実測 … 飛び
  **11〜12 → 5 回**／席の合計の幅 **38〜150 のばたつき → 150 で一定**。
- **④** 落とし所に**支点（`dx`/`dy`）**を渡していた（正は**絵の中心 `hx`/`hy`**）。
  **第108巡と同じ罠の裏返し** → 実測 ずれ **44.93px → 0.00px**。
- **⑤** JOURNAL ＝ **ラベンダー**／タスク ＝ **`TASK_GREY`**（地 1.86／黒字 7.76。
  無彩色の6段目）。★ピンクは `redOn()` の**暗い面の赤**として残す。
- **⑥** **帯が覆う高さを予算から引いていなかった** → `buildPieces(…, bandH)`。

### ★★★第117巡 ── 配色の総入れ替え（承認済み）

- 参照画像（Jardins de Métis の入場券）から**画素を測って 7色**。**タスク＝無彩色の
  塗り ＋ 黒の字**。**帯・山・GRAVITY・DRIFT・入力画面すべて追従**。
- **ドメイン4は色と形の組** … バショ＝緑・四つ葉／タイケン＝赤・六角形／ジョウホウ＝
  青・波打つ四角／モノ＝黄・トゲトゲ。**形は1行も変えていない。提案の札の面も同じ色。**
  ★★★**`lib/appAccent.ts` を削除。復活させない。**★`redOn()` は残す（暗い面では
  赤 3.19 よりピンク 4.09 が読める）。

### 第115〜116巡 ── 最初の落下の重さ／形と大きさ

- ★★★**GRAVITY が軽いのは「最初の画面ではない」から**（ホームは**起動の代金**を
  払っていた）… 先読みは1つずつ／見えていない `VoiceStudio` は測らない（524ms）／
  焼くのは1フレーム2枚／書体は落ちる前に頼む。実測 **フレーム 58 → 115〜127**。
- ★★★**形は「ピルの積み」**（第115巡の S 字は第116巡に撤回。`foldsAt`/`sideFolds` 削除）。
- ★★★**大きさは「段の高さ」1つ**（`rowSpecOf`。`h = 段の数`／`w = rowAspect`）。
  第115巡の「面積を1つ」では**段が増えるほど段が痩せた**（幅 2.25倍 → 1.00倍・9件）。
  ★★★**`specOf` は 1 行も変えない**（TASK は重要度で大きさが変わるまま）。

### 第113〜114巡 ── 帯の隙間をレイアウトへ／どこからでも入る

- ★★★**隙間と席は `transform` のずれでは書けない。レイアウトが持つ** ―― 同じ id の
  ピルが2周ぶん居るので別々の値を書けない（実測 2周目が **152px 飛ぶ**）。
  **両側 ±w/2 も片側 +w も同じ理由で直らない。**
- ★★★**帯ぜんたいを `lead`＋隙間の半分だけ左へ戻し、挿し込みで「送り（px）」へ
  畳み替える**（`currentTime` を直接ずらすと 156px 飛ぶ）／**境目は開く前の位置で
  測る**／**挿し口が移るときは受け皿 `shut` へ**／**開けているあいだ組み直さない**／
  ★★★**`K_OPEN`（5組目）** … 90% まで **117ms**・行き過ぎ **0.0%**／★★★**白抜き ⇄
  塗りは `inkMix` が返す「量」で渡る**（色の差 157 → 21）／★★★**帯へ入れる道は2本
  あるのに狙いの出し方は1本**／★★★**弾けは掛け金**（`latched`）。
- ★★★**並びは「留め金」（`AppState.bandPins`）**。`tasks` は触らない ―― ① GRAVITY と
  ALIGN まで動く ② 声の候補とフォローアップは持ち主が別で動かせない。★**挿し口は
  index ではなく「左どなりの id」**。★**日付・曜日は黒いピルに白い文字**。

### 過去の巡の要点（詳しくは `docs/project_knowledge.md` §3-h）

- ★★★**第111〜112巡** … 落とし直しの真因は **`clearOverlap` が据え置きの山にも
  掛かっていた**こと（傾いた図形の AABB は必ず重なる）→ **`Piece.fresh` だけ**＋**体を
  作り直さない**／**下の段の9件の打ち切りが戻したタスクを消していた**／**字も1文字ずつ
  `bendAt`**／**運ぶ図形は角度を留める**。
- ★★★**第109巡 … めり込みの真因は「湧いた瞬間から重なっている」**（60ms で落ちる距離
  2.5px に対し散らばり 200px）→ 投入の直前に `clearOverlap`（372件 → 10件）／
  ★★★**眠りは `GravityTab` と同じ**（大域の門は全削除）／`positionIterations` は 6／
  ★★★**光線は「正規化した空間」で飛ばす**／**`atan2` で並べてから `fromVertices`**／
  **dpr の上限は 2**／**ループは止める**（`wake()` を忘れない）／**幽霊の寿命は3重**／
  ★★★**実機は 120Hz。この環境では再現しない**。
- **第89〜101巡** … ★★★**canvas の `ctx.font` は読み込みを頼まない**し
  `document.fonts.ready` は**頼んでいないものを待たない**（→`ensureWordFont`。**締切を
  必ず**）／**参照の形は全部「円をつないだ形」。正弦波で円に似せない**／**器は必ず
  正方形**／**実機のバグ ＝ `mask-size` が効かず SVG が 1:1**→`clip-path`。

## 次の一手

1. ★★★**実機で見てもらう**（第118〜120巡）… ①**山の大きさ**（緩めたが十分か）／
   ②**日付と曜日の板も一緒に縮むか・掴めるか**／③**右端の ASSIGN と帯の判定が
   誤動作しないか**／④**帯の隙間の開き方と、離したときに飛ばないか**／
   ⑤**ラベンダーと薄いグレー**／⑥**山のおすすめ3件が「好みそう」か**／
   ⑦**押すと Explore のそのカードへ飛ぶか**／⑧**写真を押したときの遷移**
   （枠だけが回って角丸四角へ・色のベゼル・下に詳細文）。
2. ★**`magazine` と `ItemStatus "planned"` を消す**（死んだ欄。`plannedFor` が正）。

## 未解決

- ★★★**提案の生成は止まったまま**（未読 41 枚が減らず `POOL_CAP`(40) に掛かる）。
  ★おすすめ3件の母集団もこの池 ―― **生成が戻るまで新着は増えない**。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も焼く予算も直していない**（山だけ）。
  ★TASK の板は「文字そのものが図形」のまま／重要度で大きさが変わるまま。
  ★**起動の代金は残っている**（1フレーム 667ms）。★`SectionLabel` は 3.2 ＝ AA 割れ。
---

## 重要パス

- ホーム … `HomeTab.tsx`／帯 `Band.tsx`＋`lib/homeBand.ts`＋`bandMotion.ts`／山
  `components/home/`（`Pile`＋`pileWorld`＋`pilePaint`）／器 `lib/pileBox.ts`／★★★**引き
  下ろし … `lib/pullDrag.ts`／`pillGhost.ts`／`AssignRail`／`AssignSheet`／`DateGrid`**。
  ★**手つきは「輪ゴム → ばちん → 変形」**／★★★**絵は `hx`/`hy`（落とし所にも支点を
  渡さない）**／**姿は掛け金 `Ghost.pill`**／★★★**近さは3つ**（右端 `RAIL_NEAR` 36／
  幽霊 `BAND_NEAR` 48／狙い `BAND_CATCH` 24。遊び `RAIL_HYST` 12 ＝ **閾値より小さく**）／
  **変形 `K_SWING`・吸い付き `K_CATCH`**／★★★**位置は最後まで指**（`Ghost.aim` は
  `boolean`）／★★★**弾けは掛け金**（`latched`）／★★★**帯へ入れる道は2本で狙いの
  出し方は1本**／★★★**白抜き ⇄ 塗りは `inkMix` の1か所**（掛け金で切り替えない）。
- ★★★**帯の物理っぽい動きは `components/home/bandMotion.ts` の1か所**。**隙間と席は
  レイアウト（`.band-pad` の幅）が持つ**／**帯ぜんたいを `lead`＋隙間の半分だけ左へ
  戻し、挿し込みで「送り（px）」へ畳み替える**／**バネは `K_OPEN` と `K_SWING`**／
  **境目は開く前の位置で測る**／**開けているあいだ組み直さない**／**挿し口は `after`
  （左どなりの id）＋ 遊び `GAP_HYST`・`bandSettling`**。
- ★★★**速さの3つ**（第115巡）… **先読みは1つずつ**／**見えていない `VoiceStudio` は
  寸法を測らない**／**1フレーム 2枚まで焼く ＋ 書体は落ちる前に頼む**／★★★**代理の体**
  （`syncProxy`）… **絵は体そのもの**／**体は絵の中心**／**`piecesRef` に混ぜない**／
  **`clearOverlap` の前に消す**／**締切 `HANDOFF_MS`**／★★**山の倍率は決め直さない**／
  ★★**帯が覆う高さは予算から引く**＋**`crowdOf(n)`**（板は `crowd` と**上限の両方に**）。
- ★★★**セーフエリアは全画面で `SPACE.lg`、左右対称**／**器が変わったときの4つ**
  （`FLOOR_CHECK_MS`／`refitPile`／`sink`／`isLost`→`respawn`）／**器は `.bleed-x-b`**／
  ★★★**当たり判定の多角形は「軸の本数 × 頂点数」**（`PHYS_VERTS` 12・等角・偶数）／
  **投入の直前に `clearOverlap`**／**質量は全部同じ密度に**／**`atan2` で並べてから
  `fromVertices`**／**眠りは `GravityTab` と同じ**／**形は `stackOutline`・段の数は
  `rowsOf`・大きさは `rowSpecOf`**／**塗り／輪郭は `paintShape` と `isDated`**
  （`pilePaint.ts` にも同じ分岐。**両方直す**）／★★★**板も掴める。ただし遊びは 0・
  行き先は無い**（`dragRef.place`）。
- ★**カセット `lib/cassette.ts`**／**芯 `lib/reelHub.ts`**／**札の形 `lib/cardShape.ts`**／
  **文字の板 `lib/wordPlate.ts`**／**帯の「今日行ける」 `lib/todayPick.ts`**／
  ★★★**書体は3つ**／★**色は `PALETTE`（7行）だけ**（面から導く `bodyInkOn`・`redOn`・
  `inkVarsOn`・`mixHex`）／★規約13本 `design.md`。
- ★★★**おすすめ3件 `lib/offerPick.ts`**／**詳細 `components/explore/CardDetail.tsx` ＋
  変形 `lib/cardMorph.ts`**（★**両端を等角の半径の列にして lerp**／★**回るのは測る向き
  だけ・半回転で着地**／★**`u` に 1e-9 の遊び**／★**着いたら `border-radius` へ入れ
  替える**／★**器に `opacity` を掛けない**／★**`createPortal` で `body` 直下・`--nav-h`
  を読まない**）／**カード → `Item` は `lib/keepCard.ts` の1本**／★★**CSS に任せられ
  ない曲線は `easeAt(EASE_SETTLE, t)`。写経しない。**
- 検証 … ★★★**WebKit は `executablePath` 無し／Chromium だけ `/opt/pw-browsers/chromium`。
  CPU を遅くできるのは Chromium だけ**＝速さの測定はそちら／★★★**種に `keeps: []` を
  入れない**（`migrate` が `items` を丸ごと捨てる。`wishes: []` は必要）／
  ★**`generatedDecks` は `Record<号, BriefCard[]>`**（`{cards:[]}` ではない）／
  ★★**山の測定は 8 秒待つ**／★★**帯を出す種には「日付の無いタスク」が要る**／
  ★★★**帯の飛びは「見えているピル」だけで測る**（2周目の控えは画面の外＝偽陽性）／
  ★★**`bandBus.rows` の添字は論理の段**（0＝提案／1＝タスク。DOM の順ではない）／
  ★★**WebKit は続けて何回も起動すると落ちる**（1プロセス1ケース）／★★★**速さは
  `Tracing` で割り、`rAF` と `setTimeout` を包んで呼び手ごとに測る**。
