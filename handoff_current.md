# いまどこにいるか（2026-09-18／ホームを作っている最中）

**ホームが動いている**（帯3段＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。★第110〜112巡に「落とし直し」を根絶、
第113〜114巡に**帯の隙間をレイアウトへ作り直し**（並びは**留め金**）、第115〜116巡に
**起動の代金を払い、形を「ピルの積み」・大きさを「段の高さ」1つ**にし、第117〜118巡に
**配色を総入れ替えして手ざわりの4つの真因を根治**、第119〜120巡に**「おすすめ3件」と
Explore の詳細の画面**、第121巡に**提案の図形（傾き・当たり判定・体）と帯の文章**、
★★★**第122巡に帯を3段（ニュース）にして、文章を1件に絞り、写真・板・提案の
大きさと当たり判定を直した**。
★Explore の刷新（券・改札鋏）は保留（`DEV` タブごと最後に処分）。
仕様の正 … `docs/home-spec.md`（**§6 が引き下ろしの正**）／
`docs/project_knowledge.md` §3-h／`design.md`。

## 直近完了

### ★★★第122巡 ── 帯3段（ニュース）／文章1件／写真・板・提案

> 「**帯の文章はもっと短く自然に。仕組みが分からないのでちゃんとしてほしい。頻度は
> ピル5個に1個ぐらい**」「**写真がない提案のピルの見た目が崩れている**」「**山の日付と
> 曜日の当たり判定が大きすぎる。板自体も小さく**」「**山に落ちてくる提案に写真が
> ついてこない**」「**提案の図形はある程度大きく綺麗に**」「**引き抜いた穴が閉じるのが
> 速すぎてガクッとする**」「**吸い付いてくるのが強すぎる**」「**ニュースの帯を追加**」

- **①写真が付かない** … ★★★**真因は `crossOrigin="anonymous"`** ―― 提案の写真は
  他所の **OGP 画像**で、多くは `Access-Control-Allow-Origin` を返さないので
  **読み込みそのものが失敗**していた。付ける理由は「canvas を読み返すとき」だけで
  **このアプリには 0 件** → **外した**（＋落ちた写真に印／`glyph` は常に持つ）。
  ★実測 … 山のピンクの画素 **0 → 154902**（Chromium／WebKit とも）。
- **②崩れたピル** … `onError` が**丸だけ**消していたので `photo` は真のまま ＝
  左の余白が丸用の 6px のままで**題が丸い角に食い込んでいた** → **URL を憶えて
  `photo` ごと落とす**。★実測 … 題の左端がピルの縁から **6 → 24px**。
- **③文章は 0 件か 1 件** … **候補を上から順に見て最初の1つだけ**（明日の案内 ＞
  おすすめの理由 ＞ タスクの残り）。上の段は最大6件なので**ピル6個に文章1つ**。
  ★**文は主語＋助詞で言い切る**（「明日なら行けます」「建築が好きなら」「タスクは
  あと3つ」）。★★**題は文に入れない**（ピルにも図形にももう出ている）。
- **④板** … 大きさ **0.80倍**（`WORD_W` 0.84→0.67 ＋ `PILE_WORD_MAX` 72→58。
  ★**両方を同じ比で** ―― 字は `min(幅, 上限)` なので片方だけでは効かない）。実測 …
  塗り面積 **8876/6572 → 5618/4112**（0.63倍＝長さで 0.79倍）。当たり判定は
  **外接箱 → 丸い角の矩形**（板は必ず勝つので四隅の外で下の図形が掴めなかった）。
- **⑤提案を大きく** … `OFFER_D` **3.2 → 4.4**。★★★**代償はタスク** ―― `unit` は
  「予算 ÷ 全員の面積」なので提案の取り分が増えたぶん小さくなる。実測 … **外接箱
  112 → 131px（+17%）／タスクの塗り面積 0.71倍／合計 −2.7%／帯からの余裕 46 → 131px**。
- **⑥⑦ばねは役の割り当てだけ変えた**（係数は5組のまま）… **席の畳み `K_OPEN` →
  `K_SETTLE`**（90% まで **117 → 467ms**。★**素の幅へ戻すときは `K_OPEN` のまま**
  ＝向きで分ける）／**指へ追いつく `K_CATCH` → `K_OPEN`**（**67 → 117ms**・
  行き過ぎ **10.5% → 0%**）。
- **⑧ニュースの段**（新設 `app/api/news/route.ts` ＋ `lib/newsFeed.ts`）…
  **Google ニュースの RSS**。★**サーバーで取る**（CORS ヘッダが無い）／30分キャッシュ／
  **好みの語で検索、0 件なら主要ニュースへ**／★★★**`AppState` に入れない**／★★**押せ
  ない**（`row={2}` に `pull` を渡さない）／**厚み 28px**／向きは `bandReverse`。
- **検証** … 帯 **3段（60/44/28px）・上の段の文章 1件**・ニュース 6件／写真 ○／崩れ
  0 件／機械チェック13本 0 件・`tsc`・`eslint`・`build`・両ブラウザ `pageerror` **0**。

### ★★★第121巡 ── 提案の図形／夜のおすすめ／帯の文章

- **①歪み** … ★★★**測ったら形は正しかった**（半径のずれ 平均 1.1%）。違っていたのは
  **向き**で **150° 回っていた** → **提案だけ正立で固定**（`setInertia(Infinity)` は
  `setMass` の**あと**。合図は体の `inverseInertia === 0`）。★タスクは傾いたまま。
- **②③当たり判定と塗り直す箱** … ★★★**`p.r` は「絵の半径」ではない**（絵は 2r 四方
  いっぱい。遠い点は `p.r × cardShapeReach` ＝ 波打つ四角 **1.292**）。円で見ていた
  ので**出っ張りを押しても掴めず、へこみで掴めた**（誤り 368・236/1440 → **0/1440**）。
- **④めり込み** … 札の形は縁に山と谷があり `PHYS_VERTS`(12) では谷ばかり拾う
  （体÷絵 0.900）→ **提案だけ `OFFER_VERTS`(28)**（1.011）。★タスクは 12 のまま。
- **⑤夜に帯が空** … 仕様どおり（嘘を出さない）→ **今日が1件も無いときだけ明日へ**。
  ★★**今日の分があるうちは混ぜない**／**そのことは帯の文章が言う**。
- **⑥帯の文章**（新設 `lib/bandNotes.ts`）… **面も縁も持たない地の上の字**／**指の
  イベントは素通し**／**上限の数に入れない**／**`homeBand` から呼ばない**（輪になる。
  `HomeTab` が渡す）。★★**中身と頻度は第122巡に作り直した。**

### ★★★第120巡 ── 山のスケール／日付の板／近さの判定／Explore の遷移

- **①スケール** … ★★★**第118巡の見立てが逆だった** ―― 実測すると**件数が増えるほど
  山は低い** → **`CROWD_N0` 10→18・`CROWD_MIN` 0.85→0.94**。
- **②日付だけ変わらない** … 字は `min(幅に収まる値, 上限)` で**上限が効いていた**
  → **上限にも `crowd`**（どちらが効いても厳密に `crowd` 倍）。
- **③日付も掴める** … `pickAt` が板も見る。★★**`TOUCH_SLOP` を足さない**／★★**板に
  は行き先が無い**（`dragRef.place`）。
- **④判定を小さく** … `RAIL_NEAR`(72) を**3つに割った** … 右端 **36**／幽霊
  `BAND_NEAR` **48**／狙い `BAND_CATCH` **24**／遊び `RAIL_HYST` **22→12**
  （★**遊びは閾値より小さく**）。
- **⑤Explore の遷移**（第119巡の全画面は**撤回**。写真は 1000px 級なので実機の全画面
  では粗が出る）… **新設 `lib/cardMorph.ts`**。**両端を「中心から等角に測った半径の
  列」へ直して lerp**。★★★**光線が頂点をちょうど通る**ので `u` に **1e-9 の遊び**が
  要る（無いと**切れ込みが1本走る**）／★★★**着いたら `border-radius` の `<img>` と
  入れ替える**／★★★**器に `opacity` を掛けない**／★★★**閉じる1フレーム目は
  `useLayoutEffect`**／★曲線は **`easeAt(EASE_SETTLE, t)`**。

### 第117〜119巡（要点。詳しくは `docs/project_knowledge.md` §3-h）

- **第119巡** … 帯の上の段は**ストックの「今行ける」だけ**、**未読の提案は山へ**
  （新設 `lib/offerPick.ts`。★**AI を使わない**／願い 3.0 ＞ ゴール 2.0 ＞ 好みの語
  ＞ KEEP 率の差 ＞ 新着／同じドメインは2枚まで）。★★★**`createPortal` で `body`
  直下へ**（列の `transform` が重なりの文脈を作る）／★★★**`--nav-h` を読まない**。
- **第118巡** … ① `offer-` に `Item` が無く `items.find` が原理的に当たらなかった →
  **引き下ろす＝KEEP**（`lib/keepCard.ts` の1か所）／② `stepGhost` の `if (g.aim)` が
  **絵の座標を奪っていた** → **枝ごと削除。`Ghost.aim` は `boolean`**（**第109巡に
  ユーザー確定で入れたものを本人が撤回。復活させない**）／③ 挿し口 `at` に遊びが
  無かった → **`GAP_HYST`(22) ＋ `bandSettling`**／④ 落とし所に**支点**を渡していた
  （正は**絵の中心 `hx`/`hy`**。**第108巡と同じ罠の裏返し**。44.93px → 0）。
- **第117巡** … 配色の総入れ替え。**タスク＝無彩色の塗り ＋ 黒の字**／**ドメイン4は
  色と形の組**／★★★**`lib/appAccent.ts` を削除**。

### 第113〜116巡 ── 起動の代金／形と大きさ／帯の隙間

- ★★★**GRAVITY が軽いのは「最初の画面ではない」から**（ホームは**起動の代金**を
  払っていた）… 先読みは1つずつ／見えていない `VoiceStudio` は測らない／焼くのは
  1フレーム2枚／書体は落ちる前に頼む。実測 **フレーム 58 → 115〜127**。
- ★★★**形は「ピルの積み」**（S 字は撤回）／★★★**大きさは「段の高さ」1つ**
  （`rowSpecOf`）／★★★**`specOf` は 1 行も変えない**（TASK は重要度のまま）。
- ★★★**隙間と席は `transform` のずれでは書けない。レイアウトが持つ** ―― 同じ id の
  ピルが2周ぶん居るので別々の値を書けない（2周目が **152px 飛ぶ**）。
  **両側 ±w/2 も片側 +w も同じ理由で直らない。**
- ★★★**帯ぜんたいを `lead`＋隙間の半分だけ左へ戻し、挿し込みで「送り（px）」へ畳み
  替える**／**境目は開く前の位置で測る**／**挿し口が移るときは受け皿 `shut` へ**／
  **開けているあいだ組み直さない**／★★★**白抜き ⇄ 塗りは `inkMix` の「量」で渡る**。
- ★★★**並びは「留め金」（`AppState.bandPins`）**。`tasks` は触らない ―― ① GRAVITY と
  ALIGN まで動く ② 声の候補とフォローアップは持ち主が別で動かせない。★**挿し口は
  index ではなく「左どなりの id」**。★**日付・曜日は黒いピルに白い文字**。

### 過去の巡の要点（詳しくは `docs/project_knowledge.md` §3-h）

- ★★★**第111〜112巡** … 落とし直しの真因は **`clearOverlap` が据え置きの山にも
  掛かっていた**こと → **`Piece.fresh` だけ**＋**体を作り直さない**／**下の段の9件の
  打ち切りが戻したタスクを消していた**／**運ぶ図形は角度を留める**。
- ★★★**第109巡 … めり込みの真因は「湧いた瞬間から重なっている」**（60ms で落ちる
  距離 2.5px に対し散らばり 200px）→ 投入の直前に `clearOverlap`（372件 → 10件）／
  ★★★**眠りは `GravityTab` と同じ**（大域の門は全削除）／`positionIterations` は 6／
  ★★★**光線は「正規化した空間」で飛ばす**／**幽霊の寿命は3重**。
- **第89〜101巡** … ★★★**canvas の `ctx.font` は読み込みを頼まない**し
  `document.fonts.ready` は**頼んでいないものを待たない**（→`ensureWordFont`）／
  **形は「円をつないだ形」**／**器は必ず正方形**／**実機のバグ ＝ `mask-size` が
  効かず SVG が 1:1** → `clip-path`。

## 次の一手

1. ★★★**実機で見てもらう**（第122巡）… ①**帯の文章の文面と頻度**（1周に1回）／
   ②**ニュースの段**（厚み・字の大きさ・出典の見え方・中身の選び方）／
   ③**提案の図形の大きさと写真**／④**タスクが小さくなりすぎていないか**
   （提案を上げた代償。戻すなら `OFFER_D` か `FILL`）／⑤**日付・曜日の板の
   大きさと当たり判定**／⑥**穴が閉じる速さ**／⑦**吸い付きの強さ**。
2. ★**ニュースを押せるようにするか**は保留（流れているものを狙う問題が先）。
3. ★**`magazine` と `ItemStatus "planned"` を消す**（死んだ欄。`plannedFor` が正）。

## 未解決

- ★★★**提案の生成は止まったまま**（未読 41 枚が減らず `POOL_CAP`(40) に掛かる。
  おすすめ3件の母集団もこの池 ―― **生成が戻るまで新着は増えない**）。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も焼く予算も直していない**（山だけ）。
  ★**起動の代金は残っている**。★`SectionLabel` は 3.2 ＝ AA 割れ。
## 重要パス

★★**ファイルの地図と、踏んだ罠の全文は `CLAUDE.md`。ここは「どこを開くか」だけ。**

- ホーム … `HomeTab.tsx`／帯 `Band.tsx`＋`lib/homeBand.ts`＋`bandMotion.ts`／
  山 `components/home/`（`Pile`＋`pileWorld`＋`pilePaint`）／器 `lib/pileBox.ts`／
  引き下ろし `lib/pullDrag.ts`＋`pillGhost.ts`＋`AssignRail`＋`AssignSheet`＋`DateGrid`。
- 帯の中身 … **「今日行ける」`lib/todayPick.ts`**（夜は明日へ）／**「文章」
  `lib/bandNotes.ts`**（0 件か1件。理由の判定は `offerPick`、言葉は `bandNotes`）／
  **ニュース `lib/newsFeed.ts`＋`app/api/news/route.ts`**／**おすすめ3件
  `lib/offerPick.ts`**／**カード → `Item` は `lib/keepCard.ts` の1本**。
- Explore … **詳細 `components/explore/CardDetail.tsx` ＋ 変形 `lib/cardMorph.ts`**／
  **札の形 `lib/cardShape.ts`**（★**届く倍率 `cardShapeReach`・中か `inCardShape`**）。
- 図形 … `lib/solid.ts`（`stackOutline`）／`lib/taskSize.ts`（`rowsOf`・`rowSpecOf`）／
  `lib/wordPlate.ts`（板）／`lib/cassette.ts`＋`lib/reelHub.ts`（カセット）。
- 語彙 … 色 `lib/constants.ts` の `PALETTE`(7行)／動き `lib/motion.ts`（★**CSS に
  任せられない曲線は `easeAt(EASE_SETTLE, t)`。写経しない**）／バネ `lib/spring.ts`／
  目盛り `lib/tokens.ts`／★規約13本 `design.md`。

### ★ここだけは忘れると必ず壊れる

- **絵は `hx`/`hy`**（落とし所にも支点を渡さない）／**姿は掛け金 `Ghost.pill`**／
  **位置は最後まで指**（`Ghost.aim` は `boolean`）／**近さは3つ**（右端 `RAIL_NEAR` 36／
  幽霊 `BAND_NEAR` 48／狙い `BAND_CATCH` 24。遊び `RAIL_HYST` 12 ＝ **閾値より小さく**）。
- **帯の隙間と席はレイアウト（`.band-pad` の幅）が持つ**／**挿し口は `after`（左どなりの
  id）＋ 遊び `GAP_HYST`**／**狙われている段は流れを止める**／**位相は px で引き継ぐ**。
- **山**… **`clearOverlap` は `Piece.fresh` だけ**／**体も倍率も作り直さない**／
  **質量は同じ密度**／**`atan2` で並べてから `fromVertices`**／**dpr の上限は 2**／
  **ループは止める**（`wake()` を忘れない）／**帯が覆う高さは予算から引く**＋`crowdOf`
  （板は `room` と**上限の両方に**）／**塗るのは変わった図形の箱だけ**。
- **提案の図形** … **`p.r` は絵の半径ではない**（`cardShapeReach` 倍まで届く）／
  **正立で固定**（`setInertia(Infinity)` は `setMass` のあと）／**体の光線は
  `OFFER_VERTS`(28)**／**写真に `crossOrigin` を付けない**（OGP は CORS を返さない）。
- **当たり判定は全部「絵と同じ形」** … 提案 `inCardShape`／タスク `halfWidthAtStack`／
  **板は丸い角の矩形**（板は必ず勝つので、外接箱だと下の図形が掴めない）。
- **実機は 120Hz**／**iOS の上下の帯は解決済み。触らない**。

### 検証の作法

★★**WebKit は `executablePath` 無し／Chromium だけ `/opt/pw-browsers/chromium`**（CPU を
遅くできるのは Chromium だけ）／★★**種に `keeps: []` を入れない**（`migrate` が
`items` を丸ごと捨てる。`wishes: []` は必要）／
★**`generatedDecks` は `Record<号, BriefCard[]>`**／★★**山の測定は 8〜10 秒待つ**／
★★**帯を出す種には「日付の無いタスク」が要る**／
★★★**写真が乗った山の canvas は汚染されて `getImageData` で読めない**（`crossOrigin`
を外したため）→ **画面の写しを別のページで読む**か、写真の無い種で測る／
★★**`bandBus.rows` の添字は論理の段**（0＝提案／1＝タスク／2＝ニュース）／
★★**WebKit は続けて何回も起動すると落ちる**（1プロセス1ケース）／
★★**`next dev` は初回のコンパイルに 45 秒かかる**（`networkidle` は待ち切れない。
`domcontentloaded` にして、先に1度叩いて温めておく）。
