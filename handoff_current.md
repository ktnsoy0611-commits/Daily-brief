# いまどこにいるか（2026-09-16／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。★第110巡に**掴んでいる図形へ当たり判定**（代理の
体）と**山の倍率の据え置き**。★第111〜112巡に「落とし直し」を根絶し、**任意のピルの間へ戻せる**ようにした。
★★★**第113巡に帯の隙間を土台から作り直し**（`transform` のずれ → **レイアウトの器**）、
★★★**第114巡に「どこからでも・どこへでも入る」を通した**（引き抜いたピルの道を
新設／並びは**留め金**／戻した提案が消えない）＋**日付と曜日を黒いピルに白い字**へ。
★★★**Explore の刷新（券・改札鋏）は保留**（`DEV` タブごと最後に処分）／
**試している大きな変更は1つ** … **アプリごとのアクセント配色**（`lib/appAccent.ts` の
`ACCENT_TEST`。**`false` の1行で元へ戻る**）。仕様の正 … `docs/home-spec.md`
（**§6 が引き下ろしの正**）／`docs/project_knowledge.md` §3-h／`design.md`。

---

## 直近完了

### ★★★第114巡 ── どこからでも・どこへでも帯に入る／日付と曜日のピル

> 「**別の場所で離すとメッセージだけ出てどこかに消える／引き出して別のところに
> 入れようとしても元の場所に瞬間移動する／どんな時でも、帯に近づけるとその場所で
> 間が空いて入れ込めるように。これは絶対です**」

**真因1 … 帯へ入れる道は2本あるのに、実装は片方だけだった。** 帯のピルを引き抜くと
`setPointerCapture` しているのは**そのピルの DOM**なので **`Pile.onMove` は1度も
走らない** ―― 狙いも隙間もそこにしか書かれておらず、**戻せる先が「元の席」しか
無かった**。→ 狙いの出し方を**1本**に（`bandAim` ＋ `bandBottom`/`rowCenter`）。

**真因2 … 弾けに掛け金が無かった。** `armed` を毎フレーム測り直していたので、
指を上へ戻すと**弾けが取り消され、元の席へ瞬間移動して戻った**。
→ **一度弾けたら、その指のあいだは弾けたまま**（`pullFrame` の `latched`）。

**真因3 … 戻した提案が時間帯の門で落ちていた**（`pickTodayItems`）。行き先でない種類
（本・モノ）や閉まっている時間だと落ちるのに、**「帯へ戻しました」とは出ていた**。
→ **留め金の付いたものは門を通さない。表はおすすめの並べ方であって、ユーザーの
意思を却下する門ではない。**

**真因4 … 幽霊が帯の裏へ回っていた。** 帯から引くときだけ canvas を上げていた →
**掴んでいる間も上げる**（「消える」の目に見えるほうの半分）。

**真因5 … 入れ直しが「受け渡し」に乗っていなかった。** 件数が変わらないので
`now.length > was.length` では判定できず、**畳んだ席が古い所で開き直し、新しい所の
隙間が別に閉じた**（実測 **1フレーム 139px**）→ 並びが変わったときも `bandGapDone`
を通す（実測 **1px**）。

**★★★並びは「留め金」（`AppState.bandPins`）。`tasks` の並びは触らない。**「この id は、
この id の次」。第112巡は `tasks` を並べ替えていたが ① GRAVITY の山・ALIGN の一覧まで
動く ② 声の候補とフォローアップは**持ち主が別で動かせない**＝「任意のピルとピルの間」が
原理的に作れなかった。★**挿し口は index ではなく「左どなりの id」**（離した瞬間に
`items` は組み替わる）。

**★★★日付と曜日**（ユーザー指定）… `9.15` ／ `WED`、**黒いピルに白い文字**、角丸は
高さの半分（帯のピルと同じ形）。遊びは**字の高さに対する比**（`PILL_PAD`）。★**大きさの
物差しは `WD_FULL` のまま**（3文字で測ると倍近い字になる）。★**TASK の板は変えていない。**

**検証（件数つき）** … 引き抜いて別の場所へ入れ直す＝**できるようになった**（1フレームの
飛び **1px**。前は不可能）／上の段（提案）を戻す＝**3か所とも離した所へ入る**（前は
消えた）／隙間の幅 vs 実際のピルの幅 **153 vs 152**／山→帯の飛び **0.1px**／継ぎ目は
段0・段1 とも**隙間 7 件が全部 8px**（両エンジン）／`pageerror` **0 件**／
機械チェック13本 **全部 0 件**／`tsc` 0・`eslint` 0 error・`next build` 成功。

**消したもの（復活させない）** … `reorderSomeday` ／ `somedayHead` ／ `bandRowAt`。

### 第113巡 ── 帯の隙間を「ずれ」から「レイアウト」へ／塗りの渡り

- ★★★**隙間と席は `transform` のずれでは書けない。レイアウト（`.band-pad` の幅）が
  持つ** ―― **同じ id のピルが2周ぶん居る**ので別々の値を書けない（実測 2周目が
  **152px 飛ぶ**）。**両側 ±w/2 も片側 +w も同じ理由で直らない。**
- ★★★**帯ぜんたいを `lead`＋隙間の半分だけ左へ戻す**（戻さないと指のそばの境目が
  画面の外へ逃げる）。**一様なずれなので挿し込みの瞬間に「送り（px）」へ畳み替える**
  （`currentTime` を直接ずらすと 156px 飛ぶ）。
- ★★★**`K_OPEN`/`D_OPEN`（5組目）を足した** … 90% まで **117ms**・行き過ぎ **0.0%**。
  `K_SETTLE` は 467ms で遅すぎ（150px の隙間が 15px しか開かない）、
  `K_CATCH` は 10.5% 行き過ぎる。
- ★★★**境目は「まだ何も開いていなかったときの位置」で測る**／**挿し口が移るときは
  受け皿 `shut` へ**／**自分が開けている隙間では `ResizeObserver` で組み直さない**。
- ★★★**白抜き ⇄ 塗りは `inkMix` が返す「量」で渡る**（掛け金で切り替えない。
  実測 色の差 157 → 21 ＝ その瞬間の `t` の進みぶんそのもの）。

### 第111〜112巡（要点。詳しくは `docs/project_knowledge.md`）

- ★★★**落とし直しの真因は `clearOverlap` が据え置きの山にも掛かっていたこと**
  （傾いた図形どうしの AABB は必ず重なるので、落ち着いた山が**縦の塔へ積み直されて
  落ち直していた**）→ **`Piece.fresh` だけ**＋**そもそも体を作り直さない**。
- ★★★**帯の下の段は9件で打ち切られ、戻したタスクが黙って消えていた** →
  `bandRows(state, keepId)`。
- ★★★**隙間は指が決め、並びのほうが合わせる**。★**狙われている段は流れを止める**
  （止めないと挿し口が勝手に変わる）。
- ★★★**引き出し中は字も1文字ずつ `bendAt` を読む**／**運ぶ図形は角度そのものを留める**。

### 過去の巡の要点（詳しくは `docs/project_knowledge.md` §3-h）

- ★★★**第109巡 … めり込みの真因は「湧いた瞬間から重なっている」**（60ms で落ちる距離
  **2.5px** に対し散らばり **200px**）→ 投入の直前に `clearOverlap`（372件 → 10件）。
- ★★★**眠りは `GravityTab` と同じ**（`enableSleeping: true`・**大域の門は全削除**。門は
  全体の最大値で測るので1つでも転がっていれば開かない）。代わりに**体を world へ
  入れるフレームで全員を起こす**。★**`positionIterations` は既定の 6**。
- ★★★**当たり判定の光線は「正規化した空間」で飛ばす**／**`GravityTab` の間引きは
  借りない**／**`fromVertices` へ渡す前に `atan2` で並べる**（交差すると床をすり抜ける）／
  **canvas の dpr の上限は 2**／**ループは止める**（`dirtyRef`/`dragRef` を立てる所では
  必ず `wake()`）／**`ghostKey()` 1か所から取る**／**幽霊の絵は `hx`/`hy`**／
  **寿命は3重**／**隠すのは `opacity: 0`**／**`navHeightPx()` を rAF から呼ばない**／
  **塗るのは「変わった図形の箱」だけ**／**`stepGhost` と物理は同じ固定の刻み**／
  ★★★**実機は 120Hz。この環境（60fps）では再現しない**／**沈んだだけなら `sink`**／
  **指の当たり判定も `halfWidthAtStack`**／**`setVelocity` は眠った体を起こさない**／
  **列は `overflowX: "clip"`**。
- **第89〜101巡** … ★★★**canvas の `ctx.font` は読み込みを頼まない**し
  `document.fonts.ready` は**頼んでいないものを待たない**（→`ensureWordFont`。
  **待つなら締切**）／**中身を入れる引き金は「世界を作り終えた最後の1行」だけ**／
  **参照の形は全部「円をつないだ形」。正弦波で円に似せない**／
  **`objectBoundingBox` の器は必ず正方形**（→`fitBox`）／**実機のバグの正体 ＝
  `mask-size` が効かず SVG が 1:1 で置かれていた** → `clip-path`／**切るのは写真だけ**／
  **タグを全廃**（`isDated` だけ）／**ぼやけ ＝ ResizeObserver が2つ**／
  **下が空く ＝ 床の二重引き**（`.bleed-x-b`）／**effect を2本に割る**／
  **タブバーの高さは `navHeightPx()`**（実機 132px／Chromium 81px）。

## 次の一手

1. ★★★**実機で見てもらう**（第114巡）… ①**山の図形を帯へ近づけると、指の下で
   左右のピルが開いて、離すとそこに入るか**（上の段も）／②**帯のピルを引き抜いて
   別の場所へ入れ直せるか**（元の場所へ戻らないか）／③**入れたあとスクロールが
   変にならないか**／④**白抜きから塗りへ滑らかに渡るか**／⑤**日付と曜日の黒い
   ピル**（`9.15`／`WED`）の大きさと字の詰まり／⑥**アクセント配色を採るか捨てるか**
   （捨てるなら `ACCENT_TEST` を `false`）。
2. ★**`magazine` と `ItemStatus "planned"` を消す**（★死んだ欄。`plannedFor` が正。
   後片付けと永続化の移行に絡むので単独の巡で）。
3. **持ち物**（右上のアイコン＋数の印。押すと小さな一覧）／**フォローアップの夜間生成**
   （★プロンプトは先に文面で出して承認を得る）／★ピルを押したときの詳細。

## 未解決

- ★★★**提案の生成は止まったまま**（15 は表示だけの上限。裏の未読 41 枚が減らず、
  夜間生成が `POOL_CAP`(40) に掛かり続ける）。★`VoiceStudio` の `ResizeObserver` が
  ホームでも走る（未調査）。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も直していない**（山だけ直した）。
- ★★★**帯の上の段の `offer` は引き下ろしても消えない**（第110巡に判明）。`BandItem.id` は
  `offer-<card.id>` だが、そのカードは `generatedDecks` に居るだけで **`Item` が存在しない**
  （KEEP して初めて `Item` が生まれ、同時に decision が付いて帯から消える）。つまり
  `HomeTab.put` の `items.find` は**原理的に当たらず、何も書かずに return する**。
  ★★**直すなら「引き下ろす ＝ KEEP ＋ `plannedFor`」**。**勝手にやらない。**
  ★★**並べ替えは第114巡に通した**（引き抜いて別の場所へ入れ直すのは効く）。
- ★**帯の並びは `AppState.bandPins`（留め金）が持つ**（第114巡）。`tasks` は触らないので
  **GRAVITY と ALIGN は動かない**。★隣が消えると自然な位置へ戻る（＝「覚え書き」）。
- ★**TASK（GRAVITY）の日付・曜日の板は「文字そのものが図形」のまま**（ホームだけ
  黒いピルにした）。**揃えるかはユーザーに確認していない。**
- ★**新しい図形が落ちて山に当たると 3〜4px 動く**（**物理**なので直さない）。
- ★★**黄の札と地の比 1.27**（縁は**影**が持つ）／`SectionLabel` は明るい地で 3.2 ＝
  AA 割れ。**未対応。**

---

## 重要パス

- ホーム … `HomeTab.tsx`／帯 `Band.tsx` ＋ `lib/homeBand.ts` ＋ `bandMotion.ts`／
  山 `components/home/`（`Pile.tsx`＋`pileWorld.ts`＋`pilePaint.ts`）／器 `lib/pileBox.ts`
- ★★★**引き下ろし … `lib/pullDrag.ts`／`pillGhost.ts`／`AssignRail.tsx`／
  `AssignSheet.tsx`／盤は `components/DateGrid.tsx`**。★**手つきは「輪ゴム(44px・
  指と1:1) → ばちん → 変形」**／**絵は `hx`/`hy`**／**姿は掛け金 `Ghost.pill`**／
  **遊び `RAIL_HYST`**。★★★**戻りは2段** …
  `RAIL_NEAR`(72) で幽霊が生まれて**指に追従**し、`PULL_ARM`(44) で**捕まえて**
  `Ghost.aim`（**段の中心は `PullHost.rowCenter` が実測**）へ `K_SETTLE` で吸い付く。
  ★★★**変形 `K_SWING`／吸い付き `K_CATCH`**。掛け金は `tTo` で分ける。
  ★★★**弾けは掛け金**（`pullFrame` の `latched`。第114巡）―― 毎フレーム測り直すと
  **指を戻した瞬間にピルが元の席へ瞬間移動して戻る**。
  ★★★**帯へ入れる道は2本**（山の図形 ／ 引き抜いたピル）。**狙いの出し方は1本**
  （`bandAim` ＋ `PullHost.bandBottom`/`rowCenter`）―― 引き抜いたピルの指は
  **ピルの DOM が捕まえている**ので `Pile.onMove` は1度も走らない。
  ★★**掴んでいる間は山の canvas を上げる**（上げないと幽霊が帯の裏へ回って消える）。
- ★★★**帯の物理っぽい動きは `components/home/bandMotion.ts` の1か所**（詳細は
  `CLAUDE.md` のファイル地図）。**隙間と席はレイアウト（`.band-pad` の幅）が持つ**／
  **帯ぜんたいを `lead`＋隙間の半分だけ左へ戻し、挿し込みで「送り（px）」へ畳み替える**／
  **バネは `K_OPEN`（隙間と席）と `K_SWING`（帯ぜんたいの一発）**／**境目は開く前の
  位置で測る**／**開けているあいだ組み直さない**／**挿し口は `after`（左どなりの id）**。
- ★★★**白抜き ⇄ 塗りは `inkMix`（`components/home/pillGhost.ts`）の1か所**。掛け金で
  切り替えない。両端は「帯のピル／山の図形が塗りか」から導く。混ぜは `mixHex`。
- ★★★**掴んでいる図形の代理の体**（`Pile.syncProxy`／`pileWorld.ghostBodyOf`）…
  **絵は体そのもの**／**体は絵の中心**（支点ではない）／**`piecesRef` に混ぜない**／
  **`clearOverlap` の前に消す**／**停止条件に入れる**／**締切 `HANDOFF_MS`**。
  ★★**山の倍率は中身が変わっても決め直さない**（`hold`）。
- ★★★**セーフエリアは全画面で `SPACE.lg`、左右対称**（`lib/pileBox.ts`。**もう振らない**）
  ／**器が変わったときの4つ**（`FLOOR_CHECK_MS`／`refitPile`／`sink`／`isLost`→`respawn`）
  ／**器は `.bleed-x-b`**／**ResizeObserver は1つ**。
- ★★★**当たり判定の多角形は「軸の本数 × 頂点数」**（`PHYS_VERTS` 12・等角・偶数。
  **光線は正規化した空間で飛ばす**）／**投入の直前に `clearOverlap`**／**質量は全部
  同じ密度に**／**`fromVertices` へ渡す前に `atan2` で並べる**／**眠りは `GravityTab`
  と同じ**（`dirtyRef`/`dragRef` を立てる所では必ず `wake()`）／**タスクの形は
  `stackOutline`・段の数は `rowsOf(title)`・`waist` が「ピル ⇄ 図形」**／
  **塗り／輪郭は `paintShape` と `isDated`**（`pilePaint.ts` にも同じ分岐。**両方直す**）。
- ★**カセット `lib/cassette.ts`**／**芯 `lib/reelHub.ts`**／**札の形 `lib/cardShape.ts`**／
  **文字の板 `lib/wordPlate.ts`**（★ホームの日付・曜日は**黒いピルに白い字**）／
  **帯の「今日行ける」 `lib/todayPick.ts`**／★★★**書体は3つ**／★**色は `PALETTE`
  （6行）だけ**（面から導く `bodyInkOn`・`redOn`・`inkVarsOn`・`mixHex`）／
  ★規約13本 `design.md`／目盛り `lib/tokens.ts`／動き `lib/motion.ts`。
- 検証 … ★★★**WebKit がこの環境で動く**（`executablePath` 無し。**Chromium だけ**
  `/opt/pw-browsers/chromium`）／★★★**種に `keeps: []` を入れない**（`migrate` が
  `items` を丸ごと捨てる。`wishes: []` は必要）／★★**山の測定は 8 秒待つ**／
  ★★**帯を出す種には「日付の無いタスク」が要る**／★★**ピルを掴む試験は「画面の中に
  居る」ものを選ぶ**／★★★**帯の飛びは「画面に見えているピル」だけで測る**
  （2周目の控えは画面の外に居るので、数えると 152px の偽陽性が出る）／
  ★★**WebKit は続けて何回も起動すると落ちる**（1プロセス1ケースに割る）。
