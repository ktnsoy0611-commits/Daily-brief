# いまどこにいるか（2026-09-16／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。★第110巡に**掴んでいる図形へ当たり判定**（代理の
体）と**山の倍率の据え置き**。★第111巡に**体そのものの使い回し**で「落とし直し」を根絶し、**戻したタスクが消えるのを
止めた**。★第112巡に**任意のピルの間へ戻せる**ようにし（指が場所を決め、並びが合わせる）、
**掴んだ図形が回るのを止めた**。★★★**第113巡に帯の隙間を土台から作り直した**
（`transform` のずれ → **レイアウトの器**）＋**白抜き ⇄ 塗りを「量」で渡す**ようにした。
★★★**Explore の刷新（券・改札鋏）は保留**（`DEV` タブごと最後に処分）／
**試している大きな変更は1つ** … **アプリごとのアクセント配色**（`lib/appAccent.ts` の
`ACCENT_TEST`。**`false` の1行で元へ戻る**）。仕様の正 … `docs/home-spec.md`
（**§6 が引き下ろしの正**）／`docs/project_knowledge.md` §3-h／`design.md`。

---

## 直近完了

### ★★★第113巡 ── 帯の隙間を「ずれ」から「レイアウト」へ／塗りの渡り

ユーザー指摘6点を**5つの真因**まで辿って直した。

**真因1（消える・スクロールが変・瞬間移動）… 2周ぶん並んだ帯に、id で引くずれは書けない**
帯は同じ列を2周ぶん並べて送るので**同じ id のピルが2つ居る**。`transform` のずれは
`data-pill-id` で引くため**1周目と2周目に別の値を書けない** ―― 受け渡しでは
**2周目の全員がレイアウトで `+w` 動く**のに `-w` しか戻せなかった（実測 **2周目の先頭が
152px 飛ぶ**。1周目は 3px）。→ **隙間も席もレイアウトが持つ**（ピルとピルのあいだの器
`.band-pad` の幅を書く）。2周とも同じ列を描くので**自動的に2周ぶん・同じ index**に
生まれ、**挿し込みで増える幅＝開けていた幅**なので**受け渡しの計算が要らない**。

**真因2（どかない）… `K_SETTLE` は 90% まで 467ms**
指が挿し口に居るのは 0.1〜0.3秒。**150px の隙間が 15px しか開いていなかった。**
→ **`K_OPEN`/`D_OPEN`（5組目）を足した**（**90% まで 117ms・行き過ぎ 0.0%**。
★`K_CATCH` は速いが 10.5% 行き過ぎるので跳ね返り禁止の帯には使えない）。

**真因3（画面に出ない）… 1周目の隙間が2周目を丸ごと押す**
実測 … 指が x=200 なのに隙間は 405〜563（どちらの周も画面外）。→ **帯ぜんたいを `lead`
＋ 隙間の半分だけ左へ戻す**。**一様なずれなので挿し込みの瞬間に「送り（px）」へ
畳み替える**（★`currentTime` を直接ずらすと **156px 飛ぶ**）。

**真因4（挿し口が行ったり来たり）… 開いた器の中心で測っていた**
器は左の縁が動かないまま右へ育つので中心が指から逃げる（実測 指を止めていても
index が 1 → 0 へ飛ぶ）。→ **「まだ何も開いていなかったときの境目」で測る**。
挿し口が移るときは受け皿 `shut` へ預ける（**勢いは渡さない**）。

**真因5（段が止まらない）… 隙間がレイアウトなので `ResizeObserver` が毎フレーム鳴る**
組み直すと `track.animate` が新しく走り出して**止めていた段が流れ始める**。
→ **自分が開けているあいだは組み直さない**（中身が変われば `sig` の effect が組む）。

**⑥ 白抜き ⇄ 塗り** … 掛け金（`Ghost.pill`）で**絵ごと入れ替わって**いた。
→ **`inkMix(g)` が「塗りの量」を1つ返し、2つの絵が同じ量を読む**（地 → 面 `α=mix`
→ 輪郭 `α=1−mix`。字も `mixHex` で同じ量）。**両端は決め打ちにしない**（日付の無い
図形は山でも輪郭なので、決め打ちだと勝手に塗られる）。

**検証（件数つき）** … ② 隙間は 90% まで **5〜10フレーム**（前は 28）・幅 **149/150px**・
**2周ぶんに開き画面に 1 件見える**・狙っているあいだ段は `paused`／③⑤ 挿し込みは
**見えているピルの飛び 最大 2.0px**（前は **152px**）、戻ったピルは指の x=200 に対し
**189**（＝指の下）、1.2秒後も全部流れている／① 引き抜きは飛び **0〜1px**（3/3 件）・
`paused → running`／継ぎ目は段0/段1 とも**隙間 7 件が全部 8px**（周と周のあいだも同じ。
前は `SPACE.sm` ぶん広かった。両エンジン）／⑥ 塗りは掛け金が跳ぐ前後の色の差
**157 → 21**（21 は `t` の進み 0.125 ぶんそのもの＝段差ではない。標本 40 件）／
回帰は `pageerror` **0 件**（両エンジン）・機械チェック13本（#1〜#7・#10〜#13 が
**全部 0 件**）・`tsc` 0・`eslint` 0 error・`next build` 成功。

**消したもの（復活させない）** … `Off = Spring & { hole, gap, wait }`／`bandGap`／
`bandHole(ids, gap)`／`bandGapCommit`／`bandHoleCommit`／`nudge` の表／`STAGGER`／
`.band-slot` の `--nudge`／周の末尾の詰め物。

### 第111〜112巡（要点。詳しくは `docs/project_knowledge.md`）

- ★★★**落とし直しの真因は `clearOverlap` が据え置きの山にも掛かっていたこと**
  （AABB で真上へ持ち上げる道具。傾いた図形どうしの AABB は必ず重なるので、
  落ち着いた山が**縦の塔へ積み直されて落ち直していた**）→ **`Piece.fresh` だけ**＋
  **そもそも体を作り直さない**（形の署名が一致したら使い回す）。
- ★★★**帯の下の段は9件で打ち切られ、戻したタスクが黙って消えていた** →
  `bandRows(state, keepId)`。
- ★★★**隙間は指が決め、並びのほうが合わせる**（`reorderSomeday`）。★**狙われている
  段は流れを止める**（止めないと挿し口が勝手に変わる）。
- ★★★**運ぶ図形は角度そのものを留める** ―― `setAngularVelocity(0)` だけでは
  接触が毎歩 回りを注ぎ込む（0.0234 rad/歩 ＝ 約 80°/秒）。
- ★★★**引き出し中は字も1文字ずつ `bendAt` を読む**（左端1点だと平行移動）。

### 過去の巡の要点（詳しくは `docs/project_knowledge.md` §3-h）

- ★★★**第109巡 … めり込みの真因は「湧いた瞬間から重なっている」** ―― 時間（60ms）で
  落ちる距離は **2.5px** なのに散らばりは **200px**（79倍）。→ `Composite.add` の直前に
  `clearOverlap`（8px 超 **372件 → 10件**）。★**板の質量も箱から**。
- ★★★**眠りは `GravityTab` と同じ**（`enableSleeping: true`・**大域の門は全削除**。門は
  全体の最大値で測るので**1つでも転がっていれば開かない**）。代わりに**体を world へ
  入れるフレームで全員を起こす**。★**`positionIterations` は既定の 6**
  （`clamp(20/n,0,1)` なので 6 でも 10 でも減衰は 1。上げると振動が持続する）。
- ★★★**当たり判定の光線は「正規化した空間」で飛ばす**（px だと横長の形の丸い端が
  6頂点に潰れる → 16頂点）／**`GravityTab` の間引きは借りない**（重心が 2.87px ずれる）／
  **`fromVertices` へ渡す前に `atan2` で並べる**（交差すると床をすり抜ける）／
  **canvas の dpr の上限は 2**（ホームだけ 3 で実機 2.77M 画素を塗っていた）。
- ★★★**ループは止める**（`runningRef`/`wake()`。**`dirtyRef` や `dragRef` を立てる所では
  必ず `wake()`**）／**`ghostKey()` 1か所から取る**／**幽霊の絵は `hx`/`hy`**／
  **姿は掛け金 `Ghost.pill`**／**境目には遊び `RAIL_HYST`**／**寿命は3重**／
  **隠すのは `opacity: 0`**／**`navHeightPx()` を rAF から呼ばない**／
  **塗るのは「変わった図形の箱」だけ**／**`stepGhost` と物理は同じ固定の刻み**。
- ★★**測る窓を間違えない**／★★★**実機は 120Hz。この環境（60fps）では再現しない**／
  **沈んだだけなら `sink`**／**指の当たり判定も `halfWidthAtStack`**／
  **`setVelocity` は眠った体を起こさない**／**列は `overflowX: "clip"`**。
- **第89〜101巡** … ★★★**canvas の `ctx.font` は読み込みを頼まない**し、`document.fonts.ready` は**頼んで
  いないものを待たない**（→`ensureWordFont`。**待つなら締切**）／**中身を入れる引き金は
  「世界を作り終えた最後の1行」だけ**／**参照の形は全部「円をつないだ形」。正弦波で
  円に似せない**／**`objectBoundingBox` の器は必ず正方形**（→`fitBox`）／**実機のバグの
  正体 ＝ `mask-size` が効かず SVG が 1:1 で置かれていた** → `clip-path`／
  **切るのは写真だけ。札には掛けない**／**タグを全廃**（`isDated` だけ）／
  **ぼやけ ＝ ResizeObserver が2つ**／**下が空く ＝ 床の二重引き**（`.bleed-x-b`）／
  **effect を2本に割る**／**タブバーの高さは `navHeightPx()`**（実機 132px／Chromium 81px）。

## 次の一手

1. ★★★**実機で見てもらう**（第113巡）… ①**図形を帯へ近づけると指の下で左右のピルが
   開くか**（段が止まるか）／②**離したピルが指の下に現れるか**（消えないか）／
   ③**入れたあとスクロールが変にならないか**／④**引き抜いて離したとき瞬間移動
   しないか**／⑤**白抜きから塗りへ滑らかに渡るか**／⑥**アクセント配色を採るか捨てるか**
   （捨てるなら `ACCENT_TEST` を `false`）。
2. ★**`magazine` と `ItemStatus "planned"` を消す**（★死んだ欄。`plannedFor` が正。
   後片付けと永続化の移行に絡むので単独の巡で）。
3. **持ち物**（右上のアイコン＋数の印。押すと小さな一覧）／**フォローアップの夜間生成**
   （★プロンプトは先に文面で出して承認を得る）／★ピルを押したときの詳細。

## 未解決

- ★★★**提案の生成は止まったまま**（15 は表示だけの上限。裏の未読 41 枚が減らず、
  夜間生成が `POOL_CAP`(40) に掛かり続ける）。★`VoiceStudio` の `ResizeObserver` が
  ホームでも走る（未調査）。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も直していない**（山だけ直した）。**120Hz では
  物理が2倍速**のはずだが、塗る画素が少なく眠りも効いていて症状は出ていない。
- ★★★**帯の上の段の `offer` は引き下ろしても消えない**（第110巡に判明）。`BandItem.id` は
  `offer-<card.id>` だが、そのカードは `generatedDecks` に居るだけで **`Item` が存在しない**
  （KEEP して初めて `Item` が生まれ、同時に decision が付いて帯から消える）。つまり
  `HomeTab.put` の `items.find` は**原理的に当たらず、何も書かずに return する**。
  ★★**直すなら「引き下ろす ＝ KEEP ＋ `plannedFor`」**。**勝手にやらない。**
- ★**帯へ戻すと「離した場所」が並び順になる**（第112巡にユーザー確定）。`tasks` の
  並びが変わるので **GRAVITY の山・ALIGN の一覧の順も一緒に動く**（了承済み）。
  ★声の候補とフォローアップは動かせないので、その群より前を指されたら先頭へ丸まる。
- ★**上の段（提案）は隙間こそ開くが、離した所へは並ばない**（`reorderSomeday` が
  タスク専用。`today` の順は `lib/todayPick.ts` が決めている）。
- ★**新しい図形が落ちて山に当たると 3〜4px 動く**（**物理**なので直さない）。
- ★★**黄の札と地の比 1.27**（縁は**影**が持つ）／`SectionLabel` は明るい地で 3.2 ＝
  AA 割れ。**未対応。**

---

## 重要パス

- ホーム … `HomeTab.tsx`／帯 `Band.tsx` ＋ `lib/homeBand.ts` ＋ `bandMotion.ts`／
  山 `components/home/`（`Pile.tsx`＋`pileWorld.ts`＋`pilePaint.ts`）／器 `lib/pileBox.ts`
- ★★★**引き下ろし … `lib/pullDrag.ts`（算数・`pullBus`・`armOffset`・`Pin`）／
  `pillGhost.ts`／`AssignRail.tsx`／`AssignSheet.tsx`／盤は `components/DateGrid.tsx`**。
  ★**手つきは「輪ゴム(44px・指と1:1) → ばちん → 変形」**／**絵は `hx`/`hy`**／
  **姿は掛け金 `Ghost.pill`**／**境目には遊び `RAIL_HYST`**。★★★**戻りは2段** …
  `RAIL_NEAR`(72) で幽霊が生まれて**指に追従**し、`PULL_ARM`(44) で**捕まえて**
  `Ghost.aim`（**段の中心は `bandRowAt` が実測**）へ `K_SETTLE` で吸い付く。
  ★★★**変形 `K_SWING`／吸い付き `K_CATCH`**。掛け金は `tTo` で分ける。
- ★★★**帯の物理っぽい動きは `components/home/bandMotion.ts` の1か所**（第113巡に作り直し）。
  ★★★**隙間と席は `transform` では書けない。レイアウト（`.band-pad` の幅）が持つ**
  ―― 同じ id のピルが2周ぶん居るので、id で引くずれには別々の値を書けない
  （実測 2周目が 152px 飛ぶ）。**両側 ±w/2 も片側 +w も同じ理由で直らない。**
  ★★**帯ぜんたいを `lead` ＋ 隙間の半分だけ左へ戻し、挿し込みの瞬間に「送り（px）」へ
  畳み替える**（`holdRef` → `phaseRef`。**`currentTime` を直接ずらさない**）。
  ★★**バネは `K_OPEN`（隙間と席）と `K_SWING`（帯ぜんたいの一発）**。
  ★**境目は「まだ何も開いていなかったときの位置」で測る**／**挿し口が移るときは
  受け皿 `shut` へ（勢いは渡さない）**／**開けているあいだ `ResizeObserver` で
  組み直さない**／**席は `armed` に畳む**／**受け渡しは `useLayoutEffect` で `paint()`
  まで**／**`bandAim()` を通す**（段の rAF は止まっていることがある）／
  **位相は px で引き継ぎ、下の段（`reverse`）は折り返す**（幅は `lapRef` から。
  控えるのは `snapPhase()`）。
- ★★★**白抜き ⇄ 塗りは `inkMix`（`components/home/pillGhost.ts`）の1か所**。掛け金で
  切り替えない。両端は「帯のピル／山の図形が塗りか」から導く。混ぜは `mixHex`。
- ★★★**掴んでいる図形の代理の体**（`Pile.syncProxy`／`pileWorld.ghostBodyOf`）…
  **絵は体そのもの**／**体は絵の中心**（支点ではない）／**`piecesRef` に混ぜない**／
  **`clearOverlap` の前に消す**／**停止条件に入れる**／**締切 `HANDOFF_MS`**。
  ★★**山の倍率は中身が変わっても決め直さない**（`hold`）。
- ★★★**セーフエリアは全画面で `SPACE.lg`、左右対称**（`lib/pileBox.ts` の `PILE_INSET`。
  タブバーは右下の丸まで含む。**もう振らない**）／**器が変わったときの4つ** …
  `FLOOR_CHECK_MS`／`refitPile`／**`sink`**／`isLost`→`respawn`／**器は `.bleed-x-b`**／
  **ResizeObserver は1つ**。
- ★★★**当たり判定の多角形は「軸の本数 × 頂点数」**（`PHYS_VERTS` 12・等角・偶数。
  **光線は正規化した空間で飛ばす**）／**投入の直前に重なりを解く**（`clearOverlap`）／
  **質量は全部同じ密度に**（箱 ÷ `unit²`）／**`fromVertices` へ渡す前に `atan2` で並べる**／
  **眠りは `GravityTab` と同じ**（`dirtyRef`/`dragRef` を立てる所では必ず `wake()`）／
  **タスクの形は `stackOutline`・段の数は `rowsOf(title)`・`waist` が「ピル ⇄ 図形」**／
  **塗り／輪郭は `paintShape` と `isDated`**（`pilePaint.ts` にも同じ分岐。**両方直す**）。
- ★**カセット `lib/cassette.ts`**／**芯 `lib/reelHub.ts`**／**札の形 `lib/cardShape.ts`**
  （点の列が正。`clip-path`・器は必ず正方形）／**文字の板 `lib/wordPlate.ts`**／
  **帯の「今日行ける」 `lib/todayPick.ts`**／★★★**書体は3つ**（`SANS`／`LATIN`／
  `DISPLAY`）／★**色は `lib/constants.ts` の `PALETTE`（6行）だけ**（面から導く
  `bodyInkOn`・`redOn`・`inkVarsOn`・`mixHex`）／★規約13本 `design.md`／
  目盛り `lib/tokens.ts`／動き `lib/motion.ts`。
- 検証 … `localhost` で開く。★★★**WebKit がこの環境で動く**（`executablePath` 無し。
  **Chromium だけ** `/opt/pw-browsers/chromium`）／★★★**種に `keeps: []` を入れない**
  （`migrate` が `items` を丸ごと捨てる。`wishes: []` は必要）／★★**山の測定は 8 秒待つ**／
  ★★**帯を出す種には「日付の無いタスク」が要る**（全部に `dueDate` を付けると帯が空に
  なり `bandBottom()` が 0 を返す）／★★**帯のピルを掴む試験は「画面の中に居る」ものを
  選ぶ**／★★★**帯の飛びは「画面に見えているピル」だけで測る**（2周目の控えは画面の外に
  居て、1周の幅が変われば当然ずれる ―― 数えると 152px の偽陽性が出る）。
