# いまどこにいるか（2026-09-16／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★軸の操作＝**帯のピルを引いて日付を
割り当てる**と、**山の図形を帯へ戻す**。★第110〜112巡に「落とし直し」を根絶し、
代理の体と倍率の据え置きを入れた。★★★**第113巡に帯の隙間を土台から作り直し**
（`transform` のずれ → **レイアウトの器**）、★★★**第114巡に「どこからでも・どこへでも
入る」を通し**（引き抜いたピルの道を新設／並びは**留め金**）、★★★**第115巡に「最初の
落下が重い」を根治**（起動の代金だった）＋**形を S 字の折りたたみ**へ、
**ホームの図形は全部同じ大きさ**に。
★★★**Explore の刷新（券・改札鋏）は保留**（`DEV` タブごと最後に処分）／
**試している大きな変更は1つ** … **アプリごとのアクセント配色**（`lib/appAccent.ts` の
`ACCENT_TEST`。**`false` の1行で元へ戻る**）。仕様の正 … `docs/home-spec.md`
（**§6 が引き下ろしの正**）／`docs/project_knowledge.md` §3-h／`design.md`。

---

## 直近完了

### ★★★第115巡 ── 「最初の落下が重い」の根治／S 字の形／大きさを1つに

> 「**一番最初に図形が落ちてくる時、フレームレートが下がって重いです。gravity では、
> 今より多い図形の数でも軽かったです。根本から分析して修正しなさい**」

★★★**ユーザーの観察がそのまま答えの半分だった** ―― **GRAVITY が軽いのは「最初の
画面ではない」から**。ホームが払っていたのは**起動の代金**で、山の物理でも塗りでも
なかった（実測 … 物理は4秒で 35ms、塗りは1回 8ms、`Paint` は 35ms）。

**① 他の2つのアプリが落下の最中に一度に組み上がる。** `requestIdleCallback` の
`timeout` は「暇が無くても必ず呼ぶ」締切なので**必ず落下に割り込む**。全文書の
レイアウトが1回 **400ms 超**（348/516 個）→ **落ち着いてから、1つずつ。**
**② 見えていない `VoiceStudio` が毎フレーム寸法を測っていた**（`clientWidth` は全文書の
レイアウトを強制）。実測 **524ms**（12.8%）→ `appActive` で止める。
**③ 最初の1フレームで全部焼いていた**（`bakeGlyph` 150枚ぶん。ループの1回が **520ms**）
→ **1フレーム 2枚**まで（焼けていない図形には代役がある）。
**④ 和文は文字ごとに別の断片**なので、塗りの中で `document.fonts.load` を呼ぶと
**届くたびに再レイアウト＋焼き直し**が落下に重なる → **落ちる前に頼む**。
**⑤ 重要度で大きさが違うと、焼いた字が1枚も使い回せない**（bucket が全部違う）
→ 下の「大きさを1つに」で **焼き 523ms → 357ms**。★設計の変更がそのまま速さになった。

★**外した推測も残す** … 多角形の切り抜き（効果なし）／`imageSmoothingQuality`（変わらず）／
書体そのもの（`.woff2` を遮断してもストールは残った）。

**★★★形は「一本の長いピルを S 字に折りたたんだもの」**（ユーザー指定）。折り返す側は
**半径 `rowH` の半円が2段ぶんを1つの弧で覆う**／端は今までどおりキャップ／
**外接箱は変わらない＝大きさは変わらない**／**1段は完全に同じ**。★★★**左右で式が
違うので半幅では書けない** ―― 正は **`stackEdge(rows, ar, y, side)`**、
`halfWidthAtStack` は**その内側の包絡**。★点の数も y の並びも同じなので `waist` は効く。

**★★★ホームの図形は重要度でも切迫度でも大きさが変わらない**（ユーザー指定）。
`weight` と `dueDate` を固定して `specOf` に渡すだけ。★**`lib/taskSize.ts` は触らない。**

**検証（件数つき）** … CPU×4・12体・4秒で **フレーム 58 → 115〜127 件**／**90 パーセン
タイル 233ms → 50ms**／いちばん遅いフレーム 883 → 667ms（参考 … GRAVITY は 178 件）／
900ms で TASK と JOURNAL を押しても**ちゃんと出る**（先読みを遅らせた副作用なし）／
帯の継ぎ目 隙間 7 件が全部 8px／引き抜いて入れ直す飛び **1px**／`pageerror` 0 件／
機械チェック13本 全部 0 件／`tsc` 0・`eslint` 0 error・`next build` 成功。

### 第114巡 ── どこからでも・どこへでも帯に入る／日付と曜日のピル

- ★★★**帯へ入れる道は2本あるのに実装は片方だけだった** ―― 引き抜いたピルの指は
  **ピルの DOM が捕まえている**ので `Pile.onMove` は1度も走らない。狙いも隙間もそこに
  しか無く、**戻せる先が「元の席」しか無かった**。→ 狙いは**1本**。
- ★★★**弾けは掛け金**（`pullFrame` の `latched`）―― 毎フレーム測り直していたので、
  指を戻すと**元の席へ瞬間移動して戻った**。
- ★★★**戻した提案が時間帯の門（`pickTodayItems`）で落ちていた**（「メッセージだけ出て
  消える」）→ **留め金の付いたものは門を通さない。表はユーザーの意思を却下する門
  ではない。** ★★**幽霊が帯の裏へ回っていた**（掴んでいる間も canvas を上げる）。
- ★★★**入れ直しも受け渡しに乗せる**（件数が変わらないので漏れていた。139px → **1px**）。
- ★★★**並びは「留め金」（`AppState.bandPins`）**。`tasks` は触らない ―― ① GRAVITY と
  ALIGN まで動く ② 声の候補とフォローアップは**持ち主が別で動かせない**＝「任意の
  ピルとピルの間」が原理的に作れなかった。★**挿し口は index ではなく「左どなりの id」**。
- ★★★**日付 `9.15` ／ 曜日 `WED`、黒いピルに白い文字**（遊びは字の高さに対する比。
  大きさの物差しは `WD_FULL` のまま）。★**TASK の板は変えていない。**

### 第113巡 ── 帯の隙間を「ずれ」から「レイアウト」へ／塗りの渡り

- ★★★**隙間と席は `transform` のずれでは書けない。レイアウト（`.band-pad` の幅）が
  持つ** ―― **同じ id のピルが2周ぶん居る**ので別々の値を書けない（実測 2周目が
  **152px 飛ぶ**）。**両側 ±w/2 も片側 +w も同じ理由で直らない。**
- ★★★**帯ぜんたいを `lead`＋隙間の半分だけ左へ戻し、挿し込みで「送り（px）」へ
  畳み替える**（`currentTime` を直接ずらすと 156px 飛ぶ）／**境目は開く前の位置で
  測る**／**挿し口が移るときは受け皿 `shut` へ**／**開けているあいだ組み直さない**。
- ★★★**`K_OPEN`/`D_OPEN`（5組目）** … 90% まで **117ms**・行き過ぎ **0.0%**
  （`K_SETTLE` は 467ms で遅すぎ、`K_CATCH` は 10.5% 行き過ぎる）。
- ★★★**白抜き ⇄ 塗りは `inkMix` が返す「量」で渡る**（掛け金で切り替えない。
  実測 色の差 157 → 21 ＝ その瞬間の `t` の進みぶんそのもの）。

### 過去の巡の要点（詳しくは `docs/project_knowledge.md` §3-h）

- ★★★**第111〜112巡** … 落とし直しの真因は **`clearOverlap` が据え置きの山にも
  掛かっていた**こと（傾いた図形の AABB は必ず重なる＝縦の塔へ積み直される）→
  **`Piece.fresh` だけ**＋**体を作り直さない**／**下の段の9件の打ち切りが戻したタスクを
  黙って消していた**（`keepId`）／**隙間は指が決め、並びが合わせる**／**狙われている段は
  流れを止める**／**字も1文字ずつ `bendAt`**／**運ぶ図形は角度を留める**。

- ★★★**第109巡 … めり込みの真因は「湧いた瞬間から重なっている」**（60ms で落ちる距離
  **2.5px** に対し散らばり **200px**）→ 投入の直前に `clearOverlap`（372件 → 10件）／
  ★★★**眠りは `GravityTab` と同じ**（**大域の門は全削除**。門は全体の最大値で測るので
  1つでも転がっていれば開かない）。代わりに**体を world へ入れるフレームで全員を
  起こす**。★**`positionIterations` は既定の 6**。
- ★★★**当たり判定の光線は「正規化した空間」で飛ばす**／**`GravityTab` の間引きは
  借りない**／**`fromVertices` へ渡す前に `atan2` で並べる**（交差すると床をすり抜ける）／
  **canvas の dpr の上限は 2**／**ループは止める**（`dirtyRef`/`dragRef` を立てる所では
  必ず `wake()`）／**`ghostKey()` 1か所から取る**／**幽霊の絵は `hx`/`hy`**／**寿命は3重**／
  **隠すのは `opacity: 0`**／**`navHeightPx()` を rAF から呼ばない**／**塗るのは「変わった
  図形の箱」だけ**／**`stepGhost` と物理は同じ固定の刻み**／★★★**実機は 120Hz。この環境
  （60fps）では再現しない**／**沈んだだけなら `sink`**／**指の当たり判定も
  `halfWidthAtStack`**／**`setVelocity` は眠った体を起こさない**／**列は `overflowX: "clip"`**。
- **第89〜101巡** … ★★★**canvas の `ctx.font` は読み込みを頼まない**し
  `document.fonts.ready` は**頼んでいないものを待たない**（→`ensureWordFont`。**待つなら
  締切**）／**中身を入れる引き金は「世界を作り終えた最後の1行」だけ**／**参照の形は全部
  「円をつないだ形」。正弦波で円に似せない**／**`objectBoundingBox` の器は必ず正方形**／
  **実機のバグの正体 ＝ `mask-size` が効かず SVG が 1:1 で置かれていた** → `clip-path`／
  **切るのは写真だけ**／**タグを全廃**／**ぼやけ ＝ ResizeObserver が2つ**／
  **下が空く ＝ 床の二重引き**／**effect を2本に割る**／**タブバーの高さは `navHeightPx()`**。

## 次の一手

1. ★★★**実機で見てもらう**（第115巡）… ①**最初の落下が軽くなったか**／
   ②**S 字の形と、大きさが揃ったこと**／③**山の図形を帯へ近づけると指の下で
   左右のピルが開いて、離すとそこに入るか**（上の段も）／④**帯のピルを引き抜いて
   別の場所へ入れ直せるか**／⑤**日付と曜日の黒いピル**／⑥**アクセント配色を
   採るか捨てるか**（捨てるなら `ACCENT_TEST` を `false`）。
2. ★**`magazine` と `ItemStatus "planned"` を消す**（★死んだ欄。`plannedFor` が正。
   後片付けと永続化の移行に絡むので単独の巡で）。
3. **持ち物**（右上のアイコン＋数の印。押すと小さな一覧）／**フォローアップの夜間生成**
   （★プロンプトは先に文面で出して承認を得る）／★ピルを押したときの詳細。

## 未解決

- ★★★**提案の生成は止まったまま**（15 は表示だけの上限。裏の未読 41 枚が減らず、
  夜間生成が `POOL_CAP`(40) に掛かり続ける）。★`VoiceStudio` の `ResizeObserver` は
  **第115巡に止めた**（ホームで 524ms 食っていた）。
- ★★★**`GravityTab` は歩数も多角形も塗る箱も焼く予算も直していない**（山だけ直した）。
- ★★★**帯の上の段の `offer` は引き下ろしても消えない**（第110巡に判明）。`BandItem.id` は
  `offer-<card.id>` だが、そのカードは `generatedDecks` に居るだけで **`Item` が存在しない**
  （KEEP して初めて `Item` が生まれ、同時に decision が付いて帯から消える）。つまり
  `HomeTab.put` の `items.find` は**原理的に当たらず、何も書かずに return する**。
  ★★**直すなら「引き下ろす ＝ KEEP ＋ `plannedFor`」**。**勝手にやらない。**
  ★並べ替えは第114巡に通した。
- ★**帯の並びは `AppState.bandPins`（留め金）が持つ**（第114巡）。`tasks` は触らないので
  **GRAVITY と ALIGN は動かない**。★隣が消えると自然な位置へ戻る（＝「覚え書き」）。
- ★**TASK（GRAVITY）はホームと揃えていない2つ** … 日付・曜日の板は「文字そのものが
  図形」のまま／重要度で大きさが変わるまま（形の S 字だけは共通）。**確認していない。**
- ★**起動の代金は残っている** … 最初の1回の大きなレイアウトとスクリプトの評価
  （実測 1フレーム 667ms）。GRAVITY が軽いのは**ホームが払い終えたあと**だから。
- ★**新しい図形が落ちて山に当たると 3〜4px 動く**（**物理**なので直さない）。
- ★★**黄の札と地の比 1.27**／`SectionLabel` は明るい地で 3.2 ＝ AA 割れ。**未対応。**

---

## 重要パス

- ホーム … `HomeTab.tsx`／帯 `Band.tsx` ＋ `lib/homeBand.ts` ＋ `bandMotion.ts`／
  山 `components/home/`（`Pile.tsx`＋`pileWorld.ts`＋`pilePaint.ts`）／器 `lib/pileBox.ts`
- ★★★**引き下ろし … `lib/pullDrag.ts`／`pillGhost.ts`／`AssignRail.tsx`／
  `AssignSheet.tsx`／盤は `components/DateGrid.tsx`**（詳細は `CLAUDE.md` の地図）。
  ★**手つきは「輪ゴム → ばちん → 変形」**／**絵は `hx`/`hy`**／**姿は掛け金
  `Ghost.pill`**／**遊び `RAIL_HYST`**／**戻りは2段**（`RAIL_NEAR` → `PULL_ARM`）／
  **変形 `K_SWING`・吸い付き `K_CATCH`**。★★★**弾けは掛け金**（`latched`。毎フレーム
  測り直すと指を戻した瞬間に元の席へ瞬間移動する）／★★★**帯へ入れる道は2本**
  （山の図形 ／ 引き抜いたピル）で**狙いの出し方は1本**／★★**掴んでいる間は山の
  canvas を上げる**（上げないと幽霊が帯の裏へ回って消える）。
- ★★★**帯の物理っぽい動きは `components/home/bandMotion.ts` の1か所**（詳細は
  `CLAUDE.md` のファイル地図）。**隙間と席はレイアウト（`.band-pad` の幅）が持つ**／
  **帯ぜんたいを `lead`＋隙間の半分だけ左へ戻し、挿し込みで「送り（px）」へ畳み替える**／
  **バネは `K_OPEN`（隙間と席）と `K_SWING`（帯ぜんたいの一発）**／**境目は開く前の
  位置で測る**／**開けているあいだ組み直さない**／**挿し口は `after`（左どなりの id）**。
- ★★★**白抜き ⇄ 塗りは `inkMix`（`components/home/pillGhost.ts`）の1か所**。掛け金で
  切り替えない。両端は「帯のピル／山の図形が塗りか」から導く。混ぜは `mixHex`。
- ★★★**速さの3つ**（第115巡）… **先読みは落ち着いてから1つずつ**（`AppShell`）／
  **見えていない `VoiceStudio` は寸法を測らない**／**1フレームに焼くのは 2枚まで**＋
  **書体は落ちる前に頼む**。★**ホームの図形は全部同じ大きさ**（＝焼いた字が使い回せる）。
  ★**塗りも物理も元から問題ではない**（測って確かめた）。
- ★★★**掴んでいる図形の代理の体**（`Pile.syncProxy`／`pileWorld.ghostBodyOf`）…
  **絵は体そのもの**／**体は絵の中心**／**`piecesRef` に混ぜない**／**`clearOverlap` の
  前に消す**／**停止条件に入れる**／**締切 `HANDOFF_MS`**。★★**山の倍率は中身が
  変わっても決め直さない**（`hold`）。
- ★★★**セーフエリアは全画面で `SPACE.lg`、左右対称**（`lib/pileBox.ts`）／**器が
  変わったときの4つ**（`FLOOR_CHECK_MS`／`refitPile`／`sink`／`isLost`→`respawn`）／
  **器は `.bleed-x-b`**／**ResizeObserver は1つ**。
- ★★★**当たり判定の多角形は「軸の本数 × 頂点数」**（`PHYS_VERTS` 12・等角・偶数。
  **光線は正規化した空間で飛ばす**）／**投入の直前に `clearOverlap`**／**質量は全部
  同じ密度に**／**`fromVertices` へ渡す前に `atan2` で並べる**／**眠りは `GravityTab`
  と同じ**（`dirtyRef`/`dragRef` を立てる所では必ず `wake()`）／**タスクの形は
  `stackOutline`・段の数は `rowsOf(title)`・`waist` が「ピル ⇄ 図形」**／
  **塗り／輪郭は `paintShape` と `isDated`**（`pilePaint.ts` にも同じ分岐。**両方直す**）。
- ★**カセット `lib/cassette.ts`**／**芯 `lib/reelHub.ts`**／**札の形 `lib/cardShape.ts`**／
  **文字の板 `lib/wordPlate.ts`**（★ホームの日付・曜日は**黒いピルに白い字**）／
  **帯の「今日行ける」 `lib/todayPick.ts`**／★★★**書体は3つ**／★**色は `PALETTE`
  （6行）だけ**（面から導く `bodyInkOn`・`redOn`・`inkVarsOn`・`mixHex`）／
  ★規約13本 `design.md`／目盛り `lib/tokens.ts`／動き `lib/motion.ts`。
- 検証 … ★★★**WebKit がこの環境で動く**（`executablePath` 無し。**Chromium だけ**
  `/opt/pw-browsers/chromium`。**CPU を遅くできるのは Chromium だけ**＝速さの測定は
  そちら）／★★★**種に `keeps: []` を入れない**（`migrate` が `items` を丸ごと捨てる。
  `wishes: []` は必要）／★★**山の測定は 8 秒待つ**／★★**帯を出す種には「日付の無い
  タスク」が要る**／★★**ピルを掴む試験は「画面の中に居る」ものを選ぶ**／
  ★★★**帯の飛びは「画面に見えているピル」だけで測る**（2周目の控えは画面の外に
  居るので、数えると 152px の偽陽性が出る）／★★**WebKit は続けて何回も起動すると
  落ちる**（1プロセス1ケースに割る）／★★★**速さは `Tracing`（`devtools.timeline`）で
  `Layout`/`FunctionCall`/`EvaluateScript` に割り、`rAF` と `setTimeout` を包んで
  呼び手ごとに測る**（プロファイラの `(program)` だけでは何も分からない）。
