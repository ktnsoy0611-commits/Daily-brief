# いまどこにいるか（2026-09-13／ホームを作っている最中）

**ホームが動いている**（帯＋山＋日付の板）。★第94〜99巡に **JOURNAL（録音画面）・EXPLORE の札・
TASK の図形**を作り直した。
★★★**Explore の刷新（券・改札鋏）は保留**（`DEV` タブごと最後に処分）。
★★★**いま試している大きな変更は1つ**（実機で見て決めてもらう段階）… **アプリごとの
アクセント配色**（`lib/appAccent.ts` の `ACCENT_TEST`。**`false` の1行で元へ戻る**）。
仕様の正 … `docs/home-spec.md`（ホーム）／`docs/project_knowledge.md` §3-h／
`design.md`（UI の規約と機械チェック13本）。

---

## 直近完了

### ★★★第99巡 ── カセットに角ボタン／タスクを「ピルの積み」へ／帯に「今日行ける」

**① カセット**（ユーザー指定）… **角丸に戻した**（0 → 2.6。第98巡を撤回）／
**左上に四角い突起2つ**（`w = D`・`h = D/2`・隔 `D/3`、組の中心を左のリールの真上）。
★★**突起は本体の外へ出る**ので ―― 録音画面の高さの見張りを**本体 → 外接箱**へ
（`CASSETTE_BOX_H_PER_H`。そのままだと題に掛かる）／山の焼き箱を**四方に突起のぶん
広げる**（`BAKE_PAD` の 1px では**突起の頭が切れていた**）。

**② タスクの図形を「ピルの積み」へ**（ユーザー指定「行数によって段の数が変わる。最大3段」）
- 参照の実測 … 399 × 392 に3段・1段 131（隙間なく接する）・くびれ/最大 **0.717**。
  → **幅の等しいピル（角丸＝段の高さ/2）を縦に積んだ union**（`stackOutline`）。
- ★★★**「辺の数（1〜4）＝埋まり具合」はやめた**（ユーザー確定）。円・半円・三角・
  四角の語彙ごと削除。**復活させない。** 図形が言うのは**塗り／輪郭**と**大きさ**だけ。
- ★★★**段の数は `rowsOf(title)`（純粋な関数）から引く。行数の実測から引かない** ――
  実測だと ① ALIGN と山で**同じタスクの形が変わる** ② 書体が遅れて届いた瞬間に
  **絵だけ段数が変わり、物理の当たり判定とずれる**（物体は1度しか作らない）。
- ★★**1段の比は「その行が何文字を抱えるか」で伸びる**（下限 3・1字 0.62）。
  無いと**長い題 × 低い重要度**で字が入らず**文字が消える**（26字の題で実際に消えた）。
- ★★**ホームの山も同じ形**（角丸の四角を別に描いていたのをやめた）。
- ★くびれは物理では**凸包に潰れる**（`poly-decomp` が無い）。**それでよい。**

**③ 帯の「今日行くのがおすすめ」**（`lib/todayPick.ts` を新設）
- ★★★**AI を使っていない。使えなかった** ―― `Item` に営業時間も `start` も
  `venue` も**1つも無い**（`start`/`venue` は `GeneratedCard` へ移る所で捨てている）。
  モデルに聞いても材料が無いし、効くのは**これから生成される分だけ**。
  → **`kind` ごとの時間帯の表**（＝近似）。**いま在るストックに今日から効く。**
- 展覧会 10–17（★ユーザーの例）／場所 9–18／体験 10–18／食 11–14 と 17–21／
  ライブ 18–22／映画 12–21／本・音楽・記事・モノは**出さない**。
- ★**今日もう閉まったものは出さない**（23時なら 0 件。嘘を出さない）。
- ★**`offer` の後ろに積む** ―― 「その日の分を読み終わっても」がそのまま順番になる。

**検証（件数つき）** … `stackOutline` の外接箱が ±0.5（**6/6 件**）／題6本の段数が
1/1/2/2/3/3・60字でも**3段で止まる**／GRAVITY で6つとも**文字が出る**（塗り／輪郭も
維持）／山とタブのアイコンに**突起が出る**（切れない）／帯は 10時 6件・14時 6件・
20時 3件（**展覧会が出ない**）・**23時 0件**／未読4件なら today 2件・1件なら 5件／
機械チェック13本 **0件**・`tsc` 0・`eslint` 0・`build` 成功。

### ★★★第98巡 ── 形を「円をつないだ形」へ／カセットの段／札の器と下の列

★寸法の一部は第99巡に上書きされた。**残る要点だけ**:

- ★★★**参照の形は全部「円をつないだ形」**（半径 profile を実測）… スカラップ四角
  （四角の周に16個・谷÷峰 0.743）／ロゼット（円の周に12個・0.919）／
  クアトレフォイル（四隅に4個・0.717）／六角形だけ多角形。
  ★★★**正弦波（超楕円 × cos Nθ）で円に似せない** ―― 角で振幅が √2 倍に伸び、
  山が辺に揃わない。`polar` と `superR` は削除済み。**復活させない。**
  ★半径と個数は**「谷÷峰」の実測から解く**（目で振らない）。
- ★★★**バーの幅はキーの数から出る** … `barW = 3(D − 2·lip) + 4·lip`、`lip = D/8`
  なら **2.75 D**。段が痩せた（本体幅の 38.3%）ので、録音画面は**高さの見張りが勝ち**、
  本体が題のすぐ下から始まり、段が画面の端まで届かなくなった。
- 札 … 幅 **358**（タブバー・山と同じ内寸）・ベゼル **`SPACE.xl`**・器 **310**／
  **題2行・本文2行で頭打ち**（題を野放しにすると本文を押し出す）／
  **下の列は 44px の円3つ**（旗＝輪郭／記事を読む・出典＝塗り）を左へ寄せる。
  ★生成の側でも題を刈る（`SYSTEM_EXTRACT` に全角30字）。**器と生成の両方要る。**

### ★★★第96〜97巡 ── 比を正す／形を器いっぱいに・キーをバーの中心線へ

★寸法は第98巡に上書きされた。**残る教訓だけ**:

- ★★★**`objectBoundingBox` は 0〜1 のパスを器の寸法へそのまま掛ける** ――
  ①**器が正方形でないと形が潰れる** ②**外接箱が 0〜1 でないと形ごとにベゼルが
  変わる**（実測 58.9／35.9／はみ出して切れる／32）。→ `fitBox`。
- ★★★**札の比は `ITEM_CARD_ASPECT` から来ていない**。**`BRIEF_CARD_ASPECT`** が正。
- ★★★**録音画面は「アイコンを拡大して画面に嵌めた絵」** ―― 寸法は全部
  `lib/cassette.ts` の比から。**画面側に数を1つも置かない。**
  ★`DIAL_RATIO`/`DIAL_CX`/`DIAL_WAIST`・`KEY_LABEL_H` は消えた。**復活させない。**
- ★★★**コメントと値が食い違う定数を作らない** ―― `KEY_LABEL_H = 12`（実際は 7）の
  一行で、キーの列が 5px 上へずれていた。
- ★★★**円の光学的な中心を中心線へ置く**（縦に flex で積まない）。積むと誤差が足し算。
- ★★**山に落ちる5種のうちジャンルを持てるのは提案だけ**／**物理は凸の12角形**
  （`poly-decomp` が無い）／**形の正は「点の列」**（SVG も canvas もそこから導く）。

### ★★★第93〜95巡までの要点

- ★★★**実機のバグの正体 ＝ `mask-size: 100% 100%` が効かず、SVG が固有の 1:1 の比の
  まま置かれていた**（書き漏らしではない）。→ **仕組みごと替えた** …
  `clip-path` ＋ `objectBoundingBox`。★**id は札ごとに `useId()`**。
  ★★**Playwright の WebKit でも再現しなかった** ―― 実機でしか証明できない。
- ★★**形は参照画像に在るものだけ**。割り当ては `PUNCH_BY_DOMAIN` から導く。
- ★★★**切るのは写真だけ。札には掛けない**（掛けると `box-shadow` が消え、黄と
  クリームの比 1.27 では縁が1本も出ない）。
- **`lib/cassette.ts`** … 同じ形を**3つの技術**が描く（タブの SVG／山の canvas／
  録音画面の DOM）。★**タブバーの中だけは1色**（地が選択状態で変わる）。
- ★★★**タグを全廃**（第93巡）。**タスクの性質は「日付があるか／ないか」だけ**
  （合図は `isDated`）。★★**`paintKey` に日付の有無と地の色を入れること**。
  ★★**書体を分類の道具にしない**（`SHAPE_FACE` の1つ）。

### ★★★第89〜92巡までの要点

**山の作り替え**（第92巡）… ★★★**ぼやけ ＝ 同じ器に ResizeObserver が2つ**／
★★★**下が空く ＝ 床がタブバーのぶん二重引き** → `.bleed-x-b`／
★★★**「少し変えるたび不安定」＝ 中身が1文字変わると世界ごと作り直し** →
**effect を2本に割った**／**紙の目（`paperize`）を全廃**。
**`lib/wordPlate.ts`**（文字の板。★DOM で組み直さない）／**山が「塔」だった正体**／
**刻みは固定 1000/60**／**焼き込まれた色を信じない**／**タブバーの高さは
`navHeightPx()`**（実機 132px／Chromium 81px）。詳しくは `docs/project_knowledge.md`。

---

## 次の一手

1. ★★★**実機で見てもらう** … ①**タスクの「ピルの積み」**（GRAVITY・DRIFT・
   ALIGN・TIMELINE・入力画面・ホームの山。★**段の数は題の文字数**なので、
   刻み（≤5 →1／≤12 →2）が合わなければ `rowsOf` のはしごを振る）
   ②**カセットの突起**（タブのアイコン・山・録音画面の3か所）
   ③**帯の「今日行ける」**（★**未読の提案が残っているうちは出ない**。時間帯が
   合わなければ `lib/todayPick.ts` の表を振る）
   ④**録音画面のリール**（片側 243 はみ出す。嫌なら本体に頭打ちを足す）
   ⑤**アクセント配色を採るか捨てるか**（捨てるなら `ACCENT_TEST` を `false`）。
   ★★**札の中の「文の下から操作の列までの隙間」は手つかず**（本文が短い札で空く）。
   ★★**題の刈り込みは次の夜間生成から効く**（既にあるカードの題は長いまま）。
2. **引き下ろし**（★ホームの軸。まだ無い）… 帯のピルを掴んで引き下ろすと
   **7本の柱が地面から立ち上がり**（高さ＝その日の量）、離した日がその日付になる。
   外で離すと候補（ストック）へ。**ピルは落ちながら図形へ連続的に変わる**。
3. **持ち物** … 右上のアイコン＋数の印（警告）。押すと印のすぐ下に小さな一覧。
4. **フォローアップの夜間生成がまだ無い**（★プロンプトは先に文面で出して承認を得る）。
   ★「今日行くのがおすすめ」は第99巡に**ルールで**作った（`lib/todayPick.ts`）。
   `Item` に本物の営業時間が入ったら、表は**その既定値**へ格下げする。
5. ★ピルを押したときの詳細（まだ無い）。★山の図形はもう押せる（`Piece.nav`）。

## 未解決

- ★★★**提案の生成は止まったまま。** 15 は**表示だけ**の上限なので裏の未読 41 枚は
  減らず、夜間の生成は `POOL_CAP`(40) に掛かり続ける（切り替える場所は
  `app/api/cron/build-brief/route.ts` にコメント済み）。
- ★**帯の②（今日行くのがおすすめ）は生成器が無いので今は出ない。**
- ★★**輪郭の字は地の上で比 2.55**（本文の下限を割る。★目盛りの外・ユーザー確定）。
- ★★★**札の比は `ITEM_CARD_ASPECT` から来ていない。`BRIEF_CARD_ASPECT` が正。**
  あちらを変えると**ストック・ゴール・表紙・コーヴァーフロー**が動くのに、
  **札は 1px も動かない**（第96巡に踏みかけた罠）。
- ★★**録音画面の本体は左右へはみ出したまま**（片側 **243px**。第97巡は 163）
  ＝**仕様**（ユーザー確定）。★第98巡に本体が高さいっぱいへ伸びて増えた。
- ★★**黄の札とクリームの地の比は 1.27**。縁は**影**が持つので、**札を切り抜くと
  縁が消える**（切っていない）。
- ★★★**札の実機のバグは「直したはず」の段階**。Chromium と Playwright の WebKit
  では**もともと再現していなかった**ので、**直った証明は実機でしか取れない**。
- ★★**券の大きな英語が読めなくなる恐れ**（`DEV` と `/dev/explore` だけ。4タブに影響なし）。
- ★★**地と紙の差が比 1.01**／`SectionLabel` は明るい地で 3.2 ＝ AA 割れ。**既知・未対応。**
- ★★**札の中の「文の下から操作の列までの隙間」**（本文が短い札で空く）。**未着手。**
- ★★★**帯の「今日行ける」は営業時間を知らない**（`kind` ごとの表＝**近似**）。
  休館日も定休日も見ていない。`lib/todayPick.ts` の頭に理由と限界を書いた。

---

## 重要パス

- ホーム … `HomeTab.tsx`／帯 `Band.tsx` ＋ `lib/homeBand.ts`／山
  `components/home/Pile.tsx` ＋ `pileWorld.ts` ＋ `pilePaint.ts`／器 `lib/pileBox.ts`
  ／配線 `AppShell.tsx`／仕様 `docs/home-spec.md`
- ★★★**塗り／輪郭は `lib/solidPaint.ts` の `paintShape` と `isDated` が正**
  （山は `pilePaint.ts` にも同じ分岐。**片方を直したら両方**）。
- ★★★**カセットの寸法は `lib/cassette.ts` の1か所**（タブの SVG／山の canvas／
  録音画面の DOM）。目盛りだけ `lib/dial.ts`（`drawDial` は削除済み）。
- ★★★**札の形は `lib/cardShape.ts`（点の列が正）＋ `CardShapeDefs.tsx`。
  割り当ては `lib/ticket.ts` の `PUNCH_BY_DOMAIN` から導く**（対応表を2つ持たない）。
  ★**CSS のマスクを使わない。`clip-path` ＋ `objectBoundingBox`。器は必ず正方形。**
  ★★★**点の列は最後に外接箱を 0〜1 へ正規化**（`fitBox`）。canvas は `traceCardShape`。
- ★★★**山の器は `.bleed-x-b`**／**器を見る ResizeObserver は1つだけ**／
  **文字の板は `lib/wordPlate.ts` の1か所**（★DOM で組み直さない）。
- ★★**アクセントのテスト** … `lib/appAccent.ts`（`ACCENT_TEST` の1行で戻る）。
- ★**色そのものは `lib/constants.ts` の `PALETTE`（6行）だけ**／役との対応は
  `SCHEME` と `lib/palette.ts`／★**面から導く3つ** … `bodyInkOn`・`redOn`・
  `inkVarsOn`。**表を持たず、導かせる。**
- ★UI の規約と機械チェック13本 … `design.md`／目盛り `lib/tokens.ts`／動き
  `lib/motion.ts` ＋ `app/globals.css` の `:root`
- ★★★**タスクの形は `lib/solid.ts` の `stackOutline`（ピルの積み）が正**。
  **段の数は `lib/taskSize.ts` の `rowsOf(title)`**（純粋な関数。行数の実測から
  引かない）。絵も物理も山も**そこから引く**（`shapeRowsOf`）。
- ★★★**帯の「今日行ける」は `lib/todayPick.ts` の1か所**（`kind` ごとの時間帯＝**近似**）。
- タスク … `GravityTab.tsx`（★山の物理の出どころ。ホームと**対**）／カメラ
  `TaskSpace.tsx`／入口の輪 `CreateMenu.tsx`
- 券・鋏（★保留中）… `components/explore/samples/`／`Ticket.tsx`／`lib/nipperShape.ts`
  （生成物。手で直さない）／`docs/explore-redesign.md`
- ブリーフ … `lib/briefPipeline.ts`（`fetchSite`/`fetchDirect`/`jinaSlot`）／
  単体チェック `npx tsx tools/jina-check.mjs`
- ★★★**WebKit がこの環境で動く**… `playwright install webkit` ＋ `install-deps`。
  `executablePath` 無しに起動（**Chromium だけ** `/opt/pw-browsers/chromium`）。
  ★★**万能ではない** … 第95巡の札のバグは WebKit でも再現しなかった。
  **「Chromium で 0 件＝実機で直った」は今も言えない。**
- 検証 … 開発サーバーは `NODE_EXTRA_CA_CERTS` と `NODE_OPTIONS=--use-env-proxy` の
  **両方**が要る（片方だと next/font/google が落ちて 500）。**`localhost` で開く**。
  ★★**山の測定は 8 秒待つ**／**canvas の倍率は場所で違う**（山 3／他 2）／
  `migrate` の単体チェックは state に **`wishes: []` が要る**／
  `next build` は Google フォントの取得に**ときどき失敗する**（数回やり直す）。
