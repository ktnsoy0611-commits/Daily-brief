// ★★★**録音の円の「縁の目盛り」の寸法はここ1つ**（2026-09-12・第93巡）。
//
// ★カセットの本体・リール・帯の寸法は `lib/cassette.ts`。**こちらは目盛りだけ。**
// ★数は下の3つだけ。`components/VoiceStudio.tsx` の SVG が読む。

/** 目盛りの本数。★目盛りの外（部品の寸法）。 */
export const DIAL_TICKS = 5;

/** 寸法を持つ座標系の一辺（＝SVG の `viewBox` と同じ 100）。直径がこれに当たる。 */
export const DIAL_VIEW = 100;

/**
 * 縁の目盛り1本。**直径 100 に対する比**で持つ（円の大きさが変わっても形が保たれる）。
 * ★**角は丸めない**（短く太い線。`VoiceStudio` の元の指定）。
 * ★目盛りの外（部品の寸法）。
 */
export const DIAL_TICK = { w: 1.46, h: 4.5, inset: 0.8 } as const;

// ★★**canvas 版（`drawDial`）は第94巡に削除**。ホームの山が落とすのは円ではなく
//   **カセット**（`lib/cassette.ts`）になったので、呼び手が居なくなった。
//   ここに残っているのは**録音画面の SVG の目盛り**が読む3つの数だけ。
