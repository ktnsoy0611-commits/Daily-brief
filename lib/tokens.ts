// ★★★**寸法の語彙はここだけ**(2026-08-23・第33巡)。
// 動きの語彙が `lib/motion.ts` と `app/globals.css` の `:root` にあるのと同じ
// 考え方で、**余白・文字・角丸も名前で持つ**。
//
// なぜ要るか … 第33巡の実測で、fontSize が約25種類(8/8.5/9/9.5/10/10.5/11/
// 11.5/12/12.5/13/15/15.5/16/17/18/19/20/22/24/26…)、gap が 2〜12 の連番ほぼ
// 全部、borderRadius の角のある方が 2/6/8/9/10/12/14/16/18/20/22/26/28 と、
// **一点物の数字**で埋まっていた。画面ごとに手ざわりが違う直接の原因。
//
// ★**新しい数字を増やさないこと。** 足したくなったら、まず既存の段のどれかで
// 済まないかを疑う。段を増やすのは、増やさないと表現できない役が本当に
// 生まれたときだけ。

/**
 * 余白(padding / margin / gap)。**4の倍数**。
 * 唯一の例外は `hair`(2) — 罫の太さと、図形どうしを光学的に詰める場合のみ。
 */
export const SPACE = {
  hair: 2,
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
} as const;

/**
 * 文字の大きさ。**7段。この7つ以外を書かない。**
 *
 * ★`lead`(16) は iOS Safari の「フォーカス時に自動でズームする」閾値でもある。
 *   **入力欄(input / textarea)は必ず 16 以上**にすること(15 以下だと画面が
 *   勝手に拡大し、以後レイアウトが崩れたまま戻らない)。
 */
export const TYPE = {
  /** ★**小さな操作部品に焼き付いた**ラベル(NAME / TAG / 物理キーの名前)専用。
   *  文章には使わない。器の寸法が先に決まっていて、`micro` では**入り切らない**
   *  場所だけ。ここを使いたくなったら、まず器が小さすぎないかを疑うこと。 */
  nano: 7,
  /** 極小ラベル。大文字＋トラッキングで使う。 */
  micro: 9,
  /** 補助テキスト・キャプション。 */
  small: 11,
  /** 本文。 */
  body: 13,
  /** 少し目立たせる文・入力欄。 */
  lead: 16,
  /** 見出し。 */
  head: 20,
  /** 大見出し・数字の主役。 */
  display: 26,
} as const;

/**
 * 行間。**「文字サイズ」ではなく「単一行か、読ませる文か」で決まる**(第65巡)。
 * 実測で 16px の行間が 8 種類に割れていたのは、16px が**ボタンのラベル**と
 * **段落**の両方に使われていて、行間をサイズで決めようとしたから。
 */
export const LEAD = {
  /** 単一行のラベル・数字・canvas の板。 */
  flat: 1.0,
  /** 見出し(head / display)・2行までの題。 */
  snug: 1.3,
  /** 和文の本文(複数行)。 */
  body: 1.7,
} as const;

/**
 * トラッキング(字間)。★**和文には `normal` 以外を当てない** —
 * 和文は字面が正方形なので、欧文用の字間を足すと**字間だけが抜けて見える**。
 *
 * ★★`caps` / `wide` を `textAlign: "center"` と併用するときは、同じ量の
 *   **負の右マージン**を必ず添えること(字間は最後の字の右にも付くため、
 *   そのぶん右へずれて見える)。
 */
export const TRACK = {
  /** display(26) 以上・大きな数字。 */
  tight: "-0.02em",
  /** ★和文を含むすべての文。 */
  normal: "0",
  /** 欧文の大文字ラベル(micro / small)。 */
  caps: "0.16em",
  /** 層名・ロゴ的な少数の大文字。 */
  wide: "0.24em",
} as const;

/**
 * 太さ。★**`fontSize` と `fontWeight` は必ず同時に書く** —
 * 片方だけ書くと残りが既定の 400 に落ちて、意図せず弱く見える。
 */
export const WEIGHT = {
  /** 和文の本文。 */
  text: 400,
  /** 見出し・ラベル・ボタン。 */
  bold: 700,
  /** 数字の主役・display。 */
  heavy: 800,
} as const;

/**
 * 角丸。`pill`(999) と `circle`("50%") は既に全体で統一されていたので現状維持。
 * 揺れていたのは「角のある方」だけなので、そこを4段に畳んだ。
 */
export const RADIUS = {
  sm: 4,
  md: 8,
  lg: 12,
  xl: 18,
  /** 下から出るシートの上端。 */
  sheet: 28,
  pill: 999,
  circle: "50%",
} as const;

// ---- 移行表(第33巡・機械的に当てた) ----------------------------------
// 文字 … 6/7/7.5→nano(7)(小さな部品のラベルのみ) ／ 8/8.5/9/9.5→micro(9) ／ 10/10.5/11/11.5→small(11) ／
//        12/12.5/13→body(13) ／ 15/15.5/16/17→lead(16) ／
//        18/19/20/22→head(20) ／ 24/26→display(26)
// 余白 … 2/3→xs(4)(罫は hair のまま) ／ 5/6/7/8→sm(8) ／ 9/10/11/12→md(12) ／
//        14/16→lg(16) ／ 20/22/24→xl(24) ／ 28以上→xxl(32)
// 角丸 … 2→sm(4) ／ 6/8/9→md(8) ／ 10/12→lg(12) ／ 14/16/18/20/22→xl(18) ／
//        26/28→sheet(28)
//
// ---- 目盛りの外に居てよいもの(理由付きの例外) --------------------------
// 1. `env(safe-area-inset-*)` を含む式 … `TAB_PAD_TOP` / `NAV_BOTTOM_GAP` /
//    `NAV_H` / `NAV_OFFSET`(すべて lib/constants.ts)。端末が決める値なので
//    こちらの目盛りには乗らない。
// 2. **図形の座標系** … `GeoType` / `SolidCanvas` / matter.js / Leaflet。
//    これは「UIの余白」ではなく絵の寸法。触らない。
// 3. 既存の部品寸法 … `TAB_MARK`(52) / `NAV_PILL_PAD`(6) / `HEADER_CHIP_SIZE`(40)。
//    互いに噛み合って高さが決まっている(ピル = 52 + 6*2 = 64)ので動かさない。
